---
title: Session — 2026-06-02 — Canonical routing live + build fix
owner: Royce Milmlow
last_updated: 2026-06-02
scope: Everything shipped this session
read_priority: reference
status: live
---

# Session 2026-06-02 — Canonical routing live + eq-shell build fix

**Context:** Continuation of the canonical wiring arc. This session confirmed the
tenant model, audited all app wiring, unblocked the canonical-api routing gap, and
fixed a broken eq-shell Netlify build.

---

## Tenant model confirmed

Royce confirmed the architecture. Documented in STATE.md + system/architecture.md +
system/infrastructure.md:

- `eq-canonical` (`jvknxcmbtrfnxfrwfimn`) = control layer (Cards config, tenant registry)
- `eq-canonical-internal` (`zaapmfdkgedqupfjtchl`) = EQ tenant Supabase
- `sks-canonical` (`ehowgjardagevnrluult`) = SKS tenant Supabase
- Pattern: `{tenant}-canonical`

---

## Canonical routing gap — found and fixed

**Problem:** `tenant_routing` table was missing from eq-canonical. All canonical
write-through from EQ Service and Quotes was silently swallowed by `TenantNotFoundError`.

**Fix chain:**
1. Created `tenants` + `tenant_routing` tables in eq-canonical (seeded from `organisations`)
2. Generated new 32-byte `TENANT_ROUTING_MASTER_KEY`, set in eq-shell Netlify
3. Built + ran `scripts/provision-sks-tenant.mjs` — AES-256-GCM encrypted service-role key,
   inserted sks → sks-canonical routing row
4. Deleted provision file after run
5. Triggered eq-shell redeploy via git push

**Result:** canonical-api can now route `X-Tenant: sks` PUT requests to sks-canonical.
`syncCustomer`, `syncSite`, `syncAsset`, `syncDefect`, `emitEvent` from EQ Service,
and `write_event` from EQ Quotes, are live end-to-end.

---

## sks-canonical discovered fully live

Attempted to apply `app_data` schema — failed with `relation "customers" already exists`.
sks-canonical already has 60 applied migrations and live data:

| Table | Rows |
|-------|------|
| customers | 389 |
| sites | 591 |
| assets | 4,808 |
| asset_test_results | 713 |
| canonical_events | 21 |
| sks_quotes_* (public) | 37 documents, 520 customers |

No schema migration needed.

---

## eq-shell build fix

Build was failing in `@eq/confirm-ui` DTS compilation. Two related TS errors:

1. `"cap_exceeded"` missing from `ValidationError` union in `eq-validation/validate.ts`
   — added `| { kind: 'cap_exceeded'; field: string; reason: string }`
   → commit `ecd75c2`

2. Adding `cap_exceeded` made the switch exhaustive; TypeScript then narrowed `e` to
   `never` in the `default:` branch, making `e.kind` fail. Cast to `{ kind: string }`
   in the default branch.
   → commit `163d799`

Build now passing. Production deploy live at `core.eq.solutions`.

---

## Wiring audits completed

### EQ Quotes (eq-quotes-sks)
- All canonical secrets confirmed deployed ✅
- `canonical.py` wired to `core.eq.solutions` canonical API
- Fly app name `eq-quotes-sks` correct (follows `{tenant}` suffix)
- Stale `SUPABASE_URL` env var removed from `fly.toml`

### EQ Service
- `canonical-sync.ts` wired into all key server actions ✅
- `CANONICAL_API_KEY_SERVICE` confirmed in both eq-solves-service + eq-shell Netlify
- `GET /api/admin/export` bulk snapshot endpoint fully wired + admin-gated
- 4 export stubs remain (contact, attachment, maintenance_plan, maintenance_plan_item) — not blocking

---

## Context files updated

- `STATE.md` — 3 new post-sprint blocks added
- `system/architecture.md` — tenant model section rewritten
- `system/infrastructure.md` — Supabase table corrected (removed stale entries)
- `eq-solves-field/CLAUDE.md` — committed to main with eq-context substrate URL
- `eq-quotes/fly.toml` — stale SUPABASE_URL env var removed
- Memory files — `project_eq_canonical_internal.md`, `project_eq_quotes_structure.md` updated

---

## Royce-actions outstanding

| # | Action | Status |
|---|--------|--------|
| 1 | `TENANT_ORG_UUID` Netlify env var for eq-solves-field EQ site (blocks U6 PIN feature) | ⏳ pending |
| 2 | Revoke old `gho_...` PAT at github.com/settings/tokens | ⏳ pending |
| 3 | Drift CI secrets in eq-shell GitHub repo settings | ⏳ pending |
| 4 | HaveIBeenPwned toggle in eq-canonical Supabase Auth settings | ⏳ pending |
| 5 | Merge eq-roles PR `bold-boyd-e6afae` → publish v1.3.0 | ⏳ pending |

---

## Session commits

| Repo | Commit | What |
|------|--------|------|
| eq-shell | `8707693` | scripts/provision-sks-tenant.mjs |
| eq-shell | `ecd75c2` | fix: add cap_exceeded to ValidationError union |
| eq-shell | `163d799` | fix: cast e in default branch of errorSummary |
| eq-context | multiple | STATE.md + architecture.md + infrastructure.md |
| eq-field | `6c2b34b` | CLAUDE.md committed to main |
| eq-quotes | committed | fly.toml stale env var removed |
