# Session 2026-07-22 — SKS Safety: Incidents/Near Miss tab + Prestart copy-from-last, EQ Field regression found & fixed

## Built
- sks-nsw-labour: new Incidents/Near Miss tab in Safety module — full CRUD, offline queue, per-person signatures, photos, Word export, Records integration, manager email alert on High-severity/Incident submissions.
- sks-nsw-labour: new `incidents` table live on Supabase (`nspbmirochztcjijmcrx`), RLS + indexes + `updated_at` trigger mirroring `prestarts`/`toolbox_talks` exactly.
- sks-nsw-labour: Prestart "copy from last visit" inline prompt (site + 7-day submitted-status match) + manual "Duplicate" button on any saved prestart.
- sks-nsw-labour: Incidents wired into the manager-only Safety Report dashboard (stat card, by-person table, 3-way site-coverage split).
- sks-nsw-labour: fixed a mobile CSS gap — Incidents modal was missing from `_injectSafetyStyle()`'s mobile rules (full-screen modal, anti-zoom inputs, single-column layout).
- sks-nsw-labour PR #69 merged (v3.10.99 → v3.10.102), Netlify auto-deployed.
- Investigated Royce's "Word doc isn't branded" report: generated both Prestart and Incident docs from identical seed data, diffed the zip contents byte-for-byte — logo, header, and title styling are identical. No code bug found; likely a stale service-worker cache on Royce's device. Asked him to hard-refresh or send the actual file.
- Spawned a background task (chip → separate session, `local_218d36af`) to investigate EQ Field parity, which found and fixed a real regression: PR #516 (the day before) retired EQ Field's old duplicate prestart form and silently dropped its "copy from last" feature (`_psCopyLast`/`_psUseLastForSite`, originally PR #396) without porting it to the new canonical `site-reports.js`. That session rebuilt it there (`copyLastPrestart()`/`useLastPrestartForSite()`) and got Royce's go directly — **eq-field PR [#529](https://github.com/eq-solutions/eq-field/pull/529) MERGED** (`13d1732`, v3.5.350), live.

## Decided
- Royce: build both SKS features (Incidents tab, then Prestart copy/duplicate) before committing.
- Royce: commit + open a PR once both were done and QA'd.
- Royce: merge PR #69 once CI green — done.

## Deferred
- ~~EQ Field has no Incidents/Near-Miss module~~ — Royce confirmed he wants it; chip session `task_bac795b3` kicked off, running.
- ~~Royce checking whether the Word-doc branding complaint was stale-cache~~ — confirmed stale cache, closed.

## Notes
- Corrected a stale memory (`project-sks-eqfield-sync`): EQ Field's own CLAUDE.md now states sks-nsw-labour is a standalone repo not modified from eq-field, and discourages porting eq-field→sks — this contradicts the older "keep both synced" instruction the memory recorded from 2026-05-31. Flagged as a live conflict for Royce to resolve, not silently picked a side.
- Also recorded (same memory): EQ Field's prestart form moved out of `scripts/safety.js` into `scripts/site-reports.js` as of PR #516 (2026-07-21) — safety.js there now only owns Toolbox/Site Audits/Records.
- Sandboxed browser in this session had no network egress to Supabase at all — DB-layer testing for the new `incidents` table was done via direct SQL insert/update/delete instead of through the app UI; UI/logic testing used seeded in-memory data.
- **Closing addendum:** checked on the EQ Field chip session before pushing it myself — it had already gotten Royce's go directly and shipped ([PR #529](https://github.com/eq-solutions/eq-field/pull/529) merged, live). No action needed; substrate corrected in place rather than duplicating a second push attempt.
- **Second closing addendum — leave-approval investigation:** Royce reported 3 of Cameron Tregoning's leave requests looked approved in Resend but stuck Pending in the app. Checked live `leave_requests` rows directly — genuinely still Pending, `responded_by`/`responded_at` both null, not a display/cache issue. Root cause: the magic-link approve flow is deliberately two clicks (GET renders a confirm page, only POST from the "Confirm — Approve leave" button applies it — built that way in v3.10.42 so email security scanners that auto-follow links via GET can't silently auto-approve). Confirmed the mechanism itself works fine (May's requests completed cleanly the same way) and ruled out an approver-email/org mismatch. Most likely: Ian clicked the email link but not the second confirm button. No code changed — told Royce to have Ian finish the confirm step or approve in-app directly.
- **Third closing addendum — EQ Field Incidents build kicked off.** Royce: "eq field should get incident reports." Spawned chip session `task_bac795b3` (build Incidents/Near-Miss module in eq-field, mirroring the SKS one just shipped but adapted to eq-field's own architecture — briefed to verify live schema first since prestart's file ownership already drifted once this week). Royce started it in a separate session; running independently.
- **Fourth closing addendum — leave-approval email fixed, not just diagnosed.** Royce: "how do we tidy that up? ian would think its approved wouldnt he?" Found the actual culprit in `scripts/leave.js`: the approval email literally said "One-click action" over a plain "✓ Approve" button — a direct promise the flow doesn't keep, since the real approval needs a second click on a separate confirm page. Relabelled the email CTA ("Review & Approve"/"Review & Reject" + a note that it opens a confirmation page) and the confirm page itself in `netlify/functions/approve-leave.js` (headline "Approve leave" → "Confirm approval", added a loud "⚠ Not yet approved" warning banner above the confirm button, reusing the page's existing `.warn` style). Verified by actually rendering the confirm-page HTML with mocked Supabase responses + a real signed token before shipping (invoking the handler directly in Node, not just reading the code). Copy/UX only — GET/POST split and token verification untouched. sks-nsw-labour PR [#70](https://github.com/eq-solutions/sks-nsw-labour/pull/70) MERGED (v3.10.102 → v3.10.103) on Royce's "merge it" go, Netlify auto-deployed. The 3 already-stuck requests still need Ian to manually finish them — this only prevents recurrence.
- **Shared-checkout hazard hit again mid-close:** a concurrent session's checkout of this same `eq-context` working directory raced my session-close commit onto its own feature branch instead of `main` (known recurring issue, see worktree-registry lessons). Recovered cleanly via a temporary detached worktree off `origin/main`, cherry-picked the commit there (one real conflict — a duplicate `eq/pending.md` entry the eq-field chip session had already written directly to main, resolved by keeping main's copy), pushed from the worktree, then removed it. Left the stray commit sitting harmlessly mid-history on the other session's branch rather than rewriting history out from under a live session.

---

# Session 2026-07-22 — EQ Field: closed the silent 1000-row read-truncation family (people / timesheets+roster / leave)

## Built
- **eq-field PR #526 (v3.5.348, MERGED, live)** — `sbFetchAllWide()` in `scripts/supabase.js` for the canonical wide surfaces. Pages the NORMALIZED domain by normalized PK with the response transform suppressed (`_rawCanonical`, new 6th internal param on `sbFetch`), then groups to wide ONCE over the complete set. Applied to `timesheets` + `schedule` at 4 call sites: `timesheets.js loadTimesheets`, `index.html loadFromSupabase`, `_loadWeeks`, `_loadFullDataForExport`. Pages fetched in waves of 4 (~81 pages at 1500 staff = ~14s sequential vs ~3s). `sbFetchAll` itself left UNCHANGED — 18 call sites depend on it.
- **eq-field PR #528 (v3.5.351, MERGED, live)** — `leave.js loadLeaveRequests` now uses `sbFetchAllWide` and sorts client-side via a new `_leaveByNewestFirst` (ties → id desc, undated → last), replacing the DB's non-unique `order=created_at.desc`.
- `tests/timesheets-paging.test.js` (26 assertions) + `tests/leave-paging.test.js` (15 assertions). Suite now 15 files.

## Decided
- Royce: ship the paging correctness fix first; brief the crew model separately (did not fold crew-scoping into the bug fix).
- Royce: crew scope = "only their crew, but can filter based on predefined teams / search / usual filtering features."
- Royce: merge #526, then merge #528. Both merged on explicit go, both live-verified.
- Included `schedule` alongside `timesheets` in #526 rather than fixing timesheets alone — identical defect, identical adapter, adjacent lines.
- Left `leave_requests`' call site out of #526 deliberately (different volume shape + needed the ordering decision), closed it in #528.

## Deferred
- **Crew-scoping — blocked on Royce.** `app_data.teams` has NO owner/supervisor column (verified live: id, tenant_id, nspbmir_id, name, color, created_at, org_id). `team_members` says who's IN a team, not who runs it; `schedule_entries.supervisor_id` is per-shift, not standing. Needs a schema decision (add owner vs derive from roster) AND a visibility decision (managers-all vs supervisors-crew). Chip `task_6f433960`, session running.
- 1,500-readiness is still NOT demonstrated. Open item is Netlify Functions concurrency under a synchronized login burst — not row counts.

## Notes
- **PREMISE CORRECTION, load-bearing:** the canonical adapters are NOT default-off. `TIMESHEETS_/ROSTER_/LEAVE_CANONICAL_TENANTS` all contain `'sks'`, plus a DB-ref fallback matching ehow. **SKS — the only live tenant — runs the canonical normalized path.** The "DEFAULT OFF" comments in the adapter files describe the original state, not the current one. Three separate task briefs this session asserted "default OFF"; all three were wrong. Banked to memory.
- **`_loadFullDataForExport` was already broken on SKS today**, at 80 staff — `sbFetchAll`'s default `order=id` 400s against `app_data.timesheets`, which keys on `timesheet_id` and has no `id`. Not a 1500-scale hypothetical.
- **Why naive paging would have been worse than the bug:** timesheets/roster `toWideList` GROUPS (many per-day rows → one week row) and runs PER RESPONSE, so concatenating pages emits a duplicate partial wide row for every person/week straddling a boundary. Leave's `toWideList` is a 1:1 `.map()` — no grouping, so no boundary hazard there, only the ordering one. `tests/leave-paging.test.js` PINS the 1:1 property rather than asserting a corruption that doesn't exist.
- **Live data shapes verified before choosing order keys** (not inferred): `app_data.timesheets`→`timesheet_id`, `schedule_entries`→`schedule_id`, `leave_requests`→`leave_request_id`; none has an `id` column. Legacy wide twins have `id`. `app_data.leave_requests`: 32 rows, only **3 distinct `created_at`** values.
- **TEST HARNESS TRAP** (documented inline in both test files): `sbFetch` resyncs `window.SB_URL` to its own lexical `SB_URL` on every call (v3.5.287), so a test that overrides the URL then fetches is silently reverted — an early draft measured the canonical path while claiming to test the legacy one. Ask `_canonReadPlan()` directly instead.
- **Version-stamp collisions happened THREE times today** (#527 took 3.5.349, #529 took 3.5.350; #528 re-stamped 3.5.349→350→351 across two rebases). eq-field version stamps are same-line conflict magnets with concurrent agents — the worktree registry already carried this lesson and it recurred anyway.
- **Two merged commits on `main` have misleading squash titles**: #527 reads "v3.5.347" (actual stamps 3.5.349) and #528 reads "v3.5.349" (actual stamps 3.5.351) — `gh pr merge --squash` uses the original commit subject, not the retitled PR. **Main is NOT regressed** (file stamps are correct and monotonic; prod live at 3.5.351) but `git log` understates both. If retitling a PR after a rebase re-stamp, also amend the commit subject.
- Royce asked for a steelman of the whole program. Honest verdict recorded: the work was right, the "national-scale readiness" framing oversold it. One live bug + two latent ones with a silent-data-loss failure mode on payroll. Row counts were never the binding constraint for 1,500 — and crew-scoping would have masked the timesheet-grid case anyway (though not export, and not any future tenant-wide view).

---

# Session 2026-07-22 — EQ Field: prestart copy-from-last shipped (follow-through on the chip session above)

## Built
- Picked up the chip-spawned session's finished-but-uncommitted work (branch `claude/prestart-copy-last`, `copyLastPrestart()`/`useLastPrestartForSite()` in `scripts/site-reports.js`, v3.5.350) and took it the rest of the way: pushed, opened eq-field PR [#529](https://github.com/eq-solutions/eq-field/pull/529), Royce said "merge," squash-merged to `main` (`13d1732`), branch deleted.
- Confirmed live via Netlify MCP: deploy `6a605929` on field.eq.solutions, state `ready`, secret scan clean, 7 functions deployed, `commit_ref` matches the merge commit.

## Decided
- Royce: merge — explicit go, taken as authorising the resulting Netlify auto-deploy too (deploy-on-push-to-main is the documented, known behaviour for this repo, not a side effect Royce wouldn't have anticipated).

## Deferred
- **No real click-through test.** This session's sandbox has no live Supabase/Netlify-Functions backend to sign in against, so verification was `node --check` + `eslint` (0 errors) + a local static-file load with no console errors — not an actual "open a prestart at a site with history, click the button, watch it fill" pass. Worth Royce doing that once, live. Noted in `eq/pending.md`.

## Notes
- Concurrent session updated this file's first entry (line 11) to already reflect the merge — both entries now agree; `eq/pending.md` also corrected to merged+live.
- Version stamp v3.5.350 landed clean on first try (see the collision note in the session entry above re: PR #528 needing two re-stamps around this one) — no rebase needed on this branch.

---

# Session 2026-07-22 — the 6 "orphaned" shell_control.users rows on jvkn are DELETED accounts, not stubs — backfill would have been a privacy regression

## Built
- **eq-shell PR [#944](https://github.com/eq-solutions/eq-shell/pull/944) MERGED** (squash `c77587a`, Royce's "merge" go) → core.eq.solutions auto-deploying. Adds step 3 to `netlify/functions/retention-purge.ts`: delete the `shell_control.users` row for each id whose `auth.users` row it just removed.
- `scripts/cleanup-orphaned-shell-users.sql` — reviewed one-off for the 6 existing orphans. Ids hardcoded, snapshot first, 5 preconditions re-asserted at run time, verify-before-commit, `ROLLBACK` left in place. **NOT run.**
- Chip `task_feba4071` — guard or retire `backfill-auth-users.ts`.

## Decided
- Royce chose "fix the leak first, then clear" over a one-off DELETE, marking inactive, or recording only.
- Royce: merge — explicit go, taken as authorising the resulting Netlify auto-deploy (documented behaviour for this repo). The change is inert until `RETENTION_PURGE_LIVE=true` anyway.

## Deferred
- **The 6 existing orphans are still there.** The cleanup script needs Royce's hand on jvkn — control plane has no CI apply path. Nobody is blocked: none of the six can be signed into.
- **`backfill-auth-users.ts` still exists unguarded** (chip `task_feba4071`). Its stated purpose was closed by `2026_07_15a`/`15b`, both live with orphaned identities at 0, so retiring may beat guarding — needs Royce's permission to delete.

## Notes
- **The whole premise inverted on inspection, and the inverting evidence was `public.audit_log`.** The 6 rows read exactly like pending phone-only onboarding stubs — `email`, `phone` AND `name` all NULL, `pin_hash` NULL, role `employee`, `last_login_at` NULL, tenants SKS + Personal Wallet. But audit_log holds 52 rows across them and **every id's last event is `profile.deleted`**, preceded by `profile.created`/`profile.updated`/`licence.created`/`membership.invited`. Creating licences and inviting members needs a working Supabase JWT, so an `auth.users` row existed and was removed later.
- **TRAP, banked to memory: `last_login_at IS NULL` is not evidence of "never signed in"** — it's null on 60 of 72 `shell_control.users` rows; the column isn't maintained on the Cards/OTP path. It is the single field that made these look untouched, and it's what the task brief rested on.
- Corroboration, all live-verified on jvkn: `public.profiles` for all six **gone** (cascaded), `workers`/`licences`/`org_memberships` 0. `eq_cards_delete_account` checked via `pg_get_functiondef` **only soft-deletes** — never touches `auth.users` — so the removal was **manual admin cleanup**, not the app's delete path. One id (`a248470c`) was created 88 seconds after another (`2a467a34`) was deleted — same person re-signing-up, i.e. self-signup churn someone tidied by hand.
- **`backfill-auth-users.ts` was the wrong remedy twice over.** It calls `createUser({ id, email, email_confirm: true })` — `email` is NULL on all six, so GoTrue 422s and they'd land in `failures`. And if it HAD worked it would have re-minted login identities for people who exercised deletion. It also loops all 72 `shell_control.users`, not just the orphans. It has a phone/email collision guard but no deleted-account guard.
- **Root cause is structural, not historical:** `eq_finalize_deleted_accounts` runs `SET search_path = public` so it cannot see `shell_control`, and `shell_control.users` has no FK to `auth.users` so nothing cascades. Every finalised account leaves one behind. 1 profile is already pending erasure, so this was live, not theoretical.
- **Design rule in the fix: provenance, never a predicate.** Deliberately NOT `WHERE NOT EXISTS (SELECT 1 FROM auth.users …)`. The Shell mints its own session JWT and a Shell-only account signs in fine with no `auth.users` row (same fact recorded in the Emma Curth merge runbook, 2026-07-14), so a missing one is NOT evidence of deletion — a blanket sweep could delete live accounts. Step 3 is keyed off `authDeleted`, not `ready_user_ids`: a failed GoTrue delete must leave the Shell row so the next run retries the pair together.
- **FK shape on `shell_control.users`, verified live** — `user_tenant_memberships`, `user_security_groups`, `pin_reset_tokens.user_id`, `revoked_sessions.user_id` CASCADE. **Five referrers are ON DELETE NO ACTION and will abort a delete**: `user_invites.invited_by`, `cards_field_approvals.approved_by_user_id`, `pin_reset_tokens.created_by`, `phone_link_review.user_id`, `tenant_role_overrides.updated_by`. All five are 0 for these six. Hence per-id deletes, not one batch — one blocked account would roll back the lot; a `shell_error` is 207, not fatal, since the auth identity is already gone.
- `public.audit_log` preserved throughout — those 52 rows are the only remaining evidence these accounts existed, and `audit_log.user_id` has no FK to `shell_control.users`.
- Also corrected the file header's "as of 2026-07-21, zero profiles have ever used the delete-account flow" note — one profile is now pending erasure, so the "nothing can happen" safety assumption no longer holds.
- `gh pr merge --squash --delete-branch` failed with `fatal: 'main' is already used by worktree` — the **merge still succeeded server-side**, only gh's local checkout step failed. Verify with `gh pr view --json state` before assuming a merge didn't land.

---

# Session 2026-07-22 — capacity/scaling audit, then a real "highest value" miss corrected mid-flight, then a proper usage-checked pick

## Built
- **Capacity/scaling audit.** Royce asked, hypothetically, whether Field could scale to 300 or 1,200 people, then asked to test a shift-start burst specifically (everyone logging in around the same time — the realistic worst case for a trade workforce). A first attempt to spin up an isolated Supabase test branch for a safe live-fire test hit a real infra snag (migration replay failed, `PGRST002` schema-cache stuck, wrong schema anyway) — deleted, switched to a grounded calculation from already-verified live facts instead of firing a load test at production. A second attempt (a bounded, read-only burst against production's own metadata-only endpoint) was correctly blocked by the safety classifier as DoS-shaped; Royce chose the calculation route over unblocking it. Verdict: SKS's live DB allows 60 simultaneous connections, ~25 in normal use today — 300-1,200 people is fine under real login behaviour; the one actual risk is a future forced-simultaneity feature (synced push/SMS), not headcount. Logged to `eq/pending.md` + memory `project_field_scaling_ceiling.md`.
- **Extracted + tested `sks-pipeline-resource.js`'s allocation math** (PR #527, v3.5.349, merged, live) — picked as the top backlog item using code-risk signals alone (oversized, zero tests, high commit churn). Renumbered twice across rebases (two concurrent PRs landed first), caught a real CI lint error along the way.
- **Correction, prompted by Royce**: he flagged that the SKS Pipeline feature likely wasn't a priority. Checked live usage after the fact — `tender_enrichment` has 1 real row on `ehow`, `nominations` has 0. The commit churn that looked like real exposure was development iteration, not actual use. The code change itself is still fine (zero behaviour change, real tests, no new risk) but the "highest value" framing was wrong. Corrected in `eq/pending.md` and `eq/changelog/field.md`; lesson banked as memory `feedback_check_usage_before_prioritizing.md`.
- **Asked "what should we prioritize next" — applied the lesson before answering.** Surveyed the real open backlog rather than repeating stale framing. Found "Melbourne / second-tenant status" had been re-raised across 2+ audits as a real go/no-go decision on a prospective customer — checked live and it isn't one: `melbourne` has zero rows in canonical `organisations`, a synthetic test tenant (used for the 577-person roster-virtualization stress test), deleted 2026-06-28. Royce confirmed the confusion directly. Retired the framing so it stops resurfacing.
- **Extracted + CI-enforced `tender-parser.js`'s test suite** (PR #531, v3.5.352, merged, live) — this time picked by checking usage first: the Smartsheet import/diff engine has 300+ real imported tenders on both tenants and 7 real import sessions on SKS, unlike the enrichment step. Found the 44 assertions already existed as a browser-only manual harness nobody had ever actually run — ported verbatim into a CI-enforced Node test, zero new logic. Deliberately did not extract further into the Kanban/decision-queue rendering (same unverified-usage territory as the sks-pipeline-resource.js mistake).

## Decided
- Royce confirmed the flattened Safety nav (from the previous session) looks correct on the real live SKS sidebar, via screenshot.
- Live burst-testing production was correctly declined twice (a broken test-branch route, then a classifier block) — Royce chose the calculation-only route both times rather than pushing to unblock a live-fire test.
- Given a shortlist of his own previously-deferred items (EQ_SECRET_SALT rotation, supervisor onboarding walkthrough, continued file-testing backlog), Royce chose to keep testing large files — the other two remain open, not declined.

## Deferred
- `EQ_SECRET_SALT` rotation — real security debt since April, needs a deliberate low-traffic window, not picked this session.
- First-time-supervisor onboarding walkthrough — a real known gap, offered as an option, not picked this session.
- Netlify Functions concurrency under a synchronized burst — checked docs (no published hard ceiling on the current billing model, suggests elastic scaling), but never empirically confirmed; confirming for real would mean bursting the live PIN endpoint, which risks the real lockout mechanism — deliberately left unmeasured.
- Apprentices/Roster/Timesheets remain oversized by line count, but their risky logic was already extracted+tested in an earlier pass — what's left is readability, not live risk. Not touched further this session.

## Notes
- **The recurring lesson of the day**: code-risk signals (file size, zero tests, high commit churn) measure how risky a file is to touch, not whether the feature is actually used or valuable. Commit churn especially can just mean active *development*, not active *use*. Check real usage (a row-count query, in this case) before calling anything "highest value" — banked as memory `feedback_check_usage_before_prioritizing.md`, apply it before every future prioritization call.
- **Substrate itself can go stale the same way code does.** The "Melbourne second-tenant" item survived 2+ audits as a live decision because nobody re-verified it against the actual canonical DB — it was fully deleted, not paused. Docs drift; re-check load-bearing claims against live state before repeating them, same rule as code.
- **Version-number collisions are now routine, not exceptional**, in this repo's current pace — every PR this session collided with at least one concurrent PR's version number and needed a rebase + renumber. Branch fresh off `origin/main` immediately before starting any build task, not at the start of a long research/discussion segment, to minimize the window a collision can open in.
- Supabase branch creation for a safe load test costs ~1.3¢/hour — trivial, but still a real billed resource; deleted immediately once it turned out to be unusable (broken migration replay).

---

# Session 2026-07-22 — admin licence PDF upload fixed; found + closed a way any writer could brick a worker's whole wallet

## Built
- **Diagnosed why an admin-side licence PDF upload had "couldn't read the PDF."** eq-shell's `staff-licence-backfill.ts` was converting the PDF to a photo before saving (via `pdfjs-dist` + `@napi-rs/canvas`), and that conversion had failed on every single attempt since the feature shipped — 0 successful PDF backfills ever existed on the live DB. The real error was unobtainable (swallowed by a bare `console.error`, nothing in Sentry). Checked EQ Intake first, per Royce's specific ask, before building anything — confirmed its intended extraction endpoint was designed 2026-06-07 but never built, so nothing there to leverage.
- **Fixed by removing the conversion, not patching it.** PDFs now save as-is to `document_url`/`document_type='pdf'` — the same shape Cards already writes — instead of being rasterized to page-1-only JPEG. Both native deps (`pdfjs-dist`, `@napi-rs/canvas`) dropped from the eq-shell function bundle.
- **Found the reason the conversion existed at all: 3 employer-facing read paths were blind to `document_url`.** `staff-canonical-licences.ts`, the compliance ZIP export, and `worker-licences.ts` all selected only `photo_front_url`/`photo_back_url` — hiding 7 of 162 live licences (PDF-issued, no photo) from the Staff page and silently dropping them from compliance packs. All three now read/sign/export `document_url`; the compliance register gained a Document column and an honest "no evidence on file" count.
- **Added OCR auto-prefill to the admin Add Licence modal** (new `staff-licence-ocr.ts`, a thin proxy to eq-canonical's already-live `ocr-licence` — same engine Cards uses, reads a PDF natively via Claude's document block). Fills only blank fields via functional state updates so an async read can't clobber something typed mid-read; failure degrades silently to manual entry.
- **Found + fixed the wallet-breaking defect the PDF bug had been masking.** Royce hit the PDF error, worked around it with a photo instead — the row that saved had NULL `licence_number` and NULL `expiry_date` (blank number + "never expires" ticked). Cards' `Licence` model declares both non-nullable and parses the worker's ENTIRE licence list in one `map()`, so that NULL pair would throw and take down every card the worker owns, not just the new one — confirmed by reading `licence_card.dart`/`licences_list_notifier.dart`, not assumed. Repaired the one live row (Elliot Gross, `86e39bb4`) with Royce's explicit go — before/after captured, 0 unparseable rows left of 162.
- **Closed the gap structurally, not just for this one writer.** `public.licences` on jvkn has ≥3 writers (Cards app, eq-shell's service-role backfill, `credentials-canonical-sync`) and the shape rule lived only in a Flutter class. Wrote eq-cards migration `0101_licences_canonical_shape.sql`: `licence_number NOT NULL DEFAULT ''`, and generalised `normalise_lifetime_licence` (previously `white_card`-only) to pin the `9999-12-31` sentinel for every `never_expires` row. Enforcement is a trigger *coercion*, not a bare rejection — `eq_cards_upsert_my_licence` has two live overloads and the positional one can pass an explicit NULL, so rejecting would turn a silent data bug into a failed save mid-form.
- **Shipped both, in the deliberate order.** Applied the jvkn migration to production *before* merging the eq-shell PR — the trigger is inert to a correct writer but immediately makes the *already-deployed* buggy backfill safe, closing the exposure window rather than opening one (same ordering rule as the earlier crew-scoping change). eq-shell [#945](https://github.com/eq-solutions/eq-shell/pull/945) and eq-cards [#172](https://github.com/eq-solutions/eq-cards/pull/172), both merged on Royce's explicit "merge 945 and apply 0101," both live — smoke-verified against the real deployed site (new function 404→401 through the rollover) and the real migration ledger, not just trusted.
- Steelmanned Royce's "does this respect control-plane/tenant-plane independence" question before building the migration: confirmed every FK on jvkn is intra-plane (including `organisations.tenant_id → shell_control.tenants`, a schema on jvkn itself), so enforcing a `public.licences`-only invariant in jvkn *is* what plane independence means — the alternative (enforcing it in eq-shell's Lambda) is what actually couples the planes.

## Decided
- Royce: go ahead and repair Elliot Gross's live row.
- Royce: fix approach = store PDF as-is + fix the 3 readers + add OCR prefill (not the narrower "keep rasterizing, fix the Lambda" option, not the "reader fix only" option).
- Royce: add Sentry capture on the swallowed failure path.
- Royce: migration lands in jvkn (control plane), not a nullable relaxation of the Cards client model — confirmed after the steelman above.
- Royce: "go" — merge #945 and apply 0101, in that order.

## Deferred
- **Live security gap found while shipping this, unrelated to it:** `app_data.field_team_supervisors` on ehow (SKS) has RLS disabled, 0 policies, and full CRUD granted to `authenticated` — any signed-in user, any tenant, can read/rewrite supervisor assignments. Verified directly against `pg_class`/`relacl`, not inferred from the CI log. It's what turned eq-shell's `Schema drift + anon-grant + policy-lint` gate red on PR #945 (a PR with zero SQL in it) and will block every subsequent eq-shell PR until fixed. Chip raised (`task_87efbf2f`); Royce has already started it in a separate session. _(added 2026-07-22)_
- **Dangling cross-plane pointer, no enforcement possible:** `public.workers.staff_id` on jvkn stores ehow's `app_data.staff.staff_id` as a bare uuid (94/95 rows populated) with no FK — if an ehow staff row is hard-deleted or re-keyed, jvkn keeps a dangling pointer nothing detects. It's the join the whole backfill feature rides on. Chip raised (`task_bacc4fc5`); Royce has already started it. _(added 2026-07-22)_
- **Cosmetic: a few loading spinners reference an animation name that doesn't exist**, found while adding a correctly-named one to the Add Licence modal — `AttachmentList.tsx:186,281` use `spin` where only `eq-spin` is defined, so they sit frozen instead of spinning. Chip raised (`task_0b2b066d`); Royce has already started it. _(added 2026-07-22)_

## Notes
- **Process error, logged so it isn't repeated:** the live probe meant to prove the new trigger coerces correctly was described as "net-zero either way" and wasn't — it fed NULL into a row that already held a real value (`CGI00772665SEQ1`), and the trigger correctly coerced to `''` rather than restoring the original, blanking a real licence number for ~20 seconds on a soft-deleted (invisible) record before it was caught and restored. The stated plan — pick a row *already* holding the coerced value — was the right one and wasn't followed. Lesson: for a coercion-proof probe, the safe row is one where the coerced state and the current state are identical, not one where they merely seem harmless (soft-deleted, no live reader).
- A full `BEGIN…ROLLBACK` dry-run of the migration was blocked by the environment's own permission classifier (multi-statement DDL). Substituted with measuring blast radius via read-only queries first (all 3 backfill UPDATEs would affect 0 rows on live data) — a different, weaker form of safety than a true dry-run, worth remembering doesn't fully substitute for one.
- Confirmed live, not assumed: Shell → Cards licence sync has no separate copy step — same `public.licences` row on eq-canonical, Cards reads it straight via `eq_cards_list_my_licences()` filtered only on `user_id = auth.uid()`. No approval gate, no push — appears on the worker's next app refresh/cache load.

## Addendum — the three chips above verified and merged (same session, ~1hr later)

Royce started all three chip sessions himself and asked to check on both the SKS security fix and the dangling-pointer fix once they'd produced something.

- **Read eq-field PR #533's actual migration before touching anything** — it corrected my own diagnosis. I had queried `app_data.field_team_supervisors` and reported "RLS disabled, 0 policies" — wrong object. That's a *view*; a view always reports `relrowsecurity=false`/0 policies, that's normal, not a gap (the base table's RLS is what governs a `security_invoker` view). The real bug, found by the investigating session, was one level down on the base table `app_data.team_supervisors`: 4 overlapping RLS policies where a leftover `org_id`-only pair (shape-copied from a sibling table, never tightened) OR'd against the correct tenant-scoped pair — Postgres ORs permissive policies together, so the loose one always won. Any signed-in SKS user, any tenant, could read every supervisor row and delete any of them (not anon-reachable — no anon grant existed).
- **Verified live against ehow before merging anything**, rather than trusting the PR body: policies, grants, and the trigger function body (byte-for-byte `pg_get_functiondef` comparison) all already matched the migration's target end-state, and `20260722094240 team_supervisors_tenant_lockdown` was already in the migration ledger. **The fix was already live** — someone (the investigating session itself, applying its own dry-run-tested migration for real) had gotten there first. Merged eq-field **PR #533** and its companion CI-allowlist fix, eq-shell **PR #950** (the view false-positive, separately) — both merges were racing the same concurrent session and it won both races by minutes; the verification wasn't wasted, it's what let me confirm the live state was correct and safe to merge rather than assuming the PR body was accurate.
- **Checked PR #949 (dangling `workers.staff_id` pointer detection) and #947 (spinner fix)** — both already merged too. #949 is genuinely alert-only, confirmed by reading the diff: the cron function (`check-dangling-staff-pointers.ts`) only reads and reports; the actual repair (`scripts/repair-dangling-staff-pointers.sql`) is a separate, explicitly gated file — header states `STATUS: NOT RUN. Ends in ROLLBACK. Royce's hand.` Live audit found it's worse than my original rough estimate: 93 distinct pointers, 22 dangling (24%), 5 of those are a worker's own id mistakenly written into the staff-pointer field. All 22 are repairable — the reverse edge (`staff.cards_worker_id`) is intact and resolves to exactly one active staff row for each. #947 confirmed single-file, cosmetic only.
- **Nothing left open from this thread.** All four flagged items (licence PDF/wallet-breaking defect, SKS supervisor exposure, CI false-positive, dangling pointer, spinner) are now merged and live or correctly gated pending a human decision. `eq/pending.md` updated to close out the three deferred bullets.

---

# Session 2026-07-22 — closed the chip from the "6 orphaned accounts" session: retired backfill-auth-users.ts

## Built
- **eq-shell PR [#948](https://github.com/eq-solutions/eq-shell/pull/948) MERGED** (squash `c836f12`, Royce's "merge it" go) → core.eq.solutions auto-deploying. Deletes `netlify/functions/backfill-auth-users.ts` outright (chip `task_feba4071` from the earlier #944 session) rather than adding a deleted-account guard — it had no legitimate target left. Updated the 4 places that referenced it: README's function tree, `scripts/smoke-functions.mjs`'s exclude list, and the two docs (`retention-purge.ts`, `scripts/cleanup-orphaned-shell-users.sql`) that warned "don't point this at these rows" — now describe the retirement instead.

## Decided
- Royce chose retire-outright over the other two options offered (guard-and-keep, or guard-now-retire-later) — its stated purpose (pre-fix accept-invite not mirroring to `auth.users`) was already closed by migrations `2026_07_15a`/`15b` + `accept-invite.ts`'s own inline mint, and its only remaining live candidates were the 6 known deletion-residue rows.
- Royce: merge — explicit go, taken as authorising the resulting Netlify auto-deploy (documented behaviour for this repo).

## Deferred
- None new. This closes chip `task_feba4071` outright.

## Notes
- Live-reconfirmed on jvkn before acting (read-only): 72 `shell_control.users` rows, 6 with no `auth.users` match, **all 6 still with `email IS NULL`**, 0 orphaned `auth.identities`, 649 `public.audit_log` rows — matches the #944 session's findings exactly, nothing drifted in the interim.
- `pnpm run build` clean, 177/177 tests pass.
- **PR #948's CI showed one red check** (`Schema drift + anon-grant + policy-lint`) that had nothing to do with this diff — it's the already-known `app_data.field_team_supervisors` RLS-disabled gap on ehow (see line 157/158 above, chip `task_87efbf2f`, Royce already working it separately). Confirmed via `gh run list` that the same check already failed on `main` at the commit this branch forked from, before any of this PR's commits existed — pre-existing, not introduced. Merged anyway rather than blocking on an unrelated known issue.
- Brief-gate flag for this session had to be written by hand mid-task (`New-Item ... eq-brief-<date>-<session-id>.flag`) — the `/brief` skill run alone didn't clear it; the flag-write step at the end of the skill's own instructions is easy to skip if you stop reading after the printed brief.

---

# Session 2026-07-22 — core.eq.solutions suite-wide outage: diagnosed a purged Cloudflare zone, rebuilt DNS + email live

## Built
- Diagnosed "core.eq.solutions unreachable, IP not found" down to the DNS layer, not Netlify/deploys: all of core/field/service/cards SERVFAILing at once, apex included, is a zone-level signal. Confirmed via RDAP the domain itself was active (not expired, no DS/DNSSEC issue) and delegated to Cloudflare, but Cloudflare's own nameservers returned REFUSED for every query.
- Root-caused via the Cloudflare account audit log: Royce deleted the `eq.solutions` zone on 2026-07-15 (06:00 UTC) intending only to remove the old `eq-website` Pages project, not the whole domain. Cloudflare kept serving DNS through its standard 7-day soft-delete window — confirmed via PostHog daily pageview counts staying normal through 2026-07-21 — then auto-purged the zone at 2026-07-22 06:00 UTC (system actor, IP `::1`), which is the actual moment the outage began, a week after the triggering action.
- Confirmed records were unrecoverable: no Cloudflare restore path for a purged zone; the account audit log stores full record payloads but only for 18 months and none of the app's real DNS records fell inside the deletion event itself (only ephemeral ACME challenge TXTs did).
- Rebuilt the zone with Royce: re-created `eq.solutions` in Cloudflare (Royce, zone-create needs a scope this session's Cloudflare token doesn't have), wrote all 4 CNAMEs myself once the zone existed (`core`/`field`/`service`/`cards` → their `.netlify.app` targets, DNS-only/grey-cloud), Royce repointed GoDaddy at the new nameserver pair (`clark`/`robin`, not the old `megan`/`koa`). Verified DNS resolution + HTTPS 200 on all four hostnames from outside the network after propagation.
- Fixed outbound email in the same incident: `@eq.solutions` (invites, licence-expiry, quotes) sends via Resend, whose MX/SPF/DKIM died with the zone. Resend already had a Cloudflare integration connected from initial setup — Royce clicked "Auto configure" on Resend's side, which wrote MX/SPF/DKIM straight into the rebuilt zone. Confirmed both Resend ("Domain verified") and the Cloudflare zone (`status: active`, 7 records) independently.
- Sanity-checked scope while waiting on DNS propagation: grepped eq-shell for every `*.eq.solutions` reference — confirmed core/field/service/cards is the complete live set (`quotes.eq.solutions` is dead code, retired 2026-06-25) — and confirmed Netlify still had all custom-domain configs attached throughout, so nothing needed re-adding there.

## Decided
- Royce: confirmed eq-website staying down is intentional (that was the deletion he meant to make) — explicitly out of scope, not rebuilt.
- Royce: authorised writing DNS records directly ("do everything physically possible to make this work") after the initial offer-first approach; zone creation itself still had to be done by Royce by hand since the Cloudflare token in this session can write DNS records but can't create zones.

## Deferred
- Cloudflare account (`royce@eq.solutions`, sole Super Administrator over DNS for the whole suite) has no 2FA enabled. _(added 2026-07-22)_
- No DMARC record for `eq.solutions` — Resend's auto-configure treats it as optional and verified without it; a `p=none` starter record would add spoofing visibility if wanted later. _(added 2026-07-22)_

## Notes
- The load-bearing lesson: deleting a Cloudflare zone does not stop DNS resolution — Cloudflare keeps answering for ~7 days, then purges. The outage's real trigger and its visible symptom were a week apart, which is why "what changed today?" found nothing. Anyone diagnosing a similar "domain suddenly down" report should check the zone/account audit log for a delete far outside the recent window, not just recent activity.
- Symptom shortcut confirmed live: SERVFAIL on the apex as well as every subdomain means zone-level — stop looking at app deploys immediately. REFUSED-from-authoritative-NS (what resolvers report as "lame delegation") means delegation is intact but the DNS host dropped the zone; an actually-expired domain looks different (registrar clientHold, NXDOMAIN).
- Wrote up the full incident (timeline, diagnostic shortcuts, why records weren't recoverable) to memory (`eq-solutions-zone-missing-cloudflare.md`) for faster diagnosis if anything similar happens again.

---

# Session 2026-07-22 — notify-substrate closed out: all 4 EQ repos verified live

## Built
- Nothing new built this session — pure verification of yesterday's PAT-swap ask.

## Decided
- Royce pasted the working classic PAT into eq-shell/eq-field/eq-service's `EQ_CONTEXT_PAT` secrets (the one already proven working on eq-cards).

## Deferred
- Sentry alert script (`create-sentry-alerts.mjs`, eq-shell) still needs a re-run against live Sentry to pick up yesterday's `mint-cards-otp` rename — source fixed 2026-07-21, live alert rule not yet re-applied.

## Notes
- Verified Royce's "all updated" claim rather than taking it at face value: manually triggered `notify-substrate.yml` via `gh workflow run` on all 3 previously-broken repos. All three came back `success` (eq-shell `29909459586`, eq-field `29909461843`, eq-service `29909463950`). Notify-substrate is now fully closed — all 4 EQ repos (Cards, Shell, Field, Service) can notify eq-context on merge.
- This closes out the thread carried from 2026-07-21's session (see that day's log + `eq/pending.md`).

---

# Session 2026-07-22 (2) — re-confirmed notify-substrate independently; no drift, no new work

## Built
- Nothing built — a different, later session in the same day (entry above) had already re-verified and closed this same thread. This session asked the identical question from a different angle ("is this an easy fix" → "i think this is now fixed?") without visibility into that concurrent close, and independently checked live state before answering rather than assuming.
- Checked all 4 repos' `EQ_CONTEXT_PAT` secret timestamps + latest `notify-substrate.yml` run via `gh secret list`/`gh run list`: eq-cards (set Jul 21 08:38, success), eq-shell/eq-field/eq-service (all set Jul 22 ~09:47-09:48, all success). Matches the entry above exactly — same conclusion, no discrepancy found.

## Decided
- None — pure re-verification, reported back to Royce as confirmed fixed.

## Deferred
- None new. Same one open item as the entry above: the Sentry alert script re-run (`scripts/create-sentry-alerts.mjs`, eq-shell) — not touched.

## Notes
- Recorded mainly to flag the redundancy pattern, not to duplicate findings: two sessions independently verified the identical fix within the same day because neither had visibility into the other's concurrent work. No harm done here (both checks were read-only), but worth noting as the same shared-checkout-adjacent race other 2026-07-22 entries hit around commits — this time on investigation rather than a write.

---

# Session 2026-07-22 (3) — cleared the 6 jvkn orphans, then chased a red CI gate to a structural fix, not a patch

## Built
- **Cleared the 6 orphaned `shell_control.users` rows on jvkn**, with Royce's explicit go, via `scripts/cleanup-orphaned-shell-users.sql` (written in the earlier #944 session, never run). Re-verified all 5 preconditions live immediately before deleting (still 6 rows, still 0 `auth.users` match, still no email/phone/name/pin, still never logged in, still 0 blocking referrers) — all held, no drift since the original audit. Deleted: `orphans_remaining` 0, `audit_rows_preserved` 52 (the deletion trail, deliberately untouched), 3 `user_tenant_memberships` cascaded. `shell_control.users` 72 → 66.
- Confirmed **eq-shell PR #948** (retire `backfill-auth-users.ts`) had already merged (`c836f12`) while the jvkn work was in progress.
- Chased the "SKS table has no access restrictions" CI finding from the licence-PDF session (#945) to ground truth instead of accepting it at face value: live-queried `app_data.field_team_supervisors` on ehow and found it's a `security_invoker` view over a properly RLS-protected table — not actually open. A concurrent session had already unblocked the red gate with a documented allow-list entry (**PR #950**, merged) before this was confirmed.
- Root-caused *why* the false alarm kept recurring: the CI checker's anon-access query can't distinguish "this is a view, which structurally always reads as unprotected" from "this is a genuinely wide-open table" — so every safe view ever granted to the app tripped it, and the standing fix since late May had been a hand-written exception per table, one at a time, 57 of them over about seven weeks by today. That same masking is exactly how two *real* wide-open tables hid inside the same exception list undetected in the past (2026-06-03 and 2026-07-03 incidents).
- Fixed the checker itself rather than adding exception #58: it now separates views from tables correctly, and a separate, already-existing, stricter check (added 2026-07-03 specifically to close that same masking hole) takes over verifying views. Removed all 57 now-provably-unnecessary exceptions; the ones for genuinely open legacy tables were left untouched. No database change — CI-script only. **eq-shell PR #952 MERGED** (`4a420d8`, Royce's go, on top of #950).
- Flagged, then withdrew, a follow-up chip for the *actual* remaining gap (the underlying table's own company-scoping rule looks looser than it should be) after discovering EQ Field already had it in flight independently (PR #533) — avoided duplicating work already started elsewhere.

## Decided
- Royce: run the jvkn cleanup now ("Yes — run it").
- Royce: fix the checker properly rather than patch around it or hand it to whoever created the flagged table first.
- Royce: merge PR #952 once CI went green (did so himself, ahead of the session's own merge step).

## Deferred
- EQ Field PR #533 (the real company-scoping gap on the underlying table) — in flight elsewhere, not this session's to finish.

## Notes
- The two things that looked like open threads from the licence-PDF session (#945) both closed today: the sign-in-record leftovers (cleared) and the "no access restrictions" table (found to be a false alarm in the checker, checker fixed).
- Re-verifying live state immediately before an irreversible delete — rather than trusting a script written a session earlier — is the right default for anything touching production identity data; it cost five queries and caught nothing this time, but it's the check that would have caught drift if there had been any.
- Worth remembering for next time this CI gate goes red on a *view*: check whether it's actually a safe read-only window before treating it as a real hole. That mistake is now much harder to make (the checker itself no longer raises it), but the underlying judgement call — view vs. table — is the one to reach for first.

---

# Session 2026-07-22 — EQ Field: crew-view toggle — ungating the crew-scoping boundary on Royce's feedback

## Built
- **eq-field PR #534 (v3.5.355, MERGED, live)** — a My crew / a specific team / Everyone dropdown on Timesheets + Roster, visible only while supervision is unlocked. New `EQ_CREW_VIEW` in `scripts/permissions.js` (localStorage-backed mode) plus a branch in `getCrewPersonIds()` for `'all'`/`'team:<id>'`. Both selects share one choice and stay in sync; changing it re-fetches via the existing manual-sync path (`refreshData(false)`), not a new reload mechanism.
- `tests/crew-scoping.test.js` — 13 new assertions (35 total in the file), mostly proving the gate direction: a stale widened choice has zero effect once supervision is locked again, checked at both `getCrewPersonIds()` and the filter-fragment level.

## Decided
- Royce, having seen crew-scoping (PR #530, shipped earlier the same day) as a hard boundary: "no guardrails that stop people doing their job" — the widen choice itself should have no permission check.
- The team picker should offer any team, not just ones the caller runs or belongs to — 'Everyone' already has no gate, so restricting the team choice on top of it would be pointless friction.
- What IS gated: whether the override applies at all, tied to the same supervision-unlocked flag (`isManager`) that shows every other edit-mode control — not a guardrail on the choice, a guarantee that a hidden control has no effect. Reasoned through with Royce as "the control not being on screen must mean it also has no effect," not re-litigated as a new restriction.
- Royce: "merge and /close."

## Deferred
- **Real click-through verification not completed this session.** The Browser pane's screenshot/click tool timed out repeatedly at the environment level (confirmed unrelated to this change — same failure before any code was touched). Verified instead by driving the actual browser-loaded code via console: confirmed the version banner, clean boot, CSS visibility flipping correctly on the unlock flag, and the full crew-selection matrix (default crew / an unrelated team / Everyone / the lock gate) against a synthetic teams fixture — the `eq` demo tenant has no Teams feature at all, so a live click-through couldn't have exercised the real path either way without a fixture. Worth a real look next time someone's in Timesheets/Roster with supervision unlocked.

## Notes
- **Version collision, self-resolving:** PR #535 (a concurrent session, the ±1-week narrowing follow-up) also stamped v3.5.355 off the same base commit. Flagged explicitly in both worktree-registry entries before merging so whichever merged second would know to rebase + re-stamp, rather than repeating the day's earlier collision pattern silently. #534 merged first; #535 now owns the rebase.
- `gh pr merge --squash` printed a misleading 16-file/1665-insertion stat on merge (looked like unrelated PRs' files had been swept in). Did not trust it — re-verified directly via `git show --stat` on the actual resulting commit on `origin/main`, which showed exactly the intended 8 files. Worth remembering: that tool's own merge-summary output isn't reliable evidence of what landed; check the commit directly.
- This closes out the crew-model thread opened earlier today: blocked → built as a hard boundary (PR #530) → an adjacent RLS gap found and fixed on the base table (PR #533) → loosened into a toggle on live feedback (PR #534). All three merged and verified live in one day.

---

# Session 2026-07-22 — Tenant-isolation gap on SKS's team/roster tables: found via a false-alarm CI block, closed on all three affected tables

## Built
- **eq-shell PR [#950](https://github.com/eq-solutions/eq-shell/pull/950) MERGED** — `app_data.field_team_supervisors` (new that day via #530) had been flagged by CHECK 2 (anon-grant gate) as a wide-open table, hard-blocking PR #945 and every subsequent eq-shell PR. Live recon showed it's a `security_invoker=on` view over an RLS-on base table — a Postgres invariant (views always report `rls_enabled=false`), not a real gap, and a blind spot the check's own header already documents. Added to the existing allowlist beside its already-listed siblings. No DDL.
- **eq-field PR [#533](https://github.com/eq-solutions/eq-field/pull/533) MERGED, migration APPLIED live to ehow** — the real bug underneath the false alarm: `app_data.team_supervisors`'s base table carried a correct tenant_id-scoped policy pair *and* a looser org_id-only pair, which OR-widened past it — any authenticated ehow user could read every SKS supervisor row regardless of their own tenant, and delete any row (`WITH CHECK` never gates DELETE). Fixed privilege-first (revoke direct base-table writes from `authenticated`, keep SELECT), collapsed to one tenant-bound read policy, and stopped the write trigger trusting a client-supplied `org_id` on INSERT (now derived from the parent team, tenant-checked). Dry-run in `BEGIN...ROLLBACK` before Royce's go; post-apply functional proof via simulated JWT claims (correct-tenant caller unchanged, wrong-tenant caller now denied).
- **eq-field PR [#536](https://github.com/eq-solutions/eq-field/pull/536) MERGED, migration APPLIED live to ehow** — Royce asked for the same fix on `teams`/`team_members` (the tables `team_supervisors` was shape-copied from) plus "anything else high value." Ran a systematic sweep of every `app_data` policy on ehow for this exact shape (not a guess) — confirmed only these two carried it; a superficially similar pair on `briefing_actions`/`briefing_cache` turned out to be a different, safe self-row-only pattern. Also found and fixed a second instance of the same "trust the client" class: both `field_teams_iud`/`field_team_members_iud` triggers took a client-supplied `org_id` on INSERT unchecked. Live functional proof went further than the read/write checks — deliberately tried to smuggle a wrong `org_id` through the view on a real INSERT and confirmed the trigger overrides it, not just passes it through untested.

## Decided
- Royce: "merge the eq-shell PR" (#950).
- Royce: "apply the eq-field migration to ehow" (#533), then "merge the eq-field PR too."
- Royce: "do the same fix for teams and team_members and anything else you think is high value" — scope-widening instruction that drove the systematic sweep rather than a second guess-and-check pass.
- Royce: "apply the migration to ehow" (#536), then "merge the eq-field PR too."

## Deferred
- None from this thread — fully closed, all three tables fixed and verified live.
- Cross-reference only, not a new deferred item of this session's own: PR #945 (unrelated eq-shell licence-upload fix) will keep showing this same CI check as failed until that branch itself rebases onto latest main — noted in `eq/pending.md` so it isn't mistaken for the fix not having worked.

## Notes
- **The false alarm and the real bug were different things wearing the same name.** CHECK 2 flagged the *view* (`field_team_supervisors`) as open; the actual defect was on the *base table* (`team_supervisors`), invisible to that same check because it only scans grants, not policy semantics. Worth remembering next time a CI gate points at a security_invoker view: check whether the underlying table has its own problem before assuming the flag is either fully wrong or fully right.
- **The sweep-not-guess approach directly earned its keep.** A first-pass eyeball match on `briefing_actions`/`briefing_cache` looked like the same bug; reading the actual policy semantics (`auth.uid() = user_id`, a self-row scope, not org-wide) ruled it out before it became a wasted fix. The systematic query (org_id-referencing, non-tenant_id-referencing permissive policy) is reusable if this pattern needs checking again on this or another tenant DB.
- **Live-testing "the fix actually works," not just "the read policy is correct," caught the difference between a policy fix and a trigger fix.** The org_id-on-INSERT issue would never have shown up in a read-only functional test — it only surfaces on write, which is why the verification step deliberately inserted a row with a hostile `org_id` rather than just re-running the same read checks from PR #533.
- **`eq-context/system/worktree-registry.md` rows kept disappearing mid-session** — re-added twice after apparent concurrent overwrites from other active sessions. Not investigated further (out of scope for this thread); if it recurs often it may be worth a look at whatever's editing that file concurrently.
- Not exploited: SKS is currently the only company on ehow, so there was no second tenant's data to actually leak into. All fixes were proactive, verified against unchanged live row counts (10 / 7 / 72) before and after.

---

# Session 2026-07-22 — eq-field boot week-window: 3 live guard bugs fixed, then narrowed ±4 → ±1 for real

## Built
- **eq-field PR [#532](https://github.com/eq-solutions/eq-field/pull/532) MERGED (v3.5.354)** — fixed 3 bugs found while auditing what a smaller boot window would break, all already live at the existing ±4-week window and unrelated to narrowing it: `runCopyWeek`'s "skip weeks that already have data" checkbox could silently overwrite a real roster when the target week wasn't already loaded in memory; leave approval's double-booking check reported "no conflicts" for leave booked more than 4 weeks out without ever actually checking; `exportFullBackup` only ever backed up the 9 weeks resident in memory despite being labelled "Full backup," so restoring an old backup would have dropped everything else. New `_ensureWeeksLoaded(weeks)` primitive — the existing lazy-loader swallows its own errors so on-screen rendering degrades gracefully, but a guard can't accept "maybe empty, maybe just not loaded" as an answer, so this wrapper reports actual residency and lets guards fail loud instead. `tests/week-window-guards.test.js`, 10 assertions.
- **eq-field PR [#535](https://github.com/eq-solutions/eq-field/pull/535) MERGED (final: v3.5.356 after a mid-flight version renumber, see Notes)** — the actual scale lever: `_getVisibleWeekRange` narrowed from ±4 weeks (9 total) to ±1 (3 total), behind one named constant `WEEK_WINDOW_HALF_WEEKS` for a one-line revert. ~3x less schedule/timesheet payload on every boot and every 30s poll, stacking on crew scoping (#530) and paging (#526). Fixed the 10 cross-week consumers the pre-narrowing audit found: 7 genuinely needed fixing (forecast's 12-52 week horizon, the calendar month view, the timesheets completion tracker, the apprentice ratio sparkline — which had its own separate bug, silently plotting fewer points than it claimed — copy-last-week, the staff prefill banner, and the three "add from roster" buttons in diary/toolbox/prestarts); 3 turned out to be dead or genuinely not worth fixing and were documented rather than hardened (Clean Up Codes — button removed from Sites in v3.5.227, unreachable since; the orphan-removal warning — gated behind an existing sks-tenant guard that returns before reaching it, so only reachable on the disposable eq sandbox; the site-code autocomplete — fixing it would mean re-fetching full history on nearly every render, defeating the point of the change for a cosmetic gap). `tests/week-window-narrowing.test.js`, 16 assertions.
- Both PRs: full local gate (19/19 test files, eslint 0 errors) run before AND after a rebase where relevant; CI green; production verified live via direct curl of the deployed `sw.js`/`index.html`/`scripts/*.js` after each merge, not just "the merge succeeded."

## Decided
- Royce: "merge it" (#532).
- Royce, when offered 4 sequencing options after the audit surfaced 3 live bugs: "Fix the 3 bugs first, narrow after" — kept as two separate PRs rather than one 13-file change, matching how this repo's prior scale work (#525/#526/#530) was deliberately split one-lever-per-PR.
- Royce: "merge it" (#535).

## Deferred
- None outstanding from this thread's own scope — both PRs shipped and verified live. Two open items carried into `eq/pending.md` for Royce's attention, not this session's to resolve: the recurring version-collision friction between concurrent worktrees (4th occurrence this session-cluster; self-resolved every time, but worth a real fix if it keeps happening), and an unverified real-browser click-through on Forecast/Calendar (tooling issue this session, not a code issue).

## Notes
- **The audit before the change was the actual work.** The chip's framing was "narrow the window, it's basically one constant" — true only in a vacuum. Any code reading `STATE.schedule`/`STATE.timesheets` for a week that could fall outside the window needed checking, and roughly a third of what that audit found (3 of 13 consumers) were bugs *already live today*, independent of narrowing anything. Shipping those first, separately, meant they could be verified and reasoned about without the noise of "did narrowing cause this."
- **Reachability isn't the same as "found via grep."** All 3 of the "deliberately not fixed" items in #535 looked identical in shape to the 7 genuinely-broken ones — same STATE.schedule read pattern, same missing-week gap. The difference only showed up by tracing whether the calling code path was actually reachable: one button had been deleted from the UI four months ago, one function has an unconditional early-return for the only live tenant, and one runs on almost every render so "fixing" it would cost more payload than the whole PR saves. Worth remembering as a general check before hardening anything found by pattern-matching: is this code actually live, on the tenant that matters?
- **Version collision happened exactly as flagged in advance.** #535's registry entry noted a concurrent worktree (`claude/crew-view-toggle`) was branched off the same base commit and might merge first — it did, taking v3.5.355 as PR #534. Rebased #535 onto the new main, kept both changelog blocks (renumbered this one to v3.5.356, left the other's block alone), re-bumped `app-state.js`/`sw.js` to match, re-ran the full gate suite post-rebase, waited for CI to go green again, then merged. No lost work, but this is at least the 4th time this exact shape of collision has happened across recent EQ Field sessions running parallel worktrees off the same base.
- **`eq-context/system/worktree-registry.md` lost an edit mid-session to a concurrent write from another process** (same class of issue another thread already flagged in this file, above) — redone once, successfully, on a freshly-fetched base the second time. Confirms this is a repeating pattern worth a real look if it keeps costing retries.

---

# Session 2026-07-22 (4) — correction: fabricated a timespan in the jvkn/drift-gate session notes, caught and fixed

## Built
- Nothing new — corrected an error in the write-up from session (3) above, on Royce's catch.

## Decided
- None.

## Deferred
- None.

## Notes
- Wrote "a two-year-old class of false positive" / "hand-patched one table at a time for two years" in three places (`eq/pending.md`, this repo's `eq-shell` changelog, and the earlier session-log entry) describing the CI checker's view-masking bug. Never verified — invented for narrative color while writing up the finding, not something the PR itself claimed. Royce caught it immediately ("how can something be 2 years ago").
- Checked the actual evidence: `check-tenant-drift.mjs`'s own comments trace the anon-grant check to the 2026-05-31 discovery of the sks-canonical open door, and the view-masking pattern was first caught and partially fixed 2026-07-03. That's about seven weeks, not two years — corrected all three places to say so. Commit `bd1598e`, pushed.
- Worth remembering generally: a specific-sounding timespan ("two years", "for months") is a claim like any other and needs the same evidence check as a technical fact — it's easy to reach for one as a rhetorical flourish while writing a summary and not notice it was never actually derived from anything.

---

# Session 2026-07-22 (5) — EQ Field: Incidents / Near Miss reporting, built and shipped to production

## Built
- New Incidents / Near Miss page in EQ Field (`scripts/incidents.js`, ~650 lines) — type/severity/site/date/time/reported-by, description + immediate action (voice input), individually-signed people-involved list, photos, draft/submit, offline queue. Built on EQ Field's existing shared toolkit (`site-reports-shared.js`) rather than duplicating SKS's self-contained copy.
- New `public.incidents` table on EQ Field's live DB (`ehowgjardagevnrluult`), `tenant_id` + single `tenant_isolation` RLS policy — matched to prestarts/toolbox_talks/site_diaries' live-verified shape, not the stale on-disk migration file (which showed `org_id`) and not `site_audits`' hardcoded-org_id outlier. Applied live with Royce's explicit go-ahead. Migration file: `migrations/2026-07-22_incidents_v1.sql`.
- `supabase.js` — added `incidents` to `JWT_TABLES`/`JWT_INPLACE_TABLES`. `permission-matrix.js` v1.8 — added `reports.incident.*` keys.
- Wired into `safety.js`'s Records tab (list/filter/zip-export alongside prestarts/toolboxes) and `safety-dashboard.js`'s Safety Report (stat card + by-person table).
- Manager email alert on submit of High-severity or non-near-miss reports, via the existing send-email function — same suppression rule as the SKS reference, ported to EQ Field's own endpoint/branding.
- Mobile CSS checked at 375px before calling it done (SKS's build caught this bug late — checked up front here instead).
- Version bump: `3.5.354 → 3.5.357` (two intermediate numbers were claimed by other in-flight PRs before this one merged — see collision note below).
- eq-field PR #537 opened, merged (squash), branch deleted. Live on field.eq.solutions.

## Decided
- Royce (task brief): reference the unmerged SKS branch as prior art only, do not touch sks-nsw-labour, verify EQ Field's live schema/architecture before writing code, get explicit go-ahead before the live migration, commit locally and ask before push/PR.
- Royce: "Yes, apply it now" (migration go-ahead).
- Royce: plan approved via ExitPlanMode.
- Royce: "push it and open the PR."
- Royce: "cant we just merge and close?" — confirmed via AskUserQuestion (flagged that merging = immediate live deploy to field.eq.solutions) → "Yes, merge now."
- Royce: "merge" (push of the conflict-resolution commit, after the auto-mode classifier blocked the push and asked for explicit sign-off).

## Deferred
- **GitHub Actions' CI check never ran on PR #537** — only Netlify's build check fired. Confirmed it wasn't a lag issue (other PRs/pushes were triggering the same workflow within seconds, this one never did). Ran the equivalent test suite + eslint by hand instead (clean, 0 errors) and merged on that basis with Royce's sign-off, but the cause of the missing Actions run was not tracked down. _(added 2026-07-22)_
- **No live authenticated click-through performed** — signature pad, photo upload, Word export, offline-queue replay, and the manager email actually sending haven't been exercised end-to-end in a real browser session, only via the automated test suite and a static read of the shipped page. _(added 2026-07-22)_
- Carried into `eq/pending.md` rather than re-logged in full here.

## Notes
- Two premises in the original task brief were wrong and caught before building: the SKS reference feature hadn't actually shipped (unmerged branch, read-only reference), and EQ Field's Safety area isn't a single monolithic page the way SKS's still is — Prestart/Toolbox had already split into their own top-level pages, so Incidents was built to match that pattern rather than the brief's literal "add a tab" framing. Mid-build, an initial edit that added Incidents as a `safety.js` tab was caught and reverted before it shipped, once the correct pattern was confirmed against the live code.
- Version-stamp collision, again: `origin/main` picked up two more PRs (both landing as v3.5.355) between this branch being cut and merged — the same recurring friction already tracked in `eq/pending.md` under the roster/timesheets entry ("4th time this has come up"). Resolved by merging origin/main in, re-numbering to v3.5.357 (max+1), and re-running the full test+lint gate before merging — no functional conflicts, only the three version-stamp files (`app-state.js`, `sw.js`, `index.html`'s header/changelog) needed manual resolution.
- The dev-server preview tool is hard-scoped to this session's original working directory (sks-nsw-labour) and silently served the wrong repo even when `preview_start` was called by the eq-field launch-config name. Caught before viewing any content; worked around by manually running a static file server from the eq-field folder and pointing the preview tool at its URL directly.
