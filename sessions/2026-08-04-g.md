---
title: Session Log — 2026-08-04-g
owner: Royce Milmlow
last_updated: 2026-08-05
scope: One session's build/decision/deferred record
read_priority: reference
status: live
---

# Session 2026-08-04-g — Shared eq-context checkout: concurrent-session git races closed as F9, hook-enforced

## Built
- Royce reported the shared `C:\Projects\eq-context` checkout had hit its now-familiar concurrent-session collision pattern twice more in ~20 minutes: a read of `CLAUDE.md`/`system/worktree-registry.md` returning content mid-way through another session's active rebase (self-resolved), then a targeted `git add system/worktree-registry.md && git commit` sweeping up three files (`eq/pending.md`, `eq/changelog/eq-cards.md`, `sessions/2026-08-04.md`) another concurrent session had already staged — caught via `git show --stat HEAD` before push, undone with `git reset HEAD~1`, recommitted correctly scoped. The follow-up `git push` then hit a non-fast-forward rejection, `git rebase` refused to start (unrelated uncommitted files from another process already in the index), and `git stash` was blocked by an unrelated environment permission classifier — resolved via a fresh isolated clone + cherry-pick.
- Read `eq/pending.md`'s existing 2026-08-03 deferred item, `system/failures.md` (F1–F8), `system/lessons.md`, `.githooks/pre-commit`, `hooks/README.md`, `hooks/pre_tool_use.py`, `hooks/session_start.py`, `rules/agentic-coding.md`, and the 2026-07-14/2026-08-03 session logs to establish this failure class had **already recurred 3+ times (2026-07-14, 2026-08-03 ×3, 2026-08-04 ×2) with zero `system/failures.md` entry** — the exact gap the ratchet's own recurrence counter depends on not having.
- While reading (not writing), watched the shared checkout's `HEAD` advance from `63e4c3b` to `507e6a8d` between two consecutive read-only `git log` calls, with no command of mine touching it — a live demonstration of the problem mid-investigation. Moved all subsequent editing to an isolated clone in the scratchpad rather than continuing against the shared checkout, deliberately practicing the fix being built.
- Extended `hooks/pre_tool_use.py` with two new rung-4 checks (F9), both scoped to the ONE shared checkout by exact path (`is_shared_eq_context()`, `EQ_CONTEXT`-overridable, same convention `session_start.py` already uses) so the isolated-clone escape valve they recommend is never itself blocked, and deliberately **not** gated on `in_sandbox()` — every occurrence has happened natively on the Beelink:
  1. Blocks bare `git commit` (no `--` pathspec, no `--amend`) — a bare commit records the *entire* index, not just what was just `git add`ed, which is the exact mechanism that swept up a concurrent session's files today. Quoted message content is stripped before scanning for `--` so a commit message that happens to contain the literal text `-- ` can't create a false negative.
  2. Redirects `git rebase` / `git merge` / `git pull` to an isolated clone instead of the shared checkout — these mutate HEAD/index/refs across several non-atomic steps, the mechanism behind the stuck rebases, the conflict marker committed straight to `main`, and the detached-HEAD/stale-ref damage from 2026-07-14 and 2026-08-03. `--abort` / `--continue` / `--skip` stay allowed through unconditionally, so a session already stuck mid-operation can still get itself out rather than being trapped by the guard meant to prevent the stuck state in the first place.
- Added 15 new adversarial test cases to `hooks/adversarial_test.py` (the CI-authoritative suite — confirmed via `.github/workflows/adversarial-suite.yml`, since the older `hooks/adversarial_test.sh` has drifted and covers neither F7 nor F9). Full suite: **65/65 green**, verified twice — once from the scratchpad clone (62/65; the 3 shortfalls are `targets_mount()` correctly not recognising a scratchpad path as the risky virtiofs mount, proven pre-existing and unrelated by running the identical suite against the unmodified hook via `git stash`, same 3 failures), and once from a throwaway clone made directly under `C:\Projects` (deleted immediately after) to get full F2/F6/F7 coverage: **65 passed, 0 failed**.
- Added F9 to `system/failures.md` (`recurrences: 3`, `rung: 4`, built same-day, matching F8's promotion pattern) with a `signal:` regex for `failure_recurrence_signals()` so any future recurrence surfaces in the digest automatically instead of relying on a human noticing again. YAML re-parsed and the signal regex re-compiled to confirm both are syntactically valid.
- Updated `hooks/README.md`'s `pre_tool_use.py` row for F9, and fixed its test-runner pointer from the stale `adversarial_test.sh` to the CI-authoritative `adversarial_test.py` (noticed while reconciling which suite actually gates CI).
- Closed the 2026-08-03 `eq/pending.md` deferred item with a resolution pointer, and added a new dated section documenting the fix, including the two alternatives that were considered and explicitly rejected (see Decided).
- Re-clone hygiene: wired `core.hooksPath` to `.githooks` in the scratchpad clone before committing (F8/lessons.md's documented "fresh clone defaults to `.git/hooks`, silently skipping the governed pre-commit hook" gotcha) — otherwise this exact commit would have landed without the secret guard or frontmatter check running.

## Decided
- Royce: "decide/implement a concrete fix — not just re-document the risk a third time," offering three candidates (fresh-clone-by-default, a lock/coordination file, mandatory `git status --short` before targeted add+commit) and asking for something enforceable, not prose.
- Chose a composite of (mechanical pathspec-commit rule) + (scoped fresh-clone redirect for rebase/merge/pull) over the three as literally framed:
  - **Bare-commit sweep (today's incident)** is fixed unconditionally by requiring `--` pathspec on every commit in the shared checkout — stronger than "check `git status` first" (candidate c) because it can't be forgotten, and doesn't depend on the operator noticing foreign staged content before committing.
  - **Ref-level corruption (2026-07-14, 2026-08-03)** is fixed by redirecting the specific multi-step, non-atomic verbs (rebase/merge/pull) to an isolated clone — narrower than "fresh-clone-by-default for every substrate write" (the full form of candidate a), which would have broken routine, currently-reliable automation (nightly cron commits, single-file pending.md ticks, session close) that has never been the source of the actual damage.
  - **Rejected outright:** a lock/coordination file (candidate b) — a hook-enforced lock needs reliable cleanup on abnormal session termination or it becomes a new stuck-forever failure class, and this ledger already documents several failures of exactly that shape (the Loop of Despair, F2, F6, F7 — all guards that had their own bugs). A lock also wouldn't have prevented today's incident: the foreign staged content came from a session that ended without committing, not from two sessions writing at literally the same instant.
- Not asked, so not done: did not touch `hooks/adversarial_test.sh`'s own F7/F9 gap (flagged as a deferred item instead) — reconciling or deprecating the legacy runner is a separate decision, not part of fixing the concurrent-write problem itself.

## Deferred
- `hooks/adversarial_test.sh` is missing F7 and F9 coverage (distinct from the CI-authoritative `adversarial_test.py`, which has both) — bring it in sync or formally deprecate it. Logged in `eq/pending.md`. _(added 2026-08-04)_

## Notes
- This session's own git work was done in an isolated clone in the scratchpad, then committed with an explicit `--` pathspec and pushed directly from there — dogfooding the exact rule this session built, not just writing it for others to follow.
- The three pre-existing adversarial-suite "failures" seen when running from the scratchpad clone (not from a `C:\Projects`-rooted path) are a `targets_mount()` path-matching artifact, not a defect — confirmed identical on the unmodified hook via `git stash` before concluding this, rather than assumed.

---

## Session close (2026-08-04-g)
- Applied `eq/pending.md`'s archive rule to the one section this session touched that still has open items left (`eq-context + eq-shell/eq-field: CI health sweep...`, 2026-08-03): dropped the now-resolved `[x]` bullet (its full narrative already lives above and in this log), kept the 2 still-open items, and dropped the now-empty `**Deferred:**` sub-header along with it.
- Left this session's own new section ("shared-checkout git races — structural fix shipped as F9") untrimmed, per the rule's own carve-out — it mixes checklist items with free-standing "Considered and rejected" prose that isn't part of any bullet, so it's flagged here rather than guessed at.
- No product repo touched this session (eq-context substrate/tooling only) — Step 4 (changelog) doesn't apply.
- Spawned a background task (`task_9c68aeb9`, "Sync or deprecate hooks/adversarial_test.sh") for the one deferred item above; Royce started it in a separate session before this close ran.

---

## Continuation — restore adversarial_test.sh, catch a live example of F9's own blind spot

## Built
- Royce: "fix the old backup checklist and explain what you're doing." Checked live state first rather than assuming the earlier spawned task (`task_9c68aeb9`) hadn't finished — it had: `hooks/adversarial_test.sh` was already gone from `origin/main`.
- Traced the deletion to two separate events via `git show`/`git log`: (1) an ACCIDENTAL sweep — a bare `git commit` from an unrelated session (eq-solves-intake tenant-scoping work, commit `2104668`) swept up a different session's already-staged deletion, landing in a commit whose message never mentions the file; author was "via Cowork," meaning it ran outside Claude Code's own tool-call hook entirely, so last session's F9 guard never saw it. (2) A DELIBERATE follow-up (`c593a83c`) that documented keeping it deleted — sound reasoning (zero functional dependents, avoids duplicating complexity already solved in the Python suite) — written to `hooks/README.md` and `eq/pending.md` before Royce's message arrived.
- Surfaced both — the accident and the (separately reasonable) considered decision — to Royce via `AskUserQuestion` rather than picking one silently, since restoring would mean overriding another session's documented call. Royce: "Restore + sync."
- Restored `hooks/adversarial_test.sh` from git history (`git show <last-good-commit>:hooks/adversarial_test.sh`), ported F7 and F9 bash-native fixtures (real git repos via `git init` + fixture commands, matching the file's existing bash+git style rather than the Python suite's subprocess-fixture style), fixed the stale "F1-F5" header claim, and added `export EQ_FORCE_GUARD=1` — the original file never forced sandbox mode, so 9 of its own assertions had always silently no-op'ed on native Windows (correct per `in_sandbox()`'s own design, but made the suite falsely "pass" any change, not just this one).
- Verifying that fix caught a REAL bug: forcing sandbox mode exposed that F9's own "allowed" test cases (pathspec-scoped commit, `--amend`, `rebase --continue`/`--abort`) were only passing because `in_sandbox()` happened to be False on the machine the tests were written on. Under `in_sandbox()=True` (the default on Linux, i.e. CI), the pre-existing, unrelated "any git verb blocks in the sandbox" rule fires AFTER F9's own logic and overrides its correct "allowed" verdict — meaning both suites' F9 additions from last session would have failed on the next real CI run. Reproduced directly (a standalone Python repro against the live hook, not just suite output) before and after the fix. Fixed by pinning `EQ_FORCE_GUARD=0` on every F9 test case in both suites, matching the pattern F7's tests already used but F9's didn't.
- Documented, not fixed: the accidental-sweep root cause is a live recurrence of F9(a)'s exact mechanism via a path F9 cannot see by construction (git run outside Claude Code's own tool calls — a human, or a Cowork-handed-off script). Added as a "KNOWN SCOPE BOUNDARY" note to `system/failures.md`'s F9 entry (not counted as a recurrence — the guard was never positioned to catch this, same framing as F2/F6's existing Windows/sandbox scope note) and to `pre_tool_use.py`'s own module docstring. Spawned a separate follow-up task (`task_94836df0`) to investigate whether any real mitigation exists, rather than attempting one under time pressure.
- Reconciled `hooks/README.md`'s Testing section and `eq/pending.md` — both had been updated by the other session to say the file was retired; rewrote both to reflect the restored, dual-suite reality, with the full back-and-forth left visible in `eq/pending.md` rather than overwritten silently.
- All work done in the same isolated scratchpad clone from earlier in this session (not the shared checkout); every commit pathspec-scoped, dogfooding F9 throughout. Two more accidental commits from OTHER concurrent sessions landed on `origin/main` while this work was in progress (unrelated: eq-solves-intake PR merge, a digest/suite-state refresh) — each handled by fetch + `git merge --ff-only` or fetch + rebase in the isolated clone, no collision with the shared checkout.
- Final state verified clean-room from a fresh `C:\Projects`-rooted throwaway clone (deleted after): `python hooks/adversarial_test.py` 65/65, `bash hooks/adversarial_test.sh` 32/32.

## Decided
- Royce: "Restore + sync (Recommended)" via `AskUserQuestion`, after being shown both the accidental mechanism and the other session's separately-reasonable case for deletion — an informed choice, not a default.
- Didn't attempt to fix the Cowork-gap itself this session — architecturally different problem (nothing in `pre_tool_use.py` can see git that never goes through Claude Code's own tool calls), spawned as dedicated follow-up instead of a rushed partial fix.

## Deferred
- `task_94836df0` — whether any real mitigation exists for git run outside Claude Code's tool-call hook (human-typed, or Cowork-handed-off scripts). May conclude "no good fix exists" and that would be an acceptable, documented outcome, not a failure to find one.

## Notes
- The two-sessions-reached-opposite-conclusions-same-day situation on `adversarial_test.sh` is itself worth remembering: both calls were individually well-reasoned from the information each session had; the resolution needed a human, not a better hook. Surfacing the tension plainly (rather than silently picking a side) is what made that possible.
- `git status` after each commit in this clone kept showing a stray `MM` on whichever file the pre-commit hook's auto-bump last touched, immediately after a successful commit — HEAD and the working tree agreed (both had the bumped date), only the index briefly disagreed. Resolved each time with a plain re-`add`; never affected what actually got pushed. Worth knowing as a recognizable, harmless pattern rather than re-diagnosing it from scratch next time.
