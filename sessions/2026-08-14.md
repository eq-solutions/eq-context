# Session 2026-08-14 — SKS → EQ Field weekly roster CSV sync: investigated live, confirmed feasible with zero new code

## Built
- Nothing shipped. Pure investigation session — no Edit/Write to any product repo, no data mutated.

## Decided
- Royce: "CDC - SYD27" is the correct current site record in ehow's `app_data.sites`; "Microsoft SYD27" is the stale one (site rebranded, SKS's own record hasn't caught up either).
- Royce: fix stale/duplicate site rows by deactivating (`active=false`), never deleting — confirmed live that `app_data.sites` has ~30 incoming foreign keys and "Microsoft SYD27" already has 3 real historical `schedule_entries` rows pointing at it, so a hard delete would either fail outright (`NO ACTION` FK) or (if blockers were cleared first) risk silently orphaning other tables that `SET NULL` instead.
- Royce scoped this session to roster (site assignments) only — people-list reconciliation he'll handle himself, leave sync is explicitly a separate future task.
- Confirmed (via source read, not assumption): neither the site-code collisions nor the staff name-mismatches will error a CSV import. `roster-adapter.js`'s `toNormalizedRows()` explicitly degrades — an unresolved site cell saves with `site_id: null` and the code preserved as text; an unmatched staff name skips that person's whole week with a console warning. Nothing throws. A first test import is safe to run today, before any cleanup.

## Deferred
- Run the first real weekly export/import test (SKS NSW Labour → Export Schedule CSV → EQ Field, logged in as SKS org → Import Schedule CSV) — confirmed safe, not actually executed this session.
- Deactivate the two stale site rows in ehow (`Erilyan` / EC6, `Microsoft SYD27` / SYD27) — single-column flip, no code change, awaiting Royce's explicit go to execute.
- ~7 SKS staff missing from EQ Field's `app_data.staff` (hired since the 5 Jul snapshot) + a handful of name-string mismatches — Royce will manage via EQ Field's People admin himself.
- Leave sync (`schedule_entries.leave_type` has no backing `leave_requests` audit trail when written via CSV import) — parked deliberately, own future task.
- Optional, not required: `eq-field/scripts/supabase.js` (~line 992) roster site-map query filters on `active=eq.true` only, not `field_enabled` — found live, doesn't block anything since deactivating the stale rows sidesteps it.
- `SKS-CUTOVER-CRITICAL-PATH.md` and `SKS-FIELD-PARALLEL-RUN-LOG.md` are now stale against what this session found live (both assume manual EQ Field entry hadn't started) — worth a reconcile pass, out of scope for this session.

## Notes

**Substrate correction — the SKS→EQ Field merge is further along than `project-sks-merge-phased` (71 days stale) suggested.** That memory said Phase 1 (login) wasn't ready and Phase 3 (data migration) was "last, highest risk, not started" as of 2026-06-04. Live query of eq-canonical (`jvknxcmbtrfnxfrwfimn`) and ehow (`ehowgjardagevnrluult`) this session found:
- The `sks` org is live in canonical routing (`organisations.slug='sks'`), pointed at ehow (`supabase_url`), tenant_id `7dee117c-98bd-4d39-af8c-2c81d02a1e85`.
- `eq-field/scripts/roster-adapter.js` explicitly allow-lists `sks` in `ROSTER_CANONICAL_TENANTS` — "ETL done + org repointed to ehow." A one-time bulk data migration already ran 2026-07-05 (993 `schedule_entries` rows tagged `nspb-phase3-2026-07-05`) — i.e. Phase 3 (data) already partially happened, not "not started."
- EQ Field's SKS roster has had 317 more rows added natively since, up to 2026-08-05 — real usage, informal and unlogged, silently diverging from SKS NSW Labour's own live data (497 rows in `schedule`, 81 in `people`, 44 in `sites`) for over a month.

**Data-quality finding, not an SKS-side problem.** Traced two site-code collisions (`EC6`, `SYD27`) that would block automatic site resolution on import. Both are entirely on the EQ Field/canonical (ehow) side — SKS NSW Labour's own `sites` table has zero duplicate abbreviations (`group by abbr having count(*)>1` = empty), and its actual current names for both codes don't fully match what's sitting in ehow (e.g. SKS's live EC6 = "CDC EC6 - Schneider", matching *neither* of ehow's two EC6 rows). Root cause: multiple import lineages landing site rows for the same physical facility (17 Roberts Rd, Eastern Creek) without deduping. EQ Field does have a write-time dedup resolver (`app_data.site_resolution_advisory`/`verdict`/`merge_log`) — it correctly flagged the SYD27 collision at 90% confidence the moment the second row was created (28 Jul), but nobody has adjudicated the flag; it's sat unactioned for 2+ weeks. The EC6 collision wasn't flagged at all — the two site names ("Erilyan" vs "CDC - EC6") are dissimilar enough in text that the resolver's name-similarity score apparently falls under even the "raise a flag" threshold despite an exact code match. Worth someone auditing that threshold if this class of collision matters going forward.

**"Erilyan" is not junk** — cross-checked against `sks/active.md`: it's SKS's real Equinix procurement-entity name ("Procurement via Erilyan (Sabrina Lowe, Daniel Palmer)"), which is exactly why it shouldn't be casually deleted even though it's the "wrong" record for the EC6 site-code collision.

**Diagram note for future sessions:** two SVG widgets were rendered this session (weekly sync path; EC6/SYD27 duplicate-site comparison) to walk Royce through the mechanism visually — not persisted anywhere, would need to be redrawn if referenced again.

---

# Session 2026-08-14 (part 2) — eq-solves-service: session-expiry Server Action crash fixed suite-wide (EQ-SOLVES-SERVICE-D)

## Built
- Root-caused Sentry `EQ-SOLVES-SERVICE-D`: `proxy.ts`'s session guard 307-redirects a mid-flight Server Action POST to `/auth/signin` when the session expires mid-tab; the browser's Next.js runtime surfaces that as a generic "unexpected response was received from the server" crash instead of anything mentioning auth.
- Discovered a concurrent session already fixing the exact same issue live, in a locked worktree (`eq-solves-service-sessionexpiry-wt`, branch `fix/session-expiry-server-action-errors`) — caught it mid-edit via repeated diff checks (file mtimes seconds apart, diff growing between polls), so held off writing to that worktree until it stabilised rather than collide.
- That session's fix — a `callAction()` wrapper (`lib/utils/session-expired-action-error.ts`) plus error-boundary fallback — shipped scoped to the maintenance module only, as eq-service [#725](https://github.com/eq-solutions/eq-service/pull/725), merged mid-session.
- Grepped the real scope while waiting: ~160 unguarded `await <name>Action(...)` call sites across 67 files app-wide, not the ~15 the task was originally briefed at. Royce confirmed extending the fix suite-wide.
- Applied the same pattern to the remaining 58 files (~121 call sites) — auth, portal, customers, sites, defects, job-plans, contract-scope, variations, pm-calendar, onboarding, assets, testing (ACB/NSX/RCD), commercials, admin — via 5 parallel background agents, each given the exact wrapper pattern + explicit judgment criteria (only client components, only actions consumed as a result object, skip anything already wrapped or structurally different).
- Synthesis pass found real gaps the batch agents correctly declined to guess at:
  - ~15 call sites return `{ok, error}` instead of the default `{success, error}` shape (`CommercialSheetImporter`, `DerivedScopeWizard`, `CustomerDangerZone`, `ForgotPasswordForm`, `AssetReconciliationScreen`, `ArchiveTable`, `QualityAlertsTable`). Extended `callAction()` with an optional custom-fallback factory (`callAction(action, (error) => customShape)`) so these type-check correctly instead of being silently skipped.
  - 3 handlers (`JobPlanForm`'s delete, `OnboardingWizard`'s finish/skip) had their result discarded but were still an unguarded `await` outside `startTransition` — still crash-prone even though nothing displayed the result. Fixed directly.
  - 2 ACB call sites (`AcbSiteCollection`, `AcbBulkDetails`) had zero error handling at all before this pass — unrelated pre-existing bug, fixed as a side effect.
  - 1 unrelated pre-existing TS narrowing bug in `AssetReconciliationScreen.tsx` (property access on a `.find()` result inside a closure), fixed while in that file.
- Verified: `tsc --noEmit` clean across the whole repo (twice — before and after a branch rebase), `vitest run` 434/434 passed, `next build` compiles clean (Turbopack + TypeScript both pass; page-data collection fails only on a missing local Supabase env file, the same pre-existing/environmental gap #726 already documented for `/api/health`).
- Opened eq-service [#727](https://github.com/eq-solutions/eq-service/pull/727) — holds for Royce's merge approval, no merge, no deploy.

## Decided
- Royce: wait for the concurrent session rather than risk collision (first checkpoint).
- Royce: extend the fix to the remaining 58 files himself-via-Claude, once true scope was known (10x the original estimate).
- Royce: commit everything as one PR.
- Royce: explicitly approved `git checkout -b` (normally classifier-blocked) to do the branch cleanup properly rather than skip it or leave a messy diff.
- Royce: run `next build` and `vitest` before calling it done.

## Deferred
- No manual browser smoke test yet — need to actually expire a session mid-form-submit on a few touched pages and confirm the friendly message renders. Sandbox has no path to a real authenticated browser session here.
- Old branch `fix/session-expiry-server-action-errors` still has its now-redundant `df74500` commit sitting on origin (harmless, content-identical to merged #725) — worth deleting next time someone's in this repo.
- PR #727 itself — not merged, holds for Royce's review.

## Notes
**Mid-session PR merge caught and handled.** #725 merged (by the *other* concurrent session's work) partway through this session, while this session was already committed to a branch derived from the pre-merge state. Caught via `gh pr list --head` before pushing — a naive push would have opened a PR that re-showed all 9 already-merged maintenance files as "changed" (three-dot diff against the stale fork point), which would have been confusing to review even though content-identical. Fixed by stashing the 57 new/changed files, branching fresh off current `main`, and popping the stash back onto that — clean 57-file PR diff, confirmed via `gh pr view --json files`.

**Result-shape inconsistency worth remembering for future eq-solves-service work.** This app's Server Actions return at least two different result shapes in the wild: `{success, error}` (the majority) and `{ok, error}` (danger-zone/import/derive actions, plus a couple of admin tables). Any future generic action-wrapping work here needs to check which shape it's dealing with before assuming — `callAction()`'s new optional custom-fallback argument is the sanctioned way to handle the second shape going forward.

**Batch-agent judgment held up well but wasn't perfect** — of ~120 call sites delegated to 5 parallel agents, only a handful of genuine misses surfaced on manual review (3 discarded-but-unguarded results, ~15 shape mismatches correctly flagged-not-guessed, 1 file agent over-cautiously skipped a case that turned out to be safe with the default fallback). Worth the manual synthesis pass rather than trusting the batch output wholesale.

---

# Session 2026-08-14 (part 3) — eq-shell permission-drift guard + sprint execution + admin hard-delete for archived users

## Built
- **eq-shell**: permission-enforcement drift guard (ratchet pattern mirroring eq-field's PR #684) — `permission-enforcement-drift.test.ts` + a checked-in baseline of currently-dead (declared-but-unenforced) `PermKey`s, fails only on *new* regressions. PR #1322 merged. `audit.rollback`, `intake.import`, `quotes.view` wired up and removed from the baseline over the session; ended at 13 dead keys (down from the original count).
- **eq-shell**: #1318 (audit.rollback + entity-archive gates), #1329 (quote-job-consumer reads `canonical_events` directly, skips a self-HTTP hop), #1330 (O&M manuals reclassified as reference library, not sign-off), #1332 (deleted dead `CoreHome.tsx` prototype) — all merged.
- **Sprint round**: scoped an 11-item sprint via an 8-question AskUserQuestion round (everything in the queue except the 3 named open security findings), executed on "go". Covered eq-cards (OCR 502 triage, OCR 401-retry try/catch isolation, profile auto-provision race guard against mid-call session death, wallet banner-stacking collapse/expand fix, PR #235's nudge-card collapse — review caught a real regression, `AddToHomeScreenNudge` silently unreachable when `sectionCount<=0`, fixed same PR) and eq-service (`archiveCheckAction`/`archiveTestingCheckAction` deleted_at stamping #720, quality-guardian admin UI #721, stale branch deleted). eq-cards deployed via explicit `Build & Deploy` workflow dispatch (confirmed it does NOT auto-deploy on merge, unlike eq-shell/eq-service).
- **eq-service**: restore-side counterpart to #720 — `restoreEntityAction` wasn't clearing `deleted_at` on restore, so a restored maintenance/testing check stayed invisible. PR #726 merged, confirmed live (commit_ref exact match, ~92s deploy).
- **eq-shell**: Royce asked what happens to archived users and for a real hard-delete (kept seeing old test accounts resurface). Investigated live — Archive only sets `active=false`; ADR-005 keeps the row 7 years then anonymises, never deletes; no admin hard-delete existed anywhere in the suite. Scoped via 3 questions (tenant managers too, block-and-report on live references, type-the-name confirmation), verified jvkn's actual FK graph directly (corrected a stale 3-week-old code comment that listed a non-existent blocker), built `delete-user.ts` + a "Delete permanently" section on `AdminEditUser.tsx`. PR #1337 merged, confirmed live.

## Decided
- Royce: merge #1318 before re-running the drift ratchet.
- Royce: scope the sprint to everything except the 3 named security findings; ran the Q&A, then "go" to execute.
- Royce: eq-cards needs an explicit deploy dispatch, separate from merge.
- Royce, on the hard-delete feature: same roster as Archive (not platform-admin-only); block-and-report on any live reference rather than auto-reassigning it; require typing the person's name to confirm, not just a plain click (unlike Archive — this can't be undone).
- Royce: merge #1337.

## Deferred
- **Manager click-through on core.eq.solutions still not done** — asked for twice this session, no browser session available in this environment either time.
- **New**: the hard-delete UI itself hasn't been click-tested live — built and verified via `tsc`/lint/the permission-drift guard only.
- **Minor**: `admin.deactivate_user` is declared in the permission matrix but never actually enforced anywhere — both the archive action and the new delete action gate on `admin.edit_user` instead. Harmless today (same role grants), noted in `eq/pending.md` in case the two are ever meant to diverge.

## Notes
**Session gate (`/brief`) caught a live shared-checkout hazard before any file was written.** The root `eq-shell` checkout was parked on `feat/staff-licence-multi-document-queue` — a different concurrent session's active branch (PR #1335) — not `main`. Built the hard-delete feature in an isolated worktree off `origin/main` instead, per the standing shared-checkout-contention lesson.

**Live FK verification beat trusting an existing code comment.** `retention-purge.ts`'s header comment (written ~3 weeks prior, for the Cards self-delete flow) listed `worker_invites.created_by` as a blocking reference. A direct query of jvkn's `information_schema` for every FK into `shell_control.users(id)` showed no such constraint exists on this plane at all — the real blocker list is 5 tables (`cards_field_approvals`, `phone_link_review`, `pin_reset_tokens`, `tenant_role_overrides`, `user_invites`), not 6. Building the precheck off the comment instead of the live schema would have shipped a wrong blocker list.

**Permission-key architecture clarified for future work in this file.** `src/permissions/matrix.ts`'s browser-side `MATRIX`/`useCan()` and `netlify/functions/_shared/permissions.ts`'s server-side `can()`/`requirePerm()` resolve role-default grants from two *different* sources — the former from the local matrix file, the latter entirely from the external `@eq-solutions/roles` package. A brand-new Shell-local `PermKey` with a real *role default* (not just a per-user security-group grant, which the existing `STAFF_PERMS`/`staff.manage_teams` pattern already supports cleanly) genuinely cannot be server-enforced without either a cross-repo package release or reusing an existing package-backed key — which is why the hard-delete feature reuses `admin.edit_user` rather than minting `admin.delete_user`.

**Near-miss: a `git checkout -- <files>` during session-close almost discarded a concurrent session's uncommitted changelog/pending/session-log edits.** The shared `eq-context` checkout was 11 commits behind `origin/main` when the close commit hit the `stale-main-gate`; reverting the 4 touched files to "resync" wiped their in-progress EQ-SOLVES-SERVICE-D write-up from the working tree along with this session's own edits. No data was actually lost — that session had already committed and pushed to `origin/main` before this checkout went stale, confirmed via `git show origin/main:<file>` before redoing anything — but it was luck, not the process, that made it recoverable. **Lesson for next time: in a repo with known shared-checkout contention, never `git checkout -- <file>` to "reset and retry" without first confirming — via `git show origin/main:<file>` or a stash — that nothing uncommitted-but-not-yet-pushed from another session lives in that working tree.** Finished the close from an isolated worktree instead, which is what should have happened from the start.

---

# Session 2026-08-14 (part 4) — Dashboard map → own page, Map hover shows names, cache-buster hotfix

## Built
- **eq-field #689 (v3.5.491)**: Dashboard's embedded map extracted to its own dedicated "Map" nav page (`scripts/site-map.js`) — root-caused ~25-30 versions of squash/centering bugs to a live Leaflet canvas sharing a container with the Dashboard's own frequent re-renders; own page structurally avoids it rather than patching the symptom again. Staff Reviews tab (moved to eq-shell) removed in the same PR per Royce's instruction.
- **eq-field #690 (v3.5.492)**: `get_site_headcount_for_map` RPC extended with a `people text[]` column (migration `20260814_field_site_headcount_people_names.sql`, applied live to ehow). Map page + Roster Overview mini-map hover tooltips now list rostered names, capped at 15 before "+N more". Cluster-pin tooltips (multiple sites collapsed into one pin) deliberately left headcount-only.
- **eq-field #691 (v3.5.493)**: Hotfix — `index.html`'s `app-state.js?v=` cache-buster tag was stuck at v3.5.486 across 6 releases (v3.5.487-492), so returning browsers kept serving a year-cached stale copy (`Cache-Control: immutable, max-age=1yr`). Diffed content confirmed only the version string had drifted, nothing functional. Also fixed `core-bundle-a1.js`'s tag, found one release stale during the same audit (PR #658/v3.5.464). Verified live on production post-merge.

## Decided
- Royce: map squash problem → separate page, not another patch ("Simple is always best").
- Royce, via `/decide`: map-emoji idea → built as a Lucide sidebar icon instead of an emoji (repo convention).
- Royce: remove Staff Reviews tab outright, no migration needed — already moved to eq-shell.
- Royce: explicit go-ahead for the live DDL migration on ehow (production DB) — sandbox's auto-mode classifier blocks this by default, asked via AskUserQuestion.
- Royce: "merge it" on both #690 and #691 — the second one only after I explicitly held for confirmation per the "fix ≠ deploy" rule, since discovering a bug doesn't imply authorization to ship the fix.

## Deferred
- No CI check exists to catch the cache-tag-vs-content drift bug class — this is the 4th time it's shipped (v3.5.409/410, v3.5.479, v3.5.482, now). Flagged, not built.
- No live click-through of any of this session's UI changes — sandbox has no network path to a real signed-in session (confirmed again). Verified instead via direct DB queries, production file fetches, and the full local test/lint suite.
- A full systematic audit of all ~30 `?v=` cache-buster tags in `index.html` wasn't done — only the 2 found via targeted investigation were fixed; others were spot-checked against their last-modifying commit and found consistent.
- New request mid-close: audit the whole leave-approval email/digest wiring (frequency, templates) after an executive wasn't notified of an approved leave request; also simplify the CC-list modal's copy (drop the "v3.5.384" technical reference). Picked up immediately after this close.

## Notes
**Root cause of the recurring cache-buster bug**: `scripts/app-state.js` is deliberately kept outside the hand-merged `core-bundle-*.js` system (synchronous boot dependency), so `build-bundles-drift.yml`'s CI gate — which only checks bundle-vs-source consistency — has no visibility into it. Every fix so far (including this session's) has been reactive, found by a human or an agent noticing the symptom, never by tooling.

**Self-caught mistake mid-session**: a migration comment claimed "`CREATE OR REPLACE FUNCTION` can widen a return row" — wrong; Postgres throws `42P13` on any OUT-parameter shape change. Caught immediately when the first live `apply_migration` call failed with that exact error; fixed with a `DROP FUNCTION IF EXISTS` step before recreating (mirroring an existing precedent in the same migration lineage, `20260801`'s arg-count change).

**I made the cache-buster mistake myself, in this exact session** — both #689 (v3.5.491) and #690 (v3.5.492) bumped `APP_VERSION` inside `app-state.js` without bumping the file's own external `<script src>` tag, despite 3 prior dedicated incident writeups already sitting in this repo's own changelog banner. Royce caught it from the outside before I did.

**Second near-miss avoided, same class as "part 3"'s note above.** This session hit the identical `stale-main-gate` block on the shared checkout, now 15+ commits behind (2 other concurrent sessions' work). Did not `git checkout -- .` to reset. Instead diffed the shared checkout's base commit against `origin/main` first, found 2 other sessions had inserted their own new `pending.md`/`sessions/2026-08-14.md` sections at the same insertion point, and merged this session's section in alongside theirs via a fresh isolated clone rather than overwriting.

---

# Session 2026-08-14 (part 5) — Suite-wide Sentry sweep: identity-collision root cause fixed, 2 bugs shipped

## Built
- **Closed out a 17-issue Sentry sweep across all 4 apps** — 13 resolved (already-fixed-and-confirmed, or fixed this session), 4 ignored (`ignoreMode: untilEscalating`) as one-off noise that never recurred, 1 spun off as its own background job.
- **Root-caused the EQ-SHELL-Z / EQ-SHELL-11 / EQ-SHELL-1M identity-collision alerts to one mechanism**: a session dying mid-signup between the RPC call and Postgres evaluating `auth.uid()` inside `eq_cards_auto_provision()` (jvkn) let `auth.uid()` resolve NULL, inserting a broken no-email/no-phone "Personal Wallet" account. Confirmed the code fix (eq-cards PR #234, migration 0123) was already live on jvkn — didn't need re-shipping. Diagnostic signature for next time: a `shell_control.users` row with NULL email/phone, tenant = `__personal__`, `created_at` within seconds of another real account's `last_login_at`.
- **Fixed the one known victim's live data** (Royce approved via AskUserQuestion before touching identity data): `app_data.staff.user_id` on ehow repointed from the phantom account back to the real one; phantom account and its tenant membership set `active = false` (not deleted).
- **eq-cards [#236](https://github.com/eq-solutions/eq-cards/pull/236) merged + deployed live** — fixes EQ-CARDS-1H. `scan_ocr_flow.dart` was forwarding undecodable photo bytes straight to the OCR edge function instead of surfacing the existing "photo couldn't be read" message.
- **eq-shell [#1336](https://github.com/eq-solutions/eq-shell/pull/1336) merged + deployed live** — dashboard scroll-chaining fix. `.eq-hub__sidebar`/`.eq-hub__content` gained `overscroll-behavior: contain` so scrolling past either pane's own limit no longer bubbles to the outer page and exposes blank body background below the fixed-height shell. Diagnosed live from Royce's screenshot + browser console diagnostics; deliberately left `body`/`html` untouched after confirming the login page and quote portal both rely on natural body scroll.
- **Dispatched the background `bug-fixer` agent for eq-solves-service's session-expiry Server Action crash** (EQ-SOLVES-SERVICE-D) — its first fix landed as [#725](https://github.com/eq-solutions/eq-service/pull/725), merged + deployed live. A concurrent session picked up and extended the remaining ~58 files/121 call sites (see "part 2" above / `eq/pending.md`); not restated here to avoid duplicating that write-up.
- Corrected the eq-shell production-deploy note in global `CLAUDE.md` twice: first pass wrongly diagnosed a broken GitHub→Netlify webhook; second pass found the real cause (the site's own `cdp_enabled_contexts` setting excludes `production` from continuous deployment — deliberate, not broken) and documented the manual git-archive deploy workaround.

## Decided
- Royce approved the identity-collision live data fix before any write, via AskUserQuestion.
- Royce gave explicit merge + deploy instructions separately for eq-cards #236 and eq-shell #1336 ("merge eq-cards #236" → "deploy eq-cards"; "merge PR #1336" → "deploy eq-shell") — each treated as its own authorization, per the standing "fix ≠ deploy" rule.
- Royce asked to spawn the eq-solves-service hardening work as its own background session rather than have it done inline.

## Deferred
- **EQ Service: ~59 more files need the same session-expiry Server Action guard** — one instance fixed and confirmed (#725), the rest tracked in a spawned background job, not this session.
- **`.eq-hub`/`.eq-hub__content`/`.eq-hub-content` class-name collision between eq-shell's own `App.css` and `@eq-solutions/ui`'s `AppShell.css`** — noticed while tracing the scroll bug, not its cause, not yet turned into an actionable task.

## Notes
**Diagnostic pattern worth remembering**: this is the third identity-collision family found in this project (see memory `phone-format-inconsistency-identity-bugs` for the phone-normalization family). The shape — NULL email/phone + personal-wallet tenant + creation timestamp adjacent to a real login — is now documented in memory (`sentry-sweep-2026-08-14-identity-collision-root-cause.md`) so it doesn't need re-deriving from scratch next time a similar alert fires. Migration 0123 only guards `eq_cards_auto_provision()` itself — a different trigger could still land a similar phantom row elsewhere.

**Self-caught and reverted a riskier fix mid-session**: first attempt at the scroll bug added `body { overflow: hidden }` globally in `index.css` as a "belt and braces" move alongside the `overscroll-behavior` fix. Checked `auth.css`'s `.eq-login-page` (relies on `min-height: 100svh` + natural body scroll for short viewports) and the quote portal before finalizing, found the global change would have broken login-page scrolling, and reverted it — shipped only the scoped, pane-level fix.

**Third instance of the shared-checkout contention pattern, this session** — the local `eq-context` clone had a stale unpushed commit sitting 1-ahead/16-behind `origin/main` before this close even started, and `git stash pop` (recovering this session's own edits after a blocked rebase attempt) produced conflicts in 3 unrelated auto-generated files (`digest.md`, `suite-state.md`, a sprints file) never touched this session. Backed up this session's 5 target files first, left the shared checkout's conflicted stash untouched (stash entry preserved, not dropped), and did the actual commit + push from a fresh isolated clone instead — consistent with "part 4"'s and "part 3"'s notes above on the same recurring problem.

**Tooling note**: a spawned `spawn_task` chip (`task_31343bc5`, the deploy-doc correction) couldn't be programmatically dismissed after the work was completed directly in-session — `dismiss_task` reports it's already in a "started by the user" state and can't be withdrawn. Harmless (the underlying doc correction is already done), but the chip itself will need manual cleanup on Royce's side if it's still showing.

---

# Session 2026-08-14 (part 6) — EQ Service: closed the remaining unprotected /admin/* pages, merged + deployed live

## Built
- **eq-service [PR #728](https://github.com/eq-solutions/eq-service/pull/728) merged + deployed live** — gated the 6 remaining ungated `/admin/*` pages (hub, users, settings, media, reports, backup) with `getApiUser()` + `isAdmin(role)` + `redirect('/dashboard')`, matching the pattern already live on `admin/archive` and `admin/quality`. Before this, any signed-in technician who typed the URL directly could see the full team roster (names/emails/phones), branding settings, and reach the workspace-backup download page.
- Live recon (not the task's stale premise) found `main` already had 3 of 9 admin pages gated: `archive`/`quality` via `isAdmin`, and `imports`/`activity` via a deliberately *narrower* `canViewAuditLog` gate (manager + supervisor, PR #709, Royce-confirmed at the time). Gated the remaining 6 individually rather than adding one blanket `isAdmin` layout gate over the whole `/admin/*` segment — a layout gate would have silently re-tightened imports/activity back to manager-only, undoing that earlier decision.
- Verified pre-merge in an isolated worktree (root checkout was mid-session on an unrelated branch, `fix/integration-ci-app-data-bootstrap`, 38 commits behind main with its own uncommitted work — not touched): `tsc --noEmit` clean, `eslint` clean, full `npm run build` clean, `permission-enforcement-drift.test.ts` ratchet 4/4 pass with no baseline change needed (`isAdmin`/`admin.list_users` was already enforced elsewhere in the corpus).
- Merged (squash `fae2ee5`) on Royce's go. CI's one failing job, "Integration tests (Supabase local)", is the documented pre-existing `schema "app_data" does not exist` bootstrap gap (confirmed by reading the job log) — the same issue the unrelated branch above exists to fix, not a regression from this diff.
- Confirmed live via Netlify MCP: the new production deploy's `commit_ref` matches the merge commit exactly, secret scan clean (930 files, 0 matches), published on `service.eq.solutions`. Also confirms eq-service's documented "Netlify auto-deploys on push to main" behaviour is accurate today — unlike eq-shell's documented broken case, no manual deploy trigger was needed.

## Decided
- Royce, via AskUserQuestion: push the branch and open the PR now rather than hold for a live click-test (no non-manager EQ Service login available in this environment).
- Royce: "merge the PR once CI passes" — merged after confirming the one failing check was the known pre-existing integration-test gap, not a regression.

## Deferred
- **Not click-tested by a real non-manager account** — no such login was available in this environment. Worth Royce confirming a technician account now gets bounced off `/admin/*`. _(added 2026-08-14)_

## Notes
Root checkout (`C:\Projects\eq-solves-service`) still has unrelated uncommitted work on `fix/integration-ci-app-data-bootstrap` — untouched, already tracked in pending.md as awaiting Royce getting Docker running.

**Fourth instance of the shared-checkout contention pattern, same day** (see "part 3"/"part 4"/"part 5" notes above on the same recurring problem). `C:\Projects\eq-context` had a pre-existing, unresolved `git stash pop` conflict on `digest.md`/`suite-state.md`/a sprints doc — present since before this session started, visible as `UU` in the very first `git status` — plus local `main` sitting 1 commit ahead / 22 behind `origin/main`. Left the conflicted stash untouched and did not attempt to resolve it. Diffed `HEAD` against `origin/main` on the 3 files this session actually touched (`eq/pending.md`, `eq/changelog/eq-solves-service.md`, `sessions/2026-08-14.md`) before writing anything further — found origin already had 4 more concurrent sessions' work merged in (parts 2-5 of this same file) that the local checkout's stale HEAD didn't have. Rebuilt this session's 3 additions against a fresh isolated clone of `origin/main` instead of the stale local content, to avoid silently reverting that work.

---

# Session 2026-08-14 (part 7) — Closed out the image-size DoS thread; light repo hygiene, no product changes

## Built
- Nothing shipped to any product repo. Pure verification + local git hygiene.

## Decided
- Royce: "yes, delete both" — approved deleting the stale local branch and the two leftover fuzz-test files found during verification.

## Deferred
- None new.

## Notes
**Closed the loop on the thread this session originally started from (image-size DoS patch).** Confirmed eq-shell [#1288](https://github.com/eq-solutions/eq-shell/pull/1288) ("patch image-size DoS + re-vendor to restore the nanoid fix") merged 2026-08-08 and is live — its merge commit (`46829c6f`) is a confirmed ancestor of current `origin/main`, and eq-shell has deployed live multiple times since (including this same day's #1336). Nothing further needed there; this was pure verification, no new fix required.

**Local git hygiene in `C:\Projects\eq-shell` (not a git-trackable change — nothing to commit):**
- Deleted the local-only branch `fix/image-size-dos-patch` (`5e7f749c`) — never pushed to origin (confirmed via `git ls-remote`); its 2 commits were squash-merged into main under different hashes via #1288, so `git merge-base --is-ancestor` correctly reported "not merged" even though the content had shipped (same squash-merge trap documented in memory `squash-merge-branch-contains-trap.md`).
- Deleted 2 untracked files sitting in the working tree since ~2026-05-18/19 — `eq-intake/eq-platform/packages/eq-intake/fuzz-overnight.mjs` and `eq-intake/eq-platform/packages/eq-validation/fuzz-overnight-coercers.mjs`. Unrelated to the image-size thread — a leftover CSV-parsing fuzz harness for `eq-intake`, self-labeled in its own header comment as "Throwaway file. Deleted after results captured in OVERNIGHT-REVIEW." Never actually deleted; results (if any) were never captured anywhere in this repo.
- Both untracked/local-only, so there was nothing for `git commit` to pick up — confirmed via `git status` (clean working tree) after deleting.

**Also confirmed, no action needed:** eq-service PR [#727](https://github.com/eq-solutions/eq-service/pull/727) (the suite-wide session-expiry Server Action extension flagged as "needs you" on this same day's earlier close) was already merged directly by Royce on GitHub (`f7b378b`, 2026-08-14T03:26:53Z) before this session attempted to merge it — a genuine race with Royce acting outside the session, not a bug. `eq/pending.md`'s eq-solves-service section updated to reflect merged status.

---

# Session 2026-08-14 (part 8) — eq-field: Leave notification gaps closed, digest widened to 4 weeks, Email Templates pilot shipped

## Built
- **eq-field #692 (v3.5.494)**: audited leave's 3 separate approve paths (full modal, dashboard quick-approve, magic-link email button) — only 1 emailed the CC list. Dashboard quick-approve sent literally no email to anyone; fixed to mirror the modal's email/audit/analytics side-effects, plus picked up a self-approval guard it was missing. Magic-link approve (`netlify/functions/approve-leave.js`) never CC'd the configured list in canonical mode (live for SKS since 2026-07-29, confirmed via live Netlify env check) — fixed via a new `canonResolveCcEmails()` in `_shared/leave-canonical.js`. Legacy plane (disposable eq/demo tenant) left as a documented, deliberate gap. Also: CC-list modal copy simplified (dropped a raw version-number reference), and Timesheets' Job Numbers side panel fixed (was empty due to a lazy-load ordering bug — same canonical data source as the working standalone page, script just wasn't loaded on that tab).
- **eq-field #693**: `supervisor-digest` (Supabase Edge Function) leave section widened from a 1-week to a 4-week lookahead, grouped into 4 week-headed mini-tables rather than one flat list. Deployed live via Supabase MCP (version 11→12, `verify_jwt:false` preserved) after a first deploy attempt failed on file-path nesting (the entrypoint needs a `source/` prefix to resolve its `../_shared/*.ts` imports — not obvious from the API, found by reading the live-deployed function's own file layout).
- **eq-field #695 (v3.5.496, originally drafted as v3.5.495)**: Email Templates pilot — new `Testing → Email Templates` admin page (Supervision-gated) lets a supervisor reword the subject + intro paragraph of leave.js's 3 email types without a code deploy. Only the prose is DB-editable, not the surrounding HTML structure — deliberately not a full-HTML textarea. Live preview + "send test to myself" before saving; any missing row/failed fetch/bad edit falls back to today's hardcoded copy, never blocks a send. New `public.email_templates` table, new `scripts/email-templates.js` (shared helpers, extracted from `leave.js` for reuse rather than re-bumping its line ceiling a second time this session) + `scripts/manage-email-templates.js` (the editor). Migration applied live to ehow.
- **Real mid-session collision caught and resolved**: another PR (#694, "Teams route guard") merged directly to `main` using the identical version number (v3.5.495) while #695 was in flight — same changelog-banner and `showPage()` insertion points. Rebased cleanly onto the new `main`, kept both changes, renumbered mine to v3.5.496 throughout (6 files), re-verified everything, force-pushed the unmerged branch (safe — own branch, not shared history).

## Decided
- Royce: the original "exec not notified" report was from SKS NSW Labour (the old, separate app) — wanted EQ Field's own wiring verified/fixed independently, not that app touched.
- Royce, checking the live CC list (only himself on it): will add the missing executive himself via the app, once confirmed they're set up as a Supervisor record.
- Royce, via `/decide`: digest 4-week extension → "yes, but group by week" (not a flat list).
- Royce: apply the digest's live Netlify-env verification and Supabase deploy — explicit go each time (live production actions).
- Royce, via `/decide` on the email-templates question: didn't take the recommended bounded-pilot scope as a given — ran the six-step pass, which surfaced that the original 4-PR plan's value was unfalsifiable (no stated goal) and its biggest risk was an inert half-built admin screen; landed on a smaller, immediately-functional pilot (leave.js's 3 templates only) instead, and Royce's "then build this" confirmed proceeding on that scoped-down version.
- Royce: "yes, apply + merge + ship" for the email-templates migration — explicit go for the live DDL write, matching every other production DB write this session.

## Deferred
- **No "executive"/stakeholder concept exists anywhere in this app** — the CC list and the Friday digest opt-in both only ever draw from people flagged as Supervisors. Broadening that is a real product decision for Royce, not a code fix.
- **Legacy (eq/demo tenant) magic-link approve path still doesn't CC the configured list** — deliberately not built; that tenant has no real executives to notify, and building a second JWT-minting mechanism for it wasn't worth the surface area. Documented in `sendStatusEmail()`'s own header comment.
- **Digest/approve-leave.js/ts-reminder templates NOT added to the Email Templates pilot** — deliberately held per the `/decide` pass, until the pilot (leave.js's 3 templates) has actually been used at least once.
- **No live click-through of any of this session's changes** — same sandbox network-wall limitation as every prior entry in this log. Verified instead via live DB queries, live Netlify env checks, a live Supabase Edge Function deploy, and the full local test/lint suite.

## Notes
**Confirmed a real doubt before relying on it**: a background sub-agent's digest audit surfaced a 5-day-stale review claiming `LEAVE_CANONICAL_JWT_SECRET` might still be missing on Netlify, which would have meant the canonical-mode CC fix never actually executes in production. Didn't take the stale report at face value — queried the live Netlify project directly and confirmed all 4 `LEAVE_CANONICAL_*` vars are genuinely set in the `production` context.

**Version-collision handling worth remembering for next time**: this repo's shared, fast-moving `main` means a version number can be claimed by another concurrent PR between branching and merging even within one session. The fix pattern that worked cleanly: rebase (not merge) onto the new `main`, resolve the (usually simple, same-insertion-point) conflicts by keeping both changes, then grep the whole diff for every instance of the now-claimed version string and renumber — git's line-based merge will silently NOT flag a "conflict" when both sides independently produce identical text (e.g. two PRs both bumping `APP_VERSION` to the same literal string merge with zero complaint), so that renumbering step has to be done by hand, not assumed complete just because the rebase succeeded.

**Supabase Edge Function deploy path isn't obvious from the MCP tool alone**: the entrypoint file needs an explicit `source/` path prefix (`entrypoint_path: "source/index.ts"`, file `name: "source/index.ts"`) for its own relative `../_shared/*.ts` imports to resolve — a first attempt using the tool's own literal example (`entrypoint_path: "index.ts"`) failed to bundle. Found the correct shape by reading the already-deployed function's own file layout via `get_edge_function` before retrying, rather than guessing twice.

**Same shared-checkout staleness hazard hit again at session close, same lesson as "part 3"'s own note.** The root `eq-context` checkout had unresolved `UU` merge conflicts on unrelated files from another session, and its on-disk `eq/pending.md`/`sessions/2026-08-14.md` were missing two whole sections (`part 2`, `part 3`, and the eq-shell hard-delete/session-expiry pending items) that had already landed on `origin/main` — the local working tree was never re-synced after those pushes. Did not touch the shared checkout's conflicted state. Diffed the shared checkout's edited files against a fresh clone of `origin/main` before writing anything, found the drift, and spliced this session's new content into the clone's actual current version rather than pushing the stale-based edit (which would have silently reverted the missing sections). Same isolated-clone pattern as every prior close this session.

---

# Session 2026-08-14 (part 9) — eq-field: fixed the bug part 8's own pilot shipped with (Email Templates nav button invisible)

## Built
- **eq-field #696 (v3.5.497)**: the `Testing → Email Templates` nav button shipped in part 8's #695 was permanently invisible to everyone, in every role — it carried both the correct `edit-only` class AND a leftover inline `style="display:none"` copied from the neighbouring Diary button's markup, and an inline style always wins over the CSS class rule that's supposed to reveal it. Fix: removed the inline style, matching every other working `edit-only` nav item's pattern (class alone, no inline style). Merged and confirmed live via `field.eq.solutions/sw.js` showing v3.5.497.

## Decided
- Royce spotted it himself — screenshotted his own Supervision-unlocked sidebar with the button missing, asked "where does the editor live? / where should it be?" That's what surfaced the bug; a plausible-but-wrong first lead (checking whether the "Manage" nav section itself was misbuilt) was ruled out before finding the actual one-line cause.
- Royce, on the sibling bug: "leave Diary invisible for now" — the Diary nav button likely has the exact same inline-style mistake (nothing clears its inline style either, confirmed via grep), but that's explicitly not being touched this session. Stays tracked as its own follow-up chip (`task_5077d8c5`), not fixed inline and not silently dropped.

## Deferred
- **Diary nav button — likely the same invisible-nav bug, deliberately left alone.** Flagged as a separate task chip, not actioned. Royce's explicit call, not an oversight.

## Notes
**This is the second bug in this exact "pilot shipped with an unnoticed defect" shape today** — part 8 shipped a working feature that was completely unreachable in the UI, caught only because Royce happened to look for it. Worth remembering for future nav-item additions in this codebase specifically: `edit-only`/permission-gated nav buttons here use the CSS class alone, never an inline `style` — copying markup from a neighbouring button without checking whether it also carries a stray inline style is the exact failure mode that bit twice in one day (this button, and the still-open Diary one).
---

# Session 2026-08-14 (part 10) — eq-cards: invite-claim IDOR fixed, 3-day claim outage found + fixed, stale-invite cleanup + guard shipped; eq-context stash-pop conflict fixed for good

## Built
- **eq-cards migration `0124`, PR [#239](https://github.com/eq-solutions/eq-cards/pull/239) merged + deployed live** — a live code audit found `eq_cards_claim_invite` only checked `auth.uid() IS NOT NULL`, never that the OTP-verified phone matched the invite's target phone, letting one authenticated worker claim a colleague's invite by looking up their phone number via `eq_cards_lookup_invite_by_phone`. Fix adds the phone check, preferring `workers.phone` over `profile_data->>'phone'` when a worker record already exists (a real live case where the two disagreed was found during verification — using `profile_data` unconditionally would have broken a legitimate claim).
- **Same migration also fixed a live 3-day outage nobody had noticed**: an earlier migration (0121) had silently stripped `eq_cards_claim_invite`'s `GRANT EXECUTE ... TO authenticated` — this project's `eq_enforce_function_privacy` event trigger revokes execute on every function edit unless explicitly re-granted in the same migration, and 0121 hadn't. Zero invite claims had succeeded since 2026-08-11. Found via a live grant comparison before applying the phone-check fix, folded into the same migration rather than shipped separately.
- Added a client-facing error message for the new `invite_phone_mismatch` case (`claim_invite_screen.dart`, `supabase_error_handler.dart`) and an SEC-25 entry to the security register — committed this session (see below).
- **Royce asked**: "Will has a live account already? he's updated his licences recently — can we set up a way to remove these duplicate invites and check against what's live?" — investigated: confirmed via `workers.user_id` + recent licence activity that William Brown already had a claimed, active account, but an old unclaimed invite for him was still open. Cleaned it up directly.
- **eq-cards migration `0125`, PR [#241](https://github.com/eq-solutions/eq-cards/pull/241) merged + deployed live** — new `eq_cards_admin_list_stale_invites` admin RPC, so any worker who's already claimed an account but still has an invite outstanding can be found without an ad-hoc query.
- Traced William's stale invite to its actual root cause via `audit_log`: a 2026-07-22 identity-merge repair set his `workers.user_id`, but an invite was still created for him 8 days later. The client already hides the "Send Invite" button once a worker is claimed (`admin_worker_detail_screen.dart` correctly checks `worker.isClaimed`), but that's a UI-only guard — `eq_cards_admin_create_invite` itself never checked it, so a stale cache, a race, or any future UI path could hit the same gap.
- **eq-cards migration `0126`, PR [#242](https://github.com/eq-solutions/eq-cards/pull/242) merged + deployed live** — `eq_cards_admin_create_invite` now rejects creating an invite for a worker who already has `user_id` set (`worker_already_has_account`, P0026), closing the gap at the source instead of relying on future cleanup.
- Both deploys (after #239, and after #241+#242) confirmed green via CI and a live-site check on `cards.eq.solutions`.
- **Fixed the eq-context stash-pop conflict itself, not just worked around it.** `digest.md`, `suite-state.md`, and a sprints doc had literal unresolved `<<<<<<< Updated upstream` / `>>>>>>> Stashed changes` markers baked into their working-tree content (no active merge tracked — `MERGE_HEAD` absent), left over from a `git stash pop` that conflicted and was never finished. In every hunk the "Updated upstream" side was the newer, correct state; resolved all of it to that side. `git stash push`'d the working tree before rebasing and discovered `digest.md`/`suite-state.md`'s real committed content (at the true `HEAD` before rebase) was already newer than either side of that stash conflict — dropped the manual splice for those two files entirely rather than commit a step backward. Local `main`'s one other stray commit (`chore(index-drift): index attachment-upload-closeout sprint doc`) turned out to already be superseded on `origin/main` (someone else fixed the same `index-drift.yml` root cause independently) — dropped it during the rebase rather than carry a redundant commit forward.

## Decided
- Royce: "yes, do 1 and 3 now" — ship the client error mapping and security-register logging alongside the core fix, same session.
- Royce: "apply it to jvkn" (twice) — applied both the phone-check migration and the stale-invite RPC live without holding for a separate review pass.
- Royce: "yes, build and apply it" — build the root-cause fix (0126) rather than stop at the manual cleanup + detection tool.
- Royce: "merge and deploy" (twice) — both rounds of PRs merged and the deploy workflow dispatched same-session.
- Royce: "sort out the eq-context stash conflict" — fixed properly (see Built) rather than left for a future session to hit again.

## Deferred
- Local eq-cards branch `fix/prevent-invite-for-claimed-worker` is stale (already squash-merged as `f481951`) — safe to delete, not done. _(added 2026-08-14)_

## Notes
**Sixth instance of the shared-checkout contention pattern today, and the first to fix the actual root cause instead of routing around it.** Every prior instance today ("part 3" through "part 9") correctly worked around the stale/conflicted local checkout via an isolated clone, but left the underlying `digest.md`/`suite-state.md`/sprints-doc stash-pop conflict sitting there for the next session to hit again. This session fixed that conflict directly (see Built) — the shared checkout should no longer be `stale-main-gate`-blocking on a leftover stash conflict for subsequent sessions today. The local `eq-shell`-hooks-style `eq-guard` blocked a direct `git commit`/`git rebase` in the shared checkout twice during this fix (`stale-main-gate`, then a dedicated rebase guard pointing at documented failure mode F9) — both correctly, both resolved by doing the actual commit/rebase/push from an isolated clone of `origin/main` per the guard's own printed instructions, exactly like every other session today. Needed a second fetch+rebase mid-close — another session's "part 9" (also numbered part 9, before this renumbering) and the nightly digest/suite-state regeneration both landed on `origin/main` between the first push attempt and this one; renumbered this entry to "part 10" to keep the sequence unique.

---

# Session 2026-08-14 (part 11) — eq-shell: open self-join enrollment gap closed, Tom's licence-upload timeout root-caused for real

## Built
- **Closed a live, unauthenticated-adjacent open-enrollment vulnerability**: `shell-join-tenant.ts` (shared backend for both Core's `?tenant=<slug>` login door and Cards' own `/join?tenant=<slug>`) would provision a brand-new, instantly-active `employee`-role Core account for anyone who knew an active tenant's slug — phone OTP alone, no invite, no QR code. Every active tenant was exposed, not just SKS. Fixed by rejecting any brand-new self-join that resolves neither a real join code nor a phone-matched pending invite, with a new `login.join_register_rejected` audit event. eq-shell [#1339](https://github.com/eq-solutions/eq-shell/pull/1339), merged (Royce's explicit go via AskUserQuestion, per the auth-changes-need-approval rule).
- **Quantified real-world impact via direct DB query**: exactly 3 real accounts (all SKS, none on `eq`) were created through the gap; none show a login since creation. Royce reviewed and confirmed acceptable.
- **Found and fixed a real data-quality gap surfaced by that impact check**: the 3 self-joined accounts had no display name on Core, because phone-OTP self-join never asks for one, and an existing Cards profile name (`profiles.full_name`) was never synced across. `shell-join-tenant.ts` now looks up and uses an existing Cards profile name when provisioning a brand-new Core identity going forward. eq-shell [#1340](https://github.com/eq-solutions/eq-shell/pull/1340), merged + deployed live — required rebasing onto `main` mid-flight to pick up the drift-check fix below before its own CI would go green.
- **Root-caused a live recurrence Royce reported ("Tom's licences still won't upload — could not auto-read")**: NOT the same bug the earlier same-day multi-document OCR timeout fix (PR #238, prior session) addressed. That fix only bounds how many documents Claude fully extracts per call (output size) — it does nothing for how long a large, uncompressed input image takes to read. Confirmed live via jvkn's `function_edge_logs`: the specific failing call (5.1 MB image, 22.455s exec time, 504) came through eq-shell's admin "Add Licence" modal (`staff-licence-ocr.ts`, `user_agent: node`, `role: service_role`), not Tom's own Cards app.
- **Real root cause**: `AddLicenceModal.tsx` was the one OCR entry point in the whole system that never downscaled a photo before sending it anywhere — full-resolution originals up to the 4 MB raw ceiling went straight to Claude. eq-cards' own worker-facing upload path (`photo_upload.dart`'s `stripExifAndCompress`) has always compressed to 1080px/quality-80 before both storing a photo AND sending it for OCR; Shell's admin path had no equivalent step. Added the same browser-canvas compression (`createImageBitmap` → canvas draw → `toBlob` JPEG, identical 1080px/quality-0.8 numbers) before a picked photo is used for auto-read or the final save. eq-shell [#1342](https://github.com/eq-solutions/eq-shell/pull/1342), merged + deployed live on core.eq.solutions — triggered same-session, ahead of the other two PRs, given Tom was actively blocked.
- **Found and fixed a second, unrelated CI-only issue along the way**: the scheduled `Schema drift + anon-grant + policy-lint` check was failing on `public.eq_cards_admin_list_stale_invites` — created live by an eq-cards migration (`0125_admin_list_stale_invites.sql`, PR #241, a same-day parallel eq-cards session's stale-invite detection RPC), invisible to eq-shell's own drift scanner (which only scans its own `supabase/migrations/`) — the identical false-positive shape as the `is_worker_in_org` case from 2026-08-13 (PR #1328). Confirmed the real source by reading the eq-cards migration directly and diffing it against the live jvkn function definition (identical) before triaging into `KNOWN_UNSOURCED`. eq-shell [#1341](https://github.com/eq-solutions/eq-shell/pull/1341), merged — this was blocking #1340's own CI, so had to land first.
- All three PRs (#1341, #1342, #1340, in that merge order) deployed to core.eq.solutions via the documented manual git-archive workaround (production auto-deploy-on-push is deliberately disabled on this site) and confirmed live via the Netlify MCP (`state: "ready"`, `error_message: null`, `context: "production"`).

## Decided
- Royce, via `/decide` ("can we do all 3?"): do the Maylin Ung name backfill and the drift-check triage now (both small, low-risk, already scoped); split the self-join QR hardening (expiry/cap/audit entry) into its own follow-up requiring a proper spec first, rather than bundling it into the same-day auth fix.
- Royce: "merge and deploy #1341", then (after the live Tom-upload report arrived mid-turn) "merge and deploy #1342 once CI's green", then "merge and deploy #1340" — three separate explicit authorizations, each given only once its own CI was confirmed green.
- Royce reviewed the 3 exploited accounts and confirmed "they are fine" — no further remediation needed on those specific accounts beyond the name-sync fix.

## Deferred
- **Maylin Ung's Core-side display name still needs a one-time manual backfill** — #1340 fixes future joins; her existing null name needs a single id-scoped `UPDATE`, drafted and ready, blocked on the safety classifier for a live-data write to a real named individual. Needs Royce's explicit go. _(added 2026-08-14)_
- **Self-join QR hardening (default expiry, per-code use cap, security-register entry, stale header comment in `shell-join-tenant.ts`)** — described to Royce via a spawned-task prompt earlier in the session, superseded in priority by discovering the more serious open-enrollment bug, never actually implemented. Needs Royce's numbers before anyone touches the file again. _(added 2026-08-14)_
- **No formal security-register (`ops/security-register.md`) entry written for the open-enrollment finding** — deliberately held back this session. The register's highest committed number is SEC-24; a concurrent eq-cards session's own SEC-25 write-up (invite-claim IDOR) was drafted but, at the time this decision was made, explicitly noted as "not yet committed" due to this exact repo's shared-checkout contention (see "part 10" above — that session went on to commit it later the same day). Adding a SEC-26 entry blind, without knowing whether SEC-25 had landed yet, risked a numbering collision on top of an already-fragile file. The finding's full detail is preserved in `eq/pending.md` and this entry instead. _(added 2026-08-14)_
- **Not yet confirmed by Tom actually retrying his upload** — the compression fix is live, but nobody has re-tested his specific photo since deploy. _(added 2026-08-14)_

## Notes
**Seventh instance of the shared-checkout contention pattern, same day** (see "part 3" through "part 10" notes above on the same recurring problem — "part 10" claims to have fixed the underlying stash-pop conflict for good, but this close still hit a plain non-fast-forward push race against it, which is the expected/normal kind of collision on a genuinely shared, fast-moving `main`, not a sign the part-10 fix failed). `C:\Projects\eq-context`'s shared checkout had a pre-existing unpushed commit from a concurrent eq-cards session (its own already-completed close) sitting 2-ahead/47-behind `origin/main` before this close even started. A `git merge origin/main` attempt hit real conflicts in `eq/pending.md`, `eq/changelog/eq-cards.md`, and `sessions/2026-08-14.md` (parts 1-8 vs the other session's own additions at the same insertion points). A `git merge --abort` then failed outright ("Entry 'digest.md' not uptodate" / "unable to unlink") — evidence of a live concurrent process actively writing to that same working tree at the same moment, not just a stale index. Did not force a `reset --hard` to push through it (would risk discarding whatever that live process was mid-write on). Left the shared checkout's conflicted, half-merged `MERGE_HEAD` state exactly as found and did all of this close's actual work — this entry, the pending.md additions above, and the changelog entry — from a fresh, fully isolated `git clone` of `origin/main` instead. Even that isolated clone hit one further non-fast-forward push race (against the concurrent eq-cards session's own close landing in between) — resolved by a plain fetch + rebase (real conflicts in `pending.md`/this file at the same top-of-file insertion point, resolved by keeping both sides and renumbering this entry from "part 9" to "part 11" since two other concurrent sessions had already claimed "part 9" and "part 10" today), then pushed clean.

**The two live security investigations this session (self-join gap, licence-upload timeout) were found back-to-back from what started as one status check.** Royce asked "what's next" after an accidental empty message; the answer surfaced the self-join impact numbers, which surfaced the missing-names gap, which led into the fix-and-deploy sequence — and mid-deploy, a live user report (Tom) arrived as an interleaved message and got triaged ahead of the in-flight work rather than queued behind it, per the standing rule that a live user-facing blocker takes priority over already-scoped background fixes.

---

# Session 2026-08-14 (part 12) — Suite-wide nav simplification: 4-app audit → 7 merged fixes, FIELD-1 correctly left alone

## Built
- 4-agent code-level nav audit across Shell/Field/Service/Cards, published as an interactive decision-sheet artifact (saved to Royce's OneDrive) so he could triage all 14 findings in one pass with a comment field on each.
- [eq-cards PR #243](https://github.com/eq-solutions/eq-cards/pull/243) — removed the workspace-switcher/join-QR widgets duplicated in Settings (kept on Profile); confirmed via git history the duplication was accidental (a later PR pasted in what an earlier PR had already built into a different screen), not deliberate. CI caught a real miss (an unused `eq_spinner` import) before merge.
- [eq-shell PR #1343](https://github.com/eq-solutions/eq-shell/pull/1343) — 3-item cleanup batch: renamed "Import Coupa" to "Import purchase orders" (a vendor name baked into a nav label), deleted the dead `/quotes`/`/eq-quotes` redirect routes (Royce's explicit call), relabelled AdminHub's "Overview" link to "All admin tools" so it signals the full 16-tile index it actually opens.
- [eq-shell PR #1344](https://github.com/eq-solutions/eq-shell/pull/1344) — new Reports landing page (`/reports` → an index listing report types, GM Reports moved to `/reports/gm`). Verified first that `GmReportsModule` already self-gates via `<Gate perm="reports.view">`, so the restructure made zero permission difference — confirmed by reading the code, not assumed.
- [eq-field PR #697](https://github.com/eq-solutions/eq-field/pull/697) — gated the desktop "Add Person" button to supervisor+manager, matching mobile. Verified Field's role model has no separate supervisor flag (one unified "supervision unlocked" flag) before wiring anything. Collided with a concurrently-merged Email Templates hotfix that had just claimed the same version number — rebased, kept both changelog/reflection-log entries, renumbered to v3.5.498, re-verified (27/27 tests, bundle-check clean) before merging.
- [eq-field PR #698](https://github.com/eq-solutions/eq-field/pull/698) (spawned task, run and merged independently by Royce) — gated 3 more buttons with the identical bug (Contacts/Managers "Add Contact," Editor "Add Person"), found in passing during the #697 work and flagged rather than folded into that PR's scope.
- [eq-service PR #729](https://github.com/eq-solutions/eq-service/pull/729) — added the 3 nav items (Today/Insight/Search/Settings) missing from the Shell-embedded nav bar, gating copied exactly from the standalone sidebar's own logic, not just visually matched.
- [eq-service PR #730](https://github.com/eq-solutions/eq-service/pull/730) — same AdminHub-undersell fix as eq-shell #1343, applied to Service's own Admin sidebar link. Held earlier in the session to avoid a file collision with a concurrent security fix (`fix/admin-hub-role-gate`, #728); started once that merged.
- `eq/identity/nav-access-matrix.md` (new) — records the four independently-built nav-gating mechanisms across the suite and the three drift incidents above as concrete evidence, with two options for a long-term fix left open.
- `eq/sprints/2026-08-14-nav-simplification.md` (new, then corrected twice) — full sprint record.

## Decided
- Royce, via `/decide` "then go": start with the Cards de-dup, not the initially-proposed "dead route deletion" — caught before building that the "dead routes" in Shell/Cards were live compat redirects (old bookmarks, in-flight SMS/email onboarding links), not free deletions with zero downside.
- Royce triaged all 14 decision-sheet items in one pass: 8 "Do it" (CARD-1, SHELL-1/2/3/4, FIELD-2, SERVICE-1/2, SUITE-1), 2 "Skip" (CARD-2, FIELD-3 — dead code intentionally left in place), 1 "Later" later reversed to "start now" (FIELD-1).
- SHELL-1 scoped up from a simple two-gates fix to a real landing page ("we need a report homepage and then a GM report as an option") — confirmed the expanded scope via `AskUserQuestion` before building rather than guess at how big "homepage" meant.
- SUITE-1's doc location confirmed as `eq/identity/` via `AskUserQuestion`, matching the existing `IDENTITY-MODEL.md`/enforcement-site-inventory precedent.
- Royce, via `/decide`: merge all 6 open PRs now, informed up front that Field and Service auto-deploy to production on merge while Shell and Cards don't — proceeded, checking each PR's actual CI (not just this sandbox's local build) before merging any of them, not merging blind.
- Royce: "start SERVICE-1 now" once its file collision with the concurrent security fix cleared.
- FIELD-1: Royce asked to start it after marking it "Later" on the sheet. Before writing any code, checked `index.html`'s own changelog rather than trust the audit's framing alone — found the Site Audits/Records split was Royce's own deliberate v3.5.342 decision, explicitly rejecting a merged/tabbed layout, not "historical" cruft as the audit called it. Surfaced this via `AskUserQuestion` before touching anything; Royce's informed call, given the corrected picture, was to leave it separate. Closed as reviewed/no-action.

## Deferred
- **Long-term fix for nav-visibility drift not decided** — `nav-access-matrix.md` lays out two options (a shared roles-derived config each app imports, or a lighter per-PR review checklist); genuinely Royce's call, not a build task. _(added 2026-08-14)_
- **eq-shell PRs #1343/#1344 merged, not deployed** — core.eq.solutions doesn't auto-deploy on merge (deliberate platform setting); needs Royce's manual Netlify trigger before the Reports page and sidebar cleanup are actually live. _(added 2026-08-14)_
- **`guard.js`'s `stale-main-gate` has a worktree-detection bug** — only recognizes paths containing `/worktrees/` or ending `-wt`; a worktree created elsewhere (e.g. a session's own scratchpad directory) gets misclassified as "not a worktree" and blocked unnecessarily. Found and worked around while writing this session's eq-context doc commits, not fixed. _(added 2026-08-14)_

## Notes
**Caught and corrected two of my own mistakes this session, both before they did real damage.** (1) The original nav audit called the Field Site Audits/Records split "historical reasons," implying an oversight — it was actually a deliberate, explicit prior decision by Royce (v3.5.342). Caught by reading the actual changelog before building on top of the audit's own framing, not by assuming a background agent's characterization was right. (2) While checking on a spawned eq-context cleanup task, re-verified my own earlier claim that three session/failure docs had conflict markers "committed into history" — they didn't. That was a sloppy `git grep` conclusion (matched marker *text* without checking it was at line-start), and all three were prose narrating an already-resolved past incident. Corrected the sprint doc twice as a result — once per mistake — rather than let either sit uncorrected in the durable record.

**Real CI caught things this session that a toolchain-less sandbox structurally can't.** eq-cards' `flutter analyze` caught a genuinely unused import (`eq_spinner.dart`, left behind after removing the widget that used it) — fixed before merge, not after, since no local Flutter analyzer exists in this environment to catch it first. A concurrently-merged Field hotfix (Email Templates nav visibility, #696) had just claimed the same version number (v3.5.497) this session's own Field PR was about to use — caught via the rebase, not silently overwritten; resolved by keeping both changelog entries and renumbering.

**The shared eq-context checkout's contention pattern (documented extensively in earlier parts today) recurred mid-session** — a prior spawned task's fix for the underlying stash-pop conflict held clean, but three follow-up doc-correction commits still had to route through fresh isolated worktrees/clones rather than the shared checkout directly, per this repo's own `pre_tool_use.py` guard (F9, blocks `git merge` in the shared checkout outright) and `guard.js`'s `stale-main-gate` (blocks direct commits on a behind-main branch outside a worktree) — both fired exactly as designed.

---

# Session 2026-08-14 (part 13) — eq-context: index-drift.yml cron fixed; guard.js worktree-detection hardened (closes part 12's deferred item); stash-pop repair verified & shared checkout synced

## Built
- **This was the background task "part 10" spun off** (the nav-simplification session's "Substrate finding" section) — arrived to find "part 10"'s author (a concurrent eq-cards SEC-25 session) had already pushed the actual repair (`2e54aa9`) before this session's own fix landed. Verified it rather than trusted it or duplicated it: re-ran a whole-tree anchored `git grep` for stray conflict markers (zero matches, confirms clean), and confirmed the 3 files the original brief named as having conflict markers "committed into history" (`sessions/2026-08-03.md`, `sessions/2026-08-13.md`, `system/failures.md`) were false positives — an unanchored pickaxe search had matched prose that only *quoted* marker text describing an earlier, already-resolved incident, not live corruption. No fix was needed on those three.
- Synced the shared `C:\Projects\eq-context` checkout to the verified `origin/main` (`git reset --hard`, explicit go from Royce first — the working tree had diverged past a clean `merge --abort`/`stash push`, filesystem safety-snapshot taken first). Dropped the one now-fully-superseded stash entry; left the other 23 historical stashes untouched.
- **Fixed the guard.js follow-up the repair surfaced**: `~/.claude/hooks/guard.js`'s `stale-main-gate` rule only recognized a worktree by path convention (`/worktrees/` or trailing `-wt`), so a worktree created in a session's scratchpad dir (not matching either convention) was misclassified as "not a worktree" and got a real false-block during this repair. Hardened to fall back to `git rev-parse --git-dir` vs `--git-common-dir` comparison (these diverge in any linked worktree regardless of naming/location, converge everywhere else) when the path heuristic doesn't match. Committed in the separate `~/.claude` repo (`ed72be4`) — isolated from an unrelated, pre-existing uncommitted change in the same file (a 2026-08-06 `inEqContext` regex broadening) by reverting to HEAD, adding only this fix, committing, then restoring the other change to the working tree exactly as found (still uncommitted, not mine to commit). 14/14 `hooks/selftest.js` cases pass.
- **Separately, found and fixed a live `index-drift.yml` failure** (unrelated root cause to the above — surfaced via the digest's "Needs you" list): the hard CI gate over every tier README's file index had failed every scheduled run since 2026-08-12 and every PR check since. `eq/sprints/2026-08-14-nav-simplification.md` (landed via PR #156/#157) was never added to `eq/README.md`'s file table. Added the missing line, verified clean against `scripts/index_drift.py` + `scripts/test_index_drift.py` locally, pushed from an isolated worktree (the shared checkout's own `git pull` guard, F9, correctly blocked doing this in-place — HEAD was 9 commits behind), confirmed green via a manual `workflow_dispatch` re-run.

## Decided
- Royce: "Yes, reset to origin/main (Recommended)" — explicit go for the `git reset --hard` on the shared checkout, after being shown that a concurrent session's fix was already verified complete and a filesystem safety snapshot had been taken first.
- Royce: "commit the guard.js fix in ~/.claude" — explicit instruction to commit just the worktree-detection fix, separate from the unrelated pre-existing change sitting in the same file.

## Deferred
- None from this thread — repair verified complete, guard.js fix committed, index-drift.yml confirmed green.

## Notes
**Eighth instance of the shared-checkout contention pattern today** (see "part 3" through "part 11"), and the first to also hit it from the *tooling* side — the guard designed to prevent this exact class of incident (`stale-main-gate`) had its own detection gap, found live while using it during this very repair. Fixed at the source (`~/.claude`) rather than routed around a second time.

**On tier framing:** this session was opened as "EQ — security cleanup" (SEC-1/SEC-9/SEC-24 from the digest's Needs You list), but all three are manual-hands-only — SEC-1 is Royce's standing "do not touch" freeze on sks-nsw-labour, SEC-9's remediation is a Supabase-dashboard key rotation, SEC-24's is a Netlify same-value re-store the safety classifier blocks by design. Redirected to `index-drift.yml` (real, code-side CI break) once that was surfaced via `AskUserQuestion` rather than spend the session on three items with no code path forward. Worth noting for future sessions: don't treat "Needs You" security rows as automatically engineering-actionable — check the register's own remediation column first.

**Closes "part 12"'s deferred `guard.js` worktree-detection item** (same file above: "Found and worked around... not fixed") — fixed at the source this session, see Built.

**Ninth "part 12" collision of the day**: this entry was drafted as "part 12" too and hit a real content conflict against the nav-simplification session's own close landing first — renumbered to "part 13" on rebase, kept both sides in full rather than drop either.
---

# Session 2026-08-14 (part 14) — eq-cards: local branch cleanup + closed a merged-but-undeployed gap (PR #243)

## Built
- Deleted the local eq-cards branch `fix/prevent-invite-for-claimed-worker` (already squash-merged as `f481951`, PR #242) and fast-forwarded local `main` to `origin/main` — routine cleanup, Royce's explicit go.
- **Found eq-cards PR [#243](https://github.com/eq-solutions/eq-cards/pull/243) merged but never deployed.** Not this session's own work — it's `CARD-1` from a separate concurrent session's nav-simplification sprint (`eq/sprints/2026-08-14-nav-simplification.md`), which had tracked it as merged but not deploy status. eq-cards' `deploy.yml` is manual-dispatch-only (merges never auto-deploy here), so it was just sitting live-pending. Dispatched `Build & Deploy` on Royce's explicit "deploy it" — run [31786531208](https://github.com/eq-solutions/eq-cards/actions/runs/31786531208), all steps green, confirmed live on `cards.eq.solutions`.

## Decided
- Royce: "delete the stale local branch" — done.
- Royce: "deploy it" — explicit go for the production deploy, covering #243 plus everything already merged to `main`.

## Deferred
- None new.

## Notes
This closes out the eq-cards SEC-25/stale-invite thread from earlier today ("part 10") — nothing further outstanding on that work. The #243 deploy gap was found incidentally while checking deploy state, before "part 12"'s own write-up (the PR's actual author) had landed in this file — its Deferred section doesn't mention a deploy gap because #243 wasn't deployed yet at the time it was written; this entry closes that loop. Also: "part 13"'s independent verification of "part 10"'s stash-pop fix (confirmed zero stray conflict markers repo-wide) is a useful second confirmation that repair held.
