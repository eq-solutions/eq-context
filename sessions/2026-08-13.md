# Session 2026-08-13 — eq-shell: invite-path approval was silently dropping flagged licences, fixed + merged + deployed

## Built
- `cards-approve-staff.ts` invite path (`staff_id` — existing SimPRO/import staff records) now records `licence_verifications` into `cards_field_approvals` and calls `notifyWorkerOfFlaggedLicences` for any flagged entry on approval, mirroring what the self-signup application path (`application_id`) already did. Previously an admin's flag during a first-time review of an existing staff record was silently discarded — not saved, not surfaced anywhere, worker never notified.
- Verified against live git state first (task explicitly flagged the worktree as shared with concurrent sessions) — confirmed the gap was still live via `git log`/read before touching anything.
- eq-shell [PR #1323](https://github.com/eq-solutions/eq-shell/pull/1323), squash-merged `8d09ee75`. Confirmed live on core.eq.solutions via Netlify (`commit_ref` matches exactly, deploy `6a7c3b85`, state `ready`).

## Decided
- Royce: rebase the shared worktree branch onto current `main` before opening the PR, rather than merging it as-is or cherry-picking just the fix onto a clean branch (see Notes — the as-is branch would have deleted live features).

## Deferred
- **Live click-through not done** — needs a real signed-in Shell session with `admin.review_cards` to confirm the flagged-licence notification actually fires on a real approval. Off-limits for this environment. Logged in `eq/pending.md`.

## Notes
- **Near-miss found mid-session, worth flagging as a pattern:** the shared worktree `eq-shell/.claude/worktrees/labour-hire-candidate-review` (branch `feat/ocr-full-multi-document-extraction`) had no open PR and had silently diverged 6 commits behind `main` (diverged only ~3h earlier). The real tree-diff against `main` showed **2001 deletions** — `StaffOrgChartPage.tsx`, `StaffResourcingPage.tsx`, `staff-org-chart.ts`, `staff-teams.ts`, `staff-resourcing.ts`, a migration file — because `main` had gained the staff resourcing/org-chart feature (#1311) and the quotes auto-archive/bulk-select features (#1319/#1320) after this branch's last common ancestor. Opening a PR from the branch as-is and merging would have reverted those live features. Caught by running the Rule 0.6 staleness check (`log HEAD..origin/main`) — but note this had to be run again on the **worktree's own branch** specifically; running it once against the root checkout's own (different) branch earlier in the session did not catch it. Fixed by rebasing onto `origin/main`; 3 of the branch's other 4 commits (credential-per-document, labour-hire document-thumbnail display, a pdfjs-dist bugfix) turned out to already be independently merged into `main` by concurrent sessions and dropped out as empty during the rebase — only one real conflict (one line, a pdfjs-dist call-signature difference; `main`'s version was already correct, kept it).
- Confirms `_shared/licence-flag-notification.ts` and the self-signup path's flagged-notification wiring (from an earlier commit, `18fc3280`) are genuinely live on `main` now, independent of this session — verified directly rather than assumed.

---

# Session 2026-08-13 — EQ Suite production-readiness deep dive + 18-issue Sentry triage, 1 real bug found and fixed

## Built
- Production-readiness assessment across the full EQ Suite (Shell, Service, Field, Cards, Intake, Ops), grounded in live checks rather than docs: Netlify deploy state (eq-shell confirmed `ready`/current), live Supabase migration list on ehow, the security register (`ops/security-register.md`), and Sentry's unresolved-issue list. Verdict: close, not clean — 4 of 6 products live and stable, CI green everywhere, real per-app test suites confirmed (corrects suite-state.md's stale "eq-service has no test suite" claim — it runs `vitest run`), security register mostly closed (18 of 24 tracked findings).
- Triaged all 18 suite-wide unresolved Sentry issues (eq-cards 11, eq-shell 3, eq-field 2, eq-solves-service 2) via 4 parallel subagents, each root-causing against live code/`origin/main` rather than local checkouts. eq-field's local clone turned out to be 40+ commits behind origin without flagging it — a trap the agent caught and worked around.
- Resolved 7 of 18 issues: EQ-CARDS-19 and EQ-FIELD-14 marked resolved directly (already fixed in shipped code). EQ-CARDS-1D, EQ-FIELD-13, EQ-SHELL-1J also confirmed already-fixed but the resolve call was blocked by the Claude Code tool classifier on 3 of 5 identical attempts — still show unresolved in Sentry.
- EQ-CARDS-W: the first triage pass misattributed the root cause to `image_download_web.dart`. Checking a same-named local branch (`fix/licence-photo-blob-revoked`) before editing — per the repo's own Rule 0.5 — surfaced that PR #223 (merged 2026-08-11) already fixed the real cause in `licence_edit_screen.dart`'s `_PhotoSlotState`, and its commit message cites this exact Sentry issue. No edit made to the wrong file; issue marked resolved instead.
- Found and fixed a real, still-open bug: eq-cards' `ocr_service.dart` hardcoded a 14s client timeout on the `ocr-licence` edge function call, but that function's own Anthropic-call timeout (added in PR #211, `supabase/functions/ocr-licence/index.ts:481`) is 20s — tuned for a different caller (Shell's admin path). The client was giving up 6s before the server's own budget expired. Sentry (EQ-CARDS-H) showed 7 distinct users hitting this over 7 weeks. Raised the client timeout to 25s (5s margin above the server's 20s). eq-cards [PR #228](https://github.com/eq-solutions/eq-cards/pull/228), squash-merged `df5d93f`, branch deleted.

## Decided
- Royce chose EQ tier for the session, then "go wider" (all 18 Sentry issues, not just Cards), then walked the OCR-timeout fix through build → commit-no-push → push → open PR → merge as separate explicit steps.
- The originally-planned second fix (blob-download delay in `image_download_web.dart`) was correctly dropped once the branch check showed it was already fixed elsewhere — not built.

## Deferred
- **3 Sentry issues need a manual resolve** (EQ-CARDS-1D, EQ-FIELD-13, EQ-SHELL-1J) — fixed in code, blocked in Sentry by the classifier. Logged in `eq/pending.md`.
- **4 genuinely open, low-priority bugs**, none built: EQ-CARDS-1E (401-retry double-throw bypasses the intended clean sign-out), EQ-CARDS-1A/1B (profile auto-provision RPC needs an `auth.uid() IS NULL` guard, 1 user hit it), EQ-SOLVES-SERVICE-3's Media Library Upload/Delete missing the stale-deploy catch Edit already has. Logged in `eq/pending.md`.
- **GitHub MCP connector isn't scoped to eq-cards** — `create_pull_request`/`list_pull_requests` 404'd even though `gh` CLI (same login) has full admin access. Worked around via `gh` CLI. Needs the repo added to the connector's GitHub App install to fix properly.

## Notes
- **Date-labeling slip, flagged for honesty**: this session's environment `currentDate` was 2026-08-13 throughout (confirmed at session start), but digest.md's own generation timestamp read 2026-08-12, and several artifacts written mid-session — Sentry resolve comments, the PR body/commit message for #228 — were dated 2026-08-12 by mistake before the discrepancy was caught. Those artifacts were not corrected retroactively (not worth the churn on already-merged/posted content). This close correctly uses 2026-08-13. Worth a look if `digest.md`'s generation cadence vs. the actual calendar date drifts again.
- eq-cards' local checkout was 3 commits behind origin at session start (fast-forwarded cleanly before editing) — not stale in a way that mattered, but consistent with the pattern of local clones lagging origin across this suite (see eq-field above).

---

# Session 2026-08-11 → 2026-08-13 — Shell Conversations feature suite: logging, permission lockdown, resourcing dashboard, draft org chart, team assignment

Full technical detail and every decision behind this thread: `eq/shell-conversations-scoping-2026-08-11.md`. This log covers the whole arc — the session ran long enough to cross two date rollovers.

## Built
- **Conversations log** on Shell's Staff detail panel — Formal (Check-in / Development Review, sourced from Royce's real SKS HR templates) + Casual entry types, digital-only. New `staff.manage_conversations` permission (group-only, no default role grant). eq-shell PRs #1302, #1304.
- **RLS hardening**: the original policy only checked tenant, not the permission — closed by embedding security-group grants into the session token and adding the first permission-aware RLS policy in this codebase. Also fixed a real CI blind spot it exposed (`check-orphan-perms.mjs` didn't know Shell-local permissions could exist).
- **A second, more serious regression found the same day**: the RLS fix above only patched one of two separate JWT-minting functions — the one the Conversations screen doesn't use. The path it actually uses was silently failing every read/write for everyone, including Royce, since the RLS went live. Fixed in eq-shell PR #1312, merged and confirmed deployed.
- **Resourcing dashboard + draft org chart** (eq-shell PR #1311) — every active person grouped by team, read live from canonical roster data with nothing hardcoded (Royce was explicit: agnostic to the current roster). Tenure, last conversation, time since last Formal review, new-starter flag. Found live: 32 of 88 active SKS staff have no team link — chart labelled draft on the page itself because of it.
- **Team/supervisor assignment** (eq-shell PR #1321) — chosen via `/decide` over proactive nudges (no data yet to act on) and a hand-authored division/role entity (would have broken the agnostic-roster constraint). New `staff.manage_teams` permission, kept separate from `staff.manage_conversations`. Verified live before building that the underlying tables had zero write access and no edit UI existed anywhere — this was new surface, not an extension.
- Marketing/concept HTML artifact (`people-concept.html`) built early in the thread to align on the feature's shape before any code was written.

## Decided
- Shell owns this, not Field (`/decide`) — Field owns operational stuff, Shell owns the canonical staff record.
- Two Formal tiers + one untemplated Casual type; digital-only, no paper fallback.
- One Shell-local permission key per distinct capability, no default role grants, group-only via Access Control — same pattern for `staff.manage_conversations` and `staff.manage_teams`.
- `/decide`: build both the resourcing dashboard and the org chart now, org chart explicitly labelled draft.
- `/decide`: team/supervisor assignment is the highest-value next build, over nudges and over a manually-authored structure.

## Deferred
- **Royce's own click-through, still not done** — nobody has logged a conversation, added a rating, or assigned someone off the Unassigned list through the real UI. Every fix above should make this work now; only a live session can confirm it. Logged in `eq/pending.md`.
- **Proactive "overdue for review" nudges** — deliberately held; no conversation data exists yet for staleness to mean anything.

## Notes
- **Session collision, resolved clean**: a background task (`task_4e2997ca`) flagged the JWT-minting bug and Royce independently started a separate local session on the same fix while the primary fix (PR #1312) was already landing. Verified after that session ended: it made zero commits and opened no PR — no conflict, nothing to reconcile.
- **A background build agent doesn't self-verify — checked, not assumed**: the team-assignment build agent (PR #1321) reported "CI clean" but the actual PR checks had failed. Root causes were two real, mechanical CI gates: a test file sitting at the top level of `netlify/functions/` (Netlify rejects any function filename with a dot beyond one extension) and a role-literal ratchet false-flagging a team-role value (`'supervisor'`) that happens to share a name with an EQ permission role. Both fixed directly rather than re-delegated, then confirmed green before merging.
- **A transient API network error mid-agent-run is recoverable** — the same build agent was cut off by an `ENOTFOUND` error partway through its own verification pass; resumed via `SendMessage` to the same agent id rather than restarting, picking up exactly where it left off.
- **Netlify deploy confirmation for eq-shell needs the Netlify MCP project reader, not GitHub's commit-status API** — production merges to `main` don't post a GitHub commit status/check (only PR deploy-previews do), so polling `commits/{sha}/status` for a production deploy will hang on `pending` forever. The reliable signal is `get-projects`' `currentDeploy` id changing plus a `origin/main` HEAD check to confirm no later commit landed in between.

---

# Session 2026-08-13 — labour-hire licence intake shipped end-to-end; found + fixed a live duplicate-certificate bug (Richard Brown)

## Built
- eq-cards `ocr-licence`: full multi-document extraction (every card in a multi-card photo, not just detection) — [#227](https://github.com/eq-solutions/eq-cards/pull/227), merged.
- "One photo → N credentials" wired through eq-cards `labour-hire-candidate-intake` and eq-shell `create-worker-invite.ts`.
- eq-shell: one-credential-per-document rewrite + client-side (`pdfjs-dist`) PDF/image thumbnail preview in the licence review modal — [#1316](https://github.com/eq-solutions/eq-shell/pull/1316), merged, confirmed live on core.eq.solutions (deploy commit matches merge SHA exactly).
- eq-shell: flagging a licence during review now emails + SMS's the worker — new shared helper `_shared/licence-flag-notification.ts`, wired into the re-review path and the application-approval path. Found and spun off the third (invite-path) gap as `task_41714f80` — see the earlier entry above, already landed independently.
- **New this session:** Royce reported "Richard Brown - three of the same certificate have been created." Checked Sentry + live DB — found 6, not 3 (3 were `is_private`-hidden). Root-caused to `licence_edit_screen.dart`'s `_save()`: it inserts the row, then uploads the photo; if the photo step throws, `_existing` (which decides insert-vs-update) never gets set, so a retry inserts a new row instead of updating. Confirmed via Sentry (`EQ-CARDS-1G`/`1H`, same user/trace) and via `storage.objects` that no photo/PDF was ever actually captured across any of the 6 attempts (failure was in the browser's `createImageBitmap` decode step, before any network upload) — nothing recoverable to reattach.
- Fixed (`_existing = saved` after each successful upsert) — eq-cards [#229](https://github.com/eq-solutions/eq-cards/pull/229), merged, deployed live via `Build & Deploy` workflow (verified: workflow's `headSha` matches `main` exactly, Netlify deploy `context: production`, `state: ready`).
- Cleaned up the data: soft-deleted Richard Brown's 5 duplicate rows on jvkn, kept the earliest.

## Decided
- Royce (AskUserQuestion): notify the worker only (not tenant admins) when a licence is flagged.
- Royce: "Build both, go with client-side pdf.js" — multi-document extraction AND PDF preview, pdfjs-dist specifically over server-side rendering.
- Royce: "Fix the bug + clean up both" for the duplicate-certificate incident (recommended option).
- Royce: explicit merge/deploy instructions given and followed for #227, #1316, #229, and the eq-cards production deploy.

## Deferred
- Richard Brown still needs to re-add his LV Rescue (C40385) photo — nothing existed anywhere to recover, but the retry will now behave correctly.
- Live click-through on the multi-document/PDF-preview/flag-notification features — not done, no real signed-in session available in this environment.
- SMS-notification coverage inconsistent across invite paths — flagged in the original audit, not touched.
- Intake engine's CSV import bypasses the same dedup the direct upload paths now go through — flagged, not touched.
- Possible dead code: `eq_cards_find_invites_by_phone` RPC, `ClaimByPhoneScreen` — not confirmed, not removed.
- Open question: should the manual labour-hire-upload form and the regular invite form eventually merge into one? Left as two entry points.

## Notes
- **Real near-miss caught before it caused damage**: a first blast-radius query (grouping duplicate-row candidates by `user_id + licence_number` alone) flagged a second "victim," Luke Wheeler, as having the same duplicate-row bug. Re-checked before touching his data — his 3 rows were 3 genuinely different certificates (LV Rescue, CPR, First Aid) that all happened to share an empty licence number, a normal quirk of the quick-document upload path with no number field filled in. Corrected the PR description before merge and did not touch his data. Worth remembering: never group only on a field that's legitimately empty/repeated across unrelated rows when hunting for duplicates — include the type/category field too.
- `storage.objects` is directly queryable via SQL on the same Postgres instance — useful for confirming whether an upload attempt ever reached storage before concluding data is unrecoverable, rather than assuming from the row alone.
- eq-cards' Netlify deploy record has no `commit_ref` for `Build & Deploy`-triggered releases (`deploy_source: "api"`, `manual_deploy: true` — the workflow uploads a built folder rather than Netlify pulling from git). Cross-check via the GitHub Actions run's own `headSha` instead of Netlify's deploy metadata, unlike eq-shell where Netlify's own git-integrated build does carry `commit_ref`.

---

# Session 2026-08-13 — Ran /decide on "what next," killed the overseas-hardening goal

## Built
- Nothing shipped — this was governance/substrate work following on from the eq-field mobile-centering close (see `sessions/2026-08-12.md`, "cont. 5").

## Decided
- Ran `/decide "what next?"` after closing the mobile sprint. Framed against `TODAY.md`'s then-active goal ("harden the suite while overseas, no live/auth changes"): the sprint had just deployed 3 live production changes (v3.5.484/485/486) under that exact wording. Recommendation was to ask, not assume.
- Royce, via AskUserQuestion: confirmed the 3 merges were fine (his own explicit "Merge it" each time already covered it) **and** separately chose to kill the goal itself rather than hold future work to it.
- Killed `system/TODAY.md`'s goal — `claims: []`, 0 active. Note left in-file explaining why/when/reference, per the file's own "blank is honest, don't silently persist a phantom" philosophy (the 2026-07-11 lesson this file exists to prevent repeating).

## Deferred
- Nothing new — the goal-kill removes a constraint, it doesn't create backlog.

## Notes
- **Corrected 2 pending.md entries whose hold-reason cited the now-dead goal**, rather than leaving a stale citation: eq-field PR #683 (still held — but on the standing CLAUDE.md auth-change-approval rule alone, which is independent of the goal and unaffected by its death) and the eq-shell/eq-cards Cards-SSO-broker fix (already unblocked from the goal specifically by Royce's own 2026-08-11 override; the real remaining blocker there was always the manual `EQ_CARDS_HANDOFF_KEY` Netlify step, not the goal). Neither item is newly unblocked by this session — both still have independent reasons to stay held. Swept only entries actually citing the goal as a live block, not a full pending.md pass.
- **Shared `eq-context` checkout collision, again, same day:** at least one other concurrent session was mid-flight in the shared checkout with real uncommitted work (a suite-wide permission-audit thread touching `eq/pending.md`, `eq/changelog/eq-shell.md`, `sessions/2026-08-13.md`, `sks/pending.md`, `suite-state.md`, `digest.md` — eq-field PR #685 among its output). Routed this session's entire /close through the disposable isolated clone again rather than the shared checkout, same discipline as the 2026-08-12 close. `sessions/2026-08-13.md` didn't exist on `origin/main` yet when this entry was written (only in the other session's local, uncommitted copy) — created fresh here; expect a rebase/merge on push if they land first.
- Confirmed via `git show origin/main:system/TODAY.md` that the goal-kill actually landed before treating it as fact — the SessionStart gate hook's cached "GOALS set" line was simply stale (digest.md hadn't refreshed since before the kill), not a sign the write failed.
