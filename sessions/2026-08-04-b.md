# Session 2026-08-04 (b) — Substrate upgrade: agentic-coding rules, and three stale/missing rules closed

Scope: eq-context itself (EQ tier, substrate work). Branch
`claude/eq-context-substrate-upgrade-8383aa`. Prompted by an external review
arguing that with agents, building is no longer the constraint — maintainability,
multi-tenancy reliability and verification are.

## Built

- **`rules/agentic-coding.md` (new, `read_priority: critical`)** — six short sections: session gate, verification-beats-inspection, maintainability under iterative agent edits, the multi-tenancy constraint, effort threshold, search hygiene. Indexed in `CLAUDE.md` §8.
- **Rule 0.6 (session gate) + the Workflow Threshold table migrated out of `C:\Projects\CLAUDE.md`** into that file; both copies there replaced with pointers. Royce chose the migration explicitly when offered the option. Edit kept deliberately narrow — Rule 0, Rule 0.5, the load-bearing-facts list, worktree rules and the repo map were left untouched, because that file is outside git and has no CI to catch a bad rewrite. Verified after: 105 → 91 lines, all 13 section headers intact, zero NUL bytes.
- **`rules/non-negotiables.md` #11 (new)** — every service-role / admin-client query MUST carry an explicit tenant filter; tenant id resolves server-side, never from client input. Legal/Brand/Financial renumbered 11–22 → 12–23; grepped first, no other file references those numbers.
- **`rules/stack.md`** — EQ Cards added as an explicit Flutter/Dart exception alongside the Next.js one, with the gate named as the widget-test suite rather than `flutter analyze`.
- **`rules/deployment.md`** — Supabase section rewritten to the live Control Layer + per-tenant model; site registry corrected and pointed at `suite-state.md` for live URLs.
- **`CLAUDE.md` §7** — two hard-rule bullets (no untenanted service-role query; no "done" on a clean analyzer alone).
- **`system/lessons.md`** — appended "Inspection Is Not Verification — For Code, Not Just Substrate". 241 → 249 lines, NUL scan clean.
- **`eq/README.md`** — indexed `eq/ops/EQ-OPS-ARCHITECTURE.md`, a pre-existing orphan `index_drift.py` was flagging (CI runs that check strict, so it would have blocked this branch).

## Decided

- Royce: run `/decide` before building, then build — done, recorded inline.
- Royce: migrate Rule 0.6 + Workflow Threshold into governed eq-context rather than leaving them in the ungoverned umbrella file.
- Claude: do **not** touch the truth/freshness machinery. `system/substrate-plan-v2.md` owns that, is `status: draft`, and is awaiting Royce's separate adoption decision.
- Claude: do **not** add SaaS-ops/observability or "domain depth as moat" content. Both already have a home (`~/.claude/observability-ref.md`; `ops/decisions.md` 2026-06-02 ADR). Duplicating breaks one-fact-one-home.

## Deferred

Three items to `eq/pending.md`, dated 2026-08-04: no audit yet of existing code
against the new tenant rule; the remainder of the umbrella `CLAUDE.md` (Rule 0,
Rule 0.5, load-bearing facts) still ungoverned; and an unresolved disagreement
between two deploy tables about EQ Service's live URL.

## Notes

- **The brief gate is real and it fired.** The first `Edit` of the session was blocked by `guard.js`'s `brief-gate` — I had written the equivalent brief in chat, but not run `/brief`, and the hook does not care. Correct behaviour: it is rung 4, and rung 4 is not satisfied by having done the thinking. Ran `/brief eq-context` properly, then proceeded. Worth knowing that the brief-gate flag is **per-session, not per-day**.
- **Measurement artifact worth not re-learning:** PowerShell's `Get-Content | Measure-Object -Line` counts *non-blank* lines. It reported the umbrella `CLAUDE.md` at 63 lines after the edit, which reads as catastrophic truncation against a 105-line original. `[System.IO.File]::ReadAllLines().Count` gave the true 91. When verifying a write against F2, use a counter that counts blank lines, or the check itself will manufacture a false alarm.
- **Concurrent writer confirmed again.** The shared `C:\Projects\eq-context` checkout had another live session's uncommitted work in it (`system/worktree-registry.md` modified, an untracked `eq/documents/` file). Not touched. This worktree is also not listed in `worktree-registry.md` — not added, precisely because that file is what the other session is holding.
- This branch was 2 commits behind `origin/main` at session start (`6a9783d` session close, `d0f9b18` nightly digest). Both skimmed; neither touches `rules/`, `CLAUDE.md`, `system/lessons.md` or `system/architecture.md`. `eq/pending.md` *did* move on main, so that file will need a rebase before merge.
- Nothing pushed, no PR opened, no deploy — per `rules/non-negotiables.md` #1.
