# Session 2026-08-25 — Canonical wiring map: jvkn/Shell/Field read-write capabilities, verified live end-to-end

## Built
- Published an in-depth architecture review ("Canonical Wiring Map") mapping exactly how jvkn (control/identity layer), Shell-owned canonical data (sites/customers/contacts/staff on ehow), and EQ Field read and write each other — grants, RLS, view `security_invoker` state, and both `field_people_iud()`/`field_people_removed_iud()` trigger bodies pulled live, then cross-checked against a full code trace of every real call site in `scripts/*.js` (not inferred from naming). Corrected the review's own working assumption along the way: Field's "Contacts" nav page isn't `app_data.contacts` at all — it's the Staff table under a different name.
- Verified the identity upward-push webhook (`field_people_iud()` → eq-shell's `field-identity-push.ts` → jvkn `public.workers`) is not just wired but actually firing and succeeding: traced pg_net's own response log (`net._http_response`) on ehow, found 3 real requests (1 genuine write — email + emergency contact phone — 2 correctly-detected no-ops), all `200`, cross-checked against the receiver's own source to confirm what a `200` there actually proves (secret match + successful DB read/write, not just "request received").
- Corrected a stale pending-item header in `eq/pending/eq-field.md` — the birthday (day/month) bug was still marked "root cause not found" despite being fully root-caused and fixed in two passes (PRs #767/#769) earlier the same continuous session; neither of the closes that actually fixed it had circled back to tick off the entry that first tracked it.

## Decided
- No further polish/rework needed on the artifact beyond the two verification passes already run — asked directly rather than defaulting to "looks good," and the honest answer was the same both times: solid on everything checked, nothing left unverified.

## Deferred
- None new. The one open thread from the prior close (eq-shell's receiving side of the identity-push webhook) is now closed with direct evidence, not just a status check.

## Notes
- Session crossed midnight mid-conversation; the birthday-bug work above was already closed out as "24 Aug · part 2" before this portion started — this log covers only what's new since then.
- **`net._http_response` has no URL column.** A same-day, same-database, differently-shaped response (a data-quality run) initially read as a plausible 4th identity-push request purely from timing and a count match (4 responses, 4 qualifying staff updates). Caught by reading the actual response bodies, not just counting rows — worth remembering as the general technique next time this table is used for this kind of proof.
- Cross-checked this session's own verification technique against this file's own 2026-08-24 record after the fact: a different, earlier session the same night had already established `net._http_response` as the reliable way to prove this exact pipeline end-to-end (the Aiden Crowley click-test). Independent convergence on the same method, not duplicated effort — the two verifications rest on different request timestamps/evidence.
- `C:\Projects\eq-context` was 47 commits behind `origin/main` at session start; the shared checkout's own `pre_tool_use.py` hook (F9) correctly blocked a plain `git pull` there. Recovered the same way several other sessions in this file already have: isolated `git clone` into scratch, edit + push from there.

---

# Session 2026-08-25 — leave.js file-size debt closed: email templates split into leave-email.js (PR #772, v3.5.554), merged and live

## Built
- `leave.js` was flush against its eslint `max-lines` grandfather ceiling (1899/1900, zero headroom) after #768 — the next PR touching the file would break CI with no room to absorb even a one-line fix. Extracted `triggerLeaveEmail()` (the 3 email templates) + `resendLeaveEmail()` (manual resend) into new `scripts/leave-email.js`, the same file-size-convention split already used on timesheets.js/apprentices.js/people.js/roster.js/auth.js. Pure extraction, no behaviour change — extracted byte-for-byte via a small Node script (unique marker comments, sanity-checked before/after write) rather than hand-retyped, since the block contains emoji and em-dashes that are easy to corrupt by hand. `leave.js`: 1899 → 1724 lines; eslint ratchet lowered 1900 → 1750.
- `lazy-loader.js`'s `'leave'` tab entry gained `leave-email.js`; `core-bundle-b1.js` (which mirrors `lazy-loader.js`) regenerated via `build-bundles.mjs --write`, never hand-edited.
- Verified live on the deploy preview before merge (not just CI): `typeof triggerLeaveEmail === 'function'`, `typeof resendLeaveEmail === 'function'`, `leave-email.js?v=3.5.554` actually fetched by the browser, `leaveCCList`/`leaveRequests` (leave.js's own module state) readable from the split file — zero `ReferenceError`, the exact failure class this repo's own `lazy-loader.js` comments warn about repeatedly.
- eq-field [PR #772](https://github.com/eq-solutions/eq-field/pull/772), squash-merged on explicit "merge," confirmed live via `field.eq.solutions/sw.js` (`v3.5.554`).

## Decided
- Extract into a companion module rather than bump the ratchet again — user's own brief specified this up front, confirmed via a `/decide` pass mid-session (steelman/pre-mortem/value/feasibility) rather than just proceeding on instruction alone: bumping again would only move the same zero-headroom wall 50 lines out, the exact pattern that already forced 5 other files through this same extraction.
- No dedicated test file added for the new module, despite eslint.config.js's own auth.js comment mentioning an "extract-plus-test-module pattern" — checked first: none of the 6 prior extractions actually added one, so a literal reading would have invented a convention that doesn't exist.

## Deferred
- 3 more extraction candidates named in the original brief but not built (leave.js has real headroom now, none urgent): the CC-recipients config subsystem (~124 lines), the submit flow (~271 lines), the respond/approve flow (~239 lines). Logged in `eq/pending/eq-field.md`.

## Notes
- **Real version collision, live, not hypothetical.** Picked `3.5.553` at session start (confirmed free via `git fetch` + `git tag -l`), did the full extraction against it, then found — right before committing — that a different concurrent PR (#771, "+ Add by name" fix) had landed `3.5.553` first, touching the exact same three version-stamp files (`app-state.js`, `sw.js`, `index.html`) for the exact same reason. Reconciled via `git stash -u` → `git merge --ff-only origin/main` → `git stash pop` (one real conflict, in `index.html`'s changelog-banner insertion point) → manual resolution placing this PR's now-renumbered `v3.5.554` block above #771's `v3.5.553` block → repo-wide grep to renumber every string this session had already written → full verification pass (bundle drift, cache-busters, eslint, all 30 tests) re-run a second time rather than assumed safe. Confirmed via `gh pr view --json mergeable` immediately before the actual merge that nothing had moved again in the interim.
- **The `eq-field` repo's own brief-gate (`eq-guard`) is a real, active, tool-level enforcement**, not narrative text — it physically blocked several `Edit` calls mid-session with `brief-gate — brief not run this session`. Ran the actual `/brief` skill rather than setting `EQ_SKIP_BRIEF=1` to route around it.
- **This same `eq-context` shared-checkout F9 pull-guard** (see the entry directly above this one, same file, different session, same day) was hit again here — confirms the pattern documented there rather than being a one-off. Isolated-clone recovery used again, and a live in-progress entry from a *third*, currently-active concurrent session (`eq-field-confirm-gaps-wt`, uncommitted in `system/worktree-registry.md`) was found sitting in the shared checkout mid-edit — left completely untouched rather than stashed/discarded, per this file's own explicit "leave files you did NOT touch unstaged" convention.

---

# Session 2026-08-25 — BUG-009 sweep: built independently, found already done by a concurrent session (PR #775), own copy discarded before push

## Built
- Nothing shipped from this session — the task ("fix the six other spots" from the pending item logged in the prior session's close) was already completed live by a different concurrent session, PR #775 (v3.5.558), by the time this session finished its own independent build and reached the pre-push freshness check.
- Closed the loop instead: removed the now-resolved pending item from `eq/pending/eq-field.md`, archived the full write-up to `eq/pending-archive.md` with both closing PRs credited (#774 for `timesheets-approvals.js`, #775 for the remaining 5 files), and backfilled 2 missing changelog entries (PR #774 v3.5.557, PR #775 v3.5.558) that hadn't been logged by their own sessions yet.

## Decided
- No merge/deploy decision needed — nothing of this session's own work was mergeable. The only judgment call was recognizing genuine duplication (not a near-miss) from a full diff comparison before discarding: same 5 files, same 7 call sites, functionally equivalent fix per site, confirmed via `gh pr diff` before deleting anything.

## Deferred
- None new. The BUG-009 family is now fully closed across all 7 originally-flagged files (audits.js, audit.js, apprentices.js, journal.js, timesheets-export.js, timesheets-approvals.js, tender-pipeline.js) plus one additional file not in the original list.

## Notes
- **Built the identical fix in parallel, twice, same day, same repo — the third and largest such collision today** (after the `timesheets-approvals.js`/PR #774 single-file miss caught earlier in this same session's own investigation phase, and the version-number races documented in the two sessions above). This one wasn't caught by investigation — it wasn't discoverable until the very end: `origin/main` was re-fetched and stable across the ~40+ minutes of implementation, so there was no signal to catch mid-build. It only surfaced at the routine final `git fetch` immediately before `git push`, which is exactly why that check exists as a *separate* step from the one done at branch-creation time, not a redundant repeat of it.
- **Full diff comparison, not just a file-list match, was what confirmed genuine duplication rather than a coincidental overlap.** `gh pr view --json files` showing the same 10 filenames was suggestive but not proof (two sessions could plausibly touch the same files for unrelated reasons); reading the actual `gh pr diff` line-by-line against this session's own uncommitted changes confirmed the same 7 call sites, same helper-per-file architecture, functionally equivalent fixes — genuine duplication, not a false alarm. Worth the extra minute before discarding real work.
- **This session's own version of the fix was, on inspection, very slightly worse in two places** than the one that shipped: it left `apprentices.js:1232`'s non-blocking prompt() untouched (a defensible but more conservative scope call — the shipped version fixed it too, making the note actually work instead of just non-fatal), and it wrapped `tender-pipeline.js`'s `_quickPushToSchedule` in `.then()` rather than converting the function to `async` outright (functionally identical, but a larger and less idiomatic diff than the alternative). Recorded for calibration, not as a complaint — the discarded work was correct, just not the better of two valid solutions.
- **Two changelog entries (PR #774, #775) were missing from `eq/changelog/eq-field.md` despite being merged and live** — their own sessions hadn't reached (or hadn't yet run) their `/close` pass. Backfilled both here rather than leaving the gap, clearly attributed as backfilled-by-a-different-session rather than presented as this session's own work.
- Same `eq-context` shared-checkout F9 pull-guard as every entry above today — isolated-clone recovery used again (this is the 4th time today), no attempt made to fix the shared checkout itself.

---

# Session 2026-08-25 — Site Audits: Submit silently did nothing when a non-conformance was flagged, inside the Shell iframe (PR #776, v3.5.556), merged and live

## Built
- `audits.js`'s `submitAudit()` used `window.confirm()` to gate submission when a non-conformance was flagged without corrective-action details — silently a no-op inside eq-shell's sandboxed Field iframe (no `allow-modals`), so any SKS supervisor submitting a flagged audit via the normal Shell-embedded path (Core-only, their only path) was silently blocked from submitting at all. Same BUG-009 family PR #773 (v3.5.555, same day) fixed for the sibling Prestart/Toolbox case — that PR's own changelog explicitly named this exact function as the deferred follow-up.
- Fix: new local `_auditConfirm()` helper mirroring `leave.js`'s `_leaveConfirm` / `teams.js`'s `_teamsConfirm`, routed through the existing shared `#modal-confirm` dialog. Deliberately kept local rather than calling into `SiteReportsShared` — confirmed via direct read of `lazy-loader.js`'s manifest that `audits.js` is reachable alone via the `'audits'` tab bundle, where `site-reports-shared.js` isn't guaranteed loaded (only the `'safety'` bundle pulls both) — a shared-helper call would have been a live crash risk for anyone reaching Submit via the plain Audits tab, not just a missed DRY opportunity.
- eq-field [PR #776](https://github.com/eq-solutions/eq-field/pull/776) (v3.5.556), squash-merged on explicit "merge it," confirmed live via `field.eq.solutions/sw.js`.
- Logged the still-open sibling files (6 more carrying the same bug pattern, per PR #773's own sweep) in `eq/pending/eq-field.md` — this wasn't tracked anywhere in eq-context before, only visible in eq-field's own `docs/reflection-log.md`.

## Decided
- Local helper over shared-dependency reuse, via an explicit `/decide` pass (steelman/pre-mortem/value/feasibility) before writing any code — the reuse option had a real, verified crash risk (not just a style preference) that the "less code" argument didn't outweigh.
- Merge approved explicitly ("merge it") after PR review; squash-merged and production deploy verified directly (`sw.js` banner), not assumed from a green CI check alone.

## Deferred
- 6 files still carry at least one more live-blocking `confirm()`/`prompt()` call of the same shape (`audit.js`, `apprentices.js`, `journal.js`, `timesheets-export.js`, `timesheets-approvals.js`, `tender-pipeline.js`) — inherited from PR #773's own sweep, not independently re-verified this session. Now tracked in `eq/pending/eq-field.md`.
- Not click-tested through a real Shell iframe with live SKS credentials — same accepted limitation as PR #773. Confidence rests on the modal markup/helper shape being structurally identical, byte-for-byte, to 4 other instances already shipped and working, plus a live-DOM behavioural test of the helper itself against the real deploy preview.

## Notes
- **`origin/main` moved three times during this one session** — an unrelated merge (PR #772, leave.js split) landed mid-investigation, then PR #773 itself (the sibling fix this task's own brief pointed at) merged mid-investigation too, taking the version number originally planned. Each move was caught by re-fetching immediately before the next commit-adjacent step, not assumed stable from an earlier check — version went 3.5.553 → 554 → 555 → 556 as each collision was caught before, not after, committing.
- **`gh pr merge --squash --delete-branch` partially failed** on the local cleanup step only (`fatal: 'main' is already used by worktree at 'eq-field-apprentice-scope-race-wt'`, a different concurrent session's worktree) — the remote squash-merge itself had already succeeded (confirmed via `gh pr view --json state,mergedAt`). Recovered by deleting the remote branch and removing only this session's own worktree; the other session's worktree was left untouched.
- **The `/close` skill's own hardcoded path (`eq/pending.md`) is stale** — that file was split into per-product `eq/pending/<repo>.md` files on 2026-08-17 and is now just a 28-line index. Wrote to `eq/pending/eq-field.md` instead, confirmed live off `origin/main` before writing rather than trusting the skill's literal instruction. The skill file itself wasn't edited (out of scope for a close pass) — worth a follow-up correction.
- **`eq/changelog/field.md` vs `eq/changelog/eq-field.md`** — the former explicitly self-flags in its own frontmatter as an "UNRECONCILED PAIR" / dead twin accidentally recreated by a past session; wrote to `eq-field.md` (the file its own header says is canonical) instead, per that file's explicit "read that one first" instruction.
- Same `eq-context` shared-checkout F9 pull-guard as both entries above (third hit today) — isolated-clone recovery used again, no attempt made to fix the shared checkout itself, consistent with how the two prior sessions today handled it.

---

# Session 2026-08-25 — EQ Field access-control review (PR #768) merged and live; artifact rebuilt with before/after

## Built
- eq-field permission-matrix.js v2.6 — 6 role-default changes from Royce's row-by-row review of all 94 access levers (via a Claude-built review artifact): removed 4 dead Project Hours keys (feature retired at v3.4.71, these controlled nothing since), narrowed `leave.archive` to manager-only, gave labour_hire `leave.view_own_balance`/`leave.submit_request`, opened prestart/toolbox create+submit to every Field role, moved `field.manage_recognitions` to opt-in-only.
- `crewFilterFragment` (permissions.js) widened from timesheets-only to also scope Leave reads.
- Timesheets RLS fail-open gap closed on ehow (mirrors #756's Leave fix from the day before) — 14 of 21 SKS supervisors had no `team_supervisors` row and were reading every crew's timesheets, not just their own. Applied via Supabase MCP after explicit go.
- eq-field PR #768 merged → live (v3.5.552). Finishing it hit two real problems: a pushed fix commit got zero GitHub Actions runs for 24h+ (confirmed via the Actions API directly, never root-caused), and `main` had moved 3 commits including an independent version collision with another PR. Fixed by rebasing onto main and renumbering; fresh commit got real CI, all green, merged clean.
- The leave.js file-size follow-up flagged during this review was spun off as its own task and completed separately by another session same day — see that session's own entry above (PR #772, v3.5.554) rather than repeated here.
- Field Access Levers review artifact rebuilt with a before/after view — the 12 changed rows now show a plain-English change note plus struck-through/green role pills for what was lost/gained, alongside a "what changed since your review" summary.

## Decided
- Royce: apply the timesheets RLS fix now, not just investigate (mirrors the Leave precedent).
- Royce: merge PR #768 once green and mergeable.

## Deferred
- Royce hasn't yet granted himself `field.manage_recognitions` via Shell's Access Control — it's opt-in-only now, nobody has it by default.
- `people.edit_own` reachability for non-manager roles — flagged during the review, not investigated further.
- Per-tenant hiding of specific leave permissions (floated during review) — Shell-side work, out of scope for eq-field alone.

## Notes
- The zero-GitHub-Actions-runs anomaly (confirmed via `gh api .../actions/runs?head_sha=...` returning `total_count: 0` for a real, pushed, PR-branch commit, while other branches got normal runs in the same window) is worth remembering as a failure mode: `gh pr checks --watch` can exit clean showing only non-Actions checks (Netlify) with no indication Actions never ran at all. The fix that worked was a fresh commit (a rebase, in this case) forcing a new `synchronize` event — never root-caused beyond that.
- Session spanned the 2026-08-24→25 date rollover mid-task; the local brief-gate flag is stamped per-day, so it re-blocked mid-rebase and had to be re-armed by hand. Not a real re-brief — same in-flight, already-approved work continuing.
- Same `eq-context` shared-checkout F9 pull-guard as the other entries in this file today, and the same near-miss risk it exists to prevent almost landed anyway: an isolated clone was made correctly, but the first attempt at copying this session's 3 substrate edits into it was a blind file-copy from a 66-commits-stale checkout, which would have silently deleted 6+ other sessions' changelog/pending entries (PR #772/#776/#767/#769/#763/#764/#765/#766, all real, all merged) on push. Caught by diffing the isolated clone against its own freshly-cloned HEAD before committing, not by any tooling — worth being genuinely paranoid about this specific failure mode (stale local copy silently wins over fresh remote content) any time a shared-substrate file is touched from a checkout that's known to be behind.

---

# Session 2026-08-25 — CustomersPage.tsx: 4 pre-existing react-hooks/set-state-in-effect errors fixed, PR #1583 merged and live

## Built
- Fixed 4 pre-existing `react-hooks/set-state-in-effect` errors in eq-shell's `src/pages/CustomersPage.tsx`, unrelated to any in-flight feature work on the file (handed over as a fully-specified finding — file, exact eslint output, line numbers). Verified live via a fresh `eslint` run before starting rather than trusted as given.
- Matched each of the 4 call sites to the pattern that actually fit it, not a blanket fix: `queueMicrotask` defer for the 2 genuine-I/O effects (list load, detail load), matching the shape already established in PR #1504; a render-time prevId-guard for the reset-on-customer-selection effect, matching `SplitPanel.tsx`'s `prevStaffId` (PR #1525); `useMemo` + a `prevDupGroups` guard replacing the pure-derived-`dupGroups` effect entirely, per React's own "you might not need an effect" pattern. Confirmed the exact mechanics of the lint rule itself (read the `eslint-plugin-react-hooks` HIR-based implementation directly) before designing the fix, rather than pattern-matching blind.
- Ran a scaled-down `/decide` pass on the fix design before implementing, per the protocol's own scaling rule for a reversible, single-file, non-deploy, non-auth change — no blockers surfaced.
- Committed, pushed, then opened as a clean single-commit PR (#1583) from a **fresh branch off current `main`**, deliberately not from the working branch directly: that branch also carried an unrelated pre-existing P0 commit (`672bf380`, field-people trigger reattach) whose content had already shipped to `main` under a different hash via squash-merge (the same squash-merge blind-spot already documented elsewhere in this repo's memory) — bundling it would have put a misleading, scary-looking P0 commit into a lint-only PR's commit history.
- On "merge when safe - check other session": ran a `merge-readiness` audit (found the branch clean at first pass, but a fresh re-check caught a real, current merge conflict — a concurrent session, `eq-shell-9e`, had just squash-merged an unrelated site-linking PR, #1582, to the same file). Messaged `eq-shell-9e` directly before touching anything; confirmed nothing else was in flight on the file. Rebased onto current `main`, resolved the 2-line import conflict (kept both sides' additions), re-verified `eslint` + full `pnpm run build` clean post-rebase, force-pushed the solo PR branch, watched CI back to green, then squash-merged.
- Confirmed live on core.eq.solutions via the Netlify deploy API directly (`state: ready`, exact `commit_ref` match, `published_at` set, 261s actual build+publish time) — not assumed from a green CI check or the merge itself.

## Decided
- Royce directed each step explicitly and separately across the session (commit → push → open a PR → merge when safe), rather than one blanket "do all of it" — treated each as its own checkpoint.
- eslint's `set-state-in-effect` rule intent taken at face value (fix the actual pattern, not silence the rule) — confirmed by reading the rule's own implementation rather than guessing from the error text alone.

## Deferred
- None. Fully closed within the session: code fixed → committed → PR'd → merge-conflict resolved → CI green → merged → deployed → confirmed live.

## Notes
- **A task handoff's claim about existing code is a claim, not a fact, same as any other substrate content.** The original brief referenced "the fix just applied to the new `LinkSiteModal` component" as a pattern to mirror — that component didn't exist anywhere in the repo (local checkout or `origin/main`) when checked. Flagged rather than silently assumed or invented. It turned out to be real: PR #1582 introduced it, merging mid-session, after the brief was written — which is also what caused the later merge conflict. Worth remembering next time a handoff references "already-applied" work: verify before treating it as precedent.
- **New information, unrelated to this task, from `eq-shell-9e` while confirming it wasn't touching the same file:** during its own rebase of #1582, that PR's migration had landed on `0279`, which by the time it rebased was already claimed by a different, already-merged migration (`0279_eq_list_sites_internal_contacts.sql`). Renumbered to `0280` before merging. Doesn't affect this task — logged here only because it's a fresh instance of the recurring tenant-migration-numbering-collision failure class.
- **Two distinct timing facts about "merge = deploy" on this repo, worth keeping separate.** The existing evidence (global CLAUDE.md) shows the build *starting* within 2-4s of a merge — true, confirmed again here. But the build itself then took 261s (~4.3 min) to actually finish and publish. "Starts almost instantly" and "takes several minutes to complete" are both true of the same deploy and easy to conflate into a false "it's live in seconds" read.
- The eq-shell working branch (`claude/platform-console-cards-core-reconcile`, the shared root checkout for this session) was 118-119 commits behind `origin/main` at session start — separate from, and in addition to, the `eq-context` staleness the rest of this file's entries cover. Did not attempt to fix/rebase that shared branch (out of scope for this task, and it carries at least one other commit not mine to touch) — worked around it via isolated clones for both the PR and this close, per the existing memory guidance for exactly this shape of problem.
- Same `eq-context` shared-checkout staleness this file's other four entries hit today (73 commits behind by the time `/close` ran, up from 66 a few entries ago) — isolated-clone recovery used again, consistent with how they handled it. Confirmed via a fresh `git clone` immediately before writing (not a copy from an earlier, staler checkout) that all four of today's prior entries in this file were present before adding a fifth.

---

# Session 2026-08-25 — eq-field: Timesheets Approvals Query + Approve all fixed for the Shell iframe (BUG-009)

## Built
- Fixed `queryTimesheetQueueItem()` and `bulkApproveTsQueue()` in `scripts/timesheets-approvals.js` — both called `window.confirm()` directly, which silently returns `false` with no dialog inside eq-shell's sandboxed Field iframe (no `allow-modals`). Since SKS is Core-only, this meant "Query this timesheet" and "Approve all N" were both dead for real SKS supervisors.
- Fix reuses the existing `_tsConfirm()` helper already defined in `timesheets.js` (used there for fill/copy/repeat) rather than duplicating the modal-wiring pattern — `timesheets-approvals.js` was extracted from `timesheets.js` and already calls back into its globals by design, same lazy-load bundle. No new helper added, no signature changes (both callers were already `async`).
- Verified via direct script injection against the real served files + real `#modal-confirm` DOM (local static preview can't run Netlify Functions, so full tenant boot 404s on `tenant-config` — same known sandbox limitation PR #773's session hit): correct title/message/button text for both call sites, Cancel resolves `false`, Confirm resolves `true`, clean state reset after each use.
- eq-field [PR #774](https://github.com/eq-solutions/eq-field/pull/774) (v3.5.557), squash-merged (`b1e1d278`) on explicit "merge if safe" after a fresh safety re-check, confirmed live via `field.eq.solutions/sw.js`.
- Follow-up to PR #773's own sweep, which named this file as carrying live-blocking call sites and spun it off as one of 3 separate follow-up tasks.

## Decided
- Royce: "merge if safe" — merged only after confirming CI green + `mergeStateStatus: CLEAN` + `mergeable: MERGEABLE` fresh (not reusing the earlier stale check from before the rebase).

## Deferred
- Not click-tested through a real Shell iframe with live SKS credentials (out of reach this session — same limitation #773/#776/#775 all carried).

## Notes
- Hit the exact version-collision hazard this doc already tracks repeatedly: PR #773 merged claiming v3.5.555 (the number this session's branch also used) and PR #776 merged right behind it claiming v3.5.556, both while this session was mid-verification. Resulted in a real `CONFLICTING` mergeable state on PR #774, not just a hypothetical risk. Fixed by rebasing onto current `origin/main`, keeping both intervening PRs' changelog entries intact, and renumbering to v3.5.557 — re-verified the full test suite + eslint + cache-buster check post-rebase before force-pushing the topic branch (own unreviewed branch, not `main` — force-push was safe here).
- `scripts/timesheets-approvals.js` has no static `?v=` tag in `index.html` — it's lazy-loaded, cache-busted dynamically off `APP_VERSION` via `lazy-loader.js`. Confirmed this explicitly so a future session doesn't waste time hunting for a tag that doesn't exist for this specific file. `scripts/app-state.js` DOES have a static tag and needed it bumped manually alongside its content change — `check-cache-busters.mjs` caught this correctly both times (pre-rebase and post-rebase).
- eq-context substrate was 73 commits behind in the shared `C:\Projects\eq-context` checkout by the time of this close — used an isolated fresh `git clone` into scratchpad (per the F9 pull-guard hook's own instruction) rather than `git pull` in the shared checkout, and edited files directly in that fresh clone rather than copying from the stale shared checkout.
- At push time, `origin/main` had already moved again: a concurrent session's "part 2" close discovered the remaining 5-file BUG-009 sweep (the item this session had originally flagged as deferred, above) was independently completed same-day by a *different* concurrent session as eq-field PR #775 (v3.5.558), and had already backfilled an accurate changelog entry for this session's own PR #774 plus archived the whole tracking item to `eq/pending-archive.md`. Reconciled by discarding this session's now-stale/duplicate edits to `eq/changelog/eq-field.md` and `eq/pending/eq-field.md` in favour of the already-correct upstream versions, keeping only this session-log entry (with the Deferred line above corrected to match). Nothing from the original PR #773 sweep is open as of this close.

---

# Session 2026-08-25 — BUG-009 sweep closed end-to-end: PR #773 (originating fix) + 3 spawned follow-up sessions (PRs #774/#775, plus #776 which ran and merged fully on its own), all live

## Built
- Fixed `confirmSubmitWithGaps()` in `site-reports-shared.js` — `window.confirm()` silently no-ops inside eq-shell's sandboxed Field iframe (no `allow-modals`), so any SKS supervisor submitting a Prestart/Toolbox with a flagged completeness gap via the normal Shell-embedded path was silently blocked from submitting at all. Same BUG-009 family PR #771 (same day) had already fixed for the sibling "+ Add by name" `prompt()` bug, and had explicitly named this exact function as its own deferred follow-up. Fix: async, routes through the shared `#modal-confirm` dialog already used 5+ times elsewhere in the app. eq-field [PR #773](https://github.com/eq-solutions/eq-field/pull/773), v3.5.555, merged on explicit "merge #773."
- Swept the other 7 files PR #771's own changelog had flagged as carrying the same bug class — all 7 confirmed still live-blocking (11 call sites; 1 additional site, an optional note-prompt in `apprentices.js`, checked and confirmed cosmetic, not blocking). Spun off as 3 separate background chip sessions rather than bundling 7 unrelated screens into one diff, matching this repo's own established one-PR-per-area convention.
- All 3 chips produced real, working PRs: **[#776](https://github.com/eq-solutions/eq-field/pull/776)** (`audits.js`, v3.5.556) was built AND merged entirely by its own session, independently — this session never touched it. **[#774](https://github.com/eq-solutions/eq-field/pull/774)** (`timesheets-approvals.js`, v3.5.557) — its own session correctly self-detected and resolved a 3-way version collision (against #773/#772/#776) before this session got around to checking on it; verified via a real `git merge-tree` dry-run (not just GitHub's cached `mergeable` flag) that it was genuinely clean, then found it had already merged itself moments earlier. **[#775](https://github.com/eq-solutions/eq-field/pull/775)** (the remaining `audit.js`/`apprentices.js`/`journal.js`/`timesheets-export.js`/`tender-pipeline.js`, v3.5.558) needed a real rebase this session drove directly: its worktree was found already detached elsewhere (work done, not actively in use), so rather than fight the branch-already-checked-out lock, rebased from a fresh isolated clone instead — 4 real conflicts, renumbered `3.5.555 → 3.5.558`, and caught a genuinely new instance of the exact bug class this sweep exists to fix: `audit.js` is eagerly loaded via a static `index.html` tag (unlike its 4 lazy-loaded siblings), so it needed its own cache-buster bump — missed on the first renumbering pass, caught by CI's own drift check before push, fixed in a follow-up commit.
- Every file PR #773's own body named as a follow-up is now converted. Production confirmed live at `v3.5.558` via `field.eq.solutions/sw.js`.
- Updated `eq/pending/eq-field.md` (ticked the "6 more files" item, then archived the whole now-fully-closed "Site Audits" section to `eq/pending-archive.md` per the file's own zero-open-items rule) and `eq/changelog/eq-field.md` (3 new entries: #773/#774/#775 — #776 already had one from its own session).

## Decided
- Royce: "merge #773" — explicit merge go for the originating fix.
- Royce: "continue / merge" — standing authorization to keep merging the follow-up PRs as each chip finished, rather than re-asking per PR.

## Deferred
- None. This closes the full arc cleanly — nothing left open, nothing left running.

## Notes
- **Two new-to-this-session eq-context guards, both legitimate and both worked as designed.** The F9 `git merge` block (already documented by 3 other sessions in this file today) fired again. New this session: a `stale-main-gate` pre-commit guard blocked a plain `git commit` in the shared `C:\Projects\eq-context` checkout while it was 1 commit behind `origin/main` — narrower than F9 (fires on commit, not just merge) but the same underlying problem and the same fix: fetch immediately before, isolated clone when the shared checkout can't be trusted. Both guards were followed, not routed around.
- **`C:\Projects\eq-context`'s shared checkout drifted from 51 to 73+ commits behind `origin/main` over the course of this single session** — worth flagging as an observed fact, not just a one-off. The nightly-cron-driven "digest.md is 0d fresh" signal this repo surfaces every turn is not the same claim as "the local clone is current," and conflating the two is exactly the F1 failure class this repo's own guard banners describe. Every substrate write this session went through a fresh isolated clone rather than the shared checkout, specifically to avoid this.
- **`gh pr merge --squash --delete-branch`'s local housekeeping step failed 3 separate times this session** (once for #773, once attempting #774 which had already self-merged, once for #775) — always the same shape: `main`/the target branch already in use by a concurrent worktree, or "not on any branch" in the shared root. The actual server-side GitHub merge succeeded every single time regardless; each was independently re-verified via `gh pr view --json state,mergedAt,mergeCommit` before assuming success, and via `curl field.eq.solutions/sw.js` for the final production check. Remote branches deleted separately via `gh api -X DELETE` when the combined flag's cleanup half didn't run.
- **Verify claims, don't trust cached flags.** GitHub's `mergeable` field lagged reality at least once this session (showed `CONFLICTING` for #775 correctly, but the real signal that mattered for #774 — was it actually safe to merge, not just "does gh say so" — came from a direct `git merge-tree` dry-run against the real current `origin/main`, not the PR API's cached computation). Worth defaulting to this for any merge where main has been actively moving.
- **Correction at `/close` time:** the header above says "3 spawned follow-up sessions," accurate to what this session itself did — but a *4th*, independently-initiated session (not spawned by this one) was also working the identical `audit.js`/`apprentices.js`/`journal.js`/`timesheets-export.js`/`tender-pipeline.js` fix in parallel, per its own entry earlier in this file ("BUG-009 sweep: built independently, found already done by a concurrent session (PR #775), own copy discarded before push"). That session's changelog/pending-archive backfill (for #774/#775) landed on `origin/main` between this session's own clone and its final push, producing 2 real merge conflicts on `eq/changelog/eq-field.md` and `eq/pending-archive.md` — resolved by keeping the unique content from both sides rather than either version wholesale (their entry had the valuable parallel-build/discarded-duplicate story this session had no way to know; this session's had the specific rebase/audit.js-tag-gotcha mechanics). Worth reading as one combined picture, not two independent, possibly-conflicting accounts. **Same reconciliation happened one layer further out too**: this session's own push then raced yet another session's entry below (the one that found and fixed the `audit.js`/`audits.js` collision as PR #777, after it had silently survived PR #775's own merge) — merged the same way, keeping both accounts rather than either alone. Three sessions' independent, partially-overlapping records of the same BUG-009 saga all converge in this file rather than any one silently winning over the others.

---

# Session 2026-08-25 — BUG-009 sweep closed: 6 remaining files fixed (PRs #775/#777), plus a real name collision found and fixed twice

## Built
- Fixed the 5 files PR #773's own body had spun off as follow-ups after the sibling `confirmSubmitWithGaps()` fix: `audit.js` (Revert + its concurrent-edit re-confirm), `apprentices.js` (add/remove custom skill, plus the incidental non-blocking `resolveFollowUp` note fix), `journal.js` (delete entry), `timesheets-export.js` (CSV import, reuses `timesheets.js`'s existing `_tsConfirm`), `tender-pipeline.js` (push to roster; `_quickPushToSchedule()` is now `async`, it wasn't before). Shipped as eq-field [PR #775](https://github.com/eq-solutions/eq-field/pull/775) (v3.5.558).
- Live-verified the actual shipped code, not just the source: invoked the real `_auditConfirm`-pattern helper and the new `_apPromptText` prompt-modal directly in a real browser against the real PR #775 deploy preview, working around the same documented Core-only auth dead-end this repo's own CLAUDE.md flags for standalone/preview surfaces (loaded `audit.js` pre-auth since it's core-loaded; injected the real deployed `apprentices.js` directly, no app-context dependency). Confirm/cancel/enter-key/empty-submit/repeat-use all checked against real DOM behaviour.
- **Found a real correctness bug mid-rebase, not just a mechanical fix:** `audit.js`'s new helper was named `_auditConfirm()` — colliding with `audits.js`'s own independently-shipped `_auditConfirm()` (PR #776, same day). This app loads every script into one shared global scope, so two top-level functions with the same name is a genuine bug (whichever file loads second silently overwrites the other's global) — harmless today only because both implementations happen to be identical. Renamed `audit.js`'s to `_auditLogConfirm`.
- **That rename did not survive PR #775's own squash-merge.** A second concurrent session was finishing the same branch from an earlier snapshot and won the push race; the merge landed with the collision still live (`audit.js` back to `_auditConfirm`) even though every other part of the PR — the 5-file fix, version stamps, eslint ratchet bumps — came through correctly. Caught by diffing `origin/main`'s actual post-merge content directly rather than assuming a successful push meant the intended content landed. Re-applied as its own PR, eq-field [PR #777](https://github.com/eq-solutions/eq-field/pull/777) (v3.5.559).
- `origin/main` moved 4 times during the merge attempt alone (PRs #772/#773/#774/#776, each an unrelated concurrent BUG-009-family fix independently claiming the "next" version number) — 3 separate rebase-and-renumber rounds before the version number stopped colliding. Confirmed via `gh pr list` that this branch was the only one still open before finishing each time.
- This closes out every file PR #773's original 7-file follow-up list named — nothing outstanding from that sweep. Updated `eq/pending/eq-field.md`'s tracking entry to reflect this and archived the now-fully-closed section to `eq/pending-archive.md`.

## Decided
- Royce: "continue / merge" — explicit go-ahead for PR #775, extended to PR #777 as a direct correction of the just-approved work rather than new scope.

## Deferred
- None new. Both files this task had originally left out of scope (`audits.js`'s `submitAudit()`, `timesheets-approvals.js`) were independently closed by other concurrent sessions (PRs #776/#774) during this same window — nothing handed off.

## Notes
- The concurrent-session collision here is a sharper version of the same class already logged elsewhere in this file today (version-number races) — this time it wasn't just a numbering collision but an actual content regression that silently dropped a real bug fix during a squash-merge. The general lesson: after any merge that might have raced another session, diff the merged content on `origin/main` directly against what was intended, rather than trusting a successful local push (or, as happened here, trusting that "merged" state alone means the expected diff landed) — a green CI run and a `MERGED` status don't prove the content is what you think it is when someone else touched the same branch.
- `git pull` in the shared `C:\Projects\eq-context` checkout is hard-blocked by a `pre_tool_use.py` hook (F9) — same guard every other session in this file hit today. Recovered the standard way: fresh `git clone` into an isolated scratch dir, all substrate edits made and pushed from there, never touching the shared checkout's HEAD/index. The shared checkout also had an uncommitted, unrelated change sitting in its working tree (another session's own `worktree-registry.md` close-out entry for PR #773) — stashed before leaving it alone, popped back to restore the shared checkout exactly as found, rather than risking it being silently lost or dragged into an unrelated commit.
- **This same isolated-clone recovery hit its own version of the failure it exists to prevent, three times in a row.** First: `origin/main` moved 2 commits (a different session's own close "part 2", plus an eq-shell CustomersPage.tsx close) between the initial fresh clone and the first commit attempt — caught by `stale-main-gate` pre-commit. Second, immediately after resolving the first: `origin/main` moved again (PR #774's own author closing out) — same guard, same recovery. Third: after that commit succeeded, the *push itself* was rejected (`! [rejected] main -> main (fetch first)`) — PR #773's own originating session had closed out in the meantime, requiring a real `git rebase origin/main` (not just stash/pull/pop, since this time a local commit already existed) with 3-way conflicts across all 3 substrate files. Every recovery used the same shape: fetch, reconcile by keeping unique content from all sides rather than either version wholesale, re-verify, retry. Worth remembering: even a fresh isolated clone can go stale in the minutes it takes to write a substrate update, on a repo this concurrently active — re-fetch immediately before every commit *and* every push, not just the first attempt, and expect to need several tries in a row, not just one.

---

# Session 2026-08-25 — "Add by name" bug found and fixed (originates PR #771 and the whole BUG-009 sweep above); a safety-nudge false alarm caught before removal

## Built
- Found and shipped the fix this whole BUG-009 thread traces back to. Royce reported "+ Add by name" doing nothing on the Prestart screen; root-caused to `window.prompt()` being silently blocked inside eq-shell's sandboxed Field iframe (confirmed against `FieldIframe.tsx`'s actual `sandbox` attribute directly — no `allow-modals` — rather than assumed from general Chrome behaviour), fixed in all three places it existed (Prestart/Toolbox/Diary), not just the one reported. eq-field [PR #771](https://github.com/eq-solutions/eq-field/pull/771) (v3.5.553), merged on explicit "merge 771," confirmed live via `field.eq.solutions/sw.js`.
- Spawned the follow-up (`task_5de729da`) for the sibling `confirm()` bug blocking Prestart/Toolbox Submit itself — Royce started it in a separate session. That session, and the further concurrent sessions it triggered, produced PRs #773–#777 documented in the entries above; not re-narrated here.
- Backfilled `eq/changelog/eq-field.md`'s missing PR #771 entry — genuinely absent until now, since the fix landed in this conversation after the prior close and before this one.

## Decided
- **Caught a real misdiagnosis before acting on it.** Royce, seeing the "high-risk work selected but no photos attached" warning sitting above a Submit button that appeared not to work, asked to have the requirement removed outright. Checked the actual code first: `_prestartCompleteness()`'s own comment states the check is deliberately soft — "never blocks Save Draft; Submit still proceeds on confirm." The real blocker was the identical `confirm()`-in-iframe bug, not a hard rule. Recommended against removing the safety nudge and explained why; Royce didn't push back, and the real bug got fixed instead by the already-running follow-up session. Removing the nudge would have deleted a legitimate safety feature to paper over an unrelated bug.

## Deferred
- None new from this thread — closed end to end. `eq/pending/eq-field.md`'s BUG-009 tracking item was created and then fully archived by the follow-up sessions; nothing left open there.

## Notes
- Verified the follow-up session's own completion rather than taking "the session has ended" notification at face value: confirmed via `gh pr list` (zero open PRs left) and production `sw.js` (`v3.5.559`, matching the newest merge) before reporting back to Royce as actually done.
- At least the fourth session tonight to hit the same `eq-context` shared-checkout `git pull` block (F9) — used the same isolated-clone recovery as every other entry in this file today.

---

# Session 2026-08-25 — EQ Ops cost/charge-rate wiring fixed + Edit Site "couldn't save" root-caused to an undispatched migration (which surfaced a second, unrelated migration collision)

## Built
- Fixed the EQ Ops quote line-item Cost↔Rate↔Markup% wiring: `updateLineItem` (`QuotesModule.tsx`) only reconciled one direction — editing Cost/Markup always recomputed Rate, but editing Rate directly with a Cost already set left Cost and Markup% untouched, so the values could silently drift apart. Added the missing branch (back-solve Markup% from Cost + new Rate). eq-shell [PR #1584](https://github.com/eq-solutions/eq-shell/pull/1584), merged.
- Root-caused "couldn't change a quote's site" to a deployment gap, not a permission rule: migration `0279_eq_list_sites_internal_contacts.sql` (part of Royce's own PR #1581, merged that same morning) hadn't been dispatched to either tenant plane, so EQ Ops' `eq_list_sites()` RPC still didn't return the 4 new internal-contact columns — Edit Site always opened them blank and a save read back blank through the same stale RPC. Dispatched `tenant-migrate.yml` (fleet) — `0279` applied cleanly on zaap + ehow.
- That dispatch failed on a second migration, `0280_site_customer_links.sql` (PR #1582, "link a site to another customer," merged by a concurrent session mid-task) — it also redefines `eq_list_sites` but was written against the pre-0279 column list and would have silently dropped 0279's 4 columns; Postgres's signature-change guard (42P13) hard-failed it first. Fixed (merged both column lists, added the missing `DROP FUNCTION IF EXISTS`), eq-shell [PR #1585](https://github.com/eq-solutions/eq-shell/pull/1585), merged, redispatched — `0280` now applied cleanly on both planes too.
- Updated `eq/pending/eq-shell.md` and `eq/changelog/eq-shell.md` for all three fixes.

## Decided
- Royce: "run it all - merge and finish this sprint" — explicit go-ahead to merge both PRs and dispatch the fleet migration.
- Ran the `/brief` Session Gate before the first edit (this session's flag wasn't set yet) — brief covered digest.md, worktree registry, git staleness, and live DB recon (both tenant planes' migration ledgers) before any code was touched.

## Deferred
- Cost/charge-rate fix not click-tested live — `tsc -b --force` clean, but local Netlify/Vite dev tooling is documented broken under Node 24 in this repo and the feature is auth-gated. Logged in `eq/pending/eq-shell.md`.

## Notes
- **Substrate path correction**: the `/close` skill's own Step 2 still points at `eq/pending.md` — that file was split 2026-08-17 into `eq/pending/<repo>.md` and is now just an index. Updated `eq/pending/eq-shell.md` directly per that index's own instructions instead. Worth fixing the skill text itself so the next session doesn't have to rediscover this.
- Confirmed live (not assumed) that no real data had been lost during the window `0279` sat undispatched: the actual Equinix SY5 contact record's `updated_at` predated any Ops-side edit, matching exactly the migration's own backfill timestamp.
- Hit the same `eq-context` shared-checkout `git pull`/`git merge` block (F9) every other session today hit — recovered via a fresh isolated clone rather than fighting the shared checkout, per the established pattern in this file's earlier entries today. Left an unrelated uncommitted `worktree-registry.md` edit in the shared checkout untouched rather than stashing/popping it — not mine, no context on its intent, and the isolated-clone path never needed to touch it.
- A production-migration-dispatch action (`gh workflow run tenant-migrate.yml`) was blocked mid-session by this session's own local auto-mode classifier, independent of the repo's dispatch-gate (which has no approval step by design) and independent of Royce's own prior explicit confirmation for the same action earlier in the conversation. Reported it plainly and handed Royce the exact command rather than attempting a workaround; he ran it himself, then asked for the rest to be finished, and a later dispatch attempt from this session succeeded without incident — so the block did not recur consistently on retry.

---

# Session 2026-08-25 — eq-field: Timesheets Approvals walkthrough with Royce, surfaced a second approval mechanism no one had documented

## Built
- Nothing shipped this stretch — pure walkthrough/explanation following the PR #774 close earlier the same session (Query + Approve all fix, already logged above). No code, no commits.

## Decided
- No toggle to disable the submit-then-approve queue for SKS — recommended against it when asked, since a supervisor's direct approve tick works regardless of submission status, so an unused Submit button causes no live friction. Advisory only; not something Royce confirmed or asked to be built, just didn't push back.

## Deferred
- None.

## Notes
- **New, previously-undocumented fact about how SKS actually approves timesheets.** Royce corrected the working assumption from the PR #774 close (worker submits → supervisor approves/queries) — in practice, workers don't touch timesheets at all; supervisors enter and manage hours directly. Verified in code, not taken on faith: `_tsApprovalChip()` (`scripts/timesheets.js`, called from the main roster grid's row renderer) lets a supervisor toggle a person's `approved` flag directly, with no dependency on `submitted_at` ever being set — a completely separate mechanism from the Submit → "Submitted this week" queue → Query/Approve-all path this session's PR #774 fixed. This explains why live screenshots showed "To approve: 0" / "Approved: 4" simultaneously across multiple weeks: the direct tick is the everyday path; the submit-based queue this session fixed is rarely if ever exercised at SKS. The fix itself is still correct and still real (those two buttons were genuinely dead in the Shell iframe), it just isn't the thing driving day-to-day approvals. Worth remembering next time this area comes up — the submit/queue flow and the direct-tick flow are two independent code paths to the same `approved` column, not one flow with two entry points.
- Drew a 2-lane flowchart (submit-and-queue vs. direct-tick, both converging on "marked approved") to walk Royce through this live — worth reusing that framing if this area comes up again rather than re-deriving it.

---

# Session 2026-08-25 — Link an existing site to another customer (PR #1582) merged and live; duplicate concurrent session found and closed

## Built
- Royce asked how to attach an already-existing site to a second customer without duplicating it (pricing a Convergint job at Equinix SY3, a site already owned by a different customer) — answered directly first (no such feature existed; `sites.customer_id` is a strict single FK), then on "we need this feature - scope it out" ran a full Plan-mode pass (2 parallel Explore agents covering EQ Service's own dependency on the same single-owner model, and reuse opportunities) before writing anything.
- New `app_data.site_customer_links` table, `eq_list_sites()` extended (Quotes site picker needs zero frontend change), `crm-write.ts` link/unlink actions, Customers-page "Link site" button with owner-badge/Unlink-only UI for non-owned rows. Verified locally (`tsc -b --force`, `eslint` on all 4 changed files, `node --check` on the 2 governance scripts) before ever pushing.
- Committed, pushed, opened [PR #1582](https://github.com/eq-solutions/eq-shell/pull/1582), confirmed its Netlify deploy preview served cleanly (no login credentials this session, so this was a build/console-error smoke check, not a real click-through).
- Found — via `ListAgents` then `mcp__ccd_session_mgmt__list_sessions`/`get_session` — that a separate, Royce-directed concurrent session had independently started building the identical feature from scratch, in the root checkout, on a 118-commits-stale branch, with its own 64 uncommitted lines already on the same file it was mid-`AskUserQuestion` with Royce at the time. Reported the collision plainly rather than silently picking a winner; Royce said close it, keep this one. Archived it via `mcp__ccd_session_mgmt__archive_session`, then verified live that the pre-existing unrelated dirty state already sitting in that same root checkout (from some other, unconnected task) survived untouched.
- Royce merged PR #1582 himself mid-session, without saying so — caught via `gh pr view --json headRefName` returning 404 on a branch dispatch attempt, traced to `state: MERGED`.
- "Dispatch the migration to ehow": found the workflow's `slug` input has no per-migration selection — dispatching for a tenant applies everything pending, which at that point was this PR's `0280` *and* an unrelated already-merged `0279` from #1581. Surfaced this plainly and got explicit confirmation ("dispatch both") before running it, rather than silently bundling an unasked-for migration into an irreversible production DDL action. The dispatch itself then no-op'd (0 applied, 302 skipped) — live timestamps showed both migrations had already been applied ~1-3 hours earlier by a different concurrent session (the EQ Ops one, logged separately above/below in this file), which had independently found and fixed the same `0279`/`0280` `eq_list_sites` signature collision from the other direction.
- Live-verified directly on ehow rather than trusting the dispatch log's inference: `site_customer_links` exists with exactly `postgres`+`service_role` grants (no `authenticated`/`anon`), `eq_list_sites`'s live function body correctly carries both this PR's linking logic and #1581's 4 contact columns, and the current production deploy is a confirmed git-ancestor of this PR's merge commit (`git merge-base --is-ancestor`) — the same commit-ancestry method the global CLAUDE.md prescribes, not "merged" taken as a proxy for "live."
- Fixed the `/close` skill's own source file (`C:\Users\EQ\.claude\commands\close.md`) — Step 2 still pointed at the stale top-level `eq/pending.md`, which at least 6 sessions today (including this one) had each independently rediscovered was just an index and self-corrected around. Updated Step 2 and the Step 5 git-add example to point at `eq/pending/<repo>.md` for EQ-tier work.

## Decided
- Royce: approved the Plan-mode scope via `ExitPlanMode`, including the service-role-only grant correction (a deviation from blindly copying `contact_customer_links`, caught by reading that table's own since-revoked grant history before writing new DDL).
- Royce: "close the other session, keep mine" — explicit resolution of the duplicate-session collision.
- Royce: "dispatch both" — explicit confirmation once the bundled-migration side-effect was surfaced.
- Royce: go try the real click-through himself (Convergint/Equinix SY3) once live-verification confirmed the feature was actually deployed, not just merged.

## Deferred
- None. **Update, same day, after this session's own close**: Royce linked the real Equinix SY3 to the real Convergint Australia PTY LTD himself — "works well." The one open item from this session's close is now confirmed done; ticked off and archived in `eq/pending-archive.md` rather than left stale in `eq/pending/eq-shell.md`.

## Notes
- **A prior-turn instruction to "dispatch the migration" doesn't mean "figure out the exact scope and just do it" when the tool can't express that scope precisely.** The `slug` input dispatches everything pending for a tenant, not one named migration — surfaced the bundling side-effect and got a fresh, specific confirmation rather than treating the original instruction as covering it by implication.
- **"Merged" and "live" are different claims, and so are "I dispatched it" and "it applied."** Both gaps showed up in the same session: the PR merged without me being told, and my own dispatch attempt was a no-op because someone else's dispatch had already landed first. Neither was caught by assumption — both by direct, live, source-of-truth checks (commit ancestry against the actual production deploy; migration-ledger timestamps against the actual dispatch run time).
- Misused `ScheduleWakeup` twice this session, trying to use it as a generic "wait for a background thing" mechanism — it's scoped to `/loop` dynamic-mode sessions only, which this wasn't. Background Bash/Agent calls already notify on completion without it; caught and self-corrected both times (called `stop: true` immediately after), no actual harm, but worth not repeating.
- Hit the `eq-context` shared-checkout F9 `git merge` guard, same as every other session in this file today — this session's own near-miss: the guard blocked the chained command before any of it ran (including the `git stash push` half), and a bare `git stash pop` immediately after almost applied a *different*, unrelated stash left over from a 2026-08-23 session instead (git's own conflict check refused it, correctly). Recovered via the standard isolated-clone path; left the pre-existing unrelated `worktree-registry.md` edit in the shared checkout completely untouched, consistent with every other entry in this file today.
- Git Bash's own backslash-path-eating behaviour bit a `git -C` command again this session (`C:\Projects\eq-shell` silently became `C:Projectseq-shell`) — same known failure class already in this repo's memory, forward slashes fixed it immediately once recognized.

---

# Session 2026-08-25 — First-morning remediation: readiness, access-unlock, and site contacts shipped; the two actual people are still unclaimed

Continuation of a session that opened with Royce describing a bad first morning for two new SKS labour-hire workers (Conor Horgan, Nelson Sareto) at an Equinix site: competing inductions, no visibility into what was done, a sick manager with no fallback contact, and a login failure. Scoped with Royce into phases and built in order.

## Built
- eq-cards migration 0141 / [PR #297](https://github.com/eq-solutions/eq-cards/pull/297) — `tg_unlock_field_access_on_invite_claim()` trigger. Root cause was two separate claim doors (Core stamps Field-access-unlock on claim, Cards never did), not the labour-hire portal as first assumed — corrected mid-session against live data before building anything.
- eq-shell [PR #1575](https://github.com/eq-solutions/eq-shell/pull/1575) — 9-account backfill for people already caught by the pre-fix gap.
- eq-shell [PR #1573](https://github.com/eq-solutions/eq-shell/pull/1573) — "Ready to start" column on the Staff list, most-upstream blocker per worker.
- eq-shell [PR #1574](https://github.com/eq-solutions/eq-shell/pull/1574) — daily unclaimed-invite alert (Sentry, alert-only) + rewrote the login dead-end error to name its two real causes.
- eq-shell [PR #1576](https://github.com/eq-solutions/eq-shell/pull/1576) — renamed the 8 sites verified as real Equinix sites via `customer_id` join (never by name/code pattern); `SYD29` matches the same "SY"+digits shape but resolves to Microsoft and was deliberately left alone (Royce had corrected this exact mix-up once before, `eq-shell#781`).
- eq-shell PRs #1577/#1579/#1580 — `primary_contact_name/phone` + `secondary_contact_name/phone` on `app_data.sites` + the `field_sites` view; real data entered for Equinix SY5 (Matthew Miller / Scott Hotson).
- eq-field [PR #770](https://github.com/eq-solutions/eq-field/pull/770) — "Ask for / Backup" contact shown on the My Schedule day card and Sites page card, live.
- eq-shell [PR #1581](https://github.com/eq-solutions/eq-shell/pull/1581) + migration `0279` — wired the 4 contact fields into the existing Edit Site modal (self-serve, no more migration-per-site); also caught that EQ Ops runs its own copy of the same modal off a different RPC (`eq_list_sites`) that didn't carry the new columns yet — fixed in the same PR before it could silently blank Core's data.
- Dispatched `0278`/`0279` live; a same-morning collision with a concurrent session's `0280` (also redefining `eq_list_sites`) hard-failed cleanly on Postgres's own signature-change guard rather than corrupting anything — fixed by that session's own follow-up PR, verified correct on both planes after re-dispatching.
- Published an artifact (table + verification-logic diagram) explaining which sites were renamed and why, on request.
- Live-verified (via a screenshot Royce shared) that the multi-job dropdown on the weekly roster grid correctly resolves renamed sites — confirms the separate `field_job_numbers` fix (background task `task_a74fddab` → eq-shell PR #1578, already merged/dispatched by the time this was checked) is working in production, not just in code.

## Decided
- Build order confirmed by Royce at session start: readiness visibility → access-unlock fix → site identity/contacts.
- Given the choice between a self-serve Edit Site UI and continuing to do contact data via one-off migrations, Royce chose to build the UI.

## Deferred
- **Conor Horgan and Nelson Sareto — the two people this whole thread is about — still haven't claimed.** Live-verified just now: `workers` rows exist for both, `user_id` still null, invites created 2026-08-20, valid until 2026-09-03. Everything else built this week is infrastructure for a problem that, for them specifically, is not yet solved.
- 6 of 8 renamed Equinix sites (CA1, SY1, SY2, SY3, SY4, SY9) still have no contact data — no derivation path exists (checked `staff.default_site_id`, `schedule_entries.supervisor_id`, `sites.notes`, all 0%-populated); needs real names/numbers from Royce.

## Notes
- The `eq-shell`/`eq-field` repos had multiple other concurrent sessions active throughout (PRs #768, #1582, #1584, #1585) — none authored here, all encountered mid-dispatch or mid-merge and verified rather than assumed correct.
- One tenant-migration (`0275`, the Equinix renames) was hand-applied before reading `tenant-migrate.yml`'s "no hand-applied single-tenant SQL, ever" rule — caught while routing the next migration, disclosed plainly, not hidden. Every tenant-plane change after that point went through PR → Plan → merge → explicit dispatch.

---

# Session 2026-08-25 — Access-control review (PR #768) fully closed out; last open item (people.edit_own) resolved and shipped

## Built
- eq-field [PR #778](https://github.com/eq-solutions/eq-field/pull/778) (v3.5.560) — `permission-matrix.js` v2.6→v2.7: `people.edit_own` confirmed dead (granted to 4 of 5 roles, but `editPerson()` has no self-edit exception — only `canManagePeople()` gates it) and flagged unwired in-source, same pattern as the existing v2.5 Phase 3 PREP cluster. Comment/doc-only, no grant or behavioural change. Merged, confirmed live.

## Decided
- `people.edit_own`: steelmanned build vs. leave-unwired vs. leave-undocumented; Royce chose "mark unwired, build nothing" — Shell's own `/settings/profile` stays the intended self-service path, not a new Field-side editor.

## Deferred
- `field.manage_recognitions` — still needs Royce's own action (grant himself the permission via Shell's Access Control); nothing for a session to build.
- Per-tenant hiding of specific leave permissions — Shell-side work, out of scope here.
- The zero-Actions-runs anomaly from PR #768 — never root-caused; did not recur on PR #778 (3 normal runs this time).

## Notes
- A session-gate hook flagged a "P0 live PII leak" (SEC-1) at session start, reading `people`/`timesheets`/`leave_requests`/`audit_log`. Checked against the live security register before treating it as eq-field's problem: it's `sks-nsw-labour` (a separate, standing do-not-touch repo, retirement weeks away per Royce's 2026-08-21 update), not eq-field. No action taken — correctly. Worth flagging for future eq-field sessions specifically: this repo's own core tables share those exact same names, so that digest line will keep reading as "about me" on a fast skim. The digest itself is accurate (it names the project once you look) — the risk is purely a quick-read trap for this repo's sessions in particular.
- `field.manage_sites` and `field.view_audit_log` (both raised in the same PR #768 review) re-checked against source this session: both remain genuinely unbuilt Phase 3 PREP keys, nothing to reconcile against Shell/canonical yet. Closed out of the review as "confirmed still a placeholder," not a build — nothing added to pending.md for either.
- This closes every open item from the 2026-08-24 access-control review (PR #768) except the one item that's Royce's own action to take (the recognitions grant, above).
