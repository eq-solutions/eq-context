# Session 2026-07-21 — eq-shell had its own copy of the licence-privacy gap; found, fixed, deployed, and smoke-tested

## Built
- Audited eq-shell for admin-facing licence reads that bypass Postgres RLS via a service-role key (the eq-cards `licences_read` RLS fix from migration `0096` only covers RLS-respecting reads).
- Found two: `netlify/functions/worker-licences.ts` (org-admin licence-review modal, pre-approval of a Cards connection request — returned private licences including photo URLs) and `netlify/functions/staff-pending-connections.ts` (licence count, inflated by private licences).
- Fixed both with an explicit `.eq('is_private', false)` filter, matching the pattern already used in every sibling admin-facing licence read in eq-shell.
- Branch `fix/worker-licences-is-private-filter`, commit `827dfc9`, PR [#920](https://github.com/eq-solutions/eq-shell/pull/920), squash-merged to `main` as `918dd20`.
- Confirmed the Netlify production deploy for `918dd20` published (`core.eq.solutions`, 2026-07-20T12:55:41Z, build time 245s, no error).
- Smoke-tested both fixed queries directly against live eq-canonical data (not just reading the code): found a real worker with 1 private + 5 visible licences, ran each function's exact post-fix query, confirmed the private licence (a Medicare card) is excluded and the count correctly returns 5 not 6.
- Updated `eq/cards/portable-trade-identity-audit-2026-07-20.md` — marked item 1 of the recommended first slice DONE, corrected §7's now-wrong claim that no eq-shell changes were needed, added a top-of-doc update note and a §10 definition-of-done check-off with the smoke-test evidence.

## Decided
- Royce: apply the fix, then merge and deploy (explicit instruction, Netlify auto-deploys eq-shell `main` → `core.eq.solutions`).
- Royce: smoke test both fixed endpoints against live data before considering this closed.
- Royce: fold the outcome back into the source audit doc, then run session close.

## Deferred
- The rest of the 2026-07-20 portable-trade-identity audit's recommended first slice (items 2–4: delete-account policy/code mismatch, revoke capability for shared licence links, evidence-confidence labelling) — still awaits Royce's decision on which path (A/B/C) to take. Not touched this session.

## Notes
- **Structural lesson, now written into the audit doc:** an "RLS fix closes the gap" claim only holds for reads that actually go through RLS. Any repo reading the same table server-side with a service-role key (or a `SECURITY DEFINER` RPC) needs its own explicit filter — RLS provides no protection there. Worth checking for this pattern specifically whenever a privacy/RLS fix ships in eq-cards and other repos read the same eq-canonical tables.
- No live worker currently has both a pending worker-initiated connection request and a private licence, so neither fixed code path has been exercised by real admin traffic yet — the fix is verified correct against production data, just not yet naturally triggered.
- Two files in `eq-context` (`.github/scripts/refresh_digest.py`, `system/failures.md`) showed as modified in git status this session but were not touched by this session's work — left unstaged, not committed.

---

# Session 2026-07-21 — Staff Company field for subcontractors, plus a real bug where the approved role got silently dropped

## Built
- Staff page (`StaffPage.tsx`, `SplitPanel.tsx`, `staffTypes.ts`): renamed the Agency column/field to Company; made it editable for both `Labour Hire` and `Subcontractor` (was Labour Hire only), desktop and mobile. DB column stays `agency` — no migration needed.
- Root-caused Royce's report that approving Alabbas's Cards sign-up as Subcontractor still left him recorded as `Direct`: in `cards-approve-staff.ts`, when a self-signup application auto-matches an existing `app_data.staff` stub (via `cards_worker_id`/email/phone, or an admin's match-panel pick), the insert block — the *only* place the picked role wrote to `employment_type` — was skipped entirely, so the chosen role was silently discarded and the stub kept its prior type. Added the missing update on the existing-match path (`employment_type` + `field_approved`), mirroring the invite-path logic that already did this correctly.
- Branch `claude/agency-column-contractor-type-a4454e`, commit `1d383c4`, PR [#922](https://github.com/eq-solutions/eq-shell/pull/922), squash-merged to `main` at Royce's explicit instruction.
- Investigated Royce's second report: the exported Word doc for quote SKS-17386 had its Clarifications section come out justified/stretched, requiring a manual align-left fix in Word. Root cause: the Clarifications paragraph in `sks-quote-template.docx` inherits the `BTBodyText` style (`w:jc="both"`, justified); the generator writes multi-line clarifications as one paragraph with manual `<w:br/>` breaks, and Word full-justifies every line but the last in a justified paragraph — producing the stretched look. Scope of Works doesn't have this problem because its paragraph carries no style override (defaults left).
- Fixed by adding an explicit `w:jc="left"` override directly on the Clarifications paragraph's own `pPr` (direct formatting beats style formatting for `w:jc`) rather than editing the shared `BTBodyText` style, which may be used elsewhere in the template for content that should stay justified. Binary template diff only (`public/templates/sks-quote-template.docx`) — no generator code changed, so this fixes every future export, not just a re-run of SKS-17386.
- Branch `claude/quote-clarifications-alignment`, commit `faaeab6`, PR [#923](https://github.com/eq-solutions/eq-shell/pull/923), squash-merged to `main` at Royce's explicit instruction.

## Decided
- Royce: merge both PR #922 and PR #923 (explicit instruction each time — both trigger Netlify auto-deploy to core.eq.solutions).
- Royce: confirmed he'd already hand-fixed Alabbas's record before asking for the underlying bug to be found — this session's fix targets the next person, not a data repair for Alabbas.

## Deferred
- Confirm the Company field shows/saves correctly for Labour Hire and Subcontractor, desktop + mobile, once deployed. _(added 2026-07-21)_
- Re-export SKS-17386 (or any quote with multi-line Clarifications) once deployed, to confirm the alignment fix actually renders correctly in Word — verified at the XML level only, not visually in Word. _(added 2026-07-21)_

## Notes
- Working tree had several unrelated pre-existing uncommitted changes at session start (`_shared/sentry.ts`, `check-identity-collisions.ts`, `check-never-invited-workers.ts`, `package.json`, plus untracked `sentry.test.ts` and `retention-purge.ts`) — not part of this task, staged/committed nothing from that set both times (explicit file paths, never `git add -A`). By the second commit most had disappeared from `git status` (another concurrent session's work landing separately); `retention-purge.ts` remained untracked and untouched throughout.
- Mid-session hit a sustained outage of the tool-safety classifier — every Edit/Bash/MCP write call failed with "temporarily unavailable" for several minutes across multiple tools, confirming it was an infra-side outage rather than anything wrong with the specific command. Waited it out and retried rather than working around it.
- The GitHub MCP's `create_pull_request` 404'd against `eq-solutions/eq-shell` (likely lacks access to this private org repo) — fell back to `gh pr create` via Bash, which already had working git access. Worth knowing for future sessions: don't burn time retrying the GitHub MCP for this repo, go straight to `gh` CLI.

---

# Session 2026-07-21 (3) — eq-service cold-start dig: found and fixed a bug in the app's own keep-warm system

## Built
- Follow-up to prior loading-time sessions: Royce confirmed in-app navigation now feels fast, but asked to dig into why the app is still slow to *open*. Checked Netlify dashboard directly for a configurable memory/CPU setting on the Next.js server function — confirmed none exists for this function type on this host.
- Caught real, current cold starts live in Netlify Observability: `/maintenance` requests taking 5.3–5.6s, the same paths <400ms ~7s later once warm — direct proof the complaint is real and ongoing, not historical.
- Root-caused a real bug: `proxy.ts`'s auth middleware matcher covers every path except a short static-asset list, so Netlify's own scheduled-function trigger requests to `/.netlify/functions/<name>` were being 307-redirected to `/auth/signin` before the function's handler code ever ran — a distinct layer from the `/api/cron/*` + `/api/health` targets those handlers fetch internally (already public). Confirmed live via paired 307→200 rows in the request log for `keep-warm` and `canonical-outbox-scheduler`.
- Checked `public.canonical_outbox` directly before raising alarm — currently empty, so no evidence of an actual data-loss backlog from the bug, just the safety net not running as designed.
- Scoped the fix to all four scheduled functions (`keep-warm`, `canonical-outbox-scheduler`, plus `pre-visit-brief-scheduler` and `supervisor-digest-scheduler`, which share the identical outer-trigger/inner-fetch architecture even though they weren't caught live in the log window since they only run once daily) — added their `/.netlify/functions/*` paths to `PUBLIC_PATHS` in `lib/auth/mfa-routing.ts`.
- Separately found and fixed a smaller issue while re-reading `proxy.ts`: every authenticated request was making two sequential Supabase round-trips (the MFA-grace `profiles.mfa_grace_started_at` check and a separate `profiles.is_active` check) that could be one query. Combined them.
- This worktree's branch had drifted behind `main` (missed the jszip lazy-load + dashboard/maintenance `Promise.all` fixes from PR #573) — merged main in as part of the same PR rather than risk redoing or conflicting with already-shipped work.
- Two PRs, kept separate per Royce's instruction so each gets an independent CI run + deploy preview off `main` rather than stacking: eq-service PR [#574](https://github.com/eq-solutions/eq-service/pull/574) (main merge + duplicate-query fix) and PR [#575](https://github.com/eq-solutions/eq-service/pull/575) (scheduled-function redirect fix).
- Verified both safe to merge before merging: CI's two red checks on both (`npm audit` high-severity, `Integration tests`) confirmed pre-existing/unrelated — neither PR touches `package.json`/`package-lock.json`, and the integration-test failure is the already-documented `public.app_config` harness bug. `tsc + next build` — the real gate — passed clean on both. Tested the live deploy previews directly: `/api/health` 200, `/dashboard` still correctly 307s to signin (auth not weakened), and empirically proved the fix by hitting the same scheduled-function trigger URL on both previews — 307 on #574 (without fix), 200 on #575 (with fix).
- Merged both to `main` (squash, branches deleted). Confirmed production deploy (`6a5ee43b`, commit `4abed24`) is `ready`, no secrets leaked, and re-verified live in production: `/.netlify/functions/keep-warm` now returns 200 (was 307) on the real production URL.

## Decided
- Royce: reprioritized the fix order after a "steelman this" challenge — cold-start-specific fixes (keep-warm/matcher bug, bundle trimming) matter more for the "app takes a while to come up" complaint than the middleware round-trip or Suspense-streaming ideas, which help a different (already-improved) problem.
- Royce: merge main + the middleware duplicate-query fix together, but keep the scheduled-function redirect fix as a separate PR.
- Royce: explicit go-ahead to merge both after reviewing the safety verification.

## Deferred
- Suspense-streaming on dashboard/maintenance (so a page shell renders instantly while slow queries stream in) — real win for in-app browsing, but doesn't address cold start specifically, which is what this session was chasing. Not started.
- `outputFileTracingExcludes` audit of the deployed bundle (currently unset — likely some avoidable weight from test fixtures/typings/source maps) — genuine lever for cold-start duration, but payoff is unmeasured and a rushed version of this kind of change already went wrong once this week (the Sentry integration exclusion attempt), so it needs a measured pass, not a guess.
- `onboarding` is the one top-level route still missing a `loading.tsx` — low priority, one-time flow.

## Notes
- Netlify's serverless-function docs confirm there's no provisioned-concurrency / reserved-warm-instance knob for standard Functions — that avenue is fully closed, not just unconfigured.
- The bundle (~18.6MB across 112 routes) is largely genuine app code per earlier investigation in this thread — not much fat to trim there beyond the tracing-excludes idea above.
- No route or middleware currently opts into Next's Edge runtime anywhere in the app; would be a bigger, riskier lever (Node-API compatibility) than anything pursued this session.

---

# Session 2026-07-21 — closed the substrate's "is it learning" gap, then extended last session's pending.md dedup to SKS and OPS

## Built
- `failure_recurrence_signals()` in `refresh_digest.py`: retrofit all 6 `system/failures.md` ledger entries with a `signal` regex + `last_seen` date, then scan every session log dated after `last_seen` for the pattern. Closes the actual missing half of `guard-ratchet.yml` — that workflow has always proposed a rung promotion once `recurrences >= 2`, but nothing ever noticed *when* to bump the counter. Never writes to the ledger itself; a rung-4 hit surfaces in digest.md's Needs You as a possible guard bypass, anything lower lands in a new quiet "Possible recurring failures (unconfirmed)" section.
- Verified against real data before shipping, not just synthetically: caught a genuine unlogged recurrence of F1 (`raw.githubusercontent` serving stale content) in `sessions/2026-07-19.md` — `COWORK-PROMPT.md` had the same bug, fixed same session as PR #104, never counted as a recurrence. Also caught and fixed a false positive of its own (an over-broad regex alternative matching an unrelated "stale quote status" line) before it shipped.
- Royce confirmed the F1 recurrence — bumped `recurrences` 1→2, `last_seen` 2026-07-19. Re-ran the detector to confirm it correctly stops re-flagging that same incident.
- Extended last session's `eq/pending.md` done-item dedup to the other two tiers. SKS (`sks/pending.md`, 545→445 lines) turned out to be a genuinely different shape than EQ: `sks/changelog/labour.md` was missing real pre-2026-07-10 `sks-nsw-labour` app history (added the TAFE-prefill and pagination-sweep sessions), plus a second duplication layer existed against `eq/changelog/eq-shell.md`/`field.md`/`eq-cards.md` since several "SKS" sessions were actually eq-shell/eq-field/eq-cards work (NSW Comms, Plant & Equipment, the Photo ID credential requirement, the field_* views grant restore), and a large chunk of the file is pure business content (hiring, org charts, MS Planner) with no changelog home at all — left untouched. OPS (`ops/pending.md`, 444→230 lines) has no changelog directory; deduped its substrate/eq-context done-items against `sessions/*.md` instead (confirmed via spot-check the same duplication mechanism holds there too), leaving Tax & Entities and Parked-AHD content untouched.
- Ran two independent (non-nested) background agents for the SKS/OPS work rather than one nested fleet, avoiding the worktree-lifecycle bug found last session (a parent agent's own worktree gets cleaned up when its turn ends, orphaning any child still using that same path). Both worked cleanly; one detected the other's concurrent uncommitted changes in the shared checkout and correctly staged only its own files rather than colliding.
- Refreshed digest.md and confirmed Queue health reflects all three tiers: EQ 2871/392/381, SKS 445/67/63 (was 134 done), OPS 230/28/6 (was 46 done) — verified against the live generated file, not just the agents' own reports.

## Decided
- Royce: "Build the feedback loop" (recommended option) over rotating SKS/OPS immediately or fixing digest.md's own CI-status blind spots — chosen as the direct answer to "is the substrate learning from itself."
- Royce confirmed the F1 recurrence for the ledger.
- Royce: "apply the same dedup fix to sks and ops pending.md" — then, when presented with the real (more complex) shape of each, chose the thorough option for both ("all three, properly" for SKS's three-part problem; "check against sessions/*.md instead" for OPS).

## Deferred
- Still open from the earlier "what other levers" menu, not picked up this session: digest.md's own blind spots (CI status shows `? unknown` for 4 of 5 EQ repos; no `NETLIFY_TOKEN` set so deploy status is unavailable), the three pre-existing drift items from the original substrate audit (Task Brief/Asking-Questions rules triplicated across 3 CLAUDE.md files, stale model-names table, `suite-state.md`/`digest.md` CI-status disagreement), and finishing the `shell.md`/`service.md`/`cards.md` changelog consolidation properly (currently a banner, not a real merge).
- A handful of SKS-tier done items were left in `sks/pending.md` with no clean changelog home (3-way roster reconciliation, Equinix SY9 dupe check, the `schedule_entries` UNIQUE-constraint fix — all data-only investigations with no PR-shaped record) and one OPS item ("PAT rotation DONE 2026-06-15" — no matching session-log file exists for that exact date/topic). Left rather than guessed at; low-value to chase further.

## Notes
- **The real gap wasn't "the substrate doesn't remember" — it's "nothing notices when memory should update itself."** `system/failures.md`'s rung ladder and `guard-ratchet.yml`'s promotion-proposal logic already existed; the missing piece was purely the recurrence-detection step, and it was 100% manual before tonight.
- **The nested-agent worktree bug from last session didn't recur, because this session deliberately avoided the pattern that caused it** (launching two independent top-level agents instead of one agent that spawns children into a shared worktree). Worth keeping as the default shape for any future multi-agent substrate work.
- **SKS's substrate structure genuinely differs from EQ's** — its "done" content doesn't map 1:1 onto a single per-product changelog the way EQ's does, since SKS-tagged sessions frequently do the actual work in an EQ repo (eq-shell/eq-field/eq-cards). Any future SKS-tier substrate pass needs to check the EQ-tier changelogs too, not just `labour.md`.

---

# Session 2026-07-21 (5) — eq-service load-time follow-up: checked both remaining deferred ideas, no code shipped

## Built
- Nothing shipped this round — this was the two follow-up investigations deferred from the earlier same-day session, closed out with real answers rather than left open.
- **Bundle-trimming idea, checked and ruled out.** Pulled the actual list of every file the app's production build ships (989 files across all 112 pages) and sized it — 20.5MB total, matching the earlier estimate. Looked specifically for the kind of dead weight worth excluding (test files, unused reference docs, leftover data) — found almost none: a handful of small licence/readme text files, nothing else. The deployed package really is just the app itself. No change made; a guess-based trim already broke something once this week (the Sentry integration exclusion), so left alone rather than force a win that isn't there.
- **Page-shell/streaming idea, presented with the real tradeoff and declined.** Before touching either page, checked what streaming would actually buy: both dashboard and maintenance already fetch their data in one efficient batch (this session's earlier fix), and — this is the part that matters — the page *layout itself* depends on that data (a technician sees an entirely different screen to a manager on `/dashboard`). That means the shell can't render before the data arrives anyway, so the win is smaller than it looked from the outside, on the two busiest pages in the app, which can't be click-tested live as a signed-in user. Laid this out plainly; Royce said leave it deferred.

## Decided
- Royce: leave the page-shell/streaming idea deferred, having seen the real tradeoff (small win, real risk, can't be tested live).

## Deferred
- Demo account/data rebuild — unrelated to load time, carried over from an earlier session, still not urgent.
- Page-shell/streaming on dashboard + maintenance — deferred by explicit choice this time, not by default; see above for why the win is smaller than expected.

## Notes
- This is the honest-difficulty case worth remembering: two ideas that sounded promising in the abstract summary both turned out to have less value than assumed once actually measured/analysed against the real page structure and real deployed file list. Better to report that plainly than force either change through for its own sake.

---

# Session 2026-07-21 — eq-field's tenant-fallback telemetry (PR #509) was silently not reporting; found two bugs, fixed, deployed

## Built
- Royce flagged that PR #509's detect-only telemetry (merged 2026-07-20 as `30f44d4`) wasn't reliably delivering events. Confirmed via Sentry MCP: the eq-field project had zero events tagged `netlify_function` in 90 days, none of any kind in the last 7 (last event 2026-07-11), despite `SENTRY_DSN` being correctly set.
- Root cause #1: both new call sites (`canon-read.js:159`, `verify-pin.js:604`) invoked `captureServerError()` without `await`. The function awaits an HTTP POST to Sentry's ingest endpoint with a 2s timeout; un-awaited, the in-flight fetch is dropped when the Lambda freezes on return. Proved this empirically rather than by inspection: wrote a local test (`scratchpad/sentry-delivery-test.js`) pointing `SENTRY_DSN` at a stub HTTP listener — an un-awaited handler returned with 0 events delivered, the awaited version delivered before returning.
- Root cause #2, found independently while reading `_shared/sentry.js`: three pre-existing error-path call sites (`eq-agent.js:199`, `send-email.js:328`, `tenant-config.js:148`) passed `{ function: '...' }` / `{ fn: '...' }` as the opts object, but `_buildEvent` only ever reads `opts.tags` — those keys were silently discarded, so those sites never set a `netlify_function` tag at all regardless of call volume. Independently explains part of the 90-day silence.
- Fixed all five: added `await` to all, and corrected the three mistagged sites to `{ tags: { netlify_function: '<name>' } }`, matching the working pattern already at `verify-pin.js:633`.
- Hit a multi-minute tool-safety-classifier outage mid-edit (every Edit call failing "temporarily unavailable"); waited it out and re-verified every file's diff and `node --check` after resuming rather than trusting partial-session memory.
- Committed (`3da3b06`) and pushed to `claude/tenant-fallback-detect-telemetry`, then discovered PR #509 had already been merged — the branch's older commit (`dde6ee5`) was already-merged content wearing a different local SHA (the known squash-merge stranded-SHA trap). A straight merge into `main` would've hit real conflicts (both sides added the same telemetry line, one awaited, one not). Backed up the branch first (`backup-tenant-fallback-telemetry-3da3b06`, still on disk), then `git rebase --onto origin/main dde6ee5 <branch>` to drop the redundant commit and keep only the genuine fix — verified conflict-free, re-ran `node --check` on all 5 files, force-pushed.
- Opened a fresh PR (#509 was already closed) — [PR #515](https://github.com/eq-solutions/eq-field/pull/515). Waited for CI (tests+lint, header/redirect rules, deploy preview) to go green before merging, rather than merging on an `UNSTABLE` merge-state.
- Squash-merged to `main` as `cd39bc2`. Confirmed the Netlify production deploy (`6a5eec6e`, 15s build, secret-scan clean) republished all 5 touched functions with fresh build timestamps — not a stale cached bundle — and matches the merged commit.
- Re-queried Sentry post-deploy: still zero `netlify_function`/`mint-data-jwt` events, as expected — the fallback path only fires on a specific request shape (session missing `tenant_slug` + a client-supplied tenant hint), which merging code doesn't manufacture. The instrumentation is now provably capable of reporting; whether the fallback path is actually used in practice is still an open, unmeasured question.

---

# Session 2026-07-21 — eq-field highest-value-work pass: tests for the 3 biggest untested files, a wording correction, and a real duplicate-form bug found + fixed

## Built
- Asked "what's the highest-value work we could do now" — read the existing 2026-07-20 multi-lens review (`_reviews/multi-lens/2026-07-20.md`) rather than re-deriving priorities from scratch; it had already ranked "test the 3 biggest untested files" (`timesheets.js` 3,230 lines, `apprentices.js` 2,570, `roster.js` 2,329, zero direct test coverage) as the highest-leverage open item. Royce picked all three, in that order.
- Extracted the highest-risk pure logic out of each into small, dependency-injected, headless-testable `*-rules.js` modules (same pattern proven safe each time — thin wrapper call sites, zero behavior change): `timesheets.js` (hours/completion rules, fixed a real red/green display drift bug found along the way), `apprentices.js` (year-advancement eligibility + rating aggregation), `roster.js` (the approved-leave overlay — the same subsystem behind the 2026-07-11 "leave shows 0" production incident). Also reviewed `approve-leave.js`'s security surface, flagged unverified across 3 prior review cycles — no issue found. Merged: PRs #510, #511, #512.
- **Correction from Royce mid-session:** the apprentices extraction's code comments described year-advancement as a "compliance/licensing surface" and rating aggregation as "scoring" — wrong lens per the app's own human-recognition charter (`CLAUDE.md`: "not a compliance platform... make human contribution visible"). Year advancement is a trade-progression fact, not a performance judgment; ratings exist to make growth visible to the apprentice, not to score them for a file. Found and fixed 3 occurrences of the same wrong framing (2 in `scripts/apprentices-rules.js`/`apprentices.js` comments, 1 in the `index.html` changelog banner). Comment-only, no behavior change. Merged: PR #513.
- Royce flagged the session was drifting scope ("are we going down a rabbit hole?") — agreed, and used a visual (before/after wording cards + a scope timeline) to check in before continuing rather than self-directing further.
- Asked to continue to task 5 (splitting the remaining oversized files) with a "steelman first" — explained the extraction pattern in plain terms, then started on `safety.js` (2,286 lines, untouched). While surveying it for extractable logic, found a real bug: `safety.js` and `site-reports.js` **both declared 8 identically-named global functions for Prestart** (`loadPrestarts`, `renderPrestart`, `_prestartRow`, `openPrestartForm`, `renderPrestartForm`, `canSubmitPrestart`, `savePrestartDraft`, `submitPrestart`) — a v3.5.265 changelog entry confirms Prestart was meant to move fully into `site-reports.js`, but the old copy under the Safety tab was never removed. Whichever script loaded most recently silently won for both, including which submit-gate rule actually ran (`safety.js`'s never required a works-scope entry; `site-reports.js`'s does, "used in the audit trail").
- Reachability check before deciding how to fix it: for SKS, the Site Reports hub nav is hidden; for EQ, the Safety nav is hidden — normal tab-switching never loads both scripts in one session. But `nav-prestart`/`nav-toolbox`/`nav-diary` are deliberately kept `display:none` "for deep-links," which bypass nav-visibility gating — that's the real, narrower collision vector, not casual browsing between tabs.
- Royce chose "investigate first," then "retire the old copy." Removed `safety.js`'s own Prestart create/edit form, submit gate, and list view (~660 lines) via a carefully-scoped `sed` bulk-delete (boundaries verified line-by-line before cutting) plus targeted follow-up edits. Kept everything the Records/Site-Audits tabs and Toolbox's "import from prestart" still read: `_prestartCache` (loader renamed off the collision name to `_loadPrestartRecords`), `_psExportDocx` (Records tab's Word export), and two helpers that were misnamed with a `_ps` prefix despite being shared with Toolbox (renamed `_siteFieldFocus`/`_siteFieldBlur`).
- Mid-edit, hit the `eq-guard` brief-gate hook (brief not yet run this session) — ran `/brief eq-field` properly rather than setting the skip flag, which also caught a real mistake: the in-progress edits had been made directly on local `main` in the worktree instead of a feature branch. Moved to `claude/retire-safety-prestart-duplicate` before continuing (uncommitted changes preserved, nothing lost).
- Asked to "check truth / what's real" before continuing — re-grepped every deleted identifier for surviving references rather than trusting the earlier plan. Caught two real bugs the first pass missed: a dead `_sigSave()` auto-submit branch that referenced the just-deleted `_prestartDraft`/`canSubmitPrestart` **unconditionally** (would have thrown on every signature save, including Toolbox's, since the broken branch ran before the toolbox check) — the Edit that should have removed it had silently failed earlier against a transient tool-classifier block and was never retried; and Toolbox's own site-field input was silently calling the just-deleted `_psSiteFocus`/`_psSiteBlur` (genuinely shared helpers, misleadingly named with a prestart-only prefix).
- Opened the PR, hit a CI-only failure the local test run didn't catch: ESLint's `no-undef` flagged a `typeof`-guarded reference to the now-undeclared `_prestartDraft` inside `_psExportDocx` (safe at runtime, but ESLint doesn't special-case that pattern). Confirmed `_psExportDocx` has exactly one caller which always passes a real record, so removed the dead fallback entirely rather than working around the lint rule. All 11 existing test suites re-verified green (514 assertions) after every fix.
- v3.5.339, [PR #516](https://github.com/eq-solutions/eq-field/pull/516) — CI green, deploy preview ready. **NOT merged**, flagged for Royce's review since it touches a live safety-critical form.

## Decided
- Royce: "all 3 in that order" for the test-the-3-biggest-files / security-pass / split-the-oversized-files sequence.
- Royce: corrected the compliance-language framing — apprentice features are about growth, not policing. (Reinforces the existing `feedback_recognition_filter` memory.)
- Royce: "investigate first" then "retire the old copy" for the Prestart duplicate, rather than guessing which of the two implementations was authoritative.
- Royce: merge PR #513 (wording fix) same session; PR #516 (the Prestart retirement) explicitly held for review, not merged.

## Deferred
- **PR #516 not merged** — needs Royce's click-through on the deploy preview (Safety → Toolbox/Records, and the separate Prestart screen) before merging, since it touches a live safety-critical form.
- **Toolbox Talks may have the identical duplicate-copy problem Prestart had** — a second, separate "Toolbox" nav entry exists alongside the one inside Safety, same shape as the Prestart situation. Spotted in passing, not investigated. Worth the same check once #516 is confirmed working.
- **The oversized-file list from the 2026-07-20 review is still mostly open.** `apprentices.js`, `roster.js`, `timesheets.js` each had one slice extracted but remain well over the 1,500-line convention threshold; `sks-pipeline-resource.js` and `tender-pipeline.js` haven't been touched at all.
- Test coverage added this session only covers the extracted rule slices, not the bulk of the three largest files.

## Notes
- The `eq-guard` brief-gate hook firing mid-task (rather than at session start) is a real gap in the current gate design — it caught a genuine mistake (editing on local `main`) but only because the hook happened to fire at that moment, not because of any deliberate check. Worth considering whether the gate should fire earlier/more reliably for sessions that start with a broad, undirected ask ("what's the highest-value work") rather than a named repo target.
- The `_sigSave()` near-miss (an Edit call silently failing against a transient classifier block, then never being retried) is a concrete argument for exactly the kind of truth-check pass Royce asked for — a plan built from an earlier analysis without re-verifying against the current file state would have shipped a bug that broke Toolbox's own signature-saving, not just the thing being removed.

## Decided
- Royce: retry the edits after the classifier outage, and add the local delivery test to prove the fix rather than trust inspection alone.
- Royce: keep the three extra (pre-existing, out-of-brief) call-site fixes rather than drop them back out.
- Royce: commit, then push ("check we are doing this safely" — confirmed clean tree, correct non-`main` branch, fast-forward, no force needed before pushing).
- Royce: continue to merge ("just double check its safe") — surfaced that merge-to-`main` auto-deploys per eq-field's Netlify config before proceeding; when the straight merge turned out to have real conflicts, asked Royce to choose rebase-and-replay vs. manual-conflict-resolution vs. stop, rather than picking silently. Royce chose rebase.

## Deferred
- **Confirm the fallback path actually reports in production.** Needs either a normal working day's traffic or a deliberate test that hits the specific request shape (old-style session + client tenant hint) — re-run the Sentry query (`netlify_function:verify-pin action:mint-data-jwt`, eq-field project) after that. Once real events land, the `origin_mismatch` true/false ratio becomes readable for the first time and can inform whether to tighten the fallback.
- Backup branch `backup-tenant-fallback-telemetry-3da3b06` left on the eq-field remote — harmless, safe to delete once Royce confirms the rebase result is trusted, not deleted proactively.

## Notes
- **This is the second time the squash-merge "stranded SHA" pattern has bitten a session** (see `[[worktree-unpushed-commits-trap]]` memory) — worth treating "my branch shows as diverged from main" as a prompt to check for an already-merged PR with the same content before assuming a real conflict.
- **The tag-shape bug (`{ function }` / `{ fn }` silently dropped) was never part of Royce's original brief** — found by reading `_shared/sentry.js`'s `_buildEvent` before touching the three extra call sites, not assumed from the two-site brief. Confirmed via the local delivery test's own tag output before claiming it as a real bug.
- `node --check` was used as the syntax gate on this vanilla-JS repo (no `tsc`/build step) — sufficient here, but doesn't substitute for the CI `Tests + lint` job that PR #515 was actually held for before merging.

---

# Session 2026-07-21 (6) — Mobile My Schedule was silently dropping weekend shifts, on both SKS and EQ Field

## Built
- Royce shared a screenshot of the mobile "My Schedule" view showing only Monday–Friday day cards, flagging that weekends should show too.
- Root cause: `renderSchedule()` in `roster.js` (both sks-nsw-labour and eq-field) hardcoded its day list to `['mon','tue','wed','thu','fri']`, even though `schedule.sat`/`schedule.sun` and the desktop roster grid already carry weekend data via the existing `getVisibleRosterDays()` helper (shows Sat/Sun only for weeks that actually have weekend work).
- Fixed both apps' `renderSchedule()` to build its day list from `getVisibleRosterDays()` instead, with a day-offset map (`{mon:0,...,sun:6}`) so a week with Sunday-only work (no Saturday) still lines up with the correct calendar date rather than assuming array position = day offset.
- Extended the fix to each app's home-tile helpers (`home.js`): shift count, "Next shift" pill, and the supervisor "Schedule" tile subtitle all previously iterated a hardcoded Mon-Fri list too — same root cause, same fix. In eq-field, `roster.js` is lazy-loaded, so `home.js`'s usage is `typeof`-guarded with a Mon-Fri fallback (matches the existing `isLeave`/`isEducation` guard pattern already in that file).
- sks-nsw-labour: v3.10.99, all 4 version files bumped (`app-state.js`, `sw.js`, `index.html`, `CHANGELOG.md`), PR [#68](https://github.com/eq-solutions/sks-nsw-labour/pull/68), squash-merged to `main`.
- eq-field: v3.5.338, ported the identical fix into a fresh worktree (`eq-field-mobile-weekend-wt`, root checkout was mid-unrelated-work on a telemetry branch) since the pre-existing `bold-volhard-645dd7` worktree was flagged "never touch" (unpushed, no PR) in the worktree registry. 3 version files bumped (no CHANGELOG.md in this repo — banner lives in `index.html`). PR [#514](https://github.com/eq-solutions/eq-field/pull/514), squash-merged to `main`.
- Both merges hit the auto-mode classifier's merge-action block on the first `gh pr merge` attempt; explained the block to Royce rather than routing around it via a different tool, and proceeded once Royce explicitly confirmed — both merges succeeded on retry.
- Registered + closed out the `eq-field-mobile-weekend-wt` worktree row in `system/worktree-registry.md`.

## Decided
- Royce: commit + push both, open a PR for eq-field (chosen over "SKS only" or "just show diffs" when asked).
- Royce: "merge and deploy both" — explicit instruction, satisfies both repos' Netlify auto-deploy-on-push-to-main gate.
- Royce: "can you do it please?" — explicit go-ahead to retry the merge after the classifier block, rather than working around it silently.

## Deferred
- Royce to click-through confirm a real weekend-rostered person's mobile schedule + home tile on both apps once deployed — smoke-tested via `node --check` only, not browser-verified against live weekend roster data. _(added 2026-07-21)_

## Notes
- Display-logic only on both apps — no schema or data changes; reads fields (`sat`, `sun`) that were already being written.
- The GitHub MCP's `create_pull_request`/`list_pull_requests` 404'd against both `eq-solutions/sks-nsw-labour` and `eq-solutions/eq-field` (likely lacks access to these repos) — went straight to `gh` CLI for both PR creation and merge, consistent with the same finding logged in an earlier session today against `eq-shell`.

---

# Session 2026-07-21 — eq-field: cleaned up the safety-net branch from the tenant-fallback telemetry fix

## Built
- Deleted `backup-tenant-fallback-telemetry-3da3b06` in eq-field — the throwaway safety branch created before the earlier rebase in this same telemetry-fix session. **Correction to that earlier session log entry:** it said this branch was "left on the eq-field remote" — it was actually local-only, never pushed; confirmed via `git ls-remote --heads origin` before deleting.
- `git branch -d` refused ("not fully merged") because the rebase gave the fix a new commit SHA (`c5a7bb1`→`cd39bc2`) — git's ancestry check doesn't know the *content* is identical, only that the exact SHA isn't in `main`'s history. Verified content equivalence directly (`git diff origin/main <branch> -- <the 5 files>` → empty) before force-deleting with `-D`, rather than trusting the ancestry check's default judgment.
- Found local `main` itself was 2 commits stale (never fetched since the PR #515 merge) while diagnosing the above — fetched and fast-forwarded it to match `origin/main`.

## Decided
- Royce: delete the backup branch.

## Deferred
- Same as the earlier entry today: confirm the tenant-fallback path actually reports in Sentry once real traffic hits it (`netlify_function:verify-pin action:mint-data-jwt`, eq-field project). Not touched this session.

## Notes
- Housekeeping-only — no code or product behaviour changed this round.
