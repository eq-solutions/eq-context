# Session 2026-08-18 — Fixed a real bug behind a suspected-hardcoded-path guard.js gate report

## Built
- Root-caused and fixed a `stale-main-gate` false-positive bug in `~/.claude/hooks/guard.js`'s shared `resolveEffCwd()` helper: `cmd.match()` without `/g` took the first `cd` in a multi-`cd` chain instead of the last, misattributing commits made in isolated scratch clones to an earlier, unrelated (and genuinely-behind) directory in the same command chain. Fixed to scan and take the last match, mirroring `pre_tool_use.py`'s existing `_CD_CHAIN_RE` precedent. (`~/.claude` commit `315fbc0`)
- Found and fixed a second, independent copy of the same bug class in `guard.js` rule 1c (`detect-fake-worktree`, shell case) — it had its own unfixed inline `cd`/`-C` parser, worse than the original (blind to any chained `cd` at all, not just first-vs-last). Fixed by routing it through the shared `resolveEffCwd()` helper instead. (`~/.claude` commit `e272da0`)
- Verified both fixes by replaying the exact failing command shapes from `guard.log` through old vs. fixed `guard.js`; confirmed old code misbehaves and fixed code doesn't. All 14 existing self-tests (`hooks/selftest.js`) still pass.
- Swept the whole `C:\Projects` workspace (all wired hooks, every repo's `.claude/settings.json`) for the same bug shape via an Explore agent — confirmed no other live instance exists; one stale, unwired dead-code copy of an old `pre_tool_use.py` found in a leftover eq-context worktree, posing no risk.
- Confirmed no other repo (`eq-context`, `eq-shell`, `eq-field`, `eq-cards`, `eq-solves-service`, `eq-intake`, `eq-roles`, `eq-ui`, `eq-design-tokens`, `eq-solves-assets`, `sks-nsw-labour`) has any local/uncommitted changes to hook or guard files.

## Decided
- Commit both fixes to `~/.claude`, scoped to `hooks/guard.js` only (Royce's explicit instruction, both times).
- Reconfirmed (not re-decided — matches the standing 2026-07-30 / 2026-08-14 call already on record in `ops/pending-archive.md`): `~/.claude` stays a local-only git repo, no remote — so "push" for these commits is a no-op by design, not an oversight.

## Deferred
- None new this session. The one open item this touched in passing — the pre-existing "shared eq-context folder needs someone to reconcile it" note in `ops/pending.md` (2026-08-15) — was read but not actioned; still not this session's call to force, per its own note.

## Notes
- The user's original bug report suspected a hardcoded path in `eq-context/hooks/pre_tool_use.py`. That hypothesis was wrong but reasonable — `pre_tool_use.py` does have a genuinely hardcoded-by-design check (`is_shared_eq_context()`, for rule F9), just for a *different* rule. The actual `stale-main-gate` rule lives entirely in `~/.claude/hooks/guard.js`, a separate file the user hadn't been pointed at. Worth remembering: a report that "smells like" a known pattern (hardcoded path) can still have a different root cause in a different file.
- `guard.log` (`~/.claude/hooks/guard.log`) was the decisive piece of evidence both times — it records the exact resolved cwd and reason string for every fired rule, which is what made both root causes provable by direct replay rather than left as speculation.
- Possible follow-up (not done this session): this is the third distinct time this same "which cd wins in a chain" failure class has hit `stale-main-gate`'s cwd resolution (2026-08-05 worktree-naming gap, 2026-08-14 string-start anchor, now first-vs-last). `eq-context/system/failures.md` tracks every other guard bug in this lineage (F2/F6/F7/F9/F10/F13) with its own ledger entry — this one arguably deserves the same treatment, but wasn't added since it needed checking that file's own format/governance first and that wasn't done this session.

---

# Session 2026-08-18 (cont.) — Contacts screen skipped the rehire-rating prompt when archiving Labour Hire

## Built
- Royce reported archiving Timothy Chapman (Labour Hire) from Contacts showed no rating prompt. Traced live: `scripts/people-labour-hire.js`'s `openLHArchiveModal()` (the shared "would you rehire them?" modal) was only ever wired to the roster grid's archive icon (`roster.js`); Contacts' own 📦 button (`people.js` `_personActions()`) called `archivePerson()` directly for every group, bypassing the modal entirely.
- Fix: Contacts now routes Labour Hire rows through `openLHArchiveModal()`, matching the roster grid; other groups (Direct, Apprentice) unchanged, since rating only applies to Labour Hire.
- Confirmed `people-labour-hire.js` was already loaded on the Contacts tab bundle (`lazy-loader.js`) — the ★ re-rate button on archived rows already depended on it — so no lazy-load wiring gap.
- Shipped as v3.5.509, [eq-field PR #712](https://github.com/eq-solutions/eq-field/pull/712), squash-merged, confirmed live via `field.eq.solutions/sw.js`.

## Decided
- Merge and ship — Royce's explicit go after reviewing the PR.

## Deferred
- Timothy Chapman's own rating was never captured (he was already archived before the fix landed) — addable via the ★ button on his archived Contacts row, Royce's own action. _(added 2026-08-18)_
- Not click-tested live by a human — verified structurally on the live deploy-preview build (called the real render function with mock records, confirmed routing) since this sandbox has no real Core-authenticated session to click through with. _(added 2026-08-18)_

## Notes
- This repo's brief-gate and reflection-gate both fired as designed this session (no repo argument on first `/brief` invocation resolved correctly once given `eq-field`; the reflection-gate blocked the first commit attempt on `people.js` until `docs/reflection-log.md` was staged in the same commit) — no bypass used.
- CI's `check-cache-busters.mjs` drift check caught a real miss: bumping `APP_VERSION` in `scripts/app-state.js` without also bumping its `?v=` tag in `index.html` would have shipped a stale cached copy to every browser that already loaded the app. Fixed in a follow-up commit on the same PR before merge, not after.

---

# Session 2026-08-18 (cont.) — Cleared the 5-file index_drift.py --strict failure

## Built
- Fixed the exact 5 orphans `INDEX_DRIFT_STRICT=1 python3 scripts/index_drift.py` was flagging, none related to the prior `field.md`/`eq-service.md` changelog-merge commit (`0fbee16`): named `eq/pending/{cross-repo,eq-design-tokens,eq-solves-intake,sks}.md` in `eq/README.md`'s pending-index bullet, and folded `.github/scripts/test_pending_dupes.py` into `system/machinery.md`'s ci-scripts test row.
- Verified clean with a strict re-run before committing. eq-context [158b781](https://github.com/eq-solutions/eq-context/commit/158b781), pushed direct to `main`.

## Decided
- Committed and pushed directly rather than via PR — docs-only substrate fix, no build/deploy surface, Royce confirmed via AskUserQuestion.

## Deferred
- None. Task was fully scoped by the user and closed out in one pass.

## Notes
- Landed on `main` concurrently with two other sessions' work (the `~/.claude/hooks/guard.js` fix above, and the eq-field Contacts archive-rating fix above) — reconciled via an isolated `git worktree` off `origin/main` rather than `pull`/`merge` in the shared checkout (both blocked by the F9 guard), per the same pattern already on record in `eq/changelog/eq-context.md`'s 2026-08-14 entry.

---

# Session 2026-08-18 (cont.) — brief-gate exemption missed `eq/pending/<repo>.md` after the split

## Built
- Found + fixed a different bug in `~/.claude/hooks/guard.js` than the `stale-main-gate` one logged above: rule 8 (`brief-gate`)'s exemption only matched the flat `pending.md`/`pending-archive.md` filenames by exact suffix, so every file under the new `eq/pending/<repo>.md` subdirectory (split 2026-08-17, see `eq/pending.md`'s own scope note) silently started failing the gate — confirmed live: an edit to `eq/pending/eq-solves-intake.md` blocked with "brief not run this session" while the same-session `eq/changelog/eq-intake.md` edit sailed through ungated. Added `fp.includes('/pending/')`, matched the same substring way `/sessions/` and `/changelog/` already are. Verified with three simulated PreToolUse payloads (target file now allows; changelog control unaffected; a real product file still correctly blocks). (`~/.claude` commit `f5a58d7`)
- Also committed 6 other pre-existing, unrelated uncommitted changes sitting in `~/.claude` at Royce's request, since they'd accumulated: `CLAUDE.md` (eq-shell deploy-note correction + Opus 4.8→5 bump), `hooks/README.md` (selftest brief-gate isolation doc), `settings.json` (substrate-sync hook swapped for a call to eq-context's own `hooks/substrate_sync.py` + a new `autoMode.environment` context block), `plugins/known_marketplaces.json` (marketplace timestamp), `agents/` (new `bug-fixer.md` + `merge-readiness.md` subagent definitions), `plans/` (routine plan-mode session-artifact churn from other concurrent sessions on this machine).
- Caught and corrected my own mistake in one of those 6 commit messages (it mis-attributed where `substrate_sync.py` lives and which commit added it) via a non-interactive `git filter-branch --msg-filter` reword — deliberately not `git rebase -i`, which this environment's own tooling rules block outright. Verified the tree was byte-identical before/after the reword. Cleaned up the resulting `refs/original/refs/heads/master` backup ref afterward, at Royce's request.
- Confirmed `~/.claude` has no `origin` remote configured — same finding already on record in the session above; nothing to push there for any of this.

## Decided
- Commit the 6 unrelated `~/.claude` changes as separate logical commits rather than one grab-bag commit, since they were genuinely unrelated to each other and to the `pending/` fix.
- Amend the inaccurate commit message via `filter-branch` rather than leave it wrong, since Royce explicitly asked and nothing in that repo had been pushed anywhere yet.

## Deferred
- None new. (Not duplicating: the `system/failures.md` F-numbering gap for the guard.js "which cd wins" bug class was already flagged in the session above.)

## Notes
- This session's actual work (a global `~/.claude` hook fix) doesn't map to any EQ/SKS/OPS product backlog — skipped `eq/pending.md` / `sks/pending.md` / `ops/pending.md` updates for that reason rather than force-fitting an entry.
- Reconciled *this* repo's own local clone during `/close` (1 ahead / 1 behind `origin/main`): the "ahead" commit (`fe01494`, a stale `sks/pending.md` note correction) turned out to already be independently reconciled and pushed by a concurrent session as an identical rebase (`604ce28` — confirmed via direct patch diff: same author, same date, byte-identical content, before touching anything). Synced the shared checkout to `origin/main` via `git reset --hard` (safe — content was already fully preserved upstream and the working tree was clean) rather than pushing a duplicate.
- One git commit attempt this session was blocked by the Claude Code auto-mode permission classifier itself, not by Royce; by the time it was retried, a concurrent session had already landed the identical change independently — confirmed no duplicate was committed.

---

# Session 2026-08-18 (cont.) — eq-field/eq-service changelog dead-twins merged, no content lost

## Built
- Merged `eq/changelog/field.md` into `eq-field.md` (repo-slug name, matching the dominant `eq-shell.md`/`eq-cards.md` convention) — field.md was actually the larger/older file (1984 of the final 1989 lines), but the naming direction was explicit per the task. `eq-field.md`'s 5 unique entries (PR #711/#710/#709/#703/#705) prepended onto field.md's full body verbatim.
- Merged `eq/changelog/eq-solves-service.md` into `eq-service.md` — both files' internal ordering was already imperfectly chronological, so all 70 `##`-dated blocks (47 + 23) were split out and stable-sorted by date descending rather than naively concatenated.
- Verified zero content loss both times by PR-reference-token set-diffing: all 355 tokens (350 original + 5 new) present for field, all 244 for service — not just a line-count sanity check.
- Rewrote both source files (`field.md`, `eq-solves-service.md`) as `superseded_by` stubs, then `git mv`'d them into `archive/` as dead-twins — the repo's own established pattern (4 prior precedents), not a new one invented this session.
- Updated 6 forward-pointing docs to the new filename (`archive/README.md`, `eq/README.md`, root `README.md`, `eq/pending/eq-field.md`, `eq/products.md`, `eq/field/multi-tenancy/plan.md`, `sks/pending.md`) — left every `sessions/*.md` entry and other products' own historical changelog entries untouched, since those are frozen point-in-time records, not live pointers.
- Ran `changelog_duplicates.py` and `index_drift.py --strict` locally before pushing; both clean, zero regressions introduced.
- Found 5 pre-existing, unrelated `eq/README.md` index-drift orphans while verifying — out of scope for this task, flagged via a `spawn_task` chip instead of bundled in. That chip was picked up and closed out by its own session this same day (see "Cleared the 5-file index_drift.py --strict failure," commit `158b781`, above) — confirmed the fix is live on `main`.
- Ticked off the matching deferred item in `eq/pending/eq-context.md` (logged 2026-08-17, the night this duplication was first found) — left in place rather than manually archived, per the established precedent already on record in that same file for exactly this situation (avoid conflicting with `rotate_pending.py`'s nightly automated rotation).
- Pushed direct to `eq-context` main: commit `0fbee16`, merge `e9e3b2e` (synced through one non-fast-forward collision with 5 concurrent commits mid-session — resolved by verifying byte-identical content on the 3 contended working-tree files before discarding the redundant copies, not by force).

## Decided
- Commit and push directly to `main` rather than open a PR — doc-only substrate change, no build/deploy surface, sized well within the repo's own norm for this kind of work (assistant's own call, per the task's explicit instruction to size it either way based on diff size).
- Repo-slug filename wins even though `field.md` held the larger/older history — matches the task's explicit instruction and the dominant naming convention already used by every other product changelog in this tier.

## Deferred
- None new. The one open thread (5 unrelated index-drift orphans) was chip-flagged, picked up, and already closed by a separate session today — nothing carried forward.

## Notes
- This session is a direct continuation of the task logged as deferred in `eq/pending/eq-context.md` on 2026-08-17 ("found while deciding which file to append this session's own eq-field entry to... flagged as a background task rather than fixed inline tonight") — that's now closed.
- Chose Python-scripted merging (verified via automated PR-token set-diffing) over manual re-typing for both files, given sizes of ~2000 and ~450 lines — a deliberate technique choice to make "no content loss" provable rather than just asserted.

---

# Session 2026-08-18 (cont.) — eq-shell repo-wide CI block on 2 undocumented control-plane functions, fixed

## Built
- Root-caused the required `Schema drift + anon-grant + policy-lint` check failing on every open eq-shell PR (confirmed on #1428): `public.eq_audit_licence_sync_dispatch` and `public.eq_reconcile_licence_sync` were live on the jvkn control-plane database with zero source anywhere in the suite. Verified live via `pg_get_functiondef`/`information_schema`/`pg_cron.job` (not assumed) — both real, in daily use via two ~3am cron jobs, not stubs. Repo-scoped grep across eq-shell/eq-cards/eq-field/eq-solves-service found no source for either.
- Checked for concurrent/duplicate work first: found a worktree (`eq-shell-jvkn-drift-backfill`) and several stale local branches that looked related, but all were already-merged historical work on a *different* 6 functions (now the repo's existing `KNOWN_UNSOURCED` entries) — none touched the licence-sync pair.
- Ran `/decide` on the two fix options the drift script itself offers (write a real migration vs. add to `KNOWN_UNSOURCED`): concluded a real backfill migration was correct, matching this repo's own established precedent for the identical shape (`2026_07_24_reconcile_worker_sync_codify.sql` — a dispatch/reconcile function pair with two support tables and two cron jobs), since whitelisting would misuse a list whose every existing entry is verified sourced elsewhere, not undocumented debt.
- Built the backfill (`supabase/migrations/2026_08_18_reconcile_licence_sync_codify.sql`) in a fresh worktree off `origin/main` (the shared eq-shell checkout had unrelated uncommitted WIP on another branch), byte-for-byte from live. Committed, pushed, opened [eq-shell PR #1431](https://github.com/eq-solutions/eq-shell/pull/1431), verified every check green including a live re-run of the drift check itself, then squash-merged (`81bf38f`) on explicit go — live on core.eq.solutions within seconds per this repo's deploy model.
- Verified downstream: re-checked #1428 (the original failing example) — its branch was behind `main` and didn't include the fix; Royce had already merged `main` into it independently (via GitHub's "Update branch") while this was in progress, so a redundant local merge was discarded rather than pushed over it. Confirmed the real merge commit passes the drift check.
- Checked all other open eq-shell PRs (#1435, #1434, #1429, #1416) for the same drift-check status, cross-checked against actual commit ancestry rather than trusting cached CI results. #1435 turned out to have already been merged by someone else mid-session — a prepared local merge was discarded as moot. #1434 and #1429 were found to be checked out as active worktrees with commits only 15–35 minutes old — read as live concurrent sessions per this repo's own guard precedent (F12: blind-overwrite of concurrent work), so deliberately left untouched rather than merged into, even after an earlier general go-ahead — flagged the new evidence and re-confirmed before deciding not to proceed.

## Decided
- Write a real migration rather than whitelist (via `/decide`, Royce: "then go").
- Push the fix and open the PR (explicit).
- Merge #1431 once its Netlify preview check finished (explicit, given ahead of time).
- Merge `main` into #1428's branch to verify end-to-end (explicit).
- Merge `main` into #1435/#1434/#1429 too ("go ahead on all three") — but re-paused specifically on #1434/#1429 after finding they're live worktrees, since that's materially stronger evidence of concurrent work than what the original question conveyed; awaiting Royce's call on those two specifically.

## Deferred
- **#1434 and #1429 still don't have the fix** — left alone pending Royce's explicit call given the concurrent-session risk. _(added 2026-08-18)_
- **#1416 shows a cached "pass" that predates the fix** — its branch also lacks the backfill file, so that result is likely stale; not re-verified this session. _(added 2026-08-18)_
- **Formal hand-apply of the migration to live jvkn via Supabase MCP + a `CONTROL-PLANE-LEDGER.md` entry** — optional, matches the `2026_07_24` precedent, but not required to clear the CI block so left for later. _(added 2026-08-18)_

## Notes
- `gh pr merge --delete-branch` failed locally with a git-worktree conflict on `main` (another unrelated worktree had it checked out) — harmless, the remote merge had already succeeded; branch/worktree cleanup done manually.
- First background CI-watch attempt was silently broken — its polling loop depended on `jq`, which isn't installed in this shell environment. Caught before it could produce a false "still waiting" impression; rewritten using plain `grep`/`sort` instead.
- This session is a good example of the "confirm target before any write" + "investigate before overwriting" rules earning their keep: two separate near-misses this session (the #1428 concurrent main-merge, and the #1434/#1429 active-worktree discovery) would have produced confusing or disruptive git history if acted on without the extra verification step.

---

# Session 2026-08-18 (cont.) — EQ Cards licence PDF/xlsx export shipped + two real bugs found; eq-shell blank-screen export bug fixed

## Built
- **eq-cards**: New "Export PDF" action on the licence detail screen — one-page branded PDF per licence (holder, fields, status, QR verify link; private licences export too but skip the QR since there's no public share link). eq-cards [PR #265](https://github.com/eq-solutions/eq-cards/pull/265), merged (`4597bfd`), deployed live. Required bumping CI's Flutter pin 3.41.9 → 3.44.8 (the only `pdf` package version compatible with `excel_plus`'s xml dependency needs Dart ≥3.12).
- **eq-cards**: Royce's live device test surfaced two genuine bugs unit tests/CI couldn't catch (both web-only browser behaviours):
  - `excel_plus`'s `Excel.save()` silently triggers its own second browser download on web, using a hard-coded default filename (`FlutterExcel.xlsx`) — confirmed by md5-matching Royce's downloaded file against the real `licences.xlsx` inside his ZIP, then reading the package source (`save_web.dart`). Fixed by switching to `Excel.encode()` (same bytes, no side effect).
  - Design fixes from the same round: logo 20x20px → 60x60px (xlsx) / 34x34px (PDF); xlsx "Licence Type" column now shows the human label instead of the raw code. eq-cards [PR #266](https://github.com/eq-solutions/eq-cards/pull/266), merged (`5a3ce1d`), deployed live.
- **eq-cards**: Same-day follow-up — the #266 banner fill (deep blue) made the sky-blue logo nearly invisible (no white logo asset exists). Fixed by recolouring the mark's opaque pixels to white at export time, alpha untouched (`whitenLogoPng`, using the `image` package already in the app for photo cropping), verified visually against a real composited sample. eq-cards [PR #267](https://github.com/eq-solutions/eq-cards/pull/267), merged (`821d9f1`), deployed live.
- **eq-shell**: Fixed the "export defaults to a white screen on desktop" report on the Staff page's compliance-pack export. Root cause: the poll that detects job completion auto-clicked the download link from inside a `setInterval` callback — no user gesture behind it. Clicking a cross-origin signed-URL anchor with no active gesture is the exact case browsers are strictest about; some navigate the tab itself to the URL instead of downloading, unmounting the whole SPA. Fix: removed the auto-click (the button next to it already flips to "Download ready ↓" on completion — that click has a real gesture, so it's the only place this should fire from); added a toast so completion is still visible. Also added small captions under the Compliance pack and Add licence buttons. eq-shell [PR #1434](https://github.com/eq-solutions/eq-shell/pull/1434), merged (`5eca6417`) — live on core.eq.solutions (merging this repo is the deploy).

## Decided
- Push and deploy eq-cards PDF export (explicit).
- Bump CI's Flutter pin to unblock the `pdf` package (explicit, via AskUserQuestion — chosen over dropping the xlsx logo feature).
- Merge and deploy each of #266 and #267 (explicit, separately, after CI green each time).
- Push and open the eq-shell PR, then merge it (explicit, separately — flagged ahead of time that merging this repo deploys immediately).

## Deferred
- **Royce still hasn't confirmed finding "Export PDF" on his own phone** — verified the feature is genuinely in the live deployed bundle (grepped `cards.eq.solutions/main.dart.js` for the tooltip string), so this isn't a failed-deploy question. Gave him the exact tap path (open one licence → share-style icon in that screen's app bar, separate from "Export my data") and suggested a hard refresh in case of PWA cache staleness. _(added 2026-08-18)_

## Notes
- Two real, non-obvious bugs this session came from Royce's own live testing, not from code review or CI — both were browser-behaviour edge cases (a library's undocumented side effect; a no-user-gesture cross-origin navigation) that a clean local `flutter test`/`npx tsc` pass would never surface. Worth remembering: shipping something that passes every automated check is not the same as it working for a real person on a real device.
- Mid-session slip caught and corrected: a round of Edit/Write calls landed in the plain `C:\Projects\eq-cards` checkout (branch `main`) instead of this session's assigned worktree — restored `main` to clean and re-applied the same edits in the correct worktree before committing. Matches the standing feedback note on this exact failure mode; the note alone didn't prevent it, `git status` before commit did.

---

# Session 2026-08-18 (cont.) — Compliance-pack caption reverted, licence "Replace card" always visible

## Built
- **eq-shell**: Royce sent a screenshot reporting "a PDF of everyone's licenses pop up in Shell which is incorrect... I didn't want to touch the core/tenant compliance pack." Pulled the actual screenshot via Drive rather than guessing — confirmed no PDF was generated; it was the static caption added in #1434 ("PDF of everyone's current licences"), misread as the app announcing unwanted behaviour. Compliance-pack generation logic itself confirmed untouched by either PR. Reverted the caption. eq-shell [PR #1438](https://github.com/eq-solutions/eq-shell/pull/1438), merged (`8333df02`), live on core.eq.solutions.
- **eq-cards**: Royce asked how someone replaces an existing licence from Cards — "it isn't obvious to me." Investigated the full flow before building anything: the rescan+OCR mechanism already worked correctly (updates the existing record in place, not a duplicate) but was gated to expired/expiring-within-30-days only, identically in 3 places across the detail screen's layouts. Relabelled "Renew licence" → "Replace card" and removed the gate on all three layouts (left the wallet list tile's own compact per-row chip untouched — a separate, bigger call). eq-cards [PR #268](https://github.com/eq-solutions/eq-cards/pull/268), merged (`7a3377c`), deployed live — verified against a fresh, cache-busted fetch of the live bundle (`main.dart.js`): "Replace card" appears 3x, "Renew licence" 0x.

## Decided
- Push and merge the eq-shell caption revert (explicit, "push and merge it").
- Ship "Replace card" as always-visible rather than surfacing the existing hidden duplicate-detection prompt instead (via AskUserQuestion, recommended option).
- Merge and deploy #268 (explicit, separately, after CI green).

## Deferred
- Nothing new this round — the "Royce hasn't confirmed Export PDF discoverability" item from the earlier session entry is still open/unconfirmed.

## Notes
- The eq-shell "PDF of everyone's licenses" report looked at first read like a real data-exposure concern (an org-wide PDF firing unprompted). Verifying against the actual screenshot before touching any code — rather than assuming the report was literally true and reverting/investigating the wrong thing — was the right call: it was UI copy, not behaviour, and the fix was a one-line revert instead of a scope-of-export bug hunt.
- Root-cause-first paid off again on the licence-replace ask: the underlying rescan+OCR mechanism didn't need building, only unhiding. Building a new flow from scratch would have duplicated something that already worked.
- Substrate correction: the prior /close in this session (see commit `09c63fa`) only actually committed 2 of the 5 intended files (`eq/changelog/eq-cards.md`, `eq/changelog/eq-shell.md`) — `eq/pending/eq-cards.md`, `eq/pending/eq-shell.md`, and this session log's earlier entries were staged and reported as committed but never landed, discovered only when this close ran `git status` and found them still sitting as uncommitted working-tree changes. No content was lost (nothing overwrote them), but the earlier "push succeeded" report was wrong for 3 of 5 files. Root cause not fully diagnosed — worth watching for on future closes: verify `git show --stat <sha>` actually lists every file you intended to commit, not just that the commit command exited 0.

---

# Session 2026-08-18 (cont.) — F12 guard promoted to rung 4, auth-stall watchdog fixed, Cards-linked worker DOB guard bug found and fixed

## Built
- Ran `/decide` on next-sprint priorities; found a concurrent/earlier session had already produced `eq/sprints/2026-08-18-needs-you-triage.md` covering most of the same ground (SEC-1 reconfirmed standing, an F14 candidate checked live and ruled out, the "Degraded UI Performance" Sentry issue confirmed single-data-point/watch-only) — disclosed rather than duplicated.
- F12 (raw copy-back into the shared eq-context checkout) promoted rung 2 → 4 on Royce's go via `/decide`: built a general `pre_tool_use.py` guard blocking any cp/mv/Copy-Item/Move-Item/robocopy whose destination resolves inside the shared checkout while its source doesn't. Self-caught and fixed a real bug in the guard's own flag-detection while building the adversarial suite (a `/`-prefixed Git-Bash path was being misread as a Windows-style flag, defeating the guard's own msys-path handling). 124/124 adversarial tests passing. eq-context [PR #167](https://github.com/eq-solutions/eq-context/pull/167), merged.
- Built the auth-stall fix (Sentry "auth-stall: session-spinner-timeout", root-caused in an earlier pass and held for explicit go): raised `App.tsx`'s `BlockingSpinner` watchdog 20s → 35s to clear #1269's ~30s worst-case session-verify retry window. eq-shell [PR #1433](https://github.com/eq-solutions/eq-shell/pull/1433), merged, confirmed live on core.eq.solutions via commit-ancestry check against the actual production deploy.
- Royce reported an error updating Mohammed Hussain's start date ("This worker has a Cards account — set their real date of birth instead"). Root-caused via an Explore agent: yesterday's `dob_locked_to_cards` guard (`entity-patch.ts`, #1426) checked key presence of `dob_day`/`dob_month`, not whether the value changed — the Staff-page full edit form always resends both fields on every save, so any edit to a Cards-linked worker was being blocked, not just DOB edits. Fixed to compare against the currently-stored value; verified tsc/eslint clean, diff scoped to one guard block. eq-shell [PR #1439](https://github.com/eq-solutions/eq-shell/pull/1439), merged, confirmed live on core.eq.solutions.

## Decided
- Promote F12 to rung 4 and build it this sprint (Royce, via `/decide`).
- Merge eq-context PR #167 (Royce, "Merge now").
- Build the auth-stall fix (Royce, "build the auth-stall fix too").
- Merge eq-shell PR #1433 (Royce, "Merge now" — explicit, given this repo deploys on merge).
- Merge eq-shell PR #1439 (Royce, "merge it").
- Leave the shared eq-context checkout's nightly-bot sync drift (2-3 commits behind origin/main, digest.md/suite-state.md only) unreconciled — normal `pull`/`merge` blocked by the F9 guard in the shared checkout, and a `git reset --hard` fallback was itself blocked by the auto-mode classifier. Royce: "leave it, it'll sync next session."

## Deferred
- **eq-shell PR #1439's manual click-test** — edit a Cards-linked worker's start date (should now save), then try typing a different day/month for a Cards-linked worker (should still correctly block). _(added 2026-08-18)_
- Already tracked in `eq/pending/eq-shell.md`: #1434/#1429 still don't have the control-plane-drift backfill; #1416's green check predates the fix.

## Notes
- The shared eq-context checkout had uncommitted, unrelated additions already sitting in it (pure insertions across `pending/`, `changelog/`, `sessions/2026-08-18.md`) from a concurrent session at close time — this session's own close writes were made in an isolated worktree off `origin/main` instead of the shared checkout, per the established F9 pattern, to avoid touching that in-progress work.
- Confirmed both eq-shell deploys the careful way (commit ancestry against the actual `state: "ready"`/`context: "production"` deploy record, not just a green merge) — one deploy queue observed backed up 3-deep from concurrent merges mid-session, exactly the `error_message: "Skipped"` pattern already on record in the deploy-verification rules.

---

# Session 2026-08-18 (cont.) — zaap dead-table cleanup: 2 live dispatch failures, both root-caused and fixed; 2 flagged Sentry alerts triaged

## Built
- Wrote and opened the migration dropping zaap's dead legacy `public.people`/`managers`/`workers` trio + rebuilding zaap's `field_people` view to match ehow's richer definition. GitHub's API partially outaged (Issues subsystem `degraded_performance`, confirmed via githubstatus.com rather than assumed) and blocked PR creation specifically — writes failed on both GraphQL and REST for ~40+ minutes while reads stayed healthy; backed off with increasing delays rather than retry-looping, per standing instruction. eq-shell [PR #1435](https://github.com/eq-solutions/eq-shell/pull/1435), merged once GitHub recovered.
- First dispatch attempt to zaap failed live: `DROP TABLE workers` hit Postgres `2BP01`, blocked by FK constraints from three tables not in the original migration — `worker_credentials`, `worker_inductions`, `worker_assignments`. Rolled back atomically, no partial state, nothing recorded in the ledger. Verified all three empty + no live code reference anywhere (eq-field/eq-shell/eq-solves-service), and found the stale reason they'd been excluded from an earlier 2026-06-03 cleanup ("shared DB with eq-cards/shell") is architecturally wrong — eq-cards' own `worker_credentials` lives on a completely separate Supabase project (jvkn), not zaap. Fixed via [PR #1436](https://github.com/eq-solutions/eq-shell/pull/1436), merged.
- Second dispatch attempt failed live too: `DROP TABLE worker_assignments` hit `2BP01` again, this time blocked by two `SECURITY DEFINER` functions (`approve_worker_assignment`, `revoke_worker_assignment`) with a real `pending → active → revoked` state-machine implementation — looked enough like a live feature to stop and investigate properly rather than widen the drop a third time unprompted. Confirmed both are `EXECUTE`-granted to `service_role`/`postgres` only (never reachable from a browser), the table holds 0 rows, and none of zaap's 3 actually-deployed edge functions or any app-repo code calls either function. Asked Royce directly before proceeding; he confirmed drop it. Fixed via [PR #1437](https://github.com/eq-solutions/eq-shell/pull/1437), merged — also drops the now-orphaned `worker_revoked_by` enum type.
- All three PRs merged; the actual database dispatch to zaap has not been re-run yet since the second fix landed — still waiting on Royce's go for a third attempt, which should now go through clean.
- Checked two Sentry alerts flagged at session start (eq-shell): "auth-stall: session-spinner-timeout" turned out already fixed and merged by a different concurrent session minutes before this one checked (same PR #1433 logged in the entry above) — closed the now-redundant duplicate PR that had independently found the identical fix (eq-shell [PR #1430](https://github.com/eq-solutions/eq-shell/pull/1430)). "Degraded UI Performance" (a single Long-Animation-Frame blip on `/login`) confirmed as one-off noise — 1 occurrence ever in 90 days, 0 users impacted, no code smell in `LoginPage.tsx` — left alone rather than build a fix for something that isn't actually broken; matches the independent "watch-only" read another concurrent session reached the same day via the sprint-triage doc.

## Decided
- Merge each of #1435/#1436/#1437 separately, each on Royce's own explicit "merge it" (standing pattern: merge = deploy on this repo, gated every time).
- Dispatch the migration to zaap (explicit, both attempts) — third attempt not yet requested.
- Drop `worker_assignments` + its two functions + the orphaned enum, once shown the evidence (via AskUserQuestion) — Royce's explicit call given the scope had grown twice past what the original dispatch approval covered.
- Close the duplicate Sentry PR (#1430) as redundant rather than leave two competing fixes open (via AskUserQuestion, recommended option accepted).

## Deferred
- **Third dispatch attempt to zaap** — both known blockers are now fixed in merged code; waiting on Royce's go to actually run it. _(added 2026-08-17)_
- **`public.qualifications` on zaap** — carries the same stale "shared DB" note as the worker_* tables that turned out to be wrong; not checked or touched this session, needs its own verification pass. _(added 2026-08-17)_

## Notes
- Two consecutive live migration failures on the same object is unusual for this repo's otherwise-clean dispatch history — both were genuine gaps in the original verification pass (grepping app code for table-name references misses DB-level FK/type/function dependencies entirely). Worth remembering for any future tenant-plane DROP: check the live Postgres dependency graph directly (`information_schema`, `pg_proc`/`pg_get_functiondef`, grants), not just app-code references, before assuming a table is safe to drop.
- Both failures rolled back atomically with zero live impact each time (Postgres wraps a migration file's statements as one transaction) — worth confirming that's true rather than assumed before treating a failed dispatch as low-risk; it was checked live both times via direct row-count queries immediately after each failure, not inferred.
- Widening an already-approved migration's blast radius (drop scope grew from 3 tables → 6 tables → 6 tables + 2 functions + 1 type across the two fixes) was treated as needing fresh confirmation each time, not covered by the original dispatch approval — held both times rather than auto-redispatching after a self-directed fix, consistent with this repo's standing "auth/prod-migration actions get explicit separate go every time" rule.

---

# Session 2026-08-18 (cont.) — Access Control ring visual + tabs shipped; PM Calendar digest moved off a hardcoded group ID onto a real permission

Direct continuation of the earlier access-control redesign work (a prior session, prior date). Royce asked to build the ring visual from the Claude Design mockup across every sub-page, then redirected mid-session to a cross-repo permission-key build for EQ Service's calendar digest.

## Built
- **Access Control**: ring visual (conic-gradient coverage + amber override ring) replaces the old dot+fraction matrix cells and the drawer's text-only summary; real tab strip (Base permissions / Custom groups / Preview a person / Activity) replaces one long scrolling page. Fixed a stale `Tabs` type shim that was blocking the build (described an old, pre-redesign API nobody had ever actually used). [eq-shell PR #1429](https://github.com/eq-solutions/eq-shell/pull/1429), merged, confirmed live.
- **Calendar digest permission-key redirect**: verified live architecture before building — confirmed EQ Service already enforces several canonical permission keys via `can()`, but only for role defaults; group grants never reach it (the Shell→Service JWT contract explicitly scopes `extra_perms` as Field-only); found Service's own `@eq-solutions/roles` pin is 2 minor versions behind Shell's. Concluded the fix belongs at the roster level (Shell resolves effective permissions once, hands Service a plain string list), not a session/JWT change.
- Hit a real conflict mid-build: `@eq-solutions/roles`' own test suite hard-requires manager to hold every permission — a zero-default key was never going to ship. The permission classifier correctly blocked writing the forced `manager` default without Royce's explicit sign-off, twice in a row.
- Per Royce's call ("build the plumbing, don't select anyone yet"), reverted the eq-roles draft entirely and shipped the consuming code as inert plumbing instead: `list-members.ts` (eq-shell) now returns each roster member's effective permissions (`permissions: string[]`, role ∪ group grants minus denials) instead of raw group objects — [PR #1440](https://github.com/eq-solutions/eq-shell/pull/1440); EQ Service's digest gate checks the permission key instead of `CALENDAR_DIGEST_GROUP_ID` (deleted) — [eq-service PR #753](https://github.com/eq-solutions/eq-service/pull/753). Both merged, confirmed live, and completely inert at merge time — the key didn't exist in any released package yet.
- Drew a flowchart for Royce showing the two grant paths (forced manager default vs. custom-group membership) feeding one resolver → the roster API → the digest gate, to explain the "build now, decide the default later" plan.
- **Discovered during this close, not built by this session directly**: a separate thread already resolved the deferred decision — eq-roles v2.7.4 shipped `service.receive_calendar_digest` granted to `manager` only (the exact minimum the invariant allows), eq-shell's pin bumped ([PR #1441](https://github.com/eq-solutions/eq-shell/pull/1441)). Verified live on jvkn: 15 active SKS managers now hold it by default, and the "Calendar Digest Recipients" group (built empty this session) now has 18 members. The feature is fully live end-to-end, not just plumbing — confirmed by direct query, not assumed from the merged PRs alone.
- Answered two quick live questions from Royce on the deployed Access Control page: confirmed no pie/donut charts exist anywhere (a `?tab=dashboard` query param some earlier link carried is inert — the page has no tab routing keyed off the URL), and that Field's 77 extra fine-grained permissions live inside the click-through drawer (grouped, searchable), not counted in the matrix's Full/Partial numbers.

## Decided
- Royce: build the full 4-tab Access Control redesign now, split into 2 PRs (ring + tabs first; Compare roles + Custom-groups redesign later) — confirmed via AskUserQuestion.
- Royce: "commit it" / "push it" / "open a pr then merge" for PR #1429, each confirmed separately.
- Royce: "Yes, go ahead and build it" for the calendar-digest redirect, then "I don't want to select anyone as yet" when asked to confirm the specific manager-default grant line — held that one line, built everything else.
- Royce: "commit and push" / "merge and go" for PRs #1440 and #753, each confirmed separately.

## Deferred
- **Compare roles tab + Custom Groups inline-expand redesign** — explicitly scoped as PR B, not started. _(added 2026-08-18)_
- **EQ Service's own `@eq-solutions/roles` pin (v2.5.8) is 2 minor versions behind eq-shell's** — not a blocker for anything built this session, but a real, separate gap. _(added 2026-08-18)_
- **No live click-through by a person** for either the tab/ring redesign or the digest recipient change — both verified structurally (typecheck/lint/tests, live deploy confirmation, direct database query), not by watching a real person use the page or a real digest email send. _(added 2026-08-18)_

## Notes
- The permission-classifier block on the `roles: ["manager"]` edit is worth remembering as a positive example, not friction: it stopped a default-access decision from being made on inferred authorization ("build the plumbing" a few turns back) rather than an explicit one, and the eventual real answer — shipped by a separate thread — landed on exactly the same value that edit would have written. The block cost a short pause, not a wrong outcome.
- `list-members.ts`'s permission computation was designed deliberately so EQ Service never needs its own `@eq-solutions/roles` version bump for this feature — the roster API resolves everything server-side and hands Service a plain string list, checked with a bare `.includes()`. This is why the plumbing could ship completely inert ahead of the eq-roles release, and why Service's separate, real pin gap (v2.5.8 vs v2.7.4) didn't block this feature at all.
- This session's Access Control work and the calendar-digest work are unrelated except by timing — flagged separately in both pending.md sections rather than combined, so a future reader doesn't infer a dependency that doesn't exist.

---

# Session 2026-08-18 (cont., eq-solves-service) — group trimmed, and an explicit kill switch added before the digest rollout finished underneath it

Continuation of the calendar-digest curation thread from the prior session's close. Two direct asks from Royce, both time-sensitive.

## Built
- **Checked on the background task from the prior close (`task_22c1a820`)**: it had already finished and merged before this segment started — but it shipped the original raw-group-ID design, not the permission-key redesign Royce asked for. The redirect brief written for it was never actually pasted in; Royce drove that session directly instead and it ran to completion on the older plan. Read its full transcript (`mcp__ccd_session_mgmt`) rather than guessing from its own summary, since its final message ("nothing left open") didn't mention this.
- **Found a real discrepancy in that transcript**: the session told Royce he'd said Royce Milmlow and "EQ Dev" specifically shouldn't be on the digest list, then seeded the group with all 20 people anyway (including both), on a "you can trim it yourself" default.
- **Royce: "trim the group in Access Control now."** Queried `shell_control.security_groups`/`user_security_groups` directly on jvkn (found the schema — it's not in `public`), confirmed both names, removed exactly those two rows. Left the other 18 untouched — no basis to judge the rest of the roster.
- **Spun off a proper follow-up** (`task_d49dd3ad`) for the actual permission-key rebuild, this time passing the full brief through the task's own prompt field instead of chat text — the mechanism that failed silently last time.
- **Royce: "make sure no emails send in the short term please."** Ran the Session Gate (`/brief`) first — a real gate this time, not a formality: it's what surfaced that PR #753 (permission-key redesign) had *already* merged to main from a separate, concurrent thread, and was currently failing closed by accident (the `service.receive_calendar_digest` permission key didn't exist in any released package yet, so nobody had it). That accidental safety was explicitly not good enough to rely on.
- Added a real, deliberate kill switch: `SUPERVISOR_DIGEST_PAUSED`, checked at the actual send call in `runSupervisorDigests` (Preview mode returns before this point, so review still works while paused). Folded the new `paused` status into the "Send Digest Now" summary and the cron route's JSON response; gave it a badge in the digest history UI. [eq-service PR #754](https://github.com/eq-solutions/eq-service/pull/754), merged (`7be155f`), confirmed live via commit-ancestry against the newest ready production deploy (not assumed from the merge alone — same discipline as the eq-shell precedent).
- Set `SUPERVISOR_DIGEST_PAUSED=true` on eq-service's Netlify (all contexts), confirmed via a full env-var read-back, not just the write call's own success message.
- **Confirmed at close time this pause was not redundant.** The concurrent Access Control session's own close (same day, later) recorded that the permission-key rollout finished for real while this was in flight: eq-roles v2.7.4 shipped the key granted to `manager` by default, eq-shell's pin bumped, and 15 active SKS managers now hold it live — on top of the 18-person group. Had the kill switch not shipped when it did, the accidental "nobody has the permission yet" safety would have silently expired and the next cron run would have gone out for real.

## Decided
- Royce: trim the two specific people, nothing broader, right now.
- Royce: make sure no emails send in the short term — treated as authorization for the code change + Netlify env var + merge needed to actually guarantee that, not just a status question.

## Deferred
- **`SUPERVISOR_DIGEST_PAUSED` needs Royce's own call to unpause** — the feature is fully live and populated now (see above), so this isn't a "finish the build" item anymore, it's a "when are you ready" item. Also worth a look at whether the 18-person group and the 15-manager default now double up in a way he wants trimmed further.
- **`CALENDAR_DIGEST_GROUP_ID` is still sitting on eq-service's Netlify, unused** since PR #753 stopped reading it — harmless dead config, worth deleting next time someone's in the env var list.

## Notes
- **Two concurrent sessions were both actively rebuilding the same feature this session, from two different repos, without either one knowing about the other's exact progress in real time** — this one (eq-service, the pause switch) and the Access Control session (eq-shell + eq-roles, the actual permission rollout). Neither duplicated the other's work, but only because each checked live state (via `/brief`, and via direct database queries) rather than trusting its own prior assumptions about what was still incomplete. Worth remembering as the reason the brief-gate's "verify live, don't assume" step earned its keep today rather than reading as ceremony.
- **The shared `eq-context` checkout still hard-blocks `git pull`** (same F9 guard as the prior close) — used an isolated clone again for this close's writes. Consistent with the documented recovery path; no new issue.

---

# Session 2026-08-17/18 (eq-intake) — "Bring Data In" redesign shipped end-to-end and confirmed live; repo-wide eq-shell CI block found and cleared along the way

Continuation of the same session logged as "entire session's work sits uncommitted" earlier in `sessions/2026-08-17.md` — that close left everything sitting in the worktree pending Royce's go. This entry covers everything since: shipping it, discovering it hadn't actually reached production, and closing that gap.

## Built
- **Shipped the redesign**: committed (`6b2f336`), pushed, eq-solves-intake [PR #119](https://github.com/eq-solutions/eq-solves-intake/pull/119) opened, CI green, squash-merged (`61e7486`). Feature branch deleted, worktree removed — the worktree's `node_modules`/build cache stayed locked until a still-running Vite dev server inside it was stopped first.
- **Royce sent a screenshot of the live Intake dashboard and asked "nothing has changed?"** — investigated rather than assuming the merge itself was the ship event. Root cause: eq-solves-intake has no auto-deploy; core.eq.solutions serves eq-shell's own *vendored copy* of the package, which only updates when eq-shell's automated re-vendor workflow opens and merges its own PR. The screenshot was also of the Overview/dashboard tab specifically — not where this redesign lives even once deployed (that's the Bring Data In tab).
- **The re-vendor PR (eq-shell #1428) needed two real fixes before it could merge**: (1) eq-shell's stricter `tsc -b` project build caught a genuine leftover unused import (`QUICK_PREFIX`) that eq-solves-intake's own `tsc --noEmit` never would have — fixed at the source repo (`60afcbd`), not the vendored copy, per this repo's own documented anti-pattern warning. (2) `gh pr create` hit a transient GitHub API 503 on the first attempt — resolved by retrying.
- **#1428 then hit a genuinely unrelated, repo-wide CI block**: a required "Schema drift" check failing on every open eq-shell PR, root-caused to 2 database functions live on the control-plane DB with no migration file anywhere in the suite. Confirmed out of scope for an eq-solves-intake steward session (schema/DDL work routes through the owning repo's own migration pipeline, never hand-applied here) — spawned it off as background task `task_1fab30d7` rather than fixing it inline. That task delivered eq-shell [PR #1431](https://github.com/eq-solutions/eq-shell/pull/1431) (see `eq/pending/eq-shell.md` for the fix itself), which merged (by the task's own session, confirmed rather than assumed when this session's own merge attempt reported "already merged").
- Updated #1428's branch off `main` once #1431 landed, re-ran CI clean across every check, merged (squash `44a039d`) on Royce's explicit "wait for the remaining checks then merge 1428". Confirmed genuinely live via Netlify's deploy record — production `commit_ref` matches the merge SHA exactly, published 19:08:14Z UTC — not just inferred from a green merge.
- Along the way, spawned two more background tasks for guard-infrastructure gaps hit while writing to this substrate mid-session: the brief-gate exemption list not covering the `eq/pending/<repo>.md` split (`task_0fc52b9f`), and `stale-main-gate` apparently checking a hardcoded path instead of the invocation's actual repo (`task_849f9c7b`). Both written up in `eq/pending/eq-context.md`; neither confirmed landed by this close.

## Decided
- Royce: "commit this and open a PR" — ship the redesign.
- Royce: "merge it" (PR #119), "delete the branch", "yes, also remove the worktree" — each a separate explicit go.
- Royce: "merge PR 1431", then "wait for the remaining checks then merge 1428" — the drift-fix and the actual redesign ship, approved separately once CI was confirmed green rather than merged eagerly.

## Deferred
- `task_849f9c7b` (`stale-main-gate` hardcoded-path bug) — status still not independently confirmed; a same-day `~/.claude/hooks/guard.js` fix by an unrelated concurrent session (see that session's own entry elsewhere in this file, commit `315fbc0`) looks like the same root cause but wasn't cross-checked against this specific report.
- `task_0fc52b9f` (brief-gate exemption gap) — confirmed landed while resolving this close's own rebase conflicts: an unrelated concurrent session hit and fixed the identical gap independently (`~/.claude` commit `f5a58d7`, see that session's own entry elsewhere in this file) before this task's report could be located. Not verified whether `task_0fc52b9f`'s own spawned session produced a separate, now-redundant fix — worth a quick check next time that session surfaces.
- Already tracked in `eq/pending/eq-shell.md` by the task that fixed the drift: #1434/#1429 still haven't picked up the fix, #1416's green check predates it. Not this session's to fix.

## Notes
- **"Merged" and "live" are not the same claim, and this session's own earlier surprise ("nothing has changed?") is exactly why**: eq-solves-intake's PR #119 merge was real and correct, but it shipped code into a source repo with no path to production of its own — the actual production deploy didn't happen until a second, separate PR in a second, separate repo (eq-shell) merged hours later. `eq/changelog/eq-intake.md`'s own PR #119 entry already flagged this in writing at the time ("not a live-deploy event") — didn't stop it from being a genuinely confusing moment live regardless. Worth remembering for any future eq-solves-intake ship: the redesign isn't done until eq-shell's re-vendor PR is merged too, not just this repo's own PR.
- Verified the final "is it actually live" question the careful way both times this session needed it (once for #1431, once for #1428): pulled the Netlify deploy record directly and matched `commit_ref` against the merge SHA, rather than trusting a green merge or a `ready` deploy state alone — the same discipline other concurrent sessions today independently applied to their own eq-shell merges (see the entries above).

---

# Session 2026-08-18 (cont.) — calendar-digest permission rollout finished for real; recipient count correction

Received the same underlying brief as the two calendar-digest entries already in this file ("Access Control ring visual... redirected... calendar digest", and "group trimmed, kill switch added") — via direct chat, not a spawned task. Heavy overlap with concurrent work confirmed live rather than assumed throughout, per the F9/"verify before building" pattern already earning its keep elsewhere in this file today.

## Built
- Verify-first, before writing anything: confirmed `resolveEffectivePermissions()` is already eq-shell's live server-side authorization resolver (`_shared/permissions.ts`'s `can()`/`requirePerm()`, every `requirePerm()` call in the repo) and already backs `security-groups.ts`'s `?action=user_perms`; confirmed EQ Service already enforces several `service.*` keys, but only role-only local `can()` checks (`lib/utils/roles.ts`) — the digest gate is the first EQ Service consumer of the full role∪group effective set.
- Found [eq-shell PR #1440](https://github.com/eq-solutions/eq-shell/pull/1440) open (not yet merged) and [eq-service PR #753](https://github.com/eq-solutions/eq-service/pull/753) already merged at session start — attempted to merge #1440 myself; a concurrent session merged it first, moments into this session, confirmed via `mergedAt`.
- Registered `service.receive_calendar_digest` in `@eq-solutions/roles` — [PR #29](https://github.com/eq-solutions/eq-roles/pull/29). Hit the same manager-holds-every-permission invariant this file's other entries describe; asked directly via `AskUserQuestion` rather than inferring from an earlier turn — Royce chose manager-only. Merged, tagged v2.7.4.
- Bumped eq-shell's `@eq-solutions/roles` pin to v2.7.4 — [PR #1441](https://github.com/eq-solutions/eq-shell/pull/1441). Beyond the version bump, added `service.receive_calendar_digest` to `permission-enforcement-baseline.json`'s `enforced_downstream_service` category (the drift-guard test failed without it — eq-shell never itself checks this key, enforcement is entirely in EQ Service). 353/353 tests pass. Merged, confirmed live via Netlify deploy `commit_ref` match.
- **Executed the actual permission grant**: `INSERT INTO shell_control.security_group_perms` for the "Calendar Digest Recipients" group + this key — confirmed via the `INSERT ... RETURNING` result (a real new row, not a conflict no-op) that this hadn't already been done. Verified end-to-end by reproducing `list-members.ts`'s exact join + `resolveEffectivePermissions` logic against live data for every group member.
- Bumped EQ Service's own `@eq-solutions/roles` pin (v2.5.8 → v2.7.4) — [PR #755](https://github.com/eq-solutions/eq-service/pull/755), via `/decide` (scaled-down gut-check, not full six-section ceremony — low-stakes, reversible). Not functionally required (the digest gate reads a plain string), closes the drift this file's earlier entry flagged as a deferred item.
- Discovered [eq-service PR #754](https://github.com/eq-solutions/eq-service/pull/754) (the `SUPERVISOR_DIGEST_PAUSED` kill switch) already merged and live — not this session's work, found while investigating why `main` had moved since the brief was written. Confirmed via a direct Netlify env-var read that `SUPERVISOR_DIGEST_PAUSED=true` is genuinely set, not just claimed by the PR description.
- **Correction to this file's own record**: the two earlier entries above both state "15 active SKS managers... 18 group members... confirmed by direct query." Re-querying live data just now: **14** active SKS managers total, not 15. More importantly, neither earlier entry checked for overlap between the manager role-default and the group — 3 of those 14 managers (including Royce himself, `royce.milmlow@sks.com.au`, and `dev@eq.solutions`, which reads like a system/test account) were never added to the group and would receive the digest purely through the role default. True distinct recipient count once unpaused: **20**, not 18 — the group's 18 members (17 with an email on file) plus those 3 extra managers.

## Decided
- Royce: manager-only default for the new permission key, chosen directly via `AskUserQuestion` after the invariant conflict.
- Royce: "Proceed through 1-3" (release the key, bump eq-shell's pin + merge #1440's dependent work, grant the permission) — each step confirmed complete before moving to the next, given this is a live production email path.
- Royce: "Yes, go ahead" on the `/decide` pass covering the EQ Service pin bump + a fresh check for the dead `CALENDAR_DIGEST_GROUP_ID` env var.
- Did NOT unpause `SUPERVISOR_DIGEST_PAUSED` and did NOT delete `CALENDAR_DIGEST_GROUP_ID` — both flagged to Royce as needing their own explicit go, not covered by the above.

## Deferred
- **`SUPERVISOR_DIGEST_PAUSED` unpause** — needs Royce's review of the corrected 20-person list first. Tracked in `eq/pending/eq-solves-service.md`.
- **`CALENDAR_DIGEST_GROUP_ID` Netlify env var deletion** — confirmed dead in code (fresh grep of current `main`, zero references), needs Royce's go since it's a live production setting. Tracked in the same file.
- **Whether `dev@eq.solutions` should hold the manager role at all** — surfaced while correcting the recipient count, not this session's call.

## Notes
- This is at least the third session today working this exact brief (the two entries directly above, plus this one) — each apparently unaware of the others' precise real-time progress, consistent with the "two concurrent sessions... neither duplicated the other's work, but only because each checked live state" pattern already called out in this file. This session's distinctive, independently-verifiable contribution is the actual `INSERT` that granted the permission (confirmed via `RETURNING`, not assumed), the eq-shell baseline-test fix, the EQ Service pin bump, and the recipient-count correction — not the earlier design/plumbing work, which had already landed by the time this session reached it.
- The "15/18" figure being repeated twice in today's log before being caught here is a live instance of exactly the failure class `system/failures.md` tracks as F14 ("a hand-written claim about current state ages into a lie, and nothing anywhere notices") — except in this case the claim was wrong from the moment it was written (an unchecked overlap), not aged into wrongness later. Worth remembering: "confirmed by direct query" is only as good as what the query actually checked — a group-membership count is not the same claim as a distinct-recipient count once role defaults are added back in.
- Chose not to edit the two earlier entries in this file — they're frozen point-in-time records per this file's own established convention (see the 2026-08-18 "Compliance-pack caption reverted" entry's substrate-correction note for precedent); corrected the live-facing docs (`eq/pending/eq-solves-service.md`, `eq/pending/cross-repo.md`) instead, which is where a future reader or session would actually look for current state.

---

# Session 2026-08-18 — SKS NSW Labour usage data → EQ Field cold-boot schedule cache, shipped and live

## Built
- `loadFromSupabase()` (index.html) now caches the visible schedule window to `localStorage`, tenant-scoped, on every successful load.
- On a cold-boot fetch failure with nothing yet in memory, falls back to that cache (capped 3 weeks old) instead of rendering an empty/broken My Schedule + home tile.
- `home.js`'s offline banner extended to also fire on this fallback, with wording distinct from the true-offline case.
- Shipped as eq-field PR #715 → v3.5.513, squash-merged, confirmed live via `field.eq.solutions/sw.js`.

## Decided
- Royce confirmed the task brief before implementation (schedule-cache change touches a hot path with prior week-window bug history).
- Royce chose "broader: stale-while-revalidate for STATE.schedule" over a narrower My-Schedule-only scope, via AskUserQuestion.
- Royce corrected framing mid-session: EQ Field's own PostHog usage numbers are not a valid signal for anything (no real users yet) — only SKS NSW Labour's real usage data should drive Field decisions. Saved as a standing memory (`feedback_ignore_eq_field_own_posthog`).
- Royce explicitly directed "merge it" — merged (squash + delete branch), confirmed live in production.
- Royce chose "verify the schedule cache live" as the next priority over chasing the eq-shell auth-stall bug or investigating the legacy app's "Editor" screen.

## Deferred
- **Live click-through of the shipped feature** — three verification paths tried, all blocked (see pending.md for detail): local static server can't resolve tenant routing (Netlify Function 404s outside Netlify's runtime); the dev-override path was abandoned when the repo's secret-scan hook correctly caught writing a JWT-shaped (if non-secret) anon key to any file, and that block was respected rather than routed around; no local `netlify dev` with real function env vars available. Needs a real signed-in SKS session or a properly configured Functions environment.
- **eq-shell `EQ-SHELL-V`** (`auth-stall: session-spinner-timeout`, culprit `/sks/staff`, unresolved + regressed) — this session has no `eq-shell` repo access to triage it.
- **SKS NSW Labour's "Editor" screen** — 243 views/month, unclear what it is or whether Field already covers it.
- **Baseline Field's own rageclick rate** — legacy app's rate is climbing but ambiguous (tracks traffic growth); worth instrumenting Field's own rate now while volume is small.

## Notes
- **PostHog behavior analysis method**: used `$host` (not `app`/`tenant` properties, which turned out unreliable for this split) to separate SKS NSW Labour traffic from EQ Field traffic in the shared `eq-production` PostHog project (162632). Key findings: schedule-checking is a mobile habit (8% of visitors, ~15x the views-per-visitor of desktop); usage peaks hard at 6-8am Sydney time (27% of weekly sessions in a 2-hour window, confirmed via HogQL `toHour(toTimeZone(...))`, not the coarser UTC-bucket trends query); saving a timesheet is the opposite — almost entirely desktop (3503:15 in July), implying it's an office/admin bulk-entry workflow today, not worker self-service.
- **Sentry root-cause note**: `EQ-SHELL-V` couldn't be triaged past the issue metadata — this session's GitHub connection 404s on the `eq-shell` repo (no access), and Sentry's Seer autofix had no budget available. Two distinct occurrence clusters found: a likely dev-testing burst (4 events/20s, Chrome/Mac, 2026-07-23) and one isolated real-looking hit (Electron/Windows, `/sks/staff`, 2026-08-17).
- **Version-collision pattern**: this repo is under heavy concurrent-session load right now — hit two separate version-number collisions (v3.5.511, v3.5.512 both taken by other sessions mid-PR) requiring two rebases. Also caught a cache-buster drift-guard failure (a changed script's `?v=` tag hadn't moved) on the first CI run — fixed before merge.
- **Root checkout warning**: `C:\Projects\eq-field` (not a worktree) is mid-work on `claude/p1-timesheet-leave-own-crew-rls` (draft RLS scoping for timesheets/leave, upstream gone, untracked file present) — did not touch it, flagging for anyone else who lands there expecting a clean `main`.
- **New Sentry issue observed, not acted on**: `eq-field` `EQ-FIELD-16` (`TypeError: Failed to fetch`, culprit `_mintDataJwt`) appeared ~3hrs into this session, tagged `release: eq-field@3.5.513`. Checked and ruled out as related to this session's change — different code path (JWT minting via `verify-pin`, triggered by a `people` fetch), 1 event/1 user, almost certainly a transient mobile network drop on a real SKS session. Not fixed, just noted in case it recurs.

---

# Session 2026-08-18 (cont.) — zaap dead-table migration finally landed, 5 dispatch attempts total, 2 more real bugs found along the way

## Built
- Continuing the zaap cleanup logged earlier today: redispatched with 0248 widened to cover `worker_assignments`'s two functions + the orphaned enum (per Royce's explicit go, see earlier entry) — **0248 succeeded live this time**, confirmed via direct query: all six legacy tables, both functions, and the enum type gone from zaap.
- 0249 (the view-parity rebuild) then failed on its own: `column "licence" does not exist`. Root cause was bigger than the view — direct column comparison found zaap's `app_data.staff` genuinely missing 7 columns ehow's has (`agency`, `hire_company`, `job_title`, `licence`, `on_roster`, `rating`, `rto`), not just a view under-exposing existing data as originally assumed. Asked Royce directly rather than guess at the fix; he chose adding the columns to zaap over shipping a permanently narrower view. eq-shell [PR #1443](https://github.com/eq-solutions/eq-shell/pull/1443) — adds all 7 columns (types/defaults copied exactly from ehow), filename deliberately sorted to apply before the view migration in the same numeric slot (matches this repo's existing `0095_*` precedent for tie-breaking same-numbered files). Merged.
- Redispatched again: the column fix applied clean, but the view rebuild then hit a *different* wall — Postgres won't let `CREATE OR REPLACE VIEW` change an existing output column's type (`42P16`), and zaap's live `name` column was `character varying` where the target (matching ehow) is `text`. Checked both planes for anything depending on the view before touching it (`pg_depend` — zero dependents either side) — confirmed a `DROP`+`CREATE` would be a content no-op on ehow, whose view already matches exactly. eq-shell [PR #1444](https://github.com/eq-solutions/eq-shell/pull/1444) — switches `0249_field_people_view_parity.sql` from `CREATE OR REPLACE VIEW` to `DROP VIEW IF EXISTS` + `CREATE VIEW`. Merged.
- **5th dispatch succeeded.** Both migrations fully applied on zaap. Verified live, not just by the green run: all 32 `field_people` columns present and correctly typed (`name` now `text`), `security_invoker` on, `anon`/`authenticated` SELECT grants intact.
- Gave Royce a full recap of the whole saga on request (5 attempts, 3 real root causes found and fixed, not retried blind) plus what's left.

## Decided
- Add the 7 missing columns to zaap's `staff` table rather than ship a narrower view (Royce, via AskUserQuestion, recommended option).
- Merge #1443 and #1444 separately, each on Royce's own explicit "merge it".
- Dispatch the migration each of the 5 times, each on Royce's own explicit "dispatch the migration" — never auto-redispatched after a self-directed fix.

## Deferred
- **`public.qualifications` on zaap** — carries the same stale "shared DB with eq-cards" note that turned out wrong for the worker_* tables. Not checked this session, needs its own verification pass. _(added 2026-08-17, still open)_

## Notes
- Final tally on this migration: 5 dispatch attempts, 4 live failures, every single one a genuine new finding (undocumented FK dependents, live-looking functions needing an explicit safety check, a real base-table schema gap mis-diagnosed as view drift, then a Postgres DDL limitation) — never the same bug twice, and never patched by guessing. Each failure rolled back atomically with zero live impact, verified directly after each one rather than assumed.
- Worth remembering for any future tenant-plane schema change: `information_schema`/`pg_depend`/`pg_proc` against the live database caught every one of these; grepping application code alone (the original verification pass) would have missed all of them — app-code grep only sees what the app *reads*, not what the database itself enforces (FKs, function signatures, view column types).
- `CREATE OR REPLACE VIEW` cannot change an existing output column's type — only `DROP`+`CREATE` can. Worth keeping in mind for any future view migration that changes a column's cast/type, not just adds columns.

---

# Session 2026-08-18 (cont., eq-solves-service) — ACB/NSX save data-loss report investigated, root-caused, and fixed live

Royce reported a tech at SKS hit "wouldn't save / then deleted all the info" today on an ACB check at site CA1, suspected offline-related.

## Built
- Investigated via a subagent first: read the offline-stance code (banner + `isOffline()` pre-check, shipped 2026-07-13) and confirmed it only guards client-side, before a request goes out — it cannot see or prevent a failure between two separate server calls. Found the actual bug: `saveAcbVisualCheckAction`/`saveAcbElectricalReadingAction` (and the NSX mirrors) deleted existing readings, then inserted new ones, as two separate Supabase calls with no transaction. A dropped connection between the two leaves the delete committed and the insert never sent.
- Found the fix already had a precedent in this exact codebase: RCD checks hit the identical failure shape earlier (audit issue #103, migration `0095_rcd_test_save_complete.sql`) and were already fixed with a single atomic Postgres function. Mirrored that pattern for ACB/NSX rather than inventing a new approach.
- Built migration 0213: 4 new atomic database functions (ACB visual/electrical × NSX visual/electrical), each folding delete + insert + step-complete status update into one transaction. Updated the 4 save actions to call the new function instead of the old two-step delete/insert. `tsc --noEmit` and `npm run check` (tsc + full build) both clean.
- eq-service [PR #756](https://github.com/eq-solutions/eq-service/pull/756) — merged once the real CI gate (`tsc + next build`) passed (the separately-failing integration test is a known pre-existing gap per this repo's own CLAUDE.md, not a merge blocker). Migration dispatched via the governed `apply-service-migrations` workflow (not applied by hand) and confirmed live: all 4 functions present on the production database.
- Tried to recover today's specific lost data: searched the live database for the exact signature this bug leaves (a check step marked complete with zero matching readings) — found none, anywhere, for any ACB or NSX check. Narrowed to site CA1 by name once Royce gave it: all readings currently there look complete and correct (3 breakers, 23 visual + 30-40 electrical readings each, all saved earlier today). One older, unrelated-looking duplicate test record from 31 July for the same asset was found and reported, but doesn't match "today."
- Built a before/after diagram (two-call save with a dropped-connection gap vs. one atomic call) to explain the failure visually, per Royce's request.

## Decided
- Royce: "yes, go ahead" — fix the ACB/NSX save actions to use a transaction, after reviewing the investigation.
- Royce: merge once CI is green, then dispatch the migration — both confirmed via AskUserQuestion (the alternative was to just report CI status and let Royce trigger it himself).

## Deferred
- **The specific record Royce asked about couldn't be confirmed as "recovered" vs. "was never actually lost from the database in the first place."** Live data at CA1 looks complete now; asked Royce to confirm with the tech whether it's correct, and to say which breaker/field is still wrong if any is. _(added 2026-08-18)_
- **In-progress form entries still have no draft persistence** — a reload before hitting Save loses unsent data with zero trace in the database, a different (smaller) risk than the one just fixed. Flagged, not built. _(added 2026-08-18)_
- Same-day duplicate/near-duplicate of the two lines already logged in `eq/pending/eq-solves-service.md` above — not repeated here.

## Notes
- Real hard-delete tables with no soft-delete and no row-level history (`acb_test_readings`/`nsx_test_readings`) mean that if this bug's exact failure mode ever does leave a wiped record sitting live, the only recovery path is a full point-in-time restore of the whole database — an all-tenant, high-blast-radius action that needs Royce's explicit call, not something to reach for speculatively. Worth remembering next time a "can we get the data back" question comes up on one of these tables.
- Good example of finding an existing precedent before designing a fix: the RCD migration (0095) wasn't just similar, it was the *same* bug already root-caused and fixed once — mirroring it instead of re-deriving a new atomic-save pattern saved a design pass and matched established house style exactly.

---

# Session 2026-08-18 (cont.) — Dave Rimmer's Cards signup traced to a duplicate-record bug in self-join matching; fixed, shipped, and confirmed isolated by a suite-wide scan

## Built
- Royce reported a Cards signup (Dave Rimmer) not showing up in Shell despite getting the confirmation email. Traced live: shell-side provisioning (auth user, `shell_control.users`, tenant membership) all completed correctly within a minute of signup — the actual gap was `shell-join-tenant.ts`'s worker-match step, which only tried phone. Dave's pre-existing worker record (2026-06-15 bulk import) had no phone on file, so the match missed and a second, blank (`first_name: ''`, `last_name: ''`) worker/staff record was created and linked to his new login instead, while his real named record sat untouched and unlinked — invisible in any name-sorted Staff view.
- Merged Dave's two records by hand (kept the login-linked one, backfilled its name/email from the real record, archived the stub) — verified zero attached timesheets/leave/licences on the discarded side first, so nothing was orphaned.
- Fixed the root cause: added an email-match fallback to `shell-join-tenant.ts`, scoped to unlinked workers only (never repoints an already-claimed login) and requiring a unique hit. Extended further, on Royce's "we want a perfect fix," to compare emails dot-normalized — Dave's signup email (`dave.rimmer@sks.com.au`) and his on-file email (`daverimmer@sks.com.au`) differed only by a dot, so a plain exact match would have missed him too. Mirrors the same email-match tier `cards-approve-staff.ts` already uses for this class of gap. eq-shell [PR #1442](https://github.com/eq-solutions/eq-shell/pull/1442), merged, confirmed live on core.eq.solutions via commit-ancestry check (a later concurrent merge became the actual deploy; this commit is an ancestor of it).
- Reviewed all 5 self-joins from today across the three standing SKS role links (apprentice/supervisor/manager, none new, none requiring approval) — only Dave's hit the bug; the other 4 matched cleanly by phone.
- Checked licence-upload status for today's 5 joiners against `token-exchange.ts`'s Field-access gate (`field_access_unlocked_at`, only stamped once a licence is verified) — only 1 of 5 (Anthony Hartley) has uploaded anything; the other 4 can sign into Shell but will 403 on Field until they do. Confirmed as designed self-join behaviour, not a bug.
- Investigated a follow-up question ("I can only see one of Anthony's [licences]") — his second licence (driver licence) is deliberately `is_private: true`; every function feeding the Staff licence view filters that out by design. Not a bug.
- Investigated "I updated Cameron's email recently" — confirmed true via `email_locked_by_shell=true`/`phone_locked_by_shell=true` on his `app_data.staff` row (set only by a Staff-page edit). Found the edit only landed on the tenant plane (ehow) — the canonical `public.workers.email` on jvkn, which the new matcher reads, still holds his original 2026-06-01 personal Gmail. A real, one-way sync gap: Staff-page corrections never propagate upstream. Also checked whether wider self-service email update would help — `set-recovery-email.ts` exists but only sets an email once while null, never corrects one, and only ever writes to `shell_control.users` — wouldn't touch the field this gap is in anyway. Flagged, not fixed.
- Found a second, independent instance of the blank-record failure mode while drilling down: William Hong, self-joined 2026-08-05, canonical `public.workers` record sitting with a blank name for 13 days. Not a duplicate (no prior record to collide with) — the Staff-page roster itself already showed him correctly (`app_data.staff` had his real name/email all along); only the canonical copy was stale. Backfilled `public.workers` to match. Royce: "fix William's blank name now."
- Ran `/decide` on whether to build a monitor for this failure class, then ran the scan the decision's own crux called for: 0 blank-name workers remain suite-wide; of 20 admin-corrected (`*_locked_by_shell`) records checked against their canonical counterpart, only Cameron's has actually drifted. Total distinct incidents found, ever: 3 (Dave, William, Cameron) — all three already caught and fixed/flagged this session. Concluded the base rate doesn't justify a dedicated monitor; reversed the initial lean.

## Decided
- Royce: merge the data-dedup fix now, code fix after (his own recommended option via AskUserQuestion).
- Royce: "extend the fix to cover the email variant — we want a perfect fix" — added the dot-normalization tier.
- Royce: "commit and push, then merge" — PR #1442 shipped after CI went green.
- Royce: "fix William's blank name now" — done, no duplicate risk since there was no colliding record.
- Decided (via `/decide` + the scan it called for, not yet re-confirmed by Royce): don't build a blank-name/stale-record monitor — 3 total incidents ever is too low a base rate to justify it.

## Deferred
- **Cameron Tregoning's canonical email (`public.workers.email` on jvkn) is stale and can't self-heal** — his correct, locked Staff-page edit never reached the canonical copy other code (including the new matcher) reads. No fix scoped or approved. _(added 2026-08-18)_
- **No real self-service "update my email" flow exists** — `set-recovery-email.ts` is first-time-only and writes to a different table than the one the Cameron gap lives in. Royce raised it, no decision made. _(added 2026-08-18)_
- Both tracked in `eq/pending/eq-shell.md`.

## Notes
- The email-variant discovery (Dave's dot vs no-dot addresses) came directly from Royce pushing back on "is this a perfect fix?" rather than accepting the first, narrower fix — worth remembering that an honest "no, here's exactly what it still misses" answer led straight to the real fix, where a confident "yes, fixed" would have shipped something that still wouldn't have caught the reported case.
- The `/decide` pass on the monitor question is a clean example of the protocol doing its job: the initial lean (build it) flipped to the opposite call once the crux question it named ("is this actually a recurring pattern, or three isolated incidents?") was answered by data instead of assumption. The six-step structure didn't just organize the reasoning — it produced a falsifiable prediction that the follow-up scan then falsified.
- Verified eq-shell's live deploy the careful way for PR #1442, same discipline as other sessions today: a later concurrent merge became the actual production build, so the check was commit-ancestry against the real deploy record, not the merge itself going green.
