# Session 2026-08-04 — Removed the "What's new" banner from both SKS NSW Labour and EQ Field

## Built
- SKS NSW Labour: deleted `scripts/whatsnew.js` entirely, plus its script tag, sidebar link, and container div in `index.html`, and its `sw.js` precache entry. v3.10.107 → v3.10.108. PR [#75](https://github.com/eq-solutions/sks-nsw-labour/pull/75) merged to `main`, confirmed live on sks-nsw-labour.netlify.app via the Netlify deploy record.
- EQ Field: found the same banner already retired to a no-op on 2026-07-13 (Royce: "get rid of it — it's annoying"), but the code was left in three places — the embedded copy hand-merged into `scripts/core-bundle-b4.js`, the hidden `#whatsnew-banner` div, and an orphaned standalone `scripts/whatsnew.js` unreferenced since the v3.5.403 bundle merge. Deleted all three for parity. v3.5.445 → v3.5.447 (renumbered from .446 after a version-number collision with concurrently-merged PR #640). PR [#641](https://github.com/eq-solutions/eq-field/pull/641) merged to `main`, verified clean on a real Netlify deploy preview (zero console errors, full dashboard render).
- Explained the separate roster-change push-notification opt-in in SKS's `scripts/auth.js:1021-1126` (unrelated to the banner, confirmed NOT present in EQ Field — left untouched in both repos).

## Decided
- Royce: remove the What's new banner "completely" (not just disable) — applied to both repos for parity.
- Royce: commit → push → merge, confirmed explicitly and separately for each repo (SKS first, then EQ Field).

## Deferred
- None new this session — both repos' cleanup landed fully. eq/pending.md's existing "audit always-loaded boot scripts" item (2026-07-28) had `whatsnew.js` on its list; trimmed that reference since the file no longer exists — the other 4 scripts (recognitions.js, digest-settings.js, apprentice-widget.js, region-filter.js) remain open follow-up.

## Notes
- **Real git-mechanics incident, caught before it shipped:** rebasing the EQ Field branch onto `origin/main` hit the version-bump-line conflict pattern documented below; running `git commit --amend` while HEAD was still detached at the conflict-paused rebase amended `origin/main`'s own commit (PR #640, unrelated map-anchor fix, wrong author) instead of creating a new commit on top of it — silently squashing two unrelated releases together. Caught by noticing the resulting commit touched 8 files instead of the expected 6, not by trusting a green rebase. Fixed by resetting to the clean pre-rebase branch tip and redoing the integration as a `git merge` instead — fails safe to a normal push, no force/history-rewrite needed. A `--force-with-lease` attempt was also correctly blocked by the session's auto-mode classifier, which is what pushed toward the merge-instead-of-rebase fix in the first place.
- **New recurring collision pattern worth knowing about:** in both repos, when two concurrent branches each bump the SAME version-tracking lines (`APP_VERSION`, `sw.js` CACHE string, script `?v=` cache-bust tags) to the exact same next-integer version, a plain `git rebase`/`git merge` auto-resolves those lines as non-conflicts (identical text on both sides) — silently leaving two unrelated releases claiming the same version number. Not caught by git; only caught by manually diffing the touched-files list against what the change actually contains. This happened for real on EQ Field this session (PR #640 also claimed v3.5.446); resolved by manually renumbering to the next free version.
- EQ Field's `docs/reflection-log.md` has a hard commit-time gate (`reflection-gate` in `guard.js`) requiring a reflection entry staged in the same commit as any code change — not documented in the global session-gate rules, discovered live when the first `git commit --amend` attempt was blocked. SKS NSW Labour has no equivalent gate.

---

# Session 2026-08-04 — Canonical worker-link guard, roster keyboard nav, Prestart/Toolbox fixes, supervisor taxonomy + zaap parity (4 PRs), EQ-FIELD-10 closed out

## Built
- eq-field [PR #639](https://github.com/eq-solutions/eq-field/pull/639) merged — `_canonicalWorkerUpsert` could mint a second jvkn `workers` identity for a person who already had one (confirmed real: Phoenix Khatri, 2026-07-05). Added a name-only lookup as a guard (not an auto-link) — surfaces "possible duplicate" instead of silently creating one. Extracted the canonical-link helpers out of `people.js` into a new `scripts/people-canonical-link.js` (people.js had crossed its 1550-line CI ceiling with the guard added).
- eq-field [PR #642](https://github.com/eq-solutions/eq-field/pull/642) merged — Edit Roster's day-cell popover gained full keyboard nav (Arrow/Tab through pills + filtered site list, Enter to commit) and Monday now auto-fills Tue–Fri when those days are still blank (reuses existing `fillWeek()` exactly). Extracted into new `scripts/roster-cell-picker.js` (roster.js was pressed against its 2400-line ceiling).
- eq-field [PR #643](https://github.com/eq-solutions/eq-field/pull/643) merged — `exportPrestartDocx()`/`exportToolboxDocx()` had zero error handling across ~150-line async functions each; a thrown/rejected step surfaced as nothing (no toast, no visible error) — now wraps the build in try/catch, toasts on failure, reports to Sentry. Also found `saveToolboxDraft()` had no lock check at all — opening an already-submitted talk and clicking Save Draft silently flipped it back to `status='draft'`. Toolbox Talk now gets the same lock-on-submit Prestart already has (mirrors `_isPrestartLocked`/`_renderPrestartCrew` line-for-line).
- eq-field [PR #644](https://github.com/eq-solutions/eq-field/pull/644) merged — `managers.js`'s hardcoded supervisor-category taxonomy (`Management`/`Other`) had drifted from eq-shell's real `SUPERVISOR_CATEGORY_OPTIONS` (`Project Management`/`Operations`), verified by reading eq-shell's source directly — fixed to match exactly. Also: zaap (the `eq` tenant's canonical DB) never received eq-shell PR #692's migration — `app_data.staff` had no `is_supervisor`/`supervisor_role`/`supervisor_category` columns, `app_data.field_managers` was the pre-#692 view with no supervisor filter at all. New migration (`supabase/migrations/20260804_field_managers_zaap_canonical_parity.sql`) brings zaap to byte-for-byte parity with ehow's live shape (view, `security_invoker`, grants, digest trigger) — every fact read live off ehow via Supabase MCP before writing it, applied to zaap only (1 row total in `app_data.staff` today — disposable sandbox, low risk). Self-caught a security gap while applying it: `CREATE FUNCTION` grants EXECUTE to PUBLIC by default, so the new digest trigger function was briefly `anon`/`authenticated`-callable via `/rest/v1/rpc/` — caught via `get_advisors` immediately after applying, confirmed absent on ehow's real copy, revoked before considering it done.
- Sentry `EQ-FIELD-10` (`dashboard-map-css-collapsed`) investigated live and marked **resolved**. Every event pulled from Sentry predates one of two already-shipped fixes (PR #631/v3.5.438's tab-switch guard, v3.5.444's deliberate-collapse guard) — production is on v3.5.451, both fixes are live. Full write-up published as an artifact (`/gap` full-mode HTML) rather than left as chat text.

## Decided
- Royce: merge #639 and #642 (explicit, mid-session).
- Royce: add error-surfacing to the Word exports now, not wait for a live repro (explicit, via AskUserQuestion).
- Royce: give Toolbox Talk the same lock-on-submit Prestart has, not just a narrower guard (explicit, via AskUserQuestion — picked the fuller option).
- Royce: for the supervisor canonical drift, "just the findings for now" initially — then explicitly escalated to "continue towards fully tenant canonical," which is what authorized the zaap migration.
- Royce: merge #643 and #644 (explicit, separately).
- Read/write access to supervisor data *from* Field — explicitly NOT built. Existing design (`managers.js:17`, Shell-only writes) is deliberate; flagged as a real tension against Royce's "in a perfect world" aspiration rather than silently building a second write path.

## Deferred
- Someone (Royce, via Shell) needs to actually mark real `eq`-tenant people as supervisors — the DB now supports it, nobody is marked yet.
- 3 real SKS people (John Angangan, Scott Hotson, Jack Cluff) are `is_supervisor=true` in Shell but have no `supervisor_category` set — Shell-side data gap, not an eq-field fix.
- Field→canonical write access for supervisor data — needs an explicit decision, not assumed.
- Toolbox Talk inherits Prestart's existing "no reopen for correction" gap now that it's locked too — known tradeoff, not solved.
- `_dashMapWatchForVanish` watchdog removal (its own comment names the removal condition as arguably now met) — P3, revisit only if EQ-FIELD-10 stays quiet for real.

## Notes
- **Live, active multi-session collision risk, not a one-off.** Confirmed in the OTHER session's log above (PR #640 vs #641 version clash) and hit independently twice more in this session (#639/#642 both collided with concurrently-merging PRs on `main`, needing live re-sync + renumber each time — see `docs/reflection-log.md`'s 2026-08-04 entries in eq-field for the full blow-by-blow). Used `git merge` + live re-fetch before every push, never `git rebase`/force-push, per the git-mechanics incident logged in the session above.
- **Corrected my own earlier answer mid-session**: initially told Royce the Contacts save writes to Field's own `people` table, not canonical — this was wrong for the live SKS tenant. `people` is a JWT-routed table that rewrites to `app_data.field_people`, a view over canonical `app_data.staff` with an `INSTEAD OF` trigger — the profile fields already land in one canonical place. The actual gap was narrower: the `worker_id` *identity link* to a separate cross-app registry (jvkn `workers`), which is what PR #639 fixed.
- Full technical detail (file:line citations, live-verification steps, the 4-check reflection pass for each PR) lives in `eq-field/docs/reflection-log.md`'s 2026-08-04 entries — this log stays high-level by design.

---

# Session 2026-08-04 — EQ Ops jobs FK constraint landed, quotes-vs-jobs split decided against for now

## Built
- Deleted the one known orphan row in `app_data.jobs` (`job_id 7842b5ce…`, pointing at a hard-deleted quote) — direct data cleanup on ehow, not schema DDL. Confirmed zero dependent `job_notes` rows first, verified 0 orphans on both tenant planes afterward (zaap has 0 rows in `app_data.jobs` at all — no live Ops usage there).
- Migration 0237 adds `FOREIGN KEY (quote_id) REFERENCES app_data.quote(quote_id) ON DELETE RESTRICT` on `app_data.jobs`, idempotent via a `pg_constraint` existence check. `RESTRICT` (not `CASCADE`/`SET NULL`) deliberate — a job must never silently lose its quote link or get deleted as a side effect of the quote being removed. eq-shell [PR #1230](https://github.com/eq-solutions/eq-shell/pull/1230), merged, CI's own read-only "Plan" job confirmed clean before merge.
- Dispatched the migration — `1 applied, 249 skipped` on both `eq` and `sks`, live-verified via direct `pg_get_constraintdef` query on both planes (not just trusting the dispatch log).
- Ran `/decide` on "do we need the quote-vs-jobs Kanban split" (the original ask that opened this whole thread on 2026-08-03). Full six-step pass, recommendation: **not now**. Both problems that motivated it — Open column density and `job_number` reliability for Field — are already solved by cheaper, already-shipped changes (collapsed-customer-groups + the full sync-gap/backfill/FK chain). Crux to revisit: if the Open column still feels crowded with groups collapsed, or job-specific features (costing, PO dashboards) start needing a shape the current single board can't express.

## Decided
- "delete the orphan job row, add the FK constraint" — explicit go, both actioned same session.
- "dispatch it" — migration 0237 dispatched to production, live-verified on both planes.
- `/decide` outcome (quotes-vs-jobs split: not now) — a recommendation, not yet explicitly confirmed by Royce; logged as the current state of the decision, open to revisiting per the stated crux.

## Deferred
- The quotes-vs-jobs Kanban split itself — deliberately not built, per the `/decide` call above. Not closed, just not active.
- 7 quotes in `submitted` status still missing a follow-up date (unrelated pre-existing gap, carried from 2026-08-03).

## Notes
- This closes out every remaining open item from the 2026-08-03 EQ Ops board→job sync thread except the split itself (now explicitly deferred with a reasoned crux, not just "not started") and the unrelated follow-up-date gap.
- `TODAY.md` GOALS is unset — the `/decide` pass said so plainly rather than borrowing urgency from an old file, per the decision protocol's own rule.
