# Session 2026-08-04 — Removed the "What's new" banner from both SKS NSW Labour and EQ Field

## Built
- SKS NSW Labour: deleted `scripts/whatsnew.js` entirely, plus its script tag, sidebar link, and container div in `index.html`, and its `sw.js` precache entry. v3.10.107 → v3.10.108. PR [#75](https://github.com/eq-solutions/sks-nsw-labour/pull/75) merged to `main`, confirmed live on sks-nsw-labour.netlify.app via the Netlify deploy record.
- EQ Field: found the same banner already retired to a no-op on 2026-07-13 (Royce: "get rid of it — it's annoying"), but the code was left in three places — the embedded copy hand-merged into `scripts/core-bundle-b4.js`, the hidden `#whatsnew-banner` div, and an orphaned standalone `scripts/whatsnew.js` unreferenced since the v3.5.403 bundle merge. Deleted all three for parity. v3.5.445 → v3.5.447 (renumbered from .446 after a version-number collision with concurrently-merged PR #640). PR [#641](https://github.com/eq-solutions/eq-field/pull/641) merged to `main`, verified clean on a real Netlify deploy preview (zero console errors, full dashboard render).
- Explained the separate roster-change push-notification opt-in in SKS's `scripts/auth.js:1021-1126` (unrelated to the banner, confirmed NOT present in EQ Field — left untouched in both repos).

## Decided
- Royce: remove the What's new banner "completely" (not just disable) — applied to both repos for parity.
- Royce: commit → push → merge, confirmed explicitly and separately for each repo (SKS first, then EQ Field).

## Deferred
- None new this session — both repos' cleanup landed fully. eq/pending.md's existing "audit always-loaded boot scripts" item (2026-07-28) had `whatsnew.js` on its list; trimmed that reference since the file no longer exists — the other 4 scripts (recognitions.js, digest-settings.js, apprentice-widget.js, region-filter.js) remain open follow-up.

## Notes
- **Real git-mechanics incident, caught before it shipped:** rebasing the EQ Field branch onto `origin/main` hit the version-bump-line conflict pattern documented below; running `git commit --amend` while HEAD was still detached at the conflict-paused rebase amended `origin/main`'s own commit (PR #640, unrelated map-anchor fix, wrong author) instead of creating a new commit on top of it — silently squashing two unrelated releases together. Caught by noticing the resulting commit touched 8 files instead of the expected 6, not by trusting a green rebase. Fixed by resetting to the clean pre-rebase branch tip and redoing the integration as a `git merge` instead — fails safe to a normal push, no force/history-rewrite needed. A `--force-with-lease` attempt was also correctly blocked by the session's auto-mode classifier, which is what pushed toward the merge-instead-of-rebase fix in the first place.
- **New recurring collision pattern worth knowing about:** in both repos, when two concurrent branches each bump the SAME version-tracking lines (`APP_VERSION`, `sw.js` CACHE string, script `?v=` cache-bust tags) to the exact same next-integer version, a plain `git rebase`/`git merge` auto-resolves those lines as non-conflicts (identical text on both sides) — silently leaving two unrelated releases claiming the same version number. Not caught by git; only caught by manually diffing the touched-files list against what the change actually contains. This happened for real on EQ Field this session (PR #640 also claimed v3.5.446); resolved by manually renumbering to the next free version.
- EQ Field's `docs/reflection-log.md` has a hard commit-time gate (`reflection-gate` in `guard.js`) requiring a reflection entry staged in the same commit as any code change — not documented in the global session-gate rules, discovered live when the first `git commit --amend` attempt was blocked. SKS NSW Labour has no equivalent gate.

---

# Session 2026-08-04 — Canonical worker-link guard, roster keyboard nav, Prestart/Toolbox fixes, supervisor taxonomy + zaap parity (4 PRs), EQ-FIELD-10 closed out

## Built
- eq-field [PR #639](https://github.com/eq-solutions/eq-field/pull/639) merged — `_canonicalWorkerUpsert` could mint a second jvkn `workers` identity for a person who already had one (confirmed real: Phoenix Khatri, 2026-07-05). Added a name-only lookup as a guard (not an auto-link) — surfaces "possible duplicate" instead of silently creating one. Extracted the canonical-link helpers out of `people.js` into a new `scripts/people-canonical-link.js` (people.js had crossed its 1550-line CI ceiling with the guard added).
- eq-field [PR #642](https://github.com/eq-solutions/eq-field/pull/642) merged — Edit Roster's day-cell popover gained full keyboard nav (Arrow/Tab through pills + filtered site list, Enter to commit) and Monday now auto-fills Tue–Fri when those days are still blank (reuses existing `fillWeek()` exactly). Extracted into new `scripts/roster-cell-picker.js` (roster.js was pressed against its 2400-line ceiling).
- eq-field [PR #643](https://github.com/eq-solutions/eq-field/pull/643) merged — `exportPrestartDocx()`/`exportToolboxDocx()` had zero error handling across ~150-line async functions each; a thrown/rejected step surfaced as nothing (no toast, no visible error) — now wraps the build in try/catch, toasts on failure, reports to Sentry. Also found `saveToolboxDraft()` had no lock check at all — opening an already-submitted talk and clicking Save Draft silently flipped it back to `status='draft'`. Toolbox Talk now gets the same lock-on-submit Prestart already has (mirrors `_isPrestartLocked`/`_renderPrestartCrew` line-for-line).
- eq-field [PR #644](https://github.com/eq-solutions/eq-field/pull/644) merged — `managers.js`'s hardcoded supervisor-category taxonomy (`Management`/`Other`) had drifted from eq-shell's real `SUPERVISOR_CATEGORY_OPTIONS` (`Project Management`/`Operations`), verified by reading eq-shell's source directly — fixed to match exactly. Also: zaap (the `eq` tenant's canonical DB) never received eq-shell PR #692's migration — `app_data.staff` had no `is_supervisor`/`supervisor_role`/`supervisor_category` columns, `app_data.field_managers` was the pre-#692 view with no supervisor filter at all. New migration (`supabase/migrations/20260804_field_managers_zaap_canonical_parity.sql`) brings zaap to byte-for-byte parity with ehow's live shape (view, `security_invoker`, grants, digest trigger) — every fact read live off ehow via Supabase MCP before writing it, applied to zaap only (1 row total in `app_data.staff` today — disposable sandbox, low risk). Self-caught a security gap while applying it: `CREATE FUNCTION` grants EXECUTE to PUBLIC by default, so the new digest trigger function was briefly `anon`/`authenticated`-callable via `/rest/v1/rpc/` — caught via `get_advisors` immediately after applying, confirmed absent on ehow's real copy, revoked before considering it done.
- Sentry `EQ-FIELD-10` (`dashboard-map-css-collapsed`) investigated live and marked **resolved**. Every event pulled from Sentry predates one of two already-shipped fixes (PR #631/v3.5.438's tab-switch guard, v3.5.444's deliberate-collapse guard) — production is on v3.5.451, both fixes are live. Full write-up published as an artifact (`/gap` full-mode HTML) rather than left as chat text.

## Decided
- Royce: merge #639 and #642 (explicit, mid-session).
- Royce: add error-surfacing to the Word exports now, not wait for a live repro (explicit, via AskUserQuestion).
- Royce: give Toolbox Talk the same lock-on-submit Prestart has, not just a narrower guard (explicit, via AskUserQuestion — picked the fuller option).
- Royce: for the supervisor canonical drift, "just the findings for now" initially — then explicitly escalated to "continue towards fully tenant canonical," which is what authorized the zaap migration.
- Royce: merge #643 and #644 (explicit, separately).
- Read/write access to supervisor data *from* Field — explicitly NOT built. Existing design (`managers.js:17`, Shell-only writes) is deliberate; flagged as a real tension against Royce's "in a perfect world" aspiration rather than silently building a second write path.

## Deferred
- Someone (Royce, via Shell) needs to actually mark real `eq`-tenant people as supervisors — the DB now supports it, nobody is marked yet.
- 3 real SKS people (John Angangan, Scott Hotson, Jack Cluff) are `is_supervisor=true` in Shell but have no `supervisor_category` set — Shell-side data gap, not an eq-field fix.
- Field→canonical write access for supervisor data — needs an explicit decision, not assumed.
- Toolbox Talk inherits Prestart's existing "no reopen for correction" gap now that it's locked too — known tradeoff, not solved.
- `_dashMapWatchForVanish` watchdog removal (its own comment names the removal condition as arguably now met) — P3, revisit only if EQ-FIELD-10 stays quiet for real.

## Notes
- **Live, active multi-session collision risk, not a one-off.** Confirmed in the OTHER session's log above (PR #640 vs #641 version clash) and hit independently twice more in this session (#639/#642 both collided with concurrently-merging PRs on `main`, needing live re-sync + renumber each time — see `docs/reflection-log.md`'s 2026-08-04 entries in eq-field for the full blow-by-blow). Used `git merge` + live re-fetch before every push, never `git rebase`/force-push, per the git-mechanics incident logged in the session above.
- **Corrected my own earlier answer mid-session**: initially told Royce the Contacts save writes to Field's own `people` table, not canonical — this was wrong for the live SKS tenant. `people` is a JWT-routed table that rewrites to `app_data.field_people`, a view over canonical `app_data.staff` with an `INSTEAD OF` trigger — the profile fields already land in one canonical place. The actual gap was narrower: the `worker_id` *identity link* to a separate cross-app registry (jvkn `workers`), which is what PR #639 fixed.
- Full technical detail (file:line citations, live-verification steps, the 4-check reflection pass for each PR) lives in `eq-field/docs/reflection-log.md`'s 2026-08-04 entries — this log stays high-level by design.

---

# Session 2026-08-04 — EQ Ops jobs FK constraint landed, quotes-vs-jobs split decided against for now

## Built
- Deleted the one known orphan row in `app_data.jobs` (`job_id 7842b5ce…`, pointing at a hard-deleted quote) — direct data cleanup on ehow, not schema DDL. Confirmed zero dependent `job_notes` rows first, verified 0 orphans on both tenant planes afterward (zaap has 0 rows in `app_data.jobs` at all — no live Ops usage there).
- Migration 0237 adds `FOREIGN KEY (quote_id) REFERENCES app_data.quote(quote_id) ON DELETE RESTRICT` on `app_data.jobs`, idempotent via a `pg_constraint` existence check. `RESTRICT` (not `CASCADE`/`SET NULL`) deliberate — a job must never silently lose its quote link or get deleted as a side effect of the quote being removed. eq-shell [PR #1230](https://github.com/eq-solutions/eq-shell/pull/1230), merged, CI's own read-only "Plan" job confirmed clean before merge.
- Dispatched the migration — `1 applied, 249 skipped` on both `eq` and `sks`, live-verified via direct `pg_get_constraintdef` query on both planes (not just trusting the dispatch log).
- Ran `/decide` on "do we need the quote-vs-jobs Kanban split" (the original ask that opened this whole thread on 2026-08-03). Full six-step pass, recommendation: **not now**. Both problems that motivated it — Open column density and `job_number` reliability for Field — are already solved by cheaper, already-shipped changes (collapsed-customer-groups + the full sync-gap/backfill/FK chain). Crux to revisit: if the Open column still feels crowded with groups collapsed, or job-specific features (costing, PO dashboards) start needing a shape the current single board can't express.

## Decided
- "delete the orphan job row, add the FK constraint" — explicit go, both actioned same session.
- "dispatch it" — migration 0237 dispatched to production, live-verified on both planes.
- `/decide` outcome (quotes-vs-jobs split: not now) — a recommendation, not yet explicitly confirmed by Royce; logged as the current state of the decision, open to revisiting per the stated crux.

## Deferred
- The quotes-vs-jobs Kanban split itself — deliberately not built, per the `/decide` call above. Not closed, just not active.
- 7 quotes in `submitted` status still missing a follow-up date (unrelated pre-existing gap, carried from 2026-08-03).

## Notes
- This closes out every remaining open item from the 2026-08-03 EQ Ops board→job sync thread except the split itself (now explicitly deferred with a reasoned crux, not just "not started") and the unrelated follow-up-date gap.
- `TODAY.md` GOALS is unset — the `/decide` pass said so plainly rather than borrowing urgency from an old file, per the decision protocol's own rule.

---

# Session 2026-08-04 — "Where did the prestarts go" — no data loss found, real truncation risk fixed instead

## Built
- Root-caused Royce's "check where all the prestarts went" against the live SKS database (`nspbmirochztcjijmcrx`) rather than the code first: 138 prestarts intact, none deleted (checked `audit_log`), all on one `org_id`, none with a null `briefing_date`, RLS's `prestarts_select_tenant` is `USING (true)` so reads aren't blocked. No data loss occurred.
- Actual cause was two UI windows stacking: the Prestart tab hardcodes "today + past 7 days" (`renderPrestart()`, `scripts/safety.js`), and the Records tab defaults to a 30-day range — both by design, and Records already has a working "All" option that shows every row.
- While tracing the render path, found the real live risk: `loadPrestarts()`/`loadToolboxTalks()`/`loadIncidents()` each fetched a flat `&limit=200` newest-first. This is exactly the item flagged and left open on 2026-07-10 ("recency caps... if older history genuinely needs to be reachable, that's a pagination-UI or date-filter feature to design, not a copy of `sbFetchAll()`") — except the copy turned out to be the right fix once a genuine truncation edge was found, not a UI feature. At SKS's current ~25 prestarts/week against 138 total, the 200 cap was ~2-3 weeks from silently dropping the OLDEST rows — invisible even on Records → All, no error, reads exactly like data loss when it eventually hits.
- Fixed: all three loaders swapped to `sbFetchAll()` (`scripts/supabase.js`) — same call shape already proven live in `leave.js:163` and `timesheets.js:158`. `scripts/safety.js` v3.10.108 → v3.10.109, version bumped across `app-state.js`/`sw.js`/`index.html`/`CHANGELOG.md` per the standard 4-file SKS convention.

## Decided
- Royce, via AskUserQuestion: "fix the cap only" — declined both widening the Prestart tab's 7-day window and leaving the cap unfixed with just a Records-default change.

## Deferred
- **The fix itself is not shipped.** Staged uncommitted on worktree branch `claude/missing-prestarts-777d49` in `sks-nsw-labour` — no commit, no push, no PR. Per the non-negotiables (never deploy without explicit instruction), waiting on Royce's go before it goes anywhere near `main`.
- Widening the Prestart tab past 7 days, or a "Show older → Records" link — explicitly declined this session, still open if Royce wants it later.

## Notes
- **Verification chain, not just a code read:** live DB row/date/org counts → RLS policy text → `audit_log` deletion sweep → client fetch/render trace → `node --check` on all 4 edited files → static-server boot showing the new version chip with no new console errors → in-page confirmation that `sbFetchAll`'s explicit-order guard (the exact trap behind the v3.10.90–92 outage) cannot fire on any of the three new call sites, since each already carries its own `order=`.
- Session gate (`/brief`) was enforced mid-task by a repo commit hook — the first `Edit` attempt was blocked with "brief not run this session," which is what triggered running `/brief sks-nsw-labour` before continuing. Per-session flag file, not per-day; written to `C:\Users\EQ\AppData\Local\Temp\eq-brief-2026-08-04-aee72100-20a6-4767-93f0-055341e682e1.flag`.
- Closes the loop on the 2026-07-10 pagination-sweep deferred item for `prestarts`/`toolbox_talks`; `audit_log`'s own recency cap was named in that same item and is still unaudited — not touched this session.

---

# Session 2026-08-04 — Toolbox Talk "Reopen" fix, plus a steelman on Field write access

## Built
- eq-field [PR #645](https://github.com/eq-solutions/eq-field/pull/645) merged — Toolbox Talk gained a "Reopen" button on a submitted talk, permission-gated to manager/supervisor (same `reports.toolbox.submit` permission Submit itself requires — confirmed live in `permission-matrix.js`, not assumed). Persists the un-lock immediately (audited via the normal audit log) rather than a local-only flag. Deliberately leaves existing attendee signatures untouched — the confirm dialog says so plainly, since detecting which fields changed to know which signatures to invalidate would be a materially bigger feature.

## Decided
- Royce: merge #645 (explicit).
- Royce, on the 3 real SKS people missing a supervisor category: fix it in Core directly, not a database patch from Field — "why isn't this chosen at core level? we need to create synergy here." Correctly read as reinforcing the single-write-path design, not approving a one-off SQL patch.
- Royce: leave the `eq` sandbox tenant's Supervision list empty — its whole staff table is 1 row, not worth flagging.
- Steelman delivered on "should Field ever write supervisor data" — no decision made, still open. Real case for: friction lands on the person on-site rather than in the office, Field already owns adjacent operational facts (licences/dispatch), today's own audit showed Shell's data was the stale one. Real case against: two write paths on one record is exactly what caused this session's earlier duplicate-identity bug. If it's ever built, the safe shape is Field calling Shell's own `entity-patch` RPC, never a second raw-table write.

## Deferred
- The 3 SKS people's supervisor_category — still not set, Royce to do it in Core.
- **New root-cause finding, not yet actioned**: Shell's own Staff form lets you tick "Supervisor" without requiring a category (`eq-shell/src/pages/staff/SplitPanel.tsx:298-307` — the dropdown only appears once the checkbox is on, nothing enforces filling it in). Likely why the 3 people above ended up incomplete. Asked Royce whether to flag as a Shell-side follow-up (make the field required); no answer yet.
- Field write-access decision itself — steelmanned, not decided.

## Notes
- Session resumed after the first `/close` of the day; the brief-gate flag had already been cleared by that close, so the first `Edit` this round was blocked again ("brief not run this session") until `/brief eq-field` re-ran — expected per-session (not per-day) behaviour, not a bug.
- Discovered mid-session that the working branch was `claude/supervisor-category-taxonomy` — already squash-merged to `main` as a different commit hash. Switched to a fresh branch off `origin/main` (`claude/toolbox-reopen-for-correction`) rather than continuing on the stale local branch, avoiding a repeat of the earlier version-collision/duplicate-history class of mistake from the prior session block.

---

# Session 2026-08-04 — Agentic-coding rules landed (PR #125); pre-commit hook gains a status-enum guard (PR #126, F8); a repo-wide hook-wiring gap found and fixed

Substrate-upgrade session, prompted by an external review arguing that with agents, building is no longer the constraint — maintainability, multi-tenancy reliability, and verification are.

## Built

- **`rules/agentic-coding.md` (new)** — session gate, verification-beats-inspection, maintainability under iterative agent edits, multi-tenancy constraint, effort threshold, search hygiene. Rule 0.6 (session gate) and the Workflow Threshold table migrated in from the non-versioned `C:\Projects\CLAUDE.md`, which only a session rooted at `C:\Projects` ever read — those rules never reached Chat/ChatGPT/Grok. That file's copies are now pointers.
- **`rules/non-negotiables.md` #11 (new)** — every service-role/admin-client query MUST carry an explicit tenant filter. Designed 2026-04-27, told to live here, never landed until now.
- **`rules/stack.md`** — EQ Cards Flutter/Dart exception added, widget-test suite named as the gate.
- **`rules/deployment.md`** — Supabase section rewritten off a stale pre-Control-Layer model that still named a deleted project and a dead Netlify URL as live, in a `read_priority: critical` file.
- **`system/lessons.md`** — appended, generalizing three real cases where a clean analyzer/review/merge each certified something that didn't work.
- **PR [#125](https://github.com/eq-solutions/eq-context/pull/125) merged.** Along the way, found and fixed a pre-existing break: `main`'s Frontmatter validation had been red since `092466d` (prose in a `status:` field), so every branch cut from main inherited a failing required check.
- **`.githooks/pre-commit` check 17.5 (new)** — blocks any governed `.md` whose `status:` is outside `live|draft|archived|deprecated` or missing entirely, exemption list held identical to CI's. `system/failures.md` F8 added at rung 4 (recurrences: 2 — the same defect had landed on main twice in four days).
- **Found while building it: the governed hook had never run on this machine.** `core.hooksPath` pointed at `.git/hooks`, which held an untracked but load-bearing secret-scanning hook (GitHub PATs, Supabase JWTs, AWS/Stripe/Anthropic keys), byte-identical to the tracked `scripts/pre-commit-secrets.sh`. Simply repointing to `.githooks` — what `install-hooks.ps1` did — would have silently traded credential-leak protection for style checking. Fixed by delegation: the governed hook now runs the secret guard first and refuses to commit if it's missing.
- **PR [#126](https://github.com/eq-solutions/eq-context/pull/126) merged**, after a 7/7 adversarial test in a throwaway repo (real defect blocked, all 4 valid enum values pass, exempt paths skipped, missing key blocked, planted secret still blocked through the delegation, fail-closed when the guard script is absent, clean commit succeeds).

## Decided

- Royce: run `/decide` before building the rules changes, then build — done, six-step pass recorded inline in chat.
- Royce: migrate Rule 0.6 + Workflow Threshold into governed eq-context rather than leaving them in the ungoverned umbrella file.
- Royce: merge #125, merge #126, add the status-enum pre-commit check — each explicit, separately.
- Royce: delete the shadowed `.git/hooks/pre-commit` — **not done**, see Notes.

## Deferred

Four items logged in `eq/pending.md` under "eq-context: agentic-coding rules landed", dated 2026-08-04: no audit yet of existing code against the new tenant rule; the rest of the umbrella `CLAUDE.md` still ungoverned; two deploy tables disagreeing on EQ Service's URL; and the `.git/hooks/pre-commit` deletion itself, held.

## Notes

- **The delete instruction surfaced a real risk and was not executed as literally asked.** Repointed all 5 open worktrees' `core.hooksPath` to `.githooks` per Royce's chosen option ("repoint all 5, then delete"), then checked each one's actual file content before deleting anything — 4 of the 5 (`agent-af31fd71dc13a91c7`, `silly-noether-ec8a81`, `skills-list-html-908d61`, `eq-context-reflection-protocol-wt`) are on branches that predate today's secret-guard delegation, so their `.githooks/pre-commit` has zero secret-scanning in it. Repointing them would have quietly removed their only secret guard — the identical failure mode this whole thread was fixing, recreated by executing the fix. Reverted those 4 back to `.git/hooks` immediately (no window with zero protection); only this session's own worktree, which carries the merged fix, stays on `.githooks`. `.git/hooks/pre-commit` was not deleted. Resolves naturally once those 4 branches merge/rebase past `main`.
- **Main moved twice more under this session** (`6a9783d` → `b57e422`+`6d378fd` → `1059f85` → `b9167e1`), each a different concurrent session's own close. Confirmed via `git log`/`git show` each time rather than assumed; no collisions on files this session touched.
- Not touched: SEC-1, SEC-9 (both still open in digest **Needs you**), or the `EQ-FIELD-10` Sentry item logged resolved earlier today. `TODAY.md` goals remain UNSET — no deadline was invented or borrowed.

---

# Session 2026-08-04 — Shell Staff-form UX gap flagged as a follow-up task (no code changed in eq-field)

## Built
- Nothing shipped in eq-field this session — this was a short follow-up on the earlier Toolbox Talk / steelman session's open question ("flag Shell's category-not-required gap as a follow-up?").

## Decided
- Royce: flag the Shell Staff-form gap (`SplitPanel.tsx:292-307` — Supervisor checkbox doesn't require Supervision category) as a follow-up task. Spawned as a background task chip against `eq-shell`; Royce started it immediately in its own session — in progress as of this close, not yet merged.
- Royce: bundle a second, related UX nit into the same follow-up — the Company field (labour-hire agency name) shows for Direct employees too, where it's meaningless. Since the first task had already been started in a separate session (can't be edited in place), spawned a second companion chip pointed at the same file/component with an explicit instruction to combine into the same PR if that session is still open when picked up.

## Deferred
- Both eq-shell fixes (category-required, hide-Company-for-Direct) — running independently in separate local sessions, outcome unknown at this close.
- The 3 SKS people's `supervisor_category` — still Royce's to fix in Core, unchanged this session.

## Notes
- Neither task chip could be *edited* after the user started it (chips become independent sessions once launched) — the only way to "bundle" a second ask was a new companion chip cross-referencing the first, with instructions to merge into the same PR if still open. Worth remembering: bundling only works cleanly if requested *before* the first chip is started.

---

# Session 2026-08-04 — Prestart fix shipped: committed, pushed, PR'd, merged, live (v3.10.109)

## Built
- Follow-up to this same day's earlier "Where did the prestarts go" session (line 76 above), which left the fix staged and uncommitted pending Royce's go. Royce gave that go this session: committed the 5-file diff (`d560688`), pushed branch `claude/missing-prestarts-777d49`, opened [PR #76](https://github.com/eq-solutions/sks-nsw-labour/pull/76).
- Royce then separately said "merge the PR" — squash-merged (`7ea82a9`) after confirming `mergeStateStatus: CLEAN` and all checks green. Confirmed live via the Netlify project reader: deploy `6a71b52f...`, state `ready`, marked current on `sks-nsw-labour.netlify.app`.
- Summarized the shipped change for Royce in plain terms on request ("what changed").

## Decided
- Royce, explicit and separate instructions: "commit and push the prestart fix", then later "merge the PR". Treated as two distinct authorizations per the non-negotiables (commit/push ≠ merge-to-main/deploy), not assumed from the first.

## Deferred
- Nothing new. The one remaining open item from the earlier session (widen the Prestart tab past 7 days, or add a Records link) is still open — untouched, unchanged.

## Notes
- **GitHub App token gap, hit twice:** both `mcp__github__create_pull_request` and `mcp__github__merge_pull_request` 403'd with "Resource not accessible by integration" against `eq-solutions/sks-nsw-labour`. Fell back to `gh` CLI both times, which worked cleanly. Worth checking the App's installed permissions for this repo if this keeps happening — PR create/merge should be routine, not a fallback path.
- **Repo guard caught something real:** the first `gh pr create` attempt was blocked by `block-sks-live` for including the live Supabase project ref (`nspbmirochztcjijmcrx`) directly in the PR body text. Rewrote the body to say "live database" instead and it went through clean. Good catch by the guard — worth remembering not to paste raw project IDs into PR/issue text on this repo even when the content itself isn't secret.
- Closed the loop cleanly: pending.md's "not committed, not pushed" item and the changelog's "STAGED, not yet pushed/deployed" line were both stale the moment this session started — updated both to reflect merged+live rather than leaving two contradictory states in the substrate.

---

# Session 2026-08-04 — eq-shell Staff-form fix shipped: Company field hidden for Direct, Supervision category required (PR #1237, merged); migration for the 3 SKS people declined in favour of the UI path

## Built
- Picked up the "hide Company for Direct employees" task chip (companion to the earlier-flagged "require Supervision category" task from the session at line 155 above). Confirmed the category-required work wasn't actually in progress anywhere (no branch, no worktree) despite the earlier session's note — bundled both into one change per the original instruction.
- `src/pages/staff/SplitPanel.tsx`: Company field now only renders in edit mode when Employment type ≠ Direct (Apprentice/Labour Hire/Subcontractor all keep it — a prior fix, `eb331799`, deliberately made Company apply to Apprentices too, for GTO placements like NECA). Existing `agency` values on Direct records are untouched, only the input stops rendering. Save is now blocked with an inline error ("Select a supervision category") if Supervisor is checked but no category is picked — closes the exact gap that let the 3 SKS people below end up incomplete.
- eq-shell [PR #1237](https://github.com/eq-solutions/eq-shell/pull/1237) opened off a fresh branch from `main` (the session's working branch was mid-way through an unrelated feature), `pnpm run build` clean, all 4 required checks green, merged (`cce8834`) and branch deleted.
- Live-checked ehow before acting on the "3 SKS people" deferred item rather than trusting the digest: Scott Hotson already has his category set (`Project Management`, job title `Project Manager`) — that digest note was stale. Only John Angangan and Jack Cluff still have `supervisor_category`/`supervisor_role`/`job_title` all null.
- Asked to "dispatch the migration to update ehow for the 3 SKS people" — surfaced the conflict with Royce's own earlier recorded call (fix it in Core, not a database patch) before writing anything, and that a category assignment is a real business call, not something inferable from the data. Declined the migration per Royce's choice; spawned follow-up task `task_99cb6058` (Shell UI edit for both, category "Supervisor" for each, confirmed via AskUserQuestion) — Royce started it in a separate session immediately, still running as of this close.

## Decided
- Royce, via AskUserQuestion: bundle both eq-shell fixes into one PR on a fresh branch off `main`, not two separate PRs.
- Royce: merge #1237 (explicit).
- Royce, via AskUserQuestion: set both Angangan's and Cluff's category to "Supervisor", via the Shell UI once #1237 deploys — not a migration.

## Deferred
- John Angangan and Jack Cluff's Supervision category — not yet set; `task_99cb6058` is the tracked follow-up, running independently as of this close.
- Whether EQ Field should ever write supervisor data itself, and the multi-session `main`-collision risk — both carried forward unchanged from the two sessions above, not touched this session.

## Notes
- The prior session's log (line 161 above) said the category-required task was "in progress" in a separate session — live-checked (branch list + worktree list, no matches) before starting and found that was never actually true; proceeded to build it directly rather than trusting the stale claim, per the same verify-before-acting discipline as the ehow check above.
- `gh pr merge --squash --delete-branch` failed locally with a worktree conflict (`main` checked out elsewhere) even though the merge itself succeeded on GitHub — confirmed via `gh pr view` before treating it as done, then deleted the remote branch separately. Worth knowing this failure mode doesn't mean the merge didn't happen.

---

# Session 2026-08-04 — eq-shell Staff-form fix built and shipped: supervisor_category required client- and server-side (PR #1236, merged); caught and resolved a live duplicate-work collision with a second concurrent session (PR #1237)

## Built
- Added client-side validation to `src/pages/staff/SplitPanel.tsx`'s Staff editor: checking Supervisor with no category selected now blocks Save with an inline error, matching the existing validate-then-setError pattern already used in `AddToRosterModal.tsx`.
- Added a server-side guard to `netlify/functions/entity-patch.ts`: any patch that sets `is_supervisor: true` without a non-empty `supervisor_category` in the same request is rejected (400). This matters beyond "don't trust the client" — traced a second, separate write path (`StaffPage.tsx`'s inline roster-row Supervisor checkbox, `handleQuickToggle`) that PATCHes the same endpoint with no category field in its own UI at all, so the client fix alone would not have stopped new bad records from that path. The rejected quick-toggle fails gracefully (plain-English toast, checkbox reverts on its own since it reads live query data, not local state) — no UI plumbing needed there.
- Confirmed `SUPERVISOR_CATEGORY_OPTIONS` (`Supervisor / Project Management / Operations / Executive / Internal`) already matches the taxonomy EQ Field expects — no drift to fix.
- Branched fresh (`claude/staff-supervisor-category-required`) off `origin/main` rather than continuing on the session's original branch, which was 60+ commits stale and for an unrelated feature. `pnpm run build` (tsc -b + vite) and eslint clean on both changed files. Committed, pushed, opened eq-shell [PR #1236](https://github.com/eq-solutions/eq-shell/pull/1236). CI green, merged → `6808f8e4`.
- Mid-task, the shared `C:\Projects\eq-shell` checkout's active branch changed out from under this session (a routine `git status` check surfaced it) — a second, concurrent Claude session sharing the same root checkout had independently built a near-identical fix in the same file/function, bundled with an unrelated change (hide the Company field for Direct employees), already open as [PR #1237](https://github.com/eq-solutions/eq-shell/pull/1237).
- Rather than editing that branch directly (it wasn't this session's, and the other session's local state shouldn't be disturbed), rebased #1237 onto the post-merge `main` inside an isolated `git worktree` on a temporary local branch name tracking the remote ref — never checked out `claude/staff-company-field-visibility` in the shared root. The duplicate `handleSave` validation hunk conflicted as expected and was resolved by keeping the already-merged version; the Company-field-visibility change survived untouched. Force-pushed with `--force-with-lease` (safe — no one else had pushed to that ref in the interim), then deleted the temp worktree/branch. CI green, merged → `cce8834`.
- Corrected `eq/pending.md`'s existing writeup of this bug (added by the other session's own later `/close`, in the session block directly above this one): it attributed the "Save is now blocked" validation logic to PR #1237. Ground truth from performing the rebase directly: #1236 is the actual source of both the client and server-side fix; #1237 post-rebase carries only the Company-field-visibility change. Fixed the attribution and added a note on the collision itself.

## Decided
- Royce: commit the fix (explicit).
- Royce: push the branch and open the PR (explicit).
- Royce, via AskUserQuestion, on discovering the #1236/#1237 collision: merge #1236 first, then rebase #1237 on top (over the alternatives of merging #1237 first, or handling it manually).
- Royce: watch CI on #1237 and merge it (explicit).

## Deferred
- Setting John Angangan's and Jack Cluff's Supervision category — explicitly out of scope for this task from the start (a manual data-entry fix, not a code fix); tracked separately as `task_99cb6058` per the session block above.
- Whether the collision pattern (two Claude sessions racing on the same shared checkout, now confirmed in both eq-field and eq-shell the same day) needs a structural fix — worktree-per-session enforcement, a branch-claim lock, something else — is Royce's call, flagged in `pending.md`, not decided here.

## Notes
- This was a real collision, not a hypothetical one: two sessions, same repo checkout, same file, same function, within minutes of each other, each unaware of the other until a routine git-status check surfaced it. No data was lost and neither session's working directory was disturbed by the other — the isolated-worktree rebase technique (temp local branch tracking the remote ref, never touching the shared root's checked-out branch) is a clean, reusable pattern for this if it recurs.
- Live click-through of the new validation (both the edit-panel form and the inline table checkbox) was not performed — needs `netlify dev` plus a real signed-in session, which digest.md already notes is off-limits for Claude to do on Royce's behalf. Worth a quick manual check before relying on this fully.

---

# Session 2026-08-04 — eq-field PWA "Add to Home Screen" identity fixed: stale SKS colour + EQ was silently getting SKS's branding

## Built
- eq-field [PR #647](https://github.com/eq-solutions/eq-field/pull/647) merged (v3.5.452 → v3.5.453) — asked to review a 2026-08-03 flag ("app icon colour may still be the old stale SKS navy"). Confirmed real: `manifest.json`'s `theme_color` was `#1F335C`, the exact stale hex CLAUDE.md's brand table already documents as fixed elsewhere (transactional emails, 2026-07-30). Corrected to `#203060`.
- Found a wider gap while checking it: the whole PWA manifest (name, icons, colour) was static and SKS-branded regardless of tenant — an EQ-tenant "Add to Home Screen" install picked up SKS's identity entirely. The browser-tab favicon has been tenant-aware since v3.4.30 (JS-injected by hostname); the manifest never got the same treatment. Reported the wider finding to Royce before building rather than silently narrowing to just the colour.
- Fix: new `manifest-eq.json` (EQ name/icons/colour), and the existing tenant-detection script in `index.html` now also overrides the manifest `<link>`, `<meta name="theme-color">`, and `apple-mobile-web-app-title` when the SKS host is detected — static markup already defaulted correctly to EQ, so only the SKS branch needed patching. `sw.js` CACHE bump, `manifest-eq.json` added to PRECACHE.
- Verified on the deploy preview for both tenants before merge: EQ tenant confirmed directly (manifest-eq.json, EQ sky theme colour, correct favicons, zero console errors); SKS branch confirmed by exercising the same override logic directly in the console, since no deploy-preview hostname contains "sks" to trigger it naturally — same limitation the pre-existing favicon logic already has, stated plainly as not fully end-to-end verified rather than implied covered.
- Also confirmed, while closing this session, that the two eq-shell follow-ups flagged earlier today (supervisor-category-required, hide-Company-field-for-Direct) both merged — see the session block above for the collision + rebase story.

## Decided
- Royce, via AskUserQuestion: fix both the colour and the wider tenant-identity gap, not colour-only.
- Royce: merge #647 (explicit).

## Deferred
- `sw.js`'s `CACHE_FIRST_PATHS` only lists `/icons/` and `/manifest.json` (SKS), not the EQ equivalents — a pre-existing asymmetry noticed in passing (SKS assets are cache-first/faster-but-staler, EQ assets are always network-first/fresh). Left alone deliberately — it's a caching tradeoff decision, not a stale-value correction. Logged in `pending.md`.

## Notes
- Branched fresh off `origin/main` for this work rather than continuing on the prior session's now-stale `claude/toolbox-reopen-for-correction` (already squash-merged under a different SHA) — same stale-branch trap named earlier today, avoided this time by checking `git log HEAD..origin/main` before starting.
- One real merge conflict during `git merge origin/main` (not rebase, per this repo's established convention) — `docs/reflection-log.md`, append-only file, two sessions' entries landing at the same tail. Resolved by keeping both, ordered by merge order; confirmed via `git diff` that the merge itself touched none of this session's own files.
