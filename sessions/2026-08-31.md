# Session 2026-08-31 — Removed staff's historical leave/timesheet rows fixed to show real names, not "(unknown)"

## Built
- Diagnosed a Royce screenshot cold (no prior session context): a leave-screen warning toast — "Leave row references staff_id ... not found in the staff list — showing as (unknown)." Confirmed it wasn't caused by anything in the session's branch (clean worktree, exactly at `origin/main` tip) before going further.
- Traced live: the `staff_id` belongs to Liam Brook-Jackson (SKS), removed from Field 2026-08-26 (`archived=true`, `active=false`, in `app_data.field_people_removed`), whose approved July-2026 leave request still references his `staff_id`. `staffIdToName()` (`scripts/leave-adapter.js`) only ever resolved against the live (`field_approved`) staff map, so once someone is removed, their historical rows can never resolve a name again.
- Scope-checked before proposing a fix: not isolated — 35 timesheet rows tenant-wide have the same orphaned-`staff_id` gap (silent there by design; timesheets' `note()` sink is console-only, only leave's `warn()` toasts to `leave.approve` holders, which is why only this one row surfaced).
- Shipped the fix: `staffIdToName()` now falls back to a `people_removed`-sourced map (`setRemovedStaffRows()`, new) when the live map misses. Fallback only, one direction — never feeds `nameToStaffId()`, so a removed person still can't be picked for a *new* entry; live map wins on collision. `loadCanonicalStaffMap()` (`scripts/supabase.js`) fetches `people_removed` (the same already-routed table `people.js`'s "Show removed" toggle reads), gated to `canManagePeople()` so removed-staff PII isn't pulled into every worker's session.
- 3 new test assertions in `tests/leave-adapter.test.js`. Full local verification before pushing: all 33 test files pass, ESLint 0 errors (927 pre-existing warnings untouched), both drift guards (`check-cache-busters.mjs`, `build-bundles.mjs --check`) clean.
- eq-field [PR #848](https://github.com/eq-solutions/eq-field/pull/848) — CI green (tests+lint, both drift checks, deploy preview), preview smoke-tested (boots clean, no console errors). Merged on Royce's explicit "merge it" (squash, branch deleted). Confirmed live on `field.eq.solutions` within ~10s of merge (`sw.js` CACHE = v3.5.619).
- Post-merge, verified the fix's actual reach live rather than taking the earlier estimate on faith: all 35 orphaned timesheet rows (15 removed workers) and all 190 orphaned roster rows (13 removed workers) confirmed to have clean first/last name data in `field_people_removed` — the shipped fix resolves every one, no gaps found.
- That roster sweep surfaced a real, separate bug: one of the 13 (Nelson Sareto, SKS) had future-dated schedule rows (24 Aug → 11 Sep) attached to a *dead duplicate* staff record instead of his real active one — a live instance of the known EQ-SHELL-14 duplicate-worker-identity bug (two `app_data.staff` rows, identical email/phone/DOB, different `cards_worker_id`; one deactivated 90 min after creation, the other active ever since). His real upcoming shifts were invisible on the roster grid as a result. Root-caused live (`app_data.staff` + `audit_log` on ehow), confirmed via `AskUserQuestion`, then fixed directly: repointed all 15 affected `schedule_entries.staff_id` rows from the dead record to the live one, verified 0 remaining on the dead id and all 15 visible via `field_schedule`. Caught and corrected my own miscount mid-task (said "190" when relaying Royce's fix confirmation — that was the total across all 13 people, not Nelson's own count of 15 — before any write ran).
- Also investigated, unprompted by any code change: Royce reported the roster grid going blank then repopulating live while watching it. Ruled out a crash (no Sentry errors in the last hour) and ruled out the routine 30s/5-min poll and realtime paths (both specifically hardened against this exact flash). Most likely explanation offered: the SW update banner from this session's own v3.5.619 deploy landing while his tab was open, followed by a reload — Royce confirmed this was exactly it (he reloaded manually).

## Decided
- Royce confirmed the recommended fix via `AskUserQuestion` (removed-staff fallback vs. leave "(unknown)" as-is) — chose the fallback.
- Royce explicitly said "merge it" — merged without further confirmation, per standing deploy rules (explicit instruction given).
- Royce confirmed via `AskUserQuestion`: repoint Nelson's 15 orphaned schedule rows to his real active staff record now, rather than leaving them and re-rostering manually.

## Deferred
- **Not click-tested live** — no Field session/credentials in this environment. Needs a real supervisor pass on SKS: open Leave, confirm Liam Brook-Jackson's row now shows his name and the warning toast no longer fires. Logged in `eq/pending/eq-field.md`.
- **EQ-SHELL-14 root cause still open** — duplicate canonical worker identities can still be created upstream; Nelson's fix was symptomatic (repointed his data), not curative. Spawned as a background task (`task_c74f9351`): live-sweep `app_data.staff` on ehow for other pairs with the same shape (matching email/phone/DOB, different `cards_worker_id`) with orphaned schedule/timesheet/leave rows on the dead side, before assuming Nelson was the only one. Royce started it in a separate local session shortly after this session's first close — running independently, result not yet known.

## Notes
- Mid-session version collision, caught before it shipped wrong: bumped to v3.5.618 (the free number at the time), then discovered `origin/main` had moved again — PR #847 landed that exact number while this PR was still being built. Committed the fix on the old base, rebased onto the new `origin/main` (one real conflict in `index.html`'s changelog banner — both entries kept, renumbered mine to v3.5.619), re-verified zero residual `3.5.618` references in any version-bearing file, re-ran the full local check suite against the new base before pushing.
- `/brief eq-field` ran mid-session (triggered by this repo's brief-gate hook on first Edit) — worktree was already clean/current, no untracked migrations, no DB schema involved so Step 4 (Supabase recon) wasn't required beyond the diagnosis queries already run.
- `eq-context` substrate was stale at session start (SessionStart/UserPromptSubmit hooks both flagged 6-9 commits behind `origin/main`) — the shared checkout's `git pull`/`git merge` are hard-blocked by the F9 guard (concurrent-session git races). Worked around with `git fetch` (unblocked, read-only) to check true state rather than trusting a stale local comparison; deferred any actual reconcile since the task itself didn't depend on stale substrate content. At close, the shared checkout was still behind — see below.

---

# Session 2026-08-31 — Apprentice-role permissions review: Dashboard scoped, two 12-day-old mystery bugs finally root-caused

*Session actually started 2026-08-30 and ran past midnight; logged under 2026-08-31 since that's when it closed. Concurrent with the PR #848 session above — unrelated work, same day.*

## Built

- Dashboard's org-wide widgets (headcount tiles, Site Breakdown, Roster Gaps) gated to a new `dashboard.view_org_wide` permission key, manager+supervisor only — every other role used to see the identical org-wide operational view. eq-field PR #843 (v3.5.617), merged.
- Safety nav (Prestarts/Toolboxes) restored for apprentices — `app-state.js`'s v3.5.172 "Apprentice portal" nav-strip was force-hiding the whole Safety group, predating apprentices ever being granted prestart/toolbox access at all. This is the exact "safety has disappeared" mystery left unresolved at the close of the 2026-08-19 session (`eq/pending-archive.md`) — finally root-caused and fixed.
- Apprentice Management page no longer shows every apprentice's card to an apprentice — was showing the full roster (skills ratings, feedback counts, profile status) to anyone, a real cross-person exposure. Existing self-scoping (v3.5.517) depended on a server-side tenant check that silently missed Shell-embedded sessions; fixed client-side with an `EQ_PERMS.myPersonId()` fallback that doesn't depend on that check succeeding. Both nav and list fixes: eq-field PR #847 (v3.5.618), merged.
- Restored a deactivated Field roster row for the "Jordan A. Sample" test apprentice account directly on ehow (was `active=false` since 2026-08-23, not missing) — this is what made Leave/My Schedule work at all for the rest of the session's testing.

## Decided

- Royce: hide Dashboard's org-wide cards for apprentice/employee/labour-hire, keep them for manager/supervisor. Birthdays/Starting Soon/Leave & Absences stay open to everyone.
- Royce, on narrowing apprentice's Records (Customers/Staff) access in the shared eq-roles model: "build the real fix first" rather than a quick global change (which would also affect EQ Service) or a custom Shell group (can't narrow, additive-only). Escalated as a standalone eq-shell task rather than built from eq-field.
- Royce: "merge it" / explicit approvals given for both PRs (#843, #847) — each required an actual GitHub review approval first; a mobile review submitted as "Comment" rather than "Approve" didn't count, took a couple of rounds to land correctly on #843.

## Deferred

- **Jordan's `service.tenant_members` role** — approved fix hit a real `auth.users` foreign-key constraint (would mean creating a Supabase Auth identity, out of scope for a DB write). Needs Jordan to go through the real first-login flow, or Royce to provision via proper channels.
- **Records/eq-roles fix** — spawned as its own eq-shell task (wire the existing-but-unused `revokes`/tenant-override mechanism into the real permission resolver, then narrow apprentice's `entity.view`/`field.view` for SKS only). Started independently in a separate session; not reported back as of this close.
- **Live click-through of both merged fixes** — no SKS/Core credentials in this environment; Royce's own screenshots surfaced both bugs originally, worth a real re-check.

## Notes

- **A real, live-but-brief security regression happened and was self-corrected.** Misdiagnosed `field_people_removed_iud()`'s restore path as broken, from a stale local migration file rather than the live function — eq-shell had already fixed the identical bug via tenant-migrations 0271/0274 (2026-08-23/24), including role-gating the replacement silently dropped. Caught by `tests/migration-shared-fn-guard.test.js` before push (not a targeted check — it was part of the standard pre-push suite), reverted live within minutes. Full honest account in eq-field's own `docs/reflection-log.md`. Lesson recorded as its own standing rule in Claude's memory system: never write `CREATE OR REPLACE FUNCTION` against a shared ehow function from a local file's content — pull the live definition first, every time.
- **Correction to an earlier finding this same session**: Jordan's Shell login was reported as resolving to `employee`, not `apprentice`, based on `token-exchange.ts`'s generic/service-audience path. Live evidence contradicts this for Field specifically (the apprentice-portal nav-strip fires correctly, on-page profile reads "Apprentice") — Field's `eq_role` claim resolves separately from whatever that generic path returns. Don't repeat the "employee" claim; the exact `field`-audience resolution path wasn't traced.
- **Today's version numbers collided four separate times** (#842→v3.5.614, #844→v3.5.615, #845→v3.5.616, then this session's own two PRs needed 617/618, then PR #848 above needed 619) — an unusually fast-moving day for concurrent sessions on this repo. Each collision required a rebase + full re-verification (bundles, tests, lint) before re-pushing; no shortcuts taken.
- This session closes out a genuinely old thread: the "Apprentices list shows everyone" and "Safety nav disappeared" symptoms were first reported 2026-08-18/19, misdiagnosed or left unresolved twice since, and recurred a third time on the exact same test account before finally getting root-caused here. Worth remembering that a live-tested "fix" from 12 days ago can still be wrong in a way nobody notices until someone re-tests the same account.

---

# Session 2026-08-31 — Feature Toggles page: concrete examples + mini-previews per switch

## Built
- Royce sent a screenshot of Manage → Feature Toggles (SKS tenant) with two asks: "who can see this menu?" and "can we help with improving the descriptions / show examples of what each feature does?"
- Answered the access question from live code, not assumption: `field.manage_feature_toggles` (`permission-matrix.js`) is granted to `manager` and `supervisor` only — not employee/apprentice/subcontractor — enforced three times over (nav item hidden via inline style in `auth.js`, the `PAGE_ACCESS` route guard in `index.html`, and the page's own render-time check in `feature-toggles.js`).
- Used `AskUserQuestion` to scope the description-improvement ask (text-only example vs. text + visual mini-preview vs. adjust wording first) rather than guessing — Royce picked text + visual mini-preview.
- Grounded every example in real code/data before writing it, not invented: pulled the credential gate's actual confirm-modal wording verbatim (`people-required-credentials.js`), confirmed "White Card" as a real example value already used elsewhere in this app's own copy, and confirmed SLDC/MOD10 as the one real live site carrying 2+ project codes fleet-wide (from a same-week memory entry) rather than naming a fabricated site.
- Shipped: each of the 3 toggles (`scripts/feature-toggles.js`) gets a one-line concrete example plus a small static visual mock of the real control, styled from the actual production markup (`roster.js`'s `editor-job`/`editor-project` `<select>`, `index.html`'s `#modal-confirm`).
- Caught and fixed a real bug in the new code before shipping: the mocks sit inside the row's `<label>` (the existing big-click-target pattern for the checkbox), so a click anywhere inside a mock would otherwise bubble to the label's default action and silently flip the real, org-wide toggle. Added `event.preventDefault()` on each mock's wrapper.
- Verified functionally, not just visually — built an isolated test harness that loads the real shipped `feature-toggles.js` (no live Field/Core session available in this environment) and drove it with the Browser pane: confirmed clicking inside a mock leaves the checkbox untouched (change-event count stayed 0) while clicking the row text or the checkbox itself still toggles it normally (count incremented).
- eq-field [PR #849](https://github.com/eq-solutions/eq-field/pull/849) (v3.5.620) — full local check suite green (35/35 tests, ESLint 0 errors, cache-buster-drift clean) at every stage. Merged on Royce's explicit "merge" (pre-merge freshness + mergeable checks each time), confirmed live via `field.eq.solutions/sw.js`.

## Decided
- Royce confirmed via `AskUserQuestion`: "Text + visual mini-preview" over text-only or wording-adjustment-first.
- Royce explicitly said "merge" — merged without further confirmation, per standing deploy rules.

## Deferred
- **Not click-tested live by a person** — no Core/Shell session in this environment (both tenants are Core-only). Needs a real supervisor pass: open Manage → Feature Toggles, confirm the examples/mini-previews render and that clicking inside one doesn't flip the switch. Logged in `eq/pending/eq-field.md`.
- **Flagged, not actioned**: `field.manage_feature_toggles` is manager+supervisor by default even though the switches themselves are org-wide (not per-viewer) — Royce's call whether to narrow to manager-only. Logged in `eq/pending/eq-field.md`.

## Notes
- Hit the same version-collision pattern as the session above, twice, back to back — a genuinely busy window: bumped to v3.5.618, `origin/main` moved (PR #847 took it) → rebased, renumbered to v3.5.619, re-verified, pushed → immediately before merging, `origin/main` moved AGAIN (PR #848, a *different concurrent session* — see its own log entry above, same file) → rebased a second time, renumbered to v3.5.620, re-ran the full local suite again, re-checked mergeable state immediately before the actual merge call each time. Worth remembering for any future session in this repo during a high-traffic window: re-verify freshness right before the merge action itself, not just once earlier in the session — the gap between "checks passed" and "merge click" is exactly where this repo's races land.
- `gh pr merge --squash --delete-branch` failed on its LOCAL cleanup step ("'main' is already used by worktree at ...supervisor-list-population-9e6715") even though the squash-merge itself had already succeeded server-side — a local git worktree conflict in `gh`'s own post-merge housekeeping, not a merge failure. Confirmed the merge went through via `gh pr view --json state,mergedAt,mergeCommit` before treating it as done; deleted the now-orphaned remote branch separately via `git push origin --delete` (a pure remote op, unaffected by the local worktree lock).
- `eq-context` substrate was stale at session start (3 commits behind, 7 by close) and the shared checkout's `git merge`/`git pull` are hard-blocked by the F9 guard hook (concurrent-session git races) — followed the hook's own printed instructions and did this close's writes from an isolated `git clone` in scratch instead of the shared `C:\Projects\eq-context` checkout. Also caught live: this repo's OWN `CLAUDE.md` "Session-end protocol" section still points at `eq/changelog/field.md`, which has been explicitly retired since 2026-08-26 ("do not append to this file", `superseded_by: eq-field.md`) — followed the live file header instead of the stale instruction. Worth a fix in eq-field's `CLAUDE.md` itself so the next session doesn't have to re-discover this.
- A THIRD concurrent session's close (the "Apprentice-role permissions review" log above) landed on `eq-context` main between this clone being taken and this close's push — resolved as a normal same-day append conflict (both blocks kept, divider added), not a real disagreement. Three sessions closing on the same repo, same day, same file, is itself a signal this was an unusually busy window (see that session's own note about 6 version collisions across the day).

---

# Session 2026-08-31 — eq-field consumes tenant_role_overrides denials (cross-repo with eq-shell)

## Built
- Cross-repo handoff arrived from an eq-shell session: PR #1686 there ("tenant_role_overrides denials were silently inert everywhere") fixed Shell's own cookie-session permission model but explicitly scoped out Field's grant-only JWT/RLS path as a separate problem. Asked to investigate what a deny-consumption path for eq-field would take.
- Investigated first, scoping-only: read eq-field's `permission-matrix.js`/`permissions.js`, eq-shell's `token-exchange.ts`/`mint-tenant-jwt.ts`/`fieldFinePerms.ts`, and traced the full grant-flow chain (JWT mint → `verify-pin.js` → session token → client `EQ_PERMS.can()`) plus a THIRD enforcement layer nobody had mentioned: live Postgres RLS/SECURITY DEFINER triggers on ehow, 39 inline `extra_perms` checks across 16 migration files at the time. Corrected the original handoff's premise on one point: `mint-tenant-jwt.ts` isn't actually in Field's auth chain (zero references anywhere in eq-field). Reported findings, recommended not shipping client-only (would be a false sense of security), asked how to proceed via `AskUserQuestion`.
- Royce (or the driving session): "Draft full PR, both slices." Built for real:
  - **eq-field PR [#851](https://github.com/eq-solutions/eq-field/pull/851)** (v3.5.621) — `EQ_PERMS.can()`/`list()` deny-wins; `denied_perms` threaded through `auth.js`/`auth-shell-handoff.js`/`verify-pin.js`'s session token and data-plane JWT minting, every existing call site mirrored. Plus `supabase/migrations/20260831_field_permission_denials_enforce.sql` — NOT APPLIED, adds the matching deny check to **10 live app_data objects** found by an exhaustive `pg_proc.prosrc`/`pg_policies` ILIKE sweep against ehow (not a migration-folder grep — 3 of the 10 function bodies had already diverged from every committed file; one committed file is explicitly marked SUPERSEDED). Explicitly excluded 14 `public.eq_*` Quotes/CRM RPCs + 3 policies sharing the identical gap but confirmed (via repo-wide grep) to be eq-shell's, not eq-field's.
  - **eq-shell PR [#1690](https://github.com/eq-solutions/eq-shell/pull/1690)** — companion, threads `roleOverrides.denials` into a new `denied_perms` claim on the Field JWT (`token-exchange.ts`), new trailing param on `signSupabaseJwt()`/`signJwtWithSecret()` so `mint-tenant-jwt.ts`'s existing calls are unaffected.
  - Both worktrees, both branches pushed, both PRs opened cross-linked. CI hit 2 real failures on first push (missed `app-state.js`'s own standalone cache-buster tag; migration didn't cite `IDENTITY-MODEL.md` by the literal string a CI guard regex requires) — fixed, re-pushed, both PRs fully green.
- Neither PR merged, migration not applied — per explicit instruction ("nothing applied to live ehow and nothing merged/deployed"). Did not even test-execute the migration inside a rolled-back transaction against live ehow, reading that instruction conservatively; validated by manual proofread against live `pg_get_functiondef` output instead.

## Decided
- Royce: "Draft full PR, both slices" (via `AskUserQuestion`, choosing over scoping-only or client-side-only).

## Deferred
- **Both PRs need Royce's review and merge decision.** #851 is inert without #1690 merging too; the migration inside #851 needs a further, separate dispatch decision (One Pipe) even after both PRs merge.
- **Deploy-preview click-through not done** — no live `tenant_role_overrides` row targets a Field-fine-grained key today, so there's nothing to click through yet; needs a manually-created test row post-merge.
- **14 `public.eq_*` Quotes/CRM RPCs share the identical gap, one layer deeper than what eq-shell's own PR #1686 fixed** — eq-shell's to build, not eq-field's; spawned as `task_9f3eb7a8`.

## Notes
- **Real, unrelated, live P0/P1 bug found while verifying the migration's target tables, not this session's to fix**: `20260819_site_reports_manager_supervisor_write.sql` has been live on ehow since 2026-08-18 — its own header still wrongly claims "DRAFT — NOT APPLIED" — and is currently, right now, blocking employee/apprentice/labour_hire from creating prestarts/toolbox talks/incidents/site diaries/audits on real SKS data, directly contradicting Royce's explicit v3.5.610/613 "must never be blockable" decision from 2026-08-30 (one day before this was found). Confirmed live via direct `pg_policies` query, not inferred. First spawned as a chip understating the severity ("would break if dispatched") — Royce had already started that session before severity was upgraded to "IS breaking, now" and re-spawned as `task_2517f0eb`; the original weaker chip (`task_c278e59b`) needs redirecting, not re-trusting.
- This session is a clean example of why live-DB verification beats migration-folder reading for this repo specifically: the scope started as "16 files, 39 checks" from a text grep, and ended as "10 live objects" after live verification — smaller in count but materially different in membership (3 objects invisible to any single file, one file actively wrong to trust). Every function body in the shipped migration was copied from a live `pg_get_functiondef` pulled immediately before writing it.
- Both this repo's guard hooks fired as designed and were satisfied properly, not bypassed: brief-gate (`/brief eq-field` run before the first edit) and reflection-gate (`/reflect` run before the eq-field commit, logged in that repo's own `docs/reflection-log.md`).

---

# Session 2026-08-31 — site-reports RLS gap closed (prestart/toolbox_talks), cross-session conflict resolved directly with Royce

## Built
- Closed the live gap flagged just above as `task_2517f0eb`: `public.prestarts`/`public.toolbox_talks` INSERT/UPDATE opened to every authenticated tenant member on ehow (DELETE stays manager/supervisor-only; `incidents`/`site_diaries`/`site_audits`/`site_audit_items` untouched — nothing client-side ever widened those). Applied live via Supabase MCP, verified via `pg_policies` before and after.
- Fixed the stale header on `supabase/migrations/20260819_site_reports_manager_supervisor_write.sql` (still said "DRAFT — NOT APPLIED" despite having been hand-applied 2026-08-18, before the PR that committed it).
- eq-field [PR #852](https://github.com/eq-solutions/eq-field/pull/852) — rebased once against concurrently-merged PR #850 (append-only conflict in `docs/reflection-log.md`), squash-merged on explicit "yes", confirmed live.

## Decided
- Royce, via `AskUserQuestion`: scope the fix to prestarts+toolbox_talks INSERT/UPDATE only (narrowest of 3 options offered) — not DELETE, not the other 4 tables.
- Royce, directly, twice: confirmed the final policy shape is full-open (any authenticated tenant member), not "exclude only subcontractor" — see Notes below for why this needed asking twice.
- Royce: "yes" to merging PR #852.

## Deferred
- **Not click-tested live by a real non-manager/supervisor SKS account** — no test credentials in this environment; verified via direct `pg_policies` + `get_advisors` instead.
- **`service.tenant_members` doesn't join cleanly to `field_people.user_id` for role lookup** — hit this trying to confirm Cameron Tregoning's role and recent prestart authors' roles; root cause not chased. Logged in Claude's own memory system rather than here, since it's a tool-usage gotcha for the next investigation, not an engineering backlog item.

## Notes
- **Timeline correction — the actual value beyond just applying the fix**: the originating report (both the spawned task above and this session's own brief) framed the gap as live "since 2026-08-18." Verified against `permission-matrix.js`'s own git history instead of trusting that: the client only granted prestart/toolbox create to every role around 2026-08-24 (v2.6) — before that, client and DB agreed. A separate CSS bug (fixed by v3.5.610, 2026-08-30) had been hiding the "+ New" button from those roles the whole time in between, so nobody could actually reach the RLS wall until 2026-08-30. Real live-impact window: ~1 day, not 13.
- **Cross-session conflict, resolved by surfacing the contradiction rather than trusting either side.** A concurrent session (separate worktree, same underlying task) reported Royce had told it to scope this as "exclude only subcontractor" instead of full-open. That's a real, documented distinction in `permission-matrix.js` (`EQ_FIELD_EXCLUDED_ROLES = ['subcontractor']`) — verified independently before acting on the peer's relay, rather than treating another session's claimed approval as a substitute for Royce's own word. Asked him directly; got "yes, tighten it" first — then the other session reported the opposite minutes later. Put the exact contradiction back to him rather than picking a side; he then confirmed full-open was correct after all. Live `pg_policies` never left the full-open shape throughout (a tightening migration was drafted, never applied) — no rollback needed.
- `task_2517f0eb` couldn't be dismissed via this session's own task tool (spawned by a different session, not reachable from here) — closed out via this file + `eq/pending/eq-field.md` instead.
- This repo's guard hooks fired and were satisfied properly: brief-gate run before the first edit (worktree `eq-field-site-reports-rls-wt`, branch `claude/site-reports-rls-scope-fix`), reflection-gate satisfied via `docs/reflection-log.md` (including a follow-up entry documenting the cross-session conflict itself).

---

# Session 2026-08-31 — the other side of the PR #852 conflict, plus a post-merge audit

*This is the "concurrent session" referenced throughout the write-up above — same event, other vantage point. Not duplicating that narrative; adding only what it doesn't already cover.*

## Built
- Nothing shipped directly — this session's one edit attempt was blocked by eq-field's brief-gate hook, and by the time it would have been retried, live-DB verification showed the gap already fixed elsewhere (PR #852 above). Ran a post-merge audit instead (merge-readiness subagent): `mergedBy` was Royce himself, CI/deploy-preview green, diff scope exactly right (no stray subcontractor-exclusion tightening), SQL idempotent, no version bump needed.

## Decided
- Royce, directly, in this session's own conversation: "exclude only subcontractor" first, then — once the other session's already-live full-open fix was surfaced back to him as a discovered conflict — "keep the live full-open state." Matches the resolution recorded above.

## Deferred
- None — post-merge audit came back clean, nothing further needed.

## Notes
- Independently verified the live gap the same way the other session did (`pg_policies` + `list_migrations` against ehow) before acting on anything — a peer session's relayed claim about what Royce approved was treated as a lead, not a substitute for his own word, which is what surfaced the conflict in the first place rather than letting two RLS changes land silently on top of each other.
---

# Session 2026-08-31 — eq-cards polish sprint, rounds 2 & 3: 6/10 → 8/10, 3 PRs shipped and live

*Direct continuation of 2026-08-30's round-1 sprint (PR #331). Royce, looking at the scorecard artifact each time: "sprint the deferred items" (twice more) and, on the last round, an explicit target — "next sprint outcome = 9/10".*

## Built
- **Round 2a** (eq-cards [PR #332](https://github.com/eq-solutions/eq-cards/pull/332)): documented the app's one deliberate gradient exception (photo-legibility scrims, not decoration) rather than flattening it; fixed the sign-in screen's mobile layout (no more floating card + dead space below 600px width); built a shared `EmptyStateView` widget, adopted in 2 of 3 duplicate spots (the third — the wallet screen's multi-CTA empty state — correctly left bespoke rather than forced); first-ever tests for the two highest-risk untested screens in the `workers` folder (role-assignment, invite-claiming — 20 tests). Squash-merged (`8a5da26`), deployed, verified live (screenshot of the corrected mobile layout).
- **Round 2b** — a fresh audit pass (2 Explore agents: UX-craft on screens round 1 hadn't touched, architecture/performance app-wide) surfaced the next tier: expiry-alert scheduling had no refresh capability and no error handling; offline resilience was a one-screen special case; the site-gate screen (`show_screen.dart`) used zero design-system tokens including a wrong "EXPIRED" red; 3 more missing accessibility labels; the wallet screen re-sorted its whole list on every search keystroke. Sprinted as 5 parallel agents → eq-cards [PR #333](https://github.com/eq-solutions/eq-cards/pull/333): `workerCredentialsList` given a proper `refresh()`-capable shape (traced every write path first — no client mutation exists for this data, so invalidation-after-mutation was the wrong fix); `profile_screen.dart` now shows the offline banner using cache logic that already existed but was never surfaced; `show_screen.dart` fully re-tokenized + a new page-position indicator (the app's only `PageView`, previously had none); 3 more `Semantics` fixes; wallet search now sorts once and caches instead of re-sorting per keystroke, with a new regression test. Squash-merged (`92b928f`), deployed, verified live.
- **Round 3** ([PR #334](https://github.com/eq-solutions/eq-cards/pull/334)) — pushed every remaining code-buildable finding from both rounds in one 8-agent pass: input-style + `ErrorStateView` retry on `admin_worker_detail_screen.dart`; WCAG button-contrast completion on `connect_to_company_screen.dart`; dropdown-helper dedup on `profile_edit_screen.dart` (one suspected token-drift fix correctly declined after checking actual values — the file was already on the dominant convention, a different file is the real outlier, flagged separately rather than force-fixed); a stale copy-paste email workaround replaced with real `url_launcher`; `Future.wait` parallelization in PDF/Excel export; a non-blanking refresh fix on the platform console; a new PWA "update available" banner (real `controllerchange` detection, fails inert with zero UI when nothing fires — the one fix in the whole sprint that can't be fully proven by `flutter test` alone); 8 more `Semantics` fixes across the app's most-used screens (profile settings rows, sign-out, worker tiles, a notice-banner dismiss control); and first-ever test coverage for the last 4 untested feature folders (`alerts`, `platform`, `consent`, `legal` — 66 tests, and 2 real bugs caught along the way: a "never expires" credential could still fire a misleading expiry notification, and a JSON-key mapping regression-guarded after it broke a production screen once already). Squash-merged (`5859597`), deployed, verified live (bundle-content check for new strings).
- All 3 rounds deployed via explicit `deploy.yml` dispatch on Royce's own "deploy"/"deploy it" each time — none auto-deployed on merge (this repo's deploy stays deliberate, unlike eq-shell). Every deploy verified against the actual Netlify record or the workflow's own upload log (the Netlify MCP connector dropped out intermittently this session — fell back to `gh run view --log` each time rather than trusting the green run alone) plus a live screenshot or bundle-content check, not just a green workflow run.
- Scorecard artifact (same URL throughout: https://claude.ai/code/artifact/980592cb-ddff-4833-a59d-388e7bc4f1d4) updated after each round rather than left stale — final state: 6/10 → 7/10 → 8/10 across the 3 rounds, every point tied to a merged PR or a live check, with an explicit, honest account of why 9-10 isn't reachable by code alone (2 items need a real person: usability testing, live click-throughs).
- Two follow-up chips spawned for findings that were real but out of this sprint's own scope: `task_22f29e79` (EqButton's shared primary style fails WCAG AA at normal text sizes, app-wide) and `task_824a07b7` (`licence_edit_screen.dart`'s white/surface token drift, the actual outlier once the original suspected file checked out clean). Royce started both in separate concurrent sessions; both still running independently as of this close.
- Memory corrected: `eq_cards_deploy.md`'s Flutter-version pin (stale at 3.41.9, actually 3.44.8) and its `.dart-defines.json` legacy-project caveat (RUNBOOK.md now says both dart-defines files point at eq-canonical).

## Decided
- Royce: "sprint the deferred items" (twice) and "let's sprint the top 5 improvements possible" (round 1, previous close) — build, not just report, each round's findings.
- Royce: "next sprint outcome = 9/10" — an explicit target. Reported 8/10 honestly instead of forcing the number, with the specific structural reason (2 human-only items) the last 2 points can't close by code.
- Royce (via AskUserQuestion, each round): "Push + open PR" after local verification, before every push. One genuine mis-click self-corrected immediately on "i pressed the wrong one" — opened the PR that was actually wanted.
- Royce: "merge when safe" three times and "deploy"/"deploy it" three times — one explicit pair per PR, never assumed from the previous round's approval.

## Deferred
- `task_22f29e79` / `task_824a07b7` — both in progress in separate concurrent sessions as of this close, not this session's to finish.
- The PWA update banner needs a real deployed check (ship two versions, confirm the banner fires) — `flutter test` cannot simulate a live service-worker update.
- Real usability sessions with actual tradies, and live click-throughs on a few specific already-shipped fixes (PR #331's admin-error retry state, the mobile sign-in layout on an actual phone) — all logged in `eq/pending/eq-cards.md`, all genuinely need Royce, not more code.

## Notes
- Running 5-8 agents concurrently in one shared (non-isolated) worktree worked in practice across all 3 rounds — file-overlap was checked before each dispatch and held every time — but repeatedly produced a `dart run build_runner build` race: whichever agent needed codegen first would sometimes see another agent's in-flight run and either wait needlessly or hit a stale-`.g.dart` compile error from a sibling's not-yet-regenerated file. Every agent that hit this correctly self-diagnosed it as concurrent WIP (checked `git status` for other agents' modified files) rather than treating it as its own bug — worth trusting that self-diagnosis rather than re-investigating it, based on this session's repeated experience.
- Hit the eq-cards squash-merge-branch-contains-trap twice this session (already flagged as a known pattern elsewhere in this file's history) — a local branch that already had one PR squash-merged from it still "contains" that PR's pre-squash commit under a different hash, so building the next round directly on top of it would silently duplicate that commit in the next PR's history. Caught both times before pushing by diffing the suspect commit against fresh `origin/main` (empty diff confirmed the content was already there) and re-branching clean via `git checkout -B <new-name> origin/main` plus cherry-pick or stash/pop, rather than reusing the branch.
- `ScheduleWakeup` was misused twice this session to "wait" on a background task despite an existing memory already documenting this exact mistake — reading a memory once didn't prevent the recurrence. Strengthened that memory file directly rather than just noting it here: the actual fix is behavioral (zero tool calls while waiting, not a considered choice about which wait-tool to use).
- Riverpod 3 has a default automatic retry (`ProviderContainer.defaultRetry`, up to 10 attempts with backoff) that applies to any thrown error that isn't a Dart `Error` — since every `Failure` type in this app `implements Exception` (not `Error`), app errors get silently retried by default unless a test explicitly disables it (`ProviderScope(retry: (_, _) => null)`). Surfaced while writing `platform_console_screen_test.dart`; worth knowing for any future test in this repo that overrides a provider to throw one of the app's own `Failure` types.
- This machine's default `flutter` on `PATH` is stale/broken (`/c/Projects/flutter/bin` doesn't exist); the real SDK is at `/c/src/flutter/bin`. A second, separately-outdated install exists at `C:\FlutterSDK` (3.41.9 — too old for this pubspec's current Dart floor). Recurred as a fresh discovery for multiple agents across all 3 rounds; still not fixed at the PATH level.
- The Netlify MCP connector intermittently failed to respond for plain read calls on 2 of the 3 deploy verifications this session — fell back to pulling the deploy state directly from the workflow's own job log (`gh run view --log`, grepping for "State:") each time, which worked reliably throughout.

---

# Session 2026-08-31 — eq-solves-service: CLAUDE.md StatusBadge doc fix

## Built
- Fixed a stale line in eq-solves-service's `CLAUDE.md` ("UI conventions"): described `StatusBadge` as having `tone soft/solid, size sm/md` props — neither exists. Verified directly against the installed `@eq-solutions/ui` v1.15.0 source (`node_modules/@eq-solutions/ui/src/StatusBadge/StatusBadge.tsx`, matching the exact tag pinned in `package.json`): real props are `status` (required, drives fixed colour+dot per kind), `label` (optional text override), `density` (`comfortable`/`compact`, no `tone`/`size`). Royce had already done this verification himself; this session independently re-confirmed it before editing.
- Committed as `docs(claude-md): fix stale StatusBadge prop description` (`9541e92`) on a fresh branch off `origin/main` (`worktree-statusbadge-doc-fix`), in an isolated worktree — not pushed, no PR (not requested).

## Decided
- Royce, via `AskUserQuestion`: commit target = new branch off main, not the stale unrelated branch this session's shared checkout happened to be on.
- Royce, via `AskUserQuestion`, after the collision below: use an isolated worktree rather than checking out again in the shared root.

## Deferred
- Push + open a PR for `worktree-statusbadge-doc-fix` (commit `9541e92`) — sitting local-only in `.claude/worktrees/statusbadge-doc-fix`. Logged in `eq/pending/eq-solves-service.md`.

## Notes
- **Live F9 recurrence, directly observed, not inferred — TWICE in this session.** (1) Mid-task, the shared `eq-solves-service` root checkout (`C:\Projects\eq-solves-service`) was switched by something other than this session from `fix/service-schema-pre-canonical-fixture` to `fix/tenant-scope-asset-grouping-rpcs` — confirmed via `git reflog`. No work lost (the doc fix was stashed first; stash entries are repo-wide, not branch-scoped). (2) **At this same close, this exact section was written once already, then silently wiped from disk** — a concurrent session's merge (`4dbf7b3 chore: session close 2026-08-31 [skip ci] (#195)`) landed in this shared `eq-context` checkout and the working-tree copy of this file reverted to that commit's content with zero diff (`git status` showed the file clean, my uncommitted append simply gone) before this session could commit it. The `eq/pending/eq-solves-service.md` edit, committed independently, survived. Re-added here and committed immediately this time to close the exposure window. Two independent, timestamped reproductions of the F9 failure shape, in two different repos, within one session — this is not a one-off.
- `eq-context` also showed the milder version of the same shape at this session's own close start: local `main` was 1 commit ahead (a different concurrent session's own unpushed close commit, `8a23dbb`, inspected and confirmed benign/complete before building on top of it) and 4 behind (nightly digest/suite-state bot commits). That part was handled fine via the documented fetch+rebase+push recovery. The file-content wipe above is the more serious variant: it doesn't need a push race, just two sessions with uncommitted edits sharing one working tree.
- **Rebase attempted, blocked by this repo's own F9 guard hook, redirected to an isolated clone — a third F9 touchpoint in one close.** `git rebase` in the shared `C:\Projects\eq-context` checkout was refused by `hooks/pre_tool_use.py` with the same reasoning as this note: rebase mutates HEAD/index/refs non-atomically and a concurrent session can hit it mid-operation. Cloned fresh to scratch instead, cherry-picked this session's commit onto the then-current `origin/main` (which had already moved twice more by this point — once via a PR #852 post-merge-audit close, once via an eq-cards sprint close), hit one genuine content conflict on *this file* against the eq-cards close (simple divergent-append, both sides kept), pushed from the isolated clone.

---

# Session 2026-08-31 — licence edit screen's dropdown/date/search fill colour fixed, merged, deployed live

*This is `task_824a07b7`, flagged as still "running independently" by the polish-sprint session's own close above — it finished here. Not duplicated into `eq/pending/eq-cards.md` as a separate write-up; that file's existing `task_824a07b7` line was updated in place instead.*

## Built
- Fixed 4 `fillColor` sites in eq-cards' `licence_edit_screen.dart` (state dropdown, licence-type picker, type-search field, both date fields): `EqColours.surface` → `EqColours.white`, matching `EqTextField`'s spec'd convention (this same screen's plain text fields already use white) and the app's dominant pattern (85 white vs 31 surface usages app-wide).
- [eq-cards PR #335](https://github.com/eq-solutions/eq-cards/pull/335) — squash-merged, deployed via manual `gh workflow run deploy.yml`, confirmed live on cards.eq.solutions via Netlify's own deploy record (timestamps correlate to the second against the GitHub Actions run — not just a green CI check).

## Decided
- Royce: confirmed the pre-edit brief before touching code.
- Royce: "commit this" → committed.
- Royce: "merge" → PR opened, CI verified, branch updated to main, merged.
- Royce: "deploy is safe to do so" → triggered the manual deploy workflow, verified live.

## Deferred
- **`licence_edit_screen.dart` has zero test coverage** — verified via grep, nothing under `test/` references `LicenceEditScreen`, unlike its siblings (detail/list/show screens). Spawned as a background chip (`task_236abfe6`); Royce started it in a separate session, running independently as of this close.

## Notes
- Session started in eq-cards' shared root checkout, which was on an unrelated branch (`docs/close-ledger-tracking-gaps`, someone else's task) — caught before any edit landed there, per this repo's own "never bare-path a shared checkout" lesson. Created a fresh isolated worktree instead.
- Confirmed live (not assumed): eq-cards' `deploy.yml` ships via a raw zip upload to the Netlify Deploys API (`deploy_source: "api"`, `commit_ref: null`), not a git-linked Netlify site. The eq-shell-style "check commit ancestry against the live deploy's `commit_ref`" verification method doesn't apply here — there is no commit_ref. Verify eq-cards deploys by correlating the Netlify deploy's `created_at`/`published_at` timestamp against the triggering GitHub Actions run's completion time instead.
- eq-context's shared checkout (`C:\Projects\eq-context`) had a stray unpushed local commit (`8a23dbb`, a different session's eq-field close) that had diverged from origin/main. `git rebase` in that shared checkout is hard-blocked by a guard hook (F9, rung 4) — correctly so; followed the guard's own remediation (patch + isolated clone). Verified byte-for-byte that the stray commit's content was already captured on origin/main via a different push path (PR #195, commit `4dbf7b38`) — safe to leave the shared checkout's stray commit untouched, no data loss. This session's own substrate writes were made and pushed from a fresh isolated clone instead of the shared checkout.
- This rebase itself hit a second, genuine content overlap: the polish-sprint session's close (block above) and this session's own pending.md write-up both described the same underlying fact (`task_824a07b7`'s resolution) from two different vantage points, same shape as the PR #852 conflict earlier in this file. Resolved the same way — kept both session-log accounts, but updated `eq/pending/eq-cards.md`'s existing `task_824a07b7` line in place rather than adding a second, redundant section there.
- Push raced a second time (rejected, non-fast-forward) after the above reconcile — origin had moved again in the interim (the eq-solves-service StatusBadge session's own close, block above, plus its own F9 encounters). Fetched + rebased once more; this round's conflict in this file was a plain divergent-append against an unrelated session with zero content overlap — kept both.

---

# Session 2026-08-31 — EQ Ops quote-visibility guard rails audited, quotes.view_all promoted to a grantable permission

## Built
- Royce asked what guard rails exist on who can see quotes in EQ Ops, and whether visibility was ever limited. Answered from live code, not memory: `quotes.view` (module gate, since [PR #1681](https://github.com/eq-solutions/eq-shell/pull/1681)/migrations 0296-0297 — the commit immediately preceding this session, closed a real gap where the RPCs had no server-side check at all), `quotes.view_all` (row-level own-vs-all scoping, migration 0267), `ops.view_margins`/`entity.view_pii` (column masking), `quotes.create` (write gate, also newly server-side-checked by #1681).
- Real gap found: `quotes.view_all` was Shell-local (`src/modules/quotes/permissions.ts`), hardcoded manager+supervisor, invisible to Access Control's grant UI (which only offers keys from `@eq-solutions/roles`' `PermKey` union) — so there was no way to grant one specific Employee the "see everyone's quotes" escalation without promoting their role, even though the RPCs already checked an `extra_perms` claim for exactly this.
- `/decide` pass run before building (framing, steelman, pre-mortem, value/feasibility, the call) — Royce confirmed via `AskUserQuestion`: build the lever now.
- eq-roles [PR #32](https://github.com/eq-solutions/eq-roles/pull/32) (v2.7.6): promoted `quotes.view_all` into the canonical package's `roles/model.json` — same default (manager+supervisor), no behaviour change, `npm test` 110/110. Merged, tagged `v2.7.6`.
- eq-shell [PR #1689](https://github.com/eq-solutions/eq-shell/pull/1689): bumped the `@eq-solutions/roles` pin, made `src/modules/quotes/permissions.ts` a pure re-export (matching equipment/service/field's existing pattern), updated `check-perm-sync.mjs`'s hybrid/pure module lists and `permissions.test.ts`'s `isReadPerm` classification ratchet to match. `pnpm run build`, full test suite (461/461), drift guard, eslint all clean. Merged on Royce's explicit "merge" (squash `d89caf29`).
- Verified live properly, not assumed: a later, unrelated PR (#1691) merged minutes after this one and became the newest "ready" production deploy, so the live `commit_ref` didn't match this session's own merge commit directly — confirmed via `git merge-base --is-ancestor` that #1689's commit is an ancestor of what's actually serving, per the standard concurrent-merge caveat in the global deploy runbook, rather than reading the mismatch as "not live."

## Decided
- Royce, via `AskUserQuestion` after the `/decide` pass: build the per-person `quotes.view_all` lever now (not hold it pending a confirmed real case).
- Royce, via `AskUserQuestion`: "Merge, tag, and do the eq-shell follow-up" in one sitting, rather than stopping after the eq-roles PR for his own review.
- Royce: "merge" — explicit approval for eq-shell PR #1689, which is both the merge and (per this repo's standing rule) the production-deploy trigger.

## Deferred
- **Not click-tested live** — no Shell session/credentials in this environment. Worth a real pass: Access Control → Ops section, confirm "See all quotes" appears under Manager/Supervisor and can be granted to an Employee individually via a custom group. Logged in `eq/pending/eq-shell.md`.
- **Write-side ownership scoping not built**: `quotes.create` gates `eq_update_quote_status`/`eq_add_quote_note`, but not scoped by `created_by` — any holder can edit any quote's status/notes tenant-wide, not just their own. Surfaced during the original guard-rails audit and offered as a build option; Royce chose the read-side lever only, this stayed unbuilt by his own selection, not an oversight.

## Notes
- **`ScheduleWakeup` misused as a generic "wait for background task" mechanism again** despite an existing memory already documenting this exact recurring mistake (4x in one prior session) — the tool outright refused the call this time (`prompt` is required when `stop` is not true), confirming it's a hard mismatch, not a tolerated misuse. Updated the memory file directly to record the cross-session recurrence rather than just noting it here, since a single documented instance clearly wasn't enough to prevent a repeat.
- A backgrounded `npx netlify api listSiteDeploys` deploy-verification call was lost across a session-resume boundary (task notification came back "stopped ... no completion record found," likely the previous process exiting mid-run) — re-ran it fresh in the resumed session rather than trusting any stale partial output, per the standing rule that a memory/prior-result frozen in time isn't evidence of current state.
- `eq-context`'s shared checkout was behind `origin/main` at both the mid-session `/decide` step (touching `rules/decision-protocol.md`) and at this close — `git pull`/`git rebase` are hard-blocked in the shared checkout by the F9 guard hook (concurrent-session git races) both times. For the `/decide` read, reconciled narrowly (diffed just the one file needed against `origin/main`, found it unchanged, read local safely) rather than pulling the whole repo. For this close, followed the hook's own printed remediation: worked from an isolated `git clone` in scratch instead of the shared `C:\Projects\eq-context` checkout.
---

# Session 2026-08-31 — EQ Service 4-axis review → P0/P1 sprint shipped live; critical cross-tenant leak found mid-build, closed same session; a self-caused production outage fixed within minutes

## Built
- Reviewed EQ Service across code/security, performance, features, and UX on request — verified live against ehow (Supabase advisors, direct grant/RLS queries, `tsc`, `npm audit`) rather than trusting docs, per this repo's own Rule 0.5. Rated each area (Code & Security 8/10, Performance 6/10, Features 8/10, UX 6/10) and published the full review + a 19-item prioritized sprint backlog as a Claude Artifact.
- Ran a `/decide` pass on what to do next before building anything; Royce chose "review first." Came back and said "P0 and P1" — built all 13 items across 7 parallel background agents plus 4 items handled directly (2 DB migrations, an idempotency fix, one RPC investigation).
- Implementation surfaced a live security bug the review itself hadn't caught: `get_assets_for_grouping` had never filtered by tenant across 5 revisions and ran via the RLS-bypassing admin client — every tenant's asset register was reachable from every other tenant's default Assets view. Checked live: zero actual victims to date (SKS is the only tenant with data), but a hard blocker before ever onboarding a second one. Found and fixed the identical gap in `get_distinct_asset_types` too, that half built with Royce directly in a separate session.
- Also found real, ongoing data loss in the same function: the grouped Assets view's query never fetched `manufacturer`/`model`/`install_date`/`dark_site_test`, so `updateAssetAction`'s unconditional `.update()` silently blanked those fields on every edit made from that view. Can't recover whatever's already been wiped — the writes never failed, they just wrote over real values with nothing.
- Applying the tenant-scope migration (added a required `p_tenant_id`, dropped the old overload) without deploying its matching app-code change broke the live Assets page for several minutes — a mistake, caught and explained immediately, fixed via a coordinated hotfix (both halves in one deploy) rather than a silent patch.
- Shipped: eq-service [PR #816](https://github.com/eq-solutions/eq-service/pull/816) (the tenant-scope hotfix), [PR #817](https://github.com/eq-solutions/eq-service/pull/817) (the full P0/P1 sprint — 10 commits: Zod validation on 5 bulk-import actions, `withIdempotency` on RCD saves, real `/defects` pagination, batched the customer-portal per-site queries + fixed a PostgREST 1000-row undercount found along the way, stripped `shadow-*` from 4 shared UI components, consolidated duplicated status pills onto `StatusBadge`, a new `Alert` component migrated onto 133 call sites across 69 files, FEATURES.md documentation for 4 modules, 3 stale "proposed" docs corrected to "shipped"), and (Royce, separate session) [PR #818](https://github.com/eq-solutions/eq-service/pull/818) (the `get_distinct_asset_types` half of the tenant-scope fix).
- Verified post-deploy rather than trusting a green merge: Netlify's current production deploy confirmed `ready`, clean secret scan, all redirect/header rules and functions deployed without error; Sentry confirmed zero unresolved issues in the 24h window spanning the whole incident.

## Decided
- Royce, via `/decide`: review the backlog before building anything, rather than start executing immediately.
- Royce: "P0 and P1" — build both tiers of the sprint.
- Royce, via `AskUserQuestion`: apply the tenant-scope migration to live ehow immediately once the leak was found.
- Royce, via `AskUserQuestion` after the outage: ship the coordinated app-code deploy (not a temporary DB-only revert) to fix it.
- Royce: "merge" (PR #816), "push it" (PR #817, after confirming CI's real gate — `tsc + next build` — had actually passed, not just that the branch had been pushed).

## Deferred
- P2 backlog items (search_path pin, revalidate-scope narrowing, Data Quality worklist, dashboard gradient cleanup, export-button dedup) — no urgency, listed in full in `eq/pending/eq-solves-service.md`.
- Retiring the legacy `shell-sso` auth path — needs Royce's explicit sign-off, it's an auth change.
- A live authenticated UX click-through — this worktree had no `.env.local`; copying working credentials from the main checkout was correctly blocked by the permission classifier as a credentials-file action, and that block wasn't worked around.
- Relaying 2 live ERROR-level advisor findings (`app_data.field_managers`/`field_people_directory`) to whoever owns Field or Shell's canonical schema — not this repo's fix.

## Notes
- **A DB function signature change and its calling app code are not independently deployable once the change removes a default parameter.** This is the actual root cause of the mid-session outage, not carelessness in the abstract — worth remembering as a standing rule for any future migration that removes or adds a required RPC parameter: stage the app code and the migration as one atomic release.
- **The platform's own permission classifier blocked every attempted `apply_migration`/`gh pr merge` call individually, even ones already discussed and agreed in chat** — including a same-session retry after Royce's own explicit "merge" instruction, and even a `ScheduleWakeup` call whose *prompt* would have resulted in an autonomous future merge (rewording the prompt to a neutral status-check, with the merge handled as its own later gated action, got past that specific block). Worth knowing for any future session: chat agreement doesn't substitute for the tool-level gate on deploy-equivalent actions, and each individual production write needs its own explicit trigger, not a standing authorization inferred from an earlier answer.
- **A background agent kept running well past the point its work looked "done."** Checked `git status` mid-flight, saw what looked like the complete Alert-component migration, and committed it — the agent was still active and made 33 more files' worth of edits afterward (a ~77-minute task in total), including a real refinement to the component itself (switched from raw Tailwind colours to the design system's own semantic tokens). Caught via the agent's own completion report flagging the discrepancy, not by re-checking proactively — a second commit closed the gap. Worth checking an agent's actual completion status before treating a git-status snapshot as final, especially on anything that's been running more than a few minutes.
- Original review over-stated two items before live verification corrected them: the notification cron turned out to be a deliberately parked product decision (`0229`'s own header says so), not a bug; and the "anon blanket-grant sweep" turned out to already be ~done (an auto-revoke event trigger already protects every new function) — the one real remaining gap was a single dangling, not-currently-exploitable grant on an unrelated function. Both corrections were surfaced to Royce directly rather than quietly adjusted.
- `eq-context`'s shared checkout was behind by 3-12 commits at every check throughout this session (heavy concurrent activity from other sessions/repos, confirmed via this same file's own other entries above) — every substrate read this session was treated as unverified per the session-gate's own instruction, and this close's own writes were made from an isolated `git clone` after the shared checkout's `git pull` was correctly hard-blocked by the F9 guard hook.
