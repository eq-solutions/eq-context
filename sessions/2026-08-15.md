# Session 2026-08-15 — staff-update was gating an HR write on a read permission; fixed, shipped, and corrected the repo's deploy model on the way

## Built
- **eq-shell [PR #1353](https://github.com/eq-solutions/eq-shell/pull/1353)** (squash `4c17e188`) — `netlify/functions/staff-update.ts` re-gated from `field.view` to `field.manage_people`. `field.view` is a READ key held by all six roles in @eq-solutions/roles v2.5.8 (manager, supervisor, employee, apprentice, labour_hire, subcontractor); the endpoint mutates `app_data.staff` (name, email, phone, trade, level, employment_type) via `eq_update_staff`, taking `staff_id` from the request body with no self-scoping. Tenant was the only containment.
- `permission-enforcement-baseline.json` ratchet tightened 13 → 12 — `field.manage_people` dropped from `dead_keys`, `enforced_downstream_field` category note corrected, `_resolved` entry added.
- Global `~/.claude/CLAUDE.md` deploy table row + eq-shell note rewritten (Royce's explicit instruction), then the skipped-deploy caveat added on top.
- Three memory files written/updated: `eq-shell-merge-equals-deploy`, `allowlist-entries-can-mask-holes`, `staff-update-inert-endpoint`, plus the MEMORY.md index.

## Decided
- **Permission choice: `field.manage_people`, not `admin.edit_user`.** Same module as the read key it replaced, its declared purpose is literally "Add, edit, remove, restore, or permanently delete a worker record", and it matches the manager+supervisor tier Field applies to its own worker-record actions. `admin.edit_user` governs login accounts and roles — wrong domain.
- **No self-edit carve-out.** Technically possible (`app_data.staff.user_id` exists) but `email`/`phone` here are login identity and this path writes only the `app_data` mirror, so a worker editing their own reproduces #1347 (new auth uid at next sign-in, licences stranded). `trade`/`level`/`employment_type` are rate-bearing — self-service is self-promotion.
- **Royce: merge 1353** — which on this repo is also the production-deploy approval (see Notes). Merged, deployed, verified live.
- **Royce: correct the CLAUDE.md deploy row**, then **add the skipped-deploy caveat**.
- **Royce ran `/decide` on whether to keep going → recommendation was stop**, and he closed. Remaining items can't complete without his dispatch, or are sweeps that need a fresh context to be worth trusting.

## Deferred
- `public.eq_update_staff` carries `GRANT EXECUTE … TO authenticated` on both tenant planes — bypasses the function and every check in it. Needs a One Pipe migration + Royce's dispatch, after sweeping eq-field for legitimate callers. Chip `task_4d026294`.
- Re-audit the other 11 keys carrying the same unverified "enforced downstream" claim (6 field + 5 service). Chip `task_4ece4bf6`.
- `staff-update.ts` is inert and it's a repair-or-retire call, not a silent fix.
- Gate not behaviourally tested with a real signed-in session — impossible while the endpoint 500s for everyone.

## Notes

**GOALS were UNSET in `system/TODAY.md` all session.** Nothing was deferred or prioritised by appeal to a deadline; the `/decide` pass said so explicitly rather than borrowing one.

**Correction with suite-wide reach — eq-shell: merge to `main` IS the production deploy.** Live on core.eq.solutions 2-4s later, unattended. Verified by correlating merge timestamps against deploy `created_at` across 13 consecutive merges (#1341–#1355), then 14/14 including this session's own. The previous global CLAUDE.md note claimed the opposite and **had been re-verified twice while staying wrong** — because its two observations are still literally true (`cdp_enabled_contexts` really is `["deploy-preview"]`; every prod deploy really is `deploy_source: "api"`, never `"github"`). The inference from them was false: `deploy_source: "api"` does not mean a human triggered it. Anyone re-checking the config finds it exactly as documented and marks the note verified. **Never re-derive this from either field — correlate timestamps.** Trigger is Netlify's own GitHub App (installation `121276861`); ruled out in-repo workflows (none deploy on push) and repo webhooks (`gh api repos/eq-solutions/eq-shell/hooks` returns none).

**Caveat found on this session's own merge: "my deploy succeeded" ≠ "my code is live", either direction.** Under concurrent merging Netlify supersedes in-flight production builds — `state: "error"`, `error_message: "Skipped"`. Two in one 20-minute window (`84647b81`, `25b2ef56`); both were still live because a later deploy built from a `main` already containing them. A skipped deploy still carries its commit's *title*, so the deploy list reads as though that change shipped on its own. Check ancestry against what is actually serving: `git merge-base --is-ancestor <my-sha> <commit_ref of newest ready prod deploy>`.

**`staff-update.ts` has never been able to write.** Client is `db.schema='app_data'`; `eq_update_staff` exists only in `public` on both planes. PostgREST → PGRST202 → 500. True since `1f63a829`; migration 0172 independently concluded the same from the data side. Left inert on purpose and documented at the call site — the obvious one-line "fix" (`getTenantRpcClientById`) converts dead code into a live write path on `app_data.staff`. **The gate still needed fixing precisely because it is one client-swap away from going live.**

**The lesson worth carrying: an allowlist entry can mask a hole rather than describe a benign one.** `field.manage_people` sat in the ratchet's `dead_keys` as "enforced downstream in eq-field, eq-shell never gates on it itself." The ratchet mechanically proves a key has no call site but cannot prove *why* — the human-written category note supplies the why, and nobody re-checks it. The key being dead was the *symptom*: nothing gated on it because the endpoint that should have was gating on something weaker. Eleven sibling keys still carry that same unverified claim.

**Live recon on the remaining DB door (severity-setting).** zaap: 0 `auth.users` — unreachable. ehow: 5, **all carrying a real `app_metadata.tenant_id`**, so the RPC would execute for them — but all are known internal accounts (3× @sks.com.au, `dev@eq.solutions`, one outlook.com), none banned, and every one dormant 7+ weeks (last sign-in 2026-06-24). Real, worth closing, not urgent. This is what flipped `/decide` to "stop".

**Open P0s untouched and outranking everything above:** SEC-1 (live PII leak) and SEC-9 (confirmed key exposure) on the security register.

---

# Session 2026-08-15 — eq-shell: account-enumeration oracle closed on the phone+PIN sign-in door, then the timing-burn hash deduped

## Built
- **[#1355](https://github.com/eq-solutions/eq-shell/pull/1355) MERGED (`81b3a1a0`) + live** — closed two account-enumeration oracles on `netlify/functions/shell-login-phone-pin.ts`, both the class [#1350](https://github.com/eq-solutions/eq-shell/pull/1350) had just closed on the email door.
  - **Body oracle:** "no account / no PIN" returned `200 {valid:false, error:'no-pin-set'}` while a wrong PIN returned a bare `{valid:false}` — distinguishable, so it confirmed account existence for any AU mobile submitted. All three cases now return a byte-identical bare `{valid:false}`. The in-code comment defending this was true *within* the not-found branch but said nothing about the difference against the wrong-PIN branch, which was the actual leak.
  - **Timing oracle:** the not-found branch returned before `bcrypt.compare`, making "no account" ~270ms faster. Added the `NO_USER_DUMMY_HASH` burn. Measured after: 254ms vs 275ms (21ms residual = bcryptjs variance).
  - `src/pages/LoginPage.tsx` — one message for every rejection, pointing a PIN-less worker at the "Text me a code instead" button already on that form.
- **[#1359](https://github.com/eq-solutions/eq-shell/pull/1359) MERGED (`8bf83a79`) + live** — folded the duplicated `NO_USER_DUMMY_HASH` into `netlify/functions/_shared/pin-timing.ts` (constant + a `burnPinCompare(pin)` helper returning void, not the always-false result). Both doors import it. Added `_shared/pin-timing.test.ts` (4 tests) asserting the hash parses as bcrypt **at cost 12**, matches nothing, yields no boolean, and costs the same as comparing against a freshly-hashed real PIN.

## Decided
- **Royce chose "gate the guidance behind proven possession"** over collapsing to vaguer copy or accepting the leak — asked via AskUserQuestion before any code was written, per the handover's explicit "don't silently pick one".
- Royce approved both merges explicitly, and all three worktree removals.

## Deferred
- Not click-tested in a browser — see `eq/pending.md`. Verified by direct production API calls + full CI only.

## Notes
**The useful half of `no-pin-set` did not need rebuilding — it already existed.** `verify-shell-session.ts` already returns `has_phone_pin`, and `WorkerHome.tsx`'s `PinSetupNudge` already renders "Set a PIN — Sign in without waiting for a text" off it. `shell-login-phone-otp.ts` only reaches its user lookup after `auth.getUser()` **and** a token-phone-vs-submitted-phone match, so a caller there has already proven possession and telling them about their own account leaks nothing. **The OTP door needed no change at all.** Rule 0.5 applies to UX affordances, not just schema — check what exists before treating a leak as a security-vs-UX trade-off.

**`shell-login-phone-otp.ts` has no equivalent timing gap** — checked on request, same reason as above. Nothing to fix.

**Phone numbers are a cheaper enumeration target than emails.** An AU mobile is a 9-digit space always starting with 4, so the IP half of the dual-key rate limit is the only thing making a sweep expensive — and a distributed prober defeats it. The phone door mattered *more* than the email one, not less.

**I told Royce "not deployed" after merging #1355. That was wrong** — it was live 4.5 min later. Derived from `cdp_enabled_contexts: ["deploy-preview"]` + `deploy_source: "api"`, which is exactly the inference `eq-shell-merge-equals-deploy` (written this same day by a concurrent session, now also in global CLAUDE.md) warns is false. Corrected mid-session against Netlify's own `published_deploy`. **Never re-derive the deploy posture from those two fields — correlate merge time against deploy `created_at`/`published_at`.**

**#1359 went CONFLICTING between opening and merging.** [#1357](https://github.com/eq-solutions/eq-shell/pull/1357) landed in the gap and edited the exact lines, adding `auditLoginFailed(...)` immediately after the bcrypt burn on both doors; seven commits had landed on main. Rebased, kept both edits. **Checked the thing that actually mattered:** both failure branches still do identical work — one bcrypt-cost op, one audit write, identical body. Had #1357 audited one branch and not the other, that would have been a fresh timing difference sitting exactly where one had just been closed. On this repo, re-check mergeability at merge time, not just at open time.

**Shared-checkout race hit again:** `C:\Projects\eq-shell` switched branches between two consecutive commands (`claude/shell-login-dual-key-rate-limit` → `claude/staff-update-authz-gate`). All work was done in isolated worktrees. `git worktree remove --force` leaves a `node_modules` husk behind a Windows file lock — a second `Remove-Item -Recurse -Force` clears it.

**Goals are unset** — `TODAY.md` has no owned goals, so nothing this session was sequenced against a deadline. SEC-1 / SEC-9 / SEC-24 remain open and untouched by this work.

---

# Session 2026-08-15 — audited every sign-in door into Cards, retired the two that weren't real; both halves live

## Built
- **eq-cards PR #246** (squash `e090418`) — fixed the sign-in footer, which claimed "this sign-in is for existing accounts only". False for the mobile path, which creates accounts on purpose. The PR also carried the email-sign-in second door and the single-field refactor from earlier commits on the same branch.
- **eq-cards PR #248** (squash `13bfe54`) — removed `/join` entirely (`JoinTenantScreen`, `JoinContext`, `join_context_notifier`, `AuthRepository.joinTenantExchange`, `AuthFlowNotifier.joinTenant`, `InviteLookupApi`, three redirect exemptions) and the `/claim?tenant=` QR branch (`ClaimByPhoneScreen`). Tokenless `/claim` now redirects to sign-in instead of erroring.
- **eq-shell PR #1361** (squash `84647b81`) — removed the `/admin/workers/qr` route, its lazy import and `AdminWorkerQR.tsx`, plus stale cross-references in `AdminSelfJoinLinks.tsx`.
- **Both deployed.** Cards via `workflow_dispatch` run `31852098680` (both jobs green; the workflow also redeploys every edge function to jvkn). Shell live via the merge itself.

## Decided
- Royce: retire the old redeem-an-invite worker QR and make `AdminSelfJoinLinks` the single QR method.
- Royce: remove the dead `/join` route **and** delete the orphaned files (not just unwire them).
- Royce: leave the four SKS `self_join_codes` exactly as they are — no expiry, instant activate, manager and supervisor included. Raised, considered, declined.

## Deferred
- Neither half click-tested on a real phone — no scan of an old `/claim?tenant=sks` poster, no fresh sign-in walkthrough.
- `eq_cards_lookup_invite_by_phone` still carries anon EXECUTE on jvkn with no caller left (chip `task_5264c029`). Needs a live migration + `cards-api` op removal + `check-tenant-drift.mjs` update.
- Cards' PIN lock screen (`pin_entry_screen.dart`, `app_lock_notifier.dart`, `app_lock_state.dart`) is fully built and mounted by nothing (chip `task_4e685ee7`).

## Notes
**Every modern onboarding door emits `core.eq.solutions/login`, not Cards.** `create/resend/list-worker-invite` go through `shellJoinUrl()` → `?tenant=<slug>`; the role-tagged QR → `?tenant=&role_code=`. `worker-join-url.ts` says outright that it *used to* point at `cards.eq.solutions/claim`. Cards' own onboarding routes are the previous generation — worth remembering before anyone "fixes" a Cards onboarding route again.

**Live counts that drove every call (jvkn, 2026-08-15):** `org_access_requests` 54 rows / 47 approved / newest 13 Aug — this is the live path and it is `/connect`. `worker_invites` 7 rows / 6 claimed / **0 unclaimed and unexpired** — which is why the QR poster dead-ended on every scan. `provision_tokens` 2 rows / 1 used / newest 3 Jul.

**Stale copy outlives the thing it described, and passes review because it was true once.** The footer landed 2026-06-25 in `0a1a26f`; codeless self-signup shipped ~2026-06-27. Two days. Nobody went back. Same shape as `/join`, which `AdminWorkerQR.tsx` deliberately stopped pointing at on 2026-06-10 and which then sat reachable-by-nothing for two months while carrying six live source files.

**The eq-shell "merged but not live" trap caught me, and the CLAUDE.md note is what caught it back.** I told Royce #1361 was "merged but not deployed" — exactly the sentence that note says never to say. `84647b81`'s own Netlify build came back `state: error` / `Skipped`, superseded by concurrent merges, which *looks* like a failed deploy. The real check is ancestry, not your own deploy's state: `84647b81` is an ancestor of `8bf83a79`, the newest `ready` production deploy, so it shipped inside someone else's build. Two `Skipped` production deploys in that same window (`84647b81`, `25b2ef56`).

**Don't build in the eq-shell root checkout.** It was on another session's branch (`claude/staff-update-authz-gate`) with nine live worktrees. Built in a throwaway worktree off `origin/main`, pushed, removed it. Note the worktree needs `pnpm install --frozen-lockfile` (not `npm ci` — pnpm workspace), and a fresh worktree typechecks with ~91 pre-existing `@eq/intake`/`@eq/ai` module-resolution errors because the workspace packages aren't built; filter to your own files rather than reading that as breakage.

**Section left untrimmed deliberately:** the 2026-07-04 "15 July CEO presentation prep" section in `eq/pending.md` had one item closed by this session (the `JoinContextNotifier` live pass, now moot). Its `**Notes (load-bearing):**` block is free-standing prose that isn't part of any bullet, so per the close protocol's own exception the section was left intact rather than trimmed — the item was struck in place instead.

**Brief-gate gotcha:** the flag is date-stamped, so a session running past midnight gets re-blocked mid-work and needs the flag re-stamped for the new date. Hit this once tonight.

---

# Session 2026-08-15 — login outcomes made queryable across all three sign-in doors (eq-shell #1357)

## Built
- `netlify/functions/_shared/login-audit.ts` — shared event vocabulary for every sign-in door, so one alert covers all of them. `login.rate_limited` (lockout), `login.blocked` (credential RIGHT, access refused), `login.failed` (credential wrong), `login.totp_challenge_issued` (first factor passed, no session minted). Split by event NAME rather than one `login.failed` with reasons, because the classes want opposite responses: a run of `login.failed` is an attack, a run of `login.blocked` is a provisioning bug.
- Wired those through `shell-login.ts`, `shell-login-phone-pin.ts`, `shell-login-phone-otp.ts`. Previously lockouts/refusals/half-logins existed only in Netlify function logs.
- Failure rows now carry the attempted address as `detail.identity`. The 30 pre-existing `login.failed` rows were anonymous (`actor_id: null`, IP + reason only) — the trail could not answer *which account was targeted*.
- `writeAuditLogBounded` in `_shared/supabase.ts` — 1500ms ceiling on audit writes in the sign-in path.
- Fixed a forgeable-IP hole in both phone doors (see Notes).
- PR [#1357](https://github.com/eq-solutions/eq-shell/pull/1357), squash `25b2ef56`. Live.

## Decided
- Royce: merge #1350, then retarget #1357 to main. (#1350 had already been merged by a concurrent session as `3e866878`.)
- Royce: merge #1357.
- Royce: deploy to production — which turned out to already be done by the merge itself.
- Royce: clean up the worktree.

## Deferred
- No sign-in has occurred since deploy, so no event has been recorded in practice yet. Deliberately did NOT manufacture a test sign-in: it writes an unremovable row into an append-only, 7-year-retention table for a path already proven by existing rows.
- Nothing alerts on the new events. Both alert queries are written, tested against live jvkn, and live in the module header — but must be run by hand. Wiring an actual alert needs Royce's call on where it lands.

## Notes

**Two `audit_log` tables on jvkn with OPPOSITE exposure, and SEC-1 doesn't say which it means.** `shell_control.audit_log` — policy `deny_all` (`qual: false`), grants **service_role only**, no browser path. `public.audit_log` — `authenticated` has SELECT, policy `users_read_own_audit` scoped to own rows; this is the one SEC-1 flags. Writes here go to `shell_control` (the service client defaults `db.schema='shell_control'`, so a bare `sb.from('audit_log')` lands there). Checking this was load-bearing: it is the reason recording the attempted email/phone is defensible. The same write into `public.audit_log` would be a PII exposure. Verify grants + policies before either panicking about SEC-1 or writing identity into "audit_log". Neither table has a CHECK on `event`, an FK on `actor_id`, or triggers. Memory: `two-audit-log-streams-jvkn`.

**Audit writes bounded, NOT converted to fire-and-forget.** `writeAuditLog` already could not *fail* a login (it swallows everything, never rejects) but it is awaited, so a hung insert stalls a sign-in. Dropping the `await` is the obvious fix and is wrong: Netlify freezes the container on return, which is the exact race the direct-INSERT path was written to escape — it would drop precisely the lockout rows this change exists to keep. Probed all three paths: 8ms insert passes through in 8ms, a 10s hang returns at the ceiling, a throw returns in 1ms, and `clearTimeout` stops the timer pinning the Lambda.

**A rebase nearly reverted #1355's security fix, silently.** #1355 landed on `shell-login-phone-pin.ts` mid-flight, removing `error:'no-pin-set'` and adding an equal-cost dummy bcrypt on the not-found path. This branch predated it and still held the old code, so the rebase conflicted exactly there with the *insecure* version on my side. Taking "mine" would have reverted a fix merged hours earlier and **nothing would have caught it** — not typecheck, not the 330 tests, not any CI job. Resolved by keeping #1355's semantics whole. It also produced a real constraint now documented in that file's header: since #1355 equalised the two failure branches on timing, auditing one but not the other would put a measurable gap back between them, so every failure branch writes exactly one row.

**Forgeable IP on both phone doors — fixed in passing.** `shell-login-phone-pin.ts` and `shell-login-phone-otp.ts` derived `ip` from `x-forwarded-for`, which the caller sets. On phone-pin that value **keyed the IP rate-limit bucket**, so rotating one header bought a fresh allowance and the IP half of its dual key was free to bypass. Both now use Netlify's `ctx.ip`, matching `shell-login.ts`. It is also the `ip` column on every audit row those doors write.

**I repeated the documented merge-vs-deploy error despite the memory existing.** Told Royce "merging doesn't deploy this repo — `cdp_enabled_contexts` is preview-only, so it isn't live yet". Both halves wrong: the build fired 1.6s after the merge and the code was already live. The corrected note was in context and I reached past it, because when you are looking at *this repo* the platform config is right there and reads exactly like a gate. **The tell:** if your evidence for eq-shell deploy behaviour is a config field, you are about to be wrong — correlate merge timestamps against deploy `created_at`, then check ancestry against `published_deploy.commit_ref`. Recurrence appended to memory `eq-shell-merge-equals-deploy`.

**CI does not fire on a PR base change.** `ci.yml` is `on: pull_request: branches: [main]`, and GitHub's default activity types are `opened`/`synchronize`/`reopened`. Retargeting a stacked PR emits `edited`, so no run queues and the PR sits with zero checks. Close+reopen triggers it without an empty commit. Also: lint DOES run in CI, as `continue-on-error` (documented intent: flip to blocking once existing debt is burned down).

**`git branch --contains` again reported merged work as unmerged** (squash-merge artifact, known trap). Before deleting the worktree, verified by content instead — `login-audit.ts` present on `main`, and the three doors carrying 12/9/8 audit call sites.

**The `NO_USER_DUMMY_HASH` duplication chip was raised and then withdrawn — already done.** A concurrent session consolidated it into `_shared/pin-timing.ts` behind `burnPinCompare()`, imported by both PIN doors. Found while verifying the worktree was safe to delete.

**Shared-checkout contention, again.** The eq-shell root checkout was switched to another session's branch with uncommitted work mid-session, so all work ran from an isolated worktree (now removed, branches deleted local+remote). `eq/pending.md` here carried another session's uncommitted edits at close time; they ride along in this commit.

**GOALS were UNSET in `system/TODAY.md`.** Nothing in this session was prioritised or deferred by appeal to any deadline.

---

# Session 2026-08-15 (cont'd) — closed all 3 deferred items from the earlier close, found 2 more live holes on the way

## Built
- `public.eq_update_staff`'s `authenticated` EXECUTE grant revoked on both zaap and ehow — eq-shell PR [#1364](https://github.com/eq-solutions/eq-shell/pull/1364), migration `0245`, all CI green. Traced the grant to its origin: migration `0172` reasserted grants after a `CREATE OR REPLACE` (required by the `eq_enforce_function_privacy` trigger) but asserted `authenticated` instead of `service_role` — that one line was the whole bypass.
- Re-audited all 12 permission keys sharing the "enforced downstream, eq-shell never gates on this" baseline claim (background Explore agent, ~530s, 77 tool calls). Found 2 more real holes, same shape as the field.manage_people bug fixed earlier today: `field.view_hours` and `field.view_licences` were both split out of `field.view` on 2026-07-16 specifically to restrict pay-adjacent/compliance-sensitive data to manager+supervisor, but `entity-rows.ts` (timesheet + licence entities) and `staff-canonical-licences.ts` (the endpoint StaffPage.tsx actually calls) never picked up the narrower key. Fixed — eq-shell PR [#1365](https://github.com/eq-solutions/eq-shell/pull/1365).
- Confirmed `staff-update.ts` fully superseded by `entity-patch.ts` (same fields, same table, better auth model, already had the identity-lock safeguard this file lacked) and retired it on Royce's explicit call (AskUserQuestion, "Retire — delete the file"). eq-shell PR [#1366](https://github.com/eq-solutions/eq-shell/pull/1366).
- Updated `permission-enforcement-baseline.json` across all 3 PRs to keep the ratchet accurate: field.manage_people left dead_keys (PR #1353) → field.view_hours/field.view_licences left dead_keys (PR #1365) → field.manage_people re-entered dead_keys when its only call site was deleted (PR #1366). Net: 13 → 10 dead keys, composition changed to match reality at each step.

## Decided
- Royce: retire `staff-update.ts` rather than repair it (AskUserQuestion, 3 options, "Retire — delete the file" selected).
- Backend-only fix for field.view_hours/field.view_licences shipped now rather than waiting on matching frontend polish — both endpoints degrade acceptably (informative error on Timesheets, silent empty state on Staff licences), server-side is the actual security boundary.

## Deferred
- All 3 PRs (#1364, #1365, #1366) open, none merged/deployed — eq-shell merge auto-deploys, so merge is Royce's call per standing auth-change-approval rule. Independent of each other, any merge order works.
- #1364's migration needs a `tenant-migrate.yml` production dispatch after merge (One Pipe governance).
- #1365's rough edge: StaffPage.tsx's licence query has no client-side gate for excluded roles, degrades to a silent empty state rather than an informative message. Not scoped into the security fix.
- 2 high-severity Dependabot alerts noticed in passing on push, not triaged — spun off as chip `task_e97a18c2`.
- The other 4 `enforced_downstream_field` keys (field.view_rates, field.manage_roster, field.manage_licences, field.manage_labour_hire) and all `enforced_downstream_service`/db-layer entries were checked this pass and held up — but "held up on this pass" isn't "verified forever," per the memory note.

## Notes
- Base rate on the "enforced downstream" baseline category is now 3-of-7 wrong, checked in one afternoon. Worth treating that category as an unverified hypothesis by default from here, not a settled fact — see memory `allowlist-entries-can-mask-holes.md`.
- `entity-patch.ts`'s `staff` entity gates on `field.dispatch`, not `field.manage_people` — verified via `@eq-solutions/roles/roles.json` that both keys are granted to the identical role set (manager, supervisor only), so this is a naming mismatch, not a privilege gap. Not fixed (out of scope, not a real issue).
- Original session's `/close` had already run once today; this second stretch picked up its own 3 "needs you" deferred items on the user's explicit "sprint the needs me items left over" instruction, then expanded to the 2 new findings the re-audit itself surfaced — treated as in-scope since the re-audit was one of the 3 original items, not new work invented mid-session.

---

# Session 2026-08-15 (cont'd) — eq-shell load speed root-caused and fixed; a second Sentry incident's earlier "fix" turned out never to have landed

## Built
- **eq-shell [PR #1351](https://github.com/eq-solutions/eq-shell/pull/1351)** (squash) — pdf.js moved off the eager entry chunk into an on-demand loader with a module-scoped cached promise (`StaffPage.tsx`'s `DocumentThumbnail`, previously a top-level `import * as pdfjsLib from 'pdfjs-dist'` shipped to every page load for a rarely-hit branch). ~26 admin/platform/storage/OCR pages in `App.tsx` converted to `lazy()`, one `<Suspense>` per route tree; login/TenantHome/iframe hosts/Staff/Customers stay eager. 30s `staleTime` on Staff's 6 roster/licence/pending react-query keys, 5min on 2 org-config keys. `Suppliers.tsx` moved off `useState`/`useEffect` onto react-query (new tenant-scoped `opsKeys` factory in `tenantQueryKeys.ts` — it had no cache at all before, so an unscoped key would have leaked the previous workspace's supplier logins across a tenant switch).
- **eq-shell [PR #1356](https://github.com/eq-solutions/eq-shell/pull/1356)** — Plus Jakarta Sans self-hosted via `@fontsource` (latin + latin-ext, weights 400-800), removing the render-blocking `fonts.googleapis.com` stylesheet link from `index.html` (measured 549ms TTFB). `@fontsource` registers the family as exactly "Plus Jakarta Sans", matching `--eq-font-stack` already, so no token/component change needed. CSP's `fonts.googleapis.com`/`fonts.gstatic.com` allowances deliberately left in place — Google Maps JS (used for address autocomplete) injects its own Fonts stylesheet in some versions; tightening needs a live check of that flow first.
- **eq-shell [PR #1358](https://github.com/eq-solutions/eq-shell/pull/1358)** — new `netlify/functions/staff-bootstrap.ts` re-uses the 8 existing Staff-page handlers (entity-rows, staff-pending-connections, self-join-codes, labour-hire-candidates-list, staff-canonical-licences, staff-review-state, org-credential-requirements, org-join-requirements) by invoking each with the caller's own Request and passing status+body through untouched, rather than reimplementing their permission gates — a second hand-written copy of those checks is how they drift. Client half (`src/lib/staffBootstrap.ts`) coalesces only the network layer: all 8 react-query keys, their `staleTime`s and their 10 optimistic `setQueryData`+rollback sites are untouched; the 8 `queryFn`s share one in-flight promise. Follow-up commit `23ebffb4` same PR: the endpoint initially returned 200 with 8 nested 401s for an unauthenticated caller (found by probing the deploy preview, not by review) — added the same coarse signed-in gate every sibling function opens with, before any permission check, so it can only deny earlier, never widen access.
- Bug the tests caught during #1358's build: the in-flight-dedupe handle was first cleared in a `.finally()` chained onto the promise, which runs two microtasks after the caller's await resolves — an immediate refetch after a mutation got the already-settled promise back, exactly the staleness the design existed to prevent. Fixed to a try/finally inside the async body (clears before the promise settles). New test suite `staffBootstrap.test.ts` (7 tests) covers this plus scope isolation, rejection recovery, and per-part 403 passthrough.
- All three merged and confirmed live: production entry chunk 2.28MB to 1.52MB raw (33% smaller), full eager JS graph 781KB to 592KB compressed (24% smaller), `staff-bootstrap` warm GET measured 0.394 to 0.471s (was 8 parallel calls, 6-of-8 cold at ~3s each in testing).
- Separate, unrelated fix, same session: Sentry issues EQ-SHELL-1M/EQ-SHELL-Z re-fired about 21 hours after a prior session's memory claimed the underlying data was already fixed. Root-caused live: `app_data.staff` for the affected worker (Richard Brown, staff_id `ced7fabc...`) still pointed `user_id`/`cards_worker_id` at the deactivated duplicate account — the prior fix never actually reached the database (ruled out `worker-profile-push.ts` as a reverting mechanism; it never writes those two fields). Fixed live in 3 guarded writes across ehow + jvkn: repointed `app_data.staff` to the real account/worker row, deleted the duplicate `public.workers` row (checked first — zero unique data, zero FK dependents across all 8 referencing tables), marked the `identity_collision_flags` row resolved (a write-once log table nothing else clears). Verified post-fix: one worker row for that staff_id, zero unresolved collision flags suite-wide.

## Decided
- Royce: "merge it once CI passes" — #1351 merged.
- Royce: "deploy" — found already live via the merge itself (see Notes).
- Royce: "sprint 1 and 2" (font self-host + staff-bootstrap) — both built.
- Royce: "merge both" — #1356 and #1358 merged.
- On the EQ-SHELL-1M/Z fix, two rounds of explicit approval: first for a narrower "just delete the duplicate worker row" action; live-data verification then showed that would have broken Richard's currently-active `cards_worker_id` pointer, so re-asked with the corrected picture before writing anything — Royce chose repoint staff first, then delete plus resolve the flag, all in one pass.
- `/decide` run twice: "what's next" initially mis-recommended pivoting to SEC-1/SEC-9 before self-correcting against `ops/security-register.md` (both are standing Royce "not now" decisions, not neglected); "do we keep going?" recommended stopping after both threads reached verified completion, flagging the auth-session gap below as a separate, properly-scoped follow-up rather than continuing in-session.

## Deferred
- Real gap found, not fixed: `shell_control.users.active = false` does not revoke a live Supabase Auth session. The deactivated duplicate account kept authenticating and pushing profile updates via `worker-profile-push.ts` two days after being deactivated — that endpoint (and possibly others) trusts bare `auth.getUser()` and never checks the app-layer `active` flag. Needs its own investigation: which endpoints have this gap, and what "deactivate" should actually do to a live session, not a side-effect patch on one data cleanup.
- Load-speed items considered but not built, lower value after the 24% eager-JS reduction already shipped: deferring Sentry init (~82KB, trades a boot-time blind window on an auth hub — real cost, not free), keeping Staff's functions warm via cron (ongoing cost, doesn't fix a true first-of-day cold start), caching the `resolveActingTenantContext` user lookup (small, low-risk, just not done), splitting Staff itself further (traffic-risk trade-off — it's a primary nav item, not an admin page).
- Neither PR click-tested against a real signed-in session — #1358 in particular, the pending-approval and delete-staff optimistic paths are the ones worth exercising.

## Notes

A local `vite build` on this machine cannot be trusted to judge a bundle change. `pnpm run build` / `npx vite build` under this machine's Node 24.14.1 silently emits an entry chunk (~95KB) containing no routes at all — no `createRoot`, no LoginPage, no App.tsx routes — while exiting 0 and reporting success. Netlify builds on Node 22 and is unaffected; confirmed by fetching the PR's own deploy preview and finding a correct, full-size entry chunk with byte-identical vendor-chunk hashes to what Netlify serves. All bundle measurements in this session's PRs came from production/preview fetches, never the local `dist`. Flagged as a spawn-task chip, then withdrawn once root-caused as a local-machine-only issue with no shipping risk.

Grepping a built chunk for `GlobalWorkerOptions` is a false-positive test for "is pdf.js bundled." The on-demand loader assigns `lib.GlobalWorkerOptions = worker.default`, and that property name survives minification, so it appears in the entry chunk whether or not pdf.js itself is present. The reliable markers are pdf.js's own internals — `AnnotationLayer`, `PasswordException`, `RenderingCancelledException`, `PDFWorker`, `TextLayer`.

Two production deploys ("Skipped") that briefly looked like build failures around #1356/#1358's merge window were not failures. Both `error_message: "Skipped"` at the deploy AND build level, `deploy_time: null`, `summary.status: "unavailable"` — Netlify superseding an in-flight build when a newer merge lands within seconds, not a broken build. Both commits confirmed live by ancestry against the newest `ready` production deploy, not by their own deploy's state. Separately swept the last 50 production deploys for genuine failures: zero from a merge; the only real failure in that window was a manual zip-deploy hitting the documented `.git`-is-a-directory worktree trap.

`/decide`'s output was wrong on the first pass because it was missing information, not because the method failed. The six-step pass recommended pivoting to SEC-1/SEC-9 using only the one-line digest description (P0 live PII leak / P0 confirmed exposure) — reading `ops/security-register.md` in full showed both are standing, twice-reaffirmed "not now" decisions (SKS NSW Labour is explicitly not to be touched until Field replaces it; SEC-9 rotation is deferred to a calm window, Royce's 2026-08-01 call). Re-ran the high-value step with the corrected input rather than defending the first answer.

The EQ-SHELL-1M/Z re-fire investigation nearly repeated a documented mistake from the 2026-08-07 Sentry sweep (Emma Curth) — checked against it before acting. That incident's lesson: the "obviously disposable" side of a stray-identity pair (blank fields, no login history) is not safe to assume without querying `last_login_at` and current linkage for both sides, because in that case the blank-looking account had actually become the one current access was wired to. Here the verification (real account's `last_login_at` 16 seconds before the phantom's `created_at`, matching the documented signature exactly) confirmed the phantom really was the phantom — but the check was run explicitly because the pattern was named in memory, not assumed from the account's appearance.

GOALS were UNSET in `system/TODAY.md` all session — both `/decide` passes said so explicitly rather than borrowing a deadline from elsewhere.

Multiple concurrent sessions touched eq-shell tonight (see the earlier entries in this file — staff-update gate, sign-in-door audit, login-outcome auditing, dead-QR-door retirement). No overlap in files touched; `eq/pending.md` picked up other sessions' uncommitted edits at various close points, consistent with the shared-checkout contention pattern already documented in this file's earlier entries.

---

# Session 2026-08-15 (cont'd) — found the anon-grant revoke chip already being executed by a concurrent session; stood down

## Built
(none — verification only, no code shipped by this session)

## Decided
- Ran `/decide`: stand down from chip `task_5264c029` (revoke `eq_cards_lookup_invite_by_phone`'s anon EXECUTE on jvkn — the chip raised earlier today, `eq/pending.md` line 35) rather than duplicate it, once a concurrent session's in-progress commit made the collision unambiguous.

## Deferred
- `task_5264c029` remains open — not closed by this session. Left for the other session to finish and tick off itself.

## Notes
- Independently re-verified the chip's premise before finding the collision: grep across eq-cards, eq-shell, sks-nsw-labour confirmed zero remaining callers of `eq_cards_lookup_invite_by_phone`; live grants on jvkn (`information_schema.routine_privileges`) were `anon, authenticated, postgres, service_role`, all EXECUTE.
- **Two sessions can pick up the same chip in the same shared root checkout without either knowing.** Mid-session, `C:\Projects\eq-cards` (root, not a worktree) went from clean-on-main to an unrelated branch (`chore/close-sign-in-audit-followups`) carrying an untracked migration draft nearly identical to the one about to be written here — then, while still investigating, that draft got committed (`132af30`) by session `local_a3e02740`, which also had uncommitted edits to eq-shell's `cards-api.ts` and `check-tenant-drift.mjs` in a worktree at `C:\Projects\eq-shell-retire-invite-lookup-wt` (branch `chore/retire-invite-lookup-anon`). Caught via `list_sessions` + `search_session_transcripts`, confirmed via `get_session` showing `lastActivityAt` ticking forward live, closed the loop with `send_message` before standing down.
- `132af30` also folded in the adjacent chip `task_4e685ee7` (Cards' dead PIN lock screen), deleting `pin_entry_screen.dart` + `app_lock_notifier.dart` + `app_lock_state.dart` + two orphaned providers. Not this session's work; noted here so it isn't re-flagged as still open.
- Did not touch `eq/pending.md`'s `task_5264c029` line — still genuinely open (eq-shell half uncommitted as of last check) and owned by the other session; editing it here would just re-create the same shared-file race this note describes.

---

# Session 2026-08-15 (cont'd 2) — merged and deployed all 3 staff-update PRs

## Built
- Merged and deployed all three PRs from the prior stretch, on explicit instruction ("merge all three"): [#1364](https://github.com/eq-solutions/eq-shell/pull/1364) (GRANT revoke, commit `0687f2ca`), [#1365](https://github.com/eq-solutions/eq-shell/pull/1365) (field.view_hours/licences gate fix, commit `636f7fc3`), [#1366](https://github.com/eq-solutions/eq-shell/pull/1366) (retire staff-update.ts, commit `04f34504`). All three confirmed live via commit-ancestry check against `origin/main`, and #1366's own production deploy confirmed `ready` via a background poll.
- Merged in dependency-safe order: #1364 first (no file overlap), then #1365, then #1366 — #1365 and #1366 both touched `permission-enforcement-baseline.json`. Rather than trust GitHub's line-merge on the overlap, rebased #1366 onto post-#1365 `main`, hand-reconciled the file to the correct final state (11 dead keys, both narratives merged into one coherent chain), re-ran the drift test (5/5 pass) and typecheck before force-pushing and merging.

## Decided
- (Royce, via "merge all three") — ship all three fixes to production now, accepting the DB-grant migration lands as code but still needs a separate dispatch.

## Deferred
- `#1364`'s migration still needs a `tenant-migrate.yml` production dispatch — merging the PR only lands the migration file, it doesn't apply the grant change to zaap/ehow. One Pipe governance, Royce's explicit go.
- `#1365`'s rough edge (StaffPage's licence query has no client-side gate for excluded roles, degrades to a silent empty state) — real but not urgent, unscoped from the security fix.
- 2 high-severity Dependabot alerts — background triage (`task_e97a18c2`) ran in a separate session while this one continued; its outcome isn't visible from here.

## Notes
- Confirmed the git-merge conflict-avoidance approach (rebase + hand-reconcile a shared file rather than merge-and-hope) worked exactly as intended: the array field (`dead_keys`) auto-merged correctly on its own (git's line-level 3-way merge happened to produce the right 11-entry list), but the two prose fields needed genuine human/AI judgment to combine two true-but-partial narratives into one coherent one. Worth remembering as the pattern for any future baseline-file overlap across parallel PRs.

---

# Session 2026-08-15 — a changed mobile number no longer forks a worker in two; email as a second sign-in door; both apps deployed

## Built
- **eq-shell #1347** (`eb5a1348`) — admin phone change now writes the GoTrue login identity as well as the `shell_control.users` mirror, with a compensating revert if the auth write fails. Root cause of the fork: Shell resolves users by phone so it kept working, while Cards keys off `auth.uid()` and saw a stranger. Merge conflicted with #1349 (both additive at the same insertion point after `updated = data;`) — kept both, audit block first.
- **eq-shell #1352** — new `admin-change-login-phone.ts` (+889 with tests). Platform-admin **and** TOTP required, self-edit forbidden, mandatory reason + recorded confirmer, audit entries on denials as well as successes, same revert-on-failure write pattern as #1347. Answers the identity-proof question the chip explicitly left open.
- **eq-shell #1353** — `staff-update` regated from `field.view` (all six roles hold it) to `field.manage_people` (manager + supervisor).
- **eq-cards #246** (`e090418`) — one-field sign-in taking either a mobile or an email, `identifierKind()` routing, `shouldCreateUser: false` on the email path. Opened by me as #245 (parked), reopened and merged by Royce.
- **Deploys** — Cards `e090418`; Core deployed twice (`81b3a1a`, then `7d0e065` after merging #1352/#1353). All smoke-verified.
- **Suite-wide drift audit** across both repos, five classes: orphaned UI, unreachable capability, comment drift, latent SDK defaults, twin fields.
- **Artifact: EQ Access Map** — every sign-in door, invite route and identity record, read from live rather than docs.

## Decided
- (Royce) Keep Cards mobile-only "for now" — then merged the email door himself and deployed it. Net outcome: shipped. #245 and #247 closed as superseded.
- (Royce) Run the drift audit once now; codify a skill afterwards, from real findings rather than five examples.
- (Royce) Merge #1352 and #1353, then deploy Shell.

## Deferred
- `admin-change-login-phone` live but never exercised on a real account — first genuine run will be someone's actual login.
- Email door reaches 22 of 73 accounts, 0 of 6 apprentices. Blocker is a verified add-an-email flow, not the sign-in screen.
- Cards' `licence_card.dart` — 404 lines, six classes, a maintained test file, nothing displays it.
- Shell's intake staging review flow — approve and reject both fully written, neither called by any UI.
- Drift-audit skill not built.

## Notes
- **Deploy posture re-confirmed — F13 stands, do not re-derive it from this session.** Merging to eq-shell `main` **does** auto-deploy. Evidence: `7d0e065` has *two* production deploys 76 seconds apart (23:16:04 and 23:17:20) and I only triggered one — the merge fired the other. My manual `createSiteBuild` was redundant and created the duplicate. Every row in `listSiteDeploys` reads `deploy_source: api`, which is exactly the trap the CLAUDE.md note warns about: "api" does not mean human-triggered. **Nothing in this session is evidence against auto-deploy.**
- **Do not use `netlify deploy --prod --build` locally for eq-shell.** Two blockers: the build command's `corepack enable` fails `EPERM` (needs admin rights to write into `C:\Program Files\nodejs`), and more seriously it would bake this machine's `.env.local` into a production bundle — including `VITE_ANTHROPIC_API_KEY`. If a manual trigger is ever genuinely needed, `netlify api createSiteBuild` builds server-side with the production environment.
- **The Netlify MCP `deploy-site` operation deploys from the session's cwd.** Running it from a session rooted in `eq-cards` would have pushed Cards content to `core.eq.solutions`. Avoided deliberately; `createSiteBuild` has no such coupling.
- **`staff-update` has never worked.** Its client is bound to schema `app_data` while `eq_update_staff` lives in `public`, so PostgREST answers PGRST202 and it 500s. My initial claim that any worker could exploit it *through that endpoint* was wrong — the gate was still wrong and worth fixing, but the live exposure is one layer down.
- **The real exposure is the grant.** `public.eq_update_staff` on ehow is `SECURITY DEFINER` whose only authorisation is `tenant_id` from the JWT, and it carries `GRANT EXECUTE TO authenticated`. `authenticated` holds EXECUTE on 140 public functions there, ~30 of them writes (`eq_delete_customer`, `eq_delete_quote`, `eq_update_customer`…). Within-tenant only; not cross-tenant, not anonymous, not login identity. Raised as a chip; a separate session has since landed #1364 to revoke, still pending a `tenant-migrate.yml` dispatch.
- **Phone identity is clean** — 0 real divergence across 53 comparable auth/worker pairs, and 0 between auth and Shell's mirror. An earlier "26 diverged" figure was an artifact of comparing `0412…` against `61412…`; corrected by comparing the last 9 digits.
- **Two email fields, one meaning.** `workers.email` is a contact detail (73 of 101 populated); `auth.users.email` is the login (17 of those match). 58 people have an email on record that will not sign them in, and one pair already disagrees.
- **Chips need a base branch, not just a cwd.** One chip spawned with `cwd: C:\Projects\eq-cards` committed onto whatever branch that checkout happened to be on — a parked feature branch — so its fix arrived entangled with a change Royce had declined. Name the base branch explicitly in future chips.
- **Drift-audit heuristics have a high false-positive rate**: 4 of 6 orphaned-widget candidates were false positives (factory constructors, static `.show()` helpers, same-file usage). Verification is the job, not the pattern-matching.

---

# Session 2026-08-15 — retracted the false "eq-shell doesn't auto-deploy on merge" claim across the substrate

## Built
- Corrected the inverted deploy claim in all 6 places it had propagated: `eq/pending.md` (source), `digest.md` (auto-loaded into every session), `eq/changelog/eq-shell.md` (#1346 heading + bullet → MERGED **+ DEPLOYED**), `sessions/2026-08-14.md` (3 statements), `system/worktree-registry.md`.
- `rules/deployment.md` — recorded the verified merge-equals-deploy behaviour in the substrate for the first time, plus the `deploy_source`/`cdp_enabled_contexts` inference trap and the commit-ancestry liveness check. EQ Shell row now states the approval gate bites at the merge click.
- `system/failures.md` — added **F13** (`recurrences: 2`, `rung: 1`, `target_rung: 4`) so `guard-ratchet.yml` proposes a promotion. Signal regex tested 11/11 for precision/recall against real corpus lines.
- Shipped as `b4494da`; verified against a clean clone of `origin/main` afterward (0 stale items across eq/sks/ops pending + digest).

## Decided
- Royce, via `/decide` + AskUserQuestion: close the session rather than start the F13 guard, the `pre_tool_use.py` fix, or the security list.

## Deferred
- **The F13 guard is not built, deliberately.** A wording-scan across 629 open EQ items + 100+ session logs needs an adversarial false-positive pass first — a noisy gate on this repo blocks every session's push. F12's note already says this guard class needs adversarial-test-grade work, not a quick add-on.
- **`hooks/pre_tool_use.py` fires on three distinct false positives — worse than previously logged.** (1) cwd-anchoring: it resolves against the session cwd (`C:/Projects/eq-context`) rather than the actual target, so it blocked the F9-guarded git operation from a genuinely isolated worktree AND from a fresh clone — the exact escape valve its own error message recommends. (2) content-matching: it then blocked a `cat >> session-log` heredoc purely because the log *text* quoted the guarded command while narrating this incident. A guard that blocks you for writing about it is unfalsifiable from inside a session. Same bug class as `guard.js`'s worktree detection, logged 2026-08-14 and still unfixed. Cost three blocked attempts this session and forced two workarounds. **A rung-4 guard that misfires this often trains sessions to route around it — that is how guards decay into theatre.**

## Notes
**Verification, not deference.** Ground truth was checked directly rather than taken from the correction note: `25b4a0fb` (PR #1346) merged 2026-08-14 11:18:41 UTC, deploy fired 11:18:43, and it is an ancestor of the currently-serving deploy. Matches the `#1346 11:18:41→43` line in global CLAUDE.md's evidence table.

**Scope boundary held.** `eq-cards` and `eq-receipts` genuinely DO require a manual Netlify trigger — those entries were deliberately left alone. The F13 signal regex was explicitly tested not to fire on them. The naive grep from the task brief matched ~40 lines; only 7 were actually the false claim.

**The highest-consequence copy wasn't in the brief's list.** `system/worktree-registry.md` annotated OPEN auth-change PR #1352 with "core.eq.solutions is manual-deploy-only" — exactly the "auth change safely un-deployed when it is already serving production" hazard. On this repo, merging that PR *is* the auth deploy.

**Session-log corrections are annotations, not rewrites.** The 3 statements in `sessions/2026-08-14.md` were struck through with the correction beside them. The mistaken belief is itself the lesson; deleting it would erase the evidence that this failure mode is real and recurring.

**The digest needed no generator change.** `refresh_digest.py`'s `pending_open_items()` only reads `- [ ]`/`- [~]`, so closing the item removes it from "Waiting on you" automatically. Confirmed by running the real function against the edited file.

**Commit body was lost to a push race.** Three concurrent pushes landed mid-work; a message-file fallback fired and only the subject line survived on `b4494da`. Not amended — that would need a force-push to shared `main`. The full rationale lives in the files, which is the better home anyway.

---

# Session 2026-08-15 (cont'd 3) — dispatched the pending DB migration, closed out Dependabot

## Built
- Dispatched `tenant-migrate.yml` (fleet-wide apply, on explicit instruction). Applied 2 pending migrations to both zaap and ehow: `0244_drop_quote_attachment.sql` (an unrelated, already-merged, pre-verified table drop that had been queued since PR #1331 but never dispatched) and `0245_revoke_eq_update_staff_authenticated.sql` (this session's GRANT revoke, from #1364). Run succeeded in ~15s per plane; verified live via direct query — `authenticated` is gone from `eq_update_staff`'s grants on both planes, only `service_role`/`postgres` remain. Ledger confirms both applied at 03:00:01/03:00:04 UTC.
- Checked the Dependabot triage chip (`task_e97a18c2`, spun off earlier this session, completed in a separate session): both high-severity `image-size` alerts (#193/#194) were already resolved a week ago via a pnpm patch (commit `46829c6f`, PR #1288) that the base-version-only Dependabot scanner can't see, and the vulnerable code path isn't reachable in production anyway (only loads via Netlify's local dev blob emulator). Spot-verified the commit and patch file both exist before accepting the finding. Nothing to do.

## Decided
- Royce, via "dispatch the tenant-migrate.yml for #1364" — explicit go on the production DB migration dispatch, per One Pipe governance.

## Deferred
- `#1365`'s rough edge (StaffPage's licence query has no client-side gate for excluded roles) remains the only open item from today's staff-update thread — cosmetic, not urgent.

## Notes
- The staff-update security thread (started as a single finding against one endpoint) is now fully closed end-to-end: app-layer gate fixed (#1353), the RPC's separate GRANT-bypass door closed in both code and the live database (#1364 + dispatch), two more live read-exposure holes of the same shape found and fixed (#1365), the dead endpoint itself retired (#1366), and the one adjacent distraction (Dependabot alerts noticed in passing) triaged to a confirmed non-issue. Nothing outstanding except cosmetic UX polish.
- A fleet-wide migration dispatch applies EVERY pending migration, not just the one you're thinking about — worth checking `--plan` output before dispatching if a queue might have accumulated (it had here: 0244 rode along with 0245).
- This close needed an isolated worktree (`git worktree add`) to commit from — the shared eq-context checkout was 3 commits behind origin/main (two nightly bot commits plus a concurrent session's own close, which happened to touch the same two files: pending.md and today's session log) and this repo's F9 guard blocks `git rebase` in the shared root outright. Re-applied the same edits fresh against the concurrent session's already-pushed content rather than force through a merge conflict.
