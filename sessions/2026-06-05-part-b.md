---
title: Session log — 2026-06-05 (part b)
owner: Royce Milmlow
scope: PostHog MCP setup + data read + EQ Core weekend go-live readiness
status: live
---

# 2026-06-05 (part b) — PostHog MCP, data read, and EQ Core go-live runbook

> Concurrent with `sessions/2026-06-05.md` (EQ Field job numbers v3.5.73/74) — separate session, separate topic.

**Built by:** Royce Milmlow + Claude Code (Cowork)

## Done

### 1. PostHog MCP connected
- Added PostHog as a claude.ai **custom connector** (OAuth) → `https://mcp.posthog.com/mcp`,
  EU-routed via login. Confirmed live: org **EQ**, project **162632** (`eq.posthog.com`).
- **Housekeeping flag:** the connector is **mislabeled "Github"** in the claude.ai connector
  list — rename to PostHog when convenient. (Two pre-existing personal API keys exist —
  `claude-code-read-only`, `claude-cowork-readonly` — but cookie/OAuth made them unnecessary.)

### 2. Data read (eq-production)
- Usage is real and growing: ~250–900 pageviews/day in June (10× since mid-May), 16–43
  daily uniques. `$identify` spiked to 47–103/day = auth flow heavily exercised.
- **Retention:** small but sticky — flat tail (users still returning week 5). ~19 real
  recurring humans (the `tenant:handle`-identified Field users), not the inflated 419 UUIDs.
- **Top surfaces:** auth (handoff 155, signin 124), then Service `/maintenance`, Field `/licences`.

### 3. Two ingestion warnings diagnosed (both → non-blockers)
- **"Refused to merge an already identified user"** — NOT missing reset (all 3 apps call
  `posthog.reset()`). Real cause: each app identifies the same human under a different
  distinct_id (Shell UUID / Field `tenant:handle` / Service id). Confirmed by SQL: 19 handle
  ids vs 419 uuids. → **deferred, spun off as a task.**
- **`/` = ~80% of pageviews** — NOT a redirect loop. EQ Field is an SPA at `/` that never
  changes the URL + double-fires `$pageview` (autocapture + manual). → **deferred, spun off.**

### 4. Go-live readiness verified against LIVE systems
- **Canonical DB** (`jvknxcmbtrfnxfrwfimn`): healthy, populated (workers 38, creds 779,
  invites 37, tenants 4), all RLS-enabled, **0 ERROR security advisors**.
- **Deploy topology:** `core.eq.solutions` = eq-shell, iframes Field/Service/Cards/Quotes/
  SKS-Labour (CSP `frame-src` in eq-shell/netlify.toml is the map).
- **Service iframe SSO** — the #1 risk — is **already engineered**: host-gated cookie policy
  (`lib/auth/shell-cookies.ts`), ready cutover runbook (`shell-service-domain-cutover.md`).
- **Anon SECURITY DEFINER RPCs audited** (live defs): `delete_account` + `get_worker_hr_record`
  correctly self-authorise via `auth.uid()` (anon → no-op/empty); `preview_invite` token-gated
  & safe; **`claim_invite`** lacks an `auth.uid() IS NULL` guard (low/medium — optional hardening).

### 5. Runbook written
- **`eq/go-live-runbook.md`** — consolidated, live-verified weekend go-live runbook
  (verified-green gates, open gates §B, cutover/deploy sequence, smoke tests, rollback, watch).

## Decisions
- **Distinct_id unification deferred, not done inline** — it's a cross-app identity decision
  + data-migration nuance, and the repos were on in-flight/wrong branches (eq-shell dirty on
  `claude/b4-validate`, eq-field on `revert/licence-admin`). Pushed back on "fix now."
- **PostHog connector route chosen** (OAuth) over local `claude mcp add` — matches the
  existing Figma/Supabase/Netlify connector pattern; no API key to manage.

## Open — the weekend hinges on these (in the runbook §B)
1. 🔴 **`EQ_SECRET_SALT` parity** (Shell vs Service) — the silent #1 go/no-go, never compared.
2. Finish the **Service domain cutover** env steps (DNS/TLS, `NEXT_PUBLIC_SITE_URL`, Supabase
   URL allowlist) — confirm Service prod project (`urjhmkhbgaxrofurpbgc` lists as "-dev").
3. 🟠 **MFA-bypass posture** — PIN-only Shell user reaches Service CMMS single-factor; accept
   knowingly or gate behind mandatory Shell-TOTP.

## Spun off (post-launch tasks)
- Unify cross-app PostHog distinct_id (3 repos).
- Fix EQ Field double `$pageview` capture.
- Optional: `claim_invite` `auth.uid()` null-guard.
