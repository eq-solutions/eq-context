# 2026-06-07 — SKS Live roles sprint: handoff doc + agent prompts (verified, saved)

**Built by:** Royce Milmlow + Claude Code (worktree `romantic-haibt-8b9d78`)
**Tier:** EQ (cross-tier — EQ platform serving SKS as first live tenant)

## Decided / built
- New **[sks-live-sprint-2026-06-07.md](../sks-live-sprint-2026-06-07.md)** at repo root — the shared source of truth for the **roles / security-groups track** of SKS Live (sibling to, not a replacement for, `SKS-CUTOVER-CRITICAL-PATH.md`, which is the Field schema + live-data track). Holds the repo/action table, order of operations, and the five agent prompts **A–E** (converge eq-shell branches → Phase 2 session `extra_perms` → Phase 3 `AdminSecurityGroups` → Phase 4 go-live walk → Phase 5 hardening).
- Royce copy-pasted all five prompts to dispatch the agent sessions.

## Verified live before saving (per Rule 0.5 / §7)
- Project refs confirmed: `sks-canonical`=`ehowgjardagevnrluult`, `eq-canonical`=`jvknxcmbtrfnxfrwfimn`, `eq-canonical-internal`=`zaapmfdkgedqupfjtchl`, SKS LIVE `sks-labour`=`nspbmirochztcjijmcrx`. (The earlier doubt about the `sks-canonical` ref was unfounded — it's real.)
- `shell_control` @ eq-canonical: `security_groups`=**9**, `security_group_perms`=**16**, `user_security_groups`=**0** rows.
- `user_security_groups` columns: `user_id, group_id, assigned_by, assigned_at`.
- Tenant `sks` = **3 members, all `manager`**.
- `app_data.contact_customer_links` @ sks-canonical: `ccl_tenant` (ALL) has `with_check=null`; USING qual casts `::uuid`.

## Refinements baked into the prompts (drift fixes)
- Phase 3 admin write must populate `assigned_by` + `assigned_at` (audit trail).
- Phase 5 `WITH CHECK` corrected to cast `::uuid` to match the existing USING qual / uuid column.

## Not verified (flagged as leads in the doc)
- GitHub-side: eq-roles PR #7 / branch name / "91 tests"; eq-shell branch divergence counts.

## Session end
- Saved + changelog updated; merged to `main` and pushed (substrate syncs ~60s).
