# 2026-06-07 — SKS Live roles sprint: handoff doc + agent prompts (verified, saved)

**Built by:** Royce Milmlow + Claude Code (worktree `romantic-haibt-8b9d78`)
**Tier:** EQ (cross-tier — EQ platform serving SKS as first live tenant)

## Decided / built
- New **[sks-live-sprint-2026-06-07.md](../sks-live-sprint-2026-06-07.md)** at repo root — the shared source of truth for the **roles / security-groups track** of SKS Live (sibling to, not a replacement for, `SKS-CUTOVER-CRITICAL-PATH.md`, which is the Field schema + live-data track). Holds the repo/action table, order of operations, and the five agent prompts **A–E** (converge eq-shell branches → Phase 2 session `extra_perms` → Phase 3 `AdminSecurityGroups` → Phase 4 go-live walk → Phase 5 hardening).
- Royce copy-pasted all five prompts to dispatch the agent sessions.

## Verified live before saving (per Rule 0.5 / §7)
- Project refs confirmed: `sks-canonical`=`ehowgjardagevnrluult`, `eq-canonical`=`jvknxcmbtrfnxfrwfimn`, `eq-canonical-internal`=`zaapmfdkgedqupfjtchl`, SKS LIVE `sks-labour`=`nspbmirochztcjijmcrx`. (The earlier doubt about the `sks-canonical` ref was unfounded — it's real.)
- `shell_control` @ eq-canonical: `security_groups`=**9**, `security_group_perms`=**16**, `user_security_groups`=**0** rows.
- `user_security_groups` columns: `user_id, group_id, assigned_by, assigned_at`.
- Tenant `sks` = **3 members, all `manager`**.
- `app_data.contact_customer_links` @ sks-canonical: `ccl_tenant` (ALL) has `with_check=null`; USING qual casts `::uuid`.

## Refinements baked into the prompts (drift fixes)
- Phase 3 admin write must populate `assigned_by` + `assigned_at` (audit trail).
- Phase 5 `WITH CHECK` corrected to cast `::uuid` to match the existing USING qual / uuid column.

## Not verified (flagged as leads in the doc)
- GitHub-side: eq-roles PR #7 / branch name / "91 tests"; eq-shell branch divergence counts.

## Session end
- Saved + changelog updated; merged to `main` and pushed (substrate syncs ~60s).

---

# 2026-06-07 — shift.started enrichment: backfill, durable emitter, prompt fix

**Built by:** Royce Milmlow + Claude Code

## Problem
The EQ Shell dashboard AI-briefing "On Shift Now" panel (`eq-shell/netlify/functions/ai-briefing.ts`)
was always empty for SKS. It reads `shift.started` canonical_events and the prompt told the model
to "extract name and site from payload" — but the live SKS payload held only job-code -> headcount
(`{"assignments":{"SY5":8,...},"scheduled_count":150}`), no names or human site names.

## Key finding
**No `shift.started` emitter exists anywhere** — ruled out across all repos, DB functions, pg_cron,
and edge functions in both planes. The handful of `shift.started` rows in the sks-canonical data
plane (`ehowgjardagevnrluult`, `app_data.canonical_events`) are **seed/demo data**. The two docs the
task referenced (`docs/ai-briefing-emit-enrichment.md`, `eq-context/cross-repo-contracts-*.md`) do
not exist.

## Data planes (verified)
- **SKS roster** lives in `nspbmirochztcjijmcrx` (SKS LIVE op DB): `public.schedule` (one row per
  person/week; text day cells `mon`..`sun` hold a site code or a leave/admin token like A/L, RDO,
  TAFE, SICK, N/A). `public.sites.abbr` -> `public.sites.name` is the code->human-name map.
  Week key format = `DD.MM.YY` of the week-commencing Monday (e.g. `15.06.26`).
- **canonical_events** live in the separate `ehowgjardagevnrluult` plane (SKS tenant_id
  `7dee117c-98bd-4d39-af8c-2c81d02a1e85`). Any durable writer needs a cross-project hop — done here
  over HTTP via the eq-shell canonical-api, not a direct DB link.

## Shipped — Round 1 (prior session, "backfill now")
- Built `public.eq_field_shift_payload(p_week, p_day, p_on_shift_limit default 6)` in SKS-live.
  Returns the existing shape PLUS `sites` (code->name) and `on_shift` ([{name,site}], spread
  one-per-busiest-site, bounded). SECURITY INVOKER; EXECUTE -> service_role only.
- Backfilled seed rows (ids 22 thu, 26 fri) additively and inserted one fresh in-window event
  (id 31, `_demo_seed: true`) so the panel populates. Cleared `briefing_cache`.

## Shipped — Round 2 (this session, "sprint all")
1. **Durable emitter** — `public.eq_field_emit_shift_started(p_dry_run default false)` + daily
   pg_cron `eq-field-shift-started-daily` @ `30 19 * * *` UTC (= 05:30 AEST, before the brief).
   Computes today's roster in Australia/Sydney, posts `shift.started` to the eq-shell canonical-api
   (`X-Tenant: sks`, `Bearer CANONICAL_API_KEY_FIELD` -> server stamps app_source=field). Mirrors the
   existing `supervisor-digest-weekly` cron pattern.
   **Guarded inert**: no-ops (a) when no one is rostered (weekends/holidays) and (b) until two
   `app_config` rows exist, so the cron never error-spams before the key is added.
2. **SQL saved to disk** — `sks-nsw-labour/migrations/2026-06-07_eq_field_shift_payload.sql` and
   `..._eq_field_shift_started_emitter.sql` (previously live-only in the migration ledger).
3. **Prompt fix** — `eq-shell/netlify/functions/ai-briefing.ts`: the system prompt + `on_shift`
   tool-schema description now read the `on_shift` array (resolved name+site) with a `sites`-map
   fallback, never raw codes. Uncommitted on branch `claude/b4-validate` — needs to be moved to its
   own branch / cherry-picked before it lands.

## To activate the emitter (Royce-gated)
Run once in SKS-live with the real key from eq-shell's Netlify env var `CANONICAL_API_KEY_FIELD`:

    INSERT INTO app_config (key, value) VALUES
      ('canonical_api_url',       'https://core.eq.solutions/.netlify/functions/canonical-api'),
      ('canonical_api_key_field', '<CANONICAL_API_KEY_FIELD value>')
    ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;

Verify the canonical-api public path before inserting `canonical_api_url`. Then
`SELECT public.eq_field_emit_shift_started(true);` to dry-run, and `(false)` to fire for real.

## Notes / decisions
- EQ-as-tenant for SKS (B5) is the live plan, so SKS roster -> EQ canonical is consistent, not
  entity-mixing. No EQ app code was ported into the SKS repo — only SKS-live DB DDL was added.
- Pre-existing advisory: `app_data.migration_baseline` (ehowg plane) has RLS disabled — addressed
  this session (service_role bypasses RLS; no client path).
- The seed `_demo_seed` rows can be deleted once the live emitter has run a real morning.

---

# 2026-06-07 (part B) — migration_baseline RLS hardening rolled out across both tenant planes

**Built by:** Royce Milmlow + Claude Code · scope: EQ Shell canonical spine

## What landed
Finished the `app_data.migration_baseline` RLS hardening begun in eq-shell PR
[#200](https://github.com/eq-solutions/eq-shell/pull/200) (now merged, `32d77e4`). The table is
pinned to the canonical posture — **RLS ON, zero policies, service_role-only grants** — durably in
three files (0037 edited for fresh tenants, new forward migration `0039_migration_baseline_rls`,
and the `SERVICE_ROLE_ONLY` exception in `scripts/regen-tenant-baseline.mjs`) AND now rolled out to
both live tenant planes via the One Pipe.

## Rollout (One Pipe only — no hand-applied SQL)
1. **PR #200** already merged to `main` before this session.
2. **Ledger reconcile** — dispatched `tenant-migrate.yml reconcile_ledger=true` (whole fleet),
   approved the `production` gate (run
   [27080536412](https://github.com/eq-solutions/eq-shell/actions/runs/27080536412)). Both tenants:
   renamed 3 rows (`0036/0037/0038` basename → `.sql` + re-checksum), 0 dedupe, leave-pending 1
   (`0039`). This normalised the basename-vs-`.sql` mismatch so the apply was checksum-clean.
3. **Apply** — dispatched `tenant-migrate.yml` (reconcile_ledger=false, whole fleet), approved the
   gate (run [27080567537](https://github.com/eq-solutions/eq-shell/actions/runs/27080567537)).
   Summary: **core 1 applied (`0039`), 38 skipped · sks 1 applied (`0039`), 38 skipped** — no
   checksum-drift, no warnings. `0037` was skipped (edited file matched the reconciled checksum).

## Verified live (read-only, both planes)
| | zaap (EQ `zaapmfdkgedqupfjtchl`) | ehow (SKS `ehowgjardagevnrluult`) |
|---|---|---|
| `migration_baseline.relrowsecurity` | true | true |
| policy_count | 0 | 0 |
| `0039_migration_baseline_rls.sql` recorded | yes | yes |
| `0037` ledger rows | 1 (`.sql` only, no dup basename) | 1 (`.sql` only, no dup basename) |

- **Drift gate** (`tenant-drift.yml`, run
  [27080591898](https://github.com/eq-solutions/eq-shell/actions/runs/27080591898)): green —
  **"✓ SPINE identical across all tenants"**, anon-grant invariant clean on all 3 planes, both
  tenants show all 39 repo migrations applied with no out-of-band entries.

## Advisor note (accepted by design — not "cleared")
Supabase `get_advisors` security still reports `rls_enabled_no_policy` (level **INFO**) for
`app_data.migration_baseline` on both planes. **This IS the canonical posture**: RLS on + zero
policies on a service_role-only table means no rows are reachable by anon/authenticated
(service_role bypasses RLS). It cannot be silenced without either adding a policy (re-introducing
the exact drift this effort eliminated) or disabling RLS (undoing the hardening). Triaged as
accepted; no DB change made. This supersedes the stale "RLS disabled — addressed this session" note
in part A above — the posture is now pinned identically on both planes via the pipe.

## Closeout
- eq-shell worktree `.claude/worktrees/migration-baseline-rls` deleted (branch merged).
- No deploy beyond the gated tenant-migrate runs; no app code changed.

---

# 2026-06-07 (part C) — Cross-app linkage audit (Cards/Shell/Field/Service/Quotes)

**Built by:** Royce Milmlow + Claude Code · scope: suite-wide · **read-only, no changes made**

## What was done
Live-verified map of how the 5 apps are (and aren't) linked, against 4 Supabase projects + 5 repos. Outputs:
- [`cross-app-linkage-audit-2026-06-07.md`](../cross-app-linkage-audit-2026-06-07.md) — full report (security flags, per-project DB audit, code seams, linkage map, gaps).
- [`cross-app-linkage-remediation-plan-2026-06-07.md`](../cross-app-linkage-remediation-plan-2026-06-07.md) — gated 7-priority playbook.

## Key verified findings (live wins)
- **Mechanism:** soft `*_id`/`canonical_id`/`external_id` cols + HTTP to Shell `canonical-api`. No cross-DB FKs, no realtime/webhook bus. Event bus = `canonical_events` via that HTTP hop (Quotes + Service emit; ai-briefing/dashboard read).
- **Canonical model is built but its linking rows are empty:** `ehow.app_data` fully FK-wired, but `jobs` = 0 and `quote` = 0. So **quote→job→timesheet is untraceable**; live quotes are in `ehow.public.sks_quotes` (Flask), not `app_data.quote`.
- **Worker→Field-person untraceable:** `app_data.staff.cards_worker_id` 1/50, legacy `ktmj.people.worker_id` 0/605. **GATE A still open** — `workers.user_id` 4/38, `shell_control.users` = 5.
- **Customer fragmented** across 3–4 stores; `sks_quotes_customers.canonical_id` 0/520 in live ehow. **Sites→customer** 28/591 in `app_data`.
- **Works:** Service→canonical **asset sync live** (`app_data.assets` 4808, 100% linked to a site, 713 test results); Quotes customer dual-write + events; Shell SSO/tenant_routing; `nspb.people.canonical_id` 49/60.
- **Service runs off-topology** on `urjhmkhbgaxrofurpbgc` (the substrate's own project) — not audited for RLS.
- Cards→Field approval bridge writes to **legacy ktmj** (dead) not the live plane — misdirected (2 approval rows).

## Drift caught (docs vs live)
- `architecture.md` "jvkn = no operational data" is **false** — jvkn.public holds the worker HR house.
- creds 779→**737**, invites 37→**58** since the 06-03 snapshot.
- `0028_contact_customer_links` is **present on SKS** (`app_data.contact_customer_links` 291) — contradicts the 06-02 "missing on SKS" claim.

## Security (confirms, not new)
- S1/S2 anon-write exposure on `ktmj` (legacy, 605 PII rows) + `nspb` (**SKS live**) = the **known trailing edge** of the Field anon-remediation (`eq/pending.md` 2026-06-03 PM). Exact policy worklist in the remediation plan §7. ktmj closes by the already-pending pause/decommission (after the bridge is repointed); nspb by the SKS anon-remediation track.
- jvkn anon-executable `eq_cards_get_worker_hr_record` / `eq_cards_delete_account` — audit their internal authz.

## Gated (nothing executed)
No live writes/DDL, no app edits, no deploys, no commit/push. SKS-live read-only under the explicit audit instruction. Next-step decisions surfaced to Royce (run safe backfill dry-runs / Service advisor pass / draft eq-shell bridge PR).

## Continuation — steelman pass + sprint (Royce: "10/10 only, build a sprint, continue")
- **Red-team review** of the audit caught real overclaims; corrected in audit §1a (supersedes body where noted):
  1. **Customers reconcile 519/520 by exact name** (abn empty, 0 usable) — NOT disjoint as first written. They're one
     customer population under different surrogate keys → P2 is a tractable **name-keyed backfill**, not fuzzy dedup.
  2. **"No jobs anywhere" overclaimed** — canonical `jobs`/`quote` are empty on both tenants (0/0), but job-like data
     exists app-natively (`nspb.job_numbers` 23, `nspb.tenders` 380, `zaap.field_tenders` 323, Service
     `maintenance_checks` 35). Canonical work-order layer = empty scaffolding, not absent data.
  3. **`zaap` (EQ tenant) queried** — EQ roster IS converged (`schedule_entries` 500, `timesheets` 75, `staff` 26,
     `field_*` twins incl. `field_tenders` 323). Convergence is **asymmetric**: EQ converged, **SKS (`ehow`/`nspb`)
     is the laggard**. "Built but empty" was an ehow-only artifact.
  4. **Security severity inverted → fixed**: `nspb` (SKS LIVE PII anon-write) is top; `ktmj` (dead cold-backup) = MED,
     closes on decommission (confirm its anon key is still active first); `tenant_routing` key concentration = highest
     structural risk.
  5. Service DB **was** audited (S6) — the "not audited" lines in §2/appendix were stale; reconciled.
- **Sprint built**: `cross-app-linkage-sprint-2026-06-07.md` — target end-state (one canonical id per entity), 7
  workstreams with 10/10 (steelmanned) solutions, 4 dependency-ordered waves, a 3-risk pre-mortem, sprint-level DoD.
  Keystone = WS1 customer convergence (name crosswalk) + WS2 site→customer (440 resolvable) + WS3 defers to the
  approved worker-identity-linker spec. WS4 (canonical work-order spine on `quote.accepted`) + WS5 (durable outbox
  event bus, replacing fire-and-forget) are the real new-builds.
- **Git: NOT committed.** Main eq-context checkout is mid-session on `claude/identity-convergence-target-2026-06-04`
  [ahead 2] with `.git/index.lock` PRESENT (concurrent session active, or stale orphan lock). Did not run git —
  avoided index corruption. The deliverable files are on disk and will ride the concurrent session's close commit,
  or land via the scoped commit Royce runs when the lock clears.

## EXECUTED — WS2 site→customer backfill (live, "run the best possible solution")
On Royce's "run the best possible solution for us", executed the one deterministic, reversible, non-gated win:
**`app_data.sites.customer_id` backfill on `ehow`** (EQ-managed canonical plane — NOT nspb/SKS-live). Pre-checked
`external_id` uniqueness (0 dup groups → all 1:1, 0 ambiguous), ran the guarded UPDATE (440 rows, RETURNING captured),
verified: **sites linked 28→468; assets→customer 4769/4808 (99.2%)**. Exact reversible rollback recorded in
`_ws2-site-customer-backfill-2026-06-07.md` (the 440 external-derivable links are cleanly separable from the 28
pre-existing manual ones). Deliberately did **not** run the WS1 customer merge (481 ambiguous), any `nspb` write,
deploy, or auth change. The remaining 123 sites + `zaap` (30) wait on WS1 / explicit go.

## EXECUTED — WS1 customer convergence, safe subset (Royce "approval, steelman first then go")
Steelmanned the keystone, executed only the provably-safe subset, held the rest. Record + exact rollbacks:
`_ws1-customer-dedup-2026-06-07.md`.
- **Tier S — 38 stub customers retired** (`app_data.customers.active=false`; non-destructive, reversible). Predicate:
  dup-name member with 0 references + a referenced sibling exists (8 all-stub rows skipped → never empty a group).
  Active dup-name groups **117→80**, 0 groups emptied (verified).
- **Quotes backfill — 28** `sks_quotes_customers.canonical_id` set (0→28), only the 1:1-both-sides matches.
- **Structural finding:** `sks_quotes_customers.canonical_id` has a UNIQUE constraint (1:1) but the quotes side is N:1
  → full link is a both-sides dedup, not a backfill. ~65 more wait on quotes-side dedup (Intake).
- **HELD (not run):** Tier A (26 groups, FK-repoint merge — supervised), Tier C (50 ambiguous — Intake/review),
  quotes-side dedup, 99 dangling sites (need source re-import). Security WS6.4 cleared earlier (anon RPCs self-scoped).

## BUILT — canonical durable outbox for EQ Service (wiring fix, Royce "build, no mistakes")
Pivoted from data-cleanup to the integration wiring. Read the hub (`eq-shell/netlify/functions/canonical-api.ts`):
genuinely well-built (idempotent upsert on `(tenant_id, external_id)`, per-app bearer keys, tenant routing, projection
hygiene) — but the spokes call it **fire-and-forget with swallowed failures** → silent loss; the reference layer is
**write-only** (no read-back → drift undetectable); events are **observational** (no consumer acts; `jobs` has no
writer). Built the highest-leverage fix — **durable delivery** — for the cleanest spoke, EQ Service.

Files (repo **eq-solves-service**, working tree — NOT committed / NOT applied / NOT deployed):
- `supabase/migrations/0099_canonical_outbox.sql` — `canonical_outbox` table; RLS on + zero policies (service-role only).
- `lib/canonical-outbox.ts` — `enqueueCanonicalOutbox()` + `drainCanonicalOutbox()` (replay w/ exponential backoff to
  6h; permanent 4xx → dead immediately; writes `canonical_id` back to source customers/sites on success).
- `lib/canonical-sync.ts` — `putCanonical`/`emitEvent` now **enqueue on transient failure** (5xx/429/408/network)
  instead of dropping; permanent 4xx logged+dropped; **happy path unchanged** (still returns `canonical_id` inline).
- `app/api/cron/canonical-outbox-drain/route.ts` — `CRON_SECRET`-auth drain endpoint (mirrors supervisor-digest).
- `netlify/functions/canonical-outbox-scheduler.ts` + `netlify.toml` — every-5-min trigger.
Design: idempotent (hub upserts by external_id → replays safe); `dedupe_key` collapses repeated failed syncs to one
pending row; events distinct. **Verified: `tsc --noEmit` clean — 0 errors in the new files.**
Activation (gated, not done): apply 0099 to the Service DB; `supabase gen types` (I used a structural cast for the new
table); deploy Netlify. Env already present (`CANONICAL_*`, `CRON_SECRET`). Same pattern then ports to Quotes/Field.

## BUILT — operational work-order spine (wiring #2 + #3, eq-shell) — "build it all in the most correct order"
The event bus was observational: nothing consumed events and `app_data.jobs` had **no writer** (empty on both tenants).
Made it operational. Verified: `tsc -p tsconfig.netlify.json` clean — 0 errors in the changed files.
- **#2 Hub `jobs` writable** — `eq-shell/supabase/tenant-migrations/0040_jobs_canonical_upsert_index.sql` (unique
  partial index `(tenant_id, external_id) WHERE external_id IS NOT NULL`, mirroring 0022) + `canonical-api.ts`
  (`jobs` added to `WRITABLE_FIELDS` + `RESOURCE_PK` + doc). The hub's generic idempotent PUT now accepts `jobs`.
- **#3 Consumer `quote.accepted` → `PUT jobs`** — `eq-shell/netlify/functions/quote-job-consumer.ts` (scheduled
  every 15 min, in-file `config`). Lists active tenants via the control plane, reads recent `quote.accepted` per
  tenant through the hub, upserts a job keyed `external_id='eq-quotes:job:<quote_id>'` (idempotent → no cursor). v1
  sets `quote_id` + `title`; `customer_id`/`site_id` enrichment is a clean follow-up (the emit payload lacks them).
- Gated: apply 0040 via the tenant-migrate One Pipe; deploy eq-shell. `jobs.status` defaults `'active'` (no CHECK).
Order so far: [1] Service outbox ✅ · [2] hub jobs ✅ · [3] consumer ✅ · [4] Quotes outbox · [5] Field outbox · [6] reconciliation.

## BUILT — Quotes outbox (wiring #4, eq-quotes-port + eq-shell) — verified py_compile + tsc clean
Durable delivery for EQ Quotes → canonical (customer upserts + quote.* events), Python port of the verified Service pattern.
- `migrations/035_canonical_outbox.sql` — outbox table (RLS on, service-role only).
- `app/canonical_outbox.py` — `enqueue()` + `drain()` (backoff to 6h, permanent-4xx→dead, writes `canonical_id` back to
  `sks_quotes_customers`).
- `app/canonical_client.py` — `_put_canonical` enqueues on transient failure (customers).
- `app/canonical.py` — `write_event`: the background thread enqueues on transient failure via `app.app_context()`
  (events incl. **quote.accepted**, which the consumer reacts to).
- `app/cron/routes.py` — `POST /api/cron/drain-canonical-outbox` (Bearer CRON_SECRET).
- `eq-shell/netlify/functions/canonical-outbox-quotes-scheduler.ts` — every-5-min trigger (QUOTES_APP_URL + QUOTES_CRON_SECRET).
- Gated: apply 035 to the Quotes DB (ehow); deploy Quotes (Fly) + eq-shell.
Order: [1] Service ✅ [2] hub jobs ✅ [3] consumer ✅ [4] Quotes ✅ · [5] Field · [6] reconciliation.

## BUILT — Field emit durability (#5) + reconciliation backstop (#6) — PROGRAM COMPLETE
- **#5 Field `shift.started` retry** — `eq-solves-field/supabase/functions/shift-events/index.ts`: `emitWithRetry()`
  (4 attempts, 1/3/7s backoff; permanent 4xx not retried). Right-sized for a low-frequency *observational* emitter — a
  full outbox would mean a Deno + tenant-plane (SKS-live, gated) build for little value. Residual drift caught by #6.
- **#6 Reconciliation (Quotes)** — `app/cron/routes.py` `POST /api/cron/reconcile-canonical`: sweeps
  `sks_quotes_customers WHERE canonical_id IS NULL`, re-syncs via the existing `backfill_customers()` (now the durable
  path), writes back. Self-heals pre-outbox / missed rows. + `eq-shell/.../canonical-reconcile-quotes-scheduler.ts`
  (daily 22:30 UTC).
- **#6 Reconciliation (Service)** — `lib/canonical-reconcile.ts` (sweeps customers + sites with `canonical_id IS NULL`,
  re-syncs via `syncCustomer`/`syncSite` with the exact create/update field mappings, writes back) +
  `app/api/cron/reconcile-canonical/route.ts` + `netlify/functions/canonical-reconcile-scheduler.ts` + `netlify.toml`
  (daily 23:00 UTC). Verified `tsc` 0 errors. **#6 complete for both spokes.**
- Deploy runbook: `canonical-wiring-deploy-runbook-2026-06-07.md`.
- Verified: `py_compile` clean + `tsc -p tsconfig.netlify.json` 0 errors across all eq-shell functions.

**WIRING PROGRAM COMPLETE (6/6, all verified, none deployed/applied).** The reference layer went from a best-effort
write-only mirror to a durably-fed, operational, self-healing system of record:
- durable delivery from every JS/Python spoke (#1 Service, #4 Quotes) + retry on the Deno emitter (#5);
- the hub accepts work-orders (#2) and the bus is operational — `quote.accepted` → a canonical job (#3);
- drift self-heals (#6).
Gated activation per repo: apply the migrations (Service 0099, eq-shell tenant 0040, Quotes 035) + deploy
(Service Netlify, eq-shell Netlify, Quotes Fly, Field edge fn). Set `QUOTES_CRON_SECRET` in eq-shell for the two new
Quotes schedulers. Nothing applied or deployed by me.
