# Session 2026-08-08 — eq-field CSV import permission gap: found, fixed, merged, deployed

## Built
- Added the `isManager` permission gate to `importPeopleCSV`/`importSitesCSV`/`importScheduleCSV` in `scripts/import-export.js`, matching `importFullBackup`'s existing pattern in the same file.
- Verified severity live: `pg_policies` on ehow confirmed `app_data.sites` and `app_data.staff` (People/Supervision) have no role-based RLS, only tenant scoping — the missing client check was the only gate. `app_data.schedule_entries` already required manager/supervisor at the RLS layer, so Schedule had a DB-level backstop regardless.
- eq-field [PR #670](https://github.com/eq-solutions/eq-field/pull/670) — squash-merged as `3a56fa1`, deployed live to `field.eq.solutions`, verified via `sw.js` showing the new version post-deploy.
- Logged and closed SEC-21 in `ops/security-register.md`.
- Spun off a separate follow-up task for the identical, unfixed gap in `importManagersCSV` (`scripts/managers.js:498`).

## Decided
- Royce ran `/decide` on whether to proceed given `import-export.js` sits on a parked migration list — confirmed proceeding ("then build"): this fix is a different, lower-risk class of change than the parked migration (completes an existing mechanism, doesn't touch the permission model).
- Royce: push branch + open PR now, rather than hold for the SKS-cutover window.
- Royce: log the finding in `ops/security-register.md`.
- Royce: "merge it" — explicit go to squash-merge PR #670, which auto-deploys to production per this repo's standard flow.

## Deferred
- `importManagersCSV` (`scripts/managers.js`) — same missing check, same Data page, same destructive write pattern. Not built this session.
- Whether concurrent eq-field sessions should default to git worktrees instead of the shared root checkout — a real, repeated-today problem, not yet decided.

## Notes
- The shared `C:\Projects\eq-field` root checkout had a concurrent Claude Code session (`claude/mobile-p1-css-bundle`) mid-flight, which switched HEAD out from under this session partway through. Recovered by moving the fix into an isolated `git worktree` rather than touching the shared tree; the other session's uncommitted work was left untouched. At least 3 other sessions today hit the identical collision independently (visible in eq-field's own `docs/reflection-log.md`) — a repeated pattern today specifically, not a one-off.
- eq-field's strict-monotonic version-numbering convention broke down under today's concurrent load: `main` picked up 5 separate merges (`#663`, `#665`, `#666`, `#667`, `#668`) all claiming `v3.5.469` before this PR could land — three more landed in the few minutes between this branch's first rebase and its merge. This branch was rebased twice and shipped as `v3.5.470`, the actual next-free number at merge time. The 5 pre-existing duplicate-labeled commits on `main` were left as-is (cosmetic changelog-label collision only, not this session's finding to fix).
- Two `guard.js` hooks fired on the eq-field commit and were satisfied properly rather than bypassed: the brief-gate (ran `/brief eq-field`) and the reflection-gate (ran `/reflect`, logged to eq-field's own `docs/reflection-log.md`, staged in the same commit as the fix).
- eq-field's own `eq/pending.md`/`eq/changelog/field.md` note that `import-export.js` sits on a deliberately parked 11-file `isManager`→canonical-permission migration list (parked 2026-07-27 for the SKS cutover window). This fix is not that migration — it completes a missing instance of the mechanism the file already uses, with no change to the permission model — and was called out explicitly as such before Royce's go-ahead.

---

# Session 2026-08-08 — eq-shell commercial-writes RLS gap: confirmed already closed, no action taken

## Built
- Nothing new. Task on entry was to verify, rebase if needed, and land `supabase/tenant-migrations/0186_commercial_writes_management_only.sql` (branch `claude/eq-roles-enterprise-eval-177343`, 2026-07-16) — restricting INSERT/UPDATE/DELETE on `app_data.contract_scopes`/`field_tenders`/`field_projects` to manager/supervisor/platform-admin. Investigation found this had already been renumbered to `0239`, reviewed, merged (eq-shell [PR #1283](https://github.com/eq-solutions/eq-shell/pull/1283), commit `885264e1`), and dispatched live to both zaap and ehow via `tenant-migrate.yml` earlier the same day, before this session started.

## Decided
- None — no new decision needed; confirmed prior work stands.

## Deferred
- None.

## Notes
- Live-verified via `pg_policies` on both zaap and ehow: management-tier write policies are correctly in place on all three tables; reads remain unchanged/tenant-scoped. The 2026-07-16 "who can see/do what" audit finding is closed.
- Confirmed `claude/eq-roles-enterprise-eval-177343` never existed as a pushed ref anywhere — not local, not `origin`, not on GitHub. Only its final commit (`5cd95fe5`) sat as a dangling object in one local clone's `.git` store. Nothing to delete.
- Mid-session, a concurrent session working in the same shared `C:\Projects\eq-shell` root checkout (branch `fix/dependabot-js-yaml-brace-expansion`, uncommitted `package.json`/`pnpm-lock.yaml`) flagged the shared-checkout risk via cross-session message. This session had performed only read-only git commands (`fetch`/`log`/`show`/`ls-remote`, no checkout or commit), so no collision occurred — confirmed via `git status`. Logged as the 4th recurrence in the `shared-checkout-branch-race` memory pattern.
- No `eq/pending.md` entry tracked this item, so nothing to tick off there this session.

---

# Session 2026-08-08 — Field/Shell permission pipeline: a broken JWT handoff found and fixed, 6 PRs merged + live

## Built
- eq-roles: canonical `security-groups.html` export script (role matrix, permission matrix, default groups) + README fixes. A separate live per-tenant data pull was also delivered, outside this repo, per Royce's original scope call (canonical + live, no time-box). [eq-roles PR #20](https://github.com/eq-solutions/eq-roles/pull/20), merged.
- eq-field: fixed a non-functional role drift-guard (it was diffing against a hand-copy of itself, so it could never catch real drift). Building the accompanying bundle-regen tool (`scripts/build-bundles.mjs` + CI drift-guard) surfaced 3 already-merged fixes that had never actually reached production, because eq-field's hand-merged `core-bundle-*.js` files had zero automated sync-check since the v3.5.403 bundling started: the entity-patch token bridge, two missing lazy-loader tab-load entries, and the Birthdays/Starting Soon dashboard self-heal fix. All 3 restored. [eq-field PR #658](https://github.com/eq-solutions/eq-field/pull/658), merged, confirmed live.
- Diagrammed the suspected Field/Shell role conflation for Royce, then investigated it for real.
- Root-caused **the actual bug**: eq-shell's `token-exchange.ts` minted Field's JWT from a stale login-time session cookie, not live permission state — so role-matrix grants/denials set in Shell's Access Control page never reached Field at all, only per-person security-group grants did (and only as of last login). Fixed via a new `getRoleOverridePerms()` helper, deduplicating logic that had already been copy-pasted across two other call sites. [eq-shell PR #1280](https://github.com/eq-solutions/eq-shell/pull/1280), merged (admin-override — blocked by an unrelated pre-existing required check), confirmed live.
- eq-field: `verify-pin.js` had its own second, independent hardcoded role list — vendored from source via a new `_shared/eq-roles-canon.js`; an unlogged silent privilege-downgrade path (unrecognized role → silently downgraded to `employee`) now logged. [eq-field PR #662](https://github.com/eq-solutions/eq-field/pull/662), merged clean, confirmed live.
- eq-shell: the 74 Field-owned fine-grained permissions (roster/timesheets/leave/sites/etc.) — previously invisible outside eq-field's own source file — are now visible and grantable from Access Control via Custom Groups, without merging them into the canonical role model (a deliberate architecture boundary, left intact). [eq-shell PR #1281](https://github.com/eq-solutions/eq-shell/pull/1281), merged (admin-override), confirmed live.
- Of the original "3 role vocabularies" concern flagged during investigation, verified 2 were already dead/masked in production (no action needed) and 1 was a genuine stale doc caveat — corrected in eq-roles' README (folded into PR #20).
- Published a workflow diagram (Claude Artifact) tracing the whole session for Royce.
- Confirmed live-deploy status for PRs #1280/#662/#1281 directly (Netlify MCP + GitHub Actions), not assumed.
- Cleaned up the 3 merged worktrees this session created (one folder — `field-perms-visibility` — is Windows-locked and wouldn't fully delete; harmless, see Notes).
- Found the new `field-perms-drift.yml` guard (added in PR #1281) had been hard-failing every run since it has no working secret yet. Fixed it to skip clean with a `::notice::` instead of failing red — first attempt used `secrets.*` directly inside a step `if:`, which GitHub Actions rejects at the whole-file parse level (not just that step); fixed properly via `env:` → `$GITHUB_OUTPUT` → `if: steps.x.outputs.y`. [eq-shell PR #1285](https://github.com/eq-solutions/eq-shell/pull/1285), merged clean.
- Updated and committed `eq-context/system/worktree-registry.md` to reflect every merge/deploy/cleanup above.

## Decided
- Royce: build both the canonical export and a live per-tenant pull, no time-box.
- Royce: "Fix everything in this same branch" — when the bundle-regen tool surfaced the 3 dead-in-production fixes mid-build.
- Royce: "Roll it into the same plan, not separately staged" — the newly-found Shell→Field JWT bug folded into the same 4-phase plan rather than staged as a follow-up.
- 4-phase plan (`happy-knitting-karp.md`) approved via plan mode.
- Each merge explicitly approved one at a time, admin-overrides included: #1280 ("merge #1280 first"), #662 ("merge #662 next"), #1281 ("merge #1281 too"), #20 ("merge PR #20"), #1285 ("merge PR #1285").
- Royce: "close the field-perms-drift task in eq-shell" — no tracked task existed anywhere (no GitHub issue, no `pending.md` entry, no spawned chip) and the check was confirmed non-blocking (not a required status check), so interpreted as: stop it showing red for something structurally unresolvable right now. Fixed via a skip-gate rather than disabling the guard outright.
- Royce: "I can't do the secret now" — `FIELD_PERMS_DRIFT_PAT` stays explicitly on Royce, no attempt made to create it.

## Deferred
- `FIELD_PERMS_DRIFT_PAT` secret (fine-grained PAT, `Contents:read` on eq-field only, add as an eq-shell repo secret) — Royce's own action.
- Live click-through of the Shell↔Field permission changes (Phases 0-2) — needs a real signed-in session, off-limits to this environment.
- `field-perms-visibility` worktree folder — unregistered from git cleanly, but the empty directory itself is Windows-locked (`device or resource busy` on every retry); harmless, will clear on its own or after closing whatever has it open.

## Notes
- GitHub MCP connector confirmed **public-repo-only** in scope — 404s on every private EQ repo, distinct from the previously-documented session-activation bug. `gh` CLI used as the reliable fallback throughout (every PR/merge this session went through it). Documented in `system/lessons.md` and personal memory.
- Real GitHub Actions gotcha, confirmed the hard way: referencing `secrets.*` directly inside a step's `if:` condition gets the **entire workflow file** rejected at parse time — the run comes back with 0 jobs and a generic "likely failed because of a workflow file issue" message, no line number or specific reason surfaced via the API. The fix is routing the check through `env:` into `$GITHUB_OUTPUT`, then reading that step's `outputs` in later `if:` conditions — `secrets` context is fine there.
- Branch-protection required-checks were pulled directly before each admin-override, not assumed: `field-perms-drift.yml` was confirmed **not** a required check, meaning it was never actually what blocked #1281 — the real blocker was a pre-existing, unrelated required schema-drift check, independently confirmed already-fixed on `main` via `git merge-tree` before overriding.
- Deploy confirmation for eq-shell's #1280/#1281 couldn't be pinned to an exact per-commit Netlify deploy record (no list-deploys tool available in this session's toolset) — confirmed instead via green CI on the merge commit plus a verified *later* successful deploy on the same site. eq-field's #662 was confirmed directly (`commit_ref` match, state `ready`).
