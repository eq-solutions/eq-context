# Session 2026-08-05 — Tenant-rule audit extended to eq-cards + eq-solves-intake, all 4 PRs merged and live

## Built
- Extended the eq-context tenant-rule audit (`rules/non-negotiables.md` #11, landed 2026-08-04) to the two remaining suite repos: eq-cards and eq-solves-intake. All 5 relevant repos now checked: eq-shell (clean), eq-field, eq-solves-service, eq-cards, eq-solves-intake.
- **eq-field** — applied the already-merged `ts_reminders_sent_tenant_id` migration to live `ehow` (verified via `information_schema.columns`) and redeployed `ts-reminder` (v11 → v12) with the tenant-scoped code. Royce's explicit go-ahead.
- **eq-cards** — found and fixed a real authorization bug in `generate-wallet-pass`: a dead ownership check (wrong column name, unread field) let any authenticated user fetch another worker's wallet-pass data. Before deploying, checked `list_edge_functions` on `jvkn`/`zaap` directly and confirmed this function had **never been deployed to production on any version** — so the fix shipped live as the function's first-ever version (v1), fixed from birth. eq-cards [PR #214](https://github.com/eq-solutions/eq-cards/pull/214) merged (`c256cf8`), deployed. Tracked as [SEC-20](../ops/security-register.md) in the security register, closed.
- **eq-solves-intake** — 2 of 3 findings fixed:
  1. `api-intake/index.ts` — any authenticated caller whose JWT simply lacked a tenant claim (not just a genuine service-role caller) could trust a caller-supplied `body.tenant_id` outright. Fixed by explicitly comparing the raw bearer token against `SUPABASE_SERVICE_ROLE` before trusting it.
  2. `approve-worker-assignment/index.ts` — `staff`/`licences` upserts weren't tenant-scoped at the app layer (the underlying DB unique constraint isn't either, and that schema is owned by eq-shell's migration lineage — out of scope for this repo). Added explicit ownership checks before each upsert instead.
  3. `scripts/migrate-cards-to-canonical.mjs` — genuinely unscoped read of the whole Cards pool, but untouched since 2026-05-28 with column names (`given_name`/`family_name`/`worker_licences`) that no longer match the live schema. Flagged with a loud docstring warning instead of inventing a filter for code that stale.
  eq-solves-intake [PR #109](https://github.com/eq-solutions/eq-solves-intake/pull/109) merged (`f263cd7`).
- Delivered an HTML recap artifact ("One Rule, Six Real Gaps Closed") summarizing the full audit for Royce.

## Decided
- Royce: apply + redeploy the eq-field fix now.
- Royce: deploy the eq-cards fix now, given nothing in the app calls it yet and it's otherwise pre-production.
- Royce: fix the eq-solves-intake findings too.
- Royce: merge #214, then #109, after review each time.
- Royce: "keep building and sprinting" — general authorization to continue the audit chain across repos without a fresh ask at each step (fix cadence stayed: push + open PR, hold merge for explicit instruction, matching the precedent set earlier this session for eq-field/eq-solves-service).

## Deferred
- `scripts/migrate-cards-to-canonical.mjs` — flagged, not fixed. Needs either a real rewrite against the current schema or a decision to retire it; no engagement-filter model exists in this repo to build the "real" fix against.
- `C:\Projects\CLAUDE.md`'s Rule 0/0.5/load-bearing-facts — still the only home for that content, still Royce's governance call (carried over from 2026-08-04).
- `.git/hooks/pre-commit` deletion — still held on 4 worktrees merging past `main` (carried over from 2026-08-04). Largely superseded in spirit by the concurrent session's F9 hook work landing today, which structurally blocks the failure class this deletion was originally about (bare commits sweeping in unrelated staged files) — worth re-checking whether the hold is still needed once F9 is confirmed stable.

## Notes
- **Real process mistake, caught and fixed:** mid-session, a bare `git commit` (after `git add <my-file>`) on the shared eq-context checkout swept in a concurrent Cowork session's already-staged deletion of `hooks/adversarial_test.sh` and pushed it under an unrelated commit message. Investigated rather than reflexively reverting: confirmed via the session's own uncommitted `hooks/README.md` edit that the deletion was intentional (retiring the legacy bash test runner in favor of `adversarial_test.py`), so left the end state as-is. Later in the session that concurrent session committed its own work cleanly (`99a1b01`); rebased local onto it and pushed forward without incident. Saved a memory on the actual failure mechanism (bare commit = whole index, not just what was just staged) and used pathspec-scoped commits (`git commit -m "..." -- <file>`) for every eq-context commit afterward. Independently, this exact failure class got a structural fix today from the other concurrent session: `hooks/pre_tool_use.py` now blocks bare `git commit` with no `--` pathspec on this specific checkout (F9, `system/failures.md`) — so this shouldn't be possible to repeat here going forward regardless of which session's discipline lapses.
- eq-solves-intake's architecture is physical per-tenant Supabase project isolation, not one shared DB behind RLS — most missing-filter patterns there are safe by construction, not luck. Worth remembering before assuming every rule-#11 hit in that repo is equally urgent.
- Confirmed live (not assumed) before treating eq-cards' SEC-20 as urgent: `generate-wallet-pass` had zero production history and zero callers anywhere in the Cards app's own source. Changed the actual decision, not just the framing.
