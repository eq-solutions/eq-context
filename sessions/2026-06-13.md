---
date: 2026-06-13
type: session-log
scope: eq-shell
focus: EQ Service iframe loading fix (Shell PR #334)
---

# Session — 2026-06-13

## What was done

### EQ Service iframe loading fix — eq-shell PR #334

**Problem:** EQ Service showed "Loading EQ Service…" indefinitely when accessed from Shell (`core.eq.solutions/sks/service`). Broken for 1+ weeks. Royce: "isn't working — can we fix this once and for all."

**Investigation:**
- Read all TOKEN MODE auth code paths: proxy.ts, shell-auth/route.ts, (app)/layout.tsx, shell/page.tsx, ShellReadySignal.tsx
- Verified cookies (SameSite=Lax, same-site for eTLD+1 eq.solutions), proxy TOKEN MODE fast path, shell-auth JWT fast path, MFA bypass for shell sessions, ShellReadySignal target origin
- Checked Sentry (eq-solves-service): 0 related issues
- Verified no frame-ancestors restriction in Service netlify.toml
- Confirmed `app/page.tsx` redirect is client-side (no second iframe onLoad)

**Root cause:** `ServiceIframe.tsx` `onIframeLoad` fallback timer was `12_000ms` with a comment "cover the OTP round-trip". `COOKIE_AUTH = false` (TOKEN MODE) was made permanent, eliminating the OTP round-trip entirely. TOKEN MODE handshake completes in ~2-3 seconds. Users who refreshed before 12s restarted the clock — indefinitely showing the loading screen.

**Fix:** `12_000` → `4_000` ms fallback. Added Sentry breadcrumbs:
- `EQ_SERVICE_READY` received (success path, `level: info`)
- Fallback fired — EQ_SERVICE_READY not received in time (`level: warning`)

**File changed:** `src/pages/ServiceIframe.tsx` (`onIframeLoad`, line ~175)

**PR:** [#334](https://github.com/eq-solutions/eq-shell/pull/334) — merged 2026-06-13T00:44:57Z. Netlify deploy confirmed live (deploy `6a2ca798cc9a6c000854ab11`, published 2026-06-13T00:45:48Z).

---

## State at end of session

- eq-shell main: commit `90e4a137` (Quotes module on top of fix)
- Service should appear within 4-5s of navigating to `/sks/service` (was 12+s)
- No EQ Field changes this session

---

## Decisions

None — pure bug fix, no architecture changes.

---

## Deferred / Royce-gated

- Roster data entry on ehow (SKS tenant roster is empty)
- Standalone `sks-nsw-labour` retirement (after soak)
- Track 2 RLS STEP 2 (anon SELECT lockdown, after standalone retired)
- jvkn→ehow canonical identity pipeline (blocked: `WORKERS_WEBHOOK_SECRET` + `EHOW_SERVICE_ROLE_KEY` must be set in Dashboard)
- Waves 2–3 personal PIN revert (v3.5.132–134 deployed wrong direction; revert before further auth work)

---

# Session — 2026-06-13 (part b)

## What was done

### Sprint S4 — v3.5.139: CSP `_headers` sync + Sentry T3 scrubbing (PR #277, merged)

- `_headers` CSP fully synced with `netlify.toml` (wildcards, wss://, api.anthropic.com, c.bing.com, R2 img-src, worker-src)
- `netlify/functions/_shared/sentry.js`: T3 secret scrubbing added — `EQ_SECRET_SALT`, `AUDIT_SB_KEY`, `RESEND_API_KEY`, `ANTHROPIC_API_KEY` replaced with `[T3:SCRUBBED]` in error payloads
- Rebased over v3.5.137 + v3.5.138, resolved conflict in version stamps
- **Production:** commit `068920b`, deployed 2026-06-13T03:16:59Z, 5 functions, 0 secret scan matches

### Sprint S1 — credentials-canonical-sync v1 LIVE (jvkn edge fn + trigger)

- Edge fn `credentials-canonical-sync` v1 deployed to jvkn (`jvknxcmbtrfnxfrwfimn`), ID `ebbb7108-f9be-48ae-97a7-b09b8fe5ffd8`
- Trigger `credential_canonical_sync` wired to `jvkn.public.worker_credentials` (AFTER INSERT/UPDATE/DELETE)
- Forward path: jvkn credential → ehow `app_data.licences` ON CONFLICT `cards_credential_id`
- Conflict key: `cards_credential_id`; soft-delete on hard-delete; skips if no staff row
- **Data state:** 171 active credentials in jvkn → 171 licences in ehow (pre-seeded, trigger now maintains going forward)

### Cards→Core pipeline now COMPLETE

Full chain live: Cards → jvkn.workers → ehow.app_data.staff → ehow.public.people → Shell JWT → Field staff auto-mode (v3.5.138).

### ehow dedup — people + staff orphans archived

- Emma Curth (`e2ebdef9-...`): orphan `public.people` row archived (email IS NULL, correct row `9b08ec75-...` kept)
- Collin Toohey (`3d18422d-...`): **root cause — jvkn has 2 worker entries** (original `7514e57d-...` 2026-06-01, duplicate `3d18422d-...` 2026-06-11). ehow duplicate `public.people` archived + `app_data.staff` set active=false. jvkn dedup is Royce-gated.

### Housekeeping

- Local eq-solves-field main reset to `origin/main` (`068920b`) — squash-merge SHA divergence resolved
- EQ_SECRET_SALT rotation removed from tracking (Royce decision: not rotating)

---

## State at end of session (part b)

- EQ Field prod: v3.5.139 at `field.eq.solutions` + `field.sks.eq.solutions`
- jvkn canonical pipeline: workers + credentials forward paths LIVE
- ehow: 39 staff (1 inactive) + 171 licences + orphan people rows archived

---

## Open items (carry forward)

- **jvkn duplicate worker (Collin Toohey)** — `3d18422d-...` is a real jvkn worker, not just an orphan. Needs dedup in jvkn (delete the 2026-06-11 duplicate). Royce-gated.
- **Roster data entry on ehow** — SKS Field roster/timesheets/leave still empty
- **Standalone sks-nsw-labour retirement** — after soak
- **S2 (migration 0070)** — ehow KNOWN_LEGACY_ANON governance; CI green; deferred
