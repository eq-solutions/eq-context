# Session 2026-07-16 — verified migration 0185 live, explained the merge feature's location, seeded a real demo pair

## Built
- **Confirmed migration 0185 is fully live on ehow.** Traced the "merge and deploy" dispatch from the prior session through to completion: `gh run list` showed the `tenant-migrate.yml` workflow_dispatch run had gone red (exit code 2); pulled the failure log and found the actual target tenant (`sks`/ehow) applied cleanly — the red was `favour-perfect` failing on an unrelated, pre-existing migration (0184, missing `pg_cron` extension). Verified `eq_site_merge_preview`, `eq_site_merge_execute`, and `app_data.site_resolution_merge_log` all exist live on ehow by direct query.
- **Seeded one real duplicate-site advisory row on ehow (SY3), Royce's explicit go.** Found a genuine, unambiguous duplicate already sitting in the data — active "SY3" (has a customer) vs. inactive "Equinix SY3" (same address, no customer) — and inserted a `site_resolution_advisory` row (outcome='match', confidence='high', score=0.90) built from the resolver's own similarity math (code-exact + trigram address/name scores), so the Health tab's "Duplicates caught at the write" panel now has a real pair to adjudicate and merge. Source stamped `seeded-demo:royce-go-2026-07-16` so it's distinguishable from a genuine write-time catch. Nothing merged automatically — Royce still clicks Same → Preview → Confirm himself.

## Decided (Royce)
- Asked where the "Dupes" tabs (Customers/Contacts screens) related to the merge feature — clarified they're a different, older, merge-less duplicate-flagging feature entirely; the site-merge button only lives in the Health tab's "Duplicates caught at the write" section and only applies to Sites.
- "Yes, seed a real duplicate" — when asked whether to seed a synthetic test pair or a genuine one, Royce chose real data over a synthetic placeholder.

## Deferred
- **`favour-perfect` tenant stuck on migration 0184** (`pg_cron` extension not installed on that Supabase project) — flagged as a background task (task_2a98bf3b) in the prior session, still open.

## Notes
- The reason the merge feature "wasn't visible" going into this session: the "Duplicates caught at the write" section on the Health tab only renders when there's at least one flagged advisory row, and there were zero on production until this session's seed. The feature itself (RPCs, permission, UI) was already fully shipped and live — there was simply nothing for it to show.
- The SY3/SY4/SY5/SY6/SY7 pattern (an active site with a bare code name + customer, paired with an inactive "Equinix <code>" site with no customer) repeats across at least 5 site codes in ehow — SY3 was picked for the seed as the cleanest single pair; the other four remain real, unflagged duplicates worth a proper cleanup pass later (not seeded this session — only one pair was asked for).

---

# Session 2026-07-16 (2) — EQ Cards SKS minimum tenant requirements + Photo ID soft requirement

## Built
- **Answered "what are EQ Cards' minimum tenant requirements" for SKS** — traced the two provisioning paths (self-serve OTP flow, ops CLI script) and verified SKS's actual live tenant row on eq-canonical (jvkn): slug/name/tier/active, module entitlements (6 of 7 modules on, `quotes` off), branding JSON (palette/logos/gate copy), and its dedicated tenant-routing entry to `sks-canonical` (ehow).
- **Added Photo ID as a soft credential requirement for SKS** — inserted one row into `public.org_credential_requirements` (jvkn) for `licence_type='photo_id'`. Data-only change; `photo_id` already existed as a seeded licence type, no schema/migration needed.
- **Verified the gap surfaces correctly, end to end.** Ran the underlying gap query live: all 33 active SKS workers show `photo_id` unheld (expected — brand new ask); White Card gap unchanged (7 workers). Confirmed the Cards UI widget (`RequiredByOrgStrip`) is fully data-driven off the gap RPC — no code change needed, it'll pick up Photo ID automatically as a second chip, framed as a soft "add yours if you have it" nudge, never a block.
- **Audited requirement config across all 3 Cards tenants** — only SKS has any `org_credential_requirements` configured. `eq` (EQ Solutions' own active org) and `favour-perfect` (inactive) have none.

## Decided (Royce)
- Add Photo ID as a **soft** requirement for SKS (nudge only, not a hard gate) — done.

## Deferred
- **EQ Solutions' own org (`eq`) has zero credential requirements** — open question whether it should also require White Card/Photo ID, or stay requirement-free as the seed-demo org. _(added 2026-07-16)_

## Notes
- Re-verified the `licence-expiry-notifications` auto-memory against live data — still accurate (only SKS has `notification_email` set; `eq` doesn't). No memory update needed.
- No product code or migrations changed this session — the Photo ID requirement is a pure data row, applied directly via the Supabase MCP (jvkn is eq-canonical, the control-plane project, not a tenant data plane — no One Pipe migration required for this table).

---

# Session 2026-07-16 (3) — Admin Apps list naming consistency, deployed

## Built
- **Fixed the Apps toggle list on `/sks/admin/settings?tab=dashboard`.** Royce spotted it read "Intake / Cards / Field / EQ Ops / Service" — inconsistent naming, only Ops carried the "EQ" prefix. Root cause: `AdminTenantSettings.tsx` had its own hardcoded `MODULE_LABELS` map instead of using the shared `src/lib/appLabels.ts` (`APP_LABELS`) that every other surface — sidebar, command palette, mobile chrome, invite screen — already reads from. Swapped it over; now reads EQ Intake / EQ Cards / EQ Field / EQ Ops / EQ Service everywhere. Copy-only, no behaviour change.
- **Merged + deployed on Royce's explicit "deploy" instruction.** eq-shell PR #875, squash-merged (`7e4660e`) → main → core.eq.solutions auto-deploying. typecheck·test·lint green. The required schema-drift check failed on an unrelated infra error (Supabase Management API returned "resource removed" fetching a project's status — not a real security/schema issue, and this PR touched zero SQL). Flagged that explicitly and got Royce's separate go before bypassing it (the harness blocks a CI-check bypass as a distinct action from a bare "deploy").

## Decided (Royce)
- "Deploy" — confirmed pushing this fix live.
- "Admin-merge now" — when the schema-drift gate came back red on an unrelated infra hiccup, Royce chose to bypass it rather than wait-and-retry or leave the PR open.

## Deferred
- None.

## Notes
- Two-step confirmation pattern worth remembering: a bare "deploy" authorizes merging/pushing to main, but bypassing a *failing required check* is treated as a separate action needing its own explicit yes — worth flagging up front next time a required gate is red on an unrelated cause, rather than attempting the bypass silently.

---

# Session 2026-07-16 (4) — Cross-app attribution "deferred item" investigated and closed as a wrong assumption

## Built
- **Set out to "start Field's cross-app attribution fix"** (an open item from 2026-07-14: "Field / Service / Intake still log edits as 'system'"). Before writing any code, checked the claim against live code and the live database rather than building on the assumption.
- **EQ Field — checked, already correct.** Every write path (worker/team/roster edits, logins, leave approvals) captures the real person's name at the point of login and threads it through consistently; no "system" hardcode found anywhere in the codebase.
- **EQ Service — checked, already correct.** All audit writes funnel through one function that independently re-derives the real signed-in user; structurally there's no code path that can log without a real identity.
- **EQ Intake — checked, mostly correct, one dead-code loose end.** The real usage path (someone importing through the browser) correctly captures the real user. Found what looked like a gap — an API-import edge function with no actor parameter at all — but traced it further: that edge function isn't deployed on any of the three live databases (SKS's, EQ's, or the shared login one), and the database function it tries to call doesn't exist in any of them either. It's unmerged or abandoned code, not a live gap.
- **Closed the stale pending item** with the corrected finding (eq/pending.md) instead of leaving the wrong assumption in place for a future session to trip over.
- **Flagged the dead `api-intake` code as its own question** (chip task_68363ec7) — delete it or finish it, Royce's call, not folded into this fix.

## Decided (Royce)
- "Verify Service + Intake too" — when Field turned out clean, chose to check the other two named repos rather than stop at one data point.
- "Dig further now" — when Intake's finding turned out to contradict itself (source code referenced a database function that doesn't exist live), chose to trace it to a conclusive answer rather than report an ambiguous finding.

## Deferred
- ~~`api-intake` edge function fate (task_68363ec7)~~ — **dismissed by Royce, "not worth chasing."** Dead code stays as-is; not being pursued. _(added 2026-07-16, dismissed 2026-07-16)_

## Notes
- No code was written or deployed this session — this was pure verification. The original task ("start Field's fix") had no real target once checked against ground truth; the actual finding was three "is this really broken" checks that mostly said no.
- Root-cause of the wrong original claim: the 2026-07-14 session verified and fixed Shell's own attribution bug, then extrapolated "the other three apps probably have the same problem" without checking each one — a Rule 0.5 near-miss caught before any code was written this time.

---

# Session 2026-07-16 (4) — EQ Service admin hub audit, two card fixes, Today removed

## Built
- **Traced all 11 cards on `/sks/service/admin` to their actual code**, not their label — Royce asked for a trust check on the hub after noticing "Connected Apps" didn't read as doing anything real. Confirmed 9 of 11 do exactly what they say (Workspace Settings, Media Library, Report Settings, Archive, Audit Log, Imports hub, Backup, Activity, Today). Found 2 with stale copy: **Users** (page went read-only when Shell took over invite/roles — card still said "invite users, manage roles, deactivate accounts") and **Connected Apps** (card promised EQ Field sync that was never built — page only shows a static canonical-linkage completion bar with `CANONICAL_API_KEY_SERVICE`-gated status, zero interactive controls, `actions.ts` is empty since the old backfill action was retired).
- **Fixed both card descriptions** to match reality.
- **Removed the "Today" card and its `/admin/today` route** on Royce's "no need for that" — confirmed via grep it had no other code references before deleting.
- Explained the Audit Log vs Activity distinction: Audit Log is the full admin-only paginated/filterable record; Activity is a curated, unauthenticated-viewable 100-row highlight reel styled for a wall display, refreshed every 15s (not a live socket feed — ISR revalidation).
- Verified live on ehow (Rule 0.5 check, not just code-read): the Connected Apps 0/5 customer count is real, not a stub — see Deferred below.
- **Shipped as eq-service PR #544, squash-merged → main (`358858d`), live.** The commit had first landed on the old `fix/type-bypass-column-audit` branch (already merged once via #477), so re-basing it as a fresh branch off current `main` was needed to get a real CI run + Netlify preview rather than reusing a stale, already-merged branch.
- **Recovered a near-miss data-loss during the branch surgery**: an untracked in-progress doc (`docs/proposals/rcd-scheduled-check-from-contract-import.md`, not mine — another concurrent session's file) had to be stashed to free up a branch checkout. First stash-pop attempt reported "already exists, no checkout" but the file was actually NOT on disk — caught this via a difference on disk, re-ran `git stash pop stash@{0}` explicitly, confirmed the full 157-line file was restored intact before continuing. Nothing lost, but worth flagging: don't trust "already up to date" pop messages without an explicit disk check when using `--include-untracked`.

## Decided (Royce)
- "fix those 2" — fix the Users and Connected Apps card copy.
- "get rid of today - no need for that" — remove the Today admin view entirely.
- "commit this" → "push it" → "merge - stop going around it circles" — full ship, no PR review pause requested.

## Deferred
- **0 of 5 SKS customers have ever linked to a canonical ID** (sites are 9/9). Each customer row has a `canonical_synced_at` timestamp set, meaning a sync attempt fired but never returned a canonical link — genuinely looks like a stalled/broken customer-identity backfill, not just an unconfigured feature. Royce hasn't said whether to chase this. _(added 2026-07-16)_

## Notes
- The branch `fix/type-bypass-column-audit` is stale relative to `main` in ways well beyond this session's change (large unrelated diff vs. origin/main — dashboard, portal, notification, migration 0175 all differ) despite its own PR #477 already being merged; that's expected squash-merge behaviour (branch commit hashes don't match main's squashed commit even though content matches) but it means **never merge that branch directly again** — always cherry-pick the specific commit onto a fresh branch off current main first, exactly as done here.

---

# Session 2026-07-16 (4) — Root-caused and fixed eq-shell's required CI gate outage

## Built
- **Root-caused the "Tenant drift + anon-grant + policy-lint check" failures** that an earlier session today (see (3) above) had already bumped into and bypassed once as "an unrelated infra hiccup." Traced it properly this time: the check reads tenant Supabase project refs live from `shell_control.tenant_routing` on the control plane (jvknxcmbtrfnxfrwfimn), not from GitHub secrets as first suspected. Tenant `favour-perfect`'s Supabase project (`nxojbntrpxfnbhbyaspp`) had been deleted, but its routing row was still `status='active'`, so every run tried to reach a dead project and threw uncaught, killing the whole required gate — for every PR, not just the one from session (3).
- **Fixed with a data-only change**, no code deploy: set that tenant_routing row to `status='suspended'` on the control plane (plain DML, no DDL, per eq-shell's control-plane rules). Manually re-triggered the workflow and confirmed green.
- **Updated `eq-context/suite-state.md`** with a System Health incident note, a dated Key Decision entry, and corrected the stale `favour-perfect` "active" footer claim (left unstaged pending this close's commit).

## Decided (Royce)
- Chose "suspend the dead tenant row" over "just tell me what to run" or a script-hardening PR as the immediate action.
- Asked for a follow-up task to be spawned for the script-hardening fix rather than doing it inline in this session.

## Deferred
- **Follow-up spawned: task_8d6e64f1 — harden `check-tenant-drift.mjs`.** So one unreachable tenant soft-fails (skip + warn) instead of throwing and red-X'ing the required gate for the whole repo. Royce started it in a separate local session; running independently, not yet landed as of this close.

## Notes
- This is the same root cause as session (3)'s "unrelated infra hiccup" bypass earlier today — that session correctly identified it as non-security and got an explicit bypass go, but didn't chase the actual cause. Worth remembering: a required-gate failure that "looks infra-flavoured" is worth a root-cause pass even after a one-off bypass, since it was actively breaking *every* PR on the repo, not just the one being bypassed.
- Confirmed the drift check's tenant list is DB-driven (`shell_control.tenant_routing`), not secret-driven — useful to remember next time a similar failure looks like a stale `*_PROJECT_REF` secret; check the live routing table first.

---

# Session 2026-07-16 (5) — Merge from the Sites Dupes tab (migration 0186), shipped

## Built
- **Closed the gap between the generic Sites "Dupes" tab and the merge feature.** Royce noticed the Health-tab merge flow (0185, prior session) only ever surfaced pairs the write-time resolver caught live, while the older "Sites → Dupes" tab (name-match, read-only) found 43 real candidates on ehow with zero way to act on any of them. New RPC `eq_site_advisory_flag_pair(survivor, loser)` (migration 0186) — manager-gated same as merge execute, computes real similarity signals between the two named rows, idempotent on an existing unmerged advisory for the pair, deliberately does NOT set a verdict (the existing Same/Different/Unsure → Preview → Confirm flow from 0183/0185 still gates the actual merge — flagging never bypasses the human check).
- **Console: "Flag for merge" on exact 2-row Sites dupe groups.** Survivor auto-picked (active + has-customer wins, matching the SY3–SY7 pattern already observed); 3+ row groups (the SY9 shape) explicitly left to manual review rather than auto-guessed. Gated on the existing `intake.sites.merge` permission. On success routes back to the Health tab where the merge flow already lives — no new adjudication UI duplicated.
- Migration 0186 validated live via `BEGIN...ROLLBACK` against ehow — zero trace left.
- eq-solves-intake **PR #71 MERGED** (client wrapper `flagSitePairForMerge` + console UI, source of truth). eq-shell **PR #876 MERGED** (`3c4831e`, vendored copy) → core.eq.solutions auto-deploying.
- Caught and fixed a vendoring mistake mid-build: a blind `cp` of `styles.css` from eq-intake source briefly clobbered eq-shell's vendored copy, which intentionally omits `@fontsource` imports eq-shell doesn't carry — broke the vendored-package build. Fixed by restoring eq-shell's original file and adding only the 4 new CSS lines via Edit, not a full overwrite.

## Decided (Royce)
- Asked "still no way to close the dupe gap?" after the first status update — correctly read as "this isn't live yet," not a design objection.
- **"Merge + deploy now, one-by-one is fine"** over building a bulk-approve screen first — ships the flag-and-review flow now, defers any bulk-close UI to a later ask.
- **"Merge both, no review"** — both PRs were CI-green (typecheck/test/lint, migration ledger hygiene, Plan, Netlify preview); the one red check (tenant drift) was independently confirmed pre-existing/unrelated (see session (4) above — same day, different session, root-caused and fixed).

## Deferred
- **Migration 0186 apply to ehow** — same One Pipe posture as 0183/0185, needs Royce's separate go (code is merged/deploying; the RPC isn't callable against production data until applied).
- **Bulk-approve screen for closing all 43 dupes at once** — explicitly deferred; Royce chose ship-now over building this first. The current flow is ~3 taps per pair (Flag → Same → Preview → Confirm), not a batch action.

## Notes
- Even after this ships and 0186 is applied, "close the dupe gap" is still a one-pair-at-a-time human decision, not automated cleanup — worth keeping visible so a future "why hasn't this cleared itself" question doesn't reopen the same confusion.
- `eq-intake-demo`'s `IntakeHealthHome.tsx`/`IntakeModule.tsx` have real, pre-existing drift between eq-intake source and eq-shell's vendored copy from an earlier session (the whole merge-panel UI exists ONLY in eq-shell's copy, never backported to eq-intake source) — worked around it this time by threading `canMergeSites` through `IntakeModule.tsx` independently in both places rather than blindly overwriting, but the underlying drift is still there and will bite the next person who does a careless `cp` sync.

---

# Session 2026-07-16 (6) — Terms/legal review + fixes ahead of Royce's Monday SKS meeting

## Built
- **Full terms/legal/consent review across eq-cards, eq-field, eq-shell, eq-solves-service**, prompted by Royce's Monday meeting with Adam (SKS) to discuss adopting some of what's been built + data security. eq-field: no Terms of Service anywhere, just a read-only Privacy Notice modal — and it's branded as *SKS Technologies'* own notice to its own staff (their ABN), not EQ's terms imposed on SKS. eq-cards: full Terms of Use + Privacy Policy, plain-English and ACL/Privacy-Act-deferential throughout. eq-shell: no legal content of its own.
- **eq-solves-service `/terms` page taken down.** It carried its own internal code comment admitting it was an unreviewed draft ("review with Webb Financial / SaaS-literate lawyer before relying on in a commercial dispute") and read in a noticeably harder B2B/IP-protective tone than the rest of the suite (ML-training ban, "as is" liability cap, confidentiality clause). Page now `notFound()`s; prior content preserved in git history. eq-service **PR #543 MERGED** (`b587d84`) → service.eq.solutions deployed and verified live (Netlify deploy state `ready`, commit ref matches, secret scan clean). Side-finding: `/terms` was never actually in the app's `PUBLIC_PATHS` allowlist, so anonymous visitors were already redirected to `/auth/signin` before reaching it — real exposure was smaller than the code comment implied, but the takedown still matters for anyone signed in.
- **eq-cards overseas-disclosure line softened.** Privacy Policy §6 read "If you do not consent to overseas disclosure, you cannot use EQ Cards... By creating an account, you confirm you understand and accept this." Reworded to keep the same legal substance (still discloses overseas processors, still requires acceptance at signup) without the take-it-or-leave-it framing. eq-cards **PR #156 MERGED** (`ab393a6`). No effective-date bump — non-material per the policy's own §13 definition. Note: this is a bundled Flutter asset, so it only reaches real users on the next app build/release, not immediately.
- **eq-shell: found a dead, unbuilt vendored copy** of the old (unreviewed) eq-service terms text at `eq-intake/eq-platform/apps/eq-service/` — confirmed tracked in git but NOT part of eq-shell's actual Netlify build (Vite app → `dist`, not this nested Next.js scaffold), so not live-reachable. Spun off as a background task (`task_e4f355ab`) rather than actioned inline; Royce started it in a separate session, running independently as of this close.
- **Drafted Monday talking points**, iterated twice on Royce's feedback: first pass read too much like a product pitch; final version reframes entirely around "I built this because I needed it" — personal operational tooling that SKS happens to be the first real user of, not a sales pitch. Cut all marketing/customer/case-study language on Royce's explicit instruction (concern: as SKS NSW Ops Manager *and* EQ founder, wanted to avoid any appearance of steering an employer decision toward his own company's benefit). Landed on: Royce recuses from any commercial-terms question in the room, redirects to whoever owns that decision.
- **Terminology correction for the security discussion**: Royce considered calling SKS's tenant environment a "sandbox" or "data lake" — both wrong (sandbox implies non-production/test; data lake implies raw unstructured analytics storage, neither describes a live transactional system with enforced tenant isolation). Landed on "a dedicated, access-controlled environment" as accurate and non-invitational to follow-up questions the wrong terms would prompt.

## Decided (Royce)
- "Take the page down temporarily" — chose full unpublish over softening the eq-service terms wording or leaving it live and just avoiding it in the meeting.
- "push the merge and then continue the review" — confirmed deploying the eq-service takedown to production before continuing.
- "soften that overseas-disclosure line" then "push amd merge now so we dont forget" — chose to ship the eq-cards wording fix immediately despite it not taking effect until the next app release, rather than leave it staged.
- Repeatedly steered the meeting framing away from anything sales/product-shaped — this is the throughline across the whole session: dual-role (SKS Ops Manager + EQ founder) awareness driving every positioning choice.

## Deferred
- **eq-shell dead vendored-copy cleanup** — `task_e4f355ab`, in progress in a separate session as of this close.
- **Live-demo readiness check** (data cleanliness on whatever screen might get shown Monday, no visible Sentry errors mid-demo, eq-field Privacy Notice modal not click-tested) — offered, Royce hadn't said go as of session close.
- **eq-service PR #459/#458/#456** (dependency bumps, 10 days aging) and the **eq-shell/eq-service Sentry auth-stall + events-500 errors** flagged in the digest — unrelated to this session's work, surfaced only while checking whether anything could interrupt a live demo Monday. Not investigated.

## Notes
- The recurring theme this session wasn't technical — it was Royce iterating on how to present his own dual role (SKS employee showing SKS leadership a system he personally built and profits from) without it reading as a conflict of interest. The resolved approach: full disclosure stated plainly up front, no commercial ask in the room, explicit recusal from pricing/adoption-decision questions, and stripping any language that implied SKS is being "sold to" or used as a reference customer.
- Genuine follow-up still open beyond Monday: the eq-solves-service Terms of Use page needs an actual lawyer review before it's republished — the takedown is a stopgap, not a fix.

---

# Session 2026-07-16 (7) — eq-shell tenant-drift gate hardened against one dead tenant

## Built
- **`check-tenant-drift.mjs` no longer crashes the required CI gate when one tenant's Supabase project is unreachable.** This is the same root cause the earlier incident this file already documents (session 1: favour-perfect's project deleted, gate red for ~11 hours) — `fingerprint()`'s per-tenant Management API call inside the concurrency fan-out had no error handling, so ANY failure on one tenant threw uncaught and killed the whole script, red-X'ing the required gate for every unrelated PR on the repo. eq-shell **PR #878 MERGED** (squash `37b805a`) → core.eq.solutions auto-deploying; deploy verified live against the merge commit (Netlify state `ready`, commit_ref matches, no error).
- **Only recognized dead-project errors are treated as "skip and warn"** — an unreachable tenant is excluded from the drift comparison and reported informationally (new `--strict-unreachable-tenant` flag to opt into failing on it instead). A real query/schema bug against a still-live tenant still fails the build loud, same as every other check in the file — this distinction came from a review pass (below), not the first version shipped.
- **Ran a two-lens PR review ("muster") before merging** — only one lens was actually available (the codex-rescue second-opinion lens isn't installed in this environment, so the review is explicitly marked degraded/single-lens in the PR). That one lens still caught two real gaps, both fixed before merge:
  1. The first version's blanket try/catch would have also swallowed a genuine SQL/schema bug on a *live* tenant as "unreachable" — fixed by matching narrowly against known dead-project error signatures and re-throwing (hard-failing) anything else.
  2. An explicit `--reference=<slug>` pointing at a tenant that's active-but-unreachable still hard-failed instead of degrading like the automatic-default path — fixed to fall back to the first reachable tenant, with a genuinely-nonexistent slug still failing loud as a real config error.
- **Closed the loop on a parallel-session duplicate-work risk.** The earlier incident (session 1, same day) had spawned a follow-up chip (`task_8d6e64f1`) for this exact fix, run by Royce in a separate local session, "not yet landed" as of that session's close. This session shipped the same fix first — checked `list_sessions` + transcript search and found no session currently running work on `check-tenant-drift.mjs`, so nothing is actively racing against PR #878, but flagged it explicitly in `pending.md` in case that session resumes later.

## Decided (Royce)
- "check muster on PR #878" — confirmed running the review before merge rather than merging on green CI alone.
- "Fix the major finding now" / "fix the minor finding too" — both muster findings fixed inline rather than deferred.
- "merge the PR" — explicit merge instruction; squash-merged once all required checks (typecheck·test·lint, migration ledger hygiene, the drift gate itself) were green.
- "verify the deploy on core.eq.solutions" — confirmed via the Netlify MCP reader against the live site, not assumed from a green CI check alone.

## Deferred
- None — both muster findings were fixed before merge; no open follow-up specific to this change.

## Notes
- Hit the recurring shared-root-checkout collision again (documented elsewhere in `worktree-registry.md` as a repeat pattern): a concurrent session's branch switch on the shared `eq-shell` root checkout landed under this session mid-edit. Recovered by stashing just the one modified file and moving the work into a fresh worktree (`eq-shell-tenant-drift-wt`, now pruned) rather than disturbing the other session's branch.
- The `codex:codex-rescue` agent type referenced by the `muster` skill is not available in this environment — every muster run here will be single-lens/degraded until that's wired up. Worth noting if review quality on future PRs feels thin.

---

# Session 2026-07-16 (8) — Closed the customer-canonical-linkage question from earlier today: dead code, not a gap

## Built
- **Investigated the "0/5 customers linked to canonical" finding flagged earlier today** (session (4)'s admin-hub audit). Traced `canonical_id`/`canonical_synced_at` back to their source: `service.customer_local`/`service.site_local` sidecar tables and the push-based sync code in `lib/canonical-outbox.ts`/`lib/canonical-sync.ts`. That whole mechanism is dead — `lib/canonical-sync.ts`'s own docstring says the write-through adapters were removed when Shell became sole owner of customers/sites/assets. Verified live: both sidecar tables are 100% empty across every tenant, and `canonical_outbox` has zero `customers` rows, ever.
- **Found the real bug wasn't the 0% — it was the 100%.** `service.sites.canonical_id` uses `COALESCE(sl.canonical_id, s.site_id)`, silently falling back to the site's own local id whenever there's no real canonical link (always, now) — so "Sites linked: 9/9 (100%)" could never read as anything but fully synced, regardless of reality. Customers had no such fallback, which is the only reason 0/5 ever surfaced as a (meaningless) signal in the first place.
- **Took the finding to Royce rather than silently fixing it** — asked whether to (a) replace the card with a plain status note, (b) remove the card and page entirely, or (c) leave it. Royce chose full removal.
- **Removed the "Connected Apps" card and the entire `/admin/integrations` route** (`page.tsx`, `actions.ts`, `IntegrationsClient.tsx`). eq-service **PR #545 MERGED + LIVE** (`ad1a03c`) — verified live via the Netlify MCP reader (deploy `state: ready`, `commit_ref` matches the merge SHA exactly, secret scan clean).
- Shipped the same way as the two PRs earlier today: fresh branch off current `main`, not the stale `fix/type-bypass-column-audit` branch (see session (4)'s note — that branch is still stale, still shouldn't be merged directly).

## Decided (Royce)
- "look into the customer canonical linkage gap" — investigate rather than leave it as an open question.
- **"Remove the card and page entirely"** (via AskUserQuestion) — chosen over "replace with a plain status note" or "leave it for now."
- "merge and deploy" — explicit go for PR #545.

## Deferred
- None — this closes out the open item from session (4).

## Notes
- Hit the shared-checkout untracked-file trap again mid-branch-switch (same class of issue as session (7), different repo): `docs/proposals/rcd-scheduled-check-from-contract-import.md` blocked a `git checkout -b` twice. First time, a `git stash pop` silently failed to actually restore the file to disk (reported "already exists, no checkout" while the file was genuinely absent) — caught it by checking the file directly rather than trusting the git message, and had to re-run `git stash pop stash@{0}` explicitly to recover it. Second time, the file had since been **updated by a different concurrent session** (their genuinely completed work on that same proposal, "proposed" → "built") — correctly left that newer on-disk version alone and did not restore my older stashed copy over it. Worth remembering for any future `/admin` or shared-root-checkout branch surgery: always verify a stash-pop actually landed on disk, and always diff before restoring a stash over a file that could have moved on since it was parked.
- eq-service's `fix/type-bypass-column-audit` branch has now had two follow-up commits redirected around it in one day (PR #544 and #545) rather than merged onto it directly — it's meaningfully stale vs. `main` beyond just these two changes. Worth a proper cleanup or deletion next time someone's in that repo, rather than leaving it sitting there as a trap for a future session that doesn't know to avoid it.

---

# Session 2026-07-16 (8) — Equinix SY9 duplicate customer, verified already clean

## Built
- **Investigated a possible duplicate SY9 customer record on ehow.** Royce asked which company name was correct between "Equinix Hyperscale" (older, `a57bf144-…`) and "Equinix Hyperscale 2 (SY9) Pty Limited" (newer, `d79ee06f-…`, linked to the real SY9 site with 499 assets/10 contract scopes/2 quotes). Royce confirmed the longer name is correct.
- **Checked live before acting**: the older duplicate customer and its linked retired site (`95cdc37d-…`) were already `active=false`, with zero dependent rows across quotes/contacts/contract_scopes/jobs. Read the `eq_merge_customers` RPC definition to confirm what a merge would do, then confirmed it wasn't needed — the cleanup had already happened in an earlier session. No DML executed.
- **Logged the resolution to auto-memory** (`project_equinix_entity_map.md`) so a future Coupa/PO-matching session doesn't mistake "Equinix Hyperscale" (no suffix) for a live entity.

## Decided (Royce)
- "Equinix Hyperscale 2 (SY9) Pty Limited" is the correct company name for the SY9 account.

## Deferred
- None.

## Notes
- Good example of Rule 0.5 paying off in the other direction — the natural next step ("fix it properly") looked like it needed a merge RPC call, but checking live state first showed there was nothing left to fix.

---

# Session 2026-07-16 — Removed eq-shell's dead vendored eq-platform/apps scaffold + suite-wide audit

## Built
- Confirmed `eq-intake/eq-platform/apps/{eq-service,eq-shell}/` in eq-shell was genuinely dead: `pnpm-workspace.yaml` only wires in `eq-platform/packages/*` (the real workspace deps), root `tsconfig.json` project references and `vite.config.ts` never touch `apps/`, and the only repo-wide hits for "eq-platform/apps" were self-references inside the two directories being removed.
- Removed both directories (697 files, including a byte-for-byte stale copy of the old unreviewed eq-solves-service `/terms` draft that had already been taken down live via PR #543). eq-shell PR #877 merged (squash `d4ed16c`) → main, CI + deploy preview green.
- Cleaned up 99MB of orphaned, untracked `.next` build-cache debris left behind under the same path — `git rm` only removes tracked files, so this Turbopack/Next output (dated 2026-05-21, from when the app was originally vendored) survived the deletion until manually removed.
- Fanned out read-only checks across the other 8 suite repos (eq-cards, eq-intake, eq-field, eq-solves-service, eq-roles, eq-ui, eq-design-tokens, sks-nsw-labour) for the same accidental-vendoring pattern — all clean, nothing else found. Also re-confirmed eq-solves-service PR #543 is live on main with no other copy of the old terms text anywhere in the suite.

## Decided
- Royce confirmed: investigate before deleting (done), then remove the whole scaffold rather than patch just the stale file; delete the leftover untracked build cache too.

## Deferred
- None — task fully closed, no follow-up items.

## Notes
- Root cause of the original vendoring: eq-solves-intake's whole `apps/` folder (not just `packages/`) got accidentally swept in by a "vendor eq-intake sprint updates into eq-shell" commit on 2026-06-10 (b116e44) — a scope mistake in what should have been a packages-only re-vendor.
- Hit the same shared-root-checkout collision documented in the entry above this one: a concurrent session (`fix/tenant-drift-resilience`, PR #878) had its stash sitting in this root under my branch name (`stash@{0}: On chore/remove-dead-vendored-platform-apps: tenant-drift-resilience-wip`), which is what caused the stray `check-tenant-drift.mjs` diff I initially flagged as unexplained. Resolved once traced — no action needed, their work already landed cleanly in its own worktree/PR.
