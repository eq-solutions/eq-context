# Session 2026-09-02 — Live-meeting kit: sample ID sheet added, QR-at-scale question resolved by finding it already exists

## Built
- Fourth artifact for the CEO/executive live-meeting kit: "EQ Sample ID Sheet" — a double-sided A4 handout, one per table rather than one per person. Front repeats the laminated card's 3-step instructions and company link; back carries 6 different sample credentials (White Card, Driver Licence, First Aid, Working at Heights, Forklift, EWP) so a room of attendees isn't all scanning the identical fake identity.

## Decided
- Nothing new decided by Royce this session — asked whether to generate a real EQ self-join QR for the meeting, no answer yet as of this close.

## Deferred
- Whether to generate a real self-join link/QR for the `eq` tenant via eq-shell's `AdminSelfJoinLinks` and swap it into the kit in place of the generic search-and-apply flow — open question put to Royce, unanswered as of this close.

## Notes
- **Royce asked "we should have QR codes for whatever role we want via add workers" — investigated before building anything, and it already exists.** eq-shell's `AdminSelfJoinLinks.tsx` (`/:tenantSlug/admin/workers/join-links`, linked from Worker Invites) is a mature, live, actively-iterated feature: per-role QR (all 6 roles), reusable by any number of people at once, expiry + approval-required toggle, full lifecycle (regenerate/deactivate/delete). Confirmed via the live `shell_control.self_join_codes` table on jvkn, not just the code: 8 real codes, actively created and used through 2026-08-26 — all for the `sks` tenant, **zero for `eq`**. Nothing to build; the only gap is that nobody's pointed this feature at the EQ tenant yet. Full detail in `eq/pending/eq-cards.md` and `eq/pending/eq-shell.md`, both 2026-09-02 entries.
- Same eq-context F9 shared-checkout guard as every prior close this week — did this close's writes from a fresh isolated clone (re-cloned, not reused, since the prior close's clone was itself now behind) rather than the shared `C:\Projects\eq-context` checkout, which showed `behind 17 / ahead 1` at session start — a state worth being cautious around, not something this session touched or attempted to reconcile directly.
- Verified today's actual date via a shell command before writing any dated file — the session-gate hook's "digest.md 2026-09-01 (0d)" freshness stamp is about when that file was last regenerated, not today's real date (2026-09-02); worth not conflating the two.
---

# Session 2026-09-02 — Closed out the last 5 set-state-in-effect violations, upstream in eq-solves-intake

## Built
- Traced eq-shell's vendored `eq-intake-demo` package (5 remaining `react-hooks/set-state-in-effect` violations that eq-shell#1723's 33-file sweep didn't reach) to its real upstream source, `eq-solutions/eq-solves-intake` (confirmed via eq-shell's README + the source repo's own `git remote -v`).
- Fixed all 5 there using the same two established patterns from #1723: `queueMicrotask`-wrap (4 files) and an adjust-during-render mode-reset guard (1 file).
- Verified against eq-shell's real ESLint rule (eq-solves-intake has none of its own) via a cross-repo invocation from a common ancestor directory — 0 errors on all 5, down from 1 each at the exact reported lines. `tsc --noEmit` clean too.
- Opened, confirmed CI green, and (on Royce's explicit go) squash-merged eq-solves-intake [PR #121](https://github.com/eq-solutions/eq-solves-intake/pull/121) (`731951e`).

## Decided
- Royce confirmed "merge the PR" after CI came back green — merged.

## Deferred
- eq-shell's own vendored copy of `eq-intake-demo` still has all 5 violations — only a `pnpm run revendor:intake` picks up PR #121's fix, and that's a separate, deliberate manual step per eq-shell's own README. Not attempted this session (explicitly out of scope per the original task). Tracked in `eq-shell.md`.

## Notes
- eq-solves-intake has no ESLint of its own — its `check` script is schema-lint + typecheck only. Plain `npx eslint <path-outside-project>` refuses flat-config files "outside of base path"; `pnpm exec eslint` inside eq-solves-intake itself fails on a broken/orphaned `.bin/eslint` shim with no real `eslint` package underneath (not a dependency there at all). Working path: invoke eq-shell's own installed eslint binary directly, with cwd set to a common ancestor of both repos (`C:\Projects`) so the target file isn't "outside" the config's base path.
- Local naming trap, same shape as the existing eq-quotes/eq-quotes-port one already in the umbrella CLAUDE.md: the sibling checkout at `C:\Projects\eq-intake` is the `eq-solves-intake` GitHub repo, not a repo literally named `eq-intake`.
- This session's brief-gate hook required running `/brief eq-intake` before any Edit would go through — done mid-task; health/worktree/git-state checks all came back clear, no blockers.
- Hit the F9 shared-checkout guard mid-close: `git rebase` in the shared `C:\Projects\eq-context` checkout was blocked by the repo's own pre-commit hook, and a concurrent session's own close process was actively mutating the same working tree at the same time (its edits to `sessions/2026-09-02.md` appeared and my own `eq-intake.md` edit visibly reverted on disk mid-operation — no data lost, just working-tree churn from two closes racing). Recovered by anchoring the commit with a branch ref, then redoing this close's writes from a fresh isolated clone instead, per the established pattern.
- Full technical detail in eq-shell memory `eq-intake-demo-set-state-in-effect-upstream-pr`.

---

# Session 2026-09-02 — Licence review "back" navigation, merged and live

## Built
- Royce reported accidentally double-clicking "Sighted" on the last licence of an 8-licence review with no way to undo it. Added a back-chevron control next to the "Licence X of Y" header in eq-shell's `LicenceReviewModal` (staff roster add/re-review) and the matching `LabourHireLicenceReviewModal` — steps back one card and drops that card's recorded decision so it can be redone.
- Verified via `tsc -b tsconfig.app.json --force` and `eslint` on the touched file (both clean, no new warnings); no existing tests covered this modal.

## Decided
- Royce said "merge" — opened eq-shell [PR #1727](https://github.com/eq-solutions/eq-shell/pull/1727), waited for CI (typecheck/lint, schema-drift, Netlify preview all required), squash-merged once green (`a533958f`). Confirmed live via the production deploy record itself (`published_at` set, `state: ready` for that exact commit) rather than treating the merge as proof.

## Deferred
- Nothing — small, fully self-contained fix, shipped end-to-end this session.

## Notes
- Deliberately scoped to a single-step "back" control (matches the reported problem — one accidental click forward) rather than making the progress dots themselves clickable for multi-step jumps; a reasonable follow-up if Royce wants it, not built.
- Couldn't click-test live in this environment — `netlify dev` still doesn't come up on this machine's Node 24 (previously documented; reconfirmed this session, not a new finding).
- Same eq-context shared-checkout contention as today's other two entries above: the shared checkout was sitting on a different session's own `close-2026-09-02-selfclose` branch with its own uncommitted `sessions/2026-09-02.md` edit and an unpushed same-day close commit (`9eeac445`). Left it untouched — not mine to resolve — and did this close from a fresh isolated clone instead.

---

# Session 2026-09-02 — Register redesign shipped, Archive/Unarchive lifecycle added, 3 stuck SWMS PDFs converted and click-tested live

## Built
- Shipped eq-shell PR #1725: Register's per-document signer list redesigned from one flat table (mixing outstanding and signed rows) to a completion bar + outstanding-only default view + already-signed rows collapsed behind a disclosure. Pure rendering change over existing data. Merged `1338e932`, live 2026-09-01 11:15 UTC.
- Shipped eq-shell PR #1726: Unarchive action (mirrors Archive exactly — same confirm dialog, same permission gate, same handler with a `targetStatus` param) on the Register's Archived view; "Convert missing PDFs" button moved from Reference library to Register on Royce's direct ask. Merged `caca597f`, live 2026-09-01 18:24 UTC.
- Confirmed eq-shell PR #1724 (labour_hire label fix, Gotenberg timeout 15s→22s, first backfill button) live at 2026-09-01 10:54 UTC — this was mid-flight going into this session.
- Delivered an interactive two-option UX mockup for the Register redesign before building anything; Royce picked "progress-first," explicitly deferring the bulk-remind half once scoping surfaced it needs a reminder-send backend that doesn't exist anywhere in this codebase.
- Click-tested the whole thing live, in Royce's own authenticated browser session (driven via the Claude in Chrome MCP, per his standing "default browser only" preference — never the in-app Browser pane): archived all 3 originally-stuck SWMS documents, confirmed the Archived view's menu correctly offers only Unarchive (no Push/Archive), unarchived all 3, confirmed every signer (32/32/1) survived the round-trip with status intact. Fired "Convert missing PDFs" mid-test — all 3 are now `pdf_status: 'ready'` with a real stored PDF twin, confirmed via direct SQL before and after.

## Decided
- Royce: "Display now, remind later" — ship the progress-first display now, scope the reminder-send feature as its own separate follow-up rather than build it blind.
- Royce: move "Convert missing PDFs" off Reference library onto Register — "Reference library doesn't need the PDF feature, its mainly for register."
- Royce: "ignore the delete function but need to be able to unarchive" — permanent-delete explicitly declined (these are signed compliance records), Unarchive confirmed as the real ask.
- Royce: "merge it once green" for both PRs — merged once CI passed, each verified live via the Netlify deploy record's `published_at` (not just merge success) before reporting done.

## Deferred
- Bulk-remind for outstanding signers — no send-reminder path exists anywhere in this codebase yet (`reminder_count`/`last_reminded_at` are tracked, never written to). Needs a product decision on channel/content before buildable.
- "Group outstanding signers by site" (the mockup's Option 2) — `RegisterEntry` has no per-signer `site_id` for a customer/site-set-scoped push, only the group-level `covered_sites`. Needs a backend addition first.
- Certificate + tenant logo click-test (PR #1719, a different same-day session's work) — surfaced to Royce via an unrelated screenshot he shared; he hasn't said whether he wants it folded into this stream of work.

## Notes
- Real gap found while scoping the redesign, not previously known: no reminder-send feature exists in this codebase at all — `reminder_count`/`last_reminded_at` columns have been tracked since migration 0253 with nothing ever incrementing them outside direct DB access.
- `push-document-audience.ts`'s `handleArchive`/new `handleUnarchive` now share one `handleSetDocumentStatus(targetStatus)` helper — refactored rather than duplicated, matching the existing precedent `handleRegister`'s own `status` parameter already set for the GET side.
- Same eq-context F9 shared-checkout contention as this session's siblings above — hit it independently while starting this close (a different, unrelated stuck commit than the `9eeac445` one two sessions above already flagged: same failure class, different session). Did not attempt to reconcile the shared checkout; wrote this close from its own fresh isolated clone.
- Full technical detail (exact SQL, file:line references, the mockup artifact) lives in this session's own transcript and eq-shell memory, not duplicated here.

---

# Session 2026-09-02 — Unstuck a prior session's stranded substrate push (pending-invites session-close record)

## Built
- A different session's 2026-09-01 close (`close-45c82196`, commit `9db2bd0` — the session-close record for the already-merged, already-live pending-invites list fix, PR #1720) had been rejected twice pushing to `origin/main` and was stuck 1 ahead / 17 behind. Read the stuck commit's full diff before touching anything — confirmed it was exactly the 3 substrate files it claimed to be, nothing else.
- Rebuilt it onto current `origin/main` and resolved two separate rounds of merge conflicts in `sessions/2026-09-01.md` (two unrelated sessions both appending their own new section to the same end-of-file spot — content that never actually overlapped in meaning, so both sides were kept in full rather than either being dropped).
- Landed as `57be285` on `origin/main`. Verified by reading the actual pushed content back from `origin/main` directly (specific strings from both the changelog and the two session entries), not just trusting push success output.

## Decided
- Royce: confirmed via chat to proceed with landing the stuck commit, and separately asked me to run this close itself.

## Deferred
- Nothing new.

## Notes
- **Claude Code's own permission classifier hard-blocks this repo's `main`** for this kind of session — not just `git push`, but branch-creation-toward-main too (hit both, confirmed twice). The documented fallback (memory: `eq-context-shared-checkout-contention`) of handing the exact command to Royce still holds, including for the non-push prep steps, not only the final push.
- **Root cause of several "I ran it, nothing happened" reports in a row**: `GIT_EDITOR=true git ...` is bash syntax. In PowerShell it's parsed as an attempt to run a program literally named `GIT_EDITOR=true`, which doesn't exist — `CommandNotFoundException`, and `cherry-pick --continue` silently never ran. Every push after that correctly reported "Everything up-to-date" (there was nothing new to push). PowerShell needs `$env:GIT_EDITOR = "true"` as its own statement. Worth checking for this specific failure shape any time a bash-style inline env-var prefix is handed to a PowerShell user and "it didn't work" with no visible error.
- A conflict got `git add`ed once without actually being resolved first — git doesn't validate file content on `add`, only index state, so `git status` showed a clean staged file while the working copy still had literal `<<<<<<<`/`=======`/`>>>>>>>` text in it. `git status` alone isn't enough to trust a conflict is resolved; check the file content too.
- `origin/main` moved 4 separate times over the course of this one close (nightly + concurrent-session traffic) — expected per the existing shared-checkout-contention pattern, not a problem, but worth planning for on any close done right now.
- A concurrent session's own uncommitted close (eq-solves-intake set-state-in-effect fixes) was sitting mid-write in this same shared checkout's working tree when this close ran — not in any commit on any branch (checked `--all`). Deliberately left out of this push rather than shipped under this session's own commit; restored back to the working tree, uncommitted, exactly as found, so that session's own close still finds it.

---

# Session 2026-09-02 — Wallet Export button labelled + shipped live

## Built
- The Wallet screen's icon-only "Export" button (download icon, tooltip only, no visible text) now shows a visible "Export" label — mirrors the adjacent "Add" button, already fixed the same way for the same reason. eq-cards [PR #340](https://github.com/eq-solutions/eq-cards/pull/340), squash-merged (`6e6f667`).
- Deployed live: `workflow_dispatch` → Netlify, verified against Netlify's own deploy record (`state: ready`, `published_at` set, flagged as the *current* deploy) rather than trusting the Action's own green checkmark alone.

## Decided
- Royce: confirmed via chat to commit, push, and open the PR.
- Royce triggered the deploy workflow himself after Claude Code's auto-mode classifier blocked doing it directly from this session.

## Deferred
- Not click-tested live — the Wallet screen sits behind phone-OTP sign-in, no credentials available in this environment. Verified via `flutter analyze` (clean) and a successful `flutter build web` instead.
- Unrelated pre-existing bug found while verifying, not fixed this session: `flutter test` can't compile locally on Windows due to a null-safety bug in the pinned `pdfrx_engine` 0.4.5 package — reproduced on two unrelated test files, confirmed unrelated to any pubspec change made this session. Flagged as background task `task_1310e6b1`; Royce has already started it running in a separate session.

## Notes
- Claude Code's auto-mode classifier blocked `gh workflow run deploy.yml` (and, briefly, a read-only `gh pr view`) — same class of classifier wall this file documents elsewhere for direct DB/merge/push actions. Royce triggering the deploy himself was the fallback.
- `flutter analyze` and `flutter build web` both silently ran `pub get` and auto-edited `analysis_options.yaml` as side effects (6 transitive dependency bumps, an analyzer-config rewrite) — reverted both each time so the shipped diff stayed scoped to just the one file.
- This eq-context close hit the shared-checkout collision this file already documents a precedent for: a concurrent session (eq-solves-intake work) had an uncommitted edit sitting in `sessions/2026-09-02.md` in this same working tree, on top of its own unpushed commit. Handled the same way as the precedent — scoped `git stash` (tagged, `apply` not `pop`) to pull just that file's uncommitted portion out, wrote this entry, committed + pushed, then restored their content back into the working tree afterward, untouched.

---

# Session 2026-09-02 — Suite-wide Sentry sweep: 20 issues reviewed across 5 repos, 5 real bugs fixed, 2 already-fixed discovered, 4 data/upstream findings surfaced

## Built
- Pulled every unresolved Sentry issue (org `eq-solutions`) across all 5 projects that have one — eq-field (8), eq-shell (10), eq-solves-service (1), eq-cards (1), eq-quotes (0, retired) — full stack traces/extra-data/tags for all 20 before triaging any of them, rather than acting on titles alone.
- eq-field: fixed 3 real bugs directly in this session's own worktree — `eqhExternalLinkTap` (home.js) IIFE-scoped but called from an inline `onclick=`; `_renderTsApprovalQueue` called unguarded across a lazy-bucket load-order race; `onTsCellChange`'s `.closest('tr')` always null on the mobile card layout (no `<tr>` there at all). [PR #881](https://github.com/eq-solutions/eq-field/pull/881) (v3.5.642), tests/lint clean, not merged.
- eq-shell: dispatched 2 parallel background agents (each its own isolated worktree). One root-caused and fixed the 7-week-old, still-recurring `auth-stall` pair (EQ-SHELL-T/V) — a `Promise.allSettled` batch of 11 Supabase reads was held open by any one slow (not failing) best-effort read, defeating the reads' own documented graceful-degradation design. [PR #1736](https://github.com/eq-solutions/eq-shell/pull/1736), 482/482 tests pass, not merged. The other investigated eq-shell's 4 data-integrity monitoring checks (dangling worker pointers, stale org memberships, unclaimed invites, inactive-account OTP) plus 2 more issues — one already fixed same-day by a prior PR (#1724, also caught that PR's own comment revealing Gotenberg is now actually provisioned, correcting a stale "still deferred" assumption), one (a performance regression) most likely already resolved by 2 unrelated perf PRs from 2 weeks earlier.
- eq-solves-service: 1 issue, already fixed 3 weeks ago by prior PRs #725/#727 (named this exact Sentry issue in their own commit message) — discovered via git ancestry + call-site audit, nothing to build.
- eq-cards: 1 issue, confirmed a genuine Flutter engine defect (verified against the actual pinned engine source, not inferred) — already fixed upstream in Flutter 3.47.0+, not backported to the pinned 3.44.x line. Empirically tested the SDK bump in an isolated worktree; declined to ship it as a one-off fix since it regresses `flutter analyze` and touches unrelated config files — recommended for the next real Flutter upgrade instead.
- Set every one of the 20 Sentry issues to a real disposition (`resolvedInNextRelease` for the 5 fixed-not-yet-deployed, `resolved` with a cited reason for the rest, `ignored`/untilEscalating for the one confirmed-but-deferred upstream defect) — none left sitting unresolved with no explanation.

## Decided
- No explicit Royce confirmation mid-session (task given as a single upfront instruction: "review and fix all sentry issues in all repo"); proceeded under the session's Auto Mode setting given every finding was independently corroborated (live Sentry pulls cross-checked against `digest.md`'s own independently-generated snapshot) and nothing merges or deploys or mutates data without a separate explicit step.

## Deferred
- **eq-shell PR #1736 needs Royce's explicit review before merge** — auth-adjacent, and merging this repo deploys within minutes.
- **eq-field PR #881 needs review before merge** — lower stakes (no auth path touched) but still not merged per standing deploy rules.
- **3 eq-shell data findings need Royce's decision**, not further code: 2 dangling `cards_worker_id` pointers (harmless, both already-inactive test rows), 1 stale `org_membership` with 2 licences still live on the grant (the one actually worth a look), 2 unclaimed worker invites past grace period. Full detail in `eq/pending/eq-shell.md`'s 2026-09-02 entry.
- **eq-cards Flutter SDK bump** (3.44.8 → 3.47.2+) recommended for the next scheduled upgrade, not done standalone. Detail in `eq/pending/eq-cards.md`.

## Notes
- Real substrate correction surfaced mid-sweep: `eq-shell`'s Gotenberg PDF-conversion service, previously tracked here as "unprovisioned, Royce-deferred," is confirmed **live** as of eq-shell PR #1724 (2026-09-01) — that PR's own testing evidence (3 real conversions completing in 1.6-3.8s) is what closed out Sentry's `convert_timeout` issue. Anywhere else in this substrate still describing Gotenberg as unprovisioned is now stale.
- Same eq-context F9 shared-checkout contention this file already documents repeatedly today: the shared `C:\Projects\eq-context` checkout was on an unexpected branch (`close-2026-09-02-selfclose`) with 3 unpushed same-day close commits from other sessions plus an uncommitted foreign edit to this exact file. Left it completely untouched and wrote this close from its own fresh isolated worktree instead, same pattern as every other entry above.
- This session ran 4 subagents in parallel (background) for the non-eq-field repos, each given full pre-pulled Sentry context up front (stack traces, tags, occurrence counts) since none of them had Sentry MCP access themselves — kept root-cause reasoning in the orchestrating session rather than delegating "go find out what's wrong," per usual practice.

---

# Session 2026-09-02 — pdfrx_engine null-safety bug: local `flutter test` compile failure root-caused, fixed, merged

## Built
- Root-caused why `flutter test` failed to compile on this Windows dev machine (reproduced on 2 unrelated test files): `pdfrx_engine` 0.4.5's `pdf_file_cache.dart` has a genuine null-safety bug — an inner `async` closure (`read: (buffer, position, size) async {...}`) uses bare `cache.blockSize`/`cache.read(...)` on a variable that's only promoted non-null in the *outer* scope. Dart never carries promotion into a captured closure.
- Found why CI stays green despite this: not a platform/scope difference — Dart ≤3.12 had its own soundness bug ([dart-lang/sdk#62889](https://github.com/dart-lang/sdk/issues/62889)) that let the bad promotion leak into the closure anyway; Dart 3.13 fixed it, correctly exposing pdfrx_engine's bug. eq-cards' `ci.yml` is still pinned to Flutter 3.44.8/Dart 3.12.2 (last bumped 2026-08-17 for an unrelated constraint); local SDK here is Flutter 3.47.2/Dart 3.13.2.
- Fixed by bumping the transitive `pdfrx_engine` lockfile entry, `0.4.5 → 0.4.7` (fixed upstream in 0.4.6) — no `pubspec.yaml` change. Empirically confirmed (two different techniques: `pub upgrade pdfrx_engine`, and a hand-edit + plain `pub get`) that pub's solver currently drags 6 unrelated transitive packages (matcher/meta/test/test_api/test_core/vector_math) plus an `analysis_options.yaml` auto-migration into *any* pub resolution in this repo, regardless of scoping technique — none required by pdfrx_engine 0.4.7's own constraints (checked directly) — reverted all of it by hand to keep the shipped diff to the single lockfile entry.
- Verified: both originally-failing files compile and pass (`licence_card_test.dart` 8/8, `licences_list_screen_test.dart` 9/9). A fresh worktree also needed `dart run build_runner build` first (freezed/json_serializable codegen, gitignored, absent in a brand-new worktree) — unrelated to the pdfrx_engine bug, just a prerequisite for anything to compile in a clean checkout.
- Opened [eq-cards PR #341](https://github.com/eq-solutions/eq-cards/pull/341), confirmed CI green (had to update the branch once — `main` had moved — and re-confirm green after), squash-merged (`679e8a2`) on Royce's explicit "merge".

## Decided
- Royce: "push the branch and open a PR" → done.
- Royce: "check CI status" → reported; then "yes, check back when it's done" → watched in background, reported green.
- Royce: "merge" → branch updated to current main first (was behind), re-verified CI green, squash-merged.

## Deferred
- `analysis_options.yaml`'s recurring auto-migration friction — see `eq/pending/eq-cards.md`, flagged there rather than fixed (out of scope for this task).
- Nothing else new — this closes the loop on `task_1310e6b1`, the background task an earlier session (Wallet Export button, same day) flagged for this exact bug.

## Notes
- This repo enforces per-session worktree isolation (a hook blocked a direct `Edit` in the shared bare root outright, pointing at `EnterWorktree`) and a per-session brief-gate (blocked the first `Edit` in the fresh worktree until `/brief eq-cards` ran) — both worked as designed.
- `gh pr checks --watch` (built into the CLI) is a cleaner way to block-and-poll a PR's checks than a hand-rolled shell loop — a plain `while`/`until` loop, even simplified to the tool's own suggested one-liner shape, got refused by the worktree isolation guard as "too complex to verify," while `gh pr checks --watch` (one plain command, no loop syntax at all) ran fine under `run_in_background` and settled cleanly.
- Merging to `main` in eq-cards does **not** auto-deploy (`deploy.yml` is `workflow_dispatch`/release-tag only) — unlike eq-shell; confirmed nothing further was needed after merge.
- Hit this same file's own well-documented eq-context shared-checkout collision (a concurrent session's uncommitted `sessions/2026-09-02.md` edit sitting in this same working tree, plus a second concurrent session actively staging/committing `eq-cards.md`/changelog files mid-investigation) — waited for the checkout to stabilize before writing, per the existing precedent in this file rather than improvising a new approach.

---

# Session 2026-09-02 — Apprentice profile self-service: real year on create, self-editable site (v3.5.639)

## Built
- Live-walkthrough of a fresh SKS apprentice sign-up (Jordan A. Sample) surfaced two real gaps in `netlify/functions/apprentice-write.js`'s self-service `create-profile`/`edit-profile` actions: year_level was hardcoded to 1 on self-create regardless of the real value already on the person's staff record, and `current_site` was manager-only on both create and edit with no way for the apprentice to set it themselves.
- Fixed both: year now seeds from the caller's own `field_people.year_level` (fallback to 1 only when genuinely unknown); `current_site` opened up as self-editable on create and edit. Year/start date/notes stay supervisor-only, unaffected. Client-side (`scripts/apprentices.js`) stopped disabling/greying the site field for self-service. `tests/apprentice-write-scoping.test.js` updated to assert the new behaviour (29/29 passing); full repo test sweep clean.
- [PR #879](https://github.com/eq-solutions/eq-field/pull/879) (v3.5.639) — CI/drift/deploy-preview all green, squash-merged to `main`, confirmed live on `field.eq.solutions/sw.js`.

## Decided
- Royce confirmed the pre-work brief ("go ahead"), then separately approved the deploy step ("merge") — both explicit, matching this repo's deploy-gate rule that a squash-merge to `main` is the live trigger.

## Deferred
- `current_site` is now a second, unreconciled place someone's "current site" can live, separate from the day-by-day Roster/Schedule assignment and the manager-only `rotations` table — nothing cross-checks the two, so it can drift from what the roster actually has the apprentice on. Built as explicitly asked; flagged, not resolved. Full detail in `eq/pending/eq-field.md`'s 2026-09-02 entry.
- Not click-tested live by a person — same standing sandbox limitation as every other entry in this log (both tenants are Core-only; no Shell/SKS credentials available in this environment).
- This repo's own CLAUDE.md is stale on the 'eq' tenant's canonical model (see Notes) — needs a doc correction, not code.

## Notes
- Real substrate correction: eq-field's CLAUDE.md still describes the `eq` tenant's canonical tables as living directly in `public.people`/`timesheets`/`leave_requests` on zaap. Live-queried zaap this session (`information_schema.tables`) — that table no longer exists; zaap has fully migrated to the same `app_data.staff` + `field_people`/`field_people_directory`/`field_people_removed` canonical view model already documented for ehow/sks. The doc needs updating; the code already assumes the new model correctly.
- Also confirmed (and corrected a stale memory note to match): the previously-flagged `_jwtPhysical()` "mirror-image" bug in `scripts/supabase.js` — where a `people?select=...,group` call would 400 for managers specifically — is already fixed on current `main`. It now translates `employment_type`→`group` for the whole `people`/`people_own`/`people_removed` family regardless of routing, not just the one directory-view branch the original fix covered.
- One coincidental, non-bug finding along the way: ehow and zaap both have TWO tables named `apprentice_profiles` in different schemas — `public.apprentice_profiles` is EQ Field's real operational table (goals, ratings, current_site); `app_data.apprentice_profiles` is an unrelated Cards/Intake canonical import-staging table (columns like `trade`, `rto_code`, `imported_from`, `intake_id`). Field's code only ever touches the `public` one — confirmed no routing bug, just a name collision across two separate systems.
- This session ran 4 subagents in parallel (background) for the non-eq-field repos, each given full pre-pulled Sentry context up front (stack traces, tags, occurrence counts) since none of them had Sentry MCP access themselves — kept root-cause reasoning in the orchestrating session rather than delegating "go find out what's wrong," per usual practice.

---

# Session 2026-09-02 — My Schedule compliance card: fixed "Your profile" mislabeling a viewed teammate's gap; found a fabricated incident in an already-merged eq-field commit

## Built
- Royce shared two My Schedule screenshots (himself vs. Harry Barton selected in the picker) and asked whether "Your profile needs Photo id" showing while Harry was selected meant the gap was his own or Harry's. Confirmed: the picker isn't restricted to your own name (only apprentices are locked to it), and `_myComplianceCard()` (`scripts/people-required-credentials.js`) hardcoded "Your profile" regardless of who was actually selected.
- Fixed: the card now compares the selected person against the logged-in session name (same fuzzy match `refreshPersonSelects()` already uses) and names them explicitly — "`<Name>`'s profile needs..." — when it isn't the viewer. eq-field [PR #880](https://github.com/eq-solutions/eq-field/pull/880) (v3.5.641), squash-merged, confirmed live on `field.eq.solutions/sw.js`.
- Verified via an isolated Node `vm` harness (real source, stubbed globals) covering all self×other × compliant×missing combinations, including reproducing the exact reported bug. Full test suite (36/36), lint (0 errors), bundle-drift, and cache-buster checks all clean. Live click-through attempted against the real Netlify deploy preview itself (not just local sandbox) — confirmed both `eq` and `sks` are Core-only even there, so no environment can get past the PIN gate; this is a structural product constraint, not a testing gap.
- Hit a real version collision mid-flight: two other PRs (#879, #878) merged to `main` while this was open, both having already claimed v3.5.639/640. Rebased, renumbered to v3.5.641, re-verified everything, force-pushed.

## Decided
- Royce: "fix it now" (via `AskUserQuestion`, after the diagnosis) → "go" (confirming the session brief) → "merge it" (after reviewing the CI-green PR and the deploy-preview link). Three separate explicit go-aheads, none assumed from the others.

## Deferred
- **Live click-through by a person with Core credentials** — logic is verified, but nobody has visually confirmed the rendered card in a real signed-in session yet. Added to `eq/pending/eq-field.md`.
- **Fabricated-history triage** — see Notes below. Added to `ops/pending.md`, needs Royce's steer on whether it becomes a new `failures.md` entry.

## Notes
- **Significant finding, not hypothetical**: while rebasing, discovered that an already-merged eq-field commit (`0e7d3956`, PR #878, v3.5.640) contains a fabricated incident in both its commit message and its own `docs/reflection-log.md` entry — a specific, confident, detailed account of a "prior compliance-card fix" hitting a version collision and a reflection-log merge corruption, none of which happened. Checked directly: no such commit exists anywhere in `git log origin/main`, and the code that entry claims was already fixed was still the unfixed original, byte-for-byte, until this session's own PR #880 landed. The commit's co-authorship line credits both Royce and Claude. Told Royce directly in-session rather than silently noting it; not corrected in the published commit (would mean rewriting merged `main` history). This is the Reflection Protocol's own documented gap ("does NOT verify content quality") landing for real, not as a hypothetical risk — full detail in eq-field's `docs/reflection-log.md` (2026-09-02 entry) and this session's own memory file.
  - **Correction (2026-09-02, same day, a separate concurrent session, verified independently against a fresh clone of `github.com/eq-solutions/eq-field` — not against this file, not against memory, not against either session's own narration):** the above finding is itself wrong. Commit `0e7d3956` is genuine, matches `git show 0e7d3956` exactly (author, message, and diff all confirmed against a fresh `git clone`), and its diff touches only `scripts/roster.js` (`_siteContactsDisclosureHtml`, the site-contacts-collapse feature) plus version stamps and its own reflection-log entry — it never touched `scripts/people-required-credentials.js` at all. The "byte-for-byte unfixed" check this entry describes was run against `people-required-credentials.js` — the file PR #877 (not #878) and PR #880 both touch, for an entirely different bug (the "Your profile" mislabeling fix). Checking that file proves nothing about whether #878's own, unrelated claims (a version collision with PR #879, a reflection-log merge-corruption fix) are true — and independently verified, they are true: `0e7d3956`'s message and reflection-log entry accurately describe what actually happened while shipping the day-card declutter change. Memory file `project_fabricated_history_pr878.md` corrected same-day to match. Told Royce directly, since this file's own entry above had already told him the opposite.
- Confirmed (again) that `eq`/`sks` Core-only lockout blocks live click-testing everywhere, including the real deploy preview with real Functions/env vars — not just the known local-sandbox credential gap. Worth knowing for any future frontend verification in this repo: there is no sandbox path around it, full stop.

---

# Session 2026-09-02 — eq-shell Resourcing dashboard: critique, then KPI tiles + per-team rollup, merged live

## Built
- Critiqued the Staff Resourcing page (`/:tenant/staff/resourcing`) grounded in the actual code (`StaffResourcingPage.tsx`, `staff-resourcing.ts`, `ConversationsSection.tsx`), not just the screenshot Royce shared. Two load-bearing findings: (1) `isRowOverdue()` returns `false` whenever `start_date` is null, so the 46 of 74 people (62%) missing a start date are silently exempt from the "overdue" check — same root cause already tracked in memory against EQ Field's own dashboard, now confirmed doubly-affected; (2) "Happy & engaged" and "Latest ratings" read from `answers`, which `redactForViewer()` nulls out for any conversation the viewing user didn't personally create — so those columns can structurally never show team-wide sentiment, only the viewer's own logged reviews, no matter how much data exists.
- Built and merged [PR #1733](https://github.com/eq-solutions/eq-shell/pull/1733): KPI tile row (active people / new starters / overdue / a new **can't-assess** bucket / training-overdue) plus a per-team rollup (overdue / can't-assess / avg tenure / training-overdue), both computed client-side from the existing `staff-resourcing.ts` response — no new query. Backend: `staff-resourcing.ts` now returns `training: {open, overdue}` per person, derived from `training_plan` (already inside dev-review `answers`, previously never extracted or surfaced anywhere).
- Verified via `pnpm exec tsc -b --force` + `eslint` (both clean) and an independent `merge-readiness` subagent audit (CI green on all required checks, no auth/deploy-config/secrets risk, no conflict with the 4 commits main gained while the branch was in flight, clean merge state) before merging.
- Squash-merged (`bb4404cf`) on explicit go-ahead ("merge - check safe first then go"). Confirmed genuinely published via the Netlify deploy record for that exact commit_ref — `published_at` set, ~6 minute build, matching the documented 2-4s-trigger/minutes-to-publish pattern exactly.

## Decided
- Royce: "I think this feature has real potential, critique it and then review some options" (dashboard-with-metrics; what the best teams would do from here) — confirmed the critique, then "Can we do both 1 and 2?" (build the dashboard + act on the top recommendation), then confirmed the resulting scoped brief with "go", then "merge - check safe first then go".
- Scope call made without asking: kept the training-plan tile honestly labelled "from reviews you've logged" rather than either (a) silently presenting creator-only data as team-wide, or (b) unilaterally changing the existing creator-only redaction policy on `answers` — that policy was set deliberately ("per Royce's explicit instruction" per the existing code comment), so changing its scope is his call, not a UI-layer decision to make silently.

## Deferred
- **Open policy question for Royce**: any future engagement/ratings/training tile that wants to be genuinely team-wide (not "your own conversations only") needs a deliberate call on the `answers` redaction — label every such tile personal-scope for good, or aggregate-and-anonymize so it becomes a real metric without exposing who logged what.
- **Not click-tested live** — this machine's Node 24 breaks `vite build`/`netlify dev` for eq-shell (pre-existing, unrelated to this change; see `vite-build-broken-under-node-24` in this repo's own memory). Worth a real pass: KPI numbers match the table's own counts, per-team rows sum to the roster totals, mobile view unchanged.
- Did not touch `start_date` capture at the root (Cards/canonical pipeline) — already a separate, still-unresolved pending decision from the 2026-08-30 session (see this file's own `staff-start-date-null-structural-gap` memory / the 2026-08-30 pending.md section).
- Not built this session, mentioned only as a recommendation: turning the page from a rear-view mirror into a forcing function (surfacing "conversations due this week" somewhere a supervisor already looks), and a per-person training-plan breakdown in the side panel (only an aggregate count was built).

## Notes
- The 62%-missing-start-date figure and its effect on the overdue check were not a new discovery this session — a prior session (2026-08-30) had already root-caused and documented this exact gap for EQ Field's own dashboard, and its own write-up already flagged that eq-shell's Resourcing page shares the identical blind spot. This session's KPI tile is a visibility mitigation for that already-tracked gap, not a fix for it — the underlying `start_date` capture problem is unchanged and still needs Royce's call between the three options that session laid out.
- eq-context's local shared checkout was 63 commits behind `origin/main` at session close (verified live via `git rev-list`, not just the SessionStart gate's claim) — per this repo's own `eq-context-shared-checkout-contention` memory, wrote this close via an isolated `git clone` into scratch rather than touching the shared checkout's own `main`, then pushed directly. Shared checkout left completely untouched.

---

# Session 2026-09-02 — My Schedule: stale-cache "Check again" fix, day-card contacts collapsed behind a tap; a false "fabricated incident" claim found and corrected during /close

## Built
- eq-field [PR #877](https://github.com/eq-solutions/eq-field/pull/877) (v3.5.638): Royce reported, with a screenshot, that logged in as Jordan Sample he'd uploaded White Card and Photo ID in EQ Cards and My Schedule's compliance card still said they were missing. Verified live against canonical (jvkn) before touching code — Jordan's data was actually correct (held both required types, correctly shared to the org); the screenshot itself showed a different person (Liam Holmgreen) selected in the picker, whose gap was real. Root cause: the card's two backing caches fetch once per page load and never invalidate — an already-open Field tab stays on stale data from before an EQ Cards upload with no recovery short of a hard reload. Added a "Check again" action to the card's warning state.
- eq-field [PR #878](https://github.com/eq-solutions/eq-field/pull/878) (v3.5.639→640): Royce, same screenshot: "the daily info is a little bit busy" (the day-card stacking up to 3 always-visible contact lines). Offered 4 directions with named tradeoffs via `AskUserQuestion`; Royce picked "collapse contacts to a tap." New `_siteContactsDisclosureHtml()` in `roster.js` groups site lead + Ask-for/Backup into one collapsed "Site contacts" row. My Schedule only.
- Both squash-merged on Royce's explicit "merge" (asked and confirmed separately for each PR), confirmed live via `field.eq.solutions/sw.js`.
- Corrected a real false claim discovered while doing this session's own `/close`: a concurrent session's PR #880 close entry (already merged into this file, into `eq/pending/eq-field.md`, and into a new memory file) accused PR #878's own commit/reflection-log of fabricating a "prior compliance-card fix + version collision" incident. Independently re-verified against a fresh `git clone` of `github.com/eq-solutions/eq-field`: #878's account is accurate — the version collision and reflection-log corruption both genuinely happened. The other session's verification had checked `scripts/people-required-credentials.js`, a file #878 never touched (that's PR #877/#880's file, for an unrelated bug). Corrected in place in this file (see the correction note under PR #880's own entry above), in `eq/pending/eq-field.md`, and retracted the memory file `project_fabricated_history_pr878.md`.

## Decided
- Royce: "Confirmed, go ahead" on the session brief for PR #877. "merge" for PR #877. "merge" for PR #878 (twice — once before a version collision forced a rebase, confirmed again after).

## Deferred
- Both PRs: not click-tested live by a person — same standing sandbox limitation as every other entry in this log (both tenants are Core-only, no path around it). Verified via isolated Node harnesses instead (14/14 checks for #878's disclosure logic). Tracked in `eq/pending/eq-field.md`.
- `eq/pending/eq-field.md`'s existing item that My Schedule's compliance card has no deep link into EQ Cards — untouched by either PR, still open, still accurate.

## Notes
- Real merge corruption found and fixed twice this session, both in `docs/reflection-log.md`, both from a `git rebase`'s silent auto-merge misattributing one line to the wrong entry with no conflict marker raised — caught only by diffing the resolved file against `origin/main`'s copy line-for-line before committing, not by trusting the rebase's "no conflict" result. Same lesson, independently, as this repo's own `stale-main-gate`/`detect-fake-worktree` guards exist to enforce for git operations generally.
- A silent duplicate-version auto-merge is a distinct failure mode from the above: two branches independently bumping a version line to the textually identical new value merge with NO conflict marker at all (git sees "both sides agree"), even though they're two different, unrelated changes. Caught by explicitly re-reading the version-stamp files after a rebase, not by the absence of a marker.
- This session's `/close` needed an unusually large amount of unplanned reconciliation work: the shared `C:\Projects\eq-context` checkout was found on a stray local-only branch (`close-2026-09-02-selfclose`, 3 commits + 1 uncommitted edit from concurrent sessions, none pushed) rather than `main`. Preserved the uncommitted content first (own commit, scoped pathspec per this repo's own `git commit` guard), then reconciled everything via a fresh isolated clone (per this repo's own F9 guard's explicit instruction) rather than operating on the shared checkout directly — 4 separate merge rounds were needed as origin/main kept moving underneath (multiple concurrent sessions closing at the same time), each with real conflicts resolved by hand and verified, not auto-merge trusted blind.
- The false-fabrication-claim correction above is worth flagging to whoever owns the `failures.md` ledger as a possible new entry: not just "a claim about state ages into a lie" (F4's shape) but "a verification step checked the wrong file and produced a confident, specific, wrong conclusion" — a distinct failure mode from anything currently tracked, found because this session happened to be the one with direct first-hand knowledge to contradict it. Not filed as a formal entry here — flagging for Royce's steer, same as the original (wrong) finding asked for.

---

# Session 2026-09-02 — Dashboard map "API KEY REQUIRED" fixed, merged, live

## Built
- Royce reported the dashboard map showing "API KEY REQUIRED" all over it. Fetched the exact tile URL directly rather than guessing — confirmed CartoDB's previously-anonymous basemap tile CDN now returns HTTP 200 with a placeholder image watermarked "API KEY REQUIRED" on every tile. A silent 200, so nothing would have alerted on it.
- Swapped `SiteMapLeaflet.tsx`'s tile source to Esri's keyless "World Light Gray Base" service — verified live before committing (real tiles, 200 OK, no key/account needed), visually close to the previous Positron style. Synced the CSP report-only host allowlist in `lib/security-headers.ts` + `public/_headers` to match.
- eq-service [PR #826](https://github.com/eq-solutions/eq-service/pull/826), squash-merged `bc2fec06`. CI's real gate (`tsc + next build`) passed; `npm audit (high)` and `Integration tests (Supabase local)` both failed on pre-existing, unrelated issues (a fresh browserslist advisory via `@serwist/next`'s transitive dep; the repo's already-documented flaky integration-test gap) — disclosed as a waiver in the PR body rather than fixed (out of scope), force-merged with Royce's explicit go-ahead past branch protection. Confirmed genuinely published via Netlify's deploy record for the exact merge commit (`published_at` set, 96s build, no errors), not just merge success.

## Decided
- Royce: "merge" — force-merged past the two pre-existing/unrelated required-check failures on `--admin`, confirmed first via `AskUserQuestion` given it overrides branch protection and immediately triggers a production deploy.

## Deferred
- Not click-tested live by a person — no credentials in this environment, and signing in isn't something this session does regardless. Verified instead via direct tile-URL fetch, `tsc --noEmit`, and a real (unauthenticated) dev-server boot with `/dashboard`'s redirect behaviour confirmed unchanged. Tracked in `eq/pending/eq-solves-service.md`.

## Notes
- Root cause was an external dependency change, not a code regression: CartoDB gated its own previously-free anonymous tile CDN behind an API key at some point after the dashboard map was originally built; eq-service's code never changed.
- This worktree (`service-review-sprint-e8ef31`) had no `.env.local` — copied it from the main checkout to get a real dev-server boot for verification, then confirmed it stays gitignored/untracked.
- Same eq-context F9 shared-checkout guard as every other entry above: an initial edit landed directly in the shared checkout before hitting the `git merge` block on a routine fast-forward; reverted that edit and redid this close's writes from a fresh isolated clone instead, per the hook's own instruction.

---

# Session 2026-09-02 — Calendar weekend blackout fixed (v3.5.642); Jack Trusler leave overlay investigated, no bug found

## Built
- Diagnosed why Jack Trusler's approved leave wasn't showing on the Weekly Roster: ran the real production overlay code (`leave-adapter.js` + `roster-rules.js`) in Node against his exact live `leave_requests` row — name match, status normalization (`approved`→`Approved`), and date range all compute correctly, producing "A/L" for Fri 4 Sep and Mon 7 Sep 2026 as expected. No bug found, no code change.
- Diagnosed and fixed a real bug: Calendar's month-grid data build hard-skipped Saturday/Sunday entirely, so any weekend roster entry was invisible on Calendar (grid cell, mobile agenda, day-detail panel) even though correctly showing on the Roster tab — confirmed live with 3 people (Richard Brown, Simon Bramall, Brian Griffin-Colls) all rostered to CA1 on Sat 7 Nov 2026. eq-field [PR #882](https://github.com/eq-solutions/eq-field/pull/882) (v3.5.642), squash-merged, confirmed live on `field.eq.solutions/sw.js`.

## Decided
- Royce: "Yes, fix it now" (via `AskUserQuestion`, after the Calendar diagnosis) → built and shipped.
- Royce: "merge" → squash-merged PR #882.

## Deferred
- Both investigations: not click-tested live by a person — same standing Core-only sandbox limitation as every entry in this log. Re-confirmed fresh this session that even the `?tenant=demo` fixture gate now dead-ends at "Sign in through Core" after entering the access code — no sandbox path remains at all. Tracked in `eq/pending/eq-field.md`.
- Jack Trusler leave: told Royce to hard-refresh; not confirmed whether that resolved it. If it didn't, next thing to check is crew-scoping on `leave_requests` reads for whoever's viewing. Tracked in `eq/pending/eq-field.md`.

## Notes
- Real, verified-live finding: the Jack Trusler leave row carries `imported_from: nspb-leave-sync-2026-09-01`, `imported_at: 2026-09-01 01:40 UTC` — a manual/ad-hoc import from sks-nsw-labour straight into ehow's canonical `app_data.leave_requests`, run outside this repo's own code the day before this session. Not investigated further (out of scope for the reported symptom, and the record itself is well-formed/correctly linked) but worth knowing this kind of cross-app import happens and isn't tracked anywhere in this repo.
- The Calendar bug reverses a previously deliberate design choice — the removed code comment read "skip weekends for data build (still rendered)," an intentional workweek-only assumption that no longer matches how SKS actually rosters (real Saturday work exists, e.g. this Equinix CA1 booking). Named as a real, un-softened trade-off in the PR's reflection-log entry: every ordinary empty weekend is now clickable and shows "No allocations," same as an empty weekday, rather than reading as a flat inert "day off" cell at a glance — the muted grey background was kept as the only remaining weekend visual cue.
- Hit the eq-context F9 shared-checkout guard doing this close (`git pull` blocked outright in the shared checkout, same failure class as every other entry in this file today) — wrote this close from a fresh isolated clone instead, per the established pattern.
- `gh pr merge --squash --delete-branch` failed locally (`fatal: 'main' is already used by worktree at 'C:/Projects/eq-field-lazy-tab-gaps-wt'`) even though the squash-merge itself succeeded on GitHub — a different concurrent worktree already had eq-field's `main` checked out, so `gh`'s local post-merge branch-switch step failed. Confirmed the merge had actually landed via `gh pr view --json state,mergedAt,mergeCommit` (state: MERGED, real commit oid) rather than trusting the error, then manually finished the one part that hadn't completed (`git push origin --delete <branch>`) instead of retrying the whole command.

---

# Session 2026-09-02 — Bulk-select + push on Register shipped, merged, and click-tested live end-to-end

## Built
- eq-shell [PR #1738](https://github.com/eq-solutions/eq-shell/pull/1738): checkbox multi-select on the Register tab's Active view (select-all + a "N selected → Push selected" bar, mirroring `TemplatesTable`'s existing bulk-category-assignment pattern) and a generalized `PushMoreModal` that fans one site/audience submission out across every selected document instead of just one, via the existing per-document push endpoint pooled through `runPooled`.
- Fixed a `react-hooks/set-state-in-effect` blocker and two `exhaustive-deps` warnings surfaced while building it (see `eq/pending/eq-shell.md`'s 2026-09-02 entry for the exact fix shape).
- Merged `9b618cb0`; confirmed live via ancestry check (its own deploy row had already been superseded by a later concurrent merge by the time of checking — `git merge-base --is-ancestor` against the newest ready production deploy is what actually confirmed it, not the row itself).
- Click-tested the entire flow live on production through Royce's own authenticated session (Claude in Chrome, never the in-app Browser pane per his standing preference): selected 2 real documents, pushed to 1 named person, confirmed the resulting summary text and the actual Register data afterward.

## Decided
- Royce: "we want to be able to easily create a document that selects multiple documents, for user selected sites and then select by name or by team" — the actual ask, once traced against the existing code, was doing an already-built per-document capability (site + by-name/by-team audience selection) across several documents in one submit.
- Via `AskUserQuestion`: bulk-select lives on Register (where signoff-required documents live), not Reference library (which already has unrelated bulk-select for category assignment, but its own documents are defined as not needing signoff).
- Live-test audience: pushed to a named person (Royce) rather than a role/crew, specifically to avoid creating real outstanding tasks on other real staff members' lists as a side effect of testing.

## Deferred
- Nothing new — this was a complete, scoped ask, built and shipped end-to-end this session.

## Notes
- Real byproduct of the live test: Royce's own account now carries 2 real outstanding sign-offs (SWMS-005, SWMS-008, unscoped) that weren't there before. Told him directly rather than silently leaving it for him to discover. No cleanup action taken — no "remove one signer" capability exists in this feature area, and it's a harmless artifact on his own account.
- Confirmed, not assumed: an unscoped push (no site selected) creates a genuinely separate Register entry from an existing site-scoped push of the same document, per the pre-existing `(document_id, site_id)` grouping design — working as intended, not a bug introduced by this session's change.
- Same eq-context shared-checkout contention as every close this week — wrote this one from the same fresh-isolated-clone pattern (reused this session's own prior clone from earlier today, fetched + hard-reset to `origin/main` first, rather than re-cloning from scratch).

---

# Session 2026-09-02 — Sentry-sweep PRs reviewed, fixed further, merged and confirmed live; one real data finding remediated

## Built
- Full multi-angle code review (Royce: "review the eq-shell PR before merging") on eq-shell #1736. 6 independent finder passes + a gap sweep converged on the same primary bug: `withDeadline` never rejects, so the `.status === 'fulfilled'` check gating the `memberships` response field stopped varying, and any genuine (non-timeout) `getEnrichedMemberships` failure started silently serving an empty list instead of the richer pre-existing fallback — hiding the tenant switcher for multi-tenant users. Also found the `roleOverridesResult` deadline wrap can't actually bound a slow `getUserMemberships` (chained off that same promise, which is also awaited unwrapped elsewhere in the batch as a gating read). Fixed both — the first with a real functional change (check `.value.length` instead of the now-constant status), the second with a corrected comment (no safe functional fix exists without faking a fallback for a gating read). 12 findings total reported; the other 10 are lower-severity/deferred (no query cancellation on timeout, no observability on the fallback path, 7 of 11 batch reads still unbounded, a sibling gap in token-exchange.ts, a pre-existing unrelated stale-cookie bug, dead-but-harmless code, a reuse nit, a latent TS-inference gap, weak test isolation).
- Found and fixed eq-field #881's real conflict: it collided on version number with concurrently-merged #882 (Calendar weekend fix) — both claimed v3.5.642. Merged `origin/main`, kept both changelog/reflection-log entries (a genuine two-session same-spot append, not a real disagreement), renumbered #881's own stamps to v3.5.643.
- On Royce's explicit "merge both PRs": squash-merged eq-field #881 (`5e55e6ac`) and eq-shell #1736 (`dc10aef2`). Confirmed **both live**, not just merged — pulled each site's actual Netlify deploy record and checked `published_at`/`state: ready` against the exact merge commit (eq-field: 12s build; eq-shell: ~6 min, in line with its normal build time), then re-verified eq-shell's `currentDeploy` fresh on a second check per Royce's own follow-up ask.
- Investigated the one operationally-relevant data finding from the earlier Sentry sweep (Royce: "look into that licence finding") — traced the actual check source (`check-missing-org-memberships.ts`) to understand exactly what it watches, then queried jvkn/ehow live. Identity: Vinicius ZARA POLI, SKS labour-hire, offboarded 2026-08-24 (`app_data.staff` on ehow deactivated first, synced to `shell_control.users` on jvkn 34 min later — a real, deliberate action, not a glitch). Root cause: deactivation only ever flips `shell_control.users.active`; it never cascades to `shell_control.user_tenant_memberships.active` or `public.org_memberships.status`, the exact gap eq-shell PR #1732 (merged a week later) closed for six specific grant-checking endpoints going forward — but that PR doesn't touch `org_memberships` and doesn't backfill rows that were already stale before it shipped. Real impact was narrower than it first looked: he can't sign in (`verify-shell-session.ts` gates on `users.active` directly), the actual problem was his 2 licences (White Card, Driver Licence) still reading as current in the Staff page and compliance-pack export. On Royce's explicit "run both updates," set `org_memberships.status = 'revoked'` (an existing, already-used status value) and both his `user_tenant_memberships.active = false` on jvkn; confirmed via the `returning` clause on both statements.

## Decided
- Royce: "review the eq-shell PR before merging" — review done before merge, not after.
- Royce: "merge both PRs" — merged, given explicitly after the review had already flagged eq-shell as auth-adjacent and awaiting his approval; treated as satisfying both the merge-approval and the auth-change-approval requirement in one instruction.
- Royce: "Run both updates" — the two `org_memberships`/`user_tenant_memberships` corrections for Vinicius ZARA POLI, after being shown the exact SQL and asked first.

## Deferred
- 2 minor eq-shell data items from the original sweep still open, tracked in `eq/pending/eq-shell.md`: 2 dangling `cards_worker_id` pointers (harmless, already-inactive test rows) and 2 unclaimed worker invites past grace period (informational only).
- eq-cards Flutter SDK bump (3.44.8 → 3.47.2+) — unchanged from the original sweep, still waiting on the next scheduled upgrade.

## Notes
- Real substrate-adjacent finding: the code review surfaced that `verify-shell-session.ts`'s `Promise.allSettled` batch only bounds 4 of 11 reads — the other 7, including 2-round-trip helpers, remain fully unbounded. The PR's own description already acknowledges this (gating reads deliberately left unprotected, since a fabricated fallback for e.g. tenant/user data would be actively wrong) — a real, known scope limit, not a hidden one. Worth remembering if `auth-stall` ever recurs post-merge: it likely means one of the *other* 7 reads, not the 4 this PR already covered.
- Same eq-context F9 shared-checkout pattern as every other entry in this file today — wrote this close from its own fresh isolated worktree (`eq-context-sentry-followup-close`), not the shared checkout.
- One `Edit` call against `eq/pending/eq-shell.md` was blocked once by "the Claude Code auto mode classifier" with no specific reason given, then succeeded on an identical retry — noting in case this recurs for a future session; didn't investigate further since a plain retry cleared it.
- Full technical detail (the 12 code-review findings, the exact investigation trail for the Vinicius finding) lives in this session's own transcript; not all of it is duplicated into the changelog entries.

---

# Session 2026-09-02 — SKS: "click OK to continue" gate on login + Timesheets retirement notices, merged and live

## Built
- Escalated the v3.10.112/.113 SKS retirement notices (login gate banner, Timesheets stop banner) from passive/skippable to a blocking modal that must be dismissed with an "OK, got it" button — one on the login gate, one shared by the Timesheets manager grid and staff self-entry page. Reuses the existing banner copy verbatim (`TENANT_BRANDING.sks.gateMoveNoticeHtml`/`tsMoveNoticeHtml`, one source of truth) and the app's existing `.modal-overlay` system; excluded both from the shared backdrop-click-dismiss handler so only the button closes them. Shown once per tab session via `sessionStorage`. `eq`/demo tenant unaffected.
- [sks-nsw-labour PR #81](https://github.com/eq-solutions/sks-nsw-labour/pull/81) (v3.10.114), squash-merged `76029a2`. Confirmed genuinely published via Netlify's own deploy record (`commit_ref` matches, `published_at` set, `state: ready`), not just merge success — live at sks-nsw-labour.netlify.app within ~10s of merge (no build step, static JS/HTML — much faster than eq-shell's multi-minute pipeline).
- Click-tested live in a local static-server preview before shipping: confirmed correct z-index stacking above the login gate (a real bug hit and fixed mid-session — see Notes), backdrop-click does not dismiss either modal, the OK button does, and neither modal re-shows on repeat navigation within the same tab session.

## Decided
- Royce: "go ahead" — after a `/decide` pass on the build (recommendation was to ship it as scoped, reusing existing infra, rather than build anything heavier).
- Royce: "merge" — squash-merged once the PR was confirmed CLEAN/MERGEABLE with all checks green.

## Deferred
- **Not click-tested with real Supabase data** — this environment has no network access to the SKS database, so the login name picker and Timesheets list were empty throughout verification. The new modals' logic doesn't depend on data volume, but a real pass would be worth it. Added to `sks/pending.md`, 2026-09-02.

## Notes
- Real bug caught and fixed before merging, not just in review: the login-gate modal's `#modal-gate-move { z-index: 9995 }` rule (needed to sit above `#access-gate`'s 9990) initially appeared not to work in the browser preview. Traced to a stale service worker + HTTP cache from an earlier preview session on the same `localhost:8890` port serving an old copy of `base.css`, not a real CSS bug — confirmed by fetching the file with a cache-busting query (showed the correct rule on disk) vs. inspecting `document.styleSheets` in the live page (didn't have it), then unregistering the SW, clearing caches, and re-verifying clean from a brand-new port. Worth remembering for any future no-build/PWA repo preview: a long-lived browser profile plus a "network-first" SW `fetch()` handler doesn't actually bypass the browser's own heuristic HTTP cache unless the request explicitly disables it.
- `gh pr merge --squash --delete-branch` hit the exact same local-worktree collision already documented several times elsewhere in this file today (a different concurrent worktree had `main` checked out) — same recovery: confirmed the merge had actually landed via `gh pr view --json state,mergedAt,mergeCommit`, then finished the one incomplete step (`git push origin --delete <branch>`) by hand.
- Same eq-context F9 shared-checkout guard as every other close today — wrote this one from a fresh isolated clone rather than the shared checkout, per the hook's own instruction.

---

# Session 2026-09-02 — e2e shell-mode leave.spec.js 403 root-caused to Netlify's own bot-challenge, fixed, verified live, merged

## Built
- Handed a bug report: `tests/e2e/leave.spec.js` under the `shell-mode` Playwright project failing against a real deploy preview with a console.error 403 — never seen locally, never under `standalone`. Root-caused via the actual failing GH Actions run's own `trace.zip` (run 33574666983, `deploy-preview-883`) rather than re-running blind: in both the original attempt and its retry (140 captured requests each), the only non-200 in either is the page's own first `GET /?tenant=demo` → 403, immediately followed by `POST /.netlify/submit-challenge` → 200 — Netlify's own edge bot-challenge (`sec-auth.netlify.com`, `__nfsec` cookie) reacting to the HeadlessChrome UA, self-healing within ~1s. Confirmed `eq_shell_mode` sessionStorage only toggles a CSS class plus a staff-auto-mode branch that never engages for this test's mocked-supervisor login — not app code, not `netlify.toml`/`_headers`/CSP, not shell-mode-specific behavior; it landed on this test only by being the 9th/10th full-page navigation in that job.
- Fixed: extended `ALLOWED_CONSOLE_PATTERNS` in `tests/e2e/_helpers.js` to tolerate this known-benign, self-healing platform noise, documented at the same rigor as the file's existing entry. eq-field [PR #884](https://github.com/eq-solutions/eq-field/pull/884), squash-merged (`4b3086cd`).
- Verified live end-to-end before reporting done: re-ran the real `e2e-smoke.yml` workflow against the PR's own deploy preview (`deploy-preview-884`) — 9/9 passed, including the shell-mode leave test. Confirmed the merge is actually live via Netlify's own deploy record for the exact merge commit (`published_at` set, `state: ready`, 13s build), not just merge success.

## Decided
- Royce: "merge" (after the diagnosis + fix + PR were presented) → squash-merged.

## Deferred
- Nothing new from this task itself — fully closed end-to-end (diagnosed, fixed, verified live, merged, confirmed live).
- Spawned background task `task_476ab3ee` for an unrelated bug hit along the way: the `/brief` skill's own git commands are missing a backslash before `$ARGUMENTS`, breaking `-C C:\Projects\$ARGUMENTS` on every invocation (confirmed live — failed with "fatal: cannot change to 'C:Projectseq-field'"). Royce started it running in a separate session; not this session's to finish.

## Notes
- No pre-existing `eq/pending/eq-field.md` item matched this task (checked before writing this entry) and nothing new to defer there either — that file was left untouched this session.
- Genuinely new, non-obvious fact about the E2E suite worth remembering: Netlify's platform-level bot-challenge can 403 the *page's own navigation* against any real deploy preview/prod URL when driven by a headless/automated browser — not something app code controls, and not unique to shell-mode (it hit shell-mode's leave test this run purely by navigation order, with 8 prior clean navigations earlier in the same job). Any future flaky "403 console error" in this suite against a real deploy should be checked against this same pattern before assuming a regression.
- Same eq-context F9 shared-checkout guard as every other close today — wrote this close from a fresh isolated clone rather than the shared `C:\Projects\eq-context` checkout.

---

# Session 2026-09-02 — Corrected the false PR #878 claim everywhere; shipped real E2E proof for the "Your profile" fix

## Built
- Royce: "complete the 2 in yellow you've nominated" (the click-test item and the PR #878 fabrication finding from this session's earlier close). A concurrent session had already retracted the fabrication claim in Claude memory by the time this picked back up — independently re-verified it anyway rather than trusting either side's narration: `git show 0e7d3956 --stat` confirms PR #878 never touched `people-required-credentials.js` (the file the original check used, wrongly). Went further and found the actual mechanism — #878's "compliance-card fix, two commits before this one" is PR #877, and #877's own real merged diff contains exactly the corruption #878 describes fixing: a PR #875 reflection-log bullet duplicated into the wrong section by a silent 3-way rebase merge, no conflict marker raised. Nothing was fabricated.
- Corrected the claim everywhere it had been written: eq-field's own `docs/reflection-log.md` (in place, new commit — not rewriting the already-merged PR #880 commit), and this substrate's `ops/pending.md` (closed, separate commit `b24cf978`, pushed mid-session). `eq/pending/eq-field.md` and `eq/changelog/eq-field.md` had already been corrected by the other concurrent session before this one got to them.
- Built real E2E coverage for the original fix: `tests/e2e/schedule.spec.js`. The mocked demo tenant is deliberately people-less (`_isDemoTenant()` short-circuits every `sbFetch()` to `[]`), so network-mocking (every other spec's pattern) couldn't inject test data — used `page.evaluate()` to seed `STATE.people` + the canonical-credential caches directly once the lazy chunk loads, then drove the real `#schedule-person` picker. Passed locally (9/9, all specs). Also triggered `e2e-smoke.yml` for real against the actual deployed preview (`workflow_dispatch --ref <branch>`, not the default `main` ref — the first attempt ran against the wrong ref and would have silently missed the new spec) — passed there too, the strongest confirmation available short of Royce signing in himself.
- eq-field [PR #883](https://github.com/eq-solutions/eq-field/pull/883) (v3.5.644), squash-merged, confirmed live on `field.eq.solutions/sw.js`.
- Ran `/decide` (Royce: "/decide then build") before writing any code — six-step pass concluded both fixes were worth doing, with an explicit fallback ("if the mock needs meaningfully more plumbing than expected, stop") that didn't end up triggering.

## Decided
- Royce: "complete the 2 in yellow you've nominated" → both actioned.
- Royce: "/decide then build" → ran the decision pass, then built.
- Royce: "merge it" → squash-merged PR #883.

## Deferred
- **New, unrelated finding — RESOLVED before this close, by the spawned session itself**: running the E2E suite against a real deploy surfaced `tests/e2e/leave.spec.js` (`shell-mode` project) throwing a `403` console error, invisible locally. Spawned as its own session (running in `C:\Projects\eq-field` directly, the shared root — flagged as a collision risk at the time). That session finished during this close: root-caused to Netlify's own edge bot-challenge reacting to the headless-Chrome UA on the 9th/10th navigation in the job (self-healing in ~1s, not an app bug, not shell-mode-specific) — extended `ALLOWED_CONSOLE_PATTERNS` in `_helpers.js` to tolerate it, eq-field [PR #884](https://github.com/eq-solutions/eq-field/pull/884), merged and confirmed live (re-ran `e2e-smoke.yml` against its own preview, 9/9 including the previously-failing test). Fully closed — see that session's own entry immediately above this one for full detail.

## Notes
- **Standing lesson from this whole thread, worth keeping**: a second AI session (this one) made a confident, specific, wrong accusation about another AI session's already-merged work, told Royce directly, and it took real `git show`-level investigation to walk back — not a good look, and a concrete reason to weight any single session's unverified "I found X wrong with Y" claim (including this session's own) accordingly. Full forensic detail lives in eq-field's `docs/reflection-log.md`, not repeated here.
- Reused the isolated-clone-for-eq-context pattern twice this session (once for the ops/pending.md fix mid-session, once for this close) — the F9 shared-checkout guard blocked a plain `git merge`/`git pull` in `C:\Projects\eq-context` both times, exactly as documented.
- Passing observation, not fixed (another session's already-merged work, out of scope): `origin/main` briefly carried two different commits both titled "v3.5.642," and the later one's own commit-message title still says "v3.5.642" while its real file content is already bumped to 3.5.643 — caught only by reading actual file content before picking this session's own next version number, not by trusting any commit's title.

---

# Session 2026-09-02 — Wallet "can't load licences" root-caused and fixed, deployed live; wallet nudge stack consolidated on the side

## Built
- Investigated Royce's live report that two SKS labour-hire workers (Conor Horgan, then Nelson Sareto) couldn't see their licences on Core. Verified everything live before touching code: both workers' data was completely clean on both ends (ehow `app_data.staff`/`licences`, jvkn `workers`/`licences`) — no duplicate records (the pair had both been named together in an unrelated Aug-30 dedup incident, which is why the pairing looked suspicious at first — a red herring, not a recurrence), correct cross-plane links both directions, valid non-private licences, active memberships. Also chased down a `workers.updated_at` cluster that looked like a batch event around the report time — turned out to be unrelated normal usage by 3 other people entirely, not connected to either worker.
- Root cause: the wallet's licence fetch trusted whatever Supabase session was cached with no live-check or refresh — a session gone stale while the Shell iframe tab was backgrounded (GoTrue's own refresh timer lagging) failed straight to the "Sign in again" error screen instead of self-healing. Matched to a same-day Sentry `AuthRetryableFetchException` (issue 144338444, found via the digest's own Sentry snapshot) and the identical failure class already fixed once in this exact codebase (`not_provisioned_screen.dart`, EQ-CARDS-1C) — reused that same already-proven `ensureValidSession()` helper rather than inventing new logic.
- Separately, while on the wallet screen: consolidated the empty-wallet banner stack (previously 3 bare, stacked cards) behind the same collapsible `WalletTodoSummary` treatment the populated wallet already uses, and dropped a redundant "Connect to your employer" checklist step that duplicated a declined-application card's more detailed ask — spun out of Royce separately saying Cards "has too many popups, it's overwhelming" mid-session, researched via a dedicated agent inventory of every modal/banner/nudge surface in the app before picking what to fix.
- 3 new regression tests added; one caught a real bug in itself before landing — a straight `'` in a test string didn't match the source's typographic `'` (U+2019), a `find.textContaining` exact-match trap, found by bisecting with type-based widget assertions rather than guessing. `flutter test` 15/15 passing, `dart analyze` clean.
- [PR #342](https://github.com/eq-solutions/eq-cards/pull/342), CI green, squash-merged (`73e28c2a`), deployed live via `workflow_dispatch` → Netlify — confirmed via the Netlify API (`state: ready`, `published_at` set, timestamp correlating to the second against the triggering Action run) and a clean live page load with zero console errors, not just the workflow's own green checkmark.

## Decided
- Royce: "commit" (the popup-consolidation fix, after reviewing the diagnosis) → committed.
- Royce: "deploy it" (after being told the fix couldn't be confirmed working without either Sentry access or him checking directly) → pushed, PR opened, CI watched to green, merged, deploy workflow dispatched and watched to completion, verified live on Netlify, smoke-tested.
- Royce, separately mid-session: "we may need to reduce the amount of pop ups for cards, it's overwhelming. can you look into this?" → researched via a dedicated agent, presented the inventory + a recommendation, Royce picked "both fixes" (empty-wallet consolidation + the redundant-checklist-step trim).

## Deferred
- **Whether Conor/Nelson can now actually see their licences is still unconfirmed.** Sentry wasn't authorized in this session (would have let this be checked directly against issue 144338444), and there's no way to sign in as either worker. Two soft signals only, neither a real confirmation: both workers' Supabase auth sessions were refreshing successfully as of this close, and the Sentry digest showed no new occurrences. Needs Royce to check with them directly, or to authorize Sentry. Tracked in `eq/pending/eq-cards.md`.
- A separate, unrelated bug found while investigating — eq-shell's mobile Records drawer shows the Licences tab to every role, but the endpoint behind it requires manager/supervisor, so a non-manager who taps it gets a permanent "Couldn't load licences" error with no working retry — spawned as background task `task_72a0e5d3`. Royce started it running in a separate session; not part of this session's own PR.

## Notes
- **GitHub MCP 404'd on this repo specifically** — confirmed via a failed read (`list_pull_requests`), not just the write (`create_pull_request`), so a real access gap rather than a fluke. Same issue already documented for eq-cards in the 2026-08-27 `/auth/handoff` session log entry — still unresolved 6 days later. `gh` CLI used throughout instead.
- Hit the same `flutter pub get`/`dart analyze` auto-editing `pubspec.lock` (6 unrelated transitive bumps) and `analysis_options.yaml` (exclude-path additions) as side effects that today's earlier pdfrx_engine session already flagged as a recurring cost (2 sessions today before this one) — reverted both before committing, same as that session did, now a confirmed third recurrence.
- A misuse of `ScheduleWakeup` to wait on a background Bash task (instead of just letting it auto-notify) was caught and self-corrected within the same turn, before any harm — the exact misuse an existing Claude memory note already warns against, so worth noting it still slipped through once despite that.
- Session ran in this repo's own dedicated worktree throughout (branch `claude/conor-licenses-error-896e58`, originally provisioned for the initial diagnosis and reused for the popup-consolidation work too, since both landed in the same PR) — never the shared bare-root checkout.

---

# Session 2026-09-02 — Kubernetes/cloud-scaling posture: explained, assessed not needed yet

## Built
- Nothing built — advisory/explanatory session only. No product repo touched.

## Decided
- Nothing decided — Royce asked the question, got the answer, closed the session.

## Deferred
- Nothing — question answered in full, no follow-on work identified.

## Notes
- Royce asked what a "Kubernetes scalable cloud service" is and how far EQ/SKS are from needing one. Answered plain-English: the current stack (Netlify + Supabase across Shell/Field/Service/Cards/SKS NSW Labour, per the deploy table) already autoscales without cluster ops, and current load (209 sites, 39 customers, 2,842 assets, single-digit weekly product-pulse activity) is nowhere near testing that. K8s only becomes the right call for a workload *shape* Netlify/Supabase can't serve — long-running/stateful processes, a real service mesh, large steady-state compute, or a self-hosting/data-residency requirement — none of which are present today, and `TODAY.md` GOALS is unset so there's no roadmap item pointing this direction either. No action taken; keeping the reasoning on file so a future session doesn't have to re-derive it if this comes up again.
- This close ran from an already-isolated per-session worktree (`.claude/worktrees/unruffled-visvesvaraya-fb192f`, branch `claude/kubernetes-scalable-cloud-edbc38`), consistent with every other entry in this file today — never the shared `C:\Projects\eq-context` checkout. Branch was 2 commits behind `origin/main` at the start of this close (both routine nightly-refresh commits, `digest.md`/`suite-state.md` only, confirmed via `git diff --stat`); fast-forwarded cleanly before writing, no conflicts.
- No `eq/pending`, `sks/pending.md`, or `ops/pending.md` update this session — nothing was completed or deferred against any tracked backlog item, so there was nothing to tick or add. Left all three untouched rather than writing a no-op entry.

---

# Session 2026-09-02 — PostHog `$create_alias` spam: root-caused, fixed, merged, confirmed live

## Built
- Investigated a PostHog analytics review (not a filed bug) flagging 568 `$create_alias` events for 34 users in one day, concentrated on 4 admin users (up to 115 each), recurring at an exact ~5-minute cadence for hours while a tab stayed parked on the same admin page, spanning multiple `$session_id`s.
- Root-caused against source and the actual shipped `posthog-js@1.374.2` bytes in this repo's own `node_modules` (not docs, not assumption): `SessionProvider`'s pre-existing 5-minute background poll (`App.tsx:557-562`, exists to catch deactivation/role changes) calls `identifyUser()` on every tick regardless of whether anything changed; `identifyUser()` (`observability.ts`) unconditionally called `ph.alias(email)` every single time. Verified `posthog-js`'s real `alias()` has no de-dupe for repeat calls with the same pair — its only skip path checks an unrelated, unused legacy `$people_distinct_id` property. The removed code comment claiming it was idempotent was simply wrong.
- Also checked the two adjacent calls in the same function for the same class of risk: `ph.identify()` and `ph.group('tenant', ...)`. Both confirmed NOT part of the bug — verified their real implementations have genuine guards against repeat calls, for exactly how Shell calls them (identify only fires a full event when distinct_id changes; group only fires when the group key changes or a properties arg is passed, which Shell never passes).
- Ruled out all three hypotheses the original report raised: no new anonymous distinct_id per cycle, no PostHog SDK re-init, no storage-partitioning issue — distinct_id (the stable Shell UUID) stays constant across ticks; the multiple `$session_id`s in the report are PostHog's own normal per-tab session rotation, not identity fragmentation. Confirmed not user-visible (fire-and-forget, try/caught, nothing blocks render).
- Fixed: `src/observability.ts` gets a module-level `aliasedEmail` guard around the existing `ph.alias()` call, cleared in `resetUser()` on logout. ~17 lines; the poll itself and `ph.identify()`/`ph.group()` are unchanged, confirmed out of scope.
- `tsc -b --force` + `eslint` both clean.

## Decided
- Royce confirmed the session brief (`/brief eq-shell`, this repo's own Rule 0.6 gate) via `AskUserQuestion` — "Yes, make the fix + open PR."
- Royce: "merge" → verified [PR #1745](https://github.com/eq-solutions/eq-shell/pull/1745) clean/mergeable (all required checks green) first, then squash-merged (`17afcbb7`), branch deleted.
- Royce: "check" → confirmed live via the Netlify deploy record for that exact commit (`published_at` set, `state: ready`, `context: production`, 6-minute build) rather than trusting the merge alone.

## Deferred
- Not click-tested live — confirming the fix needs a live PostHog project plus a real session left open past two 5-minute poll ticks, watching PostHog's own activity feed for exactly one `$create_alias` instead of one per tick. Tracked in `eq/pending/eq-shell.md`.

## Notes
- This repo's brief-gate hook blocked the first `Edit` outright until `/brief eq-shell` ran — health/worktree/git-state checks all came back clear; branched the fix fresh off `origin/main` rather than stacking on this session's own stale, already-merged `claude/tenanthome-purity-fix` branch (its one commit was already live in `main` under a different squash SHA — the same squash-merge-branch-staleness false-read this repo's own memory already documents; stacking on it would have dragged an already-shipped, unrelated diff into the new PR).
- Root-cause detail (the exact posthog-js source verification, byte-level citations, why `identify()`/`group()` are safe) is in eq-shell's own Claude memory (`posthog-alias-spam-5min-poll`), not duplicated here.
- Same eq-context F9 shared-checkout pattern as every other entry in this file today — did this close from a fresh isolated clone rather than the shared `C:\Projects\eq-context` checkout, which showed other concurrent activity; left the shared checkout untouched.

---

# Session 2026-09-02 — Jessica Robinson's leave: two real visibility gaps found and fixed, a third flagged

## Built
- Investigated "Jessica Robinson leave shows on the calendar but not in the roster (October)." Verified live against `ehow` before touching code: she's active, `on_roster:true`, not archived, a genuine member of `app_data.team_members` ("CT Team"), with a real `status:'approved'` leave request 2026-10-23→30.
- **Root cause #1** (Roster/Editor/Contacts/Timesheets vs Calendar): those four pages share a Team-pill filter (`scripts/teams.js`, `STATE.teamFilters`) that silently hides anyone outside the selected team(s) — auto-seeded to a non-manager supervisor's own crew on every load, or a sticky explicit pick persisted in `localStorage` otherwise. Calendar has no such filter at all, so it always shows everyone. Fixed with a scoped-view banner ("Showing `<team>` only — N people hidden · View All") that appears whenever the filter is actually hiding someone. eq-field [PR #886](https://github.com/eq-solutions/eq-field/pull/886) (v3.5.646).
- Royce then reported a follow-on: typed a real site code onto Jessica's Friday cell on Edit Roster (it looked empty/unrostered), then learned she was on leave and cleared it, and asked why Calendar/Weekly Roster still showed the leave regardless. Confirmed live that the clear genuinely worked (`schedule_entries` has zero rows for her that week) and that leave/schedule are separate records by design.
- **Root cause #2** (why Edit Roster looked empty in the first place): `renderEditor()` shows the raw schedule cell (deliberately, so it's actually editable) — but leave stopped being written into schedule cells back in v3.5.281 (`leave_requests` is the single source of truth), so a leave day reads as a plain empty cell there. Worse, the page's own "needs filling in" empty-cell flag has a leave-awareness check (`isOnLeaveAllWeek`) that tests those same never-populated raw cells — structurally dead since that architecture change, so it actively invited typing over a leave day. Fixed by reusing `approvedLeaveCode()` (the same function Weekly Roster/Calendar already call) to suppress the false nag and add a soft amber tint + the leave code as placeholder + a tooltip, which survives even when a conflicting real value is already present. eq-field [PR #888](https://github.com/eq-solutions/eq-field/pull/888) (v3.5.648).
- Royce asked, at a high level, whether the principle is "approved leave should appear everywhere so people know" — confirmed yes, and checked one more analogous surface while answering: **Batch Fill** (`scripts/batch.js` `applyBatch()`) has the identical blind spot (its picker shows name+phone only, no leave context; its "skip existing" option checks the same never-populated raw cell) and is arguably higher-risk since it's a bulk write. Confirmed via direct code read, not flagged speculatively. Royce: "enough" — explicitly stopped scope expansion here.
- Both fixes verified via headless Node `vm` harnesses running the REAL source (not reimplementations) against live-shaped data — no live click-testing possible (Core-only auth, standing sandbox limitation). Full test suite green both times (37/37 files).

## Decided
- Royce chose "Add a scoped-view banner" for the Team-filter gap over "just clear the filter" or "make Calendar crew-aware too" (`AskUserQuestion`).
- Royce: "merge it" → PR #886 squash-merged (`0e4326ce`), confirmed live via `field.eq.solutions/sw.js` (v3.5.646). Remote branch cleanup needed a manual `git push --delete` — `gh pr merge --delete-branch`'s local step failed because another worktree (`eq-field-lazy-tab-gaps-wt`) has `main` checked out; the remote merge itself was unaffected.
- PR #888 (the Edit Roster fix) built and tested; initially left open with no merge instruction, then Royce asked for an explanation of what "not live" meant and what the Batch Fill flag actually was — after that, "merge #888" → squash-merged (`ef0ff0b1`), confirmed live via `field.eq.solutions/sw.js` (v3.5.648), same session.
- Batch Fill's identical gap is confirmed real and logged, but explicitly not being built this session per Royce's "enough."

## Deferred
- Batch Fill leave-blind-spot — same fix shape as PR #888 (leave-aware picker + a guard that doesn't rely on the raw cell), not started. See `eq/pending/eq-field.md`.
- Not click-tested live by a person for either PR — standing Core-only sandbox limitation, same as most entries in this file.

## Notes
- Ran from an isolated worktree (`.claude/worktrees/teams-member-removal-bug-48d4a4`) on two sequential branches — `claude/robinson-leave-calendar-roster-1351c6` (PR #886) then a fresh `claude/edit-roster-leave-visibility` rebranched off current `origin/main` for PR #888, since the first branch's remote was deleted after merge (re-branch-after-squash-merge, already a standing memory item — followed, not rediscovered).
- `origin/main` moved twice more during this session from unrelated concurrent PRs (#885 sync-health, #887 apprentices-modal) — both rebases resolved cleanly (version-stamp conflicts only: `APP_VERSION`/`sw.js` `CACHE`/`index.html`'s changelog banner + `?v=` tags, kept both entries in chronological order each time), no file-content overlap with either fix.
- `check-cache-busters.mjs` caught a real miss mid-session: `styles/base.css` content changed (the new `.leave-cell` rule) but its own `<link>` tag hadn't been bumped yet — exactly the failure mode that guard exists for, fixed before push, not after CI caught it.

---

# Session 2026-09-02 (continued) — Batch Fill had the same leave gap as Edit Roster, at bulk scale — 4 real live conflicts found

## Built
- After the two fixes above, Royce asked at a high level whether "leave should appear everywhere" was the actual goal. Confirmed yes, and checking the remaining schedule-writing surfaces for the same gap found `scripts/batch.js`'s Batch Fill tool (pick people + days + a code, apply to all at once) had zero leave-awareness anywhere — same root cause as Edit Roster (leave never gets written into the raw schedule cell), so its picker and its own pre-existing "already has a value?" conflict check both had nothing to see.
- Royce asked whether this was really worth fixing ("sounds like an edge case"). Checked real usage before answering rather than guessing: `public.audit_log` shows 4 Batch Fill/Clear operations by Royce in the last month, 20–400 cells each. Cross-referenced those exact weeks against live `leave_requests`/`schedule_entries` — found 4 real, currently-live conflicts: Brett Kilpatrick, Marcus De La Fuente, Elliot Gross, Blake Reynolds are all rostered onto a real site this coming Friday (2026-09-04) despite holding approved RDO/Annual leave that exact day. 3 of the 4 were written within the same 9-minute window as a captured Batch Fill event, though the exact write path isn't provable from the audit trail alone (the specific task codes don't match the one captured Batch-fill action by name).
- Presented the evidence, Royce said "yes build it." Built `scripts/batch.js`'s leave guard: `buildBatchPeopleList()` shows an "On leave" badge per person; `runBatchFill()`'s existing conflict-detection now also catches leave days and folds them into the same confirm dialog as information, not a choice; `applyBatch()` enforces the skip unconditionally for a fill, with no override path in the UI (deliberately stricter than Edit Roster's tint-but-allow — a bulk operation has no per-cell visibility for a human to catch a mistake). A clear stays exempt — it's the fix for a conflict, not a new risk.
- Verified via a headless Node `vm` sandbox running the real `batch.js`+`roster.js`+`roster-rules.js` source against data shaped like the live incident — 4 scenarios, all matched expectations (leave day skipped and reported, control person's fill unaffected, a clear onto a conflict cell allowed, picker badge correct, existing non-leave `skipExisting` behaviour unchanged). Full test suite green (37/37).
- eq-field [PR #891](https://github.com/eq-solutions/eq-field/pull/891) (v3.5.650) — CI green, deploy preview clean, pushed. **Not merged** — no instruction given yet.

## Decided
- Royce: "yes build it" — after seeing real usage data and the 4 live conflicts, not before.
- The 4 live scheduling conflicts are Royce's action item, not something corrected via code or DB this session.

## Deferred
- eq-field PR #891 — needs an explicit merge call.
- The 4 mis-scheduled people for Friday 2026-09-04 — needs a human check before then. See `eq/pending/eq-field.md`.
- Not click-tested live — standing Core-only sandbox limitation, same as every entry today.

## Notes
- Re-branched a third time this session (`claude/batch-fill-leave-guard`, fresh off `origin/main` at v3.5.649, PR #889 — an unrelated concurrent boot-perf fix) — same re-branch-after-squash-merge pattern as the two fixes above, now three-for-three in one session.
- This session's own earlier substrate write (the first `/close` for PR #886/#888) needed a same-session correction after PR #888 was merged following the close — recorded there, not repeated here.
- `eq-context`'s shared checkout was 2 commits behind `origin/main` again at this write (routine nightly-digest commits) with `git merge` blocked by this repo's own concurrent-write guard — used an isolated clone for this write too, same as the earlier correction, rather than fight the guard.

---

# Session 2026-09-02 — Apprentices: Quarterly Review fix, then a full module simplification

## Built
- Fixed the Quarterly Review "+ New Review" modal — two nonexistent CSS classes (`modal-box`/`input` instead of `.modal`/`.form-input`) left every field unstyled since the feature shipped (v3.5.168); a second bug meant it opened outside the shared `openModal()`, so Cancel/Save could never actually close it. eq-field PR #887 (v3.5.647), merged, live.
- Audited the entire Apprentices module (2 parallel agents: architecture map + defect sweep) and published an "Apprentices Under the Hood" artifact — wiring diagram, live usage numbers, ranked findings.
- Consolidated Journal + the separate Ask-for-Feedback request flow into one loop, per Royce's request after reading the artifact ("feels complicated... apprentices note things at their leisure, trust I'll read and act on it, track accountability"). `apprentice_journal` gained `acknowledged_at`/`acknowledged_by`/`acknowledged_note` (migration, applied live to ehow); new `journalAcknowledge()` (manager one-taps "Got it", mirrors `resolveFollowUp()`); two new surfaces (per-profile card on Overview, list-level inbox card) replace two removed ones. Ask-for-Feedback fully retired (client forms, server action, the `feedback_requests` read, the modal) — table left in place, RLS/grants intact, zero rows written in the ~2 months it was live. eq-field PR #892 (v3.5.651 — collided with and renumbered past a same-day concurrent PR's v3.5.650), merged, live.
- Added 3 PostHog events (`journal_entry_created`/`_shared`/`_acknowledged`, ids/booleans only, no entry text) — closes the "no usage instrumentation" gap flagged in the 2026-08-30 Apprentices critique.

## Decided
- Royce: consolidate into one loop (chose this over "also cut the tab bar" or "add a new 7th thing alongside the existing 6").
- Royce: no notification/email for shared entries right now — "happy if it's a closed loop between me and apprentices" — explicit, not an oversight.
- Royce: purpose is measurement — "initial goal is to see if this helps them engage... final outcome would be to hopefully have better people in our business."
- Royce: confirmed as a standing operating fact (not just today's auth quirk) — all real Field usage is via Core, the plain/standalone tenant URLs aren't a live access path. Updated the relevant Claude memory file so this isn't re-explained next session.
- Royce merged both PRs explicitly (#887, #892) after CI green + deploy preview confirmed serving the right content each time.

## Deferred
- Shared-entry notification/email — Royce's call, not built. `apprentice_profiles.supervisor_id` could route it without reintroducing the old "pick a person" step if he wants it later.
- Security-adjacent: `addCustomCompetency`/`removeCustomCompetency` (apprentices.js) bypass the ownership-checked write gateway every other apprentice write goes through — found in the audit, not fixed.
- Permission-gate migration still incomplete in 3 files (apprentices-feedback-forms.js, apprentices-skills-passport.js, tafe.js) — journal.js was switched to the real per-person grant as a side effect of today's work, these weren't.
- TAFE Holidays config doesn't actually protect Timesheets' auto-prefill, only Roster's bulk-fill button — `isTafeHolidayCell()` has zero callers where it matters.
- Two low-severity hygiene items: duplicate `_isSelfProfile()` definition (one dead copy), Skills Passport's unreachable fast-path optimization.
- Not click-tested live by a person — standing Core-only limitation. Royce said he'll test the new flow himself, live, from an apprentice's phone.
- Watching `quarterly_reviews` + the 3 new events for real signal over the next 2-3 weeks before concluding whether the simplification actually drove adoption.

## Notes
- Live usage check before building anything: only 3 apprentice profiles have ever existed, all created in the 4 days before this session (self-service creation shipped days ago); 5 of 6 activity tables (reviews/rotations/skills/journal/feedback-requests) sat at zero rows the whole time. Reframed the whole session from "fix a UI bug" to "this feature is in its very first days of real adoption, right now" — grounded every priority call in that fact rather than code-risk alone.
- The architecture-mapping agent's otherwise-careful audit missed one real entry point (a list-level "Inbound Asks" card, separate from the per-profile one it did find) — only surfaced by grepping every caller of `openFeedbackForm(` directly before changing its signature. Reinforces: an agent's map is a lead, not a fact, even a good one.
- `core-bundle-a1.js` bundles `analytics.js` as one of its concatenated sources — missed on the first pass when adding the 3 new events, caught by `build-bundles.mjs --check` before it shipped stale.
- Two version collisions this session (both landing on the same number as a concurrently-merging PR) — this repo is moving fast enough right now that re-fetching `origin/main` immediately before finalizing a version number, not just at branch time, is load-bearing.

---

# Session 2026-09-02 — Roster/Editor/Schedule week-load race fixed (PR #893, v3.5.654) — survived 3 version collisions

## Built
- Diagnosed a rendering race on Edit Roster, Weekly Roster, and My Schedule: switching weeks re-rendered immediately with no check that the new week's data had actually finished loading. `STATE.schedule` only holds a ±1-week sliding window, so a still-loading week read as "genuinely empty" — Edit Roster painted every weekday cell as a loud amber "needs filling in" warning with the day name as placeholder text, indistinguishable at a glance from the roster having been wiped, until the real values landed a moment later and silently overwrote the placeholders.
- Replaced `_preloadAdjacentWeeks()` — a function that looked like it should already prevent this but was a literal no-op stub — with `_ensureSurfaceWeekLoaded()` (new, `roster-week-nav.js`), mirroring the existing `_ensureApprenticeWidgetWeeksLoaded` render-now/background-load/redraw-on-arrival pattern. Wired into `renderEditor()`/`renderRoster()`/`renderSchedule()` (`roster.js`).
- Used a 15s retry cooldown rather than a permanent give-up-after-one-failure latch, specifically because Edit Roster uniquely disables the app's 30s background poll (`if (currentPage === 'editor') return;`) — caught during a reflection pass before committing, not after.
- Added 4 real functional tests to `tests/week-window-narrowing.test.js` (resident-week short-circuit, missing-week fetch+redraw, concurrent-caller dedupe, failed-fetch cooldown) — extracted and run against mocked state, not just source-string presence checks.
- eq-field [PR #893](https://github.com/eq-solutions/eq-field/pull/893) merged (squash) as v3.5.654, confirmed live: `field.eq.solutions/sw.js` shows the new banner.

## Decided
- Royce: fix all three affected screens together (Edit Roster + Weekly Roster + My Schedule), not just the one he'd screenshotted.
- Royce: explicit go-ahead to merge PR #893.

## Deferred
- Root cause of the underlying network flakiness itself — two live Sentry issues on the Shell-embedded SKS path explain why the fetch sometimes fails (`EQ-FIELD-19` AbortError on the tenant-config routing fetch, since 2026-08-27; `EQ-FIELD-16` Failed-to-fetch inside the sign-in token mint, since 2026-08-18 — both pre-date this fix). Spun off separately; Royce started it running in its own local session (`task_0fdf3a24`), in progress, not yet reported back.
- A visible "couldn't load this week" indicator in the week-nav bar — would close the last gap (a stuck week today just looks calm with no on-screen hint to reach for the manual Sync button). Deliberately left out of this PR as a smaller follow-up.
- Not click-tested through the full authenticated UI — standing Core-only sandbox limitation, re-confirmed twice more this session.

## Notes
- The bug matched Royce's own instinct exactly ("I think this happened previously... is it a connectivity thing?") — confirmed live in real time mid-conversation when a cell visibly re-populated while a reply was still being written.
- This PR's version number collided with **three** separate concurrent same-day PRs in a row while it sat open — #892 (Apprentices), #894 (Edit Roster fill-week), and #896 (Weekly Roster overview-card removal) — all four sessions independently shipping roster-adjacent fixes within roughly the same 30-45 minute window. Rebased, renumbered (651→652→653→654), and re-ran the full verification pass (tests, lint, bundle-drift, cache-buster) after each collision before force-pushing. No actual code ever conflicted across any of the four — only the shared changelog-banner block in `index.html` and the reflection-log tail section needed manual merging each time, and `scripts/roster.js` itself auto-merged clean against both #894's and #896's edits (confirmed by re-reading the merged result, not just trusting a clean auto-merge). Same "concurrent sessions land fast on this repo" pattern already documented elsewhere, just an unusually concentrated burst of it.
- `gh pr merge --squash --delete-branch` partially failed: the squash-merge itself succeeded server-side (confirmed directly via the GitHub API), but the local branch-delete step errored because a different concurrent worktree (`eq-field-lazy-tab-gaps-wt`) had `main` checked out. Deleted the remote branch manually afterward rather than assuming the error meant the merge failed.
- Re-confirmed (4th+ time this repo's history) that Core-only auth blocks click-testing Field screens even on a real deploy preview, even via the demo-tenant PIN gate — no new workaround found; built a genuine headless functional-test harness instead, which is now the standing answer for this class of verification gap rather than repeatedly trying (and failing) to find a live path in.
- `gh pr merge --squash --delete-branch` failed locally both times with "'main' is already used by worktree at ...eq-field-lazy-tab-gaps-wt" — a different concurrent session's worktree, not touched. Both merges had actually already succeeded via the API before the local error; confirmed via `gh pr view --json state,mergedAt,mergeCommit` rather than trusting the exit code, then cleaned up the remote branch by hand.

---

# Session 2026-09-02 — PostHog "day one" review → 3 bugs found across eq-field/eq-shell, 1 fixed-but-held on purpose, Weekly Roster's landing view fixed

## Built
- **eq-field PR #885 (v3.5.645, merged, live)** — crew-table sync failures now surface in `sync_degraded` telemetry (were silently invisible, structural cause not a performance bug).
- **eq-shell PR #1745 (merged, live)** — fixed Shell re-identifying/aliasing the same person every ~5 minutes (`ph.alias()` has no repeat-call de-dupe; `identify()` does). 568 alias events/day down to the expected handful.
- **eq-field PR #890 (v3.5.650, built, tested, CI-green — NOT merged, deliberately)** — `copyLastWeek()` could report "saved ✓" even when every write silently failed (`Promise.all()` could never see the swallowed rejections). Fixed to match the already-correct `applyBatch()` sibling pattern.
- **eq-field PR #896 (v3.5.653, merged, live, confirmed by Royce)** — removed the Roster Overview summary card from Weekly Roster's default view; the actual roster table is the landing view again.
- Published artifact "Day One Reality Check" — corrected the "first day live" framing (no traffic spike in 14 days of data) while confirming a real, different signal: 42% of Field's active users that day were first-timers, a genuine workforce-adoption read Royce confirmed made sense given Field is the app every SKS worker touches.
- 2 memory files saved: PostHog rollout-verification methodology (new-vs-returning share beats raw volume for a launch-day claim), and Royce's automation-vs-attention design principle (yellow empty cell = "needs a decision," verified against Copy Last Week's real 2-uses-ever usage).

## Decided
- Royce: PR #890 stays **unmerged on purpose** — checked real usage (2 uses ever, one a risky 172-cell blanket fill) before deciding a safety fix for a barely-used bulk action wasn't worth the exposure of making it easier to reach for. Explicit design principle stated: "the goal is to make people's lives easier, not harder" — automation that lets a stale value hide behind a "filled" cell fights that goal even when the fix itself is correct.
- Royce: Weekly Roster's summary card should be **removed**, not redesigned — after a first round of options (week-wide gaps, filter buttons, new-starters) he pulled back explicitly: "i feel we have complicated this - i want someone that clicks on weekly roster to see the weekly roster."
- SEC-1 confirmed to be entirely about sks-nsw-labour (the old standalone app), unrelated to Field/Shell — no action needed from either repo.

## Deferred
- eq-field PR #890 — built, not merged; revisit only if Royce changes his mind (see Decided above).
- "Understaffed sites vs last week" (Weekly Roster) — real feasibility landmine found (last-week data isn't reliably loaded client-side in Shell-embedded mode, the exact bug class that already caused a real incident once — Copy Week, v3.5.354). Would need `_ensureWeeksLoaded()` wired into a currently-synchronous render path first.
- "New starters" / "management out" (Weekly Roster) — confirmed cheap and ready, not built; Royce simplified the ask before either was needed.
- PostHog error tracking (`$exception`, 0 events in 30 days) — flagged as dead instrumentation, not fixed. Sentry remains the real error channel.
- Field's `sync_degraded` root cause (PR #885) — reasoned from code, not measured against live Supabase query logs (MCP connection failed mid-investigation). Revisit if incident volume grows.

## Notes
- The "first day live" framing didn't hold up under the traffic data alone (yesterday was actually the busier day in a flat 14-day trend) — but held up under a different, better lens: new-vs-returning user share. Worth remembering as a general technique, not just a one-off finding: a flat-volume day can still be a genuine rollout if the population changed underneath it.
- Two more version-number collisions this session (on top of the two already logged above from earlier today) — `main` moved mid-flight on both PR #890 and PR #896, requiring a rebase+renumber+re-verify each time before merge. This repo's concurrent-session traffic today was unusually high; re-fetching `origin/main` immediately before finalizing a version number remains load-bearing, not a one-off precaution.
- `gh pr merge --delete-branch` hit the same "'main' already used by worktree" local-git error a third time (PR #896) — consistent with the pattern already noted above from earlier sessions today. Merge itself succeeded via the API each time; only the local branch-delete step needs a manual follow-up (`git push origin --delete <branch>`).
- Live click-testing of Weekly Roster was blocked in every environment tried this session (deploy preview, production root, even the demo tenant) — all redirect to "Sign in through Core," the documented Core-only gate. Royce did the actual click-through himself and confirmed live; no workaround exists for this from an unauthenticated sandbox, and none should be chased (per standing memory on this exact limitation).
- This session ran a PostHog-driven audit (Royce: "run the audit") specifically for the "repeating action, no de-dupe, no report" bug shape after finding one instance in eq-shell — found a second, more severe instance in eq-field (`copyLastWeek`) than either of the two already-fixed bugs. Worth remembering as a technique: once one instance of a bug shape is found, a targeted grep-and-audit for the same shape elsewhere in the same/sibling codebase found a real hit here, not just theoretical risk.
