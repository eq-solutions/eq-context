# Session 2026-09-02 — Live-meeting kit: sample ID sheet added, QR-at-scale question resolved by finding it already exists

## Built
- Fourth artifact for the CEO/executive live-meeting kit: "EQ Sample ID Sheet" — a double-sided A4 handout, one per table rather than one per person. Front repeats the laminated card's 3-step instructions and company link; back carries 6 different sample credentials (White Card, Driver Licence, First Aid, Working at Heights, Forklift, EWP) so a room of attendees isn't all scanning the identical fake identity.

## Decided
- Nothing new decided by Royce this session — asked whether to generate a real EQ self-join QR for the meeting, no answer yet as of this close.

## Deferred
- Whether to generate a real self-join link/QR for the `eq` tenant via eq-shell's `AdminSelfJoinLinks` and swap it into the kit in place of the generic search-and-apply flow — open question put to Royce, unanswered as of this close.

## Notes
- **Royce asked "we should have QR codes for whatever role we want via add workers" — investigated before building anything, and it already exists.** eq-shell's `AdminSelfJoinLinks.tsx` (`/:tenantSlug/admin/workers/join-links`, linked from Worker Invites) is a mature, live, actively-iterated feature: per-role QR (all 6 roles), reusable by any number of people at once, expiry + approval-required toggle, full lifecycle (regenerate/deactivate/delete). Confirmed via the live `shell_control.self_join_codes` table on jvkn, not just the code: 8 real codes, actively created and used through 2026-08-26 — all for the `sks` tenant, **zero for `eq`**. Nothing to build; the only gap is that nobody's pointed this feature at the EQ tenant yet. Full detail in `eq/pending/eq-cards.md` and `eq/pending/eq-shell.md`, both 2026-09-02 entries.
- Same eq-context F9 shared-checkout guard as every prior close this week — did this close's writes from a fresh isolated clone (re-cloned, not reused, since the prior close's clone was itself now behind) rather than the shared `C:\Projects\eq-context` checkout, which showed `behind 17 / ahead 1` at session start — a state worth being cautious around, not something this session touched or attempted to reconcile directly.
- Verified today's actual date via a shell command before writing any dated file — the session-gate hook's "digest.md 2026-09-01 (0d)" freshness stamp is about when that file was last regenerated, not today's real date (2026-09-02); worth not conflating the two.

---

# Session 2026-09-02 — Closed out the last 5 set-state-in-effect violations, upstream in eq-solves-intake

## Built
- Traced eq-shell's vendored `eq-intake-demo` package (5 remaining `react-hooks/set-state-in-effect` violations that eq-shell#1723's 33-file sweep didn't reach) to its real upstream source, `eq-solutions/eq-solves-intake` (confirmed via eq-shell's README + the source repo's own `git remote -v`).
- Fixed all 5 there using the same two established patterns from #1723: `queueMicrotask`-wrap (4 files) and an adjust-during-render mode-reset guard (1 file).
- Verified against eq-shell's real ESLint rule (eq-solves-intake has none of its own) via a cross-repo invocation from a common ancestor directory — 0 errors on all 5, down from 1 each at the exact reported lines. `tsc --noEmit` clean too.
- Opened, confirmed CI green, and (on Royce's explicit go) squash-merged eq-solves-intake [PR #121](https://github.com/eq-solutions/eq-solves-intake/pull/121) (`731951e`).

## Decided
- Royce confirmed "merge the PR" after CI came back green — merged.

## Deferred
- eq-shell's own vendored copy of `eq-intake-demo` still has all 5 violations — only a `pnpm run revendor:intake` picks up PR #121's fix, and that's a separate, deliberate manual step per eq-shell's own README. Not attempted this session (explicitly out of scope per the original task). Tracked in `eq-shell.md`.

## Notes
- eq-solves-intake has no ESLint of its own — its `check` script is schema-lint + typecheck only. Plain `npx eslint <path-outside-project>` refuses flat-config files "outside of base path"; `pnpm exec eslint` inside eq-solves-intake itself fails on a broken/orphaned `.bin/eslint` shim with no real `eslint` package underneath (not a dependency there at all). Working path: invoke eq-shell's own installed eslint binary directly, with cwd set to a common ancestor of both repos (`C:\Projects`) so the target file isn't "outside" the config's base path.
- Local naming trap, same shape as the existing eq-quotes/eq-quotes-port one already in the umbrella CLAUDE.md: the sibling checkout at `C:\Projects\eq-intake` is the `eq-solves-intake` GitHub repo, not a repo literally named `eq-intake`.
- This session's brief-gate hook required running `/brief eq-intake` before any Edit would go through — done mid-task; health/worktree/git-state checks all came back clear, no blockers.
- Hit the F9 shared-checkout guard mid-close: `git rebase` in the shared `C:\Projects\eq-context` checkout was blocked by the repo's own pre-commit hook, and a concurrent session's own close process was actively mutating the same working tree at the same time (its edits to `sessions/2026-09-02.md` appeared and my own `eq-intake.md` edit visibly reverted on disk mid-operation — no data lost, just working-tree churn from two closes racing). Recovered by anchoring the commit with a branch ref, then redoing this close's writes from a fresh isolated clone instead, per the established pattern.
- Full technical detail in eq-shell memory `eq-intake-demo-set-state-in-effect-upstream-pr`.
