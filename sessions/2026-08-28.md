# Session 2026-08-28 — Documents to Sign: inline viewer's real bug found and fixed, then mapped audience reach

*Session ran late 2026-08-27 into the date rollover; logged under 2026-08-28 per this file's own "today" convention.*

## Built
- eq-shell PR #1635 — self-hosted Gotenberg Office→PDF conversion pipeline at upload-commit time (migration 0287). Caught a missing Netlify `-background` suffix on the backfill function before merge (would have silently capped it at 26s instead of the intended 15-minute background budget).
- eq-field PR #811 (v3.5.586) — first pass of the inline PDF.js viewer, replacing `window.open()`-the-raw-file with an embedded viewer + a reached-last-page-and-held-it-open sign gate.
- eq-shell PR #1644 — Register tab now refetches after a successful upload/push. Root cause of Royce's "I uploaded it and lost it" report: the tab's fetch only ever ran on mount.
- eq-field PR #816 (v3.5.588) — found and fixed the real reason the inline viewer never loaded any document, any size: PDF.js's own `validateFileURL` guard rejects a `?file=` URL whose origin doesn't match the viewer's, which a Supabase Storage signed URL always fails. Fixed via `PDFViewerApplication.open({url})` instead, PDF.js's own API for exactly this case. A loading spinner shipped first as a hedge while the cause was still open — kept, now genuinely useful for real large-file load time rather than papering over the total-failure bug.
- Manual DB step: Safety Handbook's `document_versions.pdf_status` set to `'ready'` by hand as part of the agreed fast-path to get to a testable state same-day, ahead of the backfill function's normal path.
- eq-field memory (`project_worker_identity_mobile_login.md`) updated with a fresh live count of the unlinked-staff population (14/74, down from 38/99 on 2026-08-19) — same population now confirmed to also gate document-push audience reach, not just RLS.

## Decided
- Royce: fast-path today (dispatch migration ahead of merge, manual DB row update) to get to a testable state same session — approved via AskUserQuestion.
- Royce: merge eq-shell #1644 (Register refresh) — approved via AskUserQuestion.
- Royce wants Documents to Sign opened up to all staff going forward, not just people it's been manually tested against — stated intent, not yet actioned (he's doing the role-pushes himself).

## Deferred
- **No "push to everyone" button exists.** `push-document-audience.ts` takes one role, one crew, or one person per push. Reaching everyone with a Shell login today = 5 role-pushes (employee 26, supervisor 11, manager 11, apprentice 8, labour_hire 6, live counts). Told to Royce directly; not built, wasn't asked for as a build.
- **14 of 74 active SKS staff have no linked Shell login**, unreachable by any push mechanism regardless of how many times it's repeated (role/crew/person pushes all require `shell_control.user_tenant_memberships`). Named list (9 Direct, 4 Labour Hire, 1 Apprentice) given to Royce in-session. Same population as the long-tracked unlinked-staff backlog in `eq/pending/eq-shell.md` — real, continued progress (was 37 mid-Aug, 24 on 2026-08-20), not a new problem.
- Full detail and file-level pointers: `eq/pending/eq-field.md` and `eq/pending/eq-shell.md`, both under their 2026-08-27 Documents entries.

## Notes
- **The "stuck viewer" bug was total, not a large-file timing issue** — it affected every document regardless of size, from the very first real one (the 43-page Safety Handbook). The spinner shipped in the first response to Royce's "is it stuck?" question was a reasonable hedge given the evidence available at that point (a blank viewer, "0 of 0" pages), but the actual cause only became findable once Royce sent a DevTools Console screenshot with the real thrown error (`Error: File origin does not match viewer's`) — Network-tab-only screenshots earlier in the thread didn't carry it, since PDF.js's guard throws before any network request is attempted.
- **Concurrent eq-shell work same evening, reconciled not duplicated**: a separate session shipped multi-select checkboxes for the document push picker (PR #1645) plus closed an unrelated jvkn RLS gap (`shell_control._eq_control_plane_migrations` had no RLS) — confirmed via commit timestamps that neither overlaps this session's audience-reach findings; the multi-select ships to the *person* picker specifically and doesn't add a bulk/all option, so the "5 role-pushes" guidance given to Royce still stands as the fastest path to reach everyone with a login.
- `eq-shell`'s "signer picker sources canonical staff, not Shell logins" change (PR #1477, 2026-08-20) was already live before this session — the *person* picker lists the full canonical roster (showing unlinked people, disabled) for visibility, but actually pushing to anyone still requires a real Shell login server-side. Not a change this session made; confirmed by reading the code, since it directly bears on the audience-reach answer given to Royce.
- eq-context substrate was 2 commits behind `origin/main` at session close (a concurrent session's own close, `43a6d2a0`) — `git pull` correctly refused in the shared checkout (F9 guard); reconciled via an isolated scratchpad clone instead, per the guard's own instructions.

---

# Session 2026-08-28 (cont.) — Digest "Send test to myself": two more real bugs found after the first real click

## Built
- eq-field PR #818 (v3.5.589) — `send-digest-test.js` now resolves the caller's own email via `app_data.staff` directly instead of the `app_data.field_managers` view, which silently returned 0 rows for every caller under this function's service-role credential (the view's WHERE clause depends on `auth.jwt()->app_metadata.tenant_id`, which doesn't exist in that context). Fixes "No email on file for your account" showing even though the same email was visibly correct in the recipients checklist directly above the button.
- eq-field PR #819 (v3.5.590) — `supervisor-digest`'s Deno edge function was filtering `app_data.leave_requests.status` against Title-case literals (`"Approved"`/`"Pending"`) that can never match the DB's lowercase-only CHECK enum. Fixed both queries; the Pending-approval and On-leave sections had shown nothing since the function's v3.5.216 canonical rewrite, regardless of real data, on every actual Friday send this whole time.
- Supabase edge function `supervisor-digest` redeployed to production ehow (v14→v15) — a separate, explicit step from the PR merge, since edge functions don't ship via Netlify/git. Content-verified live post-deploy (grepped the deployed source for the fix's own comment).

## Decided
- Royce: "go" — approved the PR #818 fix approach.
- Royce: "merge #818" — explicit merge/deploy approval.
- Royce: "merge and go" — explicit approval for both merging PR #819 and separately redeploying the edge function.

## Deferred
- **Not yet re-confirmed by Royce.** Both fixes are live and verified server-side (deploy checks, direct content grep against the deployed edge function), but nobody has re-clicked "Send test to myself" since the v3.5.590 fix to visually confirm the Pending-approval and On-leave sections now actually populate with real data in an actual received email.

## Notes
- This is the 4th and 5th real gap found in this same feature's saga (first 3: PR #791/#802, closed 2026-08-26/27, now moved to `eq/pending-archive.md`). Each gap was a genuinely distinct failure mode (missing deploy, wrong credential, credential silently failed to save, unreachable RLS-style view, wrong enum case) — worth noting for anyone tempted to assume "fixed" after any single one of these without an actual click-through, which is exactly how this string of gaps kept surfacing one at a time.
- Confirmed via grep that `supervisor-digest/index.ts` has no other Title-case status literal anywhere else in the file — the fix covers the only two call sites.
- Confirmed the client-side app was never affected by the enum-case bug — `scripts/leave-adapter.js`'s `statusFromEnum()` already normalizes every read before the browser ever sees it; only this one edge function bypasses that adapter.
- eq-context substrate was 7 commits behind `origin/main` at this session's own close — same F9 guard refusal, reconciled the same way (isolated scratchpad clone).

---

# Session 2026-08-28 — Multi-select document push, live RLS gap closed, reminder-cron deferred

## Built
- eq-shell PR #1645 — person picker on the Documents admin page (fresh-upload push step in `AdminDocumentUpload.tsx`, and the Register tab's "Push to more people" modal) switched from single-select to checkboxes. Pushes to every selected person in one action via parallel calls to the existing `push-document-audience` endpoint (no backend/schema change — it already resolved a person-push one signer at a time), aggregates into one "Pushed to N people" result, leaves only failed names checked on a partial failure so they're easy to retry. Merged (squash `368dd3db`), confirmed live via a production smoke check (3 people pushed to, screenshot confirmed).
- `supabase/migrations/2026_08_27b_control_plane_migrations_ledger_rls.sql` — found en route (the merge's required drift-check failed on it) and fixed: `shell_control._eq_control_plane_migrations` had RLS disabled on jvkn, a gap in the brand-new control-plane migration pipeline's self-creating DDL (PR #1641, merged ~30min earlier). RLS enabled, zero policies, service_role-only grants, mirroring `app_data.migration_baseline`'s posture. Dispatched live via `control-plane-migrate.yml` and merged.

## Decided
- Royce: go ahead with the checkbox approach — "Confirmed, go ahead."
- Royce: fix the RLS gap properly rather than route around it, and separately authorized dispatching the live migration — two distinct approvals (AskUserQuestion).
- Royce: merge — on eq-shell this is also the production deploy, no separate step.
- Royce, later in the same session, asked `/decide` whether to build any user-facing control (mute/opt-out/cadence) for the pre-existing sign-off reminder cron. Called: hold off — see Deferred.

## Deferred
- Not click-tested live end-to-end by a person (checkbox push specifically) — tracked in `eq/pending/eq-shell.md` under this PR's own entry, not repeated here.
- Reminder-cron user control — considered via the decision protocol, explicitly not building yet. No goal to weigh it against (TODAY.md GOALS unset), zero reminder cycles have fired so there's no operational evidence yet, and a recipient-facing opt-out would undercut the reminder's own compliance purpose. Narrow fallback if a real case shows up: admin-only per-document "stop reminding" toggle next to the reminder count already shown in the Register — not a recipient opt-out. Full write-up: `eq/pending/eq-shell.md`'s 2026-08-28 entry.

## Notes
- Answered two factual follow-ups about the pre-existing (not built this session) sign-off reminder mechanism: recipient-side experience (pull-based via Field's "Documents to Sign" nav item, no push notification, view-before-sign gate, real drawn signature, moves to Signed tab, daily 08:00 AEST email as backstop) and where the reminder itself is set up (`netlify/functions/scheduled-document-signoff-reminders.ts`, a Netlify Scheduled Function, cron `0 22 * * *` = 22:00 UTC, auto-activated on deploy with no separate enable step; the branded email shell is the shared `_shared/email/template.ts` also used by the licence-expiry and weekly-digest emails, while the reminder's own subject/body copy is inline in that file's `renderReminderEmail()`; confirmed via grep that no admin/tenant UI toggle exists anywhere for it).
- This entry was written on a second close pass — an earlier `/close` this session (before a context compaction) got as far as `eq/pending/eq-shell.md` and `eq/changelog/eq-shell.md` (both already carried PR #1645 and the RLS fix correctly when checked) but never landed a `sessions/2026-08-28.md` entry; today's file only had the concurrent PDF-viewer session's entry, which references this session's work in passing but doesn't carry its own record. Writing it now closes that gap rather than leaving this session undocumented.

---

# Session 2026-08-28 — Admin sidebar decluttered: Users/Audit log/Security groups/Settings unpinned, merged and live

## Built
- eq-shell PR #1653 — removed Users, Audit log, Security groups, and Settings from the permanently-pinned Admin sidebar list in `HubSidebar.tsx`. All four already have a matching tile on the Admin Overview page ("All admin tools"), so no access was removed — just fewer permanent links, leaving All admin tools, Add workers, Intake, Labour hire rates, and Suppliers pinned. Kept `navScope.ts`'s per-user "Simple" item list and `AccessControlPage.tsx`'s nav-preview tool trimmed the same way, per this repo's own documented 3-consumer sync invariant (confirmed `AdminEditUser.tsx` needed no direct edit — it imports the shared list rather than duplicating it). Squash-merged (`6c2b5dc2`), confirmed live via Netlify deploy state (`ready`, `published_at` set, exact `commit_ref` match).

## Decided
- Royce's opening question — "if I clean up the nav bar from my view, does it filter down to everyone since I have the highest access?" — answered from the actual code before any edit: no, the sidebar is permission-gated per signed-in user (`useCan()`), never copied from any one session. A shared-code change was what he actually wanted.
- Royce: trim all four (Users, Audit log, Security groups, Settings) — not just the two he first named — once shown all four share the identical tile-redundancy (approved via AskUserQuestion).
- Royce: Admin section only for this pass, Records/Apps/Resourcing left alone (approved via AskUserQuestion).
- Royce: "merge" — on eq-shell this is also the production deploy, no separate step. A merge-readiness audit ran first (all 5 required checks green, scope matched exactly, no auth logic touched) before merging.

## Deferred
- Not click-tested live — no Shell session/credentials in this environment; verified instead via `tsc -b --force`, a full `pnpm run build`, and a direct trace confirming `AdminHub.tsx` already tiles all four removed items. Tracked in `eq/pending/eq-shell.md` under this entry.

## Notes
- Doesn't reopen or contradict the 2026-07-06 decision (this same file, further down) to leave Import/Labour hire rates pinned when only Reports was moved off the sidebar — different items, same access-safe principle.
- Before writing any code, checked 5 old branches (`feat/nav-tile-drift-and-preview`, `claude/nav-cleanup-fixes`, `claude/access-control-polish-e8d7ca`, `claude/access-control-security-groups-287cf3`, `claude/field-perms-visibility`) for overlap with the exact files being changed — all single-commit, superseded/abandoned (Aug 7–25), none conflicted.
- This worktree's `node_modules` was missing `heic2any` (declared in `package.json` since PR #1619, 2026-08-26, unrelated to this task) — a plain `pnpm install` fixed it before the build would fully succeed.
- eq-context substrate was 7 commits behind `origin/main` at this session's close, and the shared checkout (`C:\Projects\eq-context`) had another session's uncommitted `/close` writes sitting in its working tree at the same time (the sign-off-reminder entry above, already committed upstream by the time this was checked) — reconciled via an isolated scratchpad clone rather than touching the shared checkout's working tree, per the F9 guard.

---

# Session 2026-08-28 (cont.) — Documents Register signer-name mismatch root-caused and fixed, load-time fixed

## Built
- eq-shell PR #1652 — Royce, live, comparing the Staff page to the Documents Register for the same person: "Why is Mohammed Hussain's name different? Should be the same record?" Root-caused: `shell_control.users.id` (jvkn) == `app_data.staff.user_id` (ehow) exactly — one person, not a duplicate. The Register read the raw invite-time name capture (`shell_control.users.name`); Staff reads the curated `app_data.staff` record. Same bug class Royce already resolved for 7 people via PR #1540 (2026-08-23) — Hussain was an 8th case that audit's discovery query missed.
- Data fix: same guarded-UPDATE pattern as #1540 applied live to jvkn for this one person, plus an audit-trail migration (`2026_08_28_resolve_hussain_divergent_staff_shell_name.sql`).
- Code fix: `push-document-audience.ts`'s `handleRegister`/`handleCertificate` now resolve signer names STAFF-PREFERRED via a new shared `resolveSignerNames()` helper (extracted deliberately, against this file's usual "duplicate, don't share" style — two independently-maintained copies of this exact resolution is how Register and the certificate PDF drifted from Staff in the first place). `handleOptions` picked up the same `parseSurname` cleanup Staff page already used.
- Load-time fix, same PR: Register/Templates were eagerly selecting `signature_image` (a full base64 PNG, tens of KB) for every already-signed row on every load, used only if an admin opened that row's evidence modal. Moved to on-demand `resource=signature-image`, same pattern as the pre-existing `resource=document-file`.
- Merged (squash `224548d6`), confirmed live via Netlify deploy state (`ready`, published 2026-08-27 18:53 UTC) matching the merge commit exactly.

## Decided
- Royce (AskUserQuestion): proceed with both code fixes as scoped.
- Royce (AskUserQuestion): also apply the same jvkn data-level name fix the other 7 people got, rather than leave it code-only.
- Royce: "merge" — merged PR #1652; on eq-shell this is also the production deploy, no separate step.

## Deferred
- Not click-tested live — no Shell session/credentials in this environment.
- Full re-sweep for more divergent staff/shell-login names — flagged as a spawn_task chip since PR #1540's own audit clearly wasn't exhaustive. Royce started it in a separate session before this close finished; see Notes — it already came back.

## Notes
- Ran the Session Gate brief before any edit (repo guard required it this session, blocked the first Edit attempt with `brief-gate`) — the staleness skim over branch names is what surfaced the #1537/#1540 precedent before writing anything, which is what made "staff wins" + the guarded-UPDATE pattern the obvious, already-Royce-approved move rather than something invented fresh this session.
- The spawned re-sweep (`task_97ae2b45`) finished in a separate session while this one was still closing, and wrote directly to the shared memory store (both sessions share the same user-level memory directory). It found 4 more divergent names on ehow (3 "staff wins", 1 NULL-fill) — eq-shell **PR #1654, OPEN, not yet merged** — and separately root-caused (not fixed, low urgency) a different bug on zaap: Royce Milmlow's own `app_data.staff.user_id` there resolves to zero rows anywhere on jvkn, a bad-import artifact from the 2026-05-21 Cards bulk-seed rather than the name-divergence shape. Incorporated into `eq/pending/eq-shell.md` rather than left as stale "not yet reported back" phrasing. Full detail: the `documents-register-signer-name-divergence` memory file (updated directly by that session).
- eq-context substrate was 11 commits behind `origin/main` at this session's own close, with a genuinely different concurrent session's real uncommitted `/close` work (the multi-select/RLS-gap/reminder-cron session above, second pass) sitting dirty in the shared checkout at the same time. Same F9 guard shape as the two sessions above; reconciled via an isolated scratchpad clone rather than touching the shared checkout's dirty files, and left them untouched for that session to commit itself.
- A THIRD concurrent close landed mid-reconciliation: fetching origin/main again right before this commit turned up the Admin-sidebar session's own close (PR #1653, above), pushed after this session's isolated clone was first taken. Merge-conflicted on this file and the changelog (both had inserted at the same top-of-file position) — resolved by keeping both sessions' entries, chronological order by merge time (#1653 after #1652).

---

# Session 2026-08-28 — Documents to Sign: Schneider handbook load failure root-caused and fixed (v3.5.593)

## Built
- eq-field [PR #823](https://github.com/eq-solutions/eq-field/pull/823) (v3.5.593): `scripts/sign-documents-viewer.js`'s `PDFViewerApplication.open({url})` call had no `.catch()` — any real fetch failure left the "Loading document…" spinner in the inline PDF viewer running forever with no error and no way back. Added error handling: polling stops and a plain "Couldn't load this document — go back and try again" message replaces the spinner. Squash-merged on Royce's explicit "merge", confirmed live via `field.eq.solutions/sw.js`.

## Decided
- Royce chose "fix now, verify after" (via AskUserQuestion) over waiting for a live repro or digging further first, given the concrete-but-not-fully-confirmed nature of the finding.
- Royce approved the narrower spinner fix only, not a full fallback-path redesign — the fallback gap was spawned as a separate background task instead of built in-session.

## Deferred
- Root cause of Luke Wheeler's specific report not confirmed live — no signer credentials in this environment to reproduce his exact fetch. Asked Royce to have Luke retry on a fresh tab.
- Documents to Sign's new error state still has no functional fallback (raw-file `window.open()`) if a load failure is persistent rather than transient. Spawned as `task_445c878c`; Royce has already started it in a separate local session (running independently as of this close).
- Version-number collision: [#821](https://github.com/eq-solutions/eq-field/pull/821) and [#822](https://github.com/eq-solutions/eq-field/pull/822) both claimed "v3.5.592" and were still open/unmerged at session end. This session used v3.5.593; next session should re-check before picking a version.

## Notes
- Started from a live-DB-first investigation, not code-first: queried ehow directly and confirmed the Schneider Contractor Safety Handbook's `pdf_status='ready'`, its converted PDF genuinely present in Storage at the correct size, and that 4 of Luke's 5 co-signers had already signed it successfully — ruled out a data problem before reading any client code. Matched Luke Wheeler's identity to the signoff row via `app_data.field_people.user_id` = `document_signoffs.signer_user_id` (exact UUID match), not a name-string guess.
- This is the second eq-field session in two days to touch the Documents to Sign inline-PDF-viewer feature (the first shipped v3.5.588, PR #816, and its own commit message explicitly flagged "not yet confirmed live by Royce's own session" — this session's finding is a real gap in that same code path that survived the first fix).
- Confirmed live: eq-shell shipped a large "Documents" feature batch (PDF-conversion pipeline, migration 0287, PRs #1635/#1644/#1645) on 2026-08-27, the same day the Schneider handbook was pushed — it was very likely one of the first real documents to go through the new pipeline end-to-end.
- Confirmed via `build-bundles.mjs`: `core-bundle-b1.js` does NOT embed `sign-documents-viewer.js`'s content — only `lazy-loader.js` + region-filter + dashboard are concatenated into that bundle. Lazy-loaded route files (like this one) only need `APP_VERSION` bumped (auto-versioned via `lazy-loader.js`'s `_versioned()`), no bundle regen.
- `check-cache-busters.mjs` caught a real drift locally before it reached CI: `app-state.js`'s content changed (APP_VERSION bump) without its own `?v=` tag in `index.html` moving — fixed before commit, not after.
- The shared root checkout `C:\Projects\eq-field` was found sitting on an unrelated stale branch (`claude/field-actor-drop-breadcrumb`, ~20 commits behind main) left by another session — not touched, just noted for whoever hits it next.
- `eq-context` was 16-17 commits behind `origin/main` for this entire session (F1-shaped staleness, flagged by the session's own guard reminders) — `git pull` in the shared checkout is hook-blocked (F9), so this close used an isolated scratch clone throughout rather than touching `C:\Projects\eq-context` directly, per the guard's own recommended remediation.

---

# Session 2026-08-28 — Timesheets: day-cell clicks now fill the whole broken-up week (v3.5.591)

## Built
- eq-field [PR #820](https://github.com/eq-solutions/eq-field/pull/820) (v3.5.591): `_spClickSeg` (`scripts/timesheets-spans.js`) now delegates to the same "every workable day" editor as the existing Fill Week button, so clicking any day cell — not just the dedicated button — opens one popup covering every workable day in a week split by a locked leave/TAFE day, instead of forcing two separate popups.
- Same fix closed a latent bug found along the way: Fill Week's day-set was hardcoded to Mon-Fri; it now reads the page's own weekend-toggle-aware day list, so Sat/Sun no longer silently drop from the popup when "Show weekends" is on.

## Decided
- Royce: "merge" — explicit go-ahead for PR #820, confirmed live via `field.eq.solutions/sw.js` (v3.5.591).

## Deferred
- Not click-tested live by a real signed-in supervisor — no SKS/Core credentials reachable in this environment; `?tenant=demo` resolves to the `eq` sandbox tenant, whose standalone PIN gate is a confirmed dead end live (Core-only, per this repo's own documented auth model). Verified instead via an isolated harness loading the actual shipped file and calling the exact function the click handler invokes.

## Notes
- Confirmed live via an isolated test harness (stubbed globals, loaded the real `scripts/timesheets-spans.js`, called `_spClickSeg` directly against a TAFE-Tuesday scenario matching Royce's screenshot) rather than reasoning from the code alone — before the fix, clicking Monday returned only `['mon']`; after, `['mon','wed','thu','fri']` with subtitle "Mon, Wed-Fri · 4 days". Local `npx serve` can't run Netlify Functions (`tenant-config.js` 404s), so the demo-tenant boot path needs `netlify dev`, not plain `serve` — that's why the harness approach was used instead of a full local app boot.
- Real version-number collision, not just a close call: [PR #819](https://github.com/eq-solutions/eq-field/pull/819) (the digest fix in an earlier session block above) merged to `main` mid-session and had already claimed v3.5.590 — rebased onto the new `main`, renumbered to v3.5.591, re-ran the full local test/lint/both-drift-check suite clean before re-pushing.
- Also hit an isolated, unrelated Netlify/GitHub SSH host-key checkout failure on the first deploy-preview build — confirmed via `listSiteDeploys` that every other deploy that hour succeeded, so this was infra flake, not the diff; resolved with a retry commit.
- `gh pr merge --squash --delete-branch`'s local branch-cleanup step failed after the remote merge had already succeeded, because another worktree on this machine (`eq-field-apprentice-scope-race-wt`) had `main` checked out — the squash-merge itself went through fine on GitHub; the remote branch needed a manual `git push origin --delete` afterward. Worth remembering: `gh pr merge`'s local git bookkeeping can fail for reasons unrelated to the merge itself when multiple worktrees share one machine.
- Process lesson for future sessions: used `ScheduleWakeup` (a /loop-dynamic-mode tool) to poll CI checks outside of a /loop context. It worked, but a stale wakeup fired again after the task was already done and the PR already merged, producing a confusing duplicate "check on this" prompt. `Monitor` alone (with its own bounded polling loop and completion notification) is the correct tool for "wait on CI/a background process" — not `ScheduleWakeup`.
- `eq-context` was 18+ commits behind `origin/main` for this entire session; `git pull` in the shared checkout is hook-blocked (F9) — this close used an isolated scratch clone (`git clone` into the session scratchpad) rather than touching `C:\Projects\eq-context` directly, per the guard's own recommended remediation. Same pattern the immediately-preceding session block in this file already used.

---

# Session 2026-08-28 (cont.) — Leave approver dropdown fixed for every plain SKS/EQ worker

*Session ran 2026-08-27 into the date rollover; logged under 2026-08-28 per this file's own "today" convention.*

## Built
- eq-field [PR #813](https://github.com/eq-solutions/eq-field/pull/813) (merged 2026-08-27) — `app_data.field_managers` flipped to `security_invoker=false` with an inline `tenant_id` filter, a deliberate one-off exception to this repo's "always invoker=on" rule (safe here specifically because this view carries no PII columns, unlike `field_people`). Fixes the Leave request form's empty "Select your supervisor" dropdown, reported by Brendan Drinkwater (SKS worker). Root cause traced to eq-shell's SEC-33 security fix (migration 0256/PR #1510, applied 2026-08-21 to both ehow and zaap) restricting `app_data.staff` reads to own-row-or-manager — `field_managers` silently inherited that restriction despite exposing nothing sensitive, collapsing to 0 supervisors for every non-manager caller, tenant-wide, for 6 days. Applied live to both ehow and zaap via Supabase MCP ahead of the PR; the PR itself is the audit-trail commit, not a pending change.
- Same fix incidentally restored Recognitions, Incidents notify, Sites lead display, Teams display, and CC-on-leave-emails for plain workers — all read the same `STATE.managers`/`field_managers` data source.

## Decided
- Royce: apply the fix to BOTH tenants (ehow + zaap), not just SKS. First answered "ehow only" via the question UI, then immediately corrected to "apply to both" in a follow-up message before either was acted on.
- Royce: test live in person with Brendan tomorrow (2026-08-28) on a real phone, logging in as Brendan specifically rather than as himself — his own account is tagged Executive and was never blocked by the bug, so it wouldn't have proven anything.

## Deferred
- Not yet click-tested live by Brendan — planned for 2026-08-28, in person. Verified instead via a JWT-simulated read of his exact real session claims (tenant/role/actor_id): supervisor list went from 0 to 20 rows; cross-tenant isolation reconfirmed intact on both planes.

## Notes
- Independently found the same root-cause pattern (SEC-33's RESTRICTIVE own-row-or-manager policy on `app_data.staff` silently breaking a `security_invoker=on` view that carries no PII) that a separate, spawned follow-up session then found and fixed for the sibling `field_people` view — same fix shape, independently confirmed twice. Full detail: `eq/pending/eq-field.md`'s field_people entry, and `project_field_people_sec33_visibility_regression.md` in eq-field's own memory.
- A later, separate session found a real follow-on gap in this fix: the inline `tenant_id = auth.jwt()...` filter is unreachable from a `service_role` caller (no JWT claims in that execution context), which broke the digest "Send test to myself" feature until eq-field PR #818 routed that specific caller around the view instead. Worth remembering for any future server-side (non-browser) reader of `field_managers`.
- Checked a concern raised in the field_people follow-up session's own memory — that this PR's `security_invoker=false` exception might be tripping eq-shell's "Schema drift + anon-grant + policy-lint" CI gate suite-wide. Re-checked live against eq-shell's 2 currently open PRs: that check is green on both. Not currently reproducing as a blocking issue — worth another look only if it recurs, not treated as resolved-and-forgotten.
- `gh pr merge --squash --delete-branch`'s local git-bookkeeping step failed after the remote merge had already succeeded, because another worktree on this machine (`eq-field-apprentice-scope-race-wt`) had `main` checked out. The squash-merge itself went through fine on GitHub; the remote branch needed a manual `git push origin --delete` afterward. (Same artifact a separate session hit the same day — see this file's Timesheets entry above.)
- `eq-context` was 24 commits behind `origin/main` at this session's close; `git pull` in the shared checkout is hook-blocked (F9) — reconciled via an isolated scratch clone in the session scratchpad, per the guard's own recommended remediation.

---

# Session 2026-08-28 — Mobile TOTP enrollment: same-device QR lockout fixed

## Built
- eq-shell [PR #1655](https://github.com/eq-solutions/eq-shell/pull/1655) — `EnrollTotp.tsx` gets a tappable `otpauth://` deep-link button next to the QR code (authenticator apps register that URI scheme on iOS/Android, so tapping it hands off straight to "add account" pre-filled) and a copy-to-clipboard button on the manual-entry secret, matching the pre-existing backup-codes copy pattern. Client-only — no backend, schema, or auth-policy touched; `enroll-totp`/`confirm-totp` and the forced-enrollment gate untouched. Merged (squash `18f6391d`), confirmed live via Netlify deploy state (`ready`, published 2026-08-28 05:28 UTC) matching the merge commit exactly.

## Decided
- Royce: proceed with the two-part client-side fix (deep link + copy button) over alternatives — asked as an exploratory question first, answered in 2-3 sentences with a recommendation and tradeoff, confirmed before any edit.
- Royce: "merge" — on eq-shell this is also the production deploy, no separate step. A merge-readiness audit ran first (all 5 required checks green, exact-main branch, single-file scope, no auth logic touched) before merging.

## Deferred
- Not click-tested live — no Shell session/credentials in this environment. Worth a real pass next time someone's mid-enrollment on a phone: confirm the deep link hands off to an installed authenticator app on both iOS and Android, confirm the copy button copies the right secret. Tracked in `eq/pending/eq-shell.md` under this entry.

## Notes
- Root cause was purely a same-device UX gap, not a logic bug: the enrollment screen already had a manual-key fallback, but on mobile that meant hand-typing a 32-character secret with nothing to copy — that friction, not any auth defect, was what turned a forced-enrollment grace-period expiry into an actual lockout for mobile-only managers/supervisors/admins.
- Post-merge deploy took noticeably longer to resolve than the 2-4s figure recorded elsewhere in this repo's own operating notes for other recent merges — polled `building` → `uploading` → `ready` over roughly 5 minutes via the Netlify API rather than assuming success from the merge alone. Worth revisiting that "2-4s" figure: it may describe time-to-deploy-triggered rather than time-to-live, at least for builds of this size.
- `ScheduleWakeup` was used once as a fallback safety net around a backgrounded Bash poll (not a `/loop` context) and, as a separate prior session's note in this same file already flagged, fired a redundant duplicate check after the real background-task notification had already been handled and reported — confirms that prior note rather than being a new finding; `Monitor` remains the better-fit tool for "wait on a background process" outside of `/loop`.

---

# Session 2026-08-28 — Documents: site-scoped pushes and sign-off certificates

## Built
- eq-shell [PR #1656](https://github.com/eq-solutions/eq-shell/pull/1656) — a document push can now carry a site (`document_signoffs.site_id`, FK → the real `app_data.sites` entity, not a new hardcoded list). The Register groups by (document, site) so the same document pushed to two sites reads as two independent entries, each with its own certificate showing only that site's signers. An untagged push renders byte-identical to before this shipped.
- Migration `0288_document_signoffs_site_scope.sql` — nullable `site_id` + a generated `site_key` column (`coalesce(site_id, <nil-uuid>)`, same trick `document_audiences.target_key` already uses) replacing the old tenant/version-wide `UNIQUE(version_id, signer_user_id)` with a site-aware one, so a signer can hold one independent signoff per site while an untagged signoff keeps today's exact single-global-signoff guarantee. `document_register` view extended with `site_id`/`site_code`/`site_name`, appended at the end of the column list per this view's own two prior 42P16 incidents (0253, 0287).
- Server: `document-audience-push.ts` threads an optional `siteId` through the shared upsert; `push-document-audience.ts` validates it tenant-scoped, returns the live sites list from `resource=options`, and `resource=certificate` takes a 3-state `site_id` (absent = legacy all-signers, `none` = unscoped only, a real uuid = that site only) so a mixed document's unscoped certificate can't silently pull in another site's signers.
- Client: `AdminDocumentUpload.tsx` — Site dropdown on both push forms (defaults to the card's own site when "push to more people" is opened from an already-scoped entry), Register grouping/keying reworked from document-only to (document, site), a site filter that only renders once there's more than one grouping to filter between.

## Decided
- Royce: use the live `app_data.sites` table for the Site dropdown, not the hardcoded SY1–SY9 + N/A list from his own brief — asked via AskUserQuestion once the investigation confirmed Sites already exists as a real, populated entity.
- Royce: do the full site-aware uniqueness rework (independent signoff per site) rather than a metadata-only shortcut that would silently dedupe a shared signer across sites — also asked via AskUserQuestion, since the metadata-only option was cheaper/lower-risk but wouldn't fully satisfy his own acceptance criteria.
- Royce: "merge" — the migration had to be dispatched and verified live BEFORE merging, not after (see Notes) — confirmed via AskUserQuestion once that sequencing risk was found, since it's a live DDL run against real production tenant data and deserved its own explicit go beyond the general "merge" instruction.

## Deferred
- Not click-tested live — no Shell session/credentials in this environment. Worth a real pass: push the same document to two different sites, confirm two independent Register entries and certificates, confirm an existing unscoped push renders unchanged. Tracked in `eq/pending/eq-shell.md` under this entry.

## Notes
- **New sequencing lesson, not covered by the existing "dispatch against the PR branch" memory**: that memory's trigger is a required CI check staying red until a fix is live (security_invoker/RLS/anon-grant class of fix) — circular under this repo's normal merge-then-dispatch convention. This session's trigger was different: the required checks were all fine either way (nothing about them depends on the new columns existing), but the PR's *application code* ships in the same PR as the migration and immediately queries/writes the new columns — merging first would have deployed working CI alongside a broken Documents feature (missing-column errors on every push/register/certificate call) until someone separately remembered to dispatch. Same mechanical fix (dispatch `tenant-migrate.yml --ref <pr-branch>` before merge), genuinely different reason to reach for it — worth generalizing as its own trigger condition: "does this PR's code assume the migration is already live," independent of whether any required check is currently failing.
- Before writing the migration, hand-reconstructing `document_register`'s current `CREATE OR REPLACE VIEW` from the last migration file that had touched it (0233) would have silently dropped `reminder_count`/`pdf_storage_path`/`pdf_status` — three columns added by later migrations (0253, 0287) that never appeared in 0233 itself. Caught by pulling the live `pg_get_viewdef` on both planes first rather than trusting the migration-file trail. Worth normalizing: for any view that's been `CREATE OR REPLACE`'d more than once, treat the last migration that touched it as unreliable and pull live `pg_get_viewdef` before writing a new version.
- `document_audiences` already had a `target_kind='site'` + `target_site_id` column (FK → `app_data.sites`) sitting in its schema, unused — a close read of `push-document-audience.ts`'s own header confirmed it's a *different*, currently-disabled feature (resolving signers FROM a site roster via `staff.default_site_id`, which has no real data yet), not reusable for "tag a push with a site regardless of how signers were chosen." Also confirmed `document_audiences` isn't even joined into `document_register` — it's a write-only idempotency ledger, invisible to the Register/certificate either way. Worth remembering next time "site" comes up near Documents: two unrelated features already share the word in this schema.
- Second independent same-day data point on the "merge = 2-4s to live" figure being wrong for larger builds: this session's production deploy took `deploy_time: 335` (≈5.5 min) end to end, matching the Mobile TOTP session's ~5 min observation earlier today, not the 2-4s figure recorded elsewhere in this repo's operating notes. Worth revisiting that figure — likely describes time-to-deploy-*triggered*, not time-to-*live*.
- Local `netlify dev` under Node 24 was re-confirmed broken (same `@vitejs/plugin-react can't detect preamble` crash already on file from three weeks ago) — not new information, but worth noting it was independently re-verified rather than assumed stale, and that the PR's Netlify deploy preview loaded cleanly, confirming the failure is local-machine-specific, not a real code defect.
- `eq-context` was 33 commits behind `origin/main` at this session's close; `git pull` in the shared checkout is hook-blocked (F9) — reconciled via an isolated scratch clone in the session scratchpad, same remediation this file's own earlier entry today already used.
- eq-context substrate was 29-30 commits behind `origin/main` across this session (drifted by one mid-session); `git pull` in the shared checkout is hook-blocked (F9) — reconciled via an isolated scratch clone in the session scratchpad for this close, per the guard's own recommended remediation.

---

# Session 2026-08-28 — eq-field worktree audit: 19 of 24 flagged worktrees cleaned up

## Built
- Audited all 23 eq-field worktrees the 2026-08-28 round-1 Documents-to-Sign cleanup found live-but-undocumented and explicitly deferred, plus a 24th (`eq-field-signdoc-followup-wt`) that had appeared since. Verified each independently via `gh pr list --head <branch> --state all` (never `merge-base --is-ancestor`, which false-negatives on every one of these — this repo squash-merges) and cross-checked every MERGED verdict against `git log origin/main --grep "(#<PR>)"` to confirm the squash commit actually landed, not just that GitHub's PR object says merged.
- Removed 19 confirmed merged-and-clean: `git worktree remove` for each, local branch force-deleted (`-D` — plain `-d` refuses on every one, same squash-merge false-negative), lingering remote branches cleared (16/19 already gone via this repo's `--delete-branch` merge flow; the other 3 deleted directly from `origin`).
- `eq-context/system/worktree-registry.md` updated with the full audit trail (19-item pruned list, 5-item Active table for what's left) — committed and pushed earlier in this same session (commit `74dec404`), ahead of this close.

## Decided
- No new Royce decisions this session — the task brief itself was the explicit go-ahead for the narrow "confirmed merged-and-clean" cleanup scope; everything outside that scope was deliberately left untouched rather than assumed.

## Deferred
- 5 worktrees need Royce's call before anyone touches them: `documents-to-sign-feature-3035a3` (detached HEAD, unpushed same-day commit, no PR to verify against), `eq-field-apprentice-scope-race-wt` (stale `main` snapshot, 87 behind/0 ahead of `origin/main`, no unique work but no PR either), `ci-repro` in Temp (working tree already deleted from disk, 2-week-stale commit, different ownership class — reads as a spent CI-repro checkout), and 2 genuinely open PRs already being worked elsewhere (`site-contact-mapping-fix` / [PR #821](https://github.com/eq-solutions/eq-field/pull/821); `supervisor-list-population-9e6715` / [PR #822](https://github.com/eq-solutions/eq-field/pull/822) — the latter reads as live work closing the `site_projects`-not-wired-to-Field gap already on record in this file's 2026-08-27 entry).

## Notes
- The auto-mode permission classifier blocked one bundled script chaining `git worktree remove` + `git branch -D` + `git push origin --delete` across all 19 targets at once — too large/opaque a combined destructive action to vet in one shot. Splitting into separate batches per action type (all 19 worktree removes together, then all 19 local branch deletes, then just the 3 remote deletes) went through cleanly. Worth structuring any future bulk git-hygiene pass that way from the start rather than rediscovering it.
- One empty husk left behind (`phoenix-leave-approval-visibility-14ae9e`) — git had already unregistered it and cleared its contents, just couldn't rmdir the shell itself (same AV/indexer lock pattern the registry already documents repeatedly elsewhere). Harmless.
- No eq-field product code touched this session — pure git/worktree housekeeping plus the eq-context registry update, so no `eq/changelog/eq-field.md` entry and nothing to tick or add in `eq/pending/eq-field.md` (worktree hygiene lives in the registry, a different backlog by design).
- `eq-context` was 23 commits behind `origin/main` at the start of this task and 32 behind by session close (drifted further mid-session, across at least 8 concurrent commits from other sessions today) — reconciled via an isolated scratch clone throughout, same pattern every other session block in this file used today.

---

# Session 2026-08-28 (cont.) — Documents to Sign: raw-file fallback for a load failure that doesn't resolve on retry (v3.5.594)

## Built
- eq-field [PR #824](https://github.com/eq-solutions/eq-field/pull/824) (v3.5.594) — the load-failure message the earlier same-day v3.5.593 fix introduced (this file, above) was a dead end: "go back and try again" just re-ran the identical failing viewer path. Added a real recovery action — a button that resolves and opens the original (pre-conversion) file's signed URL, the same `sbCreateSignedUrl('attachments', path, 300)` + `window.open()` pattern the pre-v3.5.586 raw-file button used. `openSignDocumentViewer()`'s `opts` now threads `rawStoragePath` through from `sign-documents.js`; opening the raw file still calls `_signMarkViewed()` and unlocks Sign, matching pre-v3.5.585 gate behaviour exactly.
- Verified via an isolated Node `vm` harness loading the real edited file and driving the real trigger path (a rejected `PDFViewerApplication.open()`, not the private error handler called directly) — 14/14 assertions across the success, no-rawStoragePath, and signed-URL-failure cases.

## Decided
- Royce (opening message): build the fallback button, framed "not urgent — a defensive gap, not a confirmed live incident — but worth closing before this happens to someone in the field with no easy way to actually get a document signed."
- Royce: "Confirmed — proceed" on the brief.
- Royce: "merge" — explicit approval for PR #824; on eq-field this is also the production deploy (Netlify auto-builds `main`).

## Deferred
- None new. This closes the fallback gap flagged and spawned (`task_445c878c`) by the earlier same-day v3.5.593 session — no further open item from this piece of work.

## Notes
- This session picked up `task_445c878c`, the background task the v3.5.593 session spawned for exactly this gap — confirms the spawn-then-pickup loop worked as intended, end to end.
- Found the shared root checkout (`C:\Projects\eq-field`) sitting ~19 commits stale on an unrelated branch left by another session; not touched — worked from a fresh worktree off `origin/main` instead, per this repo's own worktree-isolation convention.
- Checked 7-8 other sign-document-related worktrees/branches for collision risk before starting — all confirmed stale or already merged via `git log` + `gh pr list`, zero live overlap.
- `check-cache-busters.mjs` caught real drift locally before CI would have: `app-state.js`'s `?v=` tag in `index.html` hadn't moved with its `APP_VERSION` bump — it's the one eagerly-loaded file among the changed set (the two sign-document files are lazy-loaded and auto-versioned, no manual tag). Fixed before commit, matching the standing memory note on this exact failure mode.
- eq-context substrate was 24-32 commits behind `origin/main` across this close (drifted further while working, and both `git pull` and `git merge` are hook-blocked in the shared checkout — F9); reconciled via an isolated scratch clone (`git clone` into the session scratchpad) throughout, per the guard's own recommended remediation.
