# Session 2026-08-06 — Document sign-off register: real vision from Royce, two trust gaps fixed

*Continuation of 2026-08-05's work (this session ran past midnight AEST — the categories PR/migration from earlier is logged in `sessions/2026-08-05-i.md`; this entry covers what followed after that close).*

## Built
- Investigated eq-field's actual signing code (`sign-documents.js`, `site-reports-shared.js`) to answer Royce's direct question: is there a place for a physical signature in the document-control process. Confirmed no — only a drawn-signature canvas exists — and found a low-lift path to add one later (an existing photo-upload component the signing flow never calls).
- Same investigation surfaced two real, unprompted findings: the signing screen never shows the document being signed (a worker could sign without opening it), and nothing is left for a signer afterward (no receipt, the row just vanishes). Also found *why* only Royce has ever signed anything — the nav entry is hardcoded visible to his email only, not an adoption gap.
- eq-field [PR #657](https://github.com/eq-solutions/eq-field/pull/657) (v3.5.462) — fixes both gaps: signing is now blocked until the file has been opened at least once ("View first" disabled state + a defensive re-check inside the sign function), and a small Outstanding/Signed toggle gives the signer a persistent record. Built in an isolated worktree, independently re-verified (not just the build agent's report): `node --check`, all 24 test files re-run individually, eslint 0 errors, RLS/status-check re-confirmed live on ehow. **Open, not merged** — awaiting Royce's explicit go.
- Diagnosed (not fixed) `jwt-contract-drift.yml`'s 3-day failure streak: the checker looks for a local interface in eq-service that's been replaced by a shared `@eq-solutions/contracts` package import — the fix this checker was designed to eventually require has already happened on one side; the checker itself is stale. Flagged as background task `task_42424993`.
- `eq-context/eq/pending.md` — new section for all of the above.

## Decided
- Royce, unprompted: reframed the ask as "a company document register plus templates that's easily accessible and is easy for management to prove people have signed on... everything needs to be world class! but simple" — re-opens the north star the earlier `/decide` had deliberately shelved, with an explicit new constraint (simple, not maximal) that wasn't part of the original framing.
- Royce, via AskUserQuestion: fix the two trust gaps before opening access, not after and not instead of. Access-opening (removing the hardcoded nav gate) stays a deliberate next step, not bundled into #657.

## Deferred
- PR #657 not yet merged.
- Opening access beyond the hardcoded single-email nav gate — not scoped yet, deliberately sequenced after #657 lands.
- Physical-signature-as-photo-upload — real, scoped, small lift, not confirmed for build.
- `jwt-contract-drift.yml` fix/retirement — background task `task_42424993`, spans eq-context/eq-shell/eq-service, not done here.

## Notes
- **Real calendar-date rollover caught mid-close**: a pending.md commit's auto-bump flagged `last_updated` moving to 2026-08-06; `date` confirmed it's actually 05:06 AEST on 2026-08-06, not still 2026-08-05 as the conversation's own framing assumed. This session log is therefore dated today (2026-08-06) rather than continuing the 2026-08-05 lettered sequence (which had reached `-y` by the time of this close — an extraordinarily high-concurrency day). No brief-gate re-run was needed to finish this close: every file touched (`pending.md`, `sessions/`, this file) is gate-exempt regardless of date.
- Investigated 3 gate-flagged cron failures before writing them off as noise: `index-drift.yml` and `pending-rotate.yml` had both already self-resolved (another concurrent session fixed them same day, gate just hadn't caught up) — only `jwt-contract-drift.yml` was genuinely still broken, and that one got a real diagnosis, not just a re-flag.
- The two eq-field trust-gate fixes are honestly scoped as a soft UI gate, not a security boundary — documented plainly in both the PR and eq-field's own `docs/reflection-log.md` (required by that repo's reflection-protocol gate) rather than overclaimed.
- SEC-1 and SEC-9 (the two standing P0 security findings) remain unchanged — both already carry Royce's own repeated "defer" decision. Not re-flagged.

---

# Session 2026-08-06 (cont.) — `jwt-contract-drift.yml` canary fixed

*Picks up `task_42424993`, flagged in this same file's entry above.*

## Built
- Confirmed live, across all three repos (eq-context, eq-shell, eq-service, plus the new eq-contracts package), which of the canary's two possible fixes applied — not assumed from the earlier diagnosis. eq-shell's and eq-service's local clones were both stale/wrong-branch; re-verified against actual `origin/main` via `git show` before drawing any conclusion. eq-service migrated its consumer type to import `ShellHandoffClaims` from a new `@eq-solutions/contracts` package; eq-shell's mint side has not, so the canary needed updating, not retiring.
- Rewrote `scripts/check-jwt-contract-drift.mjs` to fetch the shared package as a third source and union its keys with eq-service's remaining local additive keys, and to recognize the new `app_metadata: ShellHandoffClaims & { ... }` intersection shape alongside the old plain object literal.
- Tested against real `origin/main` fixtures before landing: the happy path (exit 0), a synthetic regression via a renamed required key (exit 1, correctly caught), and a genuinely-missing interface (exit 2, still fails loud) — confirms the fix restores a real guarantee, not just a green check.
- [eq-context PR #129](https://github.com/eq-solutions/eq-context/pull/129) opened and merged (squash `65e720f`) — the workflow's own live `pull_request` self-test passed against real data, not just local fixtures.
- `eq-context/eq/pending.md` — gave this its own section; trimmed the eq-field sign-off section per the archive-touch rule (this session's edit to its "Deferred" list triggered it) and corrected its header from "PR open" to "merged," matching its own already-ticked PR #657 line.

## Decided
- Ran `/decide` before building (Royce: "then build") — confirmed fix-not-retire against live evidence, confirmed landing via isolated worktree + PR rather than the diverged/dirty shared checkout.
- Royce: "merge #129" — merged after re-confirming `mergeStateStatus: CLEAN` and all checks green.

## Deferred
- Version-pin skew between eq-shell's and eq-service's independent `@eq-solutions/contracts` pins has no guard — no live risk today (identical tag pinned on both sides), but nothing would catch a future divergence once eq-shell also migrates. Documented as a caveat in the script; not built.
- eq-shell's own migration to `@eq-solutions/contracts` (the canary's originally-envisioned "durable fix") is still not done.

## Notes
- The brief-gate hook blocked the first write attempt on `scripts/check-jwt-contract-drift.mjs` ("brief not run this session") — ran `/brief eq-context` properly rather than setting `EQ_SKIP_BRIEF=1`.
- eq-context's own shared main checkout stayed diverged (local ahead/behind origin) with foreign uncommitted changes (`ops/pending-archive.md`, `ops/pending.md`, `system/worktree-registry.md`) from a concurrent session for the entire task — all writes (code fix, this close) went through isolated worktrees off fresh `origin/main` instead, consistent with the F9 pattern this repo's own failure ledger documents. No edits made to the 3 foreign-touched files.

---

# Session 2026-08-06 — EQ-SHELL-1A (EQ Ops SKS "Failed to fetch"): two hotfixes, then a durable fix, closed + confirmed live

*Royce opened with only a garbled paraphrase ("TypeError:failedtofindehow") and "urgently need it fixed." The full arc ran across two retry hotfixes before the real recurrence pattern (multi-browser, hotspot, only his phone worked) justified building the durable fix.*

## Built
- Matched the paraphrase to the real Sentry issue: [EQ-SHELL-1A](https://eq-solutions.sentry.io/issues/EQ-SHELL-1A) — `eq-ops rpc eq_quote_pipeline_counts failed: TypeError: Failed to fetch (ehowgjardagevnrluult.supabase.co)`, culprit `/sks/ops`, one SKS user (simon.bramall@sks.com.au).
- **PR #1260** — root cause: `loadPipelineCounts`/`loadAttachmentCounts` in `QuotesModule.tsx` were one-shot RPCs with no retry and no visible error state (unlike `loadQuotes`). Added `rpcWithRetry` (300ms/900ms) to `quoteTelemetry.ts`. Merged (`5705aeb3`) on Royce's "merge", deployed on "deploy and let me know — people are waiting to use it."
- Bug recurred for the same user. **PR #1261** widened `rpcWithRetry` to `[500, 1500, 3000]`ms (~5s total) once Supabase edge logs showed his bad patches sometimes outlasted the original ~1.2s window. Merged (`d996a3f9`, after rebasing to resolve a stacked-PR conflict from #1260's squash-merge).
- Ran `/decide` on whether a bigger architectural fix was warranted off a single-user report — correctly concluded "not yet, wait for a second signal." Then over-built anyway (a full two-tier plan via EnterPlanMode) off that same single report — Royce called it out: "it's been working for awhile - this seems like a rabbit hole and over reaction." Stopped, acknowledged, wrote it up as [[stop-after-hotfix-holds-single-user-issue]] in memory.
- The actual recurrence then arrived: incognito stopped working, a separate browser failed, a mobile hotspot failed — only Simon's phone worked. That ruled out "just his browser extensions" and pointed at domain-selective interference (most likely corporate security software blocking `*.supabase.co` specifically) on his laptop. Confirmed via git/migration history that nothing on the EQ side changed to cause this, and confirmed other SKS users were succeeding on the same RPCs concurrently — ruled out a server-side cause.
- Royce authorized the durable fix explicitly ("yes, go ahead"). Design-reviewed via a Plan-agent pass before writing code — caught two HIGH-severity issues (an unconstrained proxied sub-path, and a deny-list header-forwarding scheme that would have leaked the session cookie to the tenant's Supabase project).
- **PR #1263** — built `netlify/functions/tenant-data-proxy.ts`: a same-origin reverse proxy (`/api/tenant-proxy/*`) so the browser never talks to `*.supabase.co` directly for the EQ Ops/Intake surface. Fixed both review findings: a shape-based path allow-list (`rest/v1/rpc/<name>` + 5 known tables, rejecting `auth/v1/*`, `storage/v1/*`, path-traversal/host-confusion attempts) and a request-header allow-list (never forwards Cookie/Authorization/apikey — server-injects a cached, short-lived JWT instead). New `_shared/tenant-context.ts` factors the session→tenant→routing resolution out of `mint-tenant-jwt.ts` for reuse between it and the proxy. New `src/lib/tenantProxyClient.ts` wired into `QuotesNative.tsx` as the first-tried client, falling back to the legacy direct clients with every fallback reported to Sentry (`eq-ops client fallback: <from> -> <to>`) so a proxy bug can't hide behind the fallback chain. 8 new unit tests covering both allow-lists. CI green (including the "Guard function filenames" check, which required moving the test file to `_shared/` — `.test.ts` at the functions root isn't a valid Netlify function name). Squash-merged (`9aedfa50`) on Royce's "merge and deploy when green."
- Confirmed the Netlify production deploy went live (`commit_ref` match, published 03:33:59 UTC, `core.eq.solutions`) via the Netlify MCP. Watched Sentry afterward for ~20 min: zero new EQ-SHELL-1A events post-deploy, zero fallback breadcrumbs. Royce confirmed directly: "success" — Simon is unblocked.
- Wrote/updated two memory files: extended [[stop-after-hotfix-holds-single-user-issue]] with the resolution (the earlier over-reaction was premature *before* real recurrence evidence existed — once it recurred across independent environments and Royce signed off, escalating was correct), and added a new [[eq-shell-tenant-data-proxy]] memory documenting the proxy architecture, its current scope (only `QuotesNative.tsx` migrated so far, legacy clients still live as fallback), and pointing at the approved plan doc for the retirement/rollout steps.

## Decided
- Royce gave explicit go-aheads at each gated step: "merge" (×2, for #1260 and #1261 respectively), "deploy and let me know", "yes, go ahead" (authorizing the Tier 2 build after the hotspot test result), "merge and deploy when green" (for #1263).
- Confirmed via `/decide` that nothing on the EQ side (no code change, no ehow migration) caused this — ruled out before treating it as an architecture problem worth solving.

## Deferred
- **GitHub MCP connector (`d2708d72…`) 404s on both `create_pull_request` and `list_pull_requests` for `eq-solutions/eq-shell` specifically** — looks like a repo-access/installation scope gap for that connector (other repos not tested). `gh` CLI (separately authenticated) was the working fallback all session; not itself blocking, but worth a look if it recurs.
- **Retire the legacy direct-to-Supabase browser path** (`tenantDataClient.ts`/`sksSupabaseClient.ts`, the browser-exposed `VITE_SKS_SUPABASE_URL`/anon-key env vars, and the per-tenant `*.supabase.co` CSP `connect-src` entries) — end-state cleanup from the approved plan, deliberately held until the proxy's soak period confirms clean.
- **Three other pages are unmitigated against the identical failure mode** — grepped `createTenantDataClient`/`createSKSSupabaseClient` usage post-close (Royce asked "can this happen elsewhere?"): `LabourHireRates.tsx`, `Suppliers.tsx`, and `modules/intake/index.tsx` all still call the legacy direct clients with no proxy-first fallback and no fallback telemetry — only `QuotesNative.tsx` was migrated this session. Any of these three breaks the same way if a user's network/browser blocks `*.supabase.co`. Next migration targets per the plan's rollout order.

## Notes
- The `/decide` protocol worked exactly as designed on its first pass (correctly said "wait for recurrence") — the mistake was continuing to build a full plan past that verdict anyway, on the same unconfirmed report. The protocol isn't a substitute for actually stopping once it gives an answer.
- This session's own `/close` had a false start: an earlier attempt produced a local, unpushed commit (`c1dd536`, covering PR #1260 only) in the shared `eq-context` checkout that hit a non-fast-forward push (a concurrent session had pushed first) and was abandoned mid-rebase to go handle the live production bug. That commit's content is fully superseded by this entry — folded in here rather than pushed separately, then the shared checkout was fast-forwarded to match instead of carrying a stale duplicate commit.
- **Maintenance burden of the new proxy is low, by design.** The RPC path is allowed by shape (a regex), not an explicit name list — new RPCs need zero proxy changes. Only the 5-entry table allow-list (`KNOWN_TABLES`) needs a one-line edit if Intake ever reads a new table. The real remaining cost isn't upkeep, it's the one-time rollout: the three still-exposed pages above.

---

# Session 2026-08-06 (continued) — EQ-SHELL-1A proxy extended to the 3 remaining exposed pages, PR #1264 merged

*Direct follow-through on this same day's earlier "three other pages are unmitigated" finding — Royce: "migrate the other 3 pages to the proxy - please confirm this is the best solutions."*

## Built
- Confirmed the plan was right, with one correction found by checking the actual code instead of assuming a straight copy-paste: `LabourHireRates.tsx` reads two tables (`labour_hire_companies`, `labour_hire_rates_view`) that weren't on the proxy's `KNOWN_TABLES` allow-list — a naive migration would have had it always fail-and-fallback silently, generating noise instead of signal. Added both. `Suppliers.tsx` (one RPC) and Intake needed no allow-list change — re-verified Intake's actual `.from()` surface against the vendored `@eq/intake` package's `fetch-canonical.ts` (not just the plan doc, per Rule 0.5) and confirmed it matches the existing 5-table list exactly.
- Extracted the proxy-first/fallback/telemetry logic that was inline in `QuotesNative.tsx` into a shared `src/lib/connectTenantClient.ts` (with `isAuthError` also moved there) so it isn't copy-pasted across four pages. Refactored `QuotesNative.tsx` to use it (behavior-identical) and wired `LabourHireRates.tsx`, `Suppliers.tsx`, and `modules/intake/index.tsx` to it too.
- Before writing code: rebased the worktree branch onto `origin/main` (it was 3 commits behind, missing the #1261/#1262/#1263 squash-merges), confirmed the rebased tip was content-identical to `origin/main` (`git diff` empty), then reset to `origin/main` cleanly rather than carry forward now-redundant pre-squash history.
- Ran the repo's own `/brief eq-shell` gate properly (had been cleared by the earlier `/close`) rather than bypassing it — health digest clean of anything relevant, no conflicting sibling worktree, git state confirmed and rebased.
- `pnpm run build`, `tsc -p tsconfig.netlify.json --noEmit`, and `pnpm test` (301/301) all clean. 4 lint findings (3 `react-hooks/set-state-in-effect` errors + 1 exhaustive-deps warning) confirmed via `git show HEAD` to pre-date this change in all 3 files touched — left alone, matching the precedent already set for `QuotesNative.tsx` earlier the same day.
- [eq-shell PR #1264](https://github.com/eq-solutions/eq-shell/pull/1264) opened, CI watched to green in the background (typecheck·test·lint, gitleaks, migration hygiene, schema drift, Netlify deploy preview all passed), `mergeStateStatus: CLEAN` confirmed, squash-merged (`0ae584b6`) on Royce's "merge PR 1264 once CI's green."
- Re-checked Sentry once more (scheduled follow-up from the earlier close) before this work started: still clean, zero EQ-SHELL-1A recurrence, zero fallback breadcrumbs, well past a reasonable soak window — called the original soak good and stopped scheduling further checks rather than watching indefinitely on a quiet signal.

## Decided
- Royce: "migrate the other 3 pages to the proxy - please confirm this is the best solutions" — confirmed yes, with the `LabourHireRates` allow-list gap surfaced and fixed as part of confirming it, not glossed over.
- Royce: "merge PR 1264 once CI's green" — merged after independently confirming both CI green and `mergeStateStatus: CLEAN`.

## Deferred
- **Retire the legacy direct-to-Supabase browser path** — now genuinely unblocked (soak confirmed clean twice, all 4 known browser consumers migrated), but the actual removal (drop `tenantDataClient.ts`/`sksSupabaseClient.ts`, the browser-exposed `VITE_SKS_SUPABASE_URL`/anon-key env vars, CSP `connect-src` entries) is still separate work, not done.
- **Not click-tested live** — `LabourHireRates.tsx`, `Suppliers.tsx`, and Intake pass build/typecheck/tests but haven't been opened live by a real signed-in user since the migration. Royce or a real SKS user to confirm no regression.
- GitHub MCP 404 on eq-shell — unchanged, still open, see earlier entry same day.

## Notes
- The brief-gate flag cleared by the earlier `/close` re-armed correctly and blocked the first edit this continuation — ran `/brief eq-shell` properly rather than skip it, which is exactly what the gate is for.
- Rebasing before this session's first commit caught something worth knowing generally: rebasing a branch onto `origin/main` *after* that branch's own PR has already been squash-merged replays the pre-squash commits again on top of the squash commit — git's patch-id skip-detection only caught one of the two original commits, not the pair once squashed. Content ended up identical (verified via empty `git diff`) but the history was redundant; reset to `origin/main` cleanly instead of carrying it.
