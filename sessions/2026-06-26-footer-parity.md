---
title: "Session: Safety docs footer parity"
date: 2026-06-26
version_shipped: v3.5.191
repo: eq-field
pr: "#345"
merge: 673f94f
---

# Session — 2026-06-26 — Safety docs footer parity

## What was built

**v3.5.191** — Footer parity across all safety `.docx` exports.

User attached a reference Prestart document (`PreStart_Luke_Wheeler_20260622_Equinix_SY3.docx`) and asked whether EQ Field's exports matched. Investigation confirmed:

1. **Logo + palette** were already wired end-to-end (v3.5.185 `fetchTenantLogo`, v3.5.190 `setPalette`). No code gap there.
2. **Footer format** was the real delta — the reference showed `Daily Prestart  |  Page 1 of 1  |  Generated by EQ Solves — Field`; Field was emitting plain static text with no page numbers.

## Changes

- `scripts/site-reports-shared.js` — `buildPackage(opts)` footer block replaced: now uses `opts.footerLabel` + Word PAGE/NUMPAGES field structs (`_fRPr`, `_fRun()`, `_fField()`). Arial 7pt #AAAAAA. `opts.footerText` kept as backward-compat alias.
- `scripts/safety.js` — `_psExportDocx` + `_tbExportDocx`: `footerText:` → `footerLabel: 'Daily Prestart'` / `'Toolbox Talk'`.
- `scripts/audits.js` — `exportAuditDocx`: `footerLabel: 'Site Safety Audit'`.
- `scripts/toolbox.js` — `exportToolboxDocx`: `footerLabel: 'Toolbox Talk'` (EQ-demo path; logo fetch still uses dead `/images/eq-logo.png` — noted as open item).
- Version stamped v3.5.191 (`app-state.js`, `sw.js`, `index.html`).

## Verification

Ran `buildPackage()` in a real browser harness — `ok: true`, `footer1.xml` well-formed, PAGE/NUMPAGES fields present. Prod confirmed via `curl https://field.eq.solutions/sw.js | head -3` → `v3.5.191` cache name.

## Decisions

- `sks-nsw-labour` has no docx builder at all. Reference code for logo was always in eq-field.
- Footer format confirmed by Royce from the attached reference doc.

## Open items carried

- EQ-demo toolbox logo path (`/images/eq-logo.png` is dead — needs `fetchTenantLogo()` like the SKS path).
