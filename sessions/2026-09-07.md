# Session 2026-09-07 — Labour-hire licence-photo fix re-verified live; full roster audited, no other worker exposed

## Built
- No code changes this session — verification only.

## Decided
- None — informational/verification session, nothing came up that needed Royce's call.

## Deferred
- Whether the separate 2026-09-02 stale-session wallet fix actually resolved Conor/Nelson's "Sign in again" symptom remains unconfirmed — this session checked a different bug (photo-linking) and doesn't touch that question. Still needs either Sentry access or Royce asking them directly.
- 3 claimed labour-hire accounts (Shihab Al-Gburi, Ali Alsalman, Amir Farid) have a completely empty wallet — no licence ever captured for them, by any method. Not a bug, just worth knowing if Royce expects tickets already on file for them.

## Notes
- Royce asked (1) whether labour-hire workers can see licences uploaded for them via Core, using Nelson Sareto/Conor Horgan as the example, and whether "this fix" was finished; then (2) to check the rest of the labour-hire roster too.
- Traced "the fix" to eq-cards migration `0161_promote_labour_hire_photo_on_claim.sql` (2026-08-25/26): `eq_cards_claim_invite`'s promotion loop copied a staged `worker_credentials` row into `public.licences` but never extracted `metadata.source_document_url` into `photo_front_url`/`document_url` — the ticket showed, the photo behind it didn't. The migration's own comment names Conor Horgan and Nelson Sareto as the real accounts this was live-traced against on 2026-08-26. The identical gap existed independently in eq-shell's `shell-join-tenant.ts` and `accept-invite.ts` (ported the same promotion loop rather than sharing it) — closed there via PR #1603 (2026-08-26), both now calling the same `promote-labour-hire-photo` edge function eq-cards' fix introduced.
- Verified live against jvkn (eq-canonical), not just read from migration/PR comments: `list_migrations` confirms `0161` applied 2026-08-25; a direct SQL query shows both Conor's and Nelson's 4 licences each (8 total — matching the migration's own "8 already-promoted-but-photo-less licences" backfill count exactly) now carry a `photo_front_url` or `document_url`. Zero gaps.
- Confirmed how "via Core" actually works end-to-end: Shell doesn't render the wallet itself — it mints an auth session and iframes the Cards Flutter app at `core.eq.solutions/:tenant/cards` (`CardsIframe.tsx`), reached from a generic "My Card" tile on `WorkerHome.tsx` with no role gate excluding labour-hire. The `licences_read` RLS policy (`user_id = auth.uid() OR ...`) always lets an owner read their own rows regardless of the separate employer-visibility `sharing_scope` mechanism (migration `0128`) — that mechanism was never the blocker here, it only ever governed what an *employer admin* can see.
- Widened the check on request: queried every worker tagged `labour_hire` (either `public.workers.role` or `shell_control.users.role`, 20 total). Only Conor and Nelson have ever had a `worker_credentials` row at all — the labour-hire-candidate-intake photo pipeline has only ever been run for these two, live, so nobody else was ever exposed to this specific bug. Of the other 18: 15 haven't claimed an account yet (normal pre-onboarding, already covered by the existing "chase unclaimed invites" admin alert, eq-shell #1574); 3 have claimed accounts but a genuinely empty wallet (nothing ever captured for them, not data loss); 2 (Richard Brooks, Zemi Asri) have licences via a different path (self-scan in Cards) and were never at risk.
- **Substrate cross-reference found mid-close**: `eq/pending/eq-cards.md` already carried an open 2026-09-02 item ("Wallet can't load licences") about a *different* bug affecting these exact same two workers — a stale client-side Supabase session causing a "Sign in again" error, fixed by PR #342 (session-refresh + wallet-nudge consolidation) but explicitly left unconfirmed end-to-end at that session's own close. This session's photo-fix verification is a different mechanism and doesn't close that question — cross-referenced both entries so the connection isn't lost, but deliberately did not tick that item's checkbox, since its specific question (does the Sept-2 fix actually hold) is still open.
- **Substrate hygiene**: the `SessionStart:resume` hook's "SYNC ok — HEAD == origin/main" banner had already gone stale by the time this close ran — 2 nightly-bot commits landed on origin in between. Caught via `git fetch` + `log HEAD..origin/main` before writing anything, not assumed from the banner alone. Also hit the shared-checkout `git merge` guard (F9) on `C:\Projects\eq-context` as expected for this repo — did all substrate writes in a fresh isolated clone instead of working around the block, per the hook's own recipe.

---

# Session 2026-09-07 — Timesheets RLS error fixed for Anthony Hartley; EQ-FIELD-1B triaged as not-a-bug

## Built
- eq-field [PR #933](https://github.com/eq-solutions/eq-field/pull/933) (v3.5.688, merged, confirmed live): raw Postgres RLS error no longer shown verbatim on timesheet save failures. New `_tsFriendlySaveError` in `scripts/timesheets.js` pattern-matches Postgres's fixed "new row violates row-level security policy ..." phrasing and swaps in "try signing out and back in" instead.

## Decided
- Royce approved the message-only fix over touching RLS/auth posture — the twice-declined fail-open carve-out (2026-08-16, 2026-08-19) stays declined; this fix doesn't touch that decision at all.
- Royce approved merging PR #933 once CI passed; merged and confirmed live via `field.eq.solutions/sw.js`.
- On the separate Sentry issue EQ-FIELD-1B, Royce chose "spend more time tracing the real caller" over leaving it as low-priority — that extra tracing found it isn't a bug at all.

## Deferred
- The underlying identity-linking gap: 13 of 77 active SKS staff still unlinked as of today (was 14/74 on 2026-08-27) — unchanged by this session, still Royce's call whether to dispatch the held write-side RLS migration and accept the residual risk, or keep waiting for the population to clear via real Cards/Core signups.
- Why Anthony Hartley's identity briefly fails to resolve, specifically — two hits in roughly a week (2026-08-30 and 2026-09-06). This session's mechanism (a JWT-mint timeout leaving the actor identity unresolved for one request) is a plausible match for the 2026-08-30 "client-side load-order race" theory but doesn't prove it; not investigated further.

## Notes
- **Root cause, in full:** `app_data.timesheets`'s `timesheets_own_crew_read` policy is SELECT-only RESTRICTIVE, but PostgREST's upsert always uses `RETURNING` internally — even under `Prefer: return=minimal` — so Postgres re-checks the newly-written row against SELECT policies regardless of what the client asked to get back. This means the READ-side RLS hardening shipped 2026-08-19/08-24 has already been gating writes for the unlinked-staff population this whole time — a real correction to the substrate's own tracked assumption ("6 more tables found writable... write-side re-audited and still held," 2026-08-19) that writes were unrestricted until a separate migration applied. Corrected in `eq/pending/eq-field.md` (cross-referenced, not rewritten, to preserve the historical record).
- Confirmed via live Postgres logs on ehow (5 identical failures, one iPhone session, 2026-09-06 21:32-21:34 UTC — nothing else failed on `timesheets` in the following 24h) and a live Sentry pull (issue EQ-FIELD-1H) showing a real business-rule denial ("only a manager can reopen an approved timesheet") shares the identical 42501 SQLSTATE as the raw RLS message — ruled out switching on the error code alone, matched on Postgres's literal auto-generated phrasing instead.
- Anthony Hartley's own identity is linked (`app_data.staff.user_id` populated), not a duplicate (checked against the 2026-08-31 duplicate-identity sweep), not a supervisor — not in the tracked unlinked bucket. His actual unblock is re-signing in through Core; relayed to Royce to pass on directly, since the code fix can't do this for him.
- This repo's own reflection-protocol gate caught a real issue before merge: the first draft of the friendly message said "ask your supervisor," which isn't reliably actionable — a supervisor can't complete someone else's Cards/Core sign-in. Revised to lead with "try signing out and back in" before merge.
- **EQ-FIELD-1B** ("AbortError: signal is aborted without reason", Sentry, 2 events/9 days, 2 different SKS users, last seen 2 hours before this session started) investigated as a second candidate fix after Royce asked "are there any other issues we can fix now." First search (scoped to `scripts/*.js`) failed to find the actual failing call site; widening to include `index.html` found it at line 17604 — a lesson in scoping searches too narrowly on this codebase, where the main app logic lives partly in inline `<script>` blocks, not only `scripts/*.js`. Traced fully: a rare `_getDataJwt()` mint timeout on the `people` fetch falls back to anon, which correctly 401s for SKS (in-place tenant, zero anon grants) — but `_loadSafe`/`_emitSyncHealth` (shipped v3.5.304, "SKS #60 parity") already catches exactly this, preserves last-known `STATE.people` instead of blanking it, and shows a "Sync degraded — people not updating. Showing last-known data" toast + a `sync_degraded` analytics event. Not a bug — working exactly as designed for a rare real network blip. Marked `ignored` (until escalating) in Sentry with the full trace recorded on the issue's own activity feed, rather than `resolved` (so a lone blip stays quiet but a real spike would still re-surface it). No code change made.
- Worked in an isolated worktree (`eq-field-load-time-9a800c`, branch `claude/timesheet-rls-error-5e183a`) per this repo's own Rule 0 — the shared root checkout was never touched.
- Substrate hygiene: `C:\Projects\eq-context`'s shared checkout was 108 commits behind `origin/main` with 2 unrelated dirty files (another session's in-progress work — `eq/changelog/eq-shell.md`, `sessions/2026-09-05.md` — left completely untouched) when this close ran. Did all substrate writes in a fresh isolated clone instead, matching the exact pattern the earlier session today (labour-hire licence-photo close, see above) already used for the same F9 hazard.

---

# Session 2026-09-07 — Sharon Maroni's missing timesheets traced to a database gap the 2026-09-04 "supervisors see everyone" fix never closed; day/date header contrast fixed; full DB reconciliation shipped

## Built
- eq-field [PR #935](https://github.com/eq-solutions/eq-field/pull/935) (v3.5.689, merged, live): Timesheets' day/date header text (`.sp-htick`, `timesheets-spans.js`) was `#6B7280` grey with no background of its own, sitting on the navy header — only the "today" column was correctly white. Default tick text is now white; the date sub-label opacity bumped `.6`→`.75`.
- Live data-only fix (no code): granted Sharon Maroni `field.view_all_crews` via a new Shell security group on jvkn (`shell_control.security_groups`/`user_security_groups`) — unblocks her immediately, takes effect on her next sign-in.
- eq-field [PR #936](https://github.com/eq-solutions/eq-field/pull/936) (migration `20260907_supervisor_broad_read_timesheets_leave.sql`, applied live to ehow, merged): `app_data.eq__timesheets_caller_has_broad_read()` and `eq__leave_caller_has_broad_read()` now grant tenant-wide read to any *resolved* `eq_role='supervisor'` session, matching what the client (`permission-matrix.js`) has promised since 2026-09-04. Both new branches require a resolved staff identity — does not reopen the "unresolved identity" fail-open hole 2026-08-24 (PR #768) deliberately closed.
- Same migration also finishes an independently-discovered, abandoned fix: `leave_requests_own_crew_read` had been calling the wrong (more lenient) generic RLS function since some point after 2026-08-23, not the dedicated fork built for it that day (PR #756) — a gap `20260831_field_permission_denials_enforce.sql` had already found and explicitly left unfixed. Repointed to the correct function in the same migration.

## Decided
- Royce chose "grant Sharon now, fix the DB policy properly after" over fixing the DB immediately or granting-only-Sharon-forever, when first presented with the RLS gap.
- Royce reviewed the full named blast-radius table (10 SKS supervisors) and chose "apply both" (Timesheets + Leave) over a narrower option (Timesheets only, or just fixing the Leave wiring without widening anyone's access).
- Royce approved merging both PR #935 and PR #936 once each was green.

## Deferred
- David Boyd's original 2026-09-04 complaint (the one that prompted PR #910) was very likely never actually fixed by that PR — he's `supervisor` role with 2 `team_supervisors` rows and no `extra_perms` grant, so the client-only fix wouldn't have changed what he could actually read. Not confirmed against his live session either before or after this session's DB fix — inferred from his DB state and the RLS logic, both now closed.
- Rhys Scott (job title "Site Supervisor", Field's own `is_supervisor` flag true) can't be reached by this fix — his actual Shell-granted role is `employee`, not `supervisor`. Needs a Shell-side role correction, out of this repo's reach.
- Two "Richard Brown" Shell identities exist for SKS — one resolves to a staff record, one doesn't. Likely the same duplicate-identity class already tracked as EQ-SHELL-14. Not investigated.
- How `leave_requests_own_crew_read`'s policy reverted to the wrong function between 2026-08-23 and 2026-08-31 is unknown — no migration file in the repo shows a reversion, so the mechanism that caused it once isn't ruled out from recurring.
- `leave_requests_update` still has no role check at all (tenant match only) — a pre-existing gap named in the original 2026-08-23 migration's own header, not touched this session either.
- None of the 10 named affected people were click-tested live as themselves (no credentials) — verified instead via 6 real claims-shapes exercised through `SET LOCAL request.jwt.claims` in rolled-back transactions, which tests the actual RLS boundary directly but doesn't prove the Timesheets/Leave pages render correctly end-to-end for a real one of them.

## Notes
- **Root cause, in full:** two separate, both-genuinely-deliberate decisions had drifted out of sync. 2026-08-24 (PR #768, Royce's explicit go) hardened the timesheets RLS function so only `manager` role (or an explicit per-person grant) gets broad read — closing a real leak where 14 unassigned supervisors could read the entire tenant. 2026-09-04 (PR #910) reversed that *client-side only* — `permission-matrix.js` started granting every supervisor `field.view_all_crews` by default — but touched zero SQL migrations (confirmed via `git show 07c98b10 --stat` against the shared eq-field root). The database was never told about the reversal, so the app has been assuming unscoped reads it wasn't actually getting, for 3 days, for every supervisor who wasn't already a `manager`.
- **The true affected population required querying from the right end.** Field's own `is_supervisor` flag on `app_data.staff` (a cosmetic/contact-purpose field) and the Shell-granted `eq_role` permission tier are independent — Sharon's own case proved this first (`is_supervisor=true` but the flag alone means nothing to RLS). Querying `shell_control.users.role='supervisor'` directly (the actual gate) found the real list of 10 differs from what an `is_supervisor`-flag query would have found: 5 people the flag-based query would have missed entirely (Anthony Hartley, Kurt Sticker, Rumen Iliev, William Brown, Richard Brown — all `is_supervisor=false` in Field's table but genuinely `supervisor`-tier in Shell).
- **Verification, not assertion:** before applying, drafted the exact migration with a full pre-apply prediction table (6 claims-shapes → expected true/false), matching this repo's own established convention for this class of change (`20260823`/`20260824` migrations). After applying, independently re-queried `pg_policies` fresh and re-ran all 6 claims-shapes for real via `SET LOCAL request.jwt.claims` in individually rolled-back transactions (nothing persisted) — every result matched the prediction exactly, including the critical negative case (a supervisor with an unresolved identity still gets `false`/`false`, confirming the 2026-08-24 fix's core protection stayed intact).
- All three of this repo's migration CI guards (`migration-shared-fn-guard`, `migration-function-grant-guard`, `migration-security-invoker-guard`) run locally before commit and passed clean.
- Both PRs verified against the actual Netlify deploy preview before merging (not just local static-serving) — PR #935's fix confirmed via `getComputedStyle` showing grey→white on the real preview URL; PR #936 has no client-side surface to click-test (DB-only).
- Local worktree preview gotcha, again: `.claude/launch.json` and the shared `C:\Projects\eq-field` checkout both point at the wrong tree for a worktree session (already a recorded pattern) — served this worktree's own files directly via `python3 -m http.server` to get an honest before/after comparison for PR #935.
- `gh pr merge --squash --delete-branch` failed locally both times on `'main' is already used by worktree at ...` — a local-only git-worktree collision (another concurrent worktree on this machine has `main` checked out), not a GitHub-side failure. Both merges had already succeeded via the API before the local cleanup step errored; confirmed via `gh pr view --json state,mergedAt,mergeCommit` each time, then deleted the now-orphaned remote branch via `gh api -X DELETE` since `--delete-branch` never got to run.
- Substrate: `eq/pending/eq-field.md`'s existing 2026-09-04 "supervisors see every crew" section carried an open item claiming the privacy tradeoff (everyone's hours/leave visible to every supervisor) was already real — that claim was not actually true at the database level until this session closed it. Ticked and trimmed per this file's own archive rule rather than left standing as a now-confirmed-stale claim.
- Substrate hygiene: `C:\Projects\eq-context`'s shared checkout was blocked from `git pull` by its own pre-tool-use hook (F9, concurrent-session corruption risk) — did all substrate writes in a fresh isolated clone per the hook's own documented recipe, same pattern both earlier sessions today already used.

---

# Session 2026-09-07 — adversarial_test.sh python3/python fix shipped; SEC-1 live-reverified, still exposed, register left as-is per Royce

## Built
- eq-context commit `cc22cfc4` (pushed to `main`): `hooks/adversarial_test.sh` no longer hardcodes `python3` — it now tries `python3` first and falls back to `python` if that doesn't actually execute. Fixes a false 0/36 failure on any machine where `python3` on PATH is the Windows Store app-execution-alias stub (present on PATH, exits 49, prints an install prompt) rather than a real interpreter. Verified 36/36. This closes the loose end yesterday's F10-mechanism-4 close explicitly spun off without bundling in (`ops/pending.md`).

## Decided
- Session tier: OPS.
- Commit + push the fix directly to `main`, no PR — single file, trivial, fully verified, nothing downstream depends on it.
- SEC-1: Royce initially asked to remove the finding from the register ("that app is retired"). Live-checked `sks-nsw-labour.netlify.app` directly rather than taking the claim on faith: it now shows a genuine "no longer being updated, use Field instead" banner, but the browser console confirmed it's still actively reading live config from Supabase (`app_config` for sks) and the full staff/roster dashboard still renders. `SKS-FIELD-PARALLEL-RUN-LOG.md` (the documented gate for actually decommissioning it) had logged zero clean proving-weeks as of its last update (2026-08-17), and `infrastructure.md` still flags the underlying Supabase project (`nspbmirochztcjijmcrx`) as live SKS production data under the contract's strictest never-touch rule. Presented this evidence; Royce's call was to leave `ops/security-register.md` untouched — SEC-1 stays open, unedited, register not updated with today's check either.

## Deferred
- SEC-1 — still open, still genuinely exposed (public key still reads the live backend), no register edit made this session per Royce's explicit call.
- A possible fresh F10 recurrence (see Notes) — surfaced, not investigated or fixed.

## Notes
- **Brief-gate flag is date-scoped, not just session-scoped.** A flag written under `/brief` expires at local midnight even mid-session — hit this directly when a session resume crossed midnight and a same-session, same-session-id flag written the evening before started blocking edits again. Worth knowing for any session that spans midnight.
- **Possible fresh F10 recurrence, one day after it was marked closed.** This session's resume hook reported `core.hooksPath` resolving unexpectedly ("*** WRONG ***... not .githooks") on 2026-09-07 — the day after `ops/pending.md` recorded F10 mechanism 4 as closed (2026-09-06, "now self-heals on a fresh clone"). This session used `EnterWorktree` mid-session, which may be an untested path for that self-heal fix — its own description scopes the fix to "a fresh clone," not a worktree taken off an existing one. Not investigated or fixed, just flagged given the timing.
- Shared checkout (`C:\Projects\eq-context`) went stale twice over — 2 commits behind at brief time, 15 then 18 behind by commit/close time — and hit both the F9 shared-checkout guard (on `git pull`/`rebase`) and the stale-main-gate (on a direct commit while behind). Did all actual writes, both the code fix and this close, from an isolated worktree instead, per both guards' own suggested recovery path. A concurrent session hit the identical F9 block earlier today doing unrelated eq-field work (see the three entries above).
---

# Session 2026-09-07 — David Boyd "can't see all timesheets" investigated fresh; initial answer corrected after cross-referencing a concurrent session's DB-level fix

## Built
- No code changes this session — investigation and diagnosis only.

## Decided
- None yet — findings reported to Royce; awaiting confirmation David can see everyone after a refresh.

## Deferred
- David Boyd's live click-test as himself remains unconfirmed — same open item already tracked in `eq/pending/eq-field.md` (the DB-side gap it depended on was closed today by the Sharon Maroni investigation; his own session still hasn't been checked). Not duplicated here — cross-referenced.

## Notes
- Royce relayed David Boyd's complaint directly ("cant see all the timesheets"), independent of the concurrent Sharon Maroni report being investigated elsewhere in eq-context the same day — neither side initially knew about the other.
- First pass checked only the client-side layer: `permission-matrix.js`'s supervisor grant (PR #910/v3.5.668, 2026-09-04), Shell's `shell_control.tenant_role_overrides` (no tenant-wide deny for SKS), and per-person `user_security_groups` (none on David's account) — all clean. Reported to Royce as "already fixed, likely just a stale client cache."
- That answer had a real gap: it never checked the database RLS function actually enforcing row visibility (`app_data.eq__timesheets_caller_has_broad_read`), only the client-side logic that decides whether to *ask* for everything. Caught before it mattered, reading `eq/pending/eq-field.md` fresh as part of this `/close` — it already documented the same-day Sharon Maroni investigation finding that exact function had been checking `eq_role='manager'` only since 2026-08-24 (PR #768), silently un-fixed by PR #910's client-only change, and only just repointed to also cover `supervisor` via PR #936 (merged 2026-09-07T01:36 UTC).
- Independently re-verified live rather than trusted from pending.md's own account: pulled `pg_get_functiondef` for `app_data.eq__timesheets_caller_has_broad_read` directly from ehow — confirms the supervisor-plus-resolved-identity branch PR #936 added is live now, and matches David's own account shape (`eq_role`-resolvable, `staff_id` linked via `user_id`). Confirmed PR #936 itself is real and merged via `gh pr view`, not just asserted in the doc.
- Net effect: the corrected answer reaches the same practical conclusion as the first pass (nothing should be blocking David as of right now — ask him to refresh and check) but on solid ground, not an incomplete check that happened to not be wrong only because someone else's unrelated fix landed first.
- **Reusable lesson for this codebase:** checking `permission-matrix.js`/`permissions.js` shows what the *client* will attempt, not what the *database* will actually return on an RLS-backed table. For `timesheets`/`leave_requests` specifically, the enforcing function is `app_data.eq__<table>_caller_has_broad_read()` on the tenant DB — check it directly, don't infer from client-side permission code alone.
- Substrate hygiene: `C:\Projects\eq-context`'s shared checkout was 18 commits behind `origin/main` at session start. Working tree also carried an uncommitted local edit to `hooks/adversarial_test.sh` — diffed it against the equivalent already-merged upstream commit (`cc22cfc4`, from the concurrent session above) before touching anything, confirmed byte-identical, discarded safely rather than assumed safe. `git pull` in the shared checkout was then correctly blocked by its own F9 guard; did all substrate writes in a fresh isolated clone instead, same pattern as both other sessions today.

---

# Session 2026-09-07 — eq-context clone reconciled; hooksPath drift traced to a likely-fresh F10 recurrence, self-corrected before it could be caught

## Built
- eq-context local clone (`C:\Projects\eq-context`, main worktree) fast-forwarded from 19 commits behind to 0, matching `origin/main`.
- Re-asserted `core.hooksPath = .githooks` via `git config --local` — a no-op, value was already correct at the time of the live check.
- `ops/pending.md` — new dated section logging the hooksPath finding below as a Royce-decision item (not self-filed into `failures.md`, per that file's own proposal process).

## Decided
- None — this was a delegated verification/reconciliation task; nothing came up needing Royce's call mid-session.

## Deferred
- Whether to log the hooksPath drift below as F10's 5th recurrence in `system/failures.md`, and whether `EnterWorktree` is actually the trigger — see `ops/pending.md` entry added this session.
- Whether the now-fully-merged `worktree-adversarial-test-python-fix` worktree (still locked, under `.claude/worktrees/`) should be unlocked/pruned — not actioned, out of scope for this task.

## Notes
- **The task's starting premise (an uncommitted local modification to `hooks/adversarial_test.sh`) checked out clean by the time this session looked — but per the concurrent "David Boyd" session's own account directly above, it was real.** That session found the same uncommitted edit, diffed it byte-identical against the already-merged `cc22cfc4`, and discarded it safely — in the same rough window this session was starting. So nothing was actually stale about the original ask; it resolved itself correctly via a concurrent session moments before this one got to check, not because there was never anything there.
- **Likely-fresh F10 recurrence, self-corrected before it could be investigated.** This session's own resume gate reported `core.hooksPath` resolving to the absolute path `C:\Projects\eq-context\.githooks` at start — the wrong-but-set shape matching F10 mechanisms 1-3, not the fully-unset shape the 2026-09-06 self-heal covers. A live check a few tool-calls later found it already back to the correct `.githooks` (single clean entry in `.git/config`, `extensions.worktreeConfig` confirmed off so mechanism 3's worktree-shadow is structurally ruled out this time). The other session working today (see the "adversarial_test.sh python3/python fix" entry above) hit the identical symptom and explicitly logged it as not investigated or fixed, suspecting `EnterWorktree` as an untested path around the self-heal. Neither the self-heal (wrong shape) nor that session (said they didn't) explains the correction — most likely Royce fixed it by hand in between, but there's no direct evidence either way. Treating this as a probable genuine recurrence (the effective value was observed wrong, twice, by two different sessions today) rather than a stale-print false positive, per `failures.md` F10's own documented distinction between the two.

---

# Session 2026-09-07 — New PC dev environment set up for eq-shell (node_modules, Netlify CLI, Developer Mode)

## Built
- Fixed corrupted `node_modules` in `C:\Projects\eq-shell` (copied over from Royce's old PC rather than freshly installed, breaking pnpm's hardlinked store — `tsup` failed with `Cannot find module 'resolve-from'` despite it being a real dependency) — wiped and reinstalled; `pnpm run build` and `pnpm test` now pass clean (581/583, 2 skipped).
- Netlify CLI logged in (`dev@eq.solutions`) and linked to the `eq-shell` site.
- Pulled the available dev-context env vars from Netlify into local `.env` via the CLI — kept secret values out of the conversation transcript throughout (redirected the pull straight to a scratch file, merged with a script, deleted the scratch file after).
- Diagnosed and fixed: Windows Developer Mode had never been enabled on this machine, blocking the symlink creation both pnpm and Netlify's local function bundler need. Royce enabled it; confirmed fixed via a live Node.js symlink test (PowerShell's own `New-Item -ItemType SymbolicLink` gave a false negative — it doesn't use the same unprivileged-symlink path Node's `fs.symlinkSync` does).

## Decided
- Tier: EQ.
- Pull real `.env` values via the Netlify CLI directly rather than manual copy-paste from dashboards.
- Skip the session brief-gate for this session — pure environment setup, no product code touched.

## Deferred
- `.env.example` is stale relative to both the README's documented required vars and Netlify's actual dev-context vars — logged in `eq/pending/eq-shell.md` (2026-09-07).
- `SUPABASE_URL`/`SUPABASE_JWT_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`SKS_SUPABASE_JWT_SECRET` aren't set anywhere in Netlify's dev context (only the `VITE_`-prefixed browser pair is) — blocks the full local `netlify dev` auth flow until Royce decides how these should be sourced locally. Same pending entry.
- `netlify dev`'s Functions emulator hit Windows `EMFILE` errors on the first two runs (~70 functions sharing one huge Sentry/OpenTelemetry dependency tree) — a third run over the same files got measurably further with zero errors, pointing at antivirus first-touch scanning on a cold machine rather than a real defect. Not fixed; expected to keep improving with normal use.

## Notes
- Root cause of the `node_modules` corruption: pnpm's content-addressable store uses hardlinks/junctions tied to a local store path — copying a project folder to a new machine instead of running `pnpm install` fresh there breaks them silently, until something tries to load a file that's "present" but not really linked.
- netlify-cli 27.5.x treats this repo's pnpm workspace packages as separate "projects" and throws an interactive picker on `link`/`env:list`/`dev` without `--filter eq-shell` — fatal in a non-interactive session (no stdin to answer the prompt).
- Hit `~/.claude/hooks/guard.js`'s brief-gate on a routine `Write` (a new `.claude/launch.json`). Royce chose to skip the gate for this session rather than run a full `/brief`. Satisfied it by writing the session flag file directly into `AppData\Local\Temp` (the gate's own documented exemption path) rather than via `EQ_SKIP_BRIEF`, since that env var can't actually reach the hook's process from a tool-call subprocess in this harness — set inside a Bash call, it never propagates back to whatever spawns the hook.
- A `SessionStart:resume` mid-session flagged this shared checkout as up to 19 commits behind `origin/main`, with an uncommitted `hooks/adversarial_test.sh`, and `core.hooksPath` resolving to an absolute path instead of the governed relative `.githooks` (F10). None of that is eq-shell's concern, and the guard's own message asked for a human decision rather than a silent fix, so it was spawned as `task_af5e89fe` instead of handled inline here. Royce started that task in a separate session — see "eq-context clone reconciled..." directly above in this same file for the resolution (clone fast-forwarded to 0-behind, uncommitted file confirmed a safe-to-discard duplicate of an already-merged fix).
- Session self-corrected on scope mid-stream: after three `netlify dev` attempts and a side investigation into the eq-context findings above, Royce pushed back ("rabbit hole? what are we even fixing?") — fair. The actual goal (eq-shell usable on this PC) was already done by that point; everything after "try netlify dev" was chasing one non-blocking extra (the local Functions emulator), which doesn't block normal work — `pnpm dev` covers UI work today regardless.

---

# Session 2026-09-07 — New PC dev environment set up for eq-service; 6 open PRs cleared (5 Dependabot + #791), 2 real CI issues found and fixed along the way

## Built
- eq-service: Netlify linked (site `eq-service`), dev-context env vars pulled into `.env.local`.
- eq-service [PR #811](https://github.com/eq-solutions/eq-service/pull/811) (vitest), [#813](https://github.com/eq-solutions/eq-service/pull/813) (@types/leaflet), [#812](https://github.com/eq-solutions/eq-service/pull/812) (posthog-node), [#814](https://github.com/eq-solutions/eq-service/pull/814) (resend) — merged. Each PR's only failing check was the pre-existing, unrelated `report_show_cover_page` migration-replay bug (non-required check, same on all four).
- `tests/lib/utils/permission-enforcement-baseline.json` — whitelisted 4 permission keys (`admin.assign_role`, `admin.delete_user`, `reports.manage`, `service.do_work`) surfaced by PR #810's `@eq-solutions/roles` v2.7.0 bump. Each verified live against eq-shell's actual enforcement call sites before whitelisting, per this file's own "don't silently add" rule: 3 belong to eq-shell's user-admin/GM-report surface, `service.do_work` is dead suite-wide (already in eq-shell's own `dead_keys` list).
- eq-service [PR #830](https://github.com/eq-solutions/eq-service/pull/830) — `browserslist` overridden to `^4.28.9` in `package.json`'s `overrides` block, closing 2 fresh high-severity npm audit advisories (GHSA-c83g-rgw3-j3cx, GHSA-73wf-gq98-2v4g) against a transitive dependency of `@serwist/next`. Chose this over npm's own suggested fix (`audit fix --force` → `@serwist/next` 9.5.12→9.4.1, a real breaking-change risk to the service-worker build); confirmed the vulnerable version predated this session (already in the lockfile before any of today's merges) and that 4.28.9 (latest, same 4.28.x line) also clears both advisories. Merged.
- eq-service [PR #810](https://github.com/eq-solutions/eq-service/pull/810) — merged once the baseline fix above and #830 both landed (it was blocked by both).
- eq-service [PR #791](https://github.com/eq-solutions/eq-service/pull/791) (report reissue, the 2026-08-20 prerequisite fix) — resolved a 16-day-stale merge conflict (an unrelated `Alert`-component refactor landed on main touching an adjacent line in the same file), verified against the full suite (707/707) + typecheck, pushed, then merged. Live in production now, sends real customer email.
- eq-context: fixed `core.hooksPath` (was the absolute path `C:\Projects\eq-context\.githooks` instead of the governed relative `.githooks` — F10-class fragility) and reconciled this session's view of substrate content via an isolated clone rather than pulling the shared checkout directly.

## Decided
- Royce: tier EQ, work on eq-service.
- Royce: start the open-PR cleanup with the 5 Dependabot bumps.
- Royce: fix the permission-baseline whitelist and open a PR for the browserslist fix ("yes to both").
- Royce: merge #830, then #810 once its own CI cleared.
- Royce: resolve #791's merge conflict and push it, then explicitly merge it once CI was clean — aware it deploys to production and sends real customer email.
- Royce: fix eq-context's hooksPath and reconcile the stale substrate view via an isolated clone ("yes to both").

## Deferred
- `SUPABASE_SERVICE_ROLE_KEY` still isn't in `.env.local` on this PC — confirmed via `netlify env:get` that Netlify itself has no value for it in any context (dev or production), so this isn't a per-machine gap; whatever the old PC had almost certainly got there by hand from the Supabase dashboard, not via `netlify env:pull`. Needs Royce to grab it himself (project `ehow` → Settings → API → `service_role`).
- `EQ_SECRET_SALT` / `EQ_SERVICE_JWT_SECRET` exist in Netlify but production-context only — Royce's call whether local dev should reuse the real production shared-HMAC value.
- `.claude/launch.json` for local dev-server preview was started (tripped the brief-gate) but never finished — session moved to the Dependabot batch instead.
- PR #791's live click-through still hasn't happened — now live in production, but blocked all session on the missing service-role key above.

## Notes
- Master Behavioural Contract session-start sequence run in full (identify/confirm-read/tier-question/brief). `WebFetch`'s own summarizer model refused to return the contract's raw text on the first attempt — misread the document as an attempt to make *itself* act as a persistent agent and lectured back instead of returning content. Worked around with a direct `curl` fetch, then switched to the local clone once confirmed present, per the contract's own tool-specific rule (Claude Code reads substrate from the local clone, never raw URLs).
- Local eq-context clone was already present on this new PC — the working assumption of "fresh PC needs a fresh clone" didn't hold here; only gitignored, per-machine files (`.env.local`) were actually missing.
- Confirmed `service.report_deliveries` (touched by #791) is a genuine standalone table, not a canonical view — traced its RLS through migration history (current: `0187`, direct-JWT-claim pattern) rather than trusting the PR's own claim, since an earlier migration (`0045`) describes an older, superseded `public.report_deliveries` with helper-function-based RLS that no longer applies.
- SEC-57 (org-wide `grok-by-xai` GitHub App) came up via a stale "Needs you" digest entry — already partially closed by another session today per eq-context's own commit log; only checked here, not acted on.
- Hit the same `pre_tool_use.py` (F9) shared-checkout guard other sessions today also hit — used the sanctioned isolated-clone pattern rather than working around it.
- One self-caught process slip: ran `netlify login` directly via Bash instead of handing it to Royce to run himself. Caught immediately — no credentials were or could have been entered from this side (no browser control here, the CLI just idled on an auth ticket) — Royce completed the actual authorization himself.

---

# Session 2026-09-07 — eq-shell new-PC setup finished: .env.example fixed, dev gotchas documented, remaining secrets resolved locally

## Built
- eq-shell `.env.example`: added 19 vars confirmed (by grep against `netlify/functions/` and `src/`) to be genuinely read in code — not just copied from Netlify's var list. Deliberately excluded ~8 vars that turned out unused in this repo despite being configured on Netlify (see Notes).
- eq-shell `CLAUDE.md`: 3 new bullets in "Build + dev gotchas" — don't copy `node_modules` between machines, enable Windows Developer Mode before `netlify dev`, and netlify-cli 27.x needs `--filter eq-shell` or it hangs on an interactive picker.
- eq-shell local `.env`: filled the last 4 missing vars (`SUPABASE_URL`, `SUPABASE_JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `SKS_SUPABASE_JWT_SECRET`) from Netlify's production context — confirmed present via `netlify env:list --json --context production`, merged in via script, values never displayed in the conversation.
- `netlify dev` confirmed genuinely working end-to-end on this machine — 7+ functions loading clean with zero errors on the run that mattered, after two earlier cold-machine runs both hit `EMFILE`.

## Decided
- Royce: "go ahead" on the `/brief`-confirmed plan (fix `.env.example` + `CLAUDE.md`, nothing else).
- Royce: "I just dont want to have these issues movnig forward, i dont really care where they are stored" — read as authorization to source the 4 missing secrets from wherever they already live (production context) and use them locally.
- Royce, asked whether it's worth pushing further to also write these into Netlify's dev context (so a future machine doesn't hit the same gap): "does any of this matter?" then confirmed — not much. Left deprioritized rather than pursued.

## Deferred
- Writing the 4 secrets into Netlify's **dev** context (would future-proof the next machine too) — Claude Code's own auto-mode safety classifier blocked this specific write (production-derived secrets → live infra config) when attempted; not routed around. Two paths left for Royce if he ever wants it done: the Netlify dashboard himself, or a Bash permission rule if he wants Claude able to do this class of write. Explicitly low-priority per his own call above.
- 5 more Netlify-configured vars found to have zero code references in eq-shell (`CANONICAL_SUPABASE_URL`/`_ANON_KEY`, `SERVICE_SUPABASE_URL`, `QUOTES_APP_URL`, `SKS_PIPELINE_URL`, `GOOGLE_DOC_AI_CREDENTIALS`/`_ENDPOINT`/`_REGION`) — worth folding into the existing SEC-67 dead-env-var cleanup (which already covers 4 similarly-dead vars), not actioned this session.

## Notes
- **This session corrected its own earlier mistake from the first close today.** The original pending.md entry claimed "~20 configured vars not in the template at all," implying they were all real gaps — a direct grep proved about half of them are simply unused in this codebase, not missing-but-needed. Lesson worth keeping: don't infer a var is required just because a hosting platform has it configured — check actual code usage before templating it, since dead platform config is common and copying it forward just launders the staleness into a "canonical" template.
- `netlify env:get VAR_NAME` masks its output by default (`****************6w==`-style) — useful for a human checking a value exists without seeing it, useless for a script that needs the real value. `netlify env:list --json` returns full plaintext and is the right tool when the actual value needs to move somewhere. Worth remembering for any future session touching Netlify env vars.
- Full secret-handling discipline maintained throughout both this session's env-var work and the earlier one today: every pull of a real secret was redirected straight to a scratch file, transformed by a small script, never printed into the conversation, and the scratch files (plus the intermediate script) were deleted immediately after use.
- Royce pushed back mid-session on scope ("rabbit hole? what are we even fixing?") after a long side-investigation into eq-context's own git hygiene (hooksPath drift, clone 18-19 commits behind). Fair — that tangent was properly spun off as `task_af5e89fe` rather than worked here, and a separate session has since resolved it in full (see "eq-context clone reconciled..." earlier in this file). The actual goal (eq-shell usable on this PC) was already done by the time that tangent started.
- Ran `/brief eq-shell` for real this time (available as a proper skill after the earlier session restart, unlike the flag-touch workaround used the first time this session hit the brief-gate) — health digest, worktree check, and eq-shell git state all came back clean, current branch (`fix/field-handoff-stall-visibility`) 1 commit behind `main` on an unrelated auth-timing fix.
- Substrate hygiene: shared checkout was 1 commit behind `origin/main` at close time, clean otherwise (only this session's own 2 intended files dirty) — a plain `git pull --rebase` or fetch-and-check was sufficient, no isolated-clone escalation needed this time.

---

# Session 2026-09-07 — Substrate over-structure diagnosis; `/tidy` protocol built; `/brief`'s stale-digest gap found and fixed live

## Built
- `rules/tidy-protocol.md` — new Royce-triggered (never automatic) substrate pressure-release protocol: measure standing-file sizes, classify every CLAUDE.md/rules rule as HARD/PREFERENCE/SCAR TISSUE, enforce size budgets, propose cuts with sign-off rather than editing unilaterally. Seeded with a real baseline Pass Log row from this session's own measurements, not a stub.
- `~/.claude/commands/tidy.md` — new thin-trigger `/tidy` command, following the same pattern as `/gap`/`/decide`/`/reflect` (points at the tracked rule file, no repo backup needed).
- Fixed a templating bug shared by `/brief`, `/gap`, `/decide`, `/reflect`: the "Defaults when $ARGUMENTS is empty" heading gets substituted even when an argument *was* given, producing self-contradicting text (e.g. "Defaults when eq-context is empty"). Fixed all four, live at `~/.claude/commands/` plus the `tools/commands/brief.md` repo backup.
- `/brief` Step 1: now runs one `git log HEAD..origin/main -- digest.md` check and says so explicitly if the digest is behind, instead of presenting its content as unconditionally current.
- `/brief` Step 2: no longer depends on reading `system/worktree-registry.md` (368.5KB / 647 lines, exceeds a single Read call) — now uses live `git worktree list`, matching what root `CLAUDE.md` already says to trust over that file.
- `tools/commands/README.md` — updated to document `/tidy` as a fourth thin-trigger command (table, counts, headings all cross-checked for consistency after a mid-edit heading/body mismatch was caught and fixed).
- `ops/pending.md` — new dated section logging the deferred items below.

## Decided
- Royce: `/tidy` should be a small, manually-triggered skill (not folded into `/housekeep`, not left as an unstored ad-hoc prompt).
- Royce: F10 (chronic hooksPath bug, possibly recurring again per tonight's digest) — not now, don't spawn a task either. Already tracked in this file's own F10 entries above; nothing added there.

## Deferred
- The actual `/tidy` classification pass (steps 2–6, going rule-by-rule through CLAUDE.md + `rules/*.md`) hasn't run — protocol's built, baseline logged, pass itself not done.
- `worktree-registry.md`'s 368.5KB size — investigation spawned (`task_d0ceeaf5`), Royce started it running in a separate session.
- Nightly digest/suite-state-refresh firing 2-3× the same day instead of once — investigation spawned (`task_43c901bf`), Royce started it running in a separate session.

## Notes
- **This session's own `/brief` output was stale, live, mid-session — a real instance, not a hypothetical.** The requested write (`rules/tidy-protocol.md`) sits outside the brief-gate's exemption list (only `pending.md`/`sessions/`/`changelog/`/`~/.claude/**` are exempt), so `/brief eq-context` ran for real. Its Health section, sourced from the local `digest.md`, was confirmed via `git show origin/main:digest.md` to be ~7 hours / 2 commits stale — real Needs You count was 9, not the 7 reported. The 2 misses: F10 (already tracked in `ops/pending.md`) and a Sentry alert promoted from the existing Live Errors table. Root-caused and fixed in `/brief` itself rather than just noted.
- **Caught a live concurrent-edit collision mid-session.** An `Edit` call against `tools/commands/README.md` failed on a stale match because a concurrent session (most likely the `worktree-adversarial-test-python-fix` worktree, which shipped `cc22cfc4` earlier today per the entries above) had already fixed a `housekeeping`→`housekeep` typo in the same file between this session's first read and its write. Nothing corrupted — `Edit`'s exact-match requirement refused rather than guessing wrong; re-read and reapplied cleanly. (A second `Edit` failure, appending this very section, turned out on inspection to be a plain transcription slip on this session's own part — the attempted match dropped the word "hygiene" and a leading bullet marker from the real line, not a second collision. Worth naming rather than letting this log overclaim.)
- Diagnosed the original question ("are we getting bogged down, limiting judgment?") with real numbers before opining, not vibes: of the 15 most recent `eq-context` commits at session start, 14 were automated bookkeeping (7× session-close, 3× nightly digest, 2× nightly suite-state-refresh) and 1 was product work. `eq/pending.md` — split from a 491KB single file on 2026-08-17 specifically because it was too large to load — had already regrown to 4,919 lines across shards three weeks later.
- Nothing committed to git during the session itself — held for this close, per "only commit when asked."
- Session tier: OPS (substrate/tooling, not EQ or SKS product work).

---

# Session 2026-09-07 (continued) — `/tidy` classification pass run; both spawned tasks resolved; deploy-note duplication cut

## Built
- Ran `/tidy`'s classification pass (steps 2–6): read all 11 `rules/*.md` files fresh, classified every rule as HARD/PREFERENCE/SCAR TISSUE against `CLAUDE.md` too.
- Applied 2 approved cuts: [rules/non-negotiables.md](rules/non-negotiables.md)'s lapsed sprint-scope carve-out (123→113 lines), and the eq-shell deploy-note duplication between [rules/deployment.md](rules/deployment.md) and the user's global `~/.claude/CLAUDE.md` (189→135 lines) — the global copy now points at eq-context's instead of re-stating it.
- Updated `rules/tidy-protocol.md`'s Pass Log with fresh measurements + this pass's verdict.
- Moved the resolved "`/tidy` protocol built..." section from `ops/pending.md` to `ops/pending-archive.md` (all 3 of its follow-ups closed); replaced it with one smaller, still-genuinely-open item (pending-file budget enforcement).

## Decided
- Royce, on Cut 1: pushed back on accepting the narrow trim without checking — asked for the full "Code & Deployment" section reviewed via `/decide` first. Ran the full 6-step pass; confirmed nothing else in that section needed cutting (one minor, low-stakes duplicate noted — rule 2's proxy list vs. `rules/stack.md` — not touched, both copies already agree).
- Royce approved Cut 2 as recommended: keep `rules/deployment.md` authoritative, trim the global file to a pointer.

## Deferred
- Pending-file budget enforcement (`eq/pending/*`, `lessons.md`, `failures.md`) — still open, freshly logged in `ops/pending.md` rather than left in the archived write-up where it wouldn't get reloaded.

## Notes
- **Both background tasks spawned earlier tonight resolved during this continuation.** `task_d0ceeaf5` (`worktree-registry.md` at 368.5KB) split it into a live file + pruning archive (`b7b07726`). `task_43c901bf` (nightly digest/suite-state-refresh "firing 2-3× a day") found it wasn't a bug at all: 13 of the last 15 runs on each workflow are `repository_dispatch` real-time refreshes, fired by `notify-substrate.yml` on every EQ-repo merge, by design, with an existing concurrency guard already serializing them — not the schedule cron misfiring. The only actual defect was stale "nightly" framing left in `suite-state.md`'s own frontmatter and Crons table from before the dispatch mechanism was wired up; fixed, and the generator now self-heals it going forward (`5701927f`). Worth being honest that this session's own earlier framing of the finding ("firing multiple times, investigate") undersold the real, more interesting answer — a working-as-designed real-time system with a documentation lag, not a misconfigured cron. Same failure class as everything else tonight (a doc describing an old mechanism after a newer one quietly replaced it), just benign this time.
- Substrate drifted from 8 to 13 commits behind `origin/main` over the course of this continuation — expected given tonight's concurrent-session volume; same isolated-clone recipe as the first close, not the shared checkout directly.

---

# Session 2026-09-07 — eq-shell full review (security/quality/unfinished-work); issue tracker reconciled; 4 security fixes shipped and confirmed live

## Built
- eq-shell [PR #1788](https://github.com/eq-solutions/eq-shell/pull/1788) — `.env.example` fully documented (19+ vars, including 2 CLAUDE.md calls load-bearing: `EQ_SESSION_SALT`, `EQ_QUOTES_HANDOFF_KEY`; closes #713). Merged, confirmed live.
- eq-shell [PR #1789](https://github.com/eq-solutions/eq-shell/pull/1789) — startup warning when `ENFORCE_IFRAME_ORIGIN` isn't `'true'` (mirrors `EQ_SECRET_SALT`'s existing pattern; deliberately not gated on `process.env.CONTEXT`, documented elsewhere in this repo as unreliable at Function runtime). Merged, confirmed live.
- eq-shell [PR #1790](https://github.com/eq-solutions/eq-shell/pull/1790) — `reset-user-pin` can no longer target a platform_admin (closes #709: a regular manager could otherwise escalate to full platform_admin via the returned `reset_url` + `accept-pin-reset`'s auto-sign-in). New test file — this endpoint had none before. Merged, confirmed live.
- eq-shell [PR #1791](https://github.com/eq-solutions/eq-shell/pull/1791) — a revoked session can no longer mint credentials or be laundered into a fresh one via `switch-tenant` (closes #870, all 4 of its acceptance criteria). New shared `isSessionRevoked()` helper wired into `switch-tenant.ts`, `mint-supabase-jwt.ts`, `token-exchange.ts`, and `tenant-context.ts` (the last one covers `mint-tenant-jwt.ts` + `tenant-data-proxy.ts` as a bonus, beyond the issue's own minimum list). Merged, confirmed live.
- Closed 4 stale auto-revendor PRs (#1739/#1769/#1783/#1786) as superseded by #1792 — confirmed via real 3-way merge-tree hashing against current main (byte-identical result), not inference.
- GitHub issue-tracker reconciliation across all ~50 open eq-shell issues (4 parallel batch agents + direct checks): 9 closed with cited live-code evidence (#710, #891, #708, #695, #902, #903, #734, #966), fresh evidence added to several more left open (#709, #870, #871, #1325, #908, #1290, #981, #913), ~40 total left open but with materially better evidence/narrowed scope than before.

## Decided
- Royce: proceed with issue-tracker reconciliation + CSRF/env-var quick wins + committing an already-uncommitted `.env.example` fix (chosen from several options offered).
- Royce, on `ENFORCE_IFRAME_ORIGIN`'s dashboard-only-config fragility: add a loud startup warning (additive, keeps the existing no-code-change rollback path intact) rather than hardcoding enforcement everywhere or leaving it alone.
- Royce: fix #709 and #870 directly — both real, both auth-flow changes, explicit go-ahead given per this repo's own non-negotiable on that class of change.
- Royce: run `/triage` scoped to eq-shell only, not the whole suite.
- Royce: close all 4 stale auto-revendor PRs; handle the Dependabot-secrets CI gap himself rather than have it fixed in this session.
- Royce: merge #1788 through #1791, then #1793/#1785 too (both turned out already merged live by concurrent sessions before this session's own merge attempt reached them).

## Deferred
- SEC-1 (`people`/`timesheets`/`leave_requests` anon-readable on zaap/ehow) — reconfirmed real via `check-tenant-drift.mjs`'s own `KNOWN_LEGACY_ANON` list; already tracked, active burn-down in progress, not touched this session (architectural — needs Field's own move off anon-role auth).
- #711/SEC-71 (mandatory TOTP enforced client-side only) — reconfirmed live via direct code read (`shell-login.ts:476-495`); the issue's own text says it needs Royce's call on intended grace-period semantics before implementation. Not built.
- #981 (fast-uri still vulnerable despite an already-shipped fix; 2 newly-found vulnerable packages, `browserslist`/`fflate`) — commented on the issue with fresh live Dependabot evidence; not touched, since this exact vendored-dependency area has a documented history (#1290) of re-vendoring silently reopening a fixed advisory.
- #870's own narrower remaining edge: a manager can still reset a same-tenant co-member who also belongs to a second tenant and inherit that person's other-tenant access via the resulting session — flagged on the PR/issue as a separate scoping decision, not resolved either way.
- `/triage`'s waiting-on-you/aging buckets for eq-shell — not run by this session; Royce said the needs-you batch had already been answered elsewhere and chose to stop before reaching the rest. A concurrent session ran a full pass over exactly those buckets in parallel — already reflected in `eq/pending/eq-shell.md`.

## Notes
- **Extremely heavy concurrent-session activity tonight, directly encountered rather than just inferred from digest staleness.** Multiple PRs (#1792, #1793, #1785, #1794) were merged by other sessions mid-task, in real time, while this session was still auditing/merging its own set — required re-verifying mergeability and production-deploy ancestry repeatedly instead of trusting a single check, and twice found a PR already merged by someone else before this session's own explicit-instruction merge attempt reached it (#1793, #1785 — both independently confirmed already live). Netlify itself visibly superseded/skipped 3 of this session's own 4 merge-triggered builds mid-stream (`error_message: "Skipped"`) — confirmed each one's commit was still an ancestor of whatever build DID go ready via `git merge-base --is-ancestor`, rather than assuming a skip meant a miss.
- **GitHub issue-tracker hygiene is a real, general problem for this repo, not one or two stale tickets.** 2 of the first 3 spot-checked P0/P1 security issues (#710, #891) turned out already fixed in code weeks ago, just never closed — fixes are shipping without their tracking issues being closed, as a pattern, not an exception.
- This session's own `eq-context` clone was 8→37+ commits behind by the time this close ran, with `eq/pending/eq-shell.md` (and ~20 other files) already carrying substantial uncommitted work from at least one other concurrent session (a clean, well-formed `/triage` pass over eq-shell's waiting-on-you/aging buckets) — diffed and confirmed legitimate before building this session's own additions on top of it, rather than discarded or overwritten.

---

# Session 2026-09-07 (continued) — Built the `/triage` skill, ran it live across needs-you/waiting-on-you/aging, ran a `/tidy` pass, closed with real F9 reconciliation work

## Built
- New skill: `/triage` — a clickable HTML-card pass through `digest.md`'s curated needs-you/waiting-on-you/aging buckets (`Act now`/`Spawn task`/`Defer`/`Dismiss`, optional per-card context field). Protocol at `rules/triage-protocol.md`, trigger command registered, cross-referenced from `CLAUDE.md` §8.
- Ran `/triage` live across all three buckets against real digest.md data — needs-you (9 items) and waiting-on-you (7 items) processed pre-compaction (see this file's own earlier entries and `eq/pending/eq-shell.md`'s dated `/triage` notes for outcomes); aging (7 items, AG1-AG7, all eq-shell, dated 2026-07-21→23) processed this half: AG2 (cert-read Sentry recurrence) and AG3 (stale PR #945 rebase note) closed with verified evidence; AG4 (orphaned-shell-users cleanup script) and AG5 (retire/guard the old admin restore button) spawned as background tasks; AG1 (Job Creation export re-pull) and AG7 (Company-field/SKS-17386 click-test) held — genuinely need a live Shell session or Royce's own hands, not something to fake; AG6 (diagnosed-but-unbuilt logout bug) held on writing new session/auth code with no explicit go-ahead for a specific approach — corrected the pending-file record instead, since the framing had gone stale (5 PRs already fought this problem class, most recently regressing 2026-09-06).
- Ran a `/tidy` substrate-pressure-release pass (logged as pass (d) in `rules/tidy-protocol.md`'s Pass Log): all 11 `eq/pending/<repo>.md` shards given a stated ~500-line budget (3 already over: `cross-repo.md`, `eq-field.md`, `eq-shell.md` — flagged for a dedicated prune pass, not curated here); fixed a stale conditional comment in `digest-refresh.yml` (its `repository_dispatch` trigger has been live since 2026-09-05, not "pending" as the comment still claimed).
- **Real substrate-hygiene work, not originally planned**: found and reconciled multiple instances of this session's own edits diverging from origin/main mid-session under extremely heavy concurrent load (F9's exact failure shape, caught before damage rather than after) — `system/failures.md` (was missing a Budget line + F14's already-merged 5th-recurrence update while carrying this session's own unpushed F1/F9/F12 notes), `eq/pending/eq-cards.md` (missing a whole archived-then-unarchived session write-up plus a `/triage`-added bullet from a different concurrent session), `rules/tidy-protocol.md` (missing pass (c)'s already-merged row), `suite-state.md` (a different session had already shipped a more thorough fix for the same "nightly framing" finding this session made independently — adopted theirs instead of shipping a redundant/conflicting duplicate).

## Decided
- (Design decisions for `/triage` itself were made earlier this session, pre-compaction — interaction = inline widget, scope = curated needs-you/waiting-on-you/aging only, never the raw ~1,300-item engineering backlog.)

## Deferred
- AG1, AG7 — need a live Shell session or Royce's own hands; noted plainly in `eq/pending/eq-shell.md` rather than faked.
- AG4, AG5 — spawned as background tasks (`task_1d24d05a`, `task_659a82f7`). AG5 ended with no visible commit or pending-file update found on close — the bullet it was meant to resolve is still untouched; worth checking its transcript directly if the outcome matters. AG4's outcome not yet confirmed either.
- The 3 over-budget `eq/pending/*.md` shards (`cross-repo.md` 912, `eq-field.md` 1284, `eq-shell.md` 1394 lines) — deciding which entries are stale enough to archive is real editorial judgment, not attempted mechanically this session.
- `suite-state.md`'s Crons-table fix and the `digest-refresh.yml` cadence finding were both surfaced to Royce via AskUserQuestion before touching anything (all 3 offered fixes approved).

## Notes
- **This session ran concurrently with at least 4-5 other active sessions on the same shared `eq-context` checkout for its entire duration** — origin/main advanced from ~7 commits behind to 37+ by close, driven by nightly digest/suite-state regenerations (confirmed live-firing on every EQ-repo merge, not just nightly, per the `/tidy` Step 4 finding), multiple other sessions' own `/triage` and `/close` passes (including a full eq-shell security/quality review shipping 4 PRs, #1788-#1791), and at least one background task's own reconciliation work. Several times this session, a file diffed clean against origin moments earlier came back different on the next check — `eq/pending/eq-shell.md` especially, the single hottest file across every concurrent session tonight. Treated as the expected operating condition for this shared substrate, not an anomaly: verified fresh immediately before every write, favored small targeted `Edit` calls over full-file rewrites wherever the live state was still moving, and where a full-file reconstruction was unavoidable (`system/failures.md`, `suite-state.md`), built it by layering this session's own known-unique edits onto a freshly-fetched origin base rather than trusting a snapshot taken earlier in the session.
- **One real reconstruction mistake, caught before it did damage**: mid-close, briefly treated a different concurrent session's own in-progress uncommitted top-of-file addition to `eq/pending/eq-shell.md` (a full security/quality review write-up) as if it were "origin's already-merged replacement" for an older section, and began rebuilding the file on that false premise — which would have silently discarded that session's real work. Caught by re-checking `git show origin/main:...` (a pure origin read) against what a plain working-tree `Read` had shown moments before and finding they disagreed; the working-tree content was the other session's legitimate unpushed work, not something already on origin. Abandoned the reconstruction, verified this session's own edits were still present in the live file via targeted grep instead, and committed the file as it actually stood.
- New tooling discovered mid-session, built by a concurrent session from the same night's "rate the substrate" pass: `scripts/safe_commit.py` (fetch + throwaway worktree + targeted commit + rebase-retry-on-race, automating by hand exactly the safe-commit pattern this session had been improvising) and `scripts/check_budgets.py` (enforces every file's own `**Budget:**` line — not yet CI-wired, so the newly-budgeted pending shards being over budget isn't currently a red build). Worth using `safe_commit.py` going forward instead of raw `git add`/`commit`/`push` on this checkout.
- **This session's own first `/close` attempt would have silently deleted another concurrent session's real, committed work** — a stale local read of `eq/pending/eq-shell.md`/`eq/changelog/eq-shell.md` (missing that session's already-pushed PR #1785 writeup) was caught by diffing against a freshly-checked-out `origin/main` worktree before committing, not after. Redone correctly by re-applying this session's own edits directly against the worktree's true current content instead of a locally-cached copy. Worth a general lesson: when another concurrent session's uncommitted edits are ALSO sitting in the shared checkout (as they were here — a `/triage` pass over eq-shell's waiting-on-you/aging buckets, confirmed legitimate and left untouched), diffing a stale local file against a clean worktree checkout doesn't distinguish "their uncommitted work I should leave alone" from "their committed work I'm about to delete" — only a diff against the actual worktree content catches the second case.
- Substrate: this session's `eq-context` clone was 8→37+ commits behind by the time this close ran. Hit the `stale-main-gate` hook on a direct commit attempt in the shared checkout; used the sanctioned isolated-worktree recovery path (`git worktree add <path> origin/main`), same recipe several other concurrent sessions used successfully throughout tonight per the entries above.

---

# Session 2026-09-07 — EQ Intake reviewed end-to-end; duplicate-scan perf fix + RPC column-projection shipped to eq-shell, both merged

## Built
- eq-solves-intake `main` @ `6e1e2f2` — fixed `duplicate-detect.ts`'s O(n^2) fuzzy-dedup pass rebuilding bigram sets per-pair instead of once-per-candidate. Benchmarked against live data volume (2,854 assets on ehow): ~7.7s -> ~1.3s comparison pass, ~8.7s -> ~1.7s full scan, byte-identical output. Full suite green (164 tests).
- eq-shell [PR #1792](https://github.com/eq-solutions/eq-shell/pull/1792) — re-vendored eq-intake from the fix above, merged, confirmed live via deploy-ancestry (`23c45cbc`).
- eq-shell [PR #1797](https://github.com/eq-solutions/eq-shell/pull/1797) — new migration `0303_tidy_read_entity_columns.sql`, adding a column-projected sibling RPC (`eq_tidy_read_entity_columns`) alongside the existing full-row `eq_tidy_read_entity`. Fleet-wide, all CI green, merged (`da11d54d`).
- eq-solves-intake `main` @ `9abd761` — an earlier draft of that same migration, staged in this repo's own (pre-One-Pipe, now-superseded) `sql/` folder before eq-shell's `SCHEMA-GOVERNANCE.md` was found mid-session. Left in place as a dated sketch; the real migration lives in eq-shell now.
- A full product review of EQ Intake delivered to Royce as a file (not this doc) — traced his "I don't know what it does / it's slow" complaint to two real, distinct causes: (1) the screen he'd been opening is a component test harness sharing its "actual product" banner with the genuinely-real, production-wired copy at eq-shell's `/intake`; (2) local dev-loop slowness was a stale multi-package build dependency, not the dev server itself. Also verified the underlying engine (validation, fuzzy matching, AI layer) independently — genuinely solid, AI used properly (tiered models, human-confirms-everything, injection-hardened prompts).
- Saved a persistent memory: GitHub MCP connector 404s on `eq-solutions/eq-shell` specifically (reads and writes both) — `gh` CLI works fine, same account.

## Decided
- Royce: fix the duplicate-scan perf bug once shown live (screenshotted the freeze himself after checking core.eq.solutions directly).
- Royce: commit -> push -> merge, confirmed explicitly at each step, for both eq-shell PRs (#1792, #1797).
- Royce: fix "the rpc pagination too" — redirected from literal pagination (wouldn't help; all 4 callers need whole-table facts) to column projection instead, scoped to 3 of 4 callers (the 4th, duplicate-detect.ts, needs every column for its completeness heuristic).
- Royce: dispatching `tenant-migrate.yml` to actually apply migration 0303 is explicitly deferred to its own separate decision — not done this session. When it happens, recommended `slug=ehow` for a first run over a blank fleet-wide dispatch.

## Deferred
- **Dispatch `tenant-migrate.yml` for migration 0303** (and the pre-existing, still-open 0302) — no approval gate, fleet-wide by default, Royce's explicit call. See `eq/pending/eq-shell.md`.
- **Wire `health-score.ts` / `licence-expiry-check.ts` / `decay-detect.ts` to the new column-projected RPC** — blocked on the dispatch above.
- **The demo-vs-real confusion itself** — `eq-intake-demo`'s dual-nav "engineering scenarios... not the product" banner and its silently-no-op mock backend are both still live in the local dev harness. Options laid out in the review; no decision made on which (if any) to build.

## Notes
- **Root confusion, in full:** `@eq/intake-demo` serves two roles from one source file — a local component test harness (`pnpm dev`, mock Supabase, mock AI) and the real production module eq-shell mounts at `/intake` (real Supabase, real Anthropic proxy via a server-side proxy, real permission gates, real commit path through `intake-commit.ts`). Both contexts render the identical "One-screen Intake — the actual product" copy, so opening the harness looks exactly like opening the real thing, mock data and all.
- **eq-shell vendors eq-solves-intake, doesn't depend on it live** — `eq-shell/eq-intake/` is a physical copy (`scripts/revendor-intake.mjs`), not a submodule, because eq-solves-intake is private and Netlify can't clone private submodules for the build. Confirmed the gap between them was small before re-vendoring (diffed 4 files byte-for-byte against the vendored copy — exactly 3 commits behind, all already understood).
- **eq-shell's tenant-schema governance is a full separate model** (`SCHEMA-GOVERNANCE.md`, "One Spine, One Pipe, One Guard") that this session hadn't previously needed: eq-intake's own `sql/` folder is the explicitly-deprecated pre-Pipe pattern; the real path is `eq-shell/supabase/tenant-migrations/`, 4-digit numbering, no self-insert ledger row (the runner writes that), optional `-- Plane:` header for single-tenant-only migrations. The actual apply step (`tenant-migrate.yml` `workflow_dispatch`) has **no approval gate** and applies to **every tenant in the fleet immediately** unless scoped via `slug` — confirmed via the workflow file + governance doc, not assumed.
- **Both eq-shell PRs were prepared from isolated `git worktree add` checkouts off `origin/main`**, never the shared primary checkout — its currently-checked-out branch shifted three times over the session (`fix/field-handoff-stall-visibility` -> `fix/pin-reset-platform-admin-escalation` -> `fix/hub-sidebar-fast-refresh-exports`), confirming heavy concurrent activity on that repo throughout.
- **Two background-polling mistakes, both self-corrected:** exited early on a Netlify deploy check by treating `"uploading"` as terminal (it isn't — only `ready`/`error`/`skipped`/`cancelled` are), and separately on a PR mergeability check by treating `mergeStateStatus: "BLOCKED"` as terminal when required checks were merely still running. Both re-run correctly before being reported as done.
- Substrate hygiene: `C:\Projects\eq-context`'s primary checkout carried extensive uncommitted changes across dozens of files (pending/, rules/, changelog/, scripts/) from what's very likely a concurrent session actively building new protocols (`entity-boundary-protocol.md`, `deploy-topology-protocol.md` — both surfaced as new available skills mid-session) — 41 commits behind `origin/main` at close time on top of that. Did this close from a fresh isolated worktree off `origin/main` instead of touching the dirty primary checkout, same F9-avoidance pattern other sessions used earlier today.

---

# Session 2026-09-07 — Two new verification skills built, shipped, and merged: /deploy-topology-verify + /entity-boundary-guard

(This is the session the entry directly above refers to seeing mid-flight — confirming here.)

## Built
- Researched Claude Skills best practices (Anthropic's own engineering blog + the Claude Code team's own "lessons from building Claude Code" post, fetched directly rather than trusted from training data) and recommended `/brief`, `code-review`, `security-review` as the highest-leverage skills already available, tied to their finding that verification skills have "the most measurable impact on Claude's output quality" internally.
- Researched real-world skill examples on request (`obra/superpowers`, a Karpathy-observations-derived behavioral skill) — verified both directly on GitHub after the surrounding search results turned out to be mostly SEO-farm content with fabricated star counts.
- Built and shipped `/deploy-topology-verify` and `/entity-boundary-guard`: trigger files at `~/.claude/commands/`, full protocols at `eq-context/rules/*-protocol.md` (the real house convention, found by reading `/brief`'s own source rather than assumed), both indexed in `CLAUDE.md`'s "Where Things Live" table. eq-context [PR #220](https://github.com/eq-solutions/eq-context/pull/220), merged `617d69a5`.
- Before merging, checked CI rather than merging on a bare "go": fixed the one real failure caused by this change (missing index entries), separately fixed one pre-existing orphan found in passing (`rules/tidy-protocol.md`, from a different concurrent session's already-merged `938e24f2`, with Royce's explicit go-ahead), and confirmed a third failure (3 broken links in this file, added by a different concurrent session) was pre-existing and unrelated before merging past it.
- Live-tested `/deploy-topology-verify` by invoking it directly mid-session — confirmed it loads and behaves exactly as authored.
- Removed the merged worktree + branch on request.
- This close: logged the session in `ops/pending.md` (new top section).

## Decided
- Royce: build both skills now, not just recommend them.
- Royce: push the branch and open the PR (explicit, two-part instruction).
- Royce: "go" — read as merge, given it followed push+PR completing; confirmed a correct read by his continued engagement through the CI back-and-forth that followed, no objection.
- Royce: fix the pre-existing `tidy-protocol.md` index orphan too, then merge (chose this over merging with it unfixed, or holding the PR).
- Royce: merge past the second, confirmed-unrelated CI failure rather than chase a fix into someone else's live file.
- Royce: remove the merged worktree.

## Deferred
- **Durability backup** — the 2 new trigger files aren't yet mirrored into `eq-context/tools/commands/` the way `/brief`/`/gap`/`/decide` are (their own frontmatter calls that copy a backup; source of truth stays `~/.claude/commands/`). Optional, low cost, logged in `ops/pending.md`.
- **Not this session's to fix, left alone**: the 3 broken links in this file noted above — targets confirmed to exist, so it's a path-resolution issue (a bare repo-root-style href written from inside `sessions/` needs a `../` prefix — see `ops/pending.md`'s own working example), not a missing file. Already visible via the `health` CI check; didn't guess-edit someone else's live same-day entries.

## Notes
- The real source of truth for `/brief`/`/gap`/`/decide`/etc. is `~/.claude/commands/*.md` — the eq-context `tools/commands/*.md` copies are explicitly durability backups per their own frontmatter, not canonical. Easy to miss; worth remembering for any future work on these commands.
- The house command convention is two-tier — thin trigger + full protocol file — independently matching Anthropic's own progressive-disclosure guidance for skills.
- eq-context carries its own repo-level `CLAUDE.md` ("Master Behavioural Contract"), distinct from and more elaborate than the global/`C:\Projects` ones. Didn't run its full session-start ritual for this narrow task (disproportionate to a 2-file addition), but respected its hard rules throughout — all of which turned out to be mechanically enforced, not just prose: the brief-gate, the F13 content guard, and the stale-main-gate all fired for real this session and were satisfied properly rather than routed around.
- GitHub MCP integration (`d2708d72...`) 403s on PR-write against `eq-solutions/eq-context` — used the `gh` CLI instead (Royce's own authenticated account, worked cleanly). Same class of gap another concurrent session logged earlier in this same file (the GitHub MCP 404ing on `eq-solutions/eq-shell` specifically) — worth a general lesson that the GitHub MCP's permissions don't reliably match what `gh` can do on the same account.

---

# Session 2026-09-07 (part N) — check_budgets.py wired into CI; hit and fixed a real GitHub permission gap along the way

## Built
- Wired `check_budgets.py` + its self-test into `.github/workflows/md-health.yml`, same pattern as every sibling check there (`prune_ratchet.py`, `review_clock.py`, etc.) -- a comment explaining why, the check step, then the self-test step. Placed right after the "Prune ratchet" steps as the closest thematic sibling.
- Validated the YAML parsed correctly and the new steps landed at the right position (via PyYAML, not just eyeballing) before committing.
- First push attempt was rejected by GitHub itself, not a race: "refusing to allow an OAuth App to create or update workflow `.github/workflows/md-health.yml` without `workflow` scope." Royce independently tried the identical `git push` himself from his own terminal and hit the exact same wall -- confirmed this is a shared, environment-level credential limit (the `gh` CLI's stored OAuth App token for account `Milmlow`), not session-specific.
- Diagnosed via `gh auth status`: token already had full `repo` write access (evidenced by every other push that session succeeding) but was missing the separate `workflow` scope GitHub requires specifically for `.github/workflows/*` changes.
- Presented 3 options; Royce chose to fix the credential properly (`gh auth refresh -h github.com -s workflow`) rather than a one-off web-UI workaround. First device-code attempt expired unused ("context deadline exceeded") before Royce could approve it; second attempt succeeded. Verified the scope actually landed via `gh auth status` before touching anything else.
- Pushed the parked commit (had been sitting safely on a local branch the whole time, never lost) -- hit one ordinary non-fast-forward race on the way (origin moved during the OAuth detour), rebased, retried, pushed clean. Live: eq-context commit `f2eb7c94`.
- Closed the loop in `ops/pending.md`: the "Substrate assessment -> 2 tools built" section had zero open items left, moved wholesale to `ops/pending-archive.md` per the archive rule.

## Decided
- Royce: fix the `workflow` OAuth scope properly (not a one-off web-UI edit) -- approved the device-flow authorization in browser, twice (first code expired unused).

## Deferred
- None new. This closes out the "wire check_budgets.py into CI" item from the previous close, and its own mid-task blocker, in the same session.

## Notes
- **A stray `ScheduleWakeup` call mid-task, self-corrected.** After starting the device-flow auth refresh as a background task, scheduled an additional 90s polling wakeup on top of it -- redundant with (and shorter-interval than recommended for) the background task's own completion notification, which was already the correct signal. Cancelled it (`stop: true`) once noticed, before it could fire, and relied on the background task notification instead, which is what actually surfaced both the first timeout and the second success.
- `MSYS_NO_PATHCONV=1` is required before any `git show <rev>:<path>` in this Bash/Git-Bash environment -- without it, MSYS mangles the `rev:path` colon syntax into a corrupted argument (`origin\main;path\to\file`) and the command fails with a confusing "ambiguous argument" error that looks like a bad revision, not a path-conversion bug. Same root cause as the already-documented `-C C:\...` mangling, different trigger (a colon mid-argument, not a leading drive letter).
- Substrate hygiene: root checkout was 53-54 commits behind `origin/main` by the end of this chapter (up from 8 at the start of the overall session). This close's writes went through the same isolated-worktree pattern as every other write tonight.

---

# Session 2026-09-07 — eq-shell customer activation pill: found and fixed a real live-update bug, shipped to production

## Built
- Diagnosed and fixed a real bug in `CustomersPage.tsx`: the customer-level Field/Service pill only updated on a full page refresh when toggling a site directly, not immediately. Fixed `toggleSiteActivation` to recompute the customer rollup client-side (mirroring `crm-customers.ts`), covering both the optimistic-apply and error-revert paths.
- Also fixed a related TS gap: `DetailSite` was missing `active`, even though the backend already sends it.
- Shipped eq-shell [PR #1799](https://github.com/eq-solutions/eq-shell/pull/1799): typecheck/lint clean, CI green, merged (squash `c290dea7`), confirmed live on core.eq.solutions via deploy-ancestry — not just a green deploy, since 3 unrelated production deploys had errored earlier the same evening.

## Decided
- Royce: fix the toggle-live-update bug only, not the separate URL-routing gap (customer pages losing their place on refresh) — scoped as its own future decision.
- Royce: merge PR #1799, after being told explicitly that merging this repo's `main` is the deploy.

## Deferred
- Not click-tested live by a person (PR #1799) — no Shell session/credentials in this environment. Logged in `eq/pending/eq-shell.md`.
- The URL-routing gap itself (customer/staff/etc. detail pages have no route of their own, so a refresh always drops back to the list) — diagnosed, not built. Worth checking whether other record pages (Staff, Plant & equipment) have the same gap before scoping a fix.

## Notes
- **A live screenshot from Royce was the actual trigger for finding this bug.** Asked to explain the PR #1700 mechanism with pictures; he then shared a real customer page where the picture's prediction didn't match what he was seeing. That mismatch — not a click-test, not a code review — is what surfaced the fix.
- **Deploy verification did real work here, not just ceremony.** Netlify's `currentDeploy` briefly pointed at a different, earlier PR's (#1798) deploy while mine was still building — exactly the concurrent-merge trap `rules/deployment.md` warns about. Also found 3 production deploys had errored earlier the same evening (unrelated to this change) while checking. Waited for my commit's own deploy id to reach `ready`, then confirmed via `git merge-base --is-ancestor` against `origin/main` before calling it live.
- **`jq` is not available in this Bash/Git-Bash environment.** A Monitor script built around it (polling `gh pr checks`) ran silently ineffective for its full 10-minute timeout without ever detecting CI had actually finished ~7 minutes early. Re-verified directly via plain `gh pr checks`/`gh pr view` instead; the second wait (polling the Netlify deploy directly) used grep-only parsing and worked correctly. Worth remembering for any future polling script in this environment.
- Session tier: EQ (eq-shell).

---

# Session 2026-09-07 — sks-nsw-labour retirement executed; found and flagged a live credential exposure into core.eq.solutions, Royce declined to act on it this session

## Built
- Exported `nspbmirochztcjijmcrx` (sks-nsw-labour's DB): 43 of 44 public-schema tables, anon-key REST (data only, not a pg_dump). Delivered to Royce as a zip, twice — an initial 27-table pass, then closed the gap after the Supabase MCP's `list_tables` revealed 44 real tables, not 27.
- Closed PR #64 (stale docs checklist) on `eq-solutions/sks-nsw-labour`.
- Archived the `eq-solutions/sks-nsw-labour` GitHub repo.

## Decided (Royce)
- Redirect target for any future SKS-facing messaging from this codebase is `core.eq.solutions/sks/field` — not the bare `field.eq.solutions` standalone gate, which can't authenticate the `sks` tenant. No new redirect built; the in-app banner/modal/force-logout that already shipped 2026-09-01/02 (v3.10.112–114) was confirmed sufficient.
- Supabase project `nspbmirochztcjijmcrx` left **active** on the Pro plan — not paused, not deleted. Pausing a Pro-org project isn't a toggle: confirmed via the Management API's own error ("Project is not free-tier") and Supabase's docs that it requires transferring the project to a separate Free-plan organization. Royce chose the small ongoing Pro cost over that transfer.
- PR #34 (Stage-1 of a 4-stage anon-CRUD lockdown fix for `nspbmir`) stays closed. Royce, verbatim: "yes - i want to stop spending time on sks nsw labour" — given after being walked through exactly what staying closed means.

## Deferred
- The `nspbmir` anon-CRUD/secrets-exposure vulnerability (first found 2026-07-20) is still fully open. Royce explicitly declined to pursue any part of it, including the isolated, zero-risk "Step 0" patch. Full detail recorded in `sks/pending.md`'s top section — not duplicated here.

## Notes
- **A real mistake, corrected mid-session:** closed PR #34 on the assumption that pausing the Supabase project would make it moot. Pausing never happened, so the assumption was wrong. Tried to reopen the PR — blocked, because the repo had also been archived (archived repos are read-only on GitHub). Tried to un-archive to fix that — blocked by the user's own Claude Code permission classifier. That block was correct; stopped and asked rather than working around it. Net result, by Royce's own explicit choice: repo stays archived, PR #34 stays closed, vulnerability stays open.
- Before assuming any of this still mattered post-retirement, verified specifically rather than asserting: one of the two secrets exposed via `app_config`'s wide-open anon SELECT — `canonical_api_key_field` — is a bearer token for `core.eq.solutions/.netlify/functions/canonical-api`, i.e. the **active** Shell/Field system, not the retiring app. `digest_fn_token` is seeded as a raw Supabase service-role JWT for `nspbmir` itself. Both are readable today by anyone with the still-public anon key baked into the still-served JS at `sks-nsw-labour.netlify.app`, independent of the app's retirement status. Gave Royce this precise picture; he made an informed call to decline action.
- Also found, and checked before raising it as a concern: `nspbmir` hosts a complete but **entirely empty** 13-table `sks_quotes*` CRM schema plus a few other unused tables (`site_diaries`, `schedule_archive`) — verified row counts on all of them (zero) before treating it as a live-system risk.
- There's an existing but stale automated weekly backup (`workers/supabase-backup` → Cloudflare R2 bucket `sks-assets`, cron Wednesdays) covering only 17 of the 44 real tables — informational, not actioned.
- `eq-guard`'s `block-sks-live` rule fired correctly multiple times this session. Learned it has no in-session bypass — `EQ_ALLOW_SKS_LIVE` has to be set in the harness's own environment before launch, not via an inline Bash prefix, since the guard hook runs as a separate process before the tool call executes. Worked within that instead of fighting it: read the project URL/key from `scripts/app-state.js` at runtime, mirroring this repo's own `bootstrap-smoke.mjs` pattern.

---

# Session 2026-09-07 — Correction: no durability-backup gap for the two new skills

Follow-up to this file's earlier "Two new verification skills built, shipped, and merged" entry
(same day). Royce asked to mirror `/deploy-topology-verify` and `/entity-boundary-guard` into
`eq-context/tools/commands/` to close the "durability backup gap" that entry's own Deferred
section logged. Reading `tools/commands/README.md` first (should have been read before writing
that entry, not after) showed the gap was never real: that README documents only commands whose
*entire* logic lives in the command file itself get backed up there (`/brief`, `/close`,
`/housekeep`); a thin trigger over a tracked `rules/*-protocol.md` file (`/decide`, `/gap`,
`/reflect`, `/tidy`) is already safe without a copy. Both new commands are built in exactly that
second shape — no gap, copying would only have created a driftable duplicate.

## Built
- Added `/deploy-topology-verify` and `/entity-boundary-guard` to `tools/commands/README.md`'s
  "already safe" table instead of copying them.
- Corrected the earlier same-day entry in `ops/pending.md` and rotated the whole section to
  `ops/pending-archive.md` (zero open items left once the correction was made, per this file's
  own archive rule).

## Decided
- Royce, given the choice: skip the copy, fix the record — not copy anyway for redundancy.

## Notes
- Second time this same day a claim in this file needed a same-day correction (see the
  `sessions/2026-09-07.md` broken-links note earlier) — worth remembering to check a folder's own
  README before describing what it does or doesn't contain, rather than pattern-matching from a
  sibling file alone.
---

# Session 2026-09-07 (continued) — eq-shell #981 (fast-uri) closed: bumped past 4 newer SSRF/host-confusion advisories, merged and confirmed live

## Built
- eq-shell [PR #1800](https://github.com/eq-solutions/eq-shell/pull/1800) — `fast-uri` bumped in both places it actually lives: root `pnpm.overrides` (4.1.2 → 4.1.4) and the vendored `eq-intake/eq-platform` subtree's own separate override (3.1.4 → 3.1.7, within its existing `<4.0.0` ceiling). Closes #981. Merged, confirmed live — this one's own merge commit built and published directly, no supersession needed.

## Decided
- Royce: "yes, go ahead" on fixing #981 now rather than deferring it further.
- Royce: "merge #1800" once the PR was up and its CI had passed.

## Deferred
- Nothing new — this closed out #981 cleanly. The pre-existing #1290 risk (vendored lockfile could be silently reverted by the next auto-revendor run unless the same fix lands upstream in eq-solves-intake) still applies, same as noted when #981 was first found; not solved here, not a new item.

## Notes
- **Patched versions confirmed, not guessed**: pulled `first_patched_version` directly from GitHub's `dependabot/alerts` API for all 4 GHSAs before touching anything (4.1.3 floor for the root's 4.x line, 3.1.6 for the vendored subtree's separate 3.x line), then picked the latest published patch on each line (4.1.4 / 3.1.7) via `npm view fast-uri versions`.
- **Confirmed empirically, not assumed, that the vendored `eq-intake/eq-platform/pnpm-lock.yaml` is a genuinely separate pnpm workspace** — its own `pnpm-workspace.yaml`/`package.json`/lockfile, vendored wholesale from the private `eq-solves-intake` repo. Root's own `pnpm-workspace.yaml` claims `eq-intake/eq-platform/packages/*` directly as workspace members, so root's single lockfile is what actually governs runtime resolution for every `@eq/*` package — verified after `pnpm install` that root's lockfile has exactly one `fast-uri` entry (4.1.4) across the whole tree. This means the nested lockfile's own flagged vulnerability is very likely inert for what actually ships in production; fixed it anyway (Dependabot tracks it regardless, and it's real vendored content).
- **A real, if minor, false alarm caught before it derailed anything**: a bare `pnpm run lint` in the fix's worktree returned 330 problems (187 errors) — alarming at first glance, until tracing showed every one was in files this fix never touched, matching another concurrent session's own in-progress branch name (`fix/hub-sidebar-fast-refresh-exports`). Logically impossible for a lockfile-only change to cause React-hooks lint errors in unrelated `.tsx` files. CI's own required `typecheck · test · lint` check confirmed this — passed clean.
- **Same shared-checkout collision class as the previous close in this same session, hit again**: `C:\Projects\eq-shell`'s main working directory was on a different branch with its own uncommitted changes (someone else's `fix/hub-sidebar-fast-refresh-exports` work) when this fix started, so all of it — edit, install, build, test, commit, push — was done from a fresh isolated worktree instead, same recipe as `eq-context`'s own F9 guard teaches. Also hit a Windows long-path error tearing the worktree down afterward (pnpm's `node_modules/.pnpm` tree nests deep) — resolved with `cmd /c rmdir /S /Q "\\?\<path>"` rather than leaving it orphaned.
- **`gh pr create` picked up the wrong branch on the first attempt** — auto-detected from the shared checkout's current branch (not the isolated worktree's pushed branch) because this tool's shell cwd resets between calls. Fixed by passing `--head <branch>` explicitly rather than relying on cwd-based auto-detection; worth remembering for any future PR created from an isolated worktree in this same environment.
- Substrate: `eq-context` drifted from ~41 to 62+ commits behind `origin/main` over the course of this continuation — consistent with the rest of tonight's exceptionally heavy concurrent-session volume. Diffed this close's own 3 files against a freshly-fetched worktree before writing anything (not a locally-cached copy), per the lesson recorded in the previous close's own Notes section — no clobber risk this time.

---

# Session 2026-09-07 (continued) — Real reporting-line ("Manager") field shipped for Staff; restricted to Royce only per his own request

## Built
- eq-shell [PR #1796](https://github.com/eq-solutions/eq-shell/pull/1796) — new `manager_id` self-referencing column on `app_data.staff` (migration `0303`, ehow/SKS only), a Manager field (view + edit) on the Staff page (desktop `SplitPanel.tsx`, mobile `MobileSheet`), validated server-side in `entity-patch.ts` (no self-reference, no cross-tenant assignment). Merged, migration dispatched to ehow via `tenant-migrate.yml`, confirmed live (column + FK + index exist).
- eq-shell [PR #1803](https://github.com/eq-solutions/eq-shell/pull/1803) — new permission `staff.manage_reporting_line` (Shell-local, group-only, no role default — `src/permissions/matrix.ts` `STAFF_PERMS`), gating the Manager field's view+edit client-side (`SplitPanel.tsx`, `StaffPage.tsx`'s `MobileSheet`) and the `manager_id` write server-side (`entity-patch.ts`). Wired into `AccessControlPage.tsx`'s Custom Groups picker. Granted via a new "Reporting Lines" security group on jvkn (`shell_control`), sole member Royce. Merged, confirmed live via exact commit-ref match on the newest production deploy plus ancestry against `origin/main`.
- Along the way: fixed `scripts/check-orphan-perms.mjs`'s required CI gate — its hand-kept `SHELL_LOCAL_PERMS` mirror list (it can't cheaply import `matrix.ts`) didn't know about the new permission key yet, so the live grant tripped "Schema drift + anon-grant + policy-lint." Added the key there too, plus a pointer in `matrix.ts`'s own doc comment so the next Shell-local permission doesn't repeat the miss.
- `scripts/import-sks-manager-lines.mjs` — backfill script skeleton, double-gated dry-run/`--apply`, reusing the identity-bridge resolver from `etl-nspbmir-to-ehow.mjs`. Not run — blocked on Royce's real export file.

## Decided
- Royce (via AskUserQuestion): import real reporting lines from his own SKS org-chart tool, rather than repurposing the existing `app_data.teams` crew-grouping concept — direct continuation of the want flagged twice before (2026-08-30, `sks/pending.md` HR folder audit) and never actioned.
- Royce: "Merge #1796", then separately "Dispatch the migration to ehow" — treated as two distinct gated actions per this repo's own governance (merge ≠ apply).
- Royce: "only royce milmlow should be able to see this feature for now... i dont want anyone seeing this and thinking its finished and getting cranky" — read as: literally Royce alone (not the existing "Staff Conversations" group, which also covers a second, dormant account), and explicitly temporary pending the backfill.
- Royce: "merge #1803 when safe / when green" — merged once CI was fully green, with a fresh mergeability + ancestry check immediately beforehand given the exceptionally heavy concurrent-merge volume tonight.

## Deferred
- The actual bulk backfill — genuinely blocked on Royce supplying a real JSON export from `SKS_NSW_Org_Chart_Interactive.html`'s own Export function; the script's `parseExport()` is provisional until a real file is seen.
- Widening `staff.manage_reporting_line` beyond Royce — deliberately not decided yet; revisit once the backfill lands.
- Not click-tested live by a person — verified via full build/test/lint + live DB state + production deploy-ancestry only.

## Notes
- Read `entity-rows.ts`'s actual Staff-page read-path authorization (not the RESTRICTIVE RLS policy, which only governs direct PostgREST access) before treating `manager_id`/a resolved manager name as safe to show unredacted to any `field.view` holder — confirmed it's organizational metadata, same category as `job_title`, not PII.
- The original PR #1796 branch/worktree was stale by the time the Royce-only restriction work started (its one commit had already landed via squash-merge, and `origin/main` had moved 10 commits further, including two other rewrites of the same Staff-page files) — branched fresh off current `origin/main` rather than rebase, to avoid reintroducing already-merged content.
- Left `sks/pending.md`'s "HR folder audit (2026-08-30)" section otherwise untouched (only the one org-chart bullet updated) — it mixes free-standing narrative with several unrelated open items from that earlier session; per this file's own archive-rule fallback, not guessing at how to trim a mixed section it wasn't this session's job to fully re-litigate.
- Substrate: shared `eq-context` checkout was 73 commits behind `origin/main` by close time, with all four target files (`eq/pending/eq-shell.md`, `sks/pending.md`, `sessions/2026-09-07.md`, `eq/changelog/eq-shell.md`) independently touched by other concurrent sessions since first read. Resynced each to `origin/main`'s current content before editing (not the earlier, now-stale local read), then pushed via `scripts/safe_commit.py` rather than a direct `git add`/`commit`/`push` on the shared checkout.

---

# Session 2026-09-07 (continued) — 3 of 5 Dependabot alerts closed on eq-shell

## Built
- eq-shell [PR #1805](https://github.com/eq-solutions/eq-shell/pull/1805) — `pnpm.overrides` for `browserslist` (`^4.28.7`, closes alerts #206/#207) and `posthog-js>fflate` (`^0.4.9`, closes #201, scoped so it doesn't touch the already-safe `fflate@0.8.3` used by `@react-pdf/renderer`). Built and verified (`pnpm install` + `pnpm run build`) in an isolated worktree, not the shared main checkout — its branch and uncommitted files changed three times in ~5 minutes with no action from this session, before that worktree existed.
- eq-shell [PR #1803](https://github.com/eq-solutions/eq-shell/pull/1803) (orphan-perm fix, `staff.manage_reporting_line`) — merged by something else mid-audit, not this session. A merge-readiness agent dispatched to check it "if safe" returned a retrospective clean bill (all 5 required checks passed, diff scope matched the title exactly, fail-closed permission gate, narrowing not widening) — nobody here clicked merge.
- `task_bb2405d7` spawned (separate session, still running at close) — bump `browserslist` in `eq-solves-intake` itself, the actual root cause of alerts #204/#205 (eq-shell's vendored `eq-intake/eq-platform` copy just mirrors upstream).

## Decided
- Royce: "merge 1803 if safe" → by the time the audit finished, it was already merged by a concurrent actor; reported that plainly rather than claiming the action.
- Royce: "merge 1805" → blocked on the very check #1803 had just fixed (this branch was cut before #1803 existed). Resolved with `gh pr update-branch` rather than a bare re-run, then merged once fresh CI was fully green.
- Royce: "merge it once green" with no PR number → asked rather than guessed, given 6 unrelated open PRs and an auto-deploying repo. Royce had no specific PR in mind; nothing was merged on that instruction.

## Deferred
- Alerts #204/#205 (vendored-copy browserslist) — blocked on `eq-solves-intake` itself still being unpatched. See `eq/pending/eq-shell.md` and `task_bb2405d7`.

## Notes
- **`jq` is not installed in this environment's Bash tool shell.** A `Monitor` poll loop built around it failed silently and *permanently* — retrying every 30s forever rather than erroring loudly — until diagnosed by running the same command directly. Rebuilt on `gh`'s own bundled `--jq` flag instead of piping to an external `jq` binary. Worth remembering for any future poll/`Monitor` script here.
- Independently hit the same shared-eq-shell-checkout race this file's previous session already described, before reading that description — same isolated-worktree fix, arrived at separately.
- First use by this session of `scripts/safe_commit.py` for this close — found only by reading the *previous* session's own Notes section, not from any pointer in `tools/commands/close.md` or `brief.md`. Read the script in full before trusting it. Worth those two command files linking to it directly so a session that closes without reading prior sessions' logs can still find it.

---

# Session 2026-09-07 (continued) — RPC column-projection dispatched to SKS + client wiring shipped; full delivery chain closed end-to-end

## Built
- Migration `0303_tidy_read_entity_columns.sql` renamed to `0304` — a concurrent PR ([#1806](https://github.com/eq-solutions/eq-shell/pull/1806)) had independently claimed `0303` for an unrelated same-day migration (`0303_staff_manager_id.sql`, the reporting-line feature). Confirmed safe before treating it as routine: both sides' SQL idempotent, live function unaffected by the filename-only rename.
- Dispatched `tenant-migrate.yml` (`workflow_dispatch`, `slug=ehow`) — applied migration 0304 to the SKS tenant only. Verified live independently of the dispatch API's own success report: direct `execute_sql` against ehow confirmed both the `app_data._eq_migrations` ledger row and the `pg_proc` catalog entry for `eq_tidy_read_entity_columns` with `authenticated` EXECUTE granted.
- eq-solves-intake `main` @ `81bd49a` — new `read-entity-columns.ts` helper (calls the column-projected RPC, falls back to the original full-row RPC on error) wired into all 3 callers that can use it: `health-score.ts` (columns derived from `FIELD_IMPORTANCE` + validity-check fields + `updated_at`), `decay-detect.ts` (PK + label fields + `active` + `updated_at`), `licence-expiry-check.ts` (the 4 fields it actually reads). `duplicate-detect.ts` deliberately left on the full-row RPC — its completeness tie-break needs every column.
- eq-shell [PR #1804](https://github.com/eq-solutions/eq-shell/pull/1804) — re-vendored the above via `scripts/revendor-intake.mjs`. Squash-merged `8520fdc6`. Confirmed live via direct Netlify API check (not assumed from merge success): `state: ready`, `published_at: 2026-09-07T11:50:19Z`, `commit_ref` exact match.

## Decided
- Royce: "yes, wire them up" — wire the 3 safe callers to the projected RPC (duplicate-detect stays on the full-row RPC).
- Royce: "yes, do it" — re-vendor and open the PR for that wiring.
- Royce: "merge it once it's clean" — standing instruction to merge PR #1804 the moment CI cleared, no further check-in required.
- Royce, when offered `--admin` to bypass a failing branch-protection check on PR #1804: declined — chose to wait for a genuinely clean run instead ("wait for it to clear (--auto)").

## Deferred
- Dispatching migration 0304 to the EQ/zaap tenant — not requested. zaap's callers stay on the fallback path (original full-row RPC) indefinitely unless revisited.
- The eq-intake-demo dual-nav "not the product" confusion — unchanged since the original review; no decision made on the 3 options laid out there.
- Not click-tested live by a person — same environment limitation as the rest of tonight's eq-shell work; verified via build/test/lint, live DB queries, and deploy-ancestry only.

## Notes
- **PR #1804's own CI hiccup, diagnosed and fixed rather than bypassed.** Its branch was cut before [PR #1803](https://github.com/eq-solutions/eq-shell/pull/1803)'s permission-key fix landed on `main`, so the required "Schema drift + anon-grant + policy-lint" check (which evaluates live state, not the branch's own diff) failed on a check unrelated to this change. `gh run rerun --failed` doesn't help here — it replays the original recorded merge-ref, not a fresh merge against current `main` — so the fix was to merge fresh `main` into the branch and push (`30b8c655`), which then passed clean on a genuinely new run.
- **This closes the full delivery chain** from the original "every button takes an eternity" complaint: duplicate-scan O(n²) fix (`6e1e2f2`) → eq-shell PR #1792 (merged, live) → column-projected RPC (PR #1797, migration renamed 0303→0304, dispatched to SKS, live) → client wiring (`81bd49a`) → eq-shell PR #1804 (merged, live). Both halves of the original slowness diagnosis (the fuzzy-match algorithm and the un-projected RPC payload) are now fixed and confirmed live.
- Substrate hygiene: this close found the shared `eq-context` primary checkout 98 commits behind `origin/main`, carrying substantial uncommitted work from other concurrent sessions across ~30 files (pending shards, rules, scripts) — left entirely untouched. Reused this session's own earlier close worktree (`C:\Projects\eq-context-wt-close`, already at a pushed, fully-merged commit) rather than the primary checkout or a fresh clone — fast-forwarded it to `origin/main` first (safe: clean tree, no unpushed unique commits) before writing anything.
