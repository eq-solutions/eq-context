# Session 2026-09-07 — Labour-hire licence-photo fix re-verified live; full roster audited, no other worker exposed

## Built
- No code changes this session — verification only.

## Decided
- None — informational/verification session, nothing came up that needed Royce's call.

## Deferred
- Whether the separate 2026-09-02 stale-session wallet fix actually resolved Conor/Nelson's "Sign in again" symptom remains unconfirmed — this session checked a different bug (photo-linking) and doesn't touch that question. Still needs either Sentry access or Royce asking them directly.
- 3 claimed labour-hire accounts (Shihab Al-Gburi, Ali Alsalman, Amir Farid) have a completely empty wallet — no licence ever captured for them, by any method. Not a bug, just worth knowing if Royce expects tickets already on file for them.

## Notes
- Royce asked (1) whether labour-hire workers can see licences uploaded for them via Core, using Nelson Sareto/Conor Horgan as the example, and whether "this fix" was finished; then (2) to check the rest of the labour-hire roster too.
- Traced "the fix" to eq-cards migration `0161_promote_labour_hire_photo_on_claim.sql` (2026-08-25/26): `eq_cards_claim_invite`'s promotion loop copied a staged `worker_credentials` row into `public.licences` but never extracted `metadata.source_document_url` into `photo_front_url`/`document_url` — the ticket showed, the photo behind it didn't. The migration's own comment names Conor Horgan and Nelson Sareto as the real accounts this was live-traced against on 2026-08-26. The identical gap existed independently in eq-shell's `shell-join-tenant.ts` and `accept-invite.ts` (ported the same promotion loop rather than sharing it) — closed there via PR #1603 (2026-08-26), both now calling the same `promote-labour-hire-photo` edge function eq-cards' fix introduced.
- Verified live against jvkn (eq-canonical), not just read from migration/PR comments: `list_migrations` confirms `0161` applied 2026-08-25; a direct SQL query shows both Conor's and Nelson's 4 licences each (8 total — matching the migration's own "8 already-promoted-but-photo-less licences" backfill count exactly) now carry a `photo_front_url` or `document_url`. Zero gaps.
- Confirmed how "via Core" actually works end-to-end: Shell doesn't render the wallet itself — it mints an auth session and iframes the Cards Flutter app at `core.eq.solutions/:tenant/cards` (`CardsIframe.tsx`), reached from a generic "My Card" tile on `WorkerHome.tsx` with no role gate excluding labour-hire. The `licences_read` RLS policy (`user_id = auth.uid() OR ...`) always lets an owner read their own rows regardless of the separate employer-visibility `sharing_scope` mechanism (migration `0128`) — that mechanism was never the blocker here, it only ever governed what an *employer admin* can see.
- Widened the check on request: queried every worker tagged `labour_hire` (either `public.workers.role` or `shell_control.users.role`, 20 total). Only Conor and Nelson have ever had a `worker_credentials` row at all — the labour-hire-candidate-intake photo pipeline has only ever been run for these two, live, so nobody else was ever exposed to this specific bug. Of the other 18: 15 haven't claimed an account yet (normal pre-onboarding, already covered by the existing "chase unclaimed invites" admin alert, eq-shell #1574); 3 have claimed accounts but a genuinely empty wallet (nothing ever captured for them, not data loss); 2 (Richard Brooks, Zemi Asri) have licences via a different path (self-scan in Cards) and were never at risk.
- **Substrate cross-reference found mid-close**: `eq/pending/eq-cards.md` already carried an open 2026-09-02 item ("Wallet can't load licences") about a *different* bug affecting these exact same two workers — a stale client-side Supabase session causing a "Sign in again" error, fixed by PR #342 (session-refresh + wallet-nudge consolidation) but explicitly left unconfirmed end-to-end at that session's own close. This session's photo-fix verification is a different mechanism and doesn't close that question — cross-referenced both entries so the connection isn't lost, but deliberately did not tick that item's checkbox, since its specific question (does the Sept-2 fix actually hold) is still open.
- **Substrate hygiene**: the `SessionStart:resume` hook's "SYNC ok — HEAD == origin/main" banner had already gone stale by the time this close ran — 2 nightly-bot commits landed on origin in between. Caught via `git fetch` + `log HEAD..origin/main` before writing anything, not assumed from the banner alone. Also hit the shared-checkout `git merge` guard (F9) on `C:\Projects\eq-context` as expected for this repo — did all substrate writes in a fresh isolated clone instead of working around the block, per the hook's own recipe.
