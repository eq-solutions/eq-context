# Session 2026-07-17 — AI brief's quote signals were silently zero for SKS; realigned to the live enum, guarded, shipped live

## Built
- **eq-shell `netlify/functions/_shared/briefing-engine.ts` → `fetchQuoteSignals()`** realigned to the live quote status enum. All four signals were querying literals that no live plane uses. **PR #882 MERGED** (squash `332d426`) → Netlify deploy `6a58ecde…` state `ready`, live on core.eq.solutions, smoke-verified.
- **New `netlify/functions/_shared/quote-signal-status.ts`** — three status sets (`CLIENT_PENDING_STATUSES`, `READY_TO_INVOICE_STATUSES`, `WON_NEEDS_JOB_NO_STATUSES`) as a curated projection of `src/modules/quotes/taxonomy.ts`. Declared in `_shared/` (not imported from `src/`) so the Netlify function bundle stays independent of the React tree.
- **New `netlify/functions/_shared/quote-signal-status.test.ts`** — guard test cross-checking every literal against taxonomy `QUOTE_STATUSES` + pipeline groups. Cross-tree import (`../../../src/modules/quotes/taxonomy`) works: the test runner globs both trees via `tsx/esm`, and taxonomy.ts is a dependency-free leaf. First `_shared` test to import from `src/`.

## Decided
- **`active` is a WON job, not an open quote — the task brief was wrong on this, and the live data won.** Brief said key expiring/stale off `active`/`client-reviewing`; live SKS shows 14 of 15 `active` rows carry a `job_number`. Using `active` would have flagged won jobs as "quote expiring / no client response in 14d". Used taxonomy `OPEN_PIPELINE` (`draft`/`submitted`/`client-reviewing`/`on-hold`) instead. Flagged to Royce for veto; he merged it.
- **Royce authorised the production deploy explicitly** ("merge" → then "go ahead and merge" after the deploy consequence was named). Squash-merge to main auto-deploys core.eq.solutions.
- **`ready_to_invoice` = `complete` + `ready-to-invoice`** (the board's "Complete" stage), not `complete` alone — `ready-to-invoice` is unused today but kept for forward-compat.

## Deferred
- **Eyeball the next SKS morning brief once signed in** — query logic verified via SQL against live data and deploy smoke-verified, but the authed brief output needs a signed-in SKS session (10-min per-user cache, or the daily scheduled email). Nothing blocking.

## Notes
- **Live quote enum (ehow, verified 2026-07-16/17).** Both planes carry an IDENTICAL 16-value `quote_status_check` constraint — it's a superset and therefore useless as a rename guard. SKS only uses: `draft`, `active`, `complete`, `invoiced`, `won-job-created`, `verbal-win`, `client-reviewing`. Unused-but-allowed: `submitted`, `on-hold`, `won-awaiting-job-no`, `ready-to-invoice`, `po-matched`. zaap (EQ) has **0 quote rows** — Quotes isn't live there — so the same literals are correct on both by construction (they derive from the shared taxonomy).
- **`draft` is SKS's de-facto "sent, awaiting client" state** — 18 of 20 live drafts carry `sent_at` and all 20 have a future `expires_at`. That's why `draft` belongs in the expiring/stale set; the `sent_at`/`expires_at` filters do the real gating.
- **Live effect of the fix:** ready_to_invoice 0 → 14 rows unbilled, verbal_wins_no_job 1 → 3, stale_submitted 2 → 10, expiring_soon 0 → 0 (correct — nothing falls inside the 7-day window right now; nearest expiry is 8 days out).
- **GOTCHA — stale `node_modules` produces 5 fake PermKey errors.** `pnpm run build` failed on `ops.manage_rates` / `entity.manage_activation` not assignable to `PermKey` in `labour-hire-*.ts` / `*-data-activation.ts`. NOT a main problem: `@eq-solutions/roles` is a **pinned github dep, not a workspace package**, so `build:packages` does NOT rebuild it. Installed copy was v2.5.0 (keys absent) while package.json pins v2.5.1 (keys present). Fix: `pnpm install --frozen-lockfile`. Netlify does a fresh install so main deploys green. If you see PermKey errors on those files, check the installed roles version FIRST before assuming main is broken.
- **Shared-checkout race, live instance.** Worked on `claude/perf-offline-review-05a559`; a concurrent perf agent committed its WIP as `a2a649a` (Core Web Vitals + unsaved-changes guard) **between turns**, so HEAD advanced underneath and `git switch -c` cut the new branch on top of the perf commit. Caught via reflog; `git rebase --onto 37b805a a2a649a` dropped it cleanly (no file overlap). Later rebased again onto current `origin/main` (which had moved to `71fac54`). **Lesson: after a turn boundary in a shared checkout, re-verify HEAD before branching — confirm the new branch's parent is main, not a sibling's fresh commit.**
- **Don't put live financial figures in commit messages / PR bodies.** The first commit message quoted the actual unbilled dollar total; the push was correctly blocked for publishing a client's revenue figure to the repo. Amended to keep the technical rationale without the numbers. Same rule applied to the PR body.
- The brief-engine bug class is a repeat: see the 2026-07-08 session (AI briefing's SKS pipeline fast-path silently dead). Both were "signal silently returns nothing for SKS, nobody notices". The new guard test is the first structural defence against it.

---

# Session 2026-07-17 — Core dashboard rebuilt: three permission-gated live signal bands replace the passive Activity/Upcoming columns

## Built
- **Explored the current dashboard end-to-end** (`TenantHome.tsx`, `tenant-dashboard.ts`, `ai-briefing.ts`, `briefing-engine.ts`) — root cause of the weak Activity/Upcoming columns: the briefing engine already computes a rich cross-app picture every load (licences, incidents, service/calibration due, quote signals, crew capacity) and then compresses it all into a 3-sentence Haiku paragraph, discarding the structured data. Activity/Upcoming are the leftovers.
- **Two rounds of interactive concept mockups** (published as artifacts) exploring how to surface the discarded signals — first a static "money + crew + compliance" board, then an interactive live-ops version (ticking clocks, a capacity what-if toggle, a revenue-lifecycle pipe) after steelmanning the direction and proposing four cross-app signal combinations on request.
- **Scope narrowed on Royce's explicit calls, in sequence:** security-group filtering required from the start → v1 = by-feature (not region, not per-group custom tiles) → focus shifted to compliance/service/operations, away from pipeline/dollar figures ("this currently isn't the home of all commercials, it'll never be fully correct") → landed on a governing principle: **a signal is only as loud as canonical's authority over the thing it describes.** Money survives only as one gated, dollar-free workflow nudge (deferred, not built).
- **Build spec written and committed to `eq-context`**: `dashboard-signals-build-plan-2026-07-16.md` — band definitions, gate keys, live-schema facts, sequencing. Verified against live schema before writing it: SKS's `app_data.jobs` has no crew-requirement column and `tenders`/`tender_nominations` don't exist on that plane (EQ-only); the real quote board is 5 stages (`STATUS_FILTERS` in `QuotesModule.tsx`), not the raw 16-value status column — caught and corrected before it shaped the endpoint.
- **Phase 0 ("start Phase 0 in a fresh worktree") turned out to already be built on `main`** — `netlify/functions/_shared/permissions.ts`'s `can()`/`requirePerm()` predates this session, complete with tests. Caught via a live-code check (Rule 0.5) before writing a duplicate. The quote-status single-source-of-truth half of Phase 0 was a *concurrent* session's work (the `task_2f107212` background task spawned earlier this session — see the entry above, merged as PR #882).
- **Phase 1 built in a fresh worktree** (`eq-shell-signals-wt`, branch `claude/dashboard-signals`):
  - `netlify/functions/signals.ts` — new GET endpoint. Builds a `Principal` from the session and gates each band server-side with `can()`: a band the caller can't see is never queried or serialized, not UI-hidden.
  - `_shared/signals-data.ts` — self-contained fetchers (deliberately not touching `briefing-engine.ts`, to avoid colliding with the concurrent quote-status work): **Compliance** (licences expiring, open incidents, and the cross-app "rostered but licence lapsed" join — a lapsed licence only matters because the person is rostered, and neither Field nor Cards knows that alone); **Outstanding works** (service/calibration due, open defects); **Deployable crew** (waterfall: active − rostered − on leave − licence-lapses-in-window, this-week/2-week toggle).
  - `SignalsBoard.tsx` + `signals.css` — self-fetching board, renders only the bands present in the response, honest empty states, EQ-token styling.
  - `TenantHome.tsx` — swapped the weak Activity/Upcoming columns for `<SignalsBoard/>`, removed the now-dead feed-animation subsystem (net −163 lines there).
  - 5 unit tests on the crew waterfall set-math.
- **PR #886 opened, rebased twice onto a moving `main`** (concurrent sessions pushed #882 and #884 mid-review), rebuilt clean each time, all CI green (typecheck/test/lint, schema drift, migration hygiene, Netlify deploy-preview).
- **Merged** (squash `13a6386`) on Royce's explicit "merge it" → core.eq.solutions auto-deploying. **Confirmed live** via direct Netlify MCP query: deploy `6a592f6`, `commit_ref` `13a6386` matches the merge commit exactly, `state: ready`.
- **Cleaned up on request** — worktree removed, local + remote branch deleted, `eq-context` worktree registry updated to DONE.
- **Smoke-tested what's verifiable without Royce's credentials:** the live `/signals` endpoint returns a clean `401 not_signed_in` (not a 500 — proves the function initialised and ran), the live production JS bundle contains the new fetch call and the `eq-sig-band` component CSS class (proves the frontend shipped, not just the backend), and Sentry shows zero new `eq-shell` issues in the 24h since deploy. Did **not** sign in as Royce to click through the rendered bands — entering his credentials is a hard line regardless of instruction; handed that last visual check to him.

## Decided
- **No money/pipeline/dollar figures on the default dashboard.** Core Shell isn't the system of record for commercials, so any revenue total it showed would be partial by construction — confidently wrong, not just sensitive. The one exception permitted (a dollar-free "N quotes won, no job number yet" nudge, gated behind `quotes.view`) is spec'd but not built.
- **v1 security-group filtering is feature-scoped**, not region-scoped and not a per-group custom-tile picker — reuses the existing `PermKey`/security-group model as-is, no new plumbing.
- **Royce explicitly authorised the merge** ("merge it") and the cleanup ("clean up the worktrees and branch") as separate, distinct go-aheads.

## Deferred
- **Royce to eyeball the live dashboard signed in** — every check that doesn't require his session is clean; only a signed-in pass confirms the three bands render correctly in the browser and the rostered-but-lapsed join surfaces real people.
- **Gate keys are interim** (`field.view`/`service.view`) — swap to the cluster-1 granular keys (`field.view_licences` etc., PR #885, concurrent session) once that ships.
- **Phase 2 — crew-demand overlay.** Needs a `crew_required` column added to `app_data.jobs` (One Pipe migration, both planes) so "can we staff what we've won" has a real demand side. Supply side (deployable crew) is live; demand isn't wired.
- **Phase 3 — the one commercial nudge** ("N quotes won, no job number," no dollar amount, gated `quotes.view`). Spec'd in the appendix of the build plan, not built.

## Notes
- **Rule 0.5 paid for itself twice this session.** Once catching that Phase 0 (server `can()`) was already shipped — would have written a duplicate. Once catching that my first mockup's quote "stages" were invented rather than the real 5-stage board taxonomy — Royce caught it directly ("check status in eq ops... that's not correct, check and confirm").
- **This session hit the shared-checkout/worktree-race pattern repeatedly** — the starting worktree (`eq-ops-mobile-view-af44a3`) was pruned by a concurrent session mid-turn (its `netlify/`/`src/` vanished from under me), the `eq-context` worktree registry was edited concurrently multiple times (had to re-read before each write), and `main` moved twice during PR review requiring two rebases. Consistent with the existing [[shared-checkout-branch-race]] pattern — worth the registry's continued discipline.
- **`gh pr create`/`gh pr merge` both hit transient GitHub API failures** (TLS handshake timeout on create; the auto-mode classifier blocked the merge command once with an explicitly-labelled "usually transient" reason). Both succeeded on a bare retry with no other change — worth remembering as a first response before escalating.
- **GitHub App token lacks PR-create scope** — `mcp__github__create_pull_request` returned `403 Resource not accessible by integration`; `gh` (using a different, more-privileged auth path) worked. If the GitHub MCP is ever the only tool available, PR creation may need `gh` specifically.
