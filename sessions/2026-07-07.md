# Session 2026-07-07 — eq-cards: onboarding shipped live, approval-flow audit, offline ID card + install nudge

Continuation of the 2026-07-06 onboarding session (context rolled over past midnight).

## Built
- **Deployed PR #128** (`71889a3`): merged scan-first onboarding + OCR name auto-fill + pending-application banner + "can't find your company" hatch + Sentry fix. Redeployed `ocr-licence` edge fn **v9** (holder_name from any card; `verify_jwt:false` preserved) via Supabase MCP; ran Cards Build & Deploy (green).
- **PR #129** (`a7808cf`, merged + deployed live): profile offline cache (ID-card name renders with no signal) + `AddToHomeScreenNudge` (once-only, web+not-installed, `pwa_env` conditional-import probe). `ProfileNotifier` now mirrors `LicencesListNotifier`'s cache-aware fetch. New nudge widget test; analyze clean.
- **Resolved Sentry EQ-CARDS-10** (name-gate validation was being captured as a crash; fix live + quiet).

## Decided
- Royce: deploy #128 (edge fn + app).
- Royce: onsite-login direction = offline ID card + install nudge (scan-first "A+B"), NOT biometric-on-web (device lock already covers an installed PWA).
- Royce: build the "text/ID-card offline" slice now; offline photo caching is a fast-follow.

## Deferred
- **Minimum-requirements model** — undecided. Audit found NO gate (a manager can approve a licence-less worker; only "must have a name" P0023) and NO per-org required-credentials concept anywhere (parked). Options presented: soft per-org checklist (recommended) / manager-view-only / hard gate / leave-as-is.
- **Offline photo byte-caching** — so licence images show offline / past the 1h signed-URL expiry. Fast-follow to #129.
- **Onboarding order #5 fork** — scan-first shipped; identity-first is the fallback.

## Notes
- **Sessions are already permanent**: 132 live, oldest 48 days, zero time-boxed (`auth.sessions.not_after` null on all); no signOut except genuine refresh failure / user tap. "Log in once, stay in" needed no auth change.
- **Worker approval flow (cross-repo):** worker applies via `eq_cards_submit_access_request` (gate = name only). Manager approves in Core `StaffPage.tsx` via `/.netlify/functions/cards-approve-staff`; sees name + phone + licence COUNT, "Continue without licences" is an explicit step. `eq_cards_respond_to_access_request` is the WORKER-side responder to employer-initiated invites (checks `worker_user_id=auth.uid()`), NOT the manager path. Org table = `public.organisations` (SKS id = `00000000-…-002`, the only `accepts_applications=true`).
- **ocr-licence CORS drift**: deployed v9 = self-contained inline CORS (cards/core/localhost, fallback-origin); repo `index.ts` imports `_shared/cors.ts` (adds Netlify deploy-preview origins, fail-closed). Deployed v9 inline deliberately to avoid `_shared` bundling uncertainty via MCP. Reconcile task `task_df55614d` spawned (running separately).
- Onsite "login" reframe: showing credentials is read-only, should need no login (offline + device lock); reserve auth for writes.
- Deploy of #129 (run 28819232729) completed **success** — live on cards.eq.solutions.

---

# Session 2026-07-07 — ocr-licence CORS drift reconciled (deployed v10 == repo)

Follow-on to the reconcile task (`task_df55614d`) flagged at the prior close.

## Built
- **Deployed `ocr-licence` v10** on eq-canonical — swapped the live artifact off v9's inline fallback-origin CORS onto the shared `_shared/cors.ts` module (fail-closed; Netlify deploy-preview origins allowed again). Pushed via Supabase MCP `deploy_edge_function` with both `ocr-licence/index.ts` + `_shared/cors.ts` in the files array — the `../_shared/cors.ts` sibling import resolves in the bundle (same shape live `share-licence` already has).
- **PR #130 merged** (`75e0416`) — one-line source-comment parity (response-shape comment now lists `holder_name`) so repo `main` == deployed byte-for-byte, plus a full provenance record in the PR body.

## Decided
- Royce: **Option B** — reconcile via redeploy on the shared CORS module (fail-closed + deploy-previews), not by inlining v9's copy into the repo. Rationale: the live `share-licence` already runs the shared module bundled, so it's the consistent path and the CLI-bundling worry was disproven by ground truth.
- Royce: land it as its **own branch/PR** (`claude/ocr-licence-cors-reconcile`), decoupled from the stale `claude/share-licence-comment` branch. Then: **merge**.

## Deferred
- **Supabase CLI functions-deploy blocked** (config template-path bug, CLI 2.95.4) — MCP used instead; CLI is the "next person" path so it needs unblocking. Task chip `task_61ff8686`.

## Notes
- **The substance was already on `main`.** `main` already carried the shared CORS import + any-card `holder_name` schema/prompt — the branch I started on (`claude/share-licence-comment`) was stale, which made the repo look behind. The real drift was `main` (shared) vs deployed **v9** (inline); the v10 redeploy was the actual fix. Only a one-line doc comment was left to commit.
- **Deploy eq-cards edge fns with `_shared` imports via MCP:** pass every file in the `files` array with dir-relative names (`<fn>/index.ts` + `_shared/cors.ts`), `entrypoint_path: '<fn>/index.ts'`. Verified the bundle contains both files after deploy. Memory: `eq_cards_edge_fn_shared_import_deploy`.
- Merging PR #130 does **not** deploy — v10 was already live from the MCP push.
