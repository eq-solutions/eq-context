# Session 2026-07-07 — eq-cards: onboarding shipped live, approval-flow audit, offline ID card + install nudge

Continuation of the 2026-07-06 onboarding session (context rolled over past midnight).

## Built
- **Deployed PR #128** (`71889a3`): merged scan-first onboarding + OCR name auto-fill + pending-application banner + "can't find your company" hatch + Sentry fix. Redeployed `ocr-licence` edge fn **v9** (holder_name from any card; `verify_jwt:false` preserved) via Supabase MCP; ran Cards Build & Deploy (green).
- **PR #129** (`a7808cf`, merged + deployed live): profile offline cache (ID-card name renders with no signal) + `AddToHomeScreenNudge` (once-only, web+not-installed, `pwa_env` conditional-import probe). `ProfileNotifier` now mirrors `LicencesListNotifier`'s cache-aware fetch. New nudge widget test; analyze clean.
- **Resolved Sentry EQ-CARDS-10** (name-gate validation was being captured as a crash; fix live + quiet).

## Decided
- Royce: deploy #128 (edge fn + app).
- Royce: onsite-login direction = offline ID card + install nudge (scan-first "A+B"), NOT biometric-on-web (device lock already covers an installed PWA).
- Royce: build the "text/ID-card offline" slice now; offline photo caching is a fast-follow.

## Deferred
- **Minimum-requirements model** — undecided. Audit found NO gate (a manager can approve a licence-less worker; only "must have a name" P0023) and NO per-org required-credentials concept anywhere (parked). Options presented: soft per-org checklist (recommended) / manager-view-only / hard gate / leave-as-is.
- **Offline photo byte-caching** — so licence images show offline / past the 1h signed-URL expiry. Fast-follow to #129.
- **Onboarding order #5 fork** — scan-first shipped; identity-first is the fallback.

## Notes
- **Sessions are already permanent**: 132 live, oldest 48 days, zero time-boxed (`auth.sessions.not_after` null on all); no signOut except genuine refresh failure / user tap. "Log in once, stay in" needed no auth change.
- **Worker approval flow (cross-repo):** worker applies via `eq_cards_submit_access_request` (gate = name only). Manager approves in Core `StaffPage.tsx` via `/.netlify/functions/cards-approve-staff`; sees name + phone + licence COUNT, "Continue without licences" is an explicit step. `eq_cards_respond_to_access_request` is the WORKER-side responder to employer-initiated invites (checks `worker_user_id=auth.uid()`), NOT the manager path. Org table = `public.organisations` (SKS id = `00000000-…-002`, the only `accepts_applications=true`).
- **ocr-licence CORS drift**: deployed v9 = self-contained inline CORS (cards/core/localhost, fallback-origin); repo `index.ts` imports `_shared/cors.ts` (adds Netlify deploy-preview origins, fail-closed). Deployed v9 inline deliberately to avoid `_shared` bundling uncertainty via MCP. Reconcile task `task_df55614d` spawned (running separately).
- Onsite "login" reframe: showing credentials is read-only, should need no login (offline + device lock); reserve auth for writes.
- Deploy of #129 (run 28819232729) completed **success** — live on cards.eq.solutions.

---

# Session 2026-07-07 — ocr-licence CORS drift reconciled (deployed v10 == repo)

Follow-on to the reconcile task (`task_df55614d`) flagged at the prior close.

## Built
- **Deployed `ocr-licence` v10** on eq-canonical — swapped the live artifact off v9's inline fallback-origin CORS onto the shared `_shared/cors.ts` module (fail-closed; Netlify deploy-preview origins allowed again). Pushed via Supabase MCP `deploy_edge_function` with both `ocr-licence/index.ts` + `_shared/cors.ts` in the files array — the `../_shared/cors.ts` sibling import resolves in the bundle (same shape live `share-licence` already has).
- **PR #130 merged** (`75e0416`) — one-line source-comment parity (response-shape comment now lists `holder_name`) so repo `main` == deployed byte-for-byte, plus a full provenance record in the PR body.

## Decided
- Royce: **Option B** — reconcile via redeploy on the shared CORS module (fail-closed + deploy-previews), not by inlining v9's copy into the repo. Rationale: the live `share-licence` already runs the shared module bundled, so it's the consistent path and the CLI-bundling worry was disproven by ground truth.
- Royce: land it as its **own branch/PR** (`claude/ocr-licence-cors-reconcile`), decoupled from the stale `claude/share-licence-comment` branch. Then: **merge**.

## Deferred
- **Supabase CLI functions-deploy blocked** (config template-path bug, CLI 2.95.4) — MCP used instead; CLI is the "next person" path so it needs unblocking. Task chip `task_61ff8686`.

## Notes
- **The substance was already on `main`.** `main` already carried the shared CORS import + any-card `holder_name` schema/prompt — the branch I started on (`claude/share-licence-comment`) was stale, which made the repo look behind. The real drift was `main` (shared) vs deployed **v9** (inline); the v10 redeploy was the actual fix. Only a one-line doc comment was left to commit.
- **Deploy eq-cards edge fns with `_shared` imports via MCP:** pass every file in the `files` array with dir-relative names (`<fn>/index.ts` + `_shared/cors.ts`), `entrypoint_path: '<fn>/index.ts'`. Verified the bundle contains both files after deploy. Memory: `eq_cards_edge_fn_shared_import_deploy`.
- Merging PR #130 does **not** deploy — v10 was already live from the MCP push.

---

# Session 2026-07-07 — Field mobile polish (Leave / modals / nav) + voice-to-text back on safety forms

Royce: "polish mobile view from core > field" → "merge and continue" → steelman voice-to-text on safety forms → "free form fields only" → "ship both together". Four eq-field releases + one eq-shell change, all merged and live.

## Built
- **eq-field v3.5.258** (PR #415) — Leave supervisor dashboard mobile: `.eqf-toolbar` wraps (buttons no longer run off the right edge); `.eq-lv-statrow` 4-across → 2×2 at ≤560px; Prestart/Toolbox/Diary modal footers lifted clear of Shell's bottom app bar.
- **eq-field v3.5.259** (PR #416) — generalised the modal-footer fix to ALL modals in shell-mode (`@media pointer:coarse`: `.modal-overlay` bottom-pins + 76px lift, sheet capped to `calc(100dvh - 76px)`).
- **eq-field v3.5.260** (PR #417) — mobile section nav → inline Lucide line icons (house/calendar/clock/umbrella/layout-grid/menu); killed the `⏱`/`🏖` colour-emoji outliers; `stroke:currentColor` tints navy on active.
- **eq-field v3.5.262** (PR #419) — voice-to-text on 11 freeform textareas (Prestart 4, Toolbox 4, Diary 3) via new shared `SiteReportsShared.voice` helper (mirrors audits.js `_auSpeechToggle`). Appends + editable; draft syncs by firing the field's own `onchange`; hidden on locked/unsupported.
- **eq-shell** (PR #693, `19193d1`) — mic granted to Field iframe origins: `netlify.toml` Permissions-Policy `microphone=(self "field.eq.solutions" "eq-field.netlify.app")` + `FieldIframe` `allow="microphone"`. Deployed to core.eq.solutions; live header verified.

## Decided
- Royce: nav icons → **all Lucide** (over "fix just the 2 emoji").
- Royce: voice-to-text → **freeform fields only** ("relatively quiet areas… helps tradies with spelling/grammar").
- Royce: **ship both together** (Field + Shell), and explicit **go** to merge/deploy the eq-shell auth-hub change.

## Deferred
- Live signed-in smoke of Field voice on an SKS session — needs a browser + physical mic (can't verify programmatically).
- Field mobile polish — prestart form top grid + roster grid at 375px still un-eyeballed (offered, not done).

## Notes
- **Voice wasn't removed after a problem** — it was dropped in the safety.js → site-reports.js prestart/toolbox rewrite; still lived on Site Audits (audits.js, v3.5.236). Re-added to the site-report forms.
- **Why modal footers were hidden in core > field:** Shell paints its persistent bottom app bar from the *parent* window (above the iframe's z-index) over the bottom ~76px of the Field iframe — a `position:fixed` modal-overlay covers that band, so bottom-sheet footers sat behind it. `.shell-mode .page` already reserves the same 76px.
- **Field iframe origins differ by tenant:** `sks` → `https://eq-field.netlify.app` (token-auth via `#sh=`, must be non-`.eq.solutions`); `eq` → `https://field.eq.solutions`. Both needed in the mic allowlist (`FIELD_TENANT_URLS` in eq-shell `src/lib/fieldTenants.ts`).
- **Mic needs BOTH sides:** an iframe `allow="microphone"` only delegates a capability the parent is permitted to use — with the top-level policy at `microphone=()`, delegation silently fails. The Permissions-Policy loosen + the iframe `allow` are both required.
- CSS load-order gotcha: `mobile.css` loads *before* `field-v8.css`, so equal-specificity field-v8 rules win — use 2-class selectors or `!important`.
- Version drift persists: file `APP_VERSION` runs ahead of commit labels; v3.5.261 was a concurrent Roles PR by another agent, not this session. Screenshot capture in the preview tool kept timing out — verified layouts by measuring computed geometry (more precise for CSS anyway).

---

# Session 2026-07-07 — Prestart Word export back + SW resilience + iOS export fallback

Live mobile incident from Royce (stuck loading screen, frozen spinner, "the mobile UI won't export") → diagnosed + shipped v3.5.265.

## Built
- **eq-field v3.5.265** (PR #420, `ac982d1`, merged + live):
  - `site-reports.js` `exportPrestartDocx()` — re-added Prestart Word export (was absent on the live app). Modelled on `exportToolboxDocx`, uses `SiteReportsShared.docx`. "↓ Word" button in a submitted prestart's locked footer; doc carries site/date/supervisor, prev-day issues, works scope, HRCW, SWMS, hazards, permits, crew signatures, photos, tenant logo/palette.
  - `sw.js` hardened — per-file precache (`Promise.allSettled`, was atomic `addAll`) + navigation fallback to cached `/index.html`.
  - `site-reports-shared.js` `buildPackage` — iOS-standalone export fallback (Web Share sheet / open, else unchanged anchor download).

## Decided
- Royce: ship SW hardening + iOS export fallback (multi-select).
- Royce: **"prestart export back"** — re-add Prestart Word export (chose over prestart+diary, or neither).

## Deferred
- Royce needs one more hard-reload on his phone to land on the resilient SW (v3.5.265) — hardening only helps from the next clean load.
- Diary Word export — still absent (never had one); left out per the prestart-only pick.

## Notes
- **The safety.js → site-reports.js rewrite dropped BOTH voice AND Word export from Prestart** (Diary never had export; Toolbox + Site Audits kept theirs). When a site-report feature looks "missing", diff against the old `safety.js` — it's the superseded source of what prestart used to do.
- **SW stuck-loader root cause:** network-first + atomic `addAll` + delete-old-cache-on-activate → one precache miss (mobile blip) leaves the app on an EMPTY cache and it sticks on a dead loader (frozen spinner = never booted). Fix = per-file precache + shell fallback.
- Live deploy was healthy the whole time — the incident was purely client SW state. Verified by curling live assets for 200s + version consistency before touching anything. `buildPackage` clears its palette prime after each call, so `setPalette()` must precede every `buildPackage()` (real code does; caught it in the harness).
- Version churn: concurrent agents shipped v3.5.263 + v3.5.264 (timesheets TAFE) mid-session; rebased onto the latest main each time and took the next free version (mine = 265).
