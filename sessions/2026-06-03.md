---
title: Session log — 2026-06-03
owner: Royce Milmlow
scope: EQ Field — Resources remove/archive job
status: live
---

# 2026-06-03 — EQ Field: Resources "Remove job"

**Built by:** Royce Milmlow + Claude Code

## Shipped
- **v3.5.53** (PR [#160](https://github.com/eq-solutions/eq-field/pull/160), squash-merged, live on eq-solves-field.netlify.app).
- Resources (Resource Allocation) screen: added a **Remove job** action on both Needs Allocation and Confirmed jobs. Previously you could "+ Add Active Job" but never remove one.
- **v3.5.54** (PR [#161](https://github.com/eq-solutions/eq-field/pull/161), merged, live): BUG-009 fix — Remove job button did nothing because `window.confirm` is swallowed in the eq-shell iframe / iOS PWA standalone. Now routes through the shared `#modal-confirm` dialog (`_raConfirm`).

## How it works
- `scripts/sks-pipeline-resource.js → removeProject()` sets `tenders.archived_at` — a reversible soft-remove matching the existing pipeline "kill" pattern. Archived jobs drop out of both Resources and the Pipeline (load filters already use `archived_at=is.null`).
- Roster entries already pushed to `schedule` are deliberately left in place — archiving the tender does not unwind the roster.
- Confirm dialog before archiving. Local state (`_enr`, `_noms`, `_pending`, `_ca`, `_siteCodes`, `_wplan`) cleaned on remove.
- Confirmed jobs with no labour slots now still open a panel (`_emptyConfirmedPanel`) so they remain removable.

## Decisions
- **Archive over hard delete** — reversible, leaves pushed roster intact, matches precedent. (Hard delete was offered and declined.)
- **Both sections** get the control.

## Notes
- This is the SKS-ported Resource Allocation screen, ungated for the EQ tenant since v3.5.43, so the change is tenant-agnostic. Royce is trialling it on the **eq** tenant.
- Changelog backfilled with v3.5.51 (lazy-loader fixes) and v3.5.52 (licence admin, PR #158) which were missing.

## SKS standalone repo — Remove job shipped LIVE
- **sks-nsw-labour v3.10.44** (PR [#18](https://github.com/eq-solutions/sks-nsw-labour/pull/18), squash-merged, **live on sks-nsw-labour.netlify.app**).
- Royce-directed manual cherry-pick of the EQ Field v3.5.53/54 Remove job feature into SKS's `scripts/pipeline-resource.js` (adapted to SKS `ra-` DOM-id convention; same `SKS_PIPELINE_RESOURCE` namespace + shared `#modal-confirm`).
- No schema change: SKS `tenders` already has `archived_at` (it's the feature's original home). Confirm via shared modal (BUG-009-safe).
- SKS banner title line corrected (had lagged at v3.10.42).

## Pipeline view — sliders + Keep/Discard (BOTH repos, live)
- **EQ Field v3.5.55** (PR #162) + **SKS standalone v3.10.45** (PR #19) — both squash-merged and live.
- Filter sliders (value + probability) replace the value dropdown; Keep/Discard triage on every card (Discard→archive, Keep→`reviewed_at` + ✓ Reviewed badge). User-driven triage — no auto-stale rule yet (Royce: "start by having a user decide, eventually improve").
- Schema: `tenders.reviewed_at` (timestamptz) added to all 3 DBs (EQ active ktmjmdzqrogauaevbktn, canonical-internal zaapmfdkgedqupfjtchl, SKS live nspbmirochztcjijmcrx).
- **Flag for follow-up:** EQ pipeline tenders live in ktmjmdzqrogauaevbktn (333 rows), not eq-canonical-internal (1 row) — the v3.5.50 registry flip didn't move pipeline data. Reconcile before the canonical cutover is 'done'.

## EQ Field data fix — pipeline data migrated to canonical-internal
- **Problem found:** EQ Field prod reads `eq-canonical-internal` (zaapmfdkgedqupfjtchl) per the registry, but the v3.5.50 flip never migrated the data there — it had 0 people / 0 schedule / 1 tender. The real eq data was stranded in the original `ktmjmdzqrogauaevbktn`. So the live EQ workspace was effectively empty.
- **Decision (Royce):** respect the canonical direction (consolidate into canonical-internal), not repoint. Demo/Melbourne data disposable. Scope = **pipeline only** for now.
- **Done:** migrated eq-org pipeline data ktm → canonical-internal via anon-key REST pipe (curl GET source | curl POST target): 323 tenders + 3 enrichment + 1 import_run + 5 review_decisions. Cleared the disposable trial rows first. Verified: 111 tenders visible to the app's pipeline query (anon, through RLS). Pipeline + Resources now populated on live EQ — no deploy needed.
- **Still stranded (deferred):** eq roster/people/schedule/sites/leave/timesheets remain in ktm — EQ roster is empty on the live app until a later (larger) migration or a formal decision to drop it.
- Memory `project_eq_canonical_tenant_registry` corrected (canonical-internal is the live eq data plane; v3.5.50 migration was incomplete).

## Carried forward
- **SKS promotion:** the Resources (sks-pipeline-resource) remove/archive feature is being trialled on the **eq** tenant and is intended to merge to **SKS live** once validated. Track for the SKS-side merge (manual cross-repo, Royce-directed).
