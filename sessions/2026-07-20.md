# Session 2026-07-20 — invite-accept duplicate-identity follow-up: root cause disproven, hardening shipped + deployed, then a real lead found via Sentry

## Built
- eq-shell `accept-invite.ts`: phone-stub match on invite acceptance is now format-tolerant (reuses `phoneMatchKey` from `roster-match.ts` — checks AU phone variants instead of one exact string).
- eq-shell `backfill-auth-users.ts`: added a cross-row collision guard — if two `shell_control.users` rows share a normalized phone or email, neither gets a new auth identity minted; both are reported under `collisions_skipped` for manual merge instead.
- PR #914 (`claude/invite-accept-phone-stub-collision`) opened, rebased onto main after PR #862 merged mid-session, CI green (typecheck/163 tests/lint + schema drift + migration ledger), squash-merged (`8cc321c`), auto-deployed to `core.eq.solutions`. Deploy confirmed live via Netlify MCP: `commit_ref` matches, `context: production`, `state: ready`, published ~4 min after merge.

## Decided
- Royce chose "ship the hardening anyway" after I reported the leading root-cause hypothesis was disproven — i.e. build the defense-in-depth fix even though it doesn't explain the specific incident it was prompted by.
- Royce confirmed push + PR + merge + deploy explicitly (auth-flow change, deploy normally gated).

## Deferred
- **Confirming it was actually Will on the Cards session found via Sentry, and the exact code path from a failed self-signup attempt to a real duplicate account.** See Notes — narrowed considerably post-close, but not fully closed. Not urgent, his data is already repaired.

## Notes
- **PR #862 (email-only invite-auth-identity matching) merged mid-session** by a separate/concurrent process — not this session's doing, but it landed while this investigation was in progress. Confirmed live-tested against Will's case before this session started: it would still return the wrong account for him, since his real sign-in record has no email on file (phone-only). That's why this follow-up was scoped separately rather than folded into #862.
- **Investigation method that actually worked:** the permanent activity log (`shell_control.audit_log`) is append-only and pre-dates any manual repair, so it's the one trustworthy source of 2026-07-06 history — every other table's field values had already been touched by the hand-repair done earlier the same day this session ran, which destroyed their forensic value for reconstructing the original state.
- Pulled the exact `accept-invite.ts` as it existed live on 2026-07-06 (via git history at the commit that was `main` that day) rather than trusting today's version — the code changed materially between then and now (two auth-hardening PRs landed 2026-07-13, after the incident).
- `shell_control.users` has no `updated_at` column and no row-level audit trail — worth knowing for any future identity forensics in this table; only `created_at` is trustworthy (immutable) and only the append-only audit log survives a hand-repair intact.
- **Post-close addendum:** Royce asked to check Sentry for the 2026-07-06 accept-invite errors — the Sentry MCP had been unauthorised earlier in the session (listed as requiring OAuth) but was live by this point. eq-shell's Sentry project shows zero errors at all in the 09:00–11:00 UTC incident window, confirming nothing crashed on the Shell side. But eq-cards' Sentry project has [EQ-CARDS-10](https://eq-solutions.sentry.io/issues/EQ-CARDS-10): `ValidationFailure: Add your name to your profile before connecting to a company`, fired twice 17 seconds apart at 09:01 UTC on 2026-07-06 — one hour before Will's real invite was correctly accepted (10:02:17 per the audit log). User is Sydney, Chrome Mobile, phone form factor — a real session, not a bot. The symptom (nameless profile blocked from a "connect to company" submission) matches the duplicate account's own pre-repair state (`name: null`). Sentry doesn't tag a user ID on the event, so this wasn't proof it was Will.
- **Second post-close addendum:** told Royce this lead, then asked him directly — he has no idea who that Sydney session was. Downgraded the pending.md entry from "likely origin" back to an unconfirmed candidate; the real root cause is genuinely still open. Lesson: an IP/geo/timing match is suggestive but not identification — don't let a plausible-sounding correlation read as more resolved than it is, especially once the person who'd actually know says they don't recognise it.

---

# Session 2026-07-19/20 (2) — EQ Shell worktree registry cleanup + Sentry error triage

## Built
- Live-audited every eq-shell worktree against actual GitHub PR state (not the registry doc, which had 2 stale entries) — found 6 total whose work had shipped: `bold-brattain-688290` (#901), `frozen-window-issue-58b6b2` (#898), `eq-shell-accessmodel-c1-wt` (#890+#885), `eq-shell-comms-polish-wt` (#897, doc wrongly said "not merged"), `eq-shell-dupes-merge-wt` (#880, doc's "uncommitted WIP" note was also stale), and `accept-invite-phonestub` (#862 — genuinely still open on first check, merged the next day, re-verified and pruned as a follow-up).
- Removed all 6 worktrees and deleted their local + remote branches, each pass gated on Royce's explicit confirmation (worktree removal and branch deletion are separately gated by the safety classifier, and a later instance of the same action class — the 6th worktree, a day later — needed its own fresh go rather than inheriting the earlier batch approval).
- Root-caused a live Sentry alert (`EQ Field handoff timeout` + `auto-recovery (rejected)`, same trace/user, 4 minutes apart): traced to `FieldIframe.tsx`'s self-healing handoff — a slow load let the 60s one-shot token expire before Field's own auth check ran, Field correctly rejected it, and the app's built-in silent-retry mechanism handled it with no further error. Concluded this is a known, already-hardened edge case working as designed, not an active incident — no code change made.
- Found a second Sentry issue (`auth-stall: session-spinner-timeout`) was stale — the underlying bug (frozen "Signing in…" spinner from an unbounded body-read after headers arrive) was already fixed and confirmed live via PR #863 (verified against the actual deployed commit on core.eq.solutions, not just "merged" status). Marked it resolved in Sentry with the root-cause explanation attached.
- Updated `system/worktree-registry.md` throughout to match live reality (corrected 2 stale claims, logged the full prune with PR references).

## Decided
- Royce: "yes, delete the worktrees" (the first 5).
- Royce: "yes, delete the branches too."
- Royce: "yes, delete it too" (the 6th, accept-invite-phonestub, once confirmed merged).
- Royce: "complete the highest value activity you can" — used to prioritize the live Sentry investigation over routine worktree deletion, since a real user had just hit a production error.

## Deferred
- Root cause of *why* Field took long enough to boot that the 60s token expired for that one user (2026-07-19) is unconfirmed — plausibly just a poor connection. No repeat occurrences since. Not chased further; the existing self-healing already covers it.

## Notes
- The safety classifier gates worktree removal and branch deletion as genuinely separate action classes — approval for one doesn't carry to the other, and approval for one instance doesn't carry to a later instance of the same class (the 6th worktree needed its own "yes" the next day, matching the pattern already documented elsewhere in this substrate).
- Several Sentry/Netlify/Supabase MCP calls hit transient "temporarily unavailable" classifier errors during this session that cleared on a plain retry within 1-3 attempts — not a real permission denial, just retry-and-continue.
- Netlify's `get-deploy-for-site` response routinely exceeds the tool's inline token limit and gets written to a scratch file — `python3 -c "print(open(...).read()[:N])"` or a targeted grep is the way to pull just the field needed (e.g. `commit_ref`) without reading the whole payload.

---

# Session 2026-07-20 (3) — Will Brown intake, PR #862 shipped, 8 stale eq-shell migrations cleared, SKS gap found + handed off

## Built
- Diagnosed Will Brown's "cards deleted / SKS link broken" report: not a deletion — two disconnected auth identities (phone-OTP, properly SKS-linked, 4 licences; email/password, orphaned, 0 licences until he re-added one today). Merged his data onto the real identity (5 licences, active SKS org membership confirmed live).
- eq-cards migration `0094_flag_identity_collisions.sql` — non-blocking audit table + `eq_cards_link_or_create_worker()` hardening, flags future cross-provider identity collisions instead of silently creating orphaned profiles. Applied live.
- Found eq-shell PR #862 (`fix(auth): harden invite-accept auth-identity binding + orphan detector`) open+unreviewed for 6 days, CI green. Applied its 2 control-plane migrations (`resolve_invite_auth_identity`, `list_orphan_auth_identities`) live, then merged the PR (Royce) — confirmed deployed to core.eq.solutions via Netlify (commit `8cb5e9f`, production, ready).
- Live-tested PR #862's fix against Will's actual case post-merge: doesn't match (his real identity has no email on file, PR #862 matches by email only) — correctly scoped as a partial fix, not the full story. (The full story landed later the same day via a separately-spawned session, see the entry above this one — PR #914.)
- Found + applied 8 more eq-shell control-plane migrations already merged to `main` but never hand-applied (same "merge ≠ applied" trap #862 sat in): `2026_07_12b_organisations_anon_column_scope` and `2026_07_14d_audit_retention_revoke_anon_execute`/`2026_07_14d_lock_public_audit_retention_exec` were live **unauthenticated** security gaps (anon could read tenant routing secrets for every org; anon could trigger the credential-audit retention purge) — both closed and verified. Remaining 5 (`2026_07_13_worker_completeness` + `_revoke_anon` + `_align_cards`, `2026_07_13c_worker_compliance_status`, `2026_07_14e_branding_dark_doc_logo`) were feature/correctness gaps — the branding one was an active bug (frontend already called the 4-arg RPC signature the DB didn't have yet).
- Spawned + Royce started `task_0bdf11da` (eq-shell accept-invite.ts stub-detection root cause) — completed same day, see the session entry above this one.
- Spawned `task_5aa1c55c` (SKS nspbmir anon-CRUD lockdown) after drafting Stage 3 SQL (`~/.claude/plans/nspbmir-stage3-drop-anon-crud.sql`) and finding it has 3 blocking gaps (checkins/leave_balances/pipeline_events have no authenticated replacement; site_diaries/tender_review_decisions' existing authenticated policy checks the wrong JWT claim). Royce started this task in a separate session before this one closed.

## Decided
- Royce: apply migration 0094 now.
- Royce: merge PR #862 + apply its migrations now.
- Royce: apply all 8 stale migrations, security ones first.
- Royce: merge eq-context #62 + eq-ui #22 himself (GitHub App here lacks merge permission — confirmed via repeated 403s across 4 different repos, not a fluke; eq-field returns 404, i.e. not in the App's installation at all despite the repo existing and being reachable via git).
- Royce: draft nspbmir Stage 3 (SKS security work) — then, after Royce flagged "is this a rabbit hole," agreed the SKS tangent should be its own session rather than continuing inline.

## Deferred
- eq-context #62, eq-ui #22, eq-field #466 — need Royce to merge manually (permission gap above).
- GitHub App installation should be granted `eq-field` repo access — currently invisible to this connector.
- SKS nspbmir anon-CRUD lockdown (Stages 2-4) — handed off as `task_5aa1c55c`, already started separately. Full detail in `sks/pending.md`.

## Notes
- **The "merge ≠ applied" trap bit twice in one day** — once for #862 (6 days stale), then again for 8 more migrations found by diffing `origin/main`'s migration files against Supabase's actual applied-migration history (`list_migrations`). Worth someone eventually automating this diff as a check rather than relying on a session stumbling onto it.
- **eq-solves-service (zaap project) looked like it had the same gap (45+ unapplied-looking migrations) but wasn't treated the same way** — that repo has its own migration-ledger table (implying a different apply mechanism/tracking) and an actively-live worktree (`migration-renumber`) touching the exact same migrations, with the newest migration's own header explicitly stating it ships together with matching eq-shell changes. Correctly identified as in-flight work, not a parked bug, and left untouched rather than guessed at.
- **GitHub write access is a hard ceiling in this session, not a transient failure** — 403 "Resource not accessible by integration" on merge across eq-shell, eq-context, and eq-ui (3 different repos, same error), despite `get_me` resolving to Royce's own account. This is a GitHub App installation-permission gap (missing repo-level "contents: write"), separate from Royce's own account permissions — worth fixing at the App config level so future sessions aren't blocked the same way.
- Read nspbmir's live `pg_policies` (not customer PII rows) to draft an accurate Stage 3 — justified as inspecting SKS's own security config for work Royce explicitly commissioned, not "mixing data" between entities. No SKS data was touched, only security metadata read.

---

# Session 2026-07-20 (5) — SKS nspbmir lockdown: closed Stage 3's gaps, found + fixed scope on the emergency patch, nothing applied

## Built
- Re-verified live `pg_policies` + `information_schema` on nspbmir from scratch (prior session's snapshot could have drifted) — confirmed all 3 Stage-3 blockers from `task_5aa1c55c` still stood, then closed them: extended `nspbmir-stage2-authed-policies.sql`'s `org_tables` array with `checkins`, `leave_balances`, `pipeline_events`, `site_diaries`, `tender_review_decisions`, and added a `DROP POLICY` for the two stale `field_authed_all` (tenant_id-gated) policies on the last two, replaced by org_id-gated equivalents in the same run.
- Confirmed via the actual PR #34 diff (not just its spec doc) that the Stage-1 minter's JWT sets `app_metadata.org_id` only — this is what makes the legacy tenant_id-gated policies permanently inert, not a guess.
- Found a live, independent issue beyond the original brief: `app_config`'s anon SELECT policy is `USING (true)` (no org filter at all), exposing what are effectively backend service tokens (`digest_fn_token`, `shift_events_fn_token`, `canonical_api_key_field`) to anyone with the public anon key. Verified isolated (consumed only by `SECURITY DEFINER` Postgres functions granted to `service_role`, never through the anon/PostgREST path — confirmed via repo-wide grep including `supabase/functions/`) — drafted `nspbmir-EMERGENCY-anon-select-narrowing.sql` as a denylist-based fix.
- First draft of that same file also tried to narrow `people`/`managers` anon-readable columns (closing a plaintext `people.pin` exposure). Caught before recommending it: `scripts/people.js:926` has the browser fetch-and-compare a person's own PIN client-side for a live staff-timesheet self-check kiosk flow — narrowing would have broken that feature today. Pulled that part out, documented why, and spawned `task_fcc7c11a` (move the PIN compare server-side into `verify-pin.js`, mirroring the existing login-gate pattern) as separate, correctly-scoped follow-up work rather than folding it into an emergency patch.
- Wrote `nspbmir-lockdown-runbook.md` — full execution order (emergency app_config fix → PR #34 merge → env vars → extended Stage 2 → soak → Stage 3 → remaining Stage 4 scope) with verification steps and rollback notes per stage.
- Saved 2 memory files: the nspbmir lockdown state (all drafts, nothing applied) and a feedback memory on Royce's bar for live-app changes — verified-safe isn't the same as permission to run, even after re-confirming twice.

## Decided
- Royce, after the emergency `app_config` fix was independently verified safe twice: "add to MD files and leave it, im not risking any changes." Nothing executed — all 4 SQL/doc files remain drafts in `C:\Users\EQ\.claude\plans\`, none committed to the repo (his earlier explicit choice), none applied to nspbmir.

## Deferred
- Everything in the lockdown plan — emergency `app_config` fix, PR #34 merge, Stage 2 (extended), Stage 3, remaining Stage 4 scope (people/managers column narrowing, blocked on the PIN app-fix) — all drafted and verified, none run. Royce's call on timing.
- `task_fcc7c11a` — move staff-timesheet PIN compare server-side (sks-nsw-labour `verify-pin.js`) — spawned, not started.

## Notes
- This is a continuation of `task_5aa1c55c` (spawned end of the 2026-07-19/20 (3) session above) — same underlying lockdown effort, this session did the actual gap-closing work that task was created for.
- Both `nspbmir-EMERGENCY-anon-select-narrowing.sql`'s drafts (broad, then corrected) are worth reading in full if picking this back up — the corrected version's "PULLED" section documents exactly why the column-narrowing approach is unsafe without an app change first, so a future session doesn't have to re-derive that.
