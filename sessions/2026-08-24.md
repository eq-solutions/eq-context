# Session 2026-08-24 — SEC-58 (control-plane ledger) and SEC-65 (AUDIT_SB_KEY label) closed

## Built
- **SEC-58 closed** — `supabase/CONTROL-PLANE-LEDGER.md`'s summary hadn't matched its own changelog since 2026-07-11; 54 files had no ledger row at all. All 54 object-verified against live jvkn (existence, grants, constraints, and a live function-body diff for 3 functions redefined by multiple untracked files). Result: 53 applied, 1 misfiled, 0 pending, 0 not-found — no live gap, the gap was the document. eq-shell [PR #1561](https://github.com/eq-solutions/eq-shell/pull/1561), merged, live.
- **SEC-65 closed (code + inventory)** — `AUDIT_SB_KEY` was documented as a publishable/read-scoped key in 3 eq-field files; it's actually the ehow service_role key, fingerprint-matched to the already-tracked 4-alias service_role cluster in `ops/secrets-inventory.md` (now 5 aliases). Comments corrected, inventory folded in, register note added. eq-field [PR #762](https://github.com/eq-solutions/eq-field/pull/762), merged, live.
- Both PRs merged on Royce's "merge" — confirmed via `gh pr view` mergedAt/state, not just merge-command exit status.

## Decided
- Royce confirmed which open security items to actually build this round (SEC-58 + SEC-65) out of 4 surfaced (SEC-57/60/61/63 left untouched — GitHub App trust call, org policy sign-off, and a Netlify-dashboard-only check aren't executable from a coding session).
- Royce ran `/decide then go` on the SEC-58 plan before it started — six-step pass held up (no irreversible risk: read-only against jvkn, docs-only output), proceeded without a second confirmation round.
- `2026_07_11_tender_tables_anon_lockdown.sql` confirmed misfiled (targets ehow/zaap, not jvkn) — same class as the 3 already-tombstoned files from the 2026-07-11 baseline, not a new problem.

## Deferred
- **eq-field PR #761's migration (identity upward-push trigger) — live-apply on ehow not independently confirmed this pass.** The two out-of-band secrets it needed are now both created and the eq-shell receiver end verified live, but that doesn't prove the ehow-side trigger body itself is live. Needs a direct `pg_get_functiondef` check or a real Field roster edit to confirm the pipe actually fires.
- **SEC-65's exposure question still open** — whether `AUDIT_SB_KEY` actually ships client-side in any of its 3 non-Sentry consumers. Label corrected; exposure not tested.
- SEC-57 (org-wide GitHub App `grok-by-xai` holds near-admin write access), SEC-60 (org/repo CI-CD hardening gaps), SEC-61 (dev-context secret leak, 22 vars across 4 repos), SEC-63 (account-scope Netlify secret signs every session in the suite, site-scope unconfirmed) — all still open, all need Royce's own call or click, not more building. Deliberately not touched this session.

## Notes
- The brief-gate (`guard.js`) blocked the first ledger edit — this session's SEC-58 work had pivoted in from a conversational "sprint the next round" without an explicit `/brief eq-shell` first. Ran the brief properly mid-task rather than bypassing with `EQ_SKIP_BRIEF`; worth remembering that a task growing organically out of chat still needs the gate, not just tasks that start that way.
- `ops/security-register.md`'s live content (fetched fresh via `gh api ...contents?ref=main`, not the local clone) was the actual source for this session's SEC-58/65 work — the local `C:\Projects\eq-context` clone was confirmed behind origin/main (4-6 commits, at different points this session) every time it was checked. The gate's own SYNC warning was accurate and load-bearing here, not noise.
- Verification methodology for SEC-58: where more than one untracked file redefines the same function (`eq_worker_completeness`, `eq_worker_compliance_status`, `eq_get_tenant_user`), existence alone can't tell you which file is "current" — pulled the live `pg_get_functiondef` body and matched it against each candidate file's distinguishing content instead of assuming the latest filename wins. Caught real supersession chains this way (e.g. `eq_get_tenant_user` got 3 same-day rewrites).

---

# Session 2026-08-24 — who can see Staff Conversations (Q&A only, no code changed)

## Built
- Nothing shipped — pure read-only investigation, answered in chat.

## Decided
- None new — this confirmed an existing 2026-08-19 design decision (creator-only reads, opt-in-only grant) rather than changing anything.

## Deferred
- None.

## Notes
- Royce asked who can see the "Conversations" feature on a staff record. Answered live, not from memory: exactly 2 people platform-wide hold `staff.manage_conversations` — Royce (added 2026-08-17) and Luke Johnson (added 2026-08-23) — both via one Custom Group ("Staff Conversations", SKS-tenant-scoped) in `shell_control.user_security_groups`/`security_group_perms`. `shell_control.tenant_role_overrides` has zero rows for this key — no role grants it by default, matching the deliberate design documented inline in `src/permissions/matrix.ts` and confirmed already-declined-to-change at `eq/pending/eq-shell.md:330` (Royce, 2026-08-16: "leave group-opt-in only, no role default").
- **Real trap avoided**: the RLS policy reads `auth.jwt() -> 'app_metadata' -> 'extra_perms'`, but that claim is computed FRESH at each JWT mint (`mint-supabase-jwt.ts`, merging security-group perms + role overrides) — it is NOT persisted to `auth.users.raw_app_meta_data`. A first query against `raw_app_meta_data` came back empty (zero users), which would have been reported as "nobody can see it" — wrong. Traced the real mint-time query path before answering.
- Also surfaced a nuance likely not obvious to Royce: the single `cmd: ALL` RLS policy on `app_data.staff_conversations` applies `created_by = auth.uid()` to READS too, not just writes — so even between the two people who hold the permission, neither can see the other's entries. Confirmed intentional via the table's own header comment (`ConversationsSection.tsx`), not assumed.
- Session resumed mid-close from the prior day (2026-08-23 work, already logged there) — this file starts fresh for 2026-08-24 per the date rollover.
- **Interrupted mid-`/close` by a new report**: Royce flagged Staff-page navigation as slow ("clicking the staff list seems to take an eternity") right as this close was running. Not investigated in this session — closing out cleanly first, per the user's own message arriving as a mid-turn interrupt rather than a blocking question. Worth the next session's first move: `staff-bootstrap.ts`'s own header already documents a ~3.0-3.3s cold-start cost per function and a fan-out of 8 previously-separate endpoints it was built to collapse into one — check whether that endpoint itself is now slow (regressed, or never actually wired into a cold path) before assuming a new cause.

---

# Session 2026-08-23 (cont. into 08-24) — Zemi Asri's licence photo fixed for good, two grant-restoration incidents, a suite-wide security sweep with a near-miss, and a small UI feature

*Session started 2026-08-23, ran past midnight UTC — bulk of the work and all "today" references inside it are 2026-08-23; logged here under 08-24 per `/close`'s own "YYYY-MM-DD = today (when /close runs)" rule, not backdated into the already-existing 2026-08-23.md.*

## Built

- **eq-cards**: swept all 205 live `licences` rows for the same storage-path-never-follows-an-identity-merge bug behind Zemi Asri's original report; fixed 6, root-caused the class so it can't recur (RLS now checks the live `licences.user_id`, not the frozen path). Migration `0137`, PR #294, merged + live.
- **eq-cards**: restored `authenticated` grants silently dropped by migration `0131` on two admin RPCs (`eq_cards_admin_upsert_worker`, `eq_cards_admin_create_invite`) — live ~1 week undetected, same bug class as the documented #0111 incident. PRs #295/#296, merged + live.
- **eq-solves-service**: PR #619 (2026-07-28) turned out to have introduced 3 separate migration-number collisions, not 1 — all fixed. PR #806, merged + live.
- **eq-shell**: added PDF/document preview to the licence review (Flag/Sighted) modal, reusing the existing pdf.js renderer already used elsewhere on the Staff page. PR #1559, merged + live.
- **eq-solves-service**: ran a suite-wide sweep for the same missing-grant bug class across all three canonical planes (jvkn/zaap/ehow). Found a bigger, differently-shaped issue mid-sweep — PUBLIC grants, not just missing `authenticated` — and fixed 8 more functions across 3 follow-up PRs (#807/#808/#809), all merged + live. zaap confirmed clean throughout.

## Decided

- Royce approved applying the licence-photos RLS fix live, and merging every PR above individually as CI cleared.
- Royce ran `/decide` on whether to continue the PUBLIC-grant sweep same-session vs. pause — chose to pause after the first fix (SEC-1, an already-known P0, outranked continuing a self-discovered tangent). Picked it back up later the same session once ready.
- Three functions (`clear_rate_limit`, `eq_record_mint`, `eq_write_audit_log`) were found missing `authenticated` too but deliberately left as-is — no internal caller check, restoring the grant would open a hole, not close one. Royce confirmed this reasoning.
- `get_assets_for_grouping`/`get_distinct_asset_types`: Royce initially said hold (their `anon` grant predated the sweep, might be deliberate). Came back with the actual caller traced (no public-portal consumer found) and Royce approved fixing on the second ask.
- Royce confirmed Zemi's licence photo renders live in the Staff page (click-tested), and asked for the PDF-preview feature once that was confirmed.

## Deferred

- Cards admin worker-upsert/create-invite flow — not yet click-tested live by a real authenticated org admin since either grant fix.
- None of the 9 grant-drift-sweep fixes have been exercised by a real user/cron run since (beyond the one near-miss cron job, which was specifically re-verified).
- `0197_report_settings_per_tier.sql`'s pre-existing, unrelated column bug — surfaced by the migration-collision fix getting further than before, not investigated (matches this workspace's own documented "integration tests aren't a merge blocker" rule).

## Notes

- **Root checkouts across eq-cards/eq-shell/eq-field/eq-solves-service were significantly stale this session** (5 to 92 commits behind `origin/main`) — every fix this session was done from an isolated worktree or a fresh `git archive`/clone extraction, never the stale root. Caught this mid-sweep after an initial pass from a stale checkout produced several false positives (functions already correctly, deliberately revoked by later migrations the stale tree didn't have).
- **A methodology blind spot, not just one bug**: a grant sweep filtered on `grantee IN ('authenticated','anon')` will never see a `GRANT ... TO PUBLIC` — it shows up as the literal string `'PUBLIC'`, not `'anon'`, even though PUBLIC functionally includes anon. This is what turned a routine grant-restore into a suite-wide PUBLIC-grant audit.
- **Near-miss worth remembering**: revoking a PUBLIC grant to close an anon exposure briefly broke a live cron job, because `service_role` had never been explicitly granted either — it had only ever ridden on the same PUBLIC grant. Caught within minutes by chance (a *different* function's investigation happened to require checking `service_role`'s grant). Every fix after this one checked `service_role` first.
- **A live/file drift, self-inflicted and caught**: the near-miss hotfix was applied live via Supabase MCP but never committed to a file — exactly the kind of drift this whole sweep exists to catch. Backfilled the missing migration file in the next PR once noticed.
- Several `service.`-schema functions in eq-solves-service turned out to be dead-code passthroughs to a `public.` sibling — every real caller routes through `createPublicAdminClient()` (service-role, pinned to `public`) specifically to avoid a schema-routing quirk where the default user-auth client routes `.rpc()` calls to `service.*`. Worth knowing before assuming a `service.` function's grants matter at all — check which schema the real caller's client is actually pinned to first.
- eq-solves-service auto-deploys to production on merge to `main`, same as eq-shell — confirmed via live deploy checks after every merge this session, not assumed.
- A mid-session mistake: a command meant to read another branch's files via `git checkout <ref> -- <path>` instead mutated the eq-solves-service root checkout's working tree (that command is not read-only, unlike `git show`). Recovered cleanly (untracked files only, no commits), fixed via `git worktree` for everything afterward, and logged as a durable lesson.
- **A second, near-identical mistake during this very `/close`**: wrote this session's log directly to `sessions/2026-08-23.md` via `Write` without reading it first, silently destroying 1279 lines of real content from other sessions that had already accumulated there that day. Caught immediately via `git diff --stat` against the freshly-cloned `HEAD` (routine "did the write do only what I intended" check, not a destructive-command-specific one), restored with `git checkout -- <path>`, and re-filed correctly here instead. Same underlying lesson as the eq-solves-service one, different command: never `Write` to a path in a shared/multi-session substrate location without a prior `Read`, even when the *protocol itself* says "append below a divider" — that instruction only works if it's actually followed.
