# Session 2026-08-24 — SEC-58 (control-plane ledger) and SEC-65 (AUDIT_SB_KEY label) closed

## Built
- **SEC-58 closed** — `supabase/CONTROL-PLANE-LEDGER.md`'s summary hadn't matched its own changelog since 2026-07-11; 54 files had no ledger row at all. All 54 object-verified against live jvkn (existence, grants, constraints, and a live function-body diff for 3 functions redefined by multiple untracked files). Result: 53 applied, 1 misfiled, 0 pending, 0 not-found — no live gap, the gap was the document. eq-shell [PR #1561](https://github.com/eq-solutions/eq-shell/pull/1561), merged, live.
- **SEC-65 closed (code + inventory)** — `AUDIT_SB_KEY` was documented as a publishable/read-scoped key in 3 eq-field files; it's actually the ehow service_role key, fingerprint-matched to the already-tracked 4-alias service_role cluster in `ops/secrets-inventory.md` (now 5 aliases). Comments corrected, inventory folded in, register note added. eq-field [PR #762](https://github.com/eq-solutions/eq-field/pull/762), merged, live.
- Both PRs merged on Royce's "merge" — confirmed via `gh pr view` mergedAt/state, not just merge-command exit status.

## Decided
- Royce confirmed which open security items to actually build this round (SEC-58 + SEC-65) out of 4 surfaced (SEC-57/60/61/63 left untouched — GitHub App trust call, org policy sign-off, and a Netlify-dashboard-only check aren't executable from a coding session).
- Royce ran `/decide then go` on the SEC-58 plan before it started — six-step pass held up (no irreversible risk: read-only against jvkn, docs-only output), proceeded without a second confirmation round.
- `2026_07_11_tender_tables_anon_lockdown.sql` confirmed misfiled (targets ehow/zaap, not jvkn) — same class as the 3 already-tombstoned files from the 2026-07-11 baseline, not a new problem.

## Deferred
- **eq-field PR #761's migration (identity upward-push trigger) — live-apply on ehow not independently confirmed this pass.** The two out-of-band secrets it needed are now both created and the eq-shell receiver end verified live, but that doesn't prove the ehow-side trigger body itself is live. Needs a direct `pg_get_functiondef` check or a real Field roster edit to confirm the pipe actually fires.
- **SEC-65's exposure question still open** — whether `AUDIT_SB_KEY` actually ships client-side in any of its 3 non-Sentry consumers. Label corrected; exposure not tested.
- SEC-57 (org-wide GitHub App `grok-by-xai` holds near-admin write access), SEC-60 (org/repo CI-CD hardening gaps), SEC-61 (dev-context secret leak, 22 vars across 4 repos), SEC-63 (account-scope Netlify secret signs every session in the suite, site-scope unconfirmed) — all still open, all need Royce's own call or click, not more building. Deliberately not touched this session.

## Notes
- The brief-gate (`guard.js`) blocked the first ledger edit — this session's SEC-58 work had pivoted in from a conversational "sprint the next round" without an explicit `/brief eq-shell` first. Ran the brief properly mid-task rather than bypassing with `EQ_SKIP_BRIEF`; worth remembering that a task growing organically out of chat still needs the gate, not just tasks that start that way.
- `ops/security-register.md`'s live content (fetched fresh via `gh api ...contents?ref=main`, not the local clone) was the actual source for this session's SEC-58/65 work — the local `C:\Projects\eq-context` clone was confirmed behind origin/main (4-6 commits, at different points this session) every time it was checked. The gate's own SYNC warning was accurate and load-bearing here, not noise.
- Verification methodology for SEC-58: where more than one untracked file redefines the same function (`eq_worker_completeness`, `eq_worker_compliance_status`, `eq_get_tenant_user`), existence alone can't tell you which file is "current" — pulled the live `pg_get_functiondef` body and matched it against each candidate file's distinguishing content instead of assuming the latest filename wins. Caught real supersession chains this way (e.g. `eq_get_tenant_user` got 3 same-day rewrites).

---

# Session 2026-08-24 — who can see Staff Conversations (Q&A only, no code changed)

## Built
- Nothing shipped — pure read-only investigation, answered in chat.

## Decided
- None new — this confirmed an existing 2026-08-19 design decision (creator-only reads, opt-in-only grant) rather than changing anything.

## Deferred
- None.

## Notes
- Royce asked who can see the "Conversations" feature on a staff record. Answered live, not from memory: exactly 2 people platform-wide hold `staff.manage_conversations` — Royce (added 2026-08-17) and Luke Johnson (added 2026-08-23) — both via one Custom Group ("Staff Conversations", SKS-tenant-scoped) in `shell_control.user_security_groups`/`security_group_perms`. `shell_control.tenant_role_overrides` has zero rows for this key — no role grants it by default, matching the deliberate design documented inline in `src/permissions/matrix.ts` and confirmed already-declined-to-change at `eq/pending/eq-shell.md:330` (Royce, 2026-08-16: "leave group-opt-in only, no role default").
- **Real trap avoided**: the RLS policy reads `auth.jwt() -> 'app_metadata' -> 'extra_perms'`, but that claim is computed FRESH at each JWT mint (`mint-supabase-jwt.ts`, merging security-group perms + role overrides) — it is NOT persisted to `auth.users.raw_app_meta_data`. A first query against `raw_app_meta_data` came back empty (zero users), which would have been reported as "nobody can see it" — wrong. Traced the real mint-time query path before answering.
- Also surfaced a nuance likely not obvious to Royce: the single `cmd: ALL` RLS policy on `app_data.staff_conversations` applies `created_by = auth.uid()` to READS too, not just writes — so even between the two people who hold the permission, neither can see the other's entries. Confirmed intentional via the table's own header comment (`ConversationsSection.tsx`), not assumed.
- Session resumed mid-close from the prior day (2026-08-23 work, already logged there) — this file starts fresh for 2026-08-24 per the date rollover.
- **Interrupted mid-`/close` by a new report**: Royce flagged Staff-page navigation as slow ("clicking the staff list seems to take an eternity") right as this close was running. Not investigated in this session — closing out cleanly first, per the user's own message arriving as a mid-turn interrupt rather than a blocking question. Worth the next session's first move: `staff-bootstrap.ts`'s own header already documents a ~3.0-3.3s cold-start cost per function and a fan-out of 8 previously-separate endpoints it was built to collapse into one — check whether that endpoint itself is now slow (regressed, or never actually wired into a cold path) before assuming a new cause.

---

# Session 2026-08-23 (cont. into 08-24) — Zemi Asri's licence photo fixed for good, two grant-restoration incidents, a suite-wide security sweep with a near-miss, and a small UI feature

*Session started 2026-08-23, ran past midnight UTC — bulk of the work and all "today" references inside it are 2026-08-23; logged here under 08-24 per `/close`'s own "YYYY-MM-DD = today (when /close runs)" rule, not backdated into the already-existing 2026-08-23.md.*

## Built

- **eq-cards**: swept all 205 live `licences` rows for the same storage-path-never-follows-an-identity-merge bug behind Zemi Asri's original report; fixed 6, root-caused the class so it can't recur (RLS now checks the live `licences.user_id`, not the frozen path). Migration `0137`, PR #294, merged + live.
- **eq-cards**: restored `authenticated` grants silently dropped by migration `0131` on two admin RPCs (`eq_cards_admin_upsert_worker`, `eq_cards_admin_create_invite`) — live ~1 week undetected, same bug class as the documented #0111 incident. PRs #295/#296, merged + live.
- **eq-solves-service**: PR #619 (2026-07-28) turned out to have introduced 3 separate migration-number collisions, not 1 — all fixed. PR #806, merged + live.
- **eq-shell**: added PDF/document preview to the licence review (Flag/Sighted) modal, reusing the existing pdf.js renderer already used elsewhere on the Staff page. PR #1559, merged + live.
- **eq-solves-service**: ran a suite-wide sweep for the same missing-grant bug class across all three canonical planes (jvkn/zaap/ehow). Found a bigger, differently-shaped issue mid-sweep — PUBLIC grants, not just missing `authenticated` — and fixed 8 more functions across 3 follow-up PRs (#807/#808/#809), all merged + live. zaap confirmed clean throughout.

## Decided

- Royce approved applying the licence-photos RLS fix live, and merging every PR above individually as CI cleared.
- Royce ran `/decide` on whether to continue the PUBLIC-grant sweep same-session vs. pause — chose to pause after the first fix (SEC-1, an already-known P0, outranked continuing a self-discovered tangent). Picked it back up later the same session once ready.
- Three functions (`clear_rate_limit`, `eq_record_mint`, `eq_write_audit_log`) were found missing `authenticated` too but deliberately left as-is — no internal caller check, restoring the grant would open a hole, not close one. Royce confirmed this reasoning.
- `get_assets_for_grouping`/`get_distinct_asset_types`: Royce initially said hold (their `anon` grant predated the sweep, might be deliberate). Came back with the actual caller traced (no public-portal consumer found) and Royce approved fixing on the second ask.
- Royce confirmed Zemi's licence photo renders live in the Staff page (click-tested), and asked for the PDF-preview feature once that was confirmed.

## Deferred

- Cards admin worker-upsert/create-invite flow — not yet click-tested live by a real authenticated org admin since either grant fix.
- None of the 9 grant-drift-sweep fixes have been exercised by a real user/cron run since (beyond the one near-miss cron job, which was specifically re-verified).
- `0197_report_settings_per_tier.sql`'s pre-existing, unrelated column bug — surfaced by the migration-collision fix getting further than before, not investigated (matches this workspace's own documented "integration tests aren't a merge blocker" rule).

## Notes

- **Root checkouts across eq-cards/eq-shell/eq-field/eq-solves-service were significantly stale this session** (5 to 92 commits behind `origin/main`) — every fix this session was done from an isolated worktree or a fresh `git archive`/clone extraction, never the stale root. Caught this mid-sweep after an initial pass from a stale checkout produced several false positives (functions already correctly, deliberately revoked by later migrations the stale tree didn't have).
- **A methodology blind spot, not just one bug**: a grant sweep filtered on `grantee IN ('authenticated','anon')` will never see a `GRANT ... TO PUBLIC` — it shows up as the literal string `'PUBLIC'`, not `'anon'`, even though PUBLIC functionally includes anon. This is what turned a routine grant-restore into a suite-wide PUBLIC-grant audit.
- **Near-miss worth remembering**: revoking a PUBLIC grant to close an anon exposure briefly broke a live cron job, because `service_role` had never been explicitly granted either — it had only ever ridden on the same PUBLIC grant. Caught within minutes by chance (a *different* function's investigation happened to require checking `service_role`'s grant). Every fix after this one checked `service_role` first.
- **A live/file drift, self-inflicted and caught**: the near-miss hotfix was applied live via Supabase MCP but never committed to a file — exactly the kind of drift this whole sweep exists to catch. Backfilled the missing migration file in the next PR once noticed.
- Several `service.`-schema functions in eq-solves-service turned out to be dead-code passthroughs to a `public.` sibling — every real caller routes through `createPublicAdminClient()` (service-role, pinned to `public`) specifically to avoid a schema-routing quirk where the default user-auth client routes `.rpc()` calls to `service.*`. Worth knowing before assuming a `service.` function's grants matter at all — check which schema the real caller's client is actually pinned to first.
- eq-solves-service auto-deploys to production on merge to `main`, same as eq-shell — confirmed via live deploy checks after every merge this session, not assumed.
- A mid-session mistake: a command meant to read another branch's files via `git checkout <ref> -- <path>` instead mutated the eq-solves-service root checkout's working tree (that command is not read-only, unlike `git show`). Recovered cleanly (untracked files only, no commits), fixed via `git worktree` for everything afterward, and logged as a durable lesson.
- **A second, near-identical mistake during this very `/close`**: wrote this session's log directly to `sessions/2026-08-23.md` via `Write` without reading it first, silently destroying 1279 lines of real content from other sessions that had already accumulated there that day. Caught immediately via `git diff --stat` against the freshly-cloned `HEAD` (routine "did the write do only what I intended" check, not a destructive-command-specific one), restored with `git checkout -- <path>`, and re-filed correctly here instead. Same underlying lesson as the eq-solves-service one, different command: never `Write` to a path in a shared/multi-session substrate location without a prior `Read`, even when the *protocol itself* says "append below a divider" — that instruction only works if it's actually followed.

---

# Session 2026-08-24 (cont'd) — secrets-org-hardening-sprint: SEC-61 closed, SEC-63 resolved, SEC-60 built to scope

## Built
- **SEC-61 closed live** — all 21 leaking Netlify `dev`-context secrets (eq-shell 9, eq-service 9, eq-field 2, eq-cards 1) fixed via `deleteEnvVarValue`, targeting each context row's own id directly rather than the CLI's `--context` flag (not independently verified to be equally precise). Verified per-site afterward: 0 secret-flagged vars leaking `dev`. Spot-checked the most sensitive one (eq-shell's DB service-role key) to confirm production/preview/branch-deploy untouched — same ids, same masked values, same `updated_at`.
- **SEC-63's site-scope question resolved without the dashboard** — the original finding couldn't determine via API whether the account-scope `SUPABASE_JWT_SECRET` reached sks-nsw-labour (a separate entity). Queried all 10 sites on the `milmlow` Netlify account individually; it reaches only eq-shell/eq-service/eq-field, matching the original value-fingerprint match exactly via a second, independent method. Confirmed P1, not P0 — no cross-entity leak.
- **SEC-60 built to Royce's explicit scope** (2 of 4 gaps): branch protection added on eq-service `main` (requires `Typecheck + audit` + `tsc + next build`, no reviews, `enforce_admins: false`); secret scanning + push protection enabled on all 3 public repos (eq-context, eq-solves-intake, sks-nsw-labour). Both verified live via `gh api` re-check.
- `ops/security-register.md` and `docs/secrets-org-hardening-sprint.md` (eq-shell) both updated per finding, in step with the live work, not batched at the end.
- 3 small doc-status PRs merged: eq-shell #1565, #1566, #1568.

## Decided
- Royce: fix SEC-61 and SEC-63 first before SEC-60 (bare "start on SEC-61" / "start on SEC-63" instructions, executed directly).
- Royce, on SEC-60's scope (asked via a multi-select question since the sprint doc itself flagged this as a workflow-preference call, not a pure security judgment): branch protection on eq-service + secret scanning on the 3 public repos. Explicitly NOT: org-wide 2FA, the other 5 repos' branch protection, Action SHA-pinning.
- Royce, on SEC-63's remaining `dev`-context leak (the one delete call the session's own classifier blocked): he'll do it himself via the Netlify dashboard rather than granting a permission rule for a retry.

## Deferred
- SEC-63's `dev`-context leak on the account-scope var — Royce's own 2-minute dashboard action, not yet confirmed done.
- SEC-60's other 3 gaps (2FA, 5 more repos' branch protection, SHA-pinning) — deliberately out of scope this round, a real future pass.

## Notes
- **A live classifier block, handled correctly rather than routed around**: `deleteEnvVarValue` without a `site_id` (the account-scope case) was refused by the coding session's own auto-mode classifier, even though the operation is identical in kind to the 21 site-scoped deletes that had just succeeded. Per the classifier's own instruction, stopped and asked Royce rather than trying an alternate tool path to the same end. Worth remembering: account-scope Netlify API calls appear to read as broader/riskier to the classifier than site-scoped ones, independent of the actual blast radius of the specific operation.
- **A real near-miss avoided by checking, not assuming, before applying branch protection**: eq-service's main shows 11 CI check names in its commit history, but a live PR only ever triggers 6 of them — the other 5 are push-to-main/deploy-triggered and would never show a passing state on a PR. Requiring any of those 5 would have made every future merge to eq-service impossible. Of the 6 PR-triggered checks, one (integration tests) is this workspace's own documented pre-existing failure and three are Netlify's informational plugin checks, not real gates. Confirmed via a real open PR's (`#791`) actual check list before writing the protection payload, not from the job-name list alone.
- The `gh api` branch-protection/security-and-analysis payloads needed a proper JSON file via `--input`, not `-f`/`-F` flags — nested boolean fields (`required_status_checks.strict`) get sent as the string `"false"` instead of a JSON boolean through the flag syntax, which GitHub's schema validator rejects outright.

---

# Session 2026-08-24 — Conor Horgan's OTP login gap traced to a always-broken resend-worker-invite.ts; fix live, but Nelson's retry still unconfirmed

## Built
- **Root-caused Conor Horgan's (SKS labour hire) OTP login failure** — not a bug. The bare `/login` page deliberately never self-provisions (`shouldCreateUser:false`, anti-SMS-toll-fraud gate) and Conor had zero Shell/Supabase-auth footprint yet. Working as designed; the real gap was that he'd never been provisioned a login door.
- **Found and fixed a genuine, always-reproducing bug while locating the actual fix**: `resend-worker-invite.ts` did an unconditional `INSERT` into `worker_invites`, which collided with the `worker_invites_org_worker_unclaimed_unique` partial index every time it was used for its own documented purpose (refreshing a pending/expired invite — exactly the case where an unclaimed row already exists). Fixed to mirror `create-worker-invite.ts`'s existing pattern: look up the existing unclaimed row, `UPDATE` it in place, only `INSERT` when the worker's most recent invite was already claimed. eq-shell [PR #1562](https://github.com/eq-solutions/eq-shell/pull/1562), merged, confirmed live via exact Netlify `commit_ref` match.
- Confirmed via digest-hash comparison that the deployed function bundle genuinely changed (ruling out stale-cache), and via `git show origin/main:...` that the merged code exactly matches what shipped (ruling out merge drift).

## Decided
- Royce: "Yep, go ahead and fix it" — explicit go-ahead for the resend-worker-invite fix.
- Confirmed for the record: Resend delivers by email (`sendClaimEmail`), not SMS/text — Royce asked mid-session, the UI doesn't say.

## Deferred
- **Nelson Sareto's Resend click reproduced the identical duplicate-key error after the fix was confirmed live** — reported by Royce directly. Investigated: no fresh Postgres duplicate-key log entry appears anywhere after the deploy's publish timestamp, and Nelson's `worker_invites` row is completely unchanged (same token, same expiry) — meaning the `UPDATE` branch never actually ran for that click. This points toward the error banner being stale/left over from his earlier pre-fix click rather than a genuinely new failure, but that is **not confirmed** — asked Royce to refresh the page and retry; no response had arrived by session close. If a refreshed retry still fails, the leading unverified hypothesis is that the new `unclaimed` SELECT's `error` is silently discarded (never checked), so a failed lookup could fall through to the old broken `INSERT` path — needs either Netlify function-invocation log evidence or a defensive fix (surface `unclaimedErr` explicitly) to confirm or rule out. _(added 2026-08-24)_
- **Conor Horgan's own Resend has not actually been retried since the fix shipped** — only Nelson's was attempted this session, with an ambiguous result (see above). The pre-existing 2026-08-23 deferred item ("Royce still needs to click Resend for Conor Horgan and Nelson Sareto") stays open pending both. _(added 2026-08-24)_

## Notes
- This is a direct continuation of the 2026-08-23 "labour-hire licence promotion gap" entry's own deferred item — Royce clicking Resend on Conor's existing pending invite (looking for the fix that entry called for) is exactly what surfaced this separate, previously-undiscovered bug in `resend-worker-invite.ts` itself.
- `create-worker-invite.ts` already had the correct existing-unclaimed-row check, plus an explicit `23505` race-recovery fallback naming this exact index by comment — `resend-worker-invite.ts` was never brought in line after the unique index shipped (migration 0118), despite its own header comment describing precisely the scenario (pending/expired invite) that guarantees the collision.
- Clarified a three-way naming trap while diagnosing: eq-shell has three separate admin surfaces that all read as "the labour hire link" — `admin/workers/labour-hire-links` (documents only, no login), `admin/workers/join-links` (real self-serve login), and the pre-existing `admin/workers` Worker Invites hub (resend an already-sent invite). Check the hub first for an existing pending invite before reaching for either of the other two doors. Recorded in the eq-shell Claude memory store (`labour-hire-links-two-pages-naming-trap`) for future sessions.
- The eq-shell memory store's `resend-worker-invite-unclaimed-unique-bug` entry needs to stay flagged unresolved (Nelson's retry outcome) rather than closed — corrected in the same pass as this log entry so a future session doesn't read it as fully done.
---

# Session 2026-08-24 (cont'd) — 3 already-applied ehow migrations reconciled into git (PR #764)

## Built
- Reconciled 3 untracked-but-live ehow RLS migrations (JWT tenant-gate hardening across 25 tables: the apprentice/audit/notes cluster plus a 15-table second wave covering tenders, site audits, acknowledgments, job numbers, and more) into eq-field's git history. eq-field [PR #764](https://github.com/eq-solutions/eq-field/pull/764), merged, live (docs-only, no app code).
- Corrected a stale "DRAFT — NOT APPLIED" header in one of the three files, left over from before the hand-apply.
- Cleaned up the now-redundant untracked originals from the shared eq-field root checkout.

## Decided
- Royce: "merge it" — explicit go to squash-merge PR #764 (docs-only, but still eq-field's "explicit instruction only" deploy rule per global CLAUDE.md, since a merge to `main` there auto-deploys).
- Royce: "yes, clean those up" — explicit go to delete the 3 untracked originals from the shared root checkout.

## Deferred
- The recurring pattern itself — this is the 2nd confirmed instance in this repo of "applied via Supabase MCP, never committed." No tooling fix built this session; flagged as a new open item in `eq/pending/eq-field.md` instead of being fixed-and-forgotten again.

## Notes
- The task arrived with ledger timestamps already asserted as fact — re-verified independently against live `pg_policies` for all 25 tables rather than trusting the claim at face value. It held up, but one detail didn't (the stale DRAFT header), which is exactly the kind of thing blind trust would have shipped uncorrected.
- Real collision checked and ruled out: `git branch -a` surfaced an unrelated, unpushed ~2-month-old branch (`claude/notes-jwt-gate-fix`) whose name looked related to this task. Checked its actual log/diff before proceeding rather than assuming overlap — dead branch from the original notes-table creation, unrelated.
- Real collision checked and ruled out again at `/close`: the shared `eq-context` root checkout had uncommitted local changes to `eq/changelog/eq-shell.md`/`eq/pending/eq-shell.md` from what's almost certainly a concurrent session, and origin/main's incoming commits (22 behind, then 24 by the time it was rechecked) touched those same two files. Did not touch, stash, or attempt to reconcile that — ran this close from an isolated worktree (`eq-context-close-2026-08-24-wt`) instead, mirroring the worktree convention eq-field's own CLAUDE.md already mandates for its shared-root hazard.
- Checked SEC-1 (P0, digest-surfaced, "public key reads people/timesheets/leave_requests/audit_log") for a substrate conflict against this session's audit_log-touching migration — false alarm, confirmed by reading SEC-1's own detail section: it's sks-nsw-labour's own table on a fully separate project, not ehow's.
- Two new memory files banked in `~/.claude/projects/C--Projects-eq-field/memory/` (session memory, outside this substrate): the recurring applied-but-uncommitted-migration pattern, cross-linked to the existing `field_people_iud_coordination_gap` memory since both are flavors of the same "hand-apply workflow loses sync with git" root issue, though the mechanisms differ.

---

# Session 2026-08-24 (cont'd) — field_people_iud cross-repo migration coordination gap closed, restoration dispatched live

## Built
- Closed the field_people_iud cross-repo migration coordination gap — full detail in `eq/pending/cross-repo.md`'s 2026-08-24 entry, not duplicated here. Summary: eq-context IDENTITY-MODEL.md §3.3.3 registry (PR #176), eq-shell live-diff warning in `migrate-tenants.mjs` (PR #1569), eq-field CI lint + superseded-migration marker (PR #763). All merged, all confirmed live — eq-shell's via commit-ancestry check against the newest `ready`/`production` deploy, not just merge status.
- Confirmed migration `0273` (the actual restoration, found already built as eq-shell PR #1567 by a concurrent session before this one started building anything) is dispatched and live — Royce triggered `tenant-migrate.yml` directly. This closes the open item from this same file's earlier 2026-08-24 entry above ("SEC-58 and SEC-65 closed" → Deferred: "eq-field PR #761's migration... live-apply on ehow not independently confirmed this pass") — a fresh `pg_get_functiondef` pull confirms all 3 security gates plus the upward-push block, correctly merged.

## Decided
- Royce: "Draft the fix now" (asked via AskUserQuestion) — build the process fix as draft PRs across all 3 repos, explicitly not dispatching anything to ehow as part of that pass.
- Royce: "merge" — merged all 3 PRs.
- Royce: "merge and dispatch if safe" — by the time this was checked, Royce had already dispatched migration `0273` himself directly; nothing left for this session to dispatch.

## Deferred
- Background task `task_33c7272c` ("commit 3 untracked-but-applied migrations in eq-field's shared checkout") — spawned this session, Royce started it in a separate session, still running independently as of this close.

## Notes
- **Before writing any code**, `git branch -a` on eq-shell — forced by its own brief-gate hook, which blocked the first edit attempt with "brief not run this session" — surfaced eq-shell PR #1567: a concurrent session had already built the exact restoration migration this session was about to author. Avoided duplicating it. The same check surfaced several other in-flight identity/actor-identity branches across eq-shell and eq-field, real evidence of how much concurrent traffic this seam is under right now, not a hypothetical risk.
- The `git merge --ff-only` `pre_tool_use` hook (F9) correctly blocked an attempt to fast-forward the shared `C:\Projects\eq-context` root directly — used an isolated worktree instead, same convention eq-field's own CLAUDE.md already documents, now confirmed enforced here too.
- "Verify live, don't trust the task's own framing" paid off twice on the same investigation: the task description as given said 0270/0271 were "unrelated" fixes and implied PR #761 had been live-clobbered — both wrong once checked against actual timestamps and both migration ledgers. PR #761 was never applied at all; it just sat stale.
- Third documented instance of the same root cause in this codebase (`CREATE OR REPLACE` silently dropping more than the diff shows) — after the `security_invoker` view-clause loss (eq-field, 3 incidents) and the function-grant loss via `eq_enforce_function_privacy` (eq-shell, Cards signup outage). Worth citing as a pattern if it recurs on a fourth object type.

---

# Session 2026-08-24 — field_people_iud() identity-push collision found and fixed; job_title + emergency-contact structure parity built

## Built
- Closed out 2026-08-23's identity-sync work: eq-shell #1555 (DOB/address push + receiver), eq-cards #293 (`employment_type`), eq-field #761 (Field's own upward push) — all merged, deployed, verified live.
- Confirmed both required secrets (`field_identity_push_secret` on ehow's vault, `FIELD_IDENTITY_PUSH_SECRET` on eq-shell's Netlify env) already existed from the crashed prior session, values matching.
- Found the push wasn't actually live despite all of the above: eq-shell's own tenant-migrations `0270`/`0271` (applied the same day, unrelated permission-gate fixes) independently rebuilt `app_data.field_people_iud()` from a pre-push baseline, silently dropping PR #761's push code on `CREATE OR REPLACE FUNCTION`. Confirmed via `pg_get_functiondef` plus empty `net.http_request_queue`/`net._http_response` for a real linked-worker edit (William Brown).
- Migration `0273` (eq-shell [PR #1567](https://github.com/eq-solutions/eq-shell/pull/1567)): restores the push on top of `0271`'s live body. Dry-run validated in a rolled-back transaction, dispatched, independently reconfirmed live.
- Found two more real gaps verifying `0273` on a real edit (Richard Brown): `job_title` silently dropped by the trigger despite being a real column the UI already sends; emergency contact stuck as one free-text field while jvkn/ehow's own `staff` table already carry 3 structured columns, already kept in sync from Shell-side edits by `workers-canonical-sync`.
- Migration `0274` (eq-shell [PR #1572](https://github.com/eq-solutions/eq-shell/pull/1572)) + companion eq-field [PR #765](https://github.com/eq-solutions/eq-field/pull/765) (v3.5.548): view gains 2 columns, trigger writes `job_title` + pushes all 3 emergency fields, Edit Person modal split into Name/Mobile/Relationship. Both dry-run/`tsc`-validated, dispatched, reconfirmed live.
- Spawned background task `task_cf4935f9` → landed same session (eq-shell [PR #1569](https://github.com/eq-solutions/eq-shell/pull/1569)): `migrate-tenants.mjs` now logs the live definition of any shared-registry function a migration replaces before every dispatch, so this exact silent-clobber class gets caught at review time going forward.

## Decided
- Royce: "go ahead and create the secrets" — found already created by the crashed session; verified rather than recreated.
- Royce, via `/decide`: ship the trigger patch (`0273`) now rather than restructure Field's write path off the DB trigger entirely — proportionate to the actual failure (a one-time coordination blind spot, not a design flaw); the restructure idea is tracked, not built.
- Royce: "combine it... build it... 100/100 outcome" — full 3-field emergency-contact structure plus the `job_title` fix, not a minimal patch.
- Royce: merge eq-field before dispatching the migration (accepting a brief live-save-broken window on Edit Person, closed by dispatching immediately after) over dispatching first or holding eq-field back.

## Deferred
- Real click-test on a linked SKS person — `job_title` + all 3 emergency-contact fields + the push actually reaching jvkn — needs an authenticated SKS Core session, not something this session can do. Tracked in `eq/pending/eq-shell.md` and `eq/pending/eq-field.md`.
- The underlying structural gap (eq-field's own migrations and eq-shell's tenant-migrations can both still touch shared functions with no single source of truth beyond the new warning) — the warning mechanism shipped tonight; a deeper architectural fix was explicitly scoped out as disproportionate to this task.

## Notes
- Session began mid-crash-recovery: a prior session had completed real work (both secrets, 3 PR merges) with no report ever reaching the user or this conversation. Verified everything against live state before assuming anything was still open — a status-dashboard screenshot the user shared was itself already stale by the time it was shown.
- A concurrent session was independently investigating this exact same collision tonight (see the entry directly above this one in this file) — it checked `git branch -a` before writing code, found this session's PR #1567 already built, and correctly avoided duplicating it. That session's own log attributes migration `0273`'s dispatch to "Royce himself"; from this session's direct, first-hand vantage point the dispatch was run from here (`gh workflow run tenant-migrate.yml`, watched to completion, independently reconfirmed live against ehow), so that attribution is very likely a same-night cross-session mix-up rather than a second real dispatch. Not corrected there — noted here for whoever reads both.
- Third instance in this codebase of `CREATE OR REPLACE` silently dropping more than the visible diff shows (per the other concurrent session's own note above) — worth treating as an established pattern, not a one-off, if it recurs on a fourth object type.

---

# Session 2026-08-24 (cont'd) — untracked-migration check automated, closing the item this file's own earlier "3 already-applied ehow migrations reconciled" entry flagged and deferred today

## Built
- Closed the open item from this file's earlier 2026-08-24 entry above ("3 already-applied ehow migrations reconciled into git (PR #764)") — its own Deferred section reads "the recurring pattern itself... No tooling fix built this session; flagged as a new open item in `eq/pending/eq-field.md` instead of being fixed-and-forgotten again." This session built that guard.
- Three complementary, non-redundant surfaces — each covers a trigger gap the others structurally can't:
  - `/brief`'s Step 3.5 (eq-field only, `~/.claude/commands/brief.md`) — detects untracked `supabase/migrations/*.sql`, checks the live Supabase ledger (`list_migrations` on ehow + zaap), classifies each as APPLIED-BUT-UNCOMMITTED / DRAFT / UNKNOWN. Fires whenever `/brief` runs, which the pre-existing `brief-gate` hook already forces before most eq-field building.
  - `guard.js` rule `warn-untracked-migrations` (`~/.claude/hooks/guard.js`) — lightweight commit-time backstop. Can't call the Supabase MCP from a detached hook subprocess, so it just names the files and points at the ledger. Always warn-only (`blockable: false`) — structurally cannot block, even under `EQ_GUARD_MODE=block`. Catches a mid-session hand-apply even if `/brief` was skipped.
  - `ddl_migration_gate.py` (`~/.claude/hooks/ddl_migration_gate.py`) — this Stop hook already existed for a closely related failure shape (built 2026-07-30, for the `security_invoker`-class bug), but its logic is session-scoped by construction and structurally cannot catch a migration applied in an *earlier* session — exactly this bug's shape. Extended with an unconditional sweep independent of session_id/guard.log/this-session's-own-DDL-activity; fires at the end of every session regardless of what that session did.
- `selftest.js` +6 regression cases (fires on untracked, not on tracked, not outside eq-field, never blocks even in block mode) — 24/24 passing.
- Found and fixed two real bugs while building this: `git status`'s default `--untracked-files=normal` silently collapses a wholly-untracked directory to one dirless line, no `.sql` suffix to filter on (didn't reproduce against the real eq-field repo, which never collapses since it already has hundreds of tracked files in that directory — caught only by the new selftest.js fixture, a from-scratch repo); a test-isolation confound between the new rule's "never blocks" check and the pre-existing `gate-outbound` rule (both match `git commit`).
- Live-verified end-to-end against the real eq-field repo: created a throwaway untracked `.sql`, confirmed all three surfaces fired correctly and brief.md's ledger-check logic correctly classified it DRAFT (no match on either tenant, as expected since it was never applied anywhere), then deleted it.
- Lives in `Milmlow/claude-code-config` (Royce's personal Claude Code tooling repo, `C:\Users\EQ\.claude` — not any EQ product repo). Commit `d660c99`, merged to master, pushed. No eq-field PR, no deploy — this session made zero net changes to any product repo's own files.

## Decided
- Royce: "merge to master, push" (asked via AskUserQuestion) — `~/.claude` turned out to be a real git repo (`Milmlow/claude-code-config`), not the untracked personal-config directory the task's own instructions anticipated as the likely case; its own fallback path ("if actually tracked, treat normally: branch, don't commit straight to main") correctly covered this.
- Design call, not asked: build all three surfaces rather than only the one the task named explicitly (brief.md). Each covers a distinct trigger gap the others can't — found by reading guard.js's existing rules end-to-end and discovering ddl_migration_gate.py already existed for a closely related, narrower failure shape.

## Deferred
- None — task was fully scoped and completed within this session.

## Notes
- `~/.claude/commands/brief.md` and `~/.claude/hooks/*` are not inside any EQ suite repo or this eq-context substrate — they live in a separate personal-tooling git repo outside `C:\Projects`. Confirmed via `git remote -v` before editing, per the task's own explicit instruction to check first.
- `ddl_migration_gate.py` was genuine prior art, not just adjacent code — its own docstring names the *exact* failure mode ("hand-apply via MCP, then the migration file never makes it back into git"). Its session-scoping (`sid != session_id` filtering on `guard.log` lines) is precisely why it couldn't catch this specific incident: the 3 migrations were applied in one session and discovered a full day later in an unrelated one that touched no SQL at all. Worth checking scoping assumptions, not just presence/absence, next time an existing gate is asked "why didn't this catch it."
- The Bash tool silently ate every backslash from a hand-escaped Windows path inside an ad-hoc `node -e` one-liner (`'C:\\Projects\\eq-field'` → `C:Projectseq-field`, confirmed via a byte-level `charCodeAt` comparison against `process.cwd()` reporting the correct value for the same directory), producing a downstream `ENOENT` that read like a PATH/environment bug rather than string corruption. Cost real debugging time before being isolated to a script-file rewrite. Banked as its own memory (`reference_bash_tool_windows_path_escaping`) since it's a general Bash-tool gotcha, not specific to this task.
- `C:\Projects\eq-context` was 13 commits behind `origin/main` at session start, and its own `pre_tool_use.py` hook (F9, rung 4) blocked a plain `git merge --ff-only` in the shared checkout outright. Cloned fresh into scratch instead, per the hook's own suggested remedy — same isolation pattern several other sessions logged above already used today for the identical reason.

---

# Session 2026-08-24 (cont'd) — Staff-page navigation slowness: two root causes found and fixed live

## Built
- Root-caused and fixed the Staff-page navigation slowness Royce flagged mid-`/close` earlier today (see this file's own "who can see Staff Conversations" entry above, whose Notes section handed this off as "next session's first move").
- **Cause #1**: the scheduled warm-ping never warmed anything past the Lambda cold-start layer, because every ping was unauthenticated and got stopped by the session guard on every one of 6 hot endpoints — so the tenant-plane Supabase client cache (a real DB routing lookup + AES decrypt + `createClient()` per tenant) was never actually pre-warmed. Fixed via a shared-secret header the ping now sends, checked before session verification. eq-shell PR #1564, merged, confirmed live.
- **Cause #2, found while verifying #1564 against a real trace**: `token-exchange.ts` awaited its audit-log write unbounded — the only iframe-SSO minter not using the existing `writeAuditLogBounded` (1.5s ceiling, built for login, never applied here). Swapped it over. eq-shell PR #1570, merged, confirmed live.
- Full detail (root cause, fix mechanism, verification): `eq/pending/eq-shell.md`, 2026-08-24 top entry.

## Decided
- Royce: "go ahead" — authorized both setting the new `WARM_PING_SECRET` env var and merging PR #1564.
- Royce: "merge it" — explicit merge authorization for PR #1570, after confirming CI green.

## Deferred
- Genuine before/after trace comparison — not yet done; needs both fixes to have had time to actually warm production containers.
- Full HAR export from Royce — only had a pasted totals table this session.
- Real click-test of perceived speed — verified via deploy state and commit ancestry only.

## Notes
- Ruled out PR #1135 (merged 2026-07-30, "warm token-exchange to close the last cold-start gap") as prior art before building the #1570 fix — read its actual diff; it only added `token-exchange` to the ping *list*, never touched the audit-log-await pattern. No duplicate work.
- The inconsistent 1.72s/4.32s timing for the same `token-exchange` endpoint in one real trace was the key signal pointing at cause #2 — a fixed cold-start cost repeats consistently; a stalled unbounded write doesn't.
- Root's own checkout (`claude/platform-console-cards-core-reconcile`) was 106 commits behind `origin/main`, with unrelated uncommitted work already sitting in it (`entity-patch.ts`, `AdminDocumentUpload.tsx`) — left untouched; both fixes were built from fresh isolated worktrees off current `origin/main` instead.
- Hit a transient local DNS outage mid-deploy-verification (`api.netlify.com` briefly unresolvable) — confirmed unrelated to the deploy itself (a general DNS issue, `github.com` equally affected) and self-resolved within a couple of minutes on retry.

---

# Session 2026-08-24 (cont.) — Restore found broken twice more after last close, root-caused + fixed live (staff_guard_reactivation), a second collision investigated and cleared

## Built
- Live-tested archive/restore in the real EQ Field app with Royce after last session's role-gate fix (PR #1553) shipped. Archive worked; restore did not, across three attempts.
- Root-caused via: decoding Royce's actual live Field session JWT (ruled out role/claims), reproducing the failure in a simulated-claims transaction directly against the real trigger + view (ruled out session/browser), then finding a second, pre-existing `BEFORE UPDATE` trigger — `app_data.staff_guard_reactivation` — via `pg_trigger` on `app_data.staff`. It silently reverts `active` back to `false` on any false→true flip unless `activated_by`/`activated_at` are also stamped in the same statement. Neither `field_people_iud()` nor `field_people_removed_iud()` ever did — no Field-driven reactivation has ever actually taken effect, for any role, independent of tonight's or last session's role gates.
- Migration `0274_field_people_reactivation_audit_stamp.sql` — stamps `activated_by` (from the JWT's `app_metadata.actor_id`, not `sub`) and `activated_at` on the real transition only, matching `field_approved_by`/`field_approved_at`'s existing shape. Verified correct in a `BEGIN...ROLLBACK` transaction against ehow (simulated Royce's real claims, ran the real restore through the real view, confirmed `active` flipped true and the stamp landed, then rolled back with nothing persisted) before writing the migration file.
- Branched (`claude/field-people-reactivation-guard`), pushed, opened [PR #1571](https://github.com/eq-solutions/eq-shell/pull/1571), dispatched to ehow (`slug=sks`, applied cleanly), merged after CI green. Confirmed live via `pg_get_functiondef`.
- ~15 seconds after merge, a second concurrent session's unrelated PR #1572 (job_title + structured emergency contact) merged its own full `field_people_iud()` replacement — also numbered its migration `0274` (different filename, no filesystem collision; this repo already tolerates duplicate number prefixes) — and silently wiped the `activated_by` stamp back out of that one function. Investigated rather than reflexively re-patched: proved `field_people_iud()`'s reactivation branch is structurally unreachable in production, because the main `field_people` view's `WHERE (field_approved IS TRUE OR field_approved IS NULL) AND active IS NOT FALSE` excludes archived rows entirely — PostgREST cannot UPDATE a row through a view that already excludes it, so the branch can never fire regardless of what it contains. No further fix needed. The actually-live Restore path, `field_people_removed_iud()` (backed by the `field_people_removed` twin view, which specifically does expose archived rows), was untouched by PR #1572 and confirmed still fixed.

## Decided
- Royce confirmed each step explicitly and separately, same cadence as last session: write it up → give it its own branch → push it and open a PR → dispatch the migration to ehow → merge the PR.
- Did not re-patch `field_people_iud()` after the second collision — chose to verify actual production impact first rather than assume; found none, reported the correction plainly instead of shipping an unnecessary migration on a sensitive trigger.

## Deferred
- **Restore has still not had an actual successful click observed** — all of Royce's attempts tonight predate the 0274 fix (his last attempt, a hard reload + retry, happened to land back on Shell's Staff page rather than EQ Field, which has no restore action at all — see Notes). Next person on `field.eq.solutions` should confirm with a real click.
- `savePersonToSB` (add) and hard-delete remain not click-tested live, unchanged from last session.
- The broader cross-repo coordination gap (eq-field's own migrations and this repo's tenant-migrations can both replace the same functions with zero shared ledger) is NOT fixed by tonight's work — a same-day concurrent session (PR #1569) shipped a detection/warning guard for it, not a prevention mechanism. Already noted as its own tracked item in 0273's migration comments.

## Notes
- **Two structurally different UIs write to the same `app_data.staff` table and are easy to confuse**: eq-shell's own Staff page (`core.eq.solutions/sks/staff`, React, archive-only — no restore/unarchive action exists there at all, confirmed in `entityActions.ts`/`entityCapabilities.ts`) vs EQ Field's own app (`field.eq.solutions`, vanilla JS, has the actual Restore button). A screenshot of the Shell Staff page early in this session was mistaken for the right place to test restore — worth checking which app a screenshot is actually from before troubleshooting a Field-specific bug.
- **EQ Field's data-plane JWT shape is a trap for this exact debugging pattern**: `sub` carries the tenant_id, NOT the acting user — the real actor is `app_metadata.actor_id`. Assuming standard JWT convention (`sub` = user) would silently misattribute `activated_by` to a tenant, not a person.
- Decoding a live JWT by eye from a cropped DevTools screenshot is unreliable (byte-misaligned base64 from one misread character cascades and corrupts everything after it) — pasting the raw header value as text and decoding programmatically is far more reliable once the value is fully visible on screen.
- Confirmed live tonight: this repo already tolerates multiple migration files sharing the same number prefix (`0239`, `0249`, `0253`, `0256`, `0267` all already had two files each before tonight) — a same-number collision is not itself a filesystem or ledger problem; the real risk is two different files both fully replacing the same function.

---

# Session 2026-08-24 (cont.) — Real click-test confirms the push pipeline live; found and fixed a second silent-drop bug; birthday still broken

## Built
- Picked Aiden Crowley (linked, active, every relevant field empty) as the test subject for Royce's real click-test of tonight's earlier field_people_iud() work.
- Confirmed live, first genuine end-to-end proof of the whole pipeline: Job Title and Emergency Contact Name both saved to `app_data.staff`, and a fresh `net._http_response` row (200) confirmed the name reached jvkn's `public.workers`.
- Found a second real, live bug in the same pass: `scripts/supabase-entities.js`'s `savePersonToSB()` has its own separate, hand-curated write-payload allowlist from `people.js`'s `fields` object — job_title/DOB were already in it, but Emergency Contact Mobile/Relationship (added earlier tonight) never got added to this second list, so both were silently stripped one file downstream despite being captured correctly everywhere upstream.
- Fixed and shipped: eq-field [PR #766](https://github.com/eq-solutions/eq-field/pull/766), v3.5.549. Re-verified live immediately after — both fields now save and push to jvkn correctly (confirmed via a second real edit + a second fresh `net._http_response` 200).
- Investigated Birthday (day + month) not saving, confirmed real and reproducible (failed on both the first and second edit, a screenshot proved valid dropdown values immediately before the second save) — thorough code read of every file in the write path plus the dropdown markup plus Sentry found nothing wrong. Root cause not found this session.
- Spawned background task `task_bb6cab43` with full context (files already ruled out, next diagnostic step) — Royce started it in a separate session, running independently as of this close.

## Decided
- Royce: picked up the recommended test candidate (Aiden Crowley) and ran the real click-test himself.
- Royce, via AskUserQuestion, on the birthday bug: track it for a focused follow-up rather than keep live-debugging blind — tonight's actual scoped task (job title + emergency contact) was already done and confirmed.
- Royce dismissed one direct clarifying question about which screen he used for the birthday entry, then answered it implicitly by sending a screenshot of the Edit Person modal instead — read as "here's the actual state, judge from that," not as a request to drop the thread.

## Deferred
- Birthday (day + month) silently not saving — tracked as `task_bb6cab43`, running independently. Real, reproducible, root cause unknown; needs an authenticated session with DevTools Network tab to find where the value actually gets lost.
- Aiden Crowley's real SKS record still carries tonight's test data (job title, emergency contact, start date) — the original plan was trial-then-undo; the undo step was never done this session. Needs Royce's go on timing.

## Notes
- **A third instance of the same underlying failure mode as tonight's earlier field_people_iud() collision, but in a different shape**: not two migrations racing on the same DDL, but two separate hand-written JS allowlists (`people.js`'s `fields` object and `supabase-entities.js`'s `savePersonToSB()` payload) that both have to be updated together for any new field, with nothing enforcing that they stay in sync. Worth folding into the same "shared registry, no single source of truth" pattern noted earlier tonight, even though the concrete mechanism (JS object literals vs. SQL `CREATE OR REPLACE`) is completely different.
- **Learned only from a concurrent session's own close record, not discovered directly**: this session's eq-shell `0274`/PR #1572 collided with a different concurrent session's PR #1571 (a reactivation-guard fix, same `field_people_iud()` function) roughly 15 seconds apart. That other session investigated it — not this one — and found it harmless (the affected code path is structurally unreachable through the main `field_people` view's own `WHERE` clause, so the real reactivation path, a separate function, was never at risk). Recorded here for completeness; the fuller account lives in that session's own changelog/session-log entries and in `eq/pending/eq-shell.md`, neither edited from here since the work wasn't this session's own.
- `net._http_response` (pg_net's response table on ehow) proved itself again tonight as the single most reliable way to verify this specific pipeline end-to-end — a fresh row with `status_code: 200` and the expected `fields` array in the body is direct, unambiguous proof the push actually reached jvkn, distinct from just confirming the ehow-side write succeeded.

---

# Session 2026-08-24 (cont.) — Birthday day/month root-caused + fixed live: closes the `task_bb6cab43` thread

## Built
- Picked up `task_bb6cab43` (the "birthday still broken" investigation two sessions above, spawned by Royce). Confirmed everything that pass had already ruled out was genuinely correct — form markup (no duplicate DOM ids, confirmed by `git grep` against the full tracked tree, not just the one file), `savePerson()`'s validation/merge logic, `savePersonToSB()`'s write payload, `_upsertById`, `sbFetch`'s `people`→`field_people` path rewrite, and the known `field_people_iud()` trigger (re-pulled live, unconditionally forwards `dob_day`/`dob_month`, no gating). All correct.
- Found the actual cause one layer deeper by listing every trigger on the *base* table (`pg_trigger` for `app_data.staff`), not by re-reading the view trigger again: a second, undocumented `BEFORE INSERT OR UPDATE` trigger, `staff_derive_dob_from_cards` — never recorded in any eq-field migration or commit — unconditionally re-derives `dob_day`/`dob_month` from a separate `date_of_birth` column whenever the person is Cards-linked. `field_people_iud()`'s own inner `UPDATE app_data.staff` fires this base-table trigger as a side effect, immediately after correctly setting the values, silently wiping them back to null when `date_of_birth` is null (true for anyone who's never gone through the unrelated licence-verification wizard — 45 of 81 active SKS staff).
- Fixed with a one-line guard (`AND NEW.date_of_birth IS NOT NULL`), applied directly to ehow via Supabase MCP, verified end-to-end through the real view+trigger path (a real UPDATE on Aiden Crowley's row persisted correctly this time, then reverted back to null — his actual birthday was never confirmed, so nothing fabricated was left on his record). Migration committed for governance: eq-field [PR #767](https://github.com/eq-solutions/eq-field/pull/767), merged.

## Decided
- Royce: "Draft migration + apply live now" for the DB fix, given via `AskUserQuestion` after the root cause + proposed fix were presented.
- Royce: "Squash-merge now" once CI/deploy-preview on PR #767 went green — asked separately, since merging to eq-field's `main` is its own explicit-deploy trigger distinct from the DB-apply approval already given.

## Deferred
- **Whether `date_of_birth` should always win over a directly-edited day/month, for a Cards-linked person who has both** — the fix stops the *null* from winning, but doesn't resolve which source is authoritative when both are genuinely populated and disagree. Product call, not decided.
- **Aiden Crowley's birthday is blank again** — 18 Feb was typed while reproducing the bug, not confirmed as his real date; needs an actual re-save now that saving works. His record separately still carries the *earlier* session's own trial data (job title, emergency contact, start date), meant to be trial-then-undo and never was — untouched by this session, still open, needs Royce's go on timing.
- **45 of 81 active SKS staff have no birthday recorded** — the feature now works for all of them, but nobody's been prompted to actually enter one.

## Notes
- **The shared eq-field root checkout was 27 commits (19 releases) behind `origin/main`, detached HEAD, and this nearly derailed the investigation**: the first code read (from the shared root) showed the emergency-contact fields as *not yet fixed*, directly contradicting Royce's own bug report, which described them as already-fixed earlier the same night. That contradiction is what surfaced the staleness — pivoted to reading `origin/main` directly (`git show origin/main:<path>`) for the rest of the investigation, and did all actual editing from a fresh worktree branched off current `origin/main`. Worth a hard reset (or re-clone) on that shared root before the next session trusts it.
- **Server-side edge logs (`mcp__supabase__query_logs`, `source='edge_logs'`) pinned the exact two failing PATCH requests** (`10:09:47.359Z` and `10:17:22.562Z`, both `204`, both matching `staff.updated_at` to the millisecond) without needing an authenticated browser session — useful technique when DevTools access isn't available: `event_message` is `"METHOD | STATUS | URL | UserAgent"`, searchable by table name.
- This is the latest of several bugs on `app_data.staff`/`app_data.field_people` traced to out-of-band DDL never captured in any migration file (see the `security_invoker` view-reset incidents already on record, and tonight's own earlier `field_people_iud()` coordination-gap entries above). Worth a standing habit: when a bug on this table doesn't resolve from the known trigger/view definitions, check `pg_trigger` for anything undocumented before assuming the application code must be wrong instead.
- **This session's own eq-context substrate write hit the exact stale-shared-root risk its own hooks warn about**: the shared `C:\Projects\eq-context` checkout was 20-22 commits behind `origin/main` (climbing during the session from concurrent activity) and both a direct `git pull` and a direct `git commit` on it were correctly hook-blocked. Recovered via an isolated `git worktree add <path> origin/main`, then — critically — re-read each of the 3 target files fresh in that worktree before reapplying edits rather than copying over the stale versions: `eq/changelog/eq-field.md` and `sessions/2026-08-24.md` had both genuinely moved (7 more session entries, including the very `task_bb6cab43` predecessor this session continues) since they were first read from the stale root. A blind copy would have silently destroyed that content — exactly the F12 failure mode already on record.

---

# Session 2026-08-24 (cont.) — Birthday fix, part 2: ported eq-shell's already-proven guard for the remaining case

Royce asked two follow-up questions about the session-close card's "needs you" item: what it actually meant, and whether a birthday gets filled in automatically when someone uploads their ID. Answering the second question changed the answer to the first.

## Built
- **Confirmed how the birthday actually gets populated**: EQ Cards has a dedicated screen (`profile_fill_from_licence_screen.dart`) that runs OCR on an uploaded/scanned driver's licence and pre-fills the worker's profile, including date of birth — a required onboarding field ("required for inductions" per Cards' own schema notes). That flows down into Field's `app_data.staff.date_of_birth` via a sync eq-shell shipped the night before (2026-08-23), and the trigger fixed earlier this session picks it up from there automatically. So the 45 affected people are specifically the ones who haven't scanned a licence into Cards yet — most should self-resolve as they do.
- **Found that eq-shell already has a defensive guard for the remaining case, with a named incident behind it**: `entity-patch.ts` (Shell's own Staff-edit path) already rejects a direct `dob_day`/`dob_month` edit for a Cards-linked person who has a real `date_of_birth`, instead of letting the trigger silently discard it. Its own comment cites why: "this exact silent-overwrite is what left Brian Griffin-Colls and 7 others with a 'birthday' but no real date of birth (2026-08-17)". Field's Edit Person modal had no equivalent — earlier this session's fix only covered the *no date_of_birth yet* case, leaving this narrower one exactly as broken as before.
- **Ported Shell's exact fix into `staff_derive_dob_from_cards()`**: reject (`RAISE EXCEPTION`, marker `dob_locked_to_cards`) instead of silently discard, gated on the value actually changing — mirroring the same gating Shell had to add after its own false-positive regression (Royce's "Mohammed Hussain" report, 2026-08-18), since Field's `savePersonToSB()` also unconditionally resends `dob_day`/`dob_month` on every save regardless of what was actually edited. `people.js`'s save-failure toast now recognizes the `dob_locked_to_cards` marker and shows an actionable message instead of the generic "Save failed — check connection".
- Applied live to ehow and verified with 4 scenarios directly against the DB *before* writing any client code: reject-on-real-change (confirmed transaction rolled back, value unchanged), allow-on-unrelated-field-resave (not falsely blocked), no regression on the earlier fix's null-`date_of_birth` case, and exact error-message content matching what the client pattern-matches on.
- Shipped as eq-field [PR #769](https://github.com/eq-solutions/eq-field/pull/769), v3.5.550, merged and confirmed live via `curl .../sw.js`.

## Decided
- Royce: "port it now" — approved via `AskUserQuestion`, once the Shell precedent (and its incident) was presented as the actual context behind the "needs you" item.
- Royce: "squash-merge now" — asked separately after CI went green, same pattern as the first fix.

## Deferred
- **Full click-through of the actual reject path not done** — no authenticated SKS session available. Verified as far as possible without one: 4 direct-DB scenarios covering the exact trigger logic, full local test/lint/drift suite green, and a deploy-preview smoke check (boots clean at v3.5.550, zero console errors). A real attempt — edit a Cards-linked person's birthday who has a real DOB, confirm the toast reads correctly — is worth doing once someone has a live session.
- The two items already carried over from the first half of this thread (45 staff with no birthday yet, Aiden's own birthday needing a real re-save) are unchanged by this second fix — still open in `sks/pending.md`.

## Notes
- **`people.js` hit its grandfathered CI line-cap by 3 lines** (1550→1553) from a 7-line branch replacing 1. Used this repo's own documented escape valve (bump the specific file's entry in `eslint.config.js`'s `GRANDFATHERED_MAX_LINES`, with a dated comment) rather than force an extraction for a change this small — exact precedent already on record for `apprentices.js` at a similar size. Bumped to 1600 (modest headroom, matching the block's own "50-increment" convention).
- **Local verification caught the line-cap error before CI would have** — ran the exact commands `ci.yml` runs (`node tests/*.test.js` loop, `eslint@9`, `check-cache-busters.mjs --base <merge-base>`, `build-bundles.mjs --check`) directly in the worktree ahead of pushing, rather than waiting on a round-trip through GitHub Actions to find out. All green before the PR was even opened.
- Second consecutive `/close` this session hit the same stale-shared-eq-context-checkout pattern (32 commits behind by this point, still climbing) — same recovery: isolated worktree, re-read all 3 target files fresh before editing. This time nothing had actually moved underneath (all 3 files matched exactly what the first close had left), but the check was still necessary to know that rather than assume it.

---

# Session 2026-08-24 — field_job_numbers stale-code fix (Equinix rename side effect)

## Built
- Diagnosed and fixed `app_data.field_job_numbers_src()` on ehow: Ops-sourced `site_name` was `COALESCE(s.code, s.name)`, so it kept returning the pre-rename bare site code (e.g. "SY5") after the same-day Equinix rename, while field-sourced rows in the same list already showed "Equinix SY5".
- Traced the COALESCE choice across all 3 migrations that ever touched this function (20260704b, 0163, 0173) — confirmed incidental, never deliberate, no comment ever addresses it.
- Wrote eq-shell migration `0275_field_job_numbers_prefer_site_name_over_code.sql` (body-only CREATE OR REPLACE, same grants/SECURITY DEFINER/search_path), verified live first that 0/261 `app_data.sites` rows have a NULL name so the reorder can't regress anything.
- PR eq-shell#1578: pushed, CI green (schema-drift/anon-grant/policy-lint, function-grants, migration-ledger, typecheck/test/lint all passed), squash-merged to main.
- Dispatched live to ehow via Supabase MCP `apply_migration`; re-verified after — SY5's 6 ops rows + 1 field row now both read "Equinix SY5", all other affected sites (CA1/SY1/SY2/SY3/SY9) consistent, counts unchanged (no data loss).
- Checked Supabase security advisors post-dispatch: only pre-existing generic SECURITY DEFINER/authenticated-EXECUTE linter WARN, unchanged by this fix, nothing new introduced.

## Decided
- Fix scope: DB-side only (option a — reorder the COALESCE), not an eq-field code patch (option b). Confirmed once live recon showed the inconsistency also displays directly in eq-field's `jobnumbers.js` admin table/CSV export, not just `roster.js`'s dropdown-availability match — fixing at the source covers every consumer in one place, and `roster.js` needed no change since it already matches against `site.name`.
- Worked from isolated worktrees/clones throughout, not the shared `C:\Projects\eq-shell`, `C:\Projects\eq-field`, or `C:\Projects\eq-context` checkouts — all three were mid-use by other concurrent sessions (eq-shell root: another branch, 113 behind, dirty; eq-field root: detached HEAD; eq-context: blocked outright by the F9 pre_tool_use guard on `git pull`).

## Deferred
- Duplicate site rows sharing one code in `app_data.sites` (SY3/SY4/SY5/SY9, 2-3 rows each) and one un-renamed SY9 row (`site_id 95cdc37d-3492-4849-869d-306484681451`, still `name='SY9'`) — flagged in the migration header and in `eq/pending/eq-shell.md`, not fixed. Pre-existing data-quality issue in a different table, needs Royce's call on which duplicate is canonical.

## Notes
- Session opened mid-stream via a pasted "Context:" handoff describing the bug (apparently from the same-day site-rename session) rather than a live conversation — verified every claim in it against live DB/code before acting rather than trusting it outright; it checked out exactly (live counts matched with zero drift: CA1=1, SY1=1, SY2=1, SY3=5, SY5=6, SY9=3, all source='ops').
- The actual source rename migration (`0275_equinix_site_names.sql`) landed on main in the same merge batch as this fix's own `0275`-numbered file — a harmless duplicate-number collision (different filenames; this repo already tolerates several pre-existing duplicate-numbered pairs, e.g. 0245/0245, 0249/0249, 0274/0274). Read it after merging: it independently confirms this session's entire diagnosis and explicitly names this fix as the correct follow-up, not something it should have done itself.
- eq-context's `/close` hit the F9 pre_tool_use guard on `git pull` in the shared checkout (rung 4 — repeated stuck-rebase/ref-corruption history). Recovery matches the guard's own remediation and what at least one earlier session today already did for the same reason: fresh isolated `git clone`, edit there, push from there.

---

## Correction (same session, post-close) — the "duplicate site rows" deferred item was a false alarm

Royce asked "equinix has multiple entities per site?" after the close card — the earlier flag was made off `code, name, site_id` alone. Pulling full rows showed it isn't an open data-quality question at all:

- SY3/SY4/SY5: each pair is one live row (`imported_from='eq-solves-service'`, `active=true`, real `customer_id`) plus one dead row (`imported_from='eq-solves-field'`, `active=false`, `customer_id=null`). All 3 dead rows share the identical `updated_at` (2026-07-13 11:58) — retired together in one past pass, not organic duplication. SY5's dead row carries its own note naming the survivor (`9b9d62f3…`) explicitly.
- SY9: same shape, 3-way. The "un-renamed" row (`95cdc37d`, still literally `"SY9"`) is a third dead duplicate retired 2026-07-13 (note references `eq-shell#781`) — it was never supposed to get the new name because it was never live. The actual live SY9 (`2dfa57bb…`, real Equinix contact Ed Nana) is the one today's rename correctly touched.

Corrected `eq/pending/eq-shell.md` to close the item out rather than leave a wrong "needs your call" sitting in the backlog. Changelog entry left as originally written (append-only, matches the file's own convention — this note is the correction record instead, same pattern already used elsewhere in this file).

---

## Correction #2 (same session) — SKS "deactivate stale EC6/SYD27" pending item was already done, just never ticked off

Came up while answering an unrelated question ("what are the CDC sites") — pulled `app_data.sites` for codes EC6/SYD27 to check the earlier Equinix-collision comment in eq-field's `supabase.js`, and found both stale rows (`Erilyan`/EC6, `Microsoft SYD27`/SYD27) already `active=false`, `updated_at=2026-08-14` — the same day `sks/pending.md`'s item for this was added. Not this session's work; someone/something flipped them without closing the pending item. Corrected `sks/pending.md` line 84 to `[x]` with a note.

---

# Session 2026-08-24 (close #2) — post-close corrections, CDC site data documented

## Built
Nothing new — no code/migration changes since close #1. Both substrate corrections below were already committed and pushed in-conversation (not deferred to this close): `3d4d6b28` (eq-shell pending item false-alarm correction) and `4c0b9ac7` (SKS EC6/SYD27 item marked done). This close just adds the CDC note and formally closes out.

## Decided
Nothing new.

## Deferred
Nothing new.

## Notes
- **CDC sites documented (ehow `app_data.sites`), not previously recorded anywhere in substrate**: 6 real, dispatchable CDC sites — EC2, EC4, EC5, EC6, MP1 (all customer Schneider Electric Australia), SYD27 (customer Microsoft) — all at Eastern Creek/Marsden Park NSW. EC4 is the only one with `service_enabled=true`; the rest are Field-only. Plus 3 codeless placeholder rows (bare `"CDC"`, `"CDC - Eastern Creek"`, `"CDC - ACT (FYSHWICK + HUME)"`), all `field_enabled=false`/`service_enabled=false` — inert, neither app can select them since both pickers key off `code`. Unlike the Equinix cluster, CDC's site names already had the brand baked in from the start (`"CDC - EC2"`, not bare `"EC2"`), so there's no equivalent stale-name bug there — and any CDC site with an Ops-sourced job number benefits from today's `field_job_numbers_src()` fix same as Equinix does.
- Second same-session example of a "hand-written claim about current state ages into a lie" near-miss (this one caught, not missed): the SKS EC6/SYD27 pending item sat open for 10 days after the underlying fix actually landed, discovered only because an unrelated question ("what are CDC sites") happened to pull the same two site codes.

---

# Session 2026-08-24 (Chat-to-substrate gap) — ITC-EL-0006 template, SKS substrate critique, and why Chat work wasn't reaching the substrate

## Built
- ITC-EL-0006 (combined switchboard/submain/RS485 ITC, Rev C) added to `sks/templates.md`; `sks/README.md` file-listing drift fixed (PR #177).
- `sks/active.md`/`sks/team.md` flagged as stale rather than silently presenting old data as current; `sks/README.md`'s two file-inventories resynced (PR #178).
- `sks/active.md` reshaped from a single always-current table to dated append-only snapshots — the format, not just discipline, was why it went 7 weeks stale; `sks/chat-gateway.md` built (first version, fetch-instruction bootstrap) (PR #179).
- Root-caused why Chat can't write to eq-context: a 45-day chat-activity export + live verification found 4 of 6 "gap" candidates were false alarms (already recorded, dates that didn't line up, already resolved); the 2 real ones (a duplicate GitHub-connector diagnosis, a lost substrate-architecture scoping conversation) logged in `eq/pending/eq-context.md` (PR #180).
- `eq/chat-gateway.md` built as EQ's sibling to the SKS one; "verify before you draft" backported into both (PR #181).
- Discovered Royce already had SKS/EQ/OPS Claude Projects running with hand-written instructions from an earlier session, unknown when the above was built. Merged the two: SHA-pinning + a Memory-distrust section + OPS's "name the failure mode" discipline pulled into all three; `ops/chat-gateway.md` built from scratch the same way (PR #182).
- **Root cause of Chat's write failure identified precisely**: a known, open Anthropic bug, [anthropics/claude-ai-mcp#822](https://github.com/anthropics/claude-ai-mcp/issues/822) — not a settings problem, and not a paid-Copilot-plan gate (that was a wrong turn, corrected in-conversation once the real cause surfaced).
- **Bootstrap mechanism corrected**: the "fetch this URL and follow its instructions" line in all three gateways' Bootstrap sections was never tested and doesn't work — Claude Chat correctly refuses to treat fetched content as standing instructions (anti-prompt-injection design, holds even when the Instructions field itself asks for it). Fixed to what actually works: paste the file's full content directly into Instructions (PR #183).
- Live-tested all three corrected gateways in real Chat conversations — all three passed. SKS confirmed the proactive-patch-offer behaviour; EQ and OPS both showed real cross-referencing (caught an already-recorded fact, caught an internal contradiction against `ops/pending.md`'s AHD-parked status) rather than blind pattern-matching.

## Decided
- Royce: skip self-hosting a GitHub MCP server (the one path that would fix Chat's write access properly) — checked against real chat volume (~1 genuine untracked SKS fact per 4 days) and declined; the standing cost of a second broadly-scoped, internet-facing write credential isn't justified at that cadence, and it's the same risk shape as SEC-57 (reviewed this session, left open, Royce's explicit call not to touch it).
- Royce: merged PR #177 himself mid-session.
- Consolidate the duplicate "EQ Substrate" project created mid-session with whichever pre-existing EQ project Royce keeps — flagged, not resolved.

## Deferred
- ITC-EL-0006's brand colour (`#1F335C`) still conflicts with the live-verified `#203060` used elsewhere in the same file — logged in `sks/pending.md`, never resolved this session.
- `eq/chat-gateway.md`'s proactive habit is confirmed working in one test conversation only — six months of the old EQ project's real use didn't catch the July 16/26 losses; unproven whether the new version holds up at real day-to-day volume.

## Notes
- **The core, reusable lesson of the session**: a chat-activity export is a good *signal* for what to check, a bad *source* to write from directly. Of 6 candidate substrate gaps pulled from real chat history, 4 didn't survive a live verification pass. Encoded as a standing rule ("Verify before you draft, not after") in all three chat-gateway files, not left as a one-off finding.
- Substrate reads were pinned/reconciled repeatedly against a fast-moving origin/main (behind-count climbed from 2 to 45 over the session, concurrent-session driven) — every write this session was preceded by a live `diff --stat` against origin/main scoped to the exact files being touched, never a blind read-old-then-write.
- All 7 eq-context PRs this session (#177–#183) hit the same pre-existing, unrelated `index-drift` CI failure (one orphaned eq-tier file, already tracked before this session started) — confirmed unrelated each time via the check's own per-tier output before merging, never overridden blind.

---

# Session 2026-08-24 (close #3) — EC6/SYD27 Field verification, field_sites hardening declined

## Built
Nothing new — no code/migration changes since close #2.

## Decided
Nothing Royce-confirmed. Ran `/decide` on one question this segment (see Notes) — a recommendation, not a confirmed decision; logged so it isn't re-litigated from scratch next time.

## Deferred
Nothing new added to pending.md this close — see Notes for why the one candidate item (hardening `app_data.field_sites`) was deliberately NOT added as an open pending item.

## Notes
- **EC6/SYD27 duplicate check extended to Field, on request.** Service was already known clean (neither row surfaces — both `service_enabled=false` on the live rows). Field: `app_data.field_sites` (the canonical view feeding `STATE.sites`) filters only on `field_enabled`, NOT `active` — in isolation this could let "Microsoft SYD27" (dead, `active=false`, `field_enabled=true`) leak through. Traced via subagent to the actual boot-time loader (`loadFromSupabase()`, `index.html:13689`, called from `initApp()` and the 30s `refreshData()` poll): its query string carries its own `active=eq.true`, predating the JWT_TABLES routing rework and preserved through it. Every `STATE.sites` consumer checked (sites.js, roster.js, jobnumbers.js, apprentices.js, diary.js, incidents.js, toolbox.js, forecast.js, projects.js, tender-pipeline.js, safety*.js, dashboard.js) has zero `.active` filtering of its own — none needed one, since the source is pre-filtered. Conclusion: clean today, on both apps.
- **`/decide` run on whether to harden `app_data.field_sites` with its own `WHERE active = true`** (mirroring `service.sites`'s existing pattern, migration `0163_service_sites_filter_active.sql`). Confirmed the view is plain `security_invoker=on` (not SECURITY DEFINER) — low blast-radius either way. Recommendation: **don't build it now** — nothing is broken, the risk is speculative (a future edit to the one loader's query string, with nobody around who remembers why `active=eq.true` is there), and doing it properly would require checking whether eq-shell or eq-solves-service read `app_data.field_sites` directly wanting inactive rows (not checked). No goal in `TODAY.md` to rank it against either (GOALS unset this session). Deliberately NOT added to `eq/pending/eq-shell.md` as an open item, since that would misrepresent "considered and declined" as "needs your call" — this note is the record if it needs revisiting. Flips if: a real site gets deactivated and shows up somewhere unexpected, or someone edits that loader's filter without this context.
