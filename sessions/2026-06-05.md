---
title: Session — substrate hardening, security gating, cutover prep
owner: Royce Milmlow
last_updated: 2026-06-05
scope: EQ session — substrate review + CI hardening, automated security gating (found a live PII leak), cutover runbook, staged weekend fixes
read_priority: reference
status: live
---

# 2026-06-05 — Substrate hardening + security + cutover prep

All work on branch `claude/unruffled-zhukovsky-6f31d9` → **PR #20** (not yet
merged). No live-DB writes, no deploys. Several large pieces:

## 1. Substrate delivery-layer hardening (PR earlier commits)
- Greened the **frontmatter-check CI** (was failing on every push to main, ignored).
  Added frontmatter to 13 files; exempted CHATGPT/GROK pointer prompts; aligned the
  `status` enum with the ADR vocabulary (`+proposed/accepted/superseded`).
- De-cluttered root → `archive/` (8 historical dated docs); added a pre-commit rule
  + PR CI job blocking new dated root docs.
- Rebuilt drifted indexes (eq/README 8→20 files, system/archive/sks); added an
  **index-drift CI guard**.
- Extracted the inline sync engine → `scripts/sync.py` + tests (12); added orphan-
  delete safety cap + uncovered-md guard. Dropped `[skip ci]` from auto-bump
  (was desyncing Supabase).
- `context_proposals` migration hardened + **applied** to the substrate project
  (caught a view RLS-bypass before shipping it).

## 2. Automated security gating — found a LIVE PII leak
- `scripts/rls_probe.py` (public-key leak test, no secret) + `scripts/security_audit.py`
  (advisor audit, needs `SUPABASE_ACCESS_TOKEN`) + tests + `.github/workflows/security-audit.yml`.
- **First run found SEC-1:** `sks-labour` (SKS LIVE) public key reads `people`/
  `timesheets`/`leave_requests`/`audit_log`. Pre-canonical anon app. Tracked
  (not silently green) via `KNOWN_LEAKS` baseline.
- `ops/security-register.md` — SEC-1..8 across all 6 projects, merging probe +
  advisors + F1 + the rest.

## 3. Verified the "fix now?" items (read-only) — reframed
- **SEC-4** (eq-canonical anon SECURITY DEFINER `eq_cards_*`): NOT exploitable —
  `auth.uid()` (NULL for anon → 0 rows) / token-guarded. → P3 hardening.
- **SEC-5** (always-true write policies, field + canonical-internal): LATENT —
  `has_table_privilege` confirms anon holds NO grant → policies unreachable. → P3.
  **Field's live DB is not anon-exposed.**
- **SEC-2** (zaap `eq_intake_rate_limits` RLS trusts `user_metadata`): real ERROR;
  verified 24+ other zaap policies use `app_metadata` → fix is proven-safe.

## 4. Cutover prep (`CUTOVER-RUNBOOK.md`)
Verified all 4 projects live — corrected 3 stale plan assumptions:
- `nspbm` (SKS Labour) is **off-registry**; control plane already routes `sks → ehowg`.
  Cutover = build `field_*` + auth + load data INTO `ehowg`, not "repoint nspbm".
- Live data is 22 MB, Quotes-dominated; 4 of 7 named labour tables don't exist.
- Prereqs A/B ❌, C partial (5 of 56 ehowg tables auth-wired vs EQ's 28), D pending.
Full phased runbook A–E with gates/verification/rollback + security folded in.
Verdict re-confirmed: **multi-session mini-sprint, not a weekend.**

## 5. Staged weekend fixes (`ops/weekend-security-execution.md`) — NOT applied
- **SEC-2:** exact `drop/create` switching to `app_metadata` (precondition verified).
- **SEC-1:** leak surface verified broader than 4 tables (~40 anon-SELECT tables);
  staged `revoke select on all tables in schema public from anon` (one-shot,
  reversible) or pause `nspbm`. Run once the team is on Field.
- **SEC-3 (F1):** re-verified still OPEN (routing row never re-keyed since 2026-05-24);
  linked the existing runbook into the weekend order.

## Open / next
- **PR #20 ready to merge** (all CI green) — gated on Royce.
- Weekend: SEC-2 (any time), SEC-1 interim (after team on Field), F1 (optional).
- SKS→Field cutover: schedule as its own mini-sprint (Phase A = canonicalize
  `field_*` on `zaap` is the long-pole).
- PostHog now connected — Field daily-usage confirmation pending (Royce running
  in another console).
