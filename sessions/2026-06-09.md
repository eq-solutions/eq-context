---
title: Session Log — 2026-06-09
date: 2026-06-09
tier: EQ
---

# Session Log — 2026-06-09

## Summary

Two sessions. Session 1 (earlier): Security sprint closed + WS1/WS4/WS5/WS7
workstreams completed, GATE A worker identity linker landed. Session 2
(this session): deployed all sprint work, ran GATE A backfill, fixed and
deployed eq-service encryption (migration 0123 schema fix + rekey script
fix), all security sprint env vars confirmed set.

---

## Session 1 — Completed

### Security sprint — all S0–S3 items DONE
- 5 PRs merged across eq-shell, eq-solves-field, eq-solves-service
- All security sprint items (S0 through S3) closed and verified

### WS4 — quote-job-consumer
- Canonical work-order spine built and merged
- `app_data.jobs` now wired; quote→job trace established

### WS7 — Cards to Field bridge
- Confirmed complete 2026-06-08

### WS1 — safe 1:1 customer backfill
- Safe subset (1:1 both-sides match) backfilled via direct SQL
- Remaining: 481 ambiguous customers deferred to EQ Intake human dedup

### GATE A — worker identity linker
- `backfill-worker-links.ts` built and PR #278 merged to eq-shell
- `accept-invite.ts` already had claim-time linkage on main

### WS5 — durable event marking
- `processed_by_quote_job` column added to `app_data.canonical_events`
- `quote-job-consumer-scheduler.ts` updated; PR #279 merged to eq-shell

---

## Session 2 — Completed (this session)

### eq-shell deploy live
- Epic 2 (worker self-join via QR code) build resolved
- `core.eq.solutions` confirmed live at 07:31

### Security sprint env vars — all 6 confirmed set on eq-shell
| Var | Status |
|---|---|
| `EQ_SESSION_SALT` | ✅ set |
| `EQ_FIELD_HANDOFF_KEY` | ✅ set |
| `EQ_SERVICE_HANDOFF_KEY` | ✅ set (eq-shell + eq-service) |
| `EQ_QUOTES_HANDOFF_KEY` | ✅ set |
| `EQ_SHELL_BRIDGE_SECRET` | ✅ set |
| `SITE_CREDENTIALS_KEY` | ✅ set on eq-service |

### GATE A backfill — executed
- Ran Phase 1–3 directly via Supabase MCP SQL (bypassed backfill function
  which requires platform-admin session cookie)
- 1 worker linked: luke.wheeler@sks.com.au → `user_id` set
- Final state: 39 workers, 7 have `user_id`, 29 have `staff_id`
- **25 workers have active pending invites (expire 2026-06-15)** —
  self-resolving on invite acceptance via `accept-invite.ts`
- 2 workers (emma_curth, hexperfect) have no matching staff record —
  need manual resolution
- 8 workers have no email — cannot auto-link

### eq-service encryption — migration 0123 fixed and applied
Three bugs in the original migration:
1. `public.site_credentials` → `app_data.site_credentials` (table is in app_data)
2. `public.get_user_role()` removed (doesn't exist; role auth is app-layer only)
3. New `_admin_rekey_site_credential()` RPC — service-role only, no JWT check

Rekey script fixed:
- All `.from('site_credentials')` → `.schema('app_data').from('site_credentials')`
- RPC call updated to `_admin_rekey_site_credential`
- Added `.env.local` loader (tsx doesn't auto-load it)

PRs merged: #268, #269 to eq-service main
Migration applied to live ehow (`ehowgjardagevnrluult`)
`SITE_CREDENTIALS_KEY` set in eq-service Netlify (secret)
Table has 0 rows — rekey is a no-op, script confirmed working

---

## Outstanding / Deferred

### Worker linkage (self-resolving by 2026-06-15)
- 25 workers have pending invites — they self-link on accept
- 2 workers (emma_curth@outlook.com, hexperfect@outlook.com) — no staff record
- 8 workers with no email — need email populated before they can link

### WS1 remainder
- 481 ambiguous customers — human dedup via EQ Intake (Confirm-UI not yet built)

### ktmj decommission
- Parked by Royce: "leave it running"

### eq-credentials-ref.html
- `C:\Users\EQ\eq-credentials-ref.html` — delete after importing to password manager

---

## State at Close

| Item | Status |
|---|---|
| Security sprint (S0–S3) | ✅ DONE |
| WS4 quote-job-consumer | ✅ DONE + deployed |
| WS7 Cards→Field bridge | ✅ DONE |
| WS1 safe backfill | ✅ DONE |
| WS5 durable outbox | ✅ DONE + deployed |
| GATE A worker linker + backfill | ✅ DONE (7/39 linked, 25 pending invite) |
| eq-service encryption (0123) | ✅ DONE — migration live, env var set |
| eq-shell security env vars | ✅ ALL 6 SET |
| WS1 remainder (481 ambiguous) | ⏳ DEFERRED — EQ Intake |
| Worker invite acceptance (25) | ⏳ SELF-RESOLVING by 2026-06-15 |
| ktmj decommission | ⏳ PARKED |

---

## Session 4 — EQ Shell v8 pass + JS token cleanup + Shell housekeeping

### EQ Shell v8 CSS pass — DONE (PRs #290 + #293, squash-merged)

Applied Claude Design "Direction D" warm-sand warmup to the React Shell codebase. All changes are pure CSS — no component or logic changes.

**`src/App.css`:**
- Login hub bg: `#F5F4F0` → `#F6F3EE`
- Section/panel headers (`eq-hub-actions__head`, `eq-hub-panel__head`): `background: #F9FAFB` → `#EEECEA` (2 instances via replace_all)
- Action hover + mobile records row hover: `background: #F9FAFB` → `#EEECEA`

**`src/pages/auth.css`:**
- `.eq-login-page` background: `#F5F4F0` → `#F6F3EE`

**`src/components/MobileRecordsDrawer.css`:**
- `.eq-rec-drawer__row:hover`: `background: #F9FAFB` → `#EEECEA`

**`src/components/MobileTabBar.css`:**
- Admin section label background + `.eq-mob-sheet__row:hover`: `#F9FAFB` → `#EEECEA`

Hub canvas (`#F6F3EE`) was already correct — no change needed there.

### EQ Field v3.5.119 — JS navy token cleanup (PR #260, squash-merged)

Swept all remaining `color:var(--navy)` bare inline styles in JS files:
- `scripts/apprentices.js` — 19 instances → `var(--ink,var(--navy))`
- `scripts/auth.js` — 1 instance → `var(--ink,var(--navy))`
- `scripts/audit.js` — 2 instances → `var(--ink,var(--navy))`
- `scripts/teams.js` — 2 instances → `var(--ink,var(--navy))`
- `scripts/sks-pipeline.js` — 9 instances: text headings → `var(--ink,var(--navy))`, dollar/stat accents → `var(--sky,var(--navy))`

Version bump: `APP_VERSION` + `sw.js` banner + `sw.js` CACHE + `index.html` banner all to `v3.5.119`.

### SKS PR #32 merged

Enables `sks-field.netlify.app` as a second Shell-integrated deploy + adds Supabase JWT Shell handoff verification in `verify-pin.js`.

### Shell branch housekeeping

- Deleted 3 dead/stale branches: `claude/mobile-iframe-bottom-bar-yield`, `claude/sleepy-pasteur-13a7ac`, `claude/sweet-shamir-5c90c6`
- Checked 8 unmerged branches for conflicts (all 0) → PRed and resolved:
  - 3 genuinely new → squash-merged (#296 email templates, #297 email smoke scripts, #301 durable outbox)
  - 5 already present in main from prior PRs → closed (sidebar-polish-d12, nav-records-accordion, crm-customers-hub-area2, ws4-quote-job-consumer, etl-complete-apply)
  - All 8 remote branches deleted

---

## Session 3 — EQ Field v8 Design Pass (this session)

### EQ Field v3.5.116 — v8 design update shipped

Implemented the Claude Design handoff (EQ Shell 14.zip) across EQ Field — the vanilla JS/CSS app at eq-solves-field.netlify.app.

**Approach:** Additive CSS layer (`styles/field-v8.css`) extending tokens.css + base.css. Design Direction D: warm sand canvas (#F6F3EE), white cards, sky accent (#3DA8D8), no gradients/shadows on static elements. No bundler — inline SVG Lucide icons.

**What shipped (PR #258, 4 commits, squash-merged to main):**
- `styles/field-v8.css` — 650-line v8 component layer (sidebar, stat cards, panels, badges, toolbar, search, table header override)
- Sidebar rebuild: new `.eqf-side` dark sidebar (#0F1117) with Lucide icons, group nav, user block, footer
- `index.html`: stat card row (`.eqf-statrow`), leave toolbar (`.eqf-toolbar` + `.eqf-seg`), v3.5.116 banner
- `scripts/dashboard.js`: all widgets rebuilt with `.eqf-panel` structure
- `scripts/leave.js`: view toggle → `.eqf-seg-btn`, injected CSS vars → v8 (sky/ink/alt)
- `scripts/timesheets.js`: injected CSS vars → v8, approval queue table headers → sky
- `scripts/people.js`: group badges Direct→sky, name colors→ink, checkbox accent→sky
- `scripts/managers.js`: mobile name color→ink
- `scripts/roster.js`: view-toggle active state→sky, modal header→sky, week nav, site/job labels→ink/sky
- `scripts/calendar.js`: selected cell bg→sky, group color maps Direct→sky
- `scripts/jobnumbers.js`: inline thead bg→sky (overrides base.css), job number accent→sky
- `scripts/apprentices.js`: competency thead + tab active underline→sky
- `scripts/home.js`: action button text→ink
- `scripts/projects.js`, `scripts/whatsnew.js`: minor color updates
- Global `thead tr { background: var(--sky) }` override in field-v8.css (with roster-grid carve-out)

**Decisions:**
- `--navy` text headings → `var(--ink,var(--navy))` fallback pattern throughout
- `--navy` accent/active states → `var(--sky,var(--navy))` fallback pattern
- No mass retrofit of apprentices.js heading colors (testing module, low priority)
- `var(--alt)` = #EEECEA introduced as warm-gray surface (avoids conflict with base.css --surface)
- `var(--green-v8)` = #3FA66A introduced (avoids conflict with base.css --green)

**Next:** EQ Shell v8 pass (same handoff package, React codebase).
