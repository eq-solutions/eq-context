# Session 2026-08-16 — built a personal task register for Royce (OneDrive), cross-checked it live twice, found and cleared a false alarm on eq-context's git state

## Built
- Switched Royce's default save location for Claude-delivered files from `Downloads` to `C:\Users\EQ\OneDrive - eq-power.com.au\` (his business OneDrive, confirmed live — no `royce` Windows profile exists on this machine, only the shared `EQ` account, so the path he first named didn't resolve).
- Built `EQ-Task-Register-2026-08-12.xlsx` + a companion `task-register.html` — a personal tracking artifact for Royce, not eq-context substrate. Covers the Cards SSO broker rollout sequence, the SEC-9/24 Netlify cleanup, the two key-rotation runbooks, and the open punch-list items, each with real click-by-click instructions (not just file pointers) after Royce asked for expandable per-row detail.
- Cross-checked every row against live state twice this session (PRs, `security-register.md`, `punch-list.md`, commit history) rather than trusting the register's own prior snapshot:
  - Flipped 2 items to Done: the mobile-screens review (closed 2026-08-11, no bug found) and the Cards-density nudge-stack fix (merged 2026-08-13, [eq-cards PR #235](https://github.com/eq-solutions/eq-cards/pull/235)).
  - **Caught an over-claim on the second pass:** PR #235's own description confirms it built strictly from the original scoping doc and never incorporated the "first-open popup / info overload" screenshots Royce said he was sending separately — exactly what `punch-list.md`'s own caution note said not to do. Split into a Done row (the shipped nudge-stack fix) and a new open row (the screenshot reconciliation, still outstanding).
  - Found 4 more real items sitting in `punch-list.md`'s "Waiting on you" section that had never made it onto the register: the `FIELD_PERMS_DRIFT_PAT` secret blocking security-settings-per-user-group's click-test, 3 unfinished template files (12 of 15 landed 2026-08-05, never reconfirmed), and the Document Sign-off Register's rollout-widening decision. Added all three, plus the two already-closed apprentices/simultaneous-users items for completeness.
  - Register is now 28 rows, all individually verified against live state this session, not carried forward from memory.

## Decided
- Royce: OneDrive over Downloads, business account specifically, going forward for anything delivered.
- Royce: "I am sorting this out in a different session" — the actual execution (Cards broker merge sequence, SEC-9/24 runbook, key rotations) is being run elsewhere. This session's job was keeping the tracking artifact honest, not doing the work itself.

## Deferred
- Punch-list #4's "Active" section in `system/punch-list.md` is now stale — it still shows the item as unbuilt-with-a-caveat, but PR #235 shipped 3 days after that note was written, without addressing the caveat. Not corrected in `punch-list.md` itself this session (that file is Royce's to edit per its own stated rule) — flagged here and in the OneDrive register instead.
- The screenshot reconciliation itself (Cards' first-open popup / info overload) — needs Royce to send/point to the screenshots before it can be scoped.
- Everything already tracked as open in the register (SEC-1/9/24, the Cards broker PRs, the two key rotations, the 3 template files, the sign-off rollout decision) — unchanged, still open, being handled in Royce's other session.

## Notes

**A "broken eq-context" alarm turned out to be a false one — investigated, not fixed.** Mid-session, `git status` on the shared root checkout showed 3 files in active merge conflict (`UU`), including `digest.md` with literal `<<<<<<< Updated upstream` / `>>>>>>> Stashed changes` markers physically in its content. Checked `origin/main`'s actual served copy directly (`git show origin/main:digest.md`) before assuming anything was live-broken — zero conflict markers, clean, current. A few minutes later the local working tree had self-resolved (another concurrent session's stash, literally titled "eq-context divergence fix — pre-rebase working tree 2026-08-14", finishing its own reconciliation). Left `stash@{0}` and the resulting local-vs-origin divergence untouched rather than pop/rebase over someone else's in-progress work — not this session's stash to resolve. **Given this file's own history today (see the many "session close 2026-08-15 (cont'd N)" entries above from unrelated concurrent sessions), this checkout is under heavy simultaneous load — a transient conflict here is not automatically a live incident. Check `origin/main` directly before escalating.**

**GOALS were UNSET in `system/TODAY.md`** for this entire session (confirmed via the session-start gate) — nothing here was prioritised or deferred by appeal to any deadline.

**No repo code was touched by this session.** Everything built lives in Royce's OneDrive, not in any tracked repo — the only eq-context writes are this log and the one-line `pending.md` flag below.

---

# Session 2026-08-16 (cont'd) — eq-solves-service Settings page fixed for Shell-embedded users, merged, deployed

## Built
- Audited `core.eq.solutions/sks/service/settings` per Royce's report of UI "that shouldn't be there." Root-caused live against `ehow` and `eq-canonical`: Service's own legacy sign-in record for the account (`service.profiles`/`auth.users` id `f4bd3058-…`) and the canonical Shell identity carried in the `eq_service_jwt` cookie (`85e30693-…`) are two different UUIDs on two different databases, so the settings page's `profiles` lookup always missed for anyone reaching it through Shell.
- Confirmed the concrete effects live: "Member Since"/"Last Login" rendered blank (the screenshot's "Invalid Date"), "Update Profile" silently no-op'd (0 rows matched, still reported success), and "Change Password" would have targeted an auth session that doesn't exist on `ehow` for that identity.
- Fixed [app/(app)/settings/page.tsx](../../eq-solves-service/app/(app)/settings/page.tsx) and [UserSettingsForm.tsx](../../eq-solves-service/app/(app)/settings/UserSettingsForm.tsx): Shell-embedded sessions now collapse Account/Profile/Password to a "Manage in Shell ↗" link, mirroring the existing Brand-deferral pattern already shipped in `TenantSettingsForm.tsx`. Standalone/demo logins are untouched — full forms still render for them.
- `tsc --noEmit` clean — but only after `npm ci` in this fresh worktree (no `node_modules` existed yet); the first typecheck attempt silently ran against a bare global `tsc` with no error output, which would have been a false pass if not caught.
- Committed [5d1893f](https://github.com/eq-solutions/eq-service/commit/5d1893f), pushed, opened [PR #733](https://github.com/eq-solutions/eq-service/pull/733), waited out CI (the one red check, `Integration tests (Supabase local)`, is this repo's known pre-existing gap per its own CLAUDE.md — `tsc + next build` is the real gate and passed), squash-merged to `main` at [2f40f58](https://github.com/eq-solutions/eq-service/commit/2f40f581991f1b4777e06cb6f6a66c1f3f4497c6).
- **Confirmed live, not just merged:** pulled the Netlify deploy record directly — production deploy `6a813da2` built from that exact commit, state `ready`, published 2026-08-16T04:35:17Z on `service.eq.solutions`. Confirms `eq-solves-service` behaves like eq-shell here: merging to `main` **is** the deploy, no separate trigger step, via Netlify's auto-build-on-push (already documented in the repo's own deploy table, now operationally re-verified rather than assumed).

## Decided
- Royce: implement the fix now (collapse to a "Manage in Shell" link) rather than a narrower patch, after confirming Notification Preferences was the only section doing real work in the embedded view.
- Royce: push it, open a PR, then merge it — each given as its own explicit instruction in sequence, not assumed from the audit request.

## Deferred
- **Not click-tested live in either branch.** No safe way to produce a valid Shell JWT locally (needs a production secret not present in local dev config, and pulling it just for a UI-visibility test wasn't a reasonable trade-off). No working standalone test account either — see next item. Worth Royce opening the real embedded URL once to eyeball it.
- **The practice/demo login is broken**, found while trying to get standalone credentials for testing — unrelated to this fix. `SignInForm.tsx` deliberately hides the demo tile because the demo tenant's data was lost in the urjh→ehow cutover (2026-06-22) and was never reseeded; the plumbing (`/demo` route, `DEMO_EMAIL`/`DEMO_PASSWORD`) is all still there, just pointed at nothing. Worth reseeding if the demo link is still wanted for anything (marketing, quick walkthroughs).
- **The original ask — auditing Shell's access-control/permissions screen for Service (the screenshot Royce shared showing the `service.*` permission matrix)** — was not reached this session. The settings-page bug was surfaced first ("straight away") and consumed the whole session.

## Notes
- **Worktree hygiene gap:** this worktree (`acb-check-report-wiring-1baa7e`) had neither `.env.local` nor `node_modules` — copied `.env.local` from the root checkout and ran `npm ci` to get a working dev server and a real typecheck. Worth remembering for any future worktree-based session on this repo: don't trust a clean `tsc`/dev-server result until you've confirmed `node_modules` actually exists.
- **Browser-pane quirk, not a code bug:** this session's Browser pane wasn't compositing frames (screenshot calls errored on it directly), which starves `requestAnimationFrame` — the exact mechanism React 19's streaming SSR uses to reveal a resolved Suspense boundary. Every page hung on the `app/loading.tsx` splash indefinitely, reproduced on the untouched sign-in page too. Workaround: `window.$RV(window.$RB)` in the console forces the reveal manually. Worth a note for any future session that hits a page stuck on a loading splash in this tool before assuming the app itself is broken.

---

# Session 2026-08-16 (cont'd 2) — eq-shell: per-role credential requirements, built end-to-end from a brain-dump audit through to a verified live deploy

## Built
- Built an Excel task register from two of Royce's brain-dump emails, audited every idea against live systems (not docs), ranked them; #4 (per-role minimum credential requirements) chosen as the starting priority.
- Scoped #4 with a Task Brief, ran `/decide` on the direction (including three workflow diagrams — current state, proposed state, proposed state with multi-org), then implemented with multi-org support built in from the start — nearly free once `org_memberships` (already created at every org-join point) was identified as the right storage layer, instead of a flat single-org field that would need a second migration later.
- Migration `2026_08_16_org_credential_requirements_role_scope.sql`: `org_credential_requirements` gains a `role` column (empty-string sentinel = "all roles", deliberately NOT NULL — PostgREST's upsert can only target a plain unique index, not a partial/functional one); `org_memberships` gains `eq_role` (a new per-org operational role, distinct from its existing member/admin column, which is Shell's own admin-tier gating and a different concept); `eq_worker_compliance_status()` extended to filter by the new role scope.
- Backend: `cards-approve-staff.ts` (both approval branches) and `shell-join-tenant.ts` now stamp `eq_role` at every point a worker joins an org; `org-credential-requirements.ts` rewritten for role-scoped GET/POST/DELETE; new `org-worker-roles.ts` function does a cross-plane join in application code, since the org/role data and the worker-list data live on two different Supabase projects and no single query can join them.
- Frontend: `StaffPage.tsx`/`MatrixView.tsx` wired so the Training Matrix (the compliance grid managers use) now checks each worker against their own role's requirements instead of one shared list for everyone.
- Also closed a separately-found gap while verifying the sign-up approval flow end to end: the Cards approval screen pre-selected "employee" with no way to distinguish "chosen" from "never looked at" — a rushed approval could silently misassign someone. Now starts blank; Approve is disabled until a role is actively picked.
- Full typecheck clean (0 errors, verified via a real `pnpm install` + full build in a fresh worktree, not just the per-edit hook — caught and fixed 2 real type errors first).
- Committed, pushed, opened and merged [eq-shell PR #1376](https://github.com/eq-solutions/eq-shell/pull/1376).
- Migration hand-applied to the control-plane database (jvkn — no CI apply path for this plane) — caught a real error on the first attempt (tried to drop the old uniqueness rule the wrong way; Postgres requires dropping it as a named constraint, not as a bare index), fixed, re-applied, and independently re-verified every column/grant/index afterward against the live database.
- Deploy verified live, not assumed from the merge: pulled the Netlify deploy record directly and confirmed it built from the exact merge commit, published clean, on core.eq.solutions.

## Decided
- Royce: fix the approval-screen silent-default gap too, not just flag it for later.
- Royce: extend the bootstrap now rather than defer the Training Matrix's role-awareness.
- Royce: build the larger-scoped cross-plane lookup properly (new function + wiring) rather than a narrower stopgap, after asking for the technical design explained first.
- Royce: run a full install + typecheck before continuing, rather than trusting the per-edit hook alone.
- Royce: push the PR → apply the migration → merge the PR → check the deploy, each given as its own explicit instruction in sequence.

## Deferred
- No admin screen yet to add a role-specific requirement (as opposed to one that applies to everyone) — the groundwork supports it, but today's UI can only add an org-wide requirement. Noted in the PR description as "Not included."

## Notes
- **Trust-model finding, verified live rather than assumed:** three different "role" fields exist across the platform. The one a worker can set on themselves is read by no authorisation check anywhere today (confirmed via an existing lockdown migration's own commentary) — wrong to gate a requirement on, since a worker could dodge it by relabelling themselves. The field used instead is admin-set only, and already had no ability for a worker to write to it directly as of a 2026-07-21 fix — so the new column added this session inherited that protection automatically, no extra lockdown work needed.
- A worker-identifier field needed to link the Matrix's data across two different databases turned out to already be flowing through to the frontend from an existing query that returns every column — it just needed to be read on the TypeScript side. Simplified that half of the build to pure wiring, no database-side change needed.
- Resolves the "minimum-requirements model" question Royce left undecided on 2026-07-07 — ticked off in `pending.md` this session with a pointer back to this entry.
- **False alarm caught and corrected mid-session:** briefly believed a stale, unrelated worktree showed a live, unpatched security gap (an org admin able to self-promote their own access level). Checked the real, current copy of that code before treating the stale worktree as current — it had already been fixed and verified live three weeks earlier. Correction given to Royce directly rather than left standing.

---

# Session 2026-08-16 (cont.) — EQ Field: timesheets/leave own-crew RLS drafted, two follow-ups spun off

## Built
- Drafted `supabase/migrations/20260816_timesheets_leave_own_crew_read.sql` (eq-field, ehow plane) — adds a `RESTRICTIVE` SELECT policy each to `app_data.timesheets`/`app_data.leave_requests`: own row, supervisor's crew (via `team_supervisors`/`team_members`), or manager/`field.view_all_crews`/approve-perm holders. Built on the `eq__caller_uid`/`eq__caller_staff_id` helpers already live from `20260721_field_timesheet_leave_approval_lifecycle.sql`. Chose `RESTRICTIVE` specifically because it ANDs on top of the existing tenant policy without needing to know that policy's name — this session had no live database access to check it directly.
- Committed alongside a paired `docs/reflection-log.md` entry (required by this repo's commit gate) and a `/brief`-gate flag (required by this repo's write gate) — both repo-specific mechanics, not skipped.
- Opened [eq-field PR #705](https://github.com/eq-solutions/eq-field/pull/705). CI green (tests+lint, deploy preview). **Not applied to any database** — the PR merging is safe/inert for the live app; dispatching the migration itself is a separate, explicitly-gated step.
- Stashed an unrelated uncommitted change (`import-export.js` CSV-import supervision guard, found sitting on `claude/mobile-p1-css-bundle` at session start) rather than carry it into this branch or lose it — recoverable via `stash@{0}` on that branch.

## Decided
- Royce: `staff`/`schedule_entries` — same tenant-id-only shape as timesheets/leave, but stay tenant-wide. Roster/directory visibility (who's on which job) is a real coordination need, not pay/health data; narrowing it risked silently breaking "who's on site today" for everyone. No change made to either table.
- Royce: on `licences` — corrected my own framing mid-conversation. Not "should it get the same fix as timesheets," but "does its actual behavior match the fact that a worker owns their own licence data and chooses what to share" — a different question, spun off with that framing as the test to satisfy.

## Deferred
- **jvkn's `licences` table's actual live RLS was never checked** — it turned out not to be an ehow table at all (different database, different repo, different apply path: eq-shell, hand-applied). This session's tool access didn't reach it. Spun off as a background task; user has since started it running in a separate session.
- **The write side of the same two tables has the identical gap** — any tenant member can still edit someone else's timesheet hours or leave details today, just not the approval decision itself (already separately locked down). Explicitly out of this fix's scope (read-only). Spun off as a background task; user has since started it running in a separate session.
- **zaap (the disposable EQ-side tenant) doesn't have this fix** — lower priority, no real data at risk, but flagged so it isn't forgotten: needs the prerequisite helper functions and a Teams-table equivalent ported first.

## Notes
- **Scope correction, caught before any SQL was written:** the finding as originally reported grouped `licences` together with the ehow tables as one tenant-id-only-RLS set. Checked against eq-shell's own migration headers (not assumed) and found `licences` actually lives on the control-plane database, not the tenant database — a different repo's territory entirely, governed by a different apply process. Corrected the scope before drafting anything, rather than writing SQL against a table that isn't there.
- **No live database access this session, by design of the environment, not by choice** — the database MCP tool showed as configured at the command-line level but was genuinely absent from this session's actual available tools (checked more than once, not assumed after a single miss). Compensated by sourcing every table/column referenced from already-committed setup files instead of guessing, and by choosing a fix shape that doesn't need to know the current state to apply correctly. New tool access (a working database connection, a GitHub connection) appeared later in the session, likely surfaced once the two spun-off background tasks started — not used, since the drafted fix was already complete and correct by then.
- **A real regression was caught by this session's own required self-check before it reached a commit, not by a later review:** the first draft of the crew-visibility rule would have accidentally cut 2 of SKS's 8 supervisors down to seeing only their own record, the moment it applied — because those 2 don't have a crew assigned yet, and Royce had already explicitly decided (2026-07-22) that an unassigned supervisor should keep full visibility until someone gets around to assigning them, rather than silently losing oversight of everyone. Fixed before staging the commit, not after.
- Same-day cross-repo check paid off: eq-shell merged a PR yesterday with an almost-identical-sounding title ("gate timesheet/licence reads..."). Read it in full before writing any code — it turned out to fix a different layer entirely (that repo's own admin-screen permission check), not the database-level gap this session closed, so there was no duplicate work. Worth naming plainly: these are two separate, complementary fixes for the same general class of gap, not one fix described twice.

---

# Session 2026-08-16 (cont'd 4) — eq-shell: gated 5 AI/OCR endpoints beyond session-only, constrained the Anthropic proxy against cost/SSRF abuse

## Built
- Fixed a P1 finding delivered pre-verified (file:line citations included): `netlify/functions/anthropic-proxy.ts` was an unrestricted proxy onto the server-side `ANTHROPIC_API_KEY` — any signed-in tenant member, any role, controlled model, `max_tokens`, system prompt, message body, and upstream sub-path, gated by nothing but a valid session.
- `anthropic-proxy.ts`: added `intake.import` permission gate (matches its only caller's existing UI gate), per-user rate limit (`anthropic-proxy::${user_id}`, 300s/60 calls/300s lockout via the shared RPC), a 3-model server-side allow-list (`claude-sonnet-4-5`/`claude-haiku-4-5`/`claude-opus-4-7`), `MAX_TOKENS_CEILING = 8192`, `MAX_BODY_BYTES = 512KB` (checked via `Buffer.byteLength`, not `.length`, to avoid undercounting multi-byte UTF-8), and pinned the upstream sub-path to `/messages` only (404 otherwise) — closes the SSRF-shaped risk of a caller steering the forward at an arbitrary `api.anthropic.com/v1/*` path.
- `ask-ops.ts`: per-source gating (`field.view_licences` for licence rows, `quotes.view` for quote rows), 403 only if the caller holds neither; the fetch step now only pulls domains the caller is actually permitted to see rather than over-fetching then filtering.
- `ai-briefing.ts` / `briefing-action.ts`: gated on `reports.generate_briefing` (manager-only); the briefing's rate limit sits after the cache-hit early-return so free cached loads aren't throttled, only real generation is bounded.
- `ocr-parse.ts`: gated on `field.manage_licences` + rate limit, ahead of the paid Google Document AI call. Its client page `LicenceOcrPage.tsx` had no `<Gate>` and its route was unwrapped — added both.
- `TenantHome.tsx`: client-side mirrors of all the new server gates (mobile actions card, pending-actions stat button, Ask bar, desktop TODAY'S ACTIONS column) — avoids every non-holder hitting a live 403 the moment the server gate shipped. Found and corrected a false in-code comment in the process: it claimed the dashboard return path was "already manager-only," but `WORKER_HOME_ROLES` only excludes employee/apprentice/labour_hire/subcontractor — supervisors were reaching it too, and only managers hold `reports.generate_briefing`.
- Extended `_shared/rate-limit.ts` — `checkAndIncrementRateLimit` previously only ever passed `p_key` to `check_and_increment_rate_limit`, inheriting the RPC's login-lockout-shaped defaults (5 attempts/15min/15min lockout). Added an optional `RateLimitOptions` param (`windowSecs`/`maxAttempts`/`lockoutSecs`) so AI-usage windows could be configured per-endpoint instead of borrowing auth-door defaults.
- `permission-enforcement-baseline.json`: `field.manage_licences` moved from `dead_keys` to `_resolved` (ocr-parse.ts made it live) — ratchet tightened, `permission-enforcement-drift.test.ts` passes with one fewer dead key.
- Verified the "coordinate with role-model review" instruction rather than skip it: the only in-flight role-review branch (`claude/eq-ops-permissions-review-62f9db`) had already merged as PR #1374 with no overlap — existing keys covered all 5 endpoints, nothing new needed inventing.
- Committed `49fd13a2` on `fix/ai-endpoint-authz`, pushed, opened [PR #1377](https://github.com/eq-solutions/eq-shell/pull/1377). Confirmed still OPEN (not merged) at session close.

## Decided
- Royce (via AskUserQuestion mid-session): the AI briefing + Ask bar on TenantHome (loaded by every signed-in user) should "match the data" — gate each panel on the permission that already governs its underlying data (`reports.generate_briefing` for the briefing, per-source for Ask), hide client-side for non-holders, rather than showing a broken/error state to anyone.

## Deferred
- **Not click-tested live per role** — verified by typecheck/lint/tests/build, not by a real signed-in session per role. Left as an unchecked PR test-plan item: intake's AI enrich, the home-page brief/ask bar as manager vs. supervisor, and the licence OCR flow as an apprentice (should now 403 / show "no access"). Left for Royce per the Completion Standard's authority model — production click-testing per role isn't a silent-resolve item.
- **PR #1377 merge itself** — explicit constraint in the original task ("Get Royce's explicit go before merging"). Not merged this session; also holding on the pre-existing eq-shell rule that auth-adjacent changes need explicit approval before deploy.

## Notes
- All 5 endpoints reused **existing** permission keys — no new key invented, per the task's explicit "coordinate, don't invent unilaterally" instruction. `field.manage_licences` was chosen for `ocr-parse.ts` over `intake.commit` deliberately: both currently resolve to exactly {manager, supervisor}, but `field.manage_licences` describes what the endpoint *exposes* rather than what its current caller happens to do next — the property that survives a future second caller. Documented in the baseline JSON's own `_resolved` entry, not just the PR body, so the next session that greps the baseline for context gets the reasoning too.
- Full verification chain run start to finish before opening the PR: `tsc -b --force` clean, `pnpm test` 353/353 (ratchet tightened), lint diffed against a `git stash` baseline of `main` on the 2 touched client files (0 new problems — 3 pre-existing errors matched main exactly, 1 pre-existing warning on `TenantHome.tsx` actually cleared as a side effect of adding `canBriefing` to the effect's deps array), `pnpm run build` clean (one local-only build failure along the way — missing `@fontsource/plus-jakarta-sans` in `node_modules` despite being lockfile-pinned — fixed with `pnpm install --frozen-lockfile`, not a real dependency gap; noted here so a future session doesn't mis-diagnose the same thing as a real missing dep).
- Task arrived as a fully-specified P1 finding (file:line citations, explicit task breakdown a–d, explicit coordinate/constraint instructions) rather than conversational prompting — treated as the brief itself; no separate narrative needed beyond the mechanical gate steps (worktree check, git state, DB N/A — no schema touched).

---

# Session 2026-08-16 (cont.) — EQ Intake: review-queue polish shipped, then self-critiqued and 6 more bugs fixed before close

## Built
- Fixed the reported duplicate-contacts merge crash: `ContactMergePreview`/`SiteMergePreview` already carried an `already_merged` flag from the server, but the UI ignored it and let "Confirm merge" fire anyway, throwing a raw RPC error. Preview now short-circuits to a done state instead. Fixed in both `ContactDuplicateMergePanel.tsx` and `DuplicateMergePanel.tsx` (sites had the same latent bug).
- Advisory rows (duplicate sites/contacts) now split into an actionable list vs. a collapsed "N resolved" section — a decided row leaves the working queue once there's nothing left to do, with a "Change answer" link if it needs correcting. Previously decided rows just accumulated in one flat list forever.
- Added a batch-approve toolbar to RemediationQueue's "Trade unknown" section — checkboxes + a bulk trade picker + one Approve button, reusing the same commit path as the single-row Approve.
- Restyled advisory rows to match the Trade unknown section's bordered-card look (previously a flat inline-flex row).
- eq-solves-intake [PR #116](https://github.com/eq-solutions/eq-solves-intake/pull/116) merged, CI green.
- User then asked for a critique of the whole piece of work. Ran a genuine two-pass independent review (correctness angles + cleanup/conventions angles via the Agent tool) against the merged diff, verified every candidate against the actual shipped lines (not just the diff), and found 8 real defects — 6 cheap/safe to fix immediately, 1 requiring a cross-repo server change (deferred, see below), 1 accepted as a reasonable design trade-off.
- Fixed and shipped the 6: a stale note that could attach itself to the wrong verdict after "Change answer"; a leftover note draft that could resurface after Cancel; the batch-approve button silently no-op'ing if an unrelated single-row action was mid-flight; batch approve swallowing the real per-row failure reason; an orphaned `selectedIds` entry when a row was resolved outside the batch flow; a stale cached merge-preview that could resurface after a verdict was changed away and back. Also capped the "resolved" section at 8 rows with its own expand toggle, matching the actionable list's existing pattern. All re-verified live in a browser against a mock Supabase client, not just typechecked.
- eq-solves-intake [PR #117](https://github.com/eq-solutions/eq-solves-intake/pull/117) merged, CI green.
- Scoped (not built) the one deferred finding: confirmed live against both tenant databases (ehow + zaap) the exact table names, columns, and constraints needed to add an "already merged" flag to `eq_site_advisory_summary`/`eq_contact_advisory_summary` in eq-shell, confirmed no other repo consumes those RPCs besides `@eq/intake`, and identified the next open migration number. Spun off as a background task (`task_13609108`) rather than drafted in this session — Royce's explicit call via `/decide`, to keep this session's scope inside eq-solves-intake and not reach into eq-shell unasked.

## Decided
- Royce: merge #116, then critique the whole piece of work — not a rubber-stamp ask, a real request for flaws.
- Royce (via `/decide`): leave the eq-shell migration for the already-scoped `already_merged` gap to the spawned background task rather than draft it in this session.

## Deferred
- **A "same"-verdict duplicate row can still render as actionable (with a live "Change answer" control) after a page reload, even if it was already merged in a prior session** — the summary RPCs don't expose merge status per row today, only the merge-preview RPC does, and only when explicitly clicked. Fully scoped (exact SQL, table names — `app_data.site_resolution_merge_log`/`contact_resolution_merge_log`, no unique constraint on `advisory_id` so the fix must use `EXISTS(...)` not a plain join, next migration number ~0247) and handed to `task_13609108` for a dedicated eq-shell session.

## Notes
- Several eq-solves-intake local branches with feature-sounding names (`mergepanel-demo-ui`, `site-advisory-console`, `archive-duplicate-flags`, `trades-settings-and-dismiss`, etc.) looked like unmerged in-flight work at a glance — checked each via `gh pr list --state all` before assuming duplicate-work risk; all were already squash-merged (PRs #72/#67/#95/#97) and just stale local refs. No action needed, but worth remembering `git merge-base --is-ancestor` alone would have misread these as unmerged (squash merges never leave the source branch as an ancestor of main) — matches the standing note in `system/worktree-registry.md`.
- All verification this session used a throwaway in-memory mock Supabase client in a temporary local harness file, built and torn down each time — no live tenant data was read or written by this session's own testing.

---

# Session 2026-08-16 (cont'd 5) — eq-shell: PR #1377 merged on Royce's explicit go, confirmed live

*Corrects the previous entry's "not merged this session" framing — Royce sent "merge 1377" in a later turn the same session.*

## Built
- Royce: "merge 1377" — explicit go-ahead, satisfying both the task's own constraint and the repo's standing auth-change approval rule.
- Re-verified before merging rather than trusting the earlier session's snapshot: PR #1377 was still `MERGEABLE`/`mergeStateStatus: CLEAN` against current `main`, which had moved (PR #1379 landed in between, unrelated, no conflict). All CI checks green — `typecheck · test · lint`, `gitleaks`, the tenant-drift/anon-grant/policy-lint checks, Netlify deploy preview.
- Squash-merged: `34d97647600621591333a4ce408b37810820d34d`.
- **Deploy verified live, not assumed from the merge** — pulled the exact Netlify deploy record: production deploy `6a814f11...`, `state: ready`, `context: production`, `commit_ref` matching the merge SHA exactly, published `2026-08-16T05:53:40Z` (~5m43s after merge — real build+upload time; the ~2-4s band documented elsewhere is trigger latency, a different measurement, not build duration). Live smoke request to `verify-shell-session` returned the expected 401.
- Updated `eq-context/eq/pending.md` and `eq-context/eq/changelog/eq-shell.md` from "PR open, not merged" to "merged, live."

## Notes
- **Shared-checkout contention again, twice in one session.** The plain `eq-context` checkout had a concurrent session rebase mid-commit during the earlier `/close` (ref-lock error on the first commit attempt), and by this turn had unrelated sessions' uncommitted edits sitting in its working tree (`eq/pending.md`, `sessions/2026-08-16.md`, `eq/changelog/eq-intake.md`, `ops/secrets-inventory.md` — none of them mine). `git pull --ff-only` correctly refused rather than risk a merge over someone else's in-progress work. Both substrate writes this session went through a disposable detached worktree instead of the shared checkout — see [[eq-context-shared-checkout-contention]]. A third write (this one) hit an actual rebase conflict in this same file — another concurrent session had appended its own block at the identical anchor line. Resolved by keeping both blocks, divider between them, same as every other same-day concurrent entry in this file — nothing from either session dropped.

---

# Session 2026-08-16 (cont'd 6) — eq-solves-service: P0 DB-side role enforcement written + dry-run verified, PR #735 open (not merged, not applied)

*A P0 security finding handed in directly via chat (verified live against ehow before this session started): every Service write was gated only in app code — the database enforced tenant isolation and nothing else, so any signed-in tenant member of any role could write maintenance checks, defects, test results and assets by calling PostgREST directly.*

## Built
- Re-verified the finding live before designing anything: 27 writable `service.*` views, not the 25 originally stated (`contacts` + `staff` also exist, both inert — their `app_data` base tables grant `authenticated` SELECT only, same shape as the 6 known-inert definer entities).
- Confirmed live via a direct PostgREST call that `app_data` is exposed alongside `service` (`PGRST106` lists `public, app_data, service`) — this is why gating only the `service.*` view triggers would have been bypassable (`Content-Profile: app_data` resends the same write straight to the base table) and is the reason this shipped as two files instead of one.
- **Migration 0208** (`app_data_write_role_gates.sql`) — role predicates added to `app_data` RLS for the 19 canonical objects whose view triggers are `SECURITY INVOKER` (verified `prosecdef = false` on every `tg_*_iud`); their inner DML runs as the calling user, so RLS catches both the view door and the direct `app_data` door in one change. Generalises the shape `app_data.contract_scopes` already ships.
- **Migration 0209** (`service_definer_trigger_role_gates.sql`) — the 6 `SECURITY DEFINER` entities (sites, customers, assets, customer_contacts, site_contacts, instruments) bypass RLS by design and their base tables grant `authenticated` SELECT only, so the trigger body is the only write door. Injects the guard into whatever function body is live *at apply time* (regex on the existing `assert_jwt_tenant` anchor) rather than pasting a captured copy — these functions are under active concurrent maintenance in this repo (0199, 0202 both touched them within the last few days) and a pasted body would silently revert a concurrent fix.
- Role tiers pulled from the app's actual gate call sites (`canWrite`/`canDoTestWork`/`canCreateCheck`/`canReopen`/`isAdmin`), broader-gate-wins where two call sites disagree on one table — `defects` and `assets` both needed the broader tier or a real in-use flow breaks (ACB/NSX fail-marking, inline Add Asset). `maintenance_checks` + its two children carry an assignee escape hatch (the app gate there is `canWrite(role) || isAssigned`) via a `SECURITY DEFINER` helper, built that way specifically to avoid the policy re-entering `maintenance_checks`' own RLS and recursing.
- Full `begin … rollback` dry run against live ehow for both files, nothing persisted: 0208 → 51/51 write policies gated, 17 SELECT policies untouched, post-condition check passed. 0209 → 16/16 functions guarded, all still `SECURITY DEFINER` afterward (the `pg_get_functiondef` round-trip preserves it), all 35 `service.*` triggers intact. Both rolled back clean — verified zero residue (no stray policies/functions) after.
- **The dry run caught a real bug before it shipped**: 6 of the 16 definer trigger functions write the tenant-assert anchor as lowercase `perform`, not `PERFORM` — the original case-sensitive injection regex would have aborted 0209 on the first mismatch. Found by actually running the dry-run diff rather than trusting the pasted-sample assumption; fixed to case-insensitive (`gi`) matching.
- Branched via a real git worktree (`C:\Projects\eqsvc-role-enforcement-wt`, `fix/db-role-enforcement-service-writes` off `origin/main`) rather than switching branches in the shared `eq-solves-service` checkout, which was 43 commits behind and held another session's uncommitted work (`scripts/migrate-service.mjs`, new fixture files) — left untouched throughout.
- Committed, pushed, opened [PR #735](https://github.com/eq-solutions/eq-service/pull/735). **Not merged.**
- `scripts/migrate-service.mjs --plan` needs `SUPABASE_ACCESS_TOKEN`/`SUPABASE_PROJECT_REF` — confirmed via `gh secret list` these exist only as GitHub Actions repo secrets, not in any local `.env.local` (main checkout or worktree). Replicated the script's exact diff logic by hand against the live ledger via the Supabase MCP: 216 repo files, 214 in ledger, exactly these 2 pending — matches CI's own "Plan (read-only)" check, which also passed.
- CI on #735: `tsc + next build` (this repo's real merge gate per its own `CLAUDE.md`) passed. `Integration tests (Supabase local)` failed — read the actual failure log instead of citing the repo's "pre-existing failure" note from memory unverified: dies applying migration **0152**, 56 files before either of mine and unrelated to this change, on `schema "app_data" does not exist` (the from-scratch CI bootstrap doesn't have that schema this run). 0208/0209 never got exercised, pass or fail, in that job.

## Decided
- Royce: "run the rolled-back dry run" — explicit go for the live `begin…rollback` verification against ehow.
- Royce: "push the branch and open the PR" — explicit go for both actions.
- No merge or apply decision made this session — both explicitly still open, gated on Royce.

## Deferred
- Merge of PR #735 — Royce's call.
- Actual apply to ehow via the governed `migrate-service.mjs` pipeline — separate step after merge, per this repo's deploy rules.
- Post-apply: confirm a real non-privileged JWT (apprentice/labour_hire/subcontractor) actually gets a 403 — couldn't mint a real Shell-issued JWT from this session to observe it directly; only verified the claim shape matches what `shell-auth` already writes and what the shipped `contract_scopes` policy already reads correctly in production today.
- Post-apply: confirm an assigned technician can still write their own check end-to-end in the real app.
- Post-apply: re-run Supabase advisors for any new findings.

## Notes
- **Correction to an assumption made mid-session, caught before it shipped:** I first guessed the two changelog files (`eq/changelog/eq-service.md` vs `eq/changelog/eq-solves-service.md`) split along the `C:\Projects\CLAUDE.md` "eq-solves-service (repo) vs eq-service (planned move)" naming trap. Wrong — both files' own headers say the real cause is two different sessions independently starting parallel logs for this same repo on 2026-08-14, neither aware of the other; PR numbering doesn't cleanly favour either (`eq-solves-service.md`'s newest dated section is 2026-08-08 / #692, `eq-service.md`'s is 2026-08-14 / #726-730 — genuinely interleaved, not superseded). Both files' frontmatter already flags this as **deliberately unreconciled, flagged for Royce** — "not a cleanup, a judgement call about the record itself." I appended this session's entry to `eq-solves-service.md` (matches this repo's identity most directly) rather than trying to merge or pick a winner, which isn't mine to decide. Told Royce directly rather than letting it sit quiet.
- **The brief that started this session undercounted the writable-view surface**: stated 25 `service.*` views; live count is 27. Not a live gap (both extras are inert), but worth knowing if either ever gets a real write trigger without this same role-gate pattern applied alongside it.
- **`migrate-service.mjs`'s required secrets are CI-only.** Any future session needing to run `--plan`/`--verify` locally will hit the same wall; the hand-replication workaround is documented in PR #735's body and worked cleanly, but the real script needs CI or a shell with those two vars exported.

---

# Session 2026-08-16 (cont'd 7) — eq-cards: worker consent-scope enforcement gap found, fixed, applied, merged, deployed live

## Built
- Found (verified live before writing anything): `sharing_scope` on eq-cards' /connect flow was stored and shown back to workers but enforced nowhere — the only SELECT policy on `public.licences` (`licences_read`) was `is_org_admin_of(user_id) AND is_private=false`, no scope term. Migration files' own `org_admins_read_member_licences` policy (0006) is NOT live — consolidated into `licences_read` out of band at some point; migration files overstate what's actually enforced, worth remembering for future eq-cards work.
- Same sweep found 3 more live `is_private` gaps 0096 never closed outside the licences table itself: `org_admins_read_member_licence_photos` storage policy (no is_private term at all — 7 photo objects across 6 private licences readable by 2 orgs' admins at time of writing), `eq_get_org_credential_gaps` ("held" true for private licences), `eq_get_licences_expiring_on` (missing the is_private filter its sibling `_within` got in 0046).
- Wrote migration `0128_enforce_sharing_scope_on_licence_reads.sql`: new `org_memberships.sharing_scope` column (default `'full'`, NOT NULL, check constraint), stamped by trigger `tg_org_membership_sharing_scope` from the worker's own `org_access_requests.sharing_scope` — denormalised onto the standing relationship rather than read off the request queue, since a member can have multiple requests and invite-claimed/admin-upserted members have none. New `is_org_admin_with_credential_access()` replaces `is_org_admin_of()` on every employer-facing licence read path: `licences_read` RLS, the licence-photo storage policy, `eq_get_org_licences`, `eq_get_org_credential_gaps`, `eq_get_licences_expiring_on`, `eq_get_licences_expiring_within`.
- Replay-tested against live jvkn in rolled-back transactions (`BEGIN...ROLLBACK`) impersonating real admin/manager/worker identities via RLS (`SET LOCAL role authenticated` + `request.jwt.claims`) before writing a single line of applied SQL — covered the join path, the no-request/invite-claim path (defaults to full), and both admin branches (org-role admin, Shell tenant manager).
- Caught a real gap during replay-testing that the first draft of `is_org_admin_with_credential_access` missed: a caller administering 2+ orgs the same worker belongs to could have a full-scope relationship in one org leak visibility into a second org regardless of that org's own scope — inherent to `public.licences` having no `org_id` of its own. Fixed: predicate now requires at least one shared full-scope org AND none basic (was: any full exists). Confirmed zero real (admin, worker) pairs have this shape on live jvkn today via direct query — a structural hardening, not a live exposure closed.
- Fixed `outgoing_requests_banner.dart` (eq-cards Flutter): the pending-application banner unconditionally told a worker "they'll be able to see your licences" regardless of which scope they'd actually picked — now conditional on `request.sharingScope`.
- Exposure audit: 0 of 54 historical `org_access_requests` ever chose `'basic'` (all `'full'`) — so no worker's actual restricted choice was ever violated; no notification obligation on that front. The `is_private` photo-storage gap was the real, separate, live exposure (7 objects / 6 licences / 6 workers / 2 orgs).
- Pushed branch `security/enforce-sharing-scope-licence-reads`, opened eq-cards [PR #251](https://github.com/eq-solutions/eq-cards/pull/251).
- On Royce's explicit go: drift-checked all 7 touched live objects immediately before applying (zero drift since replay-testing) → applied `0128` to jvkn via Supabase MCP `apply_migration` → verified live (column/constraint/trigger/policies/grants all match design; 56/56 active memberships correctly backfilled to `'full'`; spot-verified via RLS impersonation that a specific previously-exposed private licence is now unreadable by the admin who could read it before; ran `get_advisors` security check — no new findings, only the pre-existing "SECURITY DEFINER callable by authenticated" WARN class every function of this shape already carries).
- Updated PR #251's description to reflect applied+verified status (original body, written before the apply step, said "not yet applied").
- On Royce's explicit go: squash-merged PR #251 to main (`e4419f2`), branches cleaned up both sides.
- On Royce's explicit go: triggered `Build & Deploy` (`workflow_dispatch`) on main — edge functions redeployed to jvkn, Flutter web rebuilt and pushed to Netlify, source maps uploaded to Sentry. Run [31930472119](https://github.com/eq-solutions/eq-cards/actions/runs/31930472119), completed success in under 4 minutes.

## Decided
- Royce: apply the migration to jvkn (explicit go, after the fix was written and replay-verified).
- Royce: push the branch and open the PR (explicit go).
- Royce: merge PR #251 now — chose "merge now" over "I'll review first" or "merge + deploy together" via AskUserQuestion, then separately instructed the deploy.
- Royce: deploy it (separate explicit instruction after merge).

## Deferred
- Whether the employer-initiated "we want to add you" connect flow (`eq_cards_request_worker_access`) should also offer a worker a basic/full scope choice — it currently has no scope parameter at all, always full by construction. Spun off as `task_9e421791`; Royce started it in a separate session, still running as of this close.

## Notes
- Supabase MCP was down at session start (`supabase: ... connection timed out after 30000ms`); did all pre-apply live verification via `supabase db query --linked --project-ref jvknxcmbtrfnxfrwfimn` (CLI) instead, working around a broken `config.toml` email-template path reference by using a scratch `--workdir` with a minimal `config.toml`. MCP reconnected mid-session and was used for the actual apply + post-apply verification.
- Near-miss: briefly `cd`'d into `.claude/worktrees/worker-data-consent-sync-58aa9f` (a stale detached-HEAD worktree, unrelated to this task) to inspect it per the session-gate worktree check, and the Bash tool's cwd stayed there afterward — a plain `git status`/`git diff` came back clean/empty even though real edits existed, because they were being checked against the wrong repo. File writes were unaffected (absolute paths used throughout), but git commands were silently wrong until caught by re-running with an explicit `git -C <path>`. Worth remembering: after inspecting any worktree, verify cwd before trusting a bare `git status`.
- A first attempt to watch the deploy workflow run via `Monitor` (polling `gh run view` + `comm -13` diffing) never fired a single event across its full 10-minute window despite the run completing successfully partway through — a bug in the polling script, not the deploy. Caught by directly re-querying the run instead of trusting the silence.
- `eq_get_org_credential_gaps` doesn't filter by requirement role (unlike `eq_worker_compliance_status`, which does) — noticed in passing while drift-checking, pre-existing and unrelated to this fix, not touched.

---

# Session 2026-08-16 (cont'd 3) — eq-shell: Required tickets bar gained a role picker, shown as a mockup first, built, merged, deployed live

## Built
- Closed the one deferred item from this morning's per-role credential requirements build (#1376): `RequiredTicketsBar` (Staff > Training matrix) could only ever add a requirement that applies to everyone — the backend already accepted a role scope, there was just no UI path to it.
- Showed an interactive mockup of the proposed bar first (role dropdown in the add flow, a role badge on scoped pills) before writing any code, per Royce's "describe / show me first." Clarified on request that this isn't a new screen — it's the same bar that already sits above the Training Matrix, gaining one new control.
- Built it to match the mockup exactly: pills show a small role badge when scoped; the add flow's role dropdown defaults to "All roles" and narrows the ticket list to exclude combinations already required for the selected role; removing a pill only removes that exact (ticket, role) row, so an org-wide requirement and a role-scoped one for the same ticket coexist independently.
- No backend or migration work — reused the API shipped this morning as-is. Renamed a duplicate role-label map (`SELF_JOIN_ROLE_LABELS` → `EQ_ROLE_LABELS`) so two places in the file share one canonical 6-role list instead of two copies of the same literal.
- Reused the same worktree from this morning's build on a fresh branch off current `main`, since its original branch had already been squash-merged (rebasing it directly would have shown as a no-op diff against the squash).
- Verified: typecheck clean, lint clean (no new issues — confirmed the one pre-existing warning in the file sits ~800 lines from anything touched). Not click-tested live — flagged plainly rather than assumed, matching this repo's own recent precedent for UI-only changes that need a real signed-in session to exercise.
- Committed, pushed, opened [eq-shell PR #1378](https://github.com/eq-solutions/eq-shell/pull/1378), waited out CI (all green), squash-merged (`bd732542`).
- Deploy verified by commit ancestry, not just merge success — two other sessions merged within the same minute, so the commit's own build was no longer the "newest" production deploy by the time it was checked. `git merge-base --is-ancestor` against the current live commit confirmed it shipped anyway — the exact concurrent-merge scenario the global rules describe, caught correctly rather than mis-read from a deploy timestamp.

## Decided
- Royce: build it as shown, after asking where it would live (confirmed: same bar, same spot, not a new screen).
- Royce: push a PR (explicit instruction).
- Royce: merge the PR (explicit instruction).
- Royce: check the deploy (explicit instruction, twice — once after merge, once more after confirming, wanting the ancestry check specifically rather than a deploy-title read).

## Deferred
- None new — this closed out the one item deferred from the morning's build.

## Notes
- The mockup-first approach caught a real ambiguity before any code was written: Royce's first response to "build it as shown?" was a clarifying question about where the control would even live, not a yes/no — worth remembering that a mockup can surface placement confusion a written description wouldn't have.

---

# Session 2026-08-16 (cont'd 4) — eq-field timesheets/leave write-side RLS drafted; traced why neither it nor its read-side sibling can dispatch yet; cleaned up 3 duplicate/test SKS staff records; confirmed the QR onboarding rollout is the actual fix, not a conflict

## Built
- Designed and committed `supabase/migrations/20260816_timesheets_leave_own_crew_write.sql` on eq-field (branch `claude/p1-timesheet-leave-own-crew-rls`) — companion to the already-drafted read-side migration (PR #705). RESTRICTIVE INSERT/UPDATE/DELETE policies mirroring the read-side own/crew/manager shape, verified against real usage (`saveTsCell`, the `leave.approve`/`leave.archive` client gates) rather than assumed. Also closed a DELETE-side gap not in the original finding — found live via `pg_policies`, same tenant-only shape as the other commands. Paired reflection-log entry written to satisfy this repo's commit gate. Draft only, not applied — pushed to origin.
- Traced the actual identity-linking mechanism this depends on, live, across eq-field/ehow and eq-shell/jvkn: `app_data.staff.user_id` (ehow) is populated by the `workers-canonical-sync` edge function FROM `public.workers.user_id` (jvkn), which only gets set by Cards signup or the admin-only `backfill-worker-links.ts` — nothing automatic closes it. Found 44-47 SKS staff (9 supervisors) have never logged in at all (confirmed: zero match to `shell_control.users` by phone or email), a static backlog unchanged since 2026-07-05. This blocks BOTH the read-side and write-side RLS migrations from safe dispatch — shipping either would lock those people out of their own timesheet/leave the moment they do sign in. Delivered the named list (CSV) to Royce.
- Deleted 3 live `app_data.staff` rows on ehow at Royce's explicit request, after verifying zero dependent data first: a literal "test"/"pilot" placeholder and two empty duplicate "Emma Curth" rows. Left a third, real "Emma Jane Curth" row alone — it has a real leave request + 5 rostered shifts attached, and the DB's own foreign keys would refuse the delete anyway.
- Ran `/decide` on "should we delete all workers that haven't logged in yet" — recommended against: the unlinked population is exactly the people Royce is about to onboard via QR codes, not dead weight.
- Traced the actual QR/invite claim path live (`create-worker-invite.ts` → `eq_cards_find_or_create_worker_for_invite`, including reading the current RPC body on jvkn) to answer "will this create duplicates" — the matching logic is real, not just commented (adopt-before-create by phone/email, advisory-locked against races). Cross-checked all 44 remaining unlinked people's phone/email against their existing canonical worker record: all match cleanly today, so no duplicate risk as long as invite forms are filled from Field's existing contact data rather than re-typed.
- Updated memory (`project_worker_identity_mobile_login.md`) with the confirmed, still-stuck backlog finding, extending the existing 2026-07-13 snapshot rather than duplicating it.

## Decided
- Royce: delete the test/pilot record and the two empty Emma Curth duplicates (not the third, real one).
- Royce: proceeding with QR-code onboarding, apprentices first — confirmed this doesn't conflict with the RLS migration work; if anything it's the actual fix for the blocking dependency.
- Not yet explicitly ratified in words, only recommended: don't bulk-delete the remaining ~44 unlinked staff. Royce moved on to the duplicate-records question without objecting, but this wasn't a direct "yes."

## Deferred
- Dispatching both RLS migrations (read + write) — blocked on the identity-linking gap above; resolves as the QR rollout lands people, or needs an explicit call to proceed anyway. Logged in `sks/pending.md`.
- "Emma Jane Curth" (real leave/roster data, currently `active=false`) — worth Royce checking whether that's correct rather than assumed.
- A friendlier client-side error for "authenticated but not yet linked" in Field — suggested as a mitigation, not built.
- jvkn licences RLS check + the disposable EQ-tenant port — already-tracked, separate tasks, untouched this session.

## Notes
- This was the spawned background session from the eq-field investigation task (`task_874574bc`) — its working directory turned out to still be rooted in the eq-field checkout rather than a separate eq-shell one; cross-repo reads used absolute paths and the Supabase MCP directly rather than `cd`, which avoided the stale-cwd class of mistake logged elsewhere in today's file.
- eq-context's local clone was 1 commit behind `origin/main` at investigation start; read historical/reference files via `git show origin/main:<path>` rather than pulling in the shared checkout (blocked by the F9 guard — `git pull` is unsafe there under concurrent sessions).
- The "44 unlinked" figure here is a live count, unrelated to the "44 SKS workers" referenced in `eq/pending.md`'s One-login P5 item (that one is about migrating off the SKS NSW Labour standalone app) — coincidental overlap in the number, different population, worth not conflating if either is revisited.
- Mid-close, a system reminder claimed `sks/pending.md` had been externally modified back to its pre-edit state; a fresh direct read showed the edit was actually intact — treated as a stale snapshot rather than re-applying blind, verified before acting either way.

---

# Session 2026-08-16 (cont'd 8) — eq-solves-service: nav-visibility tier-inversion + missing service.view entry gate fixed, PR #736 open

## Built
- P2 finding delivered via chat with file:line citations (nav built by exclusion — any role the code doesn't name gets full nav). Re-verified live before touching anything: 2 of the cited paths were wrong (`components/Sidebar.tsx` → actually `components/ui/Sidebar.tsx`; `components/UserSettingsForm.tsx` → actually `app/(app)/settings/UserSettingsForm.tsx`), and the claimed "subcontractor missing from ROLE_ORDER/ROLE_LABEL" gap was already fixed on `origin/main`.
- Branch `fix/integration-ci-app-data-bootstrap` was 43 commits behind `origin/main` and held another session's uncommitted work — same `scripts/migrate-service.mjs` state noted in the "cont'd 6" #735 entry above. Branched a fresh worktree (`.claude/worktrees/service-nav-role-gate`, `fix/service-nav-role-gating`) off current `origin/main` instead; left the dirty branch untouched throughout.
- Confirmed real: `Sidebar.tsx`'s `isTechnician = role === 'employee'`, and a hand-copied duplicate in `layout.tsx`'s embedded Shell nav bar (#729), both hid Records/Insight by excluding only `employee` by name — apprentice/labour_hire/subcontractor all fell through and saw both sections (the widest nav of any non-manager role); employee (the actual on-site tech) was wrongly hidden from pages it can reach. Both now gate on `canViewRecords` (canonical `entity.view`), the same permission already enforced on the 7 pages Records links to (#716).
- Found and fixed a second real gap: `service.view` (canonical grant: manager/supervisor/employee/apprentice, not labour_hire/subcontractor) was never checked as an entry gate — `app/(app)/layout.tsx` only checked for an active `tenant_members` row. Wired `canViewService()` with a denial screen matching the existing `NoTenantGate`.
- Read `eq/identity/nav-access-matrix.md` (2026-08-14, undecided) before building — already documents this exact drift pattern suite-wide. Did not build a cross-app shared nav config (not this session's call); used Service's own already-established per-file pattern instead.
- Found and corrected a wrong claim in this repo's own permission-enforcement drift-guard baseline (`tests/lib/utils/permission-enforcement-baseline.json`) — asserted `service.view`'s grant was "every non-empty role tier"; the live `@eq-solutions/roles` v2.5.7 matrix says otherwise. Dropped `service.view` from the dead-key list (matches the file's own pattern from #712/#713/#716/#717) and rewrote the note in place.
- Verification: `tsc --noEmit` 0 errors; full `vitest` 442/442 (77/77 after the baseline fix); `npm audit --audit-level=high` 0 vulnerabilities. `npm run build` failed on a Turbopack workspace-root error — confirmed pre-existing and unrelated by reproducing a (differently-shaped) build failure on a completely untouched sibling worktree (`eq-solves-service-sessionexpiry-wt`, missing `.env.local`).
- Committed (`be0156e`), pushed, opened [PR #736](https://github.com/eq-solutions/eq-service/pull/736).
- Royce: "check ehow for labour_hire or subcontractor rows first." A Supabase MCP became available mid-session (absent at session start, confirmed via ToolSearch). Queried `service.tenant_members` live, grouped by role/is_active: 5 active rows — 2 manager, 1 supervisor, 2 employee — zero apprentice/labour_hire/subcontractor. Confirms the new entry gate has zero effect on any current user. Updated PR #736's description (had flagged this as unverified), the baseline.json note, and this session's project memory to match, rather than leave a stale "couldn't check" caveat sitting in any of them — second commit (`e5b34ba`), pushed.

## Decided
- Royce: "/decide and build" — proceed with the full brief (nav fix + duplicate-gate fix + the new `service.view` entry gate + a chat-only recommendation on `service.close`/`service.create`) as one PR now, rather than holding the entry gate back for a separate confirmation round.
- Royce: "check ehow for labour_hire or subcontractor rows first" — verify the one open crux live rather than leave it inherited/unconfirmed.

## Deferred
- `/job-plans` has no view-level role gate at all (its 4 RECORDS_PATHS siblings got `entity.view` in #716, job-plans didn't) — spun off as `task_68813d6a`; Royce has since started it running in a separate session.
- `service.view_commercials` (a real canonical key, distinct from the tenant-level `commercial_features_enabled` flag) is completely unwired anywhere — spun off as `task_61bda775`; Royce has since started it running in a separate session.
- `service.close` vs `service.create`: recommended, not decided unilaterally — both stay distinct. Currently identical grant sets by design, already independently governable via the existing `canCloseWorkOrder`/`canWrite` split (#712), same pattern already applied to `entity.edit`/`entity.manage_activation`. Left for wherever the broader role-model review lives.
- `eq/identity/nav-access-matrix.md`'s "decide the long-term fix for nav-visibility drift" item is still open — this session fixed a further, more serious instance of the same drift class (tier-inverted gating, not just missing items) but didn't make the suite-wide shared-config-vs-checklist call. Noted in `eq/pending.md`.
- PR #736 itself — not merged, not deployed. Netlify auto-deploys eq-solves-service on merge to main.

## Notes
- The finding as delivered read as pre-verified (file:line citations, explicit task breakdown) but was stale in 2 of 4 specifics — caught only by fetching `origin/main` and diffing against the actual current files rather than trusting the citations. The branch was 43 commits behind; the missing history included the exact prior art (#707–#719, #728–#730) this fix builds on.
- This session's PR #736 and this morning's PR #735 (see the "cont'd 6" entry above) are two independent, complementary fixes found the same day in the same repo by two different sessions — #735 is DB-level write RLS (any role can bypass app-layer checks via direct PostgREST), #736 is app-layer nav/entry-gate role correctness. Read #735's own worktree and PR before touching anything, confirmed zero overlap, left it untouched.
- A Supabase MCP was unavailable at session start but became available later without any action taken to enable it — same pattern the "cont'd 4" eq-field entry above independently noted the same day. Used it the moment it appeared rather than continuing on the earlier "no MCP available" assumption.
- Corrected two now-provably-wrong claims found stored in this repo's own substrate: the drift-guard baseline's note, and this session's own project memory (built from that same baseline note, so it inherited the error). Both fixed in place rather than left to mislead the next reader — the same lesson this exact baseline file already logged for itself after #710→#713.

---

# Session 2026-08-16 (cont'd 9) — eq-cards: 3 permission-audit gaps closed (dead JWT minter, empty credential list, unreachable admin policy), merged + deployed live

## Built
- Verified all 3 findings from a handed-off 2026-08-16 permission audit against live jvkn before writing anything — migration files and doc claims weren't trusted alone.
- `netlify/functions/shell-verify.js`: confirmed undeployed by curling the live endpoint directly (returns Netlify's SPA-fallback `index.html`, not a function response) rather than inferring from `netlify.toml`/`deploy.yml` alone — `deploy.yml`'s zip-upload only ever includes `build/web`. Retired on Royce's explicit call (AskUserQuestion: retire vs. add the missing tenant-entitlement check) — deleted the file and the now-empty `[functions]` stanza in `netlify.toml`.
- `worker_credentials` had an owner-only RLS policy and no org-admin read path, so `AdminWorkerDetailScreen`'s credential list silently returned `[]` for every worker but the admin's own. `worker_credentials` carries 0 live rows today — the `0122` write RPCs have never actually been exercised in production. New org-scoped read RPC (`eq_cards_admin_list_worker_credentials`, migration `0129`) mirrors `0122`'s write-path predicate (`is_org_admin` + `is_worker_in_org`) exactly, matching `eq_cards_admin_list_workers`'s existing read-RPC shape. Deliberately not gated on the sharing_scope-aware predicate from `0128` (see cont'd 7 above) — that protects a different table's worker-submitted /connect flow; retrofitting scope semantics here would mean touching `0122`'s already-shipped write RPCs too, out of scope for this fix. Threaded `orgId` through `admin_worker_repository.dart`'s `listCredentials`, the `@riverpod` `adminWorkerCredentials` provider (now keyed on a `(String, String)` record rather than adding a second positional param, to keep the hand-authored generated-file patch modeled on a single-argument shape already proven twice in the same file — no local Flutter toolchain to run `build_runner` and check a genuinely new multi-arg shape), and all 4 call sites in `admin_worker_detail_screen.dart`.
- `org_join_requirements`'s admin-manage policy (`0104`) gated on the raw `org_memberships.role='admin'` check — reachable by only 3 of 17 real org admins (verified live), because it was written ~57 minutes before `is_org_admin()`'s canonicalisation (`0105`) landed the same day and nobody went back to update it. Aligned to `is_org_admin()` (migration `0130`) — pure widening, same pattern `0105` already applied twice elsewhere in this schema.
- Both migrations applied live to jvkn via Supabase MCP before merge, matching this repo's own established apply-then-commit-as-source-of-record convention (`0105`, `0128`). `get_advisors` spot-checked after — only the expected boilerplate "SECURITY DEFINER callable by authenticated" advisory, same class every existing admin RPC in this codebase already carries.
- Committed, pushed, opened eq-cards [PR #253](https://github.com/eq-solutions/eq-cards/pull/253) on Royce's explicit "commit this and merge." Watched CI (migration-hygiene + `flutter analyze` + `flutter test`) go green — the real check on the hand-authored record-typed provider change, since no local toolchain was available. Squash-merged (`f55a126`).
- Deployed on Royce's separate explicit "deploy it": triggered `workflow_dispatch` on `deploy.yml`, watched both jobs (Flutter build/Netlify deploy, Supabase edge functions) complete green, confirmed live via the site's ETag changing (`47f39736…` → `6edad751…`) rather than trusting the green run alone.

## Decided
- Royce: retire `shell-verify.js` rather than harden it (AskUserQuestion, informed by PR #250's same-day, different-session error-handling fix to the same file — didn't change the deployment finding, but meant the file had real recent attention worth surfacing before deleting).
- Royce: use a dedicated worktree for the fix rather than the shared root (AskUserQuestion) — the shared `C:\Projects\eq-cards` checkout had moved from this session's starting branch to `main` under a concurrent session mid-task; recovered cleanly (the original branch was already squash-merged as PR #251 — cont'd 7 above — so nothing was lost), but is the second time this exact failure mode has hit an eq-cards session.
- Royce: "commit this and merge" (explicit).
- Royce: "deploy it" (explicit, separate from the merge instruction).

## Deferred
- **eq-cards PR #221 and its eq-shell companion #1294 are now stale, effectively orphaned by tonight's retirement.** Both open since 2026-08-10 (`eq/pending.md` had the full history — read during `/close`, not found before deleting the file). They propose rewriting `shell-verify.js` into a broker relaying to a new eq-shell endpoint, motivated by removing eq-cards' own copies of `SUPABASE_JWT_SECRET`/`SUPABASE_SERVICE_ROLE_KEY` (jvkn)/`EQ_SESSION_SALT`. That motivation still stands, but the file they modify no longer exists on `main`. Flagged in `eq/pending.md` with a recommendation (close both; deleting achieves the same secret-reduction goal more directly since those 3 secrets are now unreferenced in eq-cards' Netlify project) — not actioned, both are Royce's PRs/project to close.
- Live click-through of the credential-list fix specifically not done — verified via live RLS/RPC checks, CI, and the deploy's ETag change, not an actual signed-in admin session.
- Whether `worker_credentials` should eventually respect `sharing_scope` like `licences` now does — flagged as a deliberate, reversible-later scoping choice in migration `0129`'s own comment, not built.

## Notes
- The brief-gate (`/brief`) blocked mid-edit on this session's first Write/Edit attempt — its own git-state step is what surfaced the branch having moved out from under the session. Worth remembering the gate isn't just process overhead; it caught something real this time.
- Cross-references cont'd 7 above (eq-cards worker consent-scope / migration `0128`) — that PR's description was updated post-apply to say "applied," but its underlying commit message (immutable) still reads "not yet applied," which is what this session's independent live-verification actually caught before relying on either.
- `.claude/worktrees/p3-permission-audit-cleanup` (branch `security/p3-permission-audit-cleanup`, this session's) still on disk post-merge — routine cleanup, same as this file's own convention elsewhere calls "worktree + branch removable."

---

# Session 2026-08-16 (cont'd) — eq-field access-control cleanup (v3.5.502/503), then a real Shell Access Control gap found and closed across two eq-shell PRs

## Built
- eq-field v3.5.502 ([PR #702](https://github.com/eq-solutions/eq-field/pull/702)): `field.manage_pipeline` made fully opt-in (removed from every role's default array — grant-only via Shell now); `field.manage_teams` split out of `field.manage_roster`; `field.manage_apprentices` added (previously zero permission key, gated on raw `isManager`); fixed a PIN-session Documents-to-Sign bug.
- eq-field v3.5.503 ([PR #704](https://github.com/eq-solutions/eq-field/pull/704)): Email Templates admin page moved into the Manage nav group and given its own permission key (`field.manage_email_templates`, split out of `leave.approve`); fixed an empty "Pipeline" nav heading regression v3.5.502 introduced.
- Both merged, deployed, deploy preview smoke-tested before merge.
- Follow-through into eq-shell, triggered by verifying whether the 3 new keys were actually usable from Shell's Access Control page (they weren't):
  - **[eq-shell PR #1379](https://github.com/eq-solutions/eq-shell/pull/1379)**: Shell's Custom Group permission picker reads `src/lib/fieldFinePerms.ts`, a hand-vendored snapshot of eq-field's `permission-matrix.js` pinned at v3.5.467 (2026-08-07) — stale, so none of today's 3 new keys were grantable. Re-vendored with the 3 new keys. Also fixed a real bug in the drift-guard's own extraction script: it discovers "real" Field keys by scanning the `manager` role's default array, but `field.manage_pipeline` was just removed from every role's default array (opt-in only now) — the guard could no longer see it at all, meaning the next mechanical re-vendor would have silently dropped Shell's only grant path for it. Added an explicit `OPT_IN_ONLY` allowlist entry. Verified the 77-key vendored set against live eq-field source with the exact comparison logic CI runs — exact match. `tsc`/lint/full suite (353/353) clean. Merged (`19c4cb07`), confirmed live via commit-ancestry check against the newest production deploy.
  - **[eq-shell PR #1380](https://github.com/eq-solutions/eq-shell/pull/1380)**: Royce hit the Access Control page live right after #1379 shipped and found the 3 new keys still weren't discoverable — the Base Permissions matrix and its per-role "· Field" panel only ever show the 9 canonical `field.*` keys; nothing hints that ~70 more fine-grained keys exist unless you already know to open "+ New group." Ran `/decide` on "fix the hint" vs. "build a unified one-screen access-control redesign" (Royce's bigger idea) — recommended shipping the narrow, safe fix now and treating the redesign as its own design-first project, given merging the two permission layers would blur a deliberate architectural boundary (`eq-roles`' own README: "Shell owns identity, Field owns its own fine-grained actions") and a naive single-screen merge risks becoming an 86-checkbox wall. Royce agreed. Added a one-line pointer on the Field panel only (no other app has an equivalent vendored layer). `tsc`/lint/full suite clean. PR blocked on an unrelated **required** check (`Schema drift + anon-grant + policy-lint`) failing repo-wide — traced to 3 live control-plane (jvkn) functions with no committed migration file (`eq_cards_admin_list_worker_credentials`, `is_org_admin_with_credential_access`, `tg_org_membership_sharing_scope`), confirmed unrelated to this PR's diff and unchanged across a 10-minute wait + manual re-run. Merged via `gh pr merge --admin` on Royce's explicit "override and merge it," after walking through why that's low-risk for this specific PR (verified-clean diff, override doesn't change the underlying drift either way) versus the standing risk of normalizing overrides on a check that's caught real incidents before. Merged (`03dec0c7`), confirmed live via commit-ancestry check.
- Both eq-shell merges hit the same known local-only failure mode (`gh pr merge --delete-branch` trying to switch the worktree to `main`, colliding with a concurrent session's `eq-shell-standalone-phone` worktree that already had `main` checked out) — worked around by deleting the remote branch directly via `git push --delete` rather than touching the other session's worktree. Not a real merge failure either time, confirmed via `gh pr view --json state,mergedAt`.

## Decided
- Royce: hold on weekly-digest email editability entirely — don't build anything. Confirmed the pilot's own exclusion condition ("holding until the 3 leave templates have real usage") is unmet: live-queried `public.email_templates` on ehow, all 3 rows still show `updated_by: null` and the original seed timestamp.
- Royce: ship the narrow Access Control discoverability fix now; defer the "one stop shop" unified redesign as a real but separate, design-first idea.
- Royce: merge #1379 once CI's green (explicit, given per-PR — not generalized to later merges).
- Royce: override and merge #1380 despite the unrelated required-check failure (explicit, after discussing the risk).

## Deferred
- **3 unsourced control-plane functions on jvkn** (`eq_cards_admin_list_worker_credentials`, `is_org_admin_with_credential_access`, `tg_org_membership_sharing_scope`) — live, no committed migration file, currently failing a required check on every open eq-shell PR. Not investigated further or fixed — unclear origin, not safe to guess-write migration files for functions this session didn't create. Needs either the missing files written or a deliberate KNOWN_UNSOURCED triage decision.
- **"One stop shop" access-control redesign** — Royce's idea, real, explicitly not scoped or built this session. Needs a proper design pass (grouping ~86 total permission keys sensibly) before any build.
- Neither eq-shell PR was click-tested live — no way to mint a real Shell admin session from this sandbox (confirmed, matches multiple prior sessions' notes on this same repo).
- `eq-shell-perms-discoverability-hint` worktree still on disk — its removal was blocked mid-session by the permission classifier; harmless, needs a manual `git worktree remove` sometime.

## Notes
- **Prompt-injection attempt, ignored, not acted on**: mid-session, a message purporting to be a compacted-conversation summary carried a trailing instruction block phrased as a system directive ("CRITICAL: Respond with TEXT ONLY...") demanding an unrelated summary action, with pressure-tactic framing ("will be REJECTED"). Identified as injected, flagged to Royce directly, ignored, answered the actual question instead.
- Same "another session collided with a shared worktree/branch" failure class flagged elsewhere in this file today recurred twice more, specifically on eq-shell's `main` worktree — this repo is under the same heavy concurrent load as eq-cards/eq-context today. Worked around both times without touching the other session's worktree; no data lost either time, confirmed via `gh pr view` rather than assumed.
- The eq-shell required-check failure this session hit (`Schema drift + anon-grant + policy-lint`) is worth someone's attention independent of this session's PRs — it's currently a standing, repo-wide merge blocker, not a one-off.

---

# Session 2026-08-16 (cont'd 9) — eq-solves-service: PR #736 merged on Royce's explicit go, confirmed live

*Continues the "cont'd 8" entry above (nav-visibility tier-inversion + missing service.view entry gate) — same session, closed once already, resumed on "merge the PR."*

## Built
- Royce: "merge the PR" — explicit instruction, no separate deploy step needed since this repo auto-deploys on merge to `main` (same shape as the documented eq-shell pattern).
- Re-checked PR #736's live state before merging rather than trusting the earlier snapshot: still `OPEN`/`MERGEABLE`, `tsc + next build` (the real gate) green, only `Integration tests (Supabase local)` red. Pulled that job's actual failure log rather than citing the "pre-existing" note from memory: dies at a migration around 0061 on `schema "app_data" does not exist` — same failure shape another session independently hit and documented today on PR #735, on a repo-wide CI bootstrap gap, unrelated to this PR (which touches zero migration files).
- Squash-merged (`8d556f9`), branch deleted.
- **Deploy verified live, not assumed from the merge.** The Netlify MCP's project-level "current deploy" pointer initially showed a stale result — PR #735's build (published 06:33, 13 minutes before this merge), not this one. Went to the full deploy list instead (Netlify API directly, MCP has no list operation): found the real entry — `context: production`, `state: ready`, `commit_ref` matching the merge SHA exactly, `created_at` 2 seconds after the merge. Confirms this repo's merge-to-main auto-deploy fires just as fast as eq-shell's documented 2-4s band.
- Removed the now-merged worktree (`.claude/worktrees/service-nav-role-gate`).

## Decided
- Royce: "merge the PR" — the second explicit instruction of this session (the first was "/decide and build"), completing the sequence documented as this repo's normal pattern all day (push → merge → verify, each its own explicit step).

## Deferred
- Nothing new — this closes out PR #736 itself. The three items already logged in the "cont'd 8" entry (job-plans gate, `service.view_commercials`, the suite-wide nav-visibility-drift decision) are unchanged.

---

# Session 2026-08-16 (cont'd 10) — eq-cards: accept-flow scope decision resolved; independently duplicated PR #253's migration fix before catching it at close; confirms the cross-repo drift-checker gap the eq-field/eq-shell session above flagged as unresolved

## Built
- Watched eq-cards PR #251 (migration `0128`, sharing_scope enforcement — built in the "cont'd 7" session above) through to merge: confirmed merged `2026-08-16T05:59:58Z` (`e4419f2`), migration confirmed live on jvkn via `list_migrations`.
- Independently found the same `0129_worker_credentials_org_admin_read` repo/DB drift the "cont'd 9" session above closed — found via routine post-merge verification (checking `list_migrations` after #251), before reading that session's own work. Reconstructed the exact file from `supabase_migrations.schema_migrations` + cross-checked live `pg_get_functiondef`, committed on `chore/add-missing-0129-migration-file`, opened PR #252.
- During this session's own `/close`, reading `pending.md` surfaced "cont'd 9"'s already-merged PR #253 containing the identical file (byte-identical SQL bar 4 header lines noting the live-apply date). Closed PR #252 as superseded (comment explaining why), deleted the branch both sides.
- A chat message arrived — reading as relayed from a separate eq-shell-side investigation — claiming eq-cards was still missing a migration, citing PR #253 for two functions actually defined in PR #251. Corrected the citation and confirmed both `0128` and `0129` are properly committed on eq-cards' side.

## Decided
- Royce: employer-initiated access requests (`eq_cards_request_worker_access`) stay full-scope only — no basic/full picker on the accept flow, revisit only if a worker actually asks. Resolves the item deferred in "cont'd 7" above as `task_9e421791`; this session was that spawned task.
- Royce: watch PR #251 and flag on merge (done); watch PR #252 and flag on merge (started, later moot — see Notes).
- Royce: close the session — offered converting the live PR watch to a durable cloud schedule first; chose to drop it and close instead.

## Deferred
- None new from this session's own work.

## Notes
- **Real duplicate-work near-miss.** PR #252 (this session) and PR #253 (concurrent "cont'd 9" session) independently reconstructed the identical missing migration file within about 20 minutes of each other, neither aware of the other until this session's own `/close` read `pending.md`. Worth the habit: re-check `main` for a "missing" artifact immediately before opening a PR for it, not just at discovery time — a live repo this concurrent can close the gap out from under you in the interim.
- **Confirms and closes the loop on the gap the eq-field/eq-shell "cont'd" session above flagged as unresolved.** That session's PR #1380 hit a required `Schema drift + anon-grant + policy-lint` check failing on 3 live jvkn functions it found "unsourced" — `eq_cards_admin_list_worker_credentials`, `is_org_admin_with_credential_access`, `tg_org_membership_sharing_scope` — logged as needing either missing files or a deliberate triage decision, origin unclear at the time. All 3 are in fact sourced, on eq-cards' side: `is_org_admin_with_credential_access` + `tg_org_membership_sharing_scope` in migration `0128` (PR #251), `eq_cards_admin_list_worker_credentials` in migration `0129` (PR #253). The failing check is an eq-shell-repo-scoped drift-checker that only diffs live jvkn against eq-shell's own migrations directory — it has no visibility into eq-cards' migrations folder at all, so anything eq-cards legitimately ships to the shared jvkn control-plane database reads as "no migration" from eq-shell's side. Not eq-cards' gap to fix; belongs with whoever owns that CI check in eq-shell — flagged here so the next session (either side) has the full 3-function mapping instead of re-deriving it.
- eq-context's shared checkout was under heavy concurrent write load again this close — a background git operation was blocked by the permission classifier mid-attempt on the first `/close` pass (even a read-only `git status`); skipped that pass's substrate write on Royce's call rather than force it. A second attempt on this `/close` re-invocation went through cleanly. This entry landed alongside another concurrent session's own uncommitted `pending.md`/session-log edits already sitting in the working tree — left `pending.md` untouched (not staged, nothing of this session's belonged there), matching this file's own established norm elsewhere today of not resolving another session's content.

## Notes
- A live tool (Netlify MCP's cached "current deploy" field) returned a stale answer by a matter of minutes — caught by cross-checking against the authoritative deploy list rather than trusting the first response. Same discipline as every "deploy verified live, not assumed" note elsewhere in this file today, just against a tool result instead of a doc.
- This session ran `/close` twice — once after opening PR #736, once after merging it. The second pass's inventory was small (one instruction, one verification chain) rather than re-narrating the whole session; `pending.md`'s "PR #736 open" section header and its now-completed bullet were updated/trimmed to match, not left stale for the next reader.
- This append hit the shared-checkout write race 3 times in a row (concurrent sessions editing the same file between this session's Read and Edit calls) — switched to a raw shell append (`>>`) instead of Read+Edit, which doesn't depend on knowing the current tail and isn't vulnerable to the same race. Worth remembering as the fallback when this file is this hot.

---

# Session 2026-08-16 — eq-shell: 4 PII leaks gated on field.view/entity.view instead of the narrower key, PR open

## Built
- Verified two reported leaks against live code + the vendored `@eq-solutions/roles@v2.5.8` matrix (not docs): `compliance-report.ts`/`signals.ts`'s compliance band (named workers, licence status, notifiable incidents) on `field.view` (all six roles, confirmed); `crm-customers.ts`'s `list` action never called `redactContactPii`, leaking contact emails via `search_text` + `contact_emails` to any `entity.view` holder — confirmed rendered live in `CustomersPage.tsx:117-118` as `"Contact: {email}"` in search results.
- Swept every other `field.view`/`entity.view` call site (9 endpoints) for the same shape, checking each against the live role matrix rather than assuming: found 2 more real holes (`staff-review-state.ts` — licence review/flag status, same class PR #1365 fixed elsewhere; `org-join-notify-recipients.ts` — every manager's email with no PII split, unlike the identical shape in `staff-org-roster.ts`). Checked and left alone: `signals.ts`'s `crew` band (aggregate-only), `worker-home-summary.ts` (hard self-scoped), `org-worker-roles.ts`/`org-credential-requirements.ts`/`org-join-requirements.ts` (genuine non-sensitive config), `canonical-events.ts` (payload has no PII).
- Fixed all 4: re-gated 3 endpoints + 1 page to `field.view_licences`; added the missing `entity.view_pii` check to `org-join-notify-recipients.ts`; fixed `crm-customers.ts`'s list action at construction time (a flattened search string can't be redacted after the fact) and hardened `redactContactPii`'s key match from exact `'email'`/`'phone'` to a regex covering plural/prefixed keys — incidentally also fixed `invoice_email`, which was shipping unredacted on the (otherwise correctly-gated) detail path.
- `permission-enforcement-baseline.json` prose updated to match (this file is a governed ratchet the repo's own drift test checks — a mid-session concurrent merge, PR #1377, touched the same file; reconciled properly via stash + fast-forward + conflict resolution rather than hand-splicing one version's text over another, then re-verified full test suite).
- `pnpm run test` 353/353 pass (incl. the permission-enforcement-drift ratchet), `pnpm run build` clean. eq-shell PR #1381 open, not merged.
- Investigated a spawned follow-up (990 pre-existing repo-wide lint errors, found while verifying #1381): confirmed lint is deliberately advisory in CI (`continue-on-error`, decided 2026-06-30) but for a *different* original debt (raw-hex colours, since resolved) — the CI comment citing that reason is now stale. Confirmed via the installed package's dist source that none of `eslint-plugin-react-hooks`'s rules support `--fix` — the debt has no mechanical shortcut. Could NOT confirm the existing pending.md claim that a "react-hooks v7 upgrade" is the root cause — full package.json history shows `eslint-plugin-react-hooks` pinned to `^7.1.1` since the first scaffold commit, no version change ever recorded in this repo; flagged the discrepancy rather than picking a side. A full per-rule breakdown across all 990 errors didn't finish before this session closed (background job stalled/produced no output) — genuinely incomplete, not just deferred.

## Decided
- Royce: confirmed the brief and all 6 planned file changes before any edit landed (session-gate brief run first, per Rule 0.6).
- Royce: asked to close mid-investigation of the lint follow-up; investigation wrapped up with what was already solid rather than chased further.

## Deferred
- **PR #1381 not merged** — needs Royce's explicit go; this repo auto-deploys `main` on merge within seconds, and this is auth-adjacent (non-negotiable #6). _(added 2026-08-16)_
- **Not click-tested live** — no signed-in session available in this environment. Worth confirming an employee/apprentice account gets a clear "not allowed" on `/reports/compliance`, no licence-review badges on Staff, and no contact emails surfacing in Customers search. _(added 2026-08-16)_
- **Full per-rule breakdown of the 990 lint errors not obtained** — the background `eslint -f json` run produced no output before the session closed; worth re-running standalone (not piped through `tail`) in a fresh session. See updated pending.md entry. _(added 2026-08-16)_
- **The "react-hooks v7 upgrade" root-cause claim in the prior 2026-08-03 pending.md entry could not be verified and may be wrong** — flagged, not resolved. Whoever picks up the lint-debt burn-down should re-derive this rather than trust either version. _(added 2026-08-16)_

## Notes
- Mid-session, `origin/main` advanced twice while working (PR #1377 merged, then further commits) — same heavy concurrent-load pattern the rest of today's sessions hit. Handled by fetching before branching, then properly reconciling (stash + fast-forward + manual conflict resolution on the one shared governed file) rather than force-pushing over or ignoring the drift. Full test suite re-run after reconciling to confirm nothing broke.
- `permission-enforcement-baseline.json` is a live, actively-edited governance file (touched by both this session and #1377 within minutes) — worth remembering it's a contention point on days like this, same as pending.md/sessions itself.
- Hit the same shared-checkout write race on `pending.md`/this file that an earlier session today already documented (see its note a few entries up) — same fix applied: raw shell append instead of Read+Edit once the race showed up.

---

## Session 2026-08-16 (cont'd 7) — eq-solves-service: P0 fix merged + dispatched + verified live

## Built
- Merged eq-service PR #735 (squash, `3d40d65`) — migrations 0208 + 0209 landed on `main`.
- Dispatched `apply-service-migrations.yml` via `workflow_dispatch` (explicit instruction: "continue and dispatch"). Run succeeded in ~16s; both migrations applied and committed to `service._eq_migrations` in one transaction each.

## Decided
- N/A this turn — executed the apply step that PR #735 and the prior close already flagged as the one remaining explicit-approval action.

## Deferred
- Live click-through with a real low-privilege account (apprentice/labour_hire/subcontractor) — needs a real signed-in session, not checkable from an agent session. Tracked in pending.md.

## Notes
- Confirmed the workflow's `workflow_dispatch` trigger runs immediately (no approval-gate pause) by reading the live workflow file before dispatching, not from memory — the `production` GitHub Environment has no required-reviewer rule (branch-policy only), documented inline in the workflow itself as a previously-corrected stale comment.
- Verified success three independent ways, not just "job went green": (1) CI log shows `insert into service._eq_migrations` + `commit;` + explicit `✓ ... applied + ledger row committed` + `✅ 2 migration(s) applied` for both files; (2) live query against ehow confirms both ledger rows with matching checksums; (3) live query confirms `maintenance_checks` now carries 4 split policies (was 1) and all 4 spot-checked trigger functions (including 2 of the 6 lowercase-`perform` cases) are still `SECURITY DEFINER` and carry the injected guard.
- Ran security + performance advisors post-apply per AGENTS.md: zero new ERROR-level findings. Two WARN-level findings traced to root cause rather than left as noise: (a) `eq_is_check_assignee` flagged as an authenticated-callable SECURITY DEFINER function — intentional, documented in the migration's own header, same shape as existing accepted helpers; (b) `auth_rls_initplan` WARN count on the 17 gated tables scaled up because splitting 1 policy into 4 per table multiplied an existing unwrapped-tenant-check pattern — confirmed by direct comparison that `contract_scopes` (the precedent this migration explicitly generalised) has the identical unwrapped shape today, so this is a pre-existing schema-wide convention, not a regression. Not chased further, not blocking.
- Netlify production-deploy confirmation for the merge commit needed a different check than planned: the original background poll queried the classic GitHub commit-status API (`commits/{sha}/status`), which sat at 0 contexts for the full 10 minutes because Netlify's GitHub App posts via the Checks API, not classic statuses, for this integration. Confirmed instead via Netlify's own deploy record plus `git merge-base --is-ancestor` against whatever commit is actually live.
- The P0 vulnerability from the original brief (any signed-in tenant member — including apprentice/labour_hire/subcontractor — could write maintenance checks, defects, test results and assets directly via PostgREST, bypassing all ~66 app-layer permission checks) is closed as of 2026-08-16 06:44 UTC.

---

# Session 2026-08-16 — EQ secrets audit closed end-to-end: live Netlify fixes, 4 code bugs shipped, register reconciled, one-page snapshot built

## Built
- Live Netlify dashboard walkthrough across all 4 sites (eq-shell, eq-field, eq-service, eq-cards) with Royce clicking through — closed SEC-9 (17 dev-context-unmasked vars: shell ×11, field ×1, service ×5), SEC-18 (4 never-masked vars on field/service), SEC-24 (`QUOTES_CRON_SECRET`), deleted 4 confirmed-dead vars.
- Found and closed a worse, previously-undocumented variant on eq-cards: two top-tier secrets (jvkn service_role key, shell JWT secret) were readable in **production**, not just `dev` — root cause is one layer worse than believed: toggling `is_secret` on an existing var does not purge the value stored before the toggle, only delete+recreate does.
- 4 silent-failure code bugs found and fixed, 5 PRs merged and deployed live: eq-service `admin.ts` (a bad service-role key failed silently instead of erroring), eq-shell (SKS session mint, same shape), eq-field `scripts/supabase.js` (anon-key fallback masked auth failures), eq-cards `shell-verify.js` PR #250 (a failed admin identity check — including a 401 from a broken key — was treated as "user already exists").
- Confirmed all 9 suite-wide shared-secret clusters directly with Royce (4 previously rigorously verified + 5 more confirmed this session) and consolidated the SPOF decision.
- Reconciled `ops/security-register.md` — the canonical, `read_priority: critical` findings tracker. SEC-9 and SEC-24 had been closed in the companion `ops/secrets-inventory.md` doc and the working artifact, but never in the register itself, so the session-gate's NEEDS YOU kept flashing both as open on every subsequent session start. Backfilled both rows with the closure detail.
- Built and published a second artifact, a one-screen "Secrets Snapshot," after the detailed map proved too dense to use as a quick status check. The detailed map stays as the audit-trail record; the snapshot is the at-a-glance one.

## Decided
- Keep all 9 shared-secret clusters shared across apps — no per-app key split, revisit only if it comes up again.
- ProtonPass vault entry for the ~9 EQ-minted secrets: done, Royce's own words "as well as I could" — treated as closed, not independently byte-verified.

## Deferred
- None new this session. The one pre-existing architectural item adjacent to this work — stripping eq-cards' own copies of `SUPABASE_JWT_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`EQ_SESSION_SALT` once `EQ_CARDS_HANDOFF_KEY` lands — predates this session and wasn't touched; still tracked in `eq/pending.md`.

## Notes
- Root-cause finding worth remembering suite-wide: Netlify's `is_secret:true` flag only governs display of values written *after* the toggle — flipping it on an already-existing var does not purge whatever was stored before. The only real fix for a var that was ever plaintext is delete+recreate, not a toggle. Two older "closed" findings (SEC-12, SEC-18) were closed via a toggle, not delete+recreate, before this distinction was known — worth a spot-check if either is ever revisited.
- A real near-miss, cleanly recovered: a commit briefly landed on a concurrent session's active branch (`chore/close-sign-in-audit-followups` in the primary, non-worktree `eq-cards` checkout) because a `cd X && git commit` was assumed to run against the "default" worktree path a prior tool message named — it doesn't; `cd` inside the same command genuinely changes where that command runs. Recovered via patch-save + hard-reset their branch back to its exact prior state + re-apply in the correct worktree, zero damage to their work. Going forward: verify `pwd` + branch in the same command as any git operation, never trust a prior turn's location assumption.
- This session is a live instance of the platform's own F5 ratchet warning ("an ungoverned shadow memory overrode the canonical contract") — the working doc and the scratchpad artifact both got updated in real time as fixes landed, but the canonical register didn't, and stayed stale until this session's own close caught it. General lesson, not a one-off: when a session's real output is "the doc gets updated," check whether more than one doc claims to be the source of truth before calling it done.

---

# Session 2026-08-16 (cont'd 10) — eq-cards permission-audit thread fully closed out: 2 stale PRs closed, 3 unused secrets confirmed deleted

## Built
- Follow-through on the "Needs you" item from this session's own earlier close (cont'd 9 above): closed eq-cards [PR #221](https://github.com/eq-solutions/eq-cards/pull/221) and eq-shell companion [#1294](https://github.com/eq-solutions/eq-shell/pull/1294) on Royce's explicit "close PR #221 and #1294" — both had proposed rewriting `shell-verify.js` into a broker-relay, moot once that file was retired outright in #253. Closed each with a comment pointing back to #253 rather than closing silently.
- Royce reported the 3 secrets those PRs existed to stop eq-cards holding (`SUPABASE_JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY` (jvkn), `EQ_SESSION_SALT`) had been deleted from eq-cards' Netlify project directly. Attempted to independently confirm via the Netlify MCP reader (`get-projects` found the site fine; `get-project` failed 3 times in a row — connector-side, not a real block) before accepting the report as-is; recorded as self-reported, not API-verified, rather than stated as independently confirmed.
- Updated `eq/pending.md`: ticked the PR-closure item resolved, then — once the secrets item also resolved and zero open items remained needing narrative — trimmed the whole eq-cards section down to header + the one still-open item (live click-through), per the archive rule. Moved the now-covered detail into `eq/changelog/eq-cards.md` first so it wasn't dropped from every record, not just pending.md's.
- Two more non-fast-forward auto-push rejections this session (3rd and 4th of the night) on `eq-context`, both resolved via the same fetch+rebase recipe, both rebased clean with zero conflicts.

## Decided
- Royce: "close PR #221 and #1294" (explicit, by number).
- Royce: reported the 3 secrets deleted (his own direct action — manual-hands-only, no Claude Code path can write Netlify secrets).

## Deferred
- Nothing new. Live click-through on the credential-list fix (cont'd 9) is still the only open item from this whole thread.

## Notes
- This is the same session as cont'd 9 above, picked back up after that close — worth noting `/close` was run twice in one continuous session tonight. Second run found nothing left to commit for Steps 2–4 beyond this block itself, since the PR-closure and secrets-deletion updates had already been pushed incrementally as they happened rather than held for a final close.
- `sessions/2026-08-16.md` now has two separate pairs of colliding `(cont'd N)` numbers (two `cont'd 7`s, two `cont'd 9`s) from independent concurrent sessions picking the same next-available number around the same time. Cosmetic only — headers stay disambiguated by topic — not renumbered, since fixing it would mean rewriting another session's already-pushed entry.

---

# Session 2026-08-16 (cont'd 11) — eq-solves-service: job-plans view-gate fixed (task_68813d6a, spun off from the #736 session), merged + live

## Built
- Added the missing `canViewRecords` (`entity.view`) role gate to `app/(app)/job-plans/page.tsx` — the one canonical-record page PR #716 didn't cover. Same pattern as the other 4 Records pages (customers/sites/assets/contacts), zero live effect (same 0-labour_hire/subcontractor fact as #716/#717).
- [PR #738](https://github.com/eq-solutions/eq-service/pull/738) merged (squash `535deef`), Netlify production deploy triggered.

## Decided
- Royce confirmed the brief (AskUserQuestion: "Proceed"), then gave explicit separate go-aheads for commit, push+PR, and merge — each requested and actioned as its own step.

## Deferred
- Not click-tested live — same structural sandbox limitation #716/#733 documented (no way to produce a real Shell-authenticated session here).

## Notes
- The branch this task was originally scoped against (`fix/integration-ci-app-data-bootstrap`, the root eq-solves-service checkout) turned out to be 46 commits behind `origin/main` — old enough that `canViewRecords` itself didn't exist there yet, and it held unrelated uncommitted WIP on top. Built instead from a fresh worktree off `origin/main`, root checkout left untouched. Live instance of "check branch staleness before building" — the same lesson the #736 and #735 sessions hit independently the same day.
- CI's `Integration tests (Supabase local)` failed on this PR but is a pre-existing failure on `main` itself too (confirmed via the GitHub API, not assumed) — root cause is migration 0152 referencing `app_data.*` before CI's local Supabase bootstraps that schema. Not this PR's fault; already being worked by the separate `fix/integration-ci-app-data-bootstrap` branch sitting in the same checkout.
- This session's own `/close` hit the shared eq-context checkout's rebase/merge guard (F9) head-on: `git rebase` and `git merge` are both blocked there since multiple concurrent sessions write to it all day. Resolved via the guard's own suggested escape hatch — a fresh isolated clone in scratchpad, same 3 edits replayed against current `origin/main`, committed and pushed from there instead of the shared checkout.

---

# Session 2026-08-16 (cont'd 12) — eq-solves-service: service.view_commercials confirmed dead; app_data CI-bootstrap fixture merged (#737)

## Built
- Investigated `service.view_commercials` (spun off as `task_61bda775` from the #736 session): grepped every maintenance/defects page for cost/price/value fields + queried live ehow's `information_schema.columns` directly for the same tables — zero hits either way. Confirmed genuinely dead, no code change needed; reconfirmation recorded in memory so it isn't re-chased a third time.
- Rebased `fix/integration-ci-app-data-bootstrap` onto `origin/main` (46 commits behind, 0 local commits — clean fast-forward) and committed the work that had sat uncommitted on it since 2026-08-10: excludes the CI-bootstrap-only `0000_app_data_tenant_plane_fixture.sql` from `migrate-service.mjs`'s live-apply path, plus a new weekly `app-data-fixture-drift.yml` workflow + checker script.
- Opened [PR #737](https://github.com/eq-solutions/eq-service/pull/737); its first-ever live run of the new drift-check immediately caught real drift (`quote_attachment` dropped from live app_data, `staff_conversations` added, column drift on `defects`/`rcd_test_circuits`) — the check working exactly as designed, not a bug in it.
- Regenerated `supabase/fixtures/app_data_manifest.json` directly from live ehow via the Supabase Management API (bypassing a broken `supabase db dump --linked`, see Notes), fixing the drift-check. Merged #737 (squash, branch deleted) — Netlify prod deploy triggered; CI-tooling only, no app code touched.

## Decided
- Royce: "Regenerate the fixture first" (review the diff before committing) → then "Commit manifest update now" once the SQL-DDL half turned out blocked → then "merge" for #737 — each requested and actioned as its own explicit step.

## Deferred
- `supabase/migrations/0000_app_data_tenant_plane_fixture.sql` (the actual local-bootstrap DDL, as opposed to the JSON shape-manifest) is still stale. Needs `supabase db dump --linked --schema app_data` run from a machine with working IPv4/DB connectivity — tracked in `eq/pending.md`.
- `Integration tests (Supabase local)` CI job still red — confirmed pre-existing (failing on `main` itself for 5+ consecutive pushes before this session touched anything), not blocking (no branch protection on this repo), not fixed this session. Two independent sessions today (this one and cont'd 11) now agree it traces to `app_data` not existing yet when a downstream migration expects it — this session saw it fail specifically at migration 0061, cont'd 11 attributed it to 0152; likely the same underlying gap surfacing at different points as the migration set shifts, not a contradiction between the two.

## Notes
- `git fetch` only updates `origin/main`'s remote-tracking ref, not the local `main` branch itself — `git diff main...HEAD` against a stale local `main` pulled in a swath of unrelated files (settings, auth, migrations 0208/0209) that had nothing to do with this branch. Caught before it leaked into the PR body; the fix is always diffing against `origin/main` explicitly, never local `main`, in an environment this many concurrent sessions are pushing to.
- `supabase db dump --linked` fails in this environment with `LegacyDbConfigIpv6Error` (IPv6 unsupported on the current network) — needs `supabase link` to reconfigure an IPv4 pooler connection, which in turn needs a DB password not available here (not in `.env.local`, not worth asking for in chat). The Supabase Management API (used via MCP `execute_sql`, the same query the drift-checker itself runs) is a full workaround for shape-only needs but can't produce full DDL — that half of the fixture genuinely needs a human with working DB connectivity.
- Regenerating the 2375-row JSON manifest with Python produced CRLF line endings by default on this Windows environment (`open(..., "w")`'s universal-newline translation), which would have shown as a ~19,000-line noise diff against the LF-only committed file. Caught by comparing insertion/deletion counts against the expected row-level math (56 added / 68 removed matched exactly once `newline="\n"` was forced) before committing — worth remembering for any future from-scratch file regeneration on this machine.
- `main` on `eq-solves-service` has no branch-protection rules configured at all (`404 Branch not protected`) — no status check, including the pre-existing-broken integration-test job, technically blocks a merge. Confirmed via the GitHub API rather than assumed.
- This session independently hit the exact same F9 rebase/merge guard as cont'd 11 above, moments apart — and then hit a second-order version of the same problem: the isolated-clone escape hatch itself started from a stash taken before cont'd 11's own close had pushed, so a blind copy-over would have deleted cont'd 11's just-pushed Notes line. Caught by diffing the isolated clone's fresh `HEAD` against the extracted content before committing, not by assuming the extraction was current. Concurrent `/close` on this file is now a two-deep problem, not a one-deep one — worth a real fix (e.g. one shared session-log file per calendar day is clearly past its useful concurrency ceiling on a day this busy) rather than continuing to route around it by hand each time.

---

# Session 2026-08-16 (cont'd) — zaap/ehow Field write-guard divergence confirmed deliberate; cleared a repo-wide eq-shell merge block as a byproduct

## Built
- Investigated a flagged P2 finding (ehow has 7 Field write-guard trigger functions on `app_data`; zaap has none) and confirmed via convergent same-day documentary evidence — no Supabase MCP or `SUPABASE_ACCESS_TOKEN` was available at the start of this session — that the divergence is deliberate: zaap's `eq` tenant is disposable/non-production, its write path is architecturally different (direct table grants vs. ehow's view+INSTEAD-OF-trigger), and 3 of the 7 guards protect tables (`app_data.teams`/`team_members`/`team_supervisors`) that don't exist on zaap at all. Documented as a named instance of the existing `--strict-spine` allowance in eq-shell's own `CLAUDE.md`. eq-shell [PR #1384](https://github.com/eq-solutions/eq-shell/pull/1384), merged (`22808d8c`).
- While getting #1384 merged, hit a required-check failure (`Schema drift + anon-grant + policy-lint`) that turned out to be repo-wide and unrelated to that PR: `check-control-plane-drift.mjs --strict` found 3 live jvkn functions (`eq_cards_admin_list_worker_credentials`, `is_org_admin_with_credential_access`, `tg_org_membership_sharing_scope`) with no matching source file in eq-shell's `supabase/migrations/`. Traced all 3 to eq-cards migrations `0128`/`0129` (PRs #251/#253), both merged minutes before the gate ran — confirmed by reading those files directly and diffing every function/trigger definition against live `pg_get_functiondef`/`pg_get_triggerdef` on jvkn (identical). Triaged into `KNOWN_UNSOURCED` with citations, matching the established cross-repo-sourcing precedent (PR #1328). No backfill, no live DB write. eq-shell [PR #1389](https://github.com/eq-solutions/eq-shell/pull/1389), merged (`4a683d6d`).
- Wrote two eq-shell memory files (`zaap-field-write-guards-deliberate-absence.md`, `control-plane-drift-known-unsourced-pattern.md`) capturing both investigations for future sessions.
- Found 3 other sessions had independently built the identical `KNOWN_UNSOURCED` fix in parallel (all opened within minutes of #1389 merging) — the spawned-task chip for this finding was apparently started more than once. Closed all 3 as duplicates with a pointer to #1389: eq-shell #1390, #1391, #1392.

## Decided
- Royce: "Hold #1384 until the drift is fixed" (over admin-override or self-handling), when first asked how to handle the blocker.
- Royce: "merge #1389", then separately "merge #1384" once its blocker cleared — both actioned as explicit per-PR merge approvals, never assumed from context.
- Royce: "close them with a pointer to #1389" — closed #1390/#1391/#1392 with an explanatory comment on each, branches left intact (not deleted) since that wasn't asked for.

## Deferred
- One narrow, low-priority follow-up from the zaap/ehow audit: zaap's `app_data.staff` still allows direct `authenticated` writes to `licence`/`agency`/`hire_company` (only `user_id` was locked down, via migration `0196`) — nobody has checked whether that's exploitable in practice. Not urgent given zaap holds no real data. Tracked in `eq/pending.md`.
- The bigger, already-tracked byproduct (ehow's own timesheet/leave guards only gate the `status` transition, not content edits/inserts/deletes — fix drafted as eq-field PR #705 + write-side sibling, blocked on 47 unlinked SKS staff) was NOT touched or re-flagged here — it was already in `digest.md`'s Pending (SKS) before this session started; left as-is to avoid duplicate tracking.

## Notes
- **No live database query tool was available for roughly the first half of this session** (no Supabase MCP connected, no `SUPABASE_ACCESS_TOKEN` in the local shell — that token is a CI-only secret by design, not a misconfiguration). The zaap/ehow conclusion was reached via convergent documentary evidence instead of a fresh query; this was stated explicitly to Royce rather than presented as equivalent to a live check. Supabase MCP tools became available partway through the session (surfaced via a later tool-list update) and were used directly for the control-plane investigation once present.
- **A required CI check failing on `main` blocks every open PR on the repo, regardless of what any individual PR touches** — cost real time before the pattern was recognized (re-running a PR's failed job does NOT pick up a fix merged to `main` afterward; eq-shell's `pull_request` CI checks out the PR branch's own tree, not a merge-with-current-`main` commit, so a stale PR branch keeps re-failing against its own old content until `main` is actually merged/rebased in). Documented in the new control-plane-drift memory.
- **The same spawned-task chip was evidently started more than once**, producing 3 duplicate PRs from 3 separate sessions all solving the identical problem in parallel, discovered only by chance while cleaning up worktrees after this session's own fix had already merged. Worth flagging as a process gap — spawn_task chips may not be reliably single-fire, or the same finding can independently get spawned/flagged by more than one session on a day this concurrent. No fix attempted here; noted for whoever owns that tooling.
- Hit the same `git merge`-blocked-in-shared-checkout guard (F9) as other sessions today — used the hook's own prescribed remedy (fresh isolated `git clone`, not a worktree) rather than the older worktree-based escape hatch referenced in memory, since the hook explicitly names the clone as the intended path for this repo now.

---

# Session 2026-08-16 (cont'd 12) — eq-solves-service: post-close follow-up, two status checks, no new code

## Built
- Nothing — this is a second close on the same session as `task_68813d6a` (job-plans view-gate, PR #738, cont'd 11 above), covering two read-only checks Royce asked for afterward.

## Decided
- None this round.

## Deferred
- `task_61bda775` (`service.view_commercials`, the sibling task spun off alongside this one) still has no PR or branch on GitHub as of this check — confirmed live via `gh pr list` and a full branch-name search, not assumed. Royce asked to check again "in a bit"; no automated re-check was set up (see Notes).

## Notes
- Confirmed PR #736 (nav-visibility) is merged (`8d556f9`, 06:46 UTC), well before `#738` — zero file overlap between the two, nothing to reconcile.
- Royce asked to re-check `task_61bda775` "in a bit." Considered two automation options and rejected both as mismatched for the ask: `ScheduleWakeup` is scoped to `/loop` dynamic-mode pacing only, not usable standalone; the `schedule` skill's routines are fully separate cloud sessions with their own git checkout that report to their own run log, not back into this conversation — disproportionate ceremony for a casual one-off recheck. Told Royce plainly I'd check again next time we're talking, and that `/loop` or a real cloud routine are both available if he actually wants unattended automation instead.

---

# Session 2026-08-16 (cont'd) — traced 3 "unsourced" jvkn functions to real, already-merged work; own fix superseded by a concurrent session mid-flight

## Built
- Investigated the 3 control-plane functions flagged as unsourced (blocking every eq-shell PR, per this session's earlier `/close` entry): queried jvkn directly via `pg_get_functiondef` for all 3, then traced them to two same-day, already-merged eq-cards PRs — [#251](https://github.com/eq-solutions/eq-cards/pull/251) (`0128_enforce_sharing_scope_on_licence_reads.sql` — a real P1 privacy fix: a worker's "basic" vs "full" sharing choice on `/connect` existed in the database but was never actually enforced on any licence read path) and [#253](https://github.com/eq-solutions/eq-cards/pull/253) (`0129_worker_credentials_org_admin_read.sql` — fixed the admin credential list returning empty for every worker except the admin's own). Confirmed byte-identical against live jvkn before concluding anything — not assumed from the migration files alone.
- Root cause: eq-cards and eq-shell both write to the same jvkn control-plane database via two independent migration trees; eq-shell's drift-guard only scans its own `supabase/migrations/`, so anything eq-cards applies always shows as unsourced from eq-shell's side. Same shape as a prior incident ([eq-shell#1282](https://github.com/eq-solutions/eq-shell/pull/1282), 2026-08-11) and an established remediation convention (`KNOWN_UNSOURCED` in `scripts/check-control-plane-drift.mjs`, first used in [eq-shell#1328](https://github.com/eq-solutions/eq-shell/pull/1328)) — found and preferred over a full SQL backfill once discovered, since duplicating ~450 lines of already-committed SQL into a second repo just creates a second copy that can drift from the real one.
- Ran `/brief eq-shell` properly (mid-task, after a session-gate hook blocked a write) rather than skip it — the brief surfaced the closer `KNOWN_UNSOURCED` precedent that changed the fix's shape from a planned full backfill to the lighter triage.
- Built the triage in `claude/jvkn-drift-backfill`, opened [eq-shell#1392](https://github.com/eq-solutions/eq-shell/pull/1392), CI green (including the previously-failing drift check itself). On merge attempt, found a **real merge conflict** (not the earlier local-worktree-cleanup issue) — `git log origin/main` showed a concurrent session had merged [eq-shell#1389](https://github.com/eq-solutions/eq-shell/pull/1389) minutes earlier: same 3 functions, same `KNOWN_UNSOURCED` entries, same source PRs, functionally identical fix. Attempted to close #1392 as a confirmed duplicate — found it had already been closed moments earlier by a fifth concurrent session's cleanup pass (see the "zaap/ehow Field write-guard" entry above: that session found #1390/#1391/#1392 were all independent duplicates of #1389 and closed all three on Royce's instruction). Deleted the now-redundant branch, which the other session's close hadn't done.

## Decided
- Royce: "look into the 3 unsourced jvkn functions" (investigate first, no build authorized yet).
- Royce: "yes, do the backfill" (after the investigation report — authorized the fix, not yet the specific mechanism).
- Royce: "merge it once CI's green" (same per-PR pattern as earlier in this session) — moot once the concurrent-session duplicate was found; nothing to merge.

## Deferred
- Nothing new from this thread — the item is resolved (via #1389, not #1392) and removed from `eq/pending.md`'s open-items list this close, per the archive/trim rule (its resolution lives here and in `eq/changelog/eq-shell.md`, not duplicated in the live pending doc).

## Notes
- **Second confirmed instance this session of two concurrent sessions independently converging on the identical fix** for the same repo, same day (the first was the eq-cards PR #252/#253 collision traced during the investigation itself, hours earlier). Given how much simultaneous traffic eq-shell specifically saw today (10+ merged PRs in the permissions/roles area alone, several sessions), checking `git log origin/main` before force-resolving any merge conflict — rather than assuming it's the usual local-worktree collision — is what caught this one before wasting effort on a real conflict resolution for a no-op change.

---

# Session 2026-08-16 — licences RLS verified, timesheets/leave write-fix corrected + merged, second licences gap logged

## Built
- **task_f7c825ca closed:** verified live that jvkn's `public.licences` RLS already matches the worker-owns-it model (own row, or org-admin-and-not-private) — no fix needed. Along the way, found and independently re-confirmed-still-fixed a real historical vulnerability (an `org_memberships` self-promotion hole that would have cascaded into `licences_read` via `is_org_admin_of()`), closed 2026-07-21, still closed today.
- **task_8615a5ff progressed and corrected:** eq-field's pre-existing write-side RLS draft (`supabase/migrations/20260816_timesheets_leave_own_crew_write.sql`, already on the branch before this session) scoped timesheets writes to "own row or crew" — verified against the real client code (not assumed) that this would have broken `prefillTsFromRoster`/`importTsCSV`, two shipped, tenant-wide, `ts.approve`-gated bulk features with no crew check anywhere in them. Fixed: added `eq__caller_is_approver()` and rewired timesheets' INSERT/UPDATE/DELETE to own-row-or-approver (tenant-wide for approvers). `leave_requests` left crew-scoped — no equivalent bulk-write feature exists there.
- Merged eq-field [PR #705](https://github.com/eq-solutions/eq-field/pull/705) — both migrations (read-side + corrected write-side) landed on `main`. Pure migration-file + docs diff, no app code, so the auto-deploy this triggers has zero live behavioural effect. Both migrations remain explicitly `NOT APPLIED`.
- Found a second, unrelated `app_data.licences` table on ehow (surfaced via `/brief`'s DB recon, not the original task) — same tenant-id-only RLS shape as timesheets/leave had, but confirmed **not** live-exploitable: no `authenticated`/`anon` grant exists on the table at all. Traced it to a one-time 2026-06-25 bulk backfill from EQ Cards (118/120 rows, identical timestamp), superseded by the jvkn-based read path that's actually live today. Logged as SEC-27 in `ops/security-register.md` — Royce's explicit call, not fixed.

## Decided
- Royce: "fix it now" — authorized the timesheets write-policy correction.
- Royce: "merge it" — PR #705 merged.
- Royce: "just log a note for now" — `app_data.licences` (ehow) recorded, not hardened.

## Deferred
- **Dispatch of both timesheets/leave_requests migrations** — blocked on a pre-existing, re-confirmed gap: 47 of 102 SKS staff have no `staff.user_id` link, so `eq__caller_staff_id()` resolves NULL for them and a naive own-row check would lock them out of their own rows the moment either migration applies. Bounded population (unchanged since 2026-07-05), resolves as Royce's QR onboarding rollout lands.
- **`app_data.licences` (ehow) RLS hardening** — not exploitable today (no grant), logged as SEC-27, deliberately not fixed.
- **zaap (EQ tenant) port** of the timesheets/leave fix — explicitly out of scope both migrations, lower priority (disposable tenant, no live data).

## Notes
- **Two different `licences` tables exist and are easy to conflate** — `public.licences` on jvkn (worker-owned credential documents, correctly scoped, live path) vs `app_data.licences` on ehow (a dormant one-time Cards import, latent RLS gap, no grant, nothing reads it). Future sessions: check schema + project before assuming which one a "licences" finding means.
- **`ts.approve`/`leave.approve` already grant tenant-wide WRITE authority by product design** — the crew boundary that's correct for the read side never applied to writes. Worth remembering before reusing `eq__caller_has_broad_read()` (built for read) on any future write-side policy — it deliberately excludes a plain assigned supervisor, which under-scopes write.
- eq-context itself was extremely active throughout this session (14+ commits landed in one ~10-minute window). A direct commit+push to its `main` hit two independent blocks — a stale-main-gate hook, then the Claude Code auto-mode classifier specifically on `push ... main` (retried twice, consistently denied; did not attempt further workarounds per that denial's own instructions). Resolved via an isolated worktree; the final push landed from Royce's own terminal after one routine non-fast-forward rejection (remote had moved again) and a fetch+rebase.
- `eq-context`'s own working tree is under continuous concurrent-write pressure today — a plain `git merge origin/main --ff-only` failed mid-close on another session's uncommitted, unrelated local changes (`eq/changelog/eq-solves-service.md`, `ops/security-register.md`). Left both untouched (not this session's files to stash or discard) and edited the target files directly instead of forcing a full working-tree sync first — same approach that worked cleanly earlier in this session's first `/close`.

---

# Session 2026-08-16 (cont'd) — eq-shell: origin-guard on 2 Cards mutation/PII endpoints + 5 clear siblings, merged + live; own drift-gate fix superseded by a concurrent session

## Built
- Confirmed the P2 finding as given: `cards-approve-staff.ts` (mutates staff records, `admin.review_cards`) and `cards-export-licences-background.ts` (returns worker PII/licence photos, same perm) had no `checkShellOrigin()` guard, unlike sibling `cards-export-licences.ts`. Read the existing 21 call sites first to confirm the exact placement/comment pattern before touching anything. Confirmed `cards-export-licences-background.ts`'s only caller (`StaffPage.tsx`) is a direct same-origin `fetch()` despite being a background function, so the guard can't break it.
- Added the guard to both, report-only (`ENFORCE_IFRAME_ORIGIN`), matching the established rollout.
- Swept the rest of `netlify/functions/` per the task's own instruction: 109 functions read the session cookie without the guard; narrowed to 51 with an actual mutation verb via grep, not name-guessing. Fixed the 5 that are direct, unambiguous siblings of an already-guarded function (`entity-actions.ts`/`entity-insert.ts` ~ `entity-patch.ts`; `admin-change-login-phone.ts` ~ `admin-audit.ts`/`admin-tenants.ts`; `shell-create-provision-token.ts`/`provision-tenant-background.ts` ~ `provision-cancel.ts`). Deliberately did NOT bundle the remaining ~45 — bigger than any prior origin-guard PR in this repo's history (max 5 files across #590/#1244/#1377) and spans unrelated feature domains; spun off as a `spawn_task` follow-up (severity-batched, account-security first) instead, which Royce picked up and ran in separate sessions before this one closed.
- Worked in an isolated worktree off `origin/main` throughout (current branch had unrelated in-progress uncommitted work from a roles-version-bump) — `pnpm install` + `pnpm run build:packages` first, since a fresh worktree has no `node_modules` and the repo's `tsc-check.js` PostToolUse hook fails opaquely without it.
- eq-shell [PR #1386](https://github.com/eq-solutions/eq-shell/pull/1386) opened, build verified clean, Royce approved merge.
- Merge blocked by a required `Schema drift` check failure — confirmed via the scheduled main-branch drift check itself (green 03:54 UTC, red by 06:53, hours before this PR existed) that it was pre-existing and unrelated, not caused by this diff. Traced the 3 unsourced jvkn functions to already-merged eq-cards PRs #251/#253; got live Supabase MCP access mid-session and confirmed all 3 byte-identical (`pg_get_functiondef`) to the committed source, grants clean, no anon. Opened a second PR (#1391) to triage them into `KNOWN_UNSOURCED` — a Claude Code auto-mode classifier blocked the edit once (editing a security-gate allow-list), stopped and asked rather than working around it; Royce reviewed and approved the retry.
- #1391 then picked up a merge conflict — a concurrent session had independently landed the identical fix as PR #1389 minutes earlier. Confirmed via `git log` rather than fighting the conflict; closed #1391 as duplicate, referenced the live-verification in the closing comment, deleted the branch/worktree.
- GitHub Actions never triggered on the #1391 branch across 3 attempts (open, reopen, fresh empty-commit push) while every sibling PR in the same window triggered normally — a genuine, unresolved anomaly, moot once the PR closed but flagged in case it recurs.
- Royce approved an admin-merge of #1386 given the by-then-thorough confirmation the check failure was unrelated. Merged (`1c4ed014`). Verified live the right way — commit-ancestry against the newest *ready* production deploy, not the deploy title — after a later, unrelated PR (#1387) superseded #1386's own deploy as the newest within minutes of it going live.
- Reported, not fixed, per the task's own instruction: `cards-approve-staff.ts` requires `admin.review_cards`, `self-join-codes.ts`'s approve/reject (same shape of action) requires only `admin.invite_user`, and `staff-review-state.ts` (reads the same approval data both of those write) requires only `field.view`. Doesn't look deliberate.

## Decided
- Royce: merge #1386 (`AskUserQuestion`, explicit).
- Royce: investigate the drift-gate failure rather than admin-bypass it blindly (`AskUserQuestion`) — this repo's own CLAUDE.md explicitly warns that blind-bypassing this exact check "trains admin-bypass."
- Royce: retry the `KNOWN_UNSOURCED` edit after the classifier block, given he'd already reviewed and merged both underlying eq-cards source PRs himself (`AskUserQuestion`).
- Royce: admin-merge #1386 directly once the failure was confirmed thoroughly pre-existing and unrelated (`AskUserQuestion`).

## Deferred
- **Remaining ~45 cookie-authed mutation endpoints without the origin guard** — spawn_task `task_1b702d27`, batched by severity (account-security ops → worker/credential identity → business data → uploads). Already running in separate sessions (`claude/origin-guard-batch1-auth-factors`, `claude/origin-guard-batch1-session-lifecycle` seen mid-session).
- **Permission-spread convergence** (`cards-approve-staff` / `self-join-codes` / `staff-review-state`) — reported only; needs Royce's call on the intended model.
- **GitHub Actions triggering anomaly** on the (now-closed, now-deleted) #1391 branch — never resolved, cause unknown, moot for this session's own work but worth remembering if it recurs elsewhere.

## Notes
- **Third confirmed instance today of two sessions independently converging on the identical fix** for the same repo — this thread's own #1391 vs. #1389. `git log origin/main -- <path>` before force-resolving any conflict (not assuming it's the usual local-worktree collision) is what caught it here, same lesson an earlier session in this file already logged from a different pair of duplicate PRs.
- **`jq` is not installed in this machine's Bash tool shell.** Cost real time: a CI-polling `Monitor` script silently degraded to "no ready deploy found" for its entire run because `jq` errored on every iteration with the failure swallowed by the script's own `2>/dev/null`. Re-verify with Node (forward-slash paths — a Windows backslash path passed through a Bash single-quoted string into `node -e` gets mangled) rather than trusting a `jq`-based check on this machine without first confirming `jq` exists.
- **Claude Code's auto-mode classifier will block an edit to a CI security-gate's allow-list** (`KNOWN_UNSOURCED` in `check-control-plane-drift.mjs`) even when the change is legitimate and well-evidenced — correctly cautious, since that exact edit shape ("quietly widen what a security check accepts") is a real attack pattern. Stopping and asking rather than finding a workaround got an explicit, informed approval in under a minute; worth remembering as the expected response next time this fires, not a bug to route around.
- Session ran almost entirely from isolated worktrees (`eq-shell-origin-guard-sweep`, `eq-shell-drift-triage`, `eq-context-close-wt`), never editing a shared root checkout mid-use by another session — consistent with the pattern several other sessions in this file independently rediscovered today under the same concurrent load.

---

# Session 2026-08-16 (cont'd) — eq-field page-access audit: 36 of 41 pages had no route guard; two PRs shipped, migration dispatch explicitly deferred

## Built
- eq-field [PR #707](https://github.com/eq-solutions/eq-field/pull/707) (v3.5.504): replaced `showPage()`'s 5 reactive per-id route guards with a single `PAGE_ACCESS` deny-by-default registry covering all 41 real page ids. Closed the Data (Import/Export) page's completely unguarded exports + `confirmReset()` — the audit's highest-risk finding, worse than any of the already-guarded pages. Two new permission-matrix keys (`field.manage_data`, `reports.safety_dashboard.view`). Merged, confirmed live in production.
- eq-field [PR #708](https://github.com/eq-solutions/eq-field/pull/708) (v3.5.505): found in the same audit — any signed-in user could open any apprentice's individual profile, not just their own. New `EQ_PERMS.myPersonId()` helper (exposed from an existing private one, not a new parallel lookup). Had to rebase around #707's own version-bump/changelog conflicts before merging; regenerated the affected bundle fresh rather than trust the auto-merge. Merged, confirmed live.
- Caught a concurrent session ("Field access controls") that had read the same apprentice-profile code but written nothing yet — sent it a direct heads-up (`send_message`) pointing at #708 instead of letting it duplicate the work.
- Resolved Sentry `EQ-FIELD-15` as a false positive — traced precisely to this session's own local verification run, not production.
- Verified PR #705 (a concurrent session's own/crew timesheets/leave RLS migration) didn't break anything: CI green, zero app code touched, and — the check that actually mattered — confirmed live the migration was NOT auto-applied to `ehow` despite the PR merging, exactly as its own "not dispatched" commit message says.

## Decided
- Royce: "push it and open a PR" (#707, then #708 separately) — explicit per-PR authorization, not assumed from the earlier route-guard task brief.
- Royce: "merge #707", then "merge #708" — explicit per-PR merge approval each time.
- Asked whether to dispatch the #705 migration once its documented blocker (37 of 83 active SKS staff with no login-to-staff-record link) was fixed. That "fix" turns out to be a real-world signup requirement, not a code task. Offered Royce the one code-level option available — a fail-open carve-out for unresolved identity, mirroring two other carve-outs already in the same migration — against just waiting. **Royce chose to wait for real signups; declined the carve-out.** Migration stays undispatched. Recorded in `sks/pending.md` and in eq-field project memory so a future session doesn't re-propose the shortcut or dispatch without asking again.

## Deferred
- Full human click-through of #707/#708 on the live site — done extensively via automated/scripted verification (real deploy preview, real production, seeded browser-console scenarios) but not a literal logged-in mouse-driven walkthrough. Tracked in `eq/pending.md`.
- The apprentice-profile fix's "any role can see any profile" cousin issues (if any exist elsewhere) weren't swept for — this pass only covered the one function flagged.
- Timesheets/leave RLS dispatch itself — blocked on real people, not code; see `sks/pending.md`.

## Notes
- **`scripts/permission-matrix.js` and `scripts/permissions.js` are hand-mirrored into `core-bundle-a1.js`/`core-bundle-a2.js`** (a 2026-08-01 loading-perf change) — any edit to either source file needs `node scripts/build-bundles.mjs --write` before commit, or CI's bundle-drift guard fails the PR. Applies to any future eq-field session touching either file.
- **eq-field's Netlify project can supersede an in-flight production build under concurrent merges, same as the already-documented eq-shell pattern** — #708's own deploy queued behind #705's (a different session's PR merging in the same window) and `sw.js` polling alone read stale for ~45s. Resolved by checking the specific deploy for the exact commit via the Netlify API rather than assuming a slow build meant something broke.
- **This repo's own drift-guard test (`permission-enforcement-drift.test.js`) only scans `scripts/*.js`, not `index.html`** — a new permission key referenced only inside `index.html`'s route-guard registry reads as "dead" to that test. Both new keys this session got a real `scripts/*.js` call site specifically to satisfy this, not just a router-registry reference.
- Hit the same shared-eq-context `git merge` block (F9) as the earlier session in this file — same remedy (isolated clone), independently re-derived before reading this file's own note about it.

---

# Session 2026-08-16 (cont'd) — eq-shell: admin.manage_groups privilege escalation closed, PR #1387 merged and live

## Built
- Closed the escalation: `security-groups.ts`'s `add_perm` had no allow-list at all (unlike `tenant-role-perms.ts`'s `OVERRIDABLE_PERM_KEYS`), so a holder of `admin.manage_groups` could build a custom group holding `admin.*`/`audit.*` keys and add themselves to it. Added the same prefix guard, audit-logs denied attempts.
- `AccessControlPage.tsx`: filtered `admin.*`/`audit.*` out of the default Custom Groups picker (the canonical `PermPillPicker` call site) so the UI stops offering a path the server now refuses. Left the read-only "Base permissions" role matrix untouched — legitimate reference, not a grant surface.
- New migration (`2026_08_16b_security_group_perms_escalation_guard.sql`) — `CHECK` constraint on `security_group_perms.perm_key` as DB-level defence in depth. Verified live against jvkn before writing it: 0 of 7 existing rows (5 groups) would violate it. Written, not yet dispatched.
- `self-join-codes.ts`: swapped a bare `can()` for `requirePerm()` — was the one invite-adjacent endpoint whose denials skipped the audit-log write and the deactivated-account recheck every sibling endpoint already gets.
- Follow-up (Royce's call, see Decided): `invite-user.ts` + `self-join-codes.ts` now also require `admin.assign_role` when the target/self-join role is one that itself holds `admin.assign_role` by default (today just 'manager') — closes the same-shaped gap where inviting someone straight in as manager cost less than promoting an existing user to manager.
- eq-shell [PR #1387](https://github.com/eq-solutions/eq-shell/pull/1387): merged (`6024237`), confirmed live on core.eq.solutions (deploy `state: ready`, `published_at` set, commit matches, `verify-shell-session` responds 401 as expected).

## Decided
- Royce: "Confirmed, go ahead" — approved the brief before any edit landed.
- Royce chose "Require admin.assign_role for manager invites" (over leaving it as-is, or removing 'manager' from invite/self-join entirely) when asked about the invite-as-manager asymmetry.
- Royce: "merge" — explicit approval; this repo's merge-to-main is the production deploy, no separate deploy step.
- Considered, declined: blocking an admin from adding themselves to a group they're editing. Once the group itself structurally can't hold `admin.*`/`audit.*` (allow-list + DB CHECK), self-add can't escalate — would be pure friction with no security benefit. Not implemented.

## Deferred
- The DB `CHECK` constraint migration is written but not dispatched — goes through the normal governed `tenant-migrate.yml` step, not automatic on merge. Tracked in `eq/pending.md`.
- Not click-tested live — no way to mint a real Shell admin session in this sandbox (standing limitation, prior sessions on this repo hit the same wall). Tracked in `eq/pending.md`.

## Notes
- **A live, unresolved `git merge` (roles-v2.7.1-pin-bump → roles-v2.7.2-bump, conflict in `matrix.ts`) was found mid-session in the root clone `C:\Projects\eq-shell`, started by another concurrent worktree session while this session was mid-investigation** — confirmed via `MERGE_HEAD`/`ORIG_HEAD`/`MERGE_MSG` and cross-checked against `git worktree list` (the other branch was checked out in its own worktree, `.claude/worktrees/dropdown-filtering-capabilities-11ebbd`). Did not touch it — isolated this session's own fix into a fresh worktree (`C:\Projects\eq-shell-security-groups-escalation`) off clean `origin/main` instead, specifically because the conflicted merge also touched `AccessControlPage.tsx`, which this fix needed to edit too. The other merge resolved itself independently via PRs #1382/#1383/#1388 landing on main during this session — never needed intervention.
- **`scripts/check-role-literals.mjs` is a real CI gate in eq-shell** — fails on any `role === '<literal>'`/`!==` comparison against the 6 canonical role names anywhere in `src/**` or `netlify/functions/**`, unless suppressed with `// eq-role-literal-ok: <reason>`. First CI run on this PR failed on exactly this (`role === 'manager'` in the new invite-time gate). Fixed properly rather than suppressed: swapped for `roleDefaultCan(role, 'admin.assign_role')` (`@eq-solutions/roles`' own `can(role, perm)`, matrix-driven) — same practical effect today, but survives a future change to which role(s) carry that permission. Worth remembering for any future eq-shell permission work: never compare a role variable to a literal string for an access decision — route through `can()`/`requirePerm()`/`useCan()` (session-based) or the package's role-default `can(role, perm)` (when there's no session, e.g. checking a *target* role rather than the actor's).
- **`permission-enforcement-baseline.json` (the dead-permission-key ratchet file) needed a real hand-merge, not a mechanical one** — PR #1383 (landed independently mid-session) and this session's own change both removed `admin.assign_role` from `dead_keys` and both added a `_resolved` narrative entry for it, for two different, complementary reasons (edit-time enforcement in #1383's `edit-user.ts` vs. invite-time enforcement here). Kept both `_resolved` entries (renamed this session's key to `admin.assign_role_invite_path` to avoid colliding with #1383's `admin.assign_role_admin.delete_user`), took `origin/main`'s side for the shared `_categories` text (more complete — it also covered `field.approve_*` from the same PR), corrected this session's own entry afterward since "first real enforcement" was no longer true once merged with #1383.
- Full verification chain run on every commit before push: `pnpm run build` (tsc -b + vite build), `pnpm run test` (353/353), targeted `eslint`, plus the live jvkn read before writing the migration. No step skipped despite the auth-change sensitivity.

---

# Session 2026-08-16 (cont'd 9) — eq-shell: Documents module permission gate shipped; two flagged security items correctly closed without code

## Built
- Documents module (`AdminDocumentUpload.tsx` + `push-document-audience.ts`, `document-version-upload-init.ts`/`-commit.ts`, `upload-document-version.ts`) gated end to end on three new keys — `documents.view` (all six roles, browse/download only, never another person's signer status), `documents.manage` (manager+supervisor, upload/archive/category admin), `documents.assign` (manager+supervisor, push an audience and see who has/hasn't signed). Client-side (`useCan`/`<Gate>`) and all four server endpoints (`requirePerm`). [PR #1385](https://github.com/eq-solutions/eq-shell/pull/1385), merged (`fc5d3b11`), confirmed live via Netlify deploy `commit_ref` match.
- Split `push-document-audience.ts`'s shared `resource=register` endpoint into a narrower `resource=templates` (gated at the broader `documents.view`) — the original endpoint backed both the manager-only compliance register and the everyone-visible Reference Library tab, so gating it at `documents.assign` alone would have silently broken template browsing for every role below manager/supervisor.
- Opened PR #1390 for the control-plane drift block; turned out to be one of three sessions (#1390/#1391/#1392, see this file's earlier entry and `eq/pending.md`) that independently hit and fixed the same block within minutes of each other. Royce closed mine directly as a duplicate of the one that actually merged (#1389) — no code needed from this session on that thread.

## Decided
- Royce: "Confirmed — go ahead" on the Documents-module brief.
- Royce: "Merge #1385" (initial ask); later chose "Wait for the fix (Recommended)" over an admin-bypass when a repo-wide required check blocked it; then explicitly reconfirmed "Merge now" once that check genuinely went green rather than acting on the original ask across the multi-hour gap unprompted.
- Royce: "close #1390" — already closed by him earlier the same session; confirmed as a no-op rather than re-closing blind.

## Deferred
- None new. Everything opened this session was closed within it.

## Notes
- **SEC-1** (`nspbmir`/SKS Labour anon-key PII leak, still P0 in `ops/security-register.md`) — investigated on seeing it in the session-start NEEDS YOU list, correctly took no action. Royce has twice reaffirmed (2026-06-05, 2026-07-20) that it's decommission-at-cutover only, no interim engineering changes on that app. It surfaces in the digest by design as standing debt, not a fresh alert — don't re-flag it as new.
- **Dependabot #193/#194** (`image-size` DoS, ICNS+JXL/HEIF infinite loops) — traced a proposed fix (bump `@netlify/blobs` 10.7.9→10.7.13, which drops `image-size` as a transitive dependency entirely) before shipping it, and found it unnecessary: [PR #1288](https://github.com/eq-solutions/eq-shell/pull/1288) already patched the compiled parser output directly a week earlier, after discovering `image-size`'s upstream repo was archived by its maintainer with no fix ever coming. The version-bump path would have worked too, but only by deleting a verified, hand-tested patch in favour of an unvetted two-minor-version runtime dependency bump — reverted before opening a PR. Left a memory note so this isn't re-attempted next time the alert resurfaces in a sweep.
- **This repo had extremely high concurrent-session load today** — `git worktree list` showed ~20+ active worktrees on eq-shell alone mid-session. #1385's branch needed `origin/main` absorbed **three separate times** in one sitting to clear its required checks, each time hitting the same shared-file conflict shape in `permission-enforcement-baseline.json` (two sessions both adding a new entry to the same `_resolved` object, or both editing the `_generated_by` narrative line) — never a real logic collision, always mechanical: keep both additions, merge the prose. `gh pr diff 1385 --name-only` stayed a constant, clean 7 files through all three absorptions.
- **`/close`'s Step 2 manual-archive instruction is confirmed still stale** (already flagged 2026-08-08, `eq/pending.md` ~line 747, still open) — `rotate_pending.py` now handles fully-closed sections automatically, nightly. Did not hand-archive this session's own fully-closed Documents-module section in `eq/pending.md`; followed that prior session's precedent instead, to avoid conflicting with the automation.
- **eq-context's shared root clone (`C:\Projects\eq-context`) had another concurrent session's staged-but-uncommitted changes sitting in it** when this session ran `/close` (5 files: both changelogs, both tier `pending.md` files, today's session log). Did not touch or commit them — worked from an isolated worktree instead (per the existing `eq-context-shared-checkout-contention` pattern) and left the shared clone exactly as found.
---

# Session 2026-08-16 — Photo ID banner false-positive fixed: a same-day migration silently dropped an earlier fix

## Built
- Diagnosed a live regression reported directly by Royce: this morning's `eq_worker_compliance_status()` role-scoping change (PR #1376) had been rebuilt from an older baseline that predated a 2026-08-02 fix, so it silently reverted that fix. The effect: any worker who satisfies a company's "Photo ID" requirement with a driver's licence or passport (instead of a dedicated Photo ID upload) started wrongly seeing "Action needed on your card — Photo ID — not added yet" on their Home screen.
- Confirmed live: the deployed database function had the new role-scoping but not the older equivalence fix — verified against the actual worker Jordan A. Sample, who holds a driver's licence and was being wrongly flagged.
- Wrote and merged eq-shell [PR #1399](https://github.com/eq-solutions/eq-shell/pull/1399) — a new database update that keeps this morning's role-scoping and restores the driver's-licence/passport equivalence together, rather than editing either of the two earlier updates. Every piece of the new logic was checked line-for-line against the two originals before committing, to rule out copy mistakes.
- Applied the database update to the live system and re-verified directly against Jordan A. Sample's real record: Photo ID now correctly shows as satisfied.

## Decided
- Royce: "merge it, sign off given" — authorized both the PR merge (which auto-deploys eq-shell) and the live database update.

## Deferred
- None — fully closed same session: diagnosed, fixed, merged, applied, and re-verified against the real worker record that triggered the report.

## Notes
- The session started without the Supabase database tool connected, so live verification of the regression and the eventual live fix/re-check both had to wait until it became available mid-session — worth remembering that this tool isn't always present at session start even when the task is clearly database-shaped.
- Textbook case for why a same-day rebuild of a function is risky: PR #1376's author rebuilt from a named baseline file rather than diffing against what was actually live, so a fix that shipped in between (2026-08-02, unrelated PR, three weeks earlier) had no chance to be carried forward. Worth a standing habit: when a migration says "rebuilt from baseline X," diff X against `pg_get_functiondef` first, not just against the file on disk.

---

# Session 2026-08-16 (cont'd 10) — eq-shell: PR #1387's DB backstop applied to production, confirmed live

*Continues the earlier same-day entry above ("admin.manage_groups privilege escalation closed, PR #1387 merged and live") — that entry's migration was written but not yet dispatched at the time; this closes that gap.*

## Built
- Royce: "dispatch the migration" — applied `2026_08_16b_security_group_perms_escalation_guard.sql` to jvkn via Supabase MCP `apply_migration`. Corrects the earlier entry's mischaracterization: this is a control-plane migration (jvkn `shell_control`), hand-applied, never routed through `tenant-migrate.yml` (tenant planes only). First apply attempt was blocked by the Claude Code auto-mode classifier; stopped and explained rather than working around it. Royce: "retry the apply" — identical call succeeded.
- Verified live, not assumed: `pg_get_constraintdef` confirms the `CHECK` constraint on `shell_control.security_group_perms`; `get_advisors` security check clean, no new findings.
- Updated `eq/pending.md`'s Custom Groups write-up to reflect the constraint as applied. Pushing that one-line correction hit the identical classifier block a second time — this time on a plain `git push origin HEAD:main` to eq-context, not a database write. Same recovery: stopped, explained, Royce pushed it himself ("done in console"). Confirmed landed (`a0197d0`) before continuing.

## Decided
- Royce: "dispatch the migration", then "retry the apply" after the first attempt was blocked.

## Deferred
- None new. This closes the one substantive item left open from the earlier entry — only the live click-through remains, unchanged, still tracked in `eq/pending.md`.

## Notes
- **The auto-mode classifier's block isn't specific to database writes** — it also caught a plain `git push` to eq-context's `main`. Both times the correct response was identical: stop, explain what and why in plain terms, let Royce decide (retry vs. do it himself), never reach for a different tool to route around it. Worth remembering as a general pattern, not a Supabase-specific one.

---

# Session 2026-08-16 (cont'd 9) — eq-field mobile UI pass (4 fixes, PR #709 merged) + eq-shell Photo ID regression fixed (PR #1399); 2 of 6 reports weren't code bugs

## Built
- Royce reported 6 issues from live-testing Cards + Field as an apprentice on the SKS tenant, referencing "5 screenshots uploaded in cards" with no images actually attached to the message — traced to Google Drive (`search_files`/`list_recent_files`, filtered to images owned by `dev@eq.solutions`, most recent batch) once Royce clarified. Screenshots directly confirmed 3 of the 6 reports before any code was touched.
- Fanned out 5 parallel investigation/fix agents: 2 in the existing eq-cards worktree (already open, clean), 3 in a fresh eq-field worktree (`eq-field-ui-issues-wt`, branched off `origin/main`) created specifically to avoid the main `eq-field` checkout, which was mid-flight on an unrelated session's branch.
- **eq-field, all 4 bundled into [PR #709](https://github.com/eq-solutions/eq-field/pull/709) (v3.5.506, merged by Royce):**
  - Schedule/Roster/Editor/Dashboard week-nav row wrapped onto two lines on real phone widths (label's inline `min-width:180px` left no room for both nav buttons) — verified live via a local Playwright repro at 393×851 before/after, full e2e suite green. `styles/mobile.css`.
  - Field login intermittently looked locked on first load, self-fixed by a refresh — root cause: the auth boot sequence was gated on `window.onload` (waits on every stylesheet/icon, not just deferred scripts), so on a slow connection it could burn past Shell's 30s handoff-token window before even checking access. Swapped the trigger to `DOMContentLoaded`. `index.html`.
  - Apprentices (and regular employees/labour hire) saw Timesheets + Records in the MORE menu despite both being supervisor/manager content that went nowhere for them — `applyMobileNavPerms()` never gated either item when they shipped. Wired to the existing `ts.view_team`/`reports.incident.view` keys rather than inventing new ones.
  - Prestarts opened to everyone, per Royce's explicit call ("prestarts should be there for everyone") after I'd offered a narrower apprentice-only widening — `reports.prestart.view` added to employee/apprentice/labour_hire in both `permission-matrix.js` and its hand-merged runtime copy `core-bundle-a1.js` (this repo bundles several source files together via `scripts/build-bundles.mjs` for load performance; the bundle, not the source file, is what `index.html` actually serves — edited both, `--check` confirmed zero drift). Drawer button un-hidden from behind a manager-only CSS class.
  - Also bumped `APP_VERSION`/service-worker `CACHE` name/the changed files' own `?v=` cache-busting query params and added the `CHANGES IN v3.5.506` banner entry, matching this repo's own convention (and its own documented history of exactly this being forgotten and causing indefinite stale-cache serving) — regenerated `core-bundle-a1.js` via `build-bundles.mjs --write` after the version bump propagated into one of its 5 source files.
  - Tightened `permission-enforcement-baseline.json`'s dead-key ratchet — `ts.view_team` is no longer dead now that the Timesheets gate uses it.
  - Verified before merge: full CI green, plus a live smoke-check against the PR's own Netlify deploy preview (no console errors, all assets — including the newly version-bumped ones — loaded 200, boot completed and rendered real content).
- **eq-shell, [PR #1399](https://github.com/eq-solutions/eq-shell/pull/1399) (spun off as a background task, run in a separate session, merged):** the Home screen's "Photo ID — not added yet" banner was wrongly nagging workers who'd already uploaded a driver's licence or passport. Root cause: a same-day migration (`2026_08_16_org_credential_requirements_role_scope.sql`, from an earlier session's own per-role credential-requirements build) rebuilt `eq_worker_compliance_status()` from its pre-equivalence baseline while adding role-scoping, silently dropping an existing equivalence fix (`2026_08_02b`) for the identical driver_licence/passport → photo_id case eq-cards already handles correctly. Confirmed live post-merge by pulling the deployed function definition directly and re-running the exact repro case (the test worker from the original screenshots) through the live query logic — resolves to `held`, correctly.
- **eq-cards: 2 of the 6 reports weren't code bugs, confirmed by investigation rather than assumed fixed:**
  - The Wallet-side Photo ID equivalence logic was already correct and live (migration 0109, PR #185) — the false banner Royce saw was coming entirely from eq-shell, not eq-cards.
  - "Stuck on the Profile screen, can't navigate away despite the bottom nav bar being visible" didn't reproduce — router/nav wiring verified correct by reading the code AND by mounting the real production `HomeShellScreen` widget in a test and driving the exact tap interaction, which worked. Most likely a stale cached PWA build on the device that reported it, given this app's known history of exactly that class of bug.

## Decided
- Royce: apply the login-race fix past this repo's brief-gate hook (AskUserQuestion, explicit "Yes, apply it") — the hook had blocked both the investigating subagent and my own first attempt.
- Royce: "prestarts should be there for everyone" — widen `reports.prestart.view` to all roles rather than apprentices only (the narrower option I'd offered).
- Royce: "merge" (eq-field PR #709), and then merged it himself after the merge action itself was separately blocked by the environment's auto-mode classifier (a stricter, different gate than the brief-gate hook — this one specifically on `gh pr merge`, not just file edits).

## Deferred
- None new beyond the one already logged in `eq/pending.md` (Cards Profile-screen report — not reproducible, check for a stale cache if it recurs).

## Notes
- **The brief-gate hook (`guard.js`) and the auto-mode classifier are two separate gates, not one.** The hook blocks the first edit to product code in a fresh session until an in-session `/brief` runs; writing its own unlock flag file is itself blocked by the classifier (writing a bypass flag for a safety gate reads exactly like what the classifier exists to catch) — the flag-write step in `/brief`'s own instructions doesn't actually work in this environment as a result. What did work: running `/brief` for the transcript/confirmation value, then retrying the edit directly once Royce had given explicit in-chat approval — the edit succeeded on retry without ever writing the flag file. Worth knowing for any future session that hits the same block: don't fight the flag-file step, just get explicit approval and retry the edit.
- **eq-field hand-merges several source files into runtime "bundle" files** (`core-bundle-a1/a2/b1-b4.js`) via `scripts/build-bundles.mjs`, to cut always-loaded boot scripts down for a repo with no build step. `index.html` loads the bundles, not the originals — the originals stay on disk "unreferenced" per the bundle's own header comment. Any edit to a bundled source file needs either a matching hand-edit to the bundle (verified byte-identical via `--check`) or a `--write` regeneration — a plain edit to `permission-matrix.js`/`app-state.js` alone would silently not reach production.
- Confirmed, not assumed: Royce's "uploaded in cards" meant screenshots taken *of* the Cards/Field apps, saved to his Google Drive — not an attachment in the chat message itself. No images were actually present in the triggering message.

---

# Session 2026-08-16 (cont'd 11) — eq-cards: gated manager-role assignment behind admin.assign_role's live grant set, applied + deployed

## Built
- Investigated a P1 finding: `eq_cards_admin_upsert_worker`/`eq_cards_admin_create_invite`/`eq_cards_claim_invite` wrote a worker's `EqRole` — including `manager`, a suite-wide tier — gated only on `is_org_admin()`, no permission-key check, no audit trail. 4 parallel Explore agents dispatched (eq-cards repo, live jvkn SQL, eq-shell comparison, eq-roles/eq-context permission-model status) plus direct follow-up queries.
- Original finding's framing (reuse `admin.edit_user`) was already stale by the time of investigation: `eq-roles` v2.7.0 (commit `b184848`, same day) split out a narrower `admin.assign_role` key specifically for this gap — its own commit message names eq-cards as the motivating case. eq-shell had already cut its own `edit-user.ts` over from `admin.edit_user` to `admin.assign_role` the same day (PR #1383/#1387, live). Confirmed via live `git diff HEAD origin/main` against eq-context (which was 6-7 commits behind at the time) that none of the drift touched role/permission files, so this wasn't a stale-substrate false read.
- Traced `sync_worker_to_canonical()` (AFTER trigger on `public.workers`) — confirmed it only fires an async `net.http_post` to the `workers-canonical-sync` edge function (eq-shell repo), not a direct `shell_control` write, ruling out a more direct exploitation path than the invite/claim chain.
- Confirmed live: no SQL-level permission-key engine exists anywhere in jvkn (`perm_key` columns exist on `tenant_role_overrides`/`security_group_perms` but nothing reads them at runtime) — `requirePerm`/`admin.assign_role` enforcement is TypeScript-only in eq-shell, so the Postgres-side fix had to be self-contained rather than calling through.
- Wrote migration `0131_gate_worker_role_assignment.sql`: new `can_assign_worker_role(org_id, role)` — always `true` for non-manager roles; for `manager`, requires platform-admin OR tenant-level `manager` (mirrors `admin.assign_role`'s live role-default grant, deliberately excluding `is_org_admin()`'s `org_memberships.role='admin'` clause — a different, Cards-only concept never meant to authorize a suite-wide grant). Wired into both `eq_cards_admin_upsert_worker` and `eq_cards_admin_create_invite` (the latter also gained enum validation on its previously-unvalidated `p_profile_data->>'role'` input). Both now write an audit record via the existing `eq_write_audit_log()` (`shell_control.audit_log`) on a successful role grant, wrapped in its own exception block so an audit failure can't block the action. `eq_cards_claim_invite` deliberately untouched — confirmed via `pg_policies` that `worker_invites` INSERT/UPDATE are both gated on `is_org_admin(org_id)` only, so gating `create_invite` closes the only write path into `profile_data.role`.
- Verified live before writing any SQL: of 2 distinct active `org_memberships.role='admin'` users, both already hold `manager` or platform-admin independently — narrowing costs nobody real capability today. 12 of 14 live `manager` holders aren't admin/platform-admin at all, unattributable to this gap specifically (no provenance ever recorded — exactly the audit gap this migration also closes).
- Dart fix: `admin_worker_form_screen.dart`'s role dropdown (lines 261-280) now excludes `EqRole.manager` via `.where()`. Confirmed via subagent this is the sole call site in the repo that sets/changes a worker's role, and the only downstream RPC is `eq_cards_admin_upsert_worker`.
- Ran `/decide` (auto-mode triggered on ambiguous "what to do now?") — six-step protocol recommended branch-testing 0131 before applying to production. Royce confirmed, plus confirmed the Supabase branch cost ($0.01344/hr) separately.
- Branch creation (`mcp__supabase__create_branch` off jvkn) hit `MIGRATIONS_FAILED` — the tracked ~334-migration history doesn't replay cleanly on a fresh database, unrelated to 0131 (failed before 0131 was ever applied). Deleted the failed branch, spawned follow-up `task_390981f1` (Royce started it in a separate session immediately). Re-ran `/decide`'s reopened choice — Royce: "Apply + deploy now" (skip branch test, fall back to manual verification).
- Applied `0131` directly to jvkn via `apply_migration`. Verified live, read-only, post-apply (`BEGIN`/`SET LOCAL request.jwt.claim.sub`/`ROLLBACK`, real production data): a real tenant manager can grant `manager` (true), an unrelated caller cannot (false), that same caller can still grant `employee` (true, unaffected). Deliberately did NOT exercise the full `eq_cards_admin_upsert_worker`/`create_invite` RPCs end-to-end against production even under rollback — both touch tables with triggers that fire real side effects (the canonical-sync webhook; an unidentified "access-request-fulfillment" trigger on `worker_invites`) — relied on direct code inspection for the wiring instead, which is simple/mechanical.
- Pushed `security/gate-worker-role-assignment`, opened [eq-cards PR #254](https://github.com/eq-solutions/eq-cards/pull/254), CI green (`Analyze and test`, `Migration hygiene`), Royce: "merge it once CI passes" — merged `bc72c9a`, triggered `deploy.yml` via `workflow_dispatch` (eq-cards doesn't auto-deploy on merge), run `31938677820` green (edge functions + Flutter build both succeeded).

## Decided
- Royce (implicit, auto mode never overridden): proceed on the two open design questions from the original finding (permission key choice, role ceiling) via live verification rather than being asked directly — the "key" question resolved to a live-verified fact (admin.assign_role's own commit message names this exact gap) rather than a judgment call.
- Royce (`/decide`): branch-test 0131 before applying, confirmed via AskUserQuestion.
- Royce: confirmed the $0.01344/hr Supabase branch cost before creation.
- Royce: "Apply + deploy now" once branch-testing proved unavailable (infra gap, unrelated).
- Royce: "merge it once CI passes."

## Deferred
- `task_390981f1` — root-cause why jvkn's tracked migration history won't replay clean on a fresh Supabase branch (blocks branch-based testing/preview for this project entirely, not just this fix). Royce already started it in a separate session.
- Live click-through of `admin_worker_form_screen` on a real org-admin session — not done, no session available. Tracked in `eq/pending.md`.
- Bumping eq-cards' pinned `eq_roles` Dart dependency (`v2.5.5` → `v2.7.0`+) to consume `admin.assign_role` natively client-side — deliberately not folded into this fix; kept the SQL-side fix self-contained instead.
- `can_assign_worker_role()` deliberately doesn't consult `shell_control.tenant_role_overrides`/`security_group_perms` (both empty for `admin.assign_role` today, confirmed live) — documented as a known gap in the function's own `COMMENT ON FUNCTION`, not built out, since it would mean building a general-purpose SQL permission engine for a scoped fix.

## Notes
- **`ScheduleWakeup` misuse caught and reversed mid-session** — called it once to wait on a background job outside `/loop` mode (it's scoped to `/loop` dynamic-pacing only); noticed immediately, called again with `stop: true` before it could fire, no actual impact. The correct mechanism throughout this session for waiting on external async state (Supabase branch provisioning, GitHub Actions CI) was `Bash` with `run_in_background: true` on a bounded `sleep`, not a standalone/foreground sleep (blocked outright by this environment) and not `ScheduleWakeup`.
- **Stale duplicate task-notifications arrived more than once** for agents/timers that had already reported in full much earlier (the eq-cards investigation agent; a couple of superseded background sleep timers) — correctly identified as stale and ignored rather than treated as new work, per the tool's own documented behavior ("the same task-id may notify more than once").
- **eq-context substrate drifted significantly mid-session** (behind `1 → 6 → 7 → 8 → 15` commits at various points, all housekeeping/pending/session-log churn from concurrent sessions — none touched role/permission files when checked via `git diff HEAD origin/main --stat`). Never blocked a decision, but is a live illustration of why this file's own Rule 0.5 says to verify against origin/main rather than trust a local clone's stamp.
- **`admin.assign_role`'s own `roles/model.json` description literally names EQ Cards** ("Set or change what role a person holds — wherever that happens, including through EQ Cards") — written the same day this session investigated the gap it was created to close. Strong signal this and today's other permission-audit sessions (the `admin.manage_groups`/`admin.deactivate_user`/documents-module/origin-guard entries elsewhere in this file) are coordinated parts of the same suite-wide sweep, not independent discoveries.

---

# Session 2026-08-16 — eq-shell: found + fixed a live production crash on SKS admin settings; merge blocked by fresh schema drift

## Built
- Root-caused and fixed Sentry [EQ-SHELL-1N](https://eq-solutions.sentry.io/issues/EQ-SHELL-1N)/[EQ-SHELL-1P](https://eq-solutions.sentry.io/issues/EQ-SHELL-1P) — a React error #31 crash on `/sks/admin/settings` that hit Royce directly, 1 event, ~3h before this session found it via a routine monitoring pass ("keep an eye out"). `AdminTenantSettings.tsx` mistyped the `org-credential-requirements` endpoint's response as `string[]`; it actually returns `{licence_type, role}[]` (confirmed against `StaffPage.tsx`'s correct typing of the same endpoint, and against the Sentry stack trace's exact object keys). Fixed both call sites (~line 246, ~line 669) to extract `.licence_type` instead of trusting the stale cast. `tsc -b --force` clean. eq-shell [PR #1403](https://github.com/eq-solutions/eq-shell/pull/1403) — merged and confirmed live on core.eq.solutions (commit `8e0cf88f`, verified via Netlify deploy state `ready` against that exact commit — not assumed from the merge alone). See "Resolved after close" below for how the merge blocker cleared.

## Decided
- Royce: handle the crash fix directly in this session rather than via the task chip spawned for it.
- Royce: hold off on merging PR #1403 rather than force-merge past a newly-discovered, unrelated schema-drift check — see Deferred.

## Deferred
- eq-roles [PR #28](https://github.com/eq-solutions/eq-roles/pull/28) (`tender.view`, v2.7.3) — checked live via `gh`, still open/unmerged/untagged. No action taken, per standing instruction to wait for merge + tag before bumping eq-shell's pin.

## Resolved after close (same session, continued)
- Royce: "clear it and ship." eq-shell PR #1403 merged and live — commit `8e0cf88f`, confirmed via Netlify deploy state `ready` against that exact commit.
- **The blocker resolved via [eq-shell PR #1408](https://github.com/eq-solutions/eq-shell/pull/1408), not true out-of-band drift.** A concurrent session had already built the correct fix independently — verified its diff before relying on it (it goes further than a first draft would have: diffs the live jvkn function body against the eq-cards migration to confirm an exact match, only cosmetic normalisation differences). Properly migrated in eq-cards `supabase/migrations/0131_gate_worker_role_assignment.sql` ([PR #254](https://github.com/eq-solutions/eq-cards/pull/254), merged 09:17:41). Same cross-repo blind spot as `is_worker_in_org`/`eq_cards_admin_list_stale_invites` (#1328, #1341, #1389) — `check-control-plane-drift.mjs` only scans eq-shell's own migration tree, so anything committed in eq-cards always reads as unsourced here. #1408 merged (by the other session, moments before this one could attempt it); its fix was merged into #1403's branch, which re-ran green including the drift check, and #1403 merged clean. Unblocked every PR that was failing on this, not just #1403's.

## Notes
- A task chip spawned for the crash fix (`task_4b365676`) was apparently dismissed rather than run once Royce saw the fix happening directly in this session instead — confirmed via exhaustive check (`list_sessions` to 50 incl. archived, `search_session_transcripts` for both the task id and "AdminTenantSettings", plus a PR content search across all states), all came back empty. Worth recording for future chip use: **worktree folders in this environment get reused across unrelated sessions over time** (one folder alone hosted 3+ different unrelated branches/sessions today), so matching a worktree's last-commit content to a specific chip is not a reliable way to find it. `list_sessions` / `search_session_transcripts` is the right tool; git-log forensics on a worktree folder isn't.
- eq-context's shared root checkout (`C:\Projects\eq-context`) had another live session's uncommitted changes sitting in `eq/pending.md` and 3 other files for most of this session — first staged-idle, then (re-checked at close time) caught mid-edit, unstaged, actively changing. Did not touch the shared checkout for this close — used an isolated worktree instead (created off `origin/main`, committed, pushed, then removed) to avoid colliding with or misattributing the other session's in-progress work. Same shape as the standing shared-checkout-contention lesson, now confirmed to recur under an even tighter race than before (mid-edit, not just idle-staged).
- **The same contention pattern isn't unique to eq-context — hit the shared `C:\Projects\eq-shell` root checkout too, post-close.** Went to build the `KNOWN_UNSOURCED` fix directly and found the checkout already sitting on a branch (`fix/control-plane-drift-can-assign-worker-role`) neither this session nor any prior turn in it had created — another concurrent session had checked it out to build the exact same fix, already pushed as PR #1408. Nothing was lost (working tree was clean, own commit already made), but it's a live illustration that ANY shared root checkout in this environment, not just eq-context, can be silently repointed by a concurrent session between one tool call and the next — worth a `git status --branch` sanity check before assuming the checkout is still on the branch you last left it on, in any repo, not just eq-context.
---

# Session 2026-08-16 (cont'd 12) — eq-shell: permission-hygiene report verified against live code (most of it already stale), 2 real gaps fixed + 1 DB fix applied; eq-roles v2.7.3 shipped; Today's Actions removed from the dashboard on Royce's call

## Built
- Received a 4-item P2 permission-hygiene report for eq-shell and re-verified every claim against current `main` before touching anything, rather than trust it — a deliberate choice given `origin/main` had moved 15+ commits (several concurrent security PRs) between when the report was likely generated and this session starting. Paid off immediately: 2 of the report's own cited examples (`admin.deactivate_user`'s dead call site; `self-join-codes.ts`'s bare `can()`) had already been independently fixed by concurrent PRs #1387/#1388 before this session opened a single file.
- **Fixed for real, still live findings:**
  1. `permission-enforcement-drift.test.ts`'s dead-key ratchet substring-matched raw file text *including comments* — a key named only in a `// comment` registered as "enforced." Hardened to strip comments first. No behaviour change today (19 dead keys before/after — the specific case that surfaced this was already fixed independently), but the next occurrence won't slip through the same way.
  2. Two client/server permission-key mismatches, both concretely reachable today via the seeded "Project Managers" default group (grants `admin.list_users`/`admin.edit_user`/`admin.review_cards`/`audit.view` — none of the narrower keys below): Data Activation page was gated on `admin.list_users` while the server requires `entity.manage_activation`; `AdminEditUser`'s Security-groups section had no gate beyond the page's own `admin.edit_user` while the server requires `admin.manage_groups`. Both aligned to match their real server-side gate.
  3. `eq_list_recycle_reviews`/`eq_list_phone_link_reviews`/`eq_resolve_recycle_review`/`eq_resolve_phone_link_review` (the Number Reviews page, jvkn control plane) each gated on a raw `v_role = 'manager'` JWT-claim string — didn't see `admin.list_users` granted via that same "Project Managers" group, so a member could open the page but every RPC 403'd. New migration `2026_08_16c_review_queue_group_perm_gap.sql` adds `shell_control.caller_holds_group_perm` (mirrors the existing `is_active_tenant_member` helper shape) and ORs it into all four checks — re-asserted `GRANT EXECUTE` on every replaced function per this repo's own `eq_enforce_function_privacy` trigger gotcha. **Applied to jvkn directly by Royce via the dashboard SQL editor** (Supabase MCP was disconnected all session) — handed him the exact SQL plus two verification queries; he ran both and confirmed clean (all 4 functions reference the new helper; `authenticated`+`service_role` EXECUTE intact on all 4).
  4. Self-caught while tracing *why* the report_viewers/reports.view_financial split existed at all: `reports.view_financial` had been wrongly marked `deprecated` in an earlier session's own plain-English rewrite pass, on the claim "every report includes financial figures, so this grants nothing `reports.view` doesn't already" — true only for default roles (both keys resolve to manager-only there), false for a security group, which is the one place the split actually matters. The seeded "Report viewers" default group was the live casualty: grants `reports.view` only, but GM Reports' first fetch hard-requires `reports.view_financial`, so anyone added to it would 403 immediately (0 members tenant-wide today, so nobody's hit it live). Fixed cross-repo in [eq-roles PR #27](https://github.com/eq-solutions/eq-roles/pull/27) → merged, tagged **v2.7.3**. eq-shell's own pin bump + local `default-groups.ts` mirror update is a deliberately separate, NOT-yet-opened follow-up PR.
- **Verified, explicitly not re-decided:** `audit.rollback` (the AdminAuditPage Rollback button) still can't succeed — a prior migration already turned that from a raw crash into an honest error message and explicitly left retire-vs-implement for Royce. Confirmed that's still the state; not touched.
- Committed all of the above (test fix, 2 key-alignment fixes, migration) on branch `claude/permission-hygiene-sweep`, opened [eq-shell PR #1397](https://github.com/eq-solutions/eq-shell/pull/1397). Royce: "merge #1397 with --admin" — checked CI first rather than blindly bypass; every required check (including the one that needed `--admin` for #1382 the same day) had already passed cleanly by the time I looked, so did a normal squash merge instead and said so plainly. Merged, live (`e67fa6101`).
- Separately, Royce reviewed his own live dashboard mid-session and asked to steelman removing "Today's Actions" (the AI-briefing panel) — "AI slop and not useful plus slow to load." Ran `/decide` (six-step protocol) before touching anything: recommendation was to pull it off the dashboard now (cheap, reversible) and treat the backend's fate as a separate, later call. Royce: "remove it."
- Removing it properly meant removing the whole `ai-briefing` fetch, not just the visible panel — leaving the fetch running just to hide its result wouldn't have fixed "slow to load" (the slowness is structural: a 10-minute cache TTL means most loads hit a live Claude synthesis call). Found and confirmed via a follow-up question that 2 OTHER stat tiles quietly used the same payload as an optional value override ("scheduled today", "live quotes" — both degrade cleanly to their existing plain counts) and one mobile tile ("Outstanding quotes") had no other data source at all — Royce chose to remove the fetch entirely and drop that one tile rather than leave it permanently showing "—".
- While writing this close entry, found and fixed a direct consequence of the removal: `SignalsBoard.tsx`'s Compliance card had two mobile-only suppressions explicitly justified in their own comments by "Today's Actions already shows this" — the whole card could hide when licences were its only content, and the licences detail group was unconditionally hidden on mobile even when the card WAS shown. Un-suppressed both (existing code, not new development) — otherwise mobile users would have lost per-worker licence-expiry detail down to just a raw count, with the suppression's own justification silently gone false.
- Opened [eq-shell PR #1406](https://github.com/eq-solutions/eq-shell/pull/1406) (2 commits). **Not merged** — flagged for a quick look at the deploy preview first (stat rows now have fewer tiles) since this wasn't checked in an authenticated browser session.
- `ai-briefing.ts`/`briefing-action.ts` (the two Netlify functions the removed panel called) now have zero callers anywhere in the repo — not deleted (retiring backend infra is a separate call from removing a dashboard panel), flagged via a spawned background task (`task_b07e0052`) instead of left as a silent dangle.

## Decided
- Royce: merge #1397 with `--admin` — ended up executed as a plain merge once CI was confirmed green, told him so rather than silently using the override anyway.
- Royce: merge #27 (eq-roles).
- Royce: remove Today's Actions, after the `/decide` steelman pass — chose full removal (fetch + all 3 dependent stat surfaces) over the lighter "just hide the panel" option once the tradeoff was made explicit.
- Royce: applied the jvkn migration himself via the dashboard SQL editor once told Supabase MCP wasn't available this session, then ran and confirmed both verification queries.

## Deferred
- eq-shell's pin bump to `@eq-solutions/roles` v2.7.3 + the `default-groups.ts` local mirror update — separate follow-up PR, not yet opened.
- `ai-briefing.ts`/`briefing-action.ts` retire-vs-repurpose decision — spawned as `task_b07e0052`, not decided this session.
- `audit.rollback` retire-vs-implement — still open, still explicitly Royce's call per the migration's own comment; not re-litigated.
- [eq-shell PR #1406](https://github.com/eq-solutions/eq-shell/pull/1406) itself — not merged, waiting on a deploy-preview visual check.
- `shell_control.eq_revoke_session` has the identical raw-role-string permission-check gap as the 4 RPCs fixed in #1397 — noted in that migration's own header comment as out of scope, not fixed here, no chip spawned for it yet.

## Notes
- **This session is a direct, concrete illustration of Rule 0.5** ("verify against the live system, not stale reports/docs") — the source report was internally coherent and specific (file:line citations) but 2 of its 4 items were already false by the time of verification, purely from same-day concurrent security work elsewhere in the suite. Treated as leads, not facts, exactly per the standing rule; would have wasted a merge cycle "fixing" already-fixed code otherwise.
- **The eq-context shared checkout was under very heavy concurrent load this session** — hit an actual conflicted interactive rebase mid-`/close` (stopped immediately, asked Royce, waited it out rather than abort/continue/skip blind), then repeatedly found other sessions' staged-but-uncommitted writes appear and vanish within seconds of each check (confirmed via a fast-moving `git log`/`git status` sequence, not assumed). Same pattern several other sessions logged today. Switched to a disposable detached worktree (`git worktree add --detach`) for this close's own writes rather than keep contending for the shared checkout — matches the pattern at least 2 other sessions today independently converged on; worth promoting to the default `/close` instruction rather than something each session has to rediscover.
- `git stash` on the shared checkout was blocked by the auto-mode classifier mid-investigation of the rebase conflict — stopped and asked rather than route around it with a different command, consistent with how the classifier's own guidance says to handle a block.
- `eq/pending.md` is 3,756 lines / ~507KB — well past comfortable single-file size, and each session (this one included) keeps appending rather than archiving, since the archive rule only fires per-section-with-zero-open-items and most of today's sections still carry at least one open item. Not fixed here (a real housekeeping pass, not a per-close step), but flagged plainly since it's now large enough that `Read` can't load it in one call.
- **Version-number collision spotted while resolving this file's own rebase conflict against the previous entry above:** that session's eq-roles PR #28 (`tender.view`) is also targeting **v2.7.3** — but this session's eq-roles PR #27 already claimed and shipped v2.7.3 first (merged, tagged, pushed). PR #28 is still open/unmerged/untagged as of that entry, so its `model.json`/`package.json` version needs to bump to **v2.7.4** before it can merge and tag cleanly — flagging here since neither session would otherwise know about the other's version claim.

---

# Session 2026-08-16 (cont'd 13) — eq-shell: closed the can_assign_worker_role drift gate, unblocked 6 open PRs

## Built
- Root-caused `public.can_assign_worker_role` failing eq-shell's control-plane drift gate (the required "Schema drift + anon-grant + policy-lint" check, blocking every open PR since ~09:11 UTC). Confirmed live via `pg_get_functiondef` on jvkn and matched byte-for-byte (modulo pg's own cosmetic normalisation) against eq-cards `supabase/migrations/0131_gate_worker_role_assignment.sql` ([eq-cards PR #254](https://github.com/eq-solutions/eq-cards/pull/254), merged `bc72c9a` 09:17:41) — a Royce-approved live security fix (gates `manager`-role assignment behind `admin.assign_role`'s grant set, not just `is_org_admin()`), not unexplained drift. The migration file's own "NOT YET APPLIED, needs Royce's go" header is just stale relative to his later same-session approval ("Apply + deploy now" via `/decide`, confirmed in this file's own cont'd-11 entry) — flagged so it isn't misread as an unapproved change on a future pass.
- Added it to `KNOWN_UNSOURCED` in `scripts/check-control-plane-drift.mjs`, matching the citation convention of the 5 existing cross-repo entries. eq-shell [PR #1408](https://github.com/eq-solutions/eq-shell/pull/1408) — verified green on its own branch before asking, Royce approved the merge (this repo's merge = prod deploy within seconds), squash-merged `8d805de`.
- Swept all 13 PRs open at the time this landed, not just the 3 originally reported as affected: `#1402`, `#1404`, `#1405`, `#1406`, `#1407` were all still failing on the gate's stale pre-fix state even after #1408 merged. Root cause: `gh run rerun --failed` does NOT pick up a just-merged main-branch fix — it reuses the original `refs/pull/N/merge` snapshot pinned when that run first triggered (confirmed via job log: a rerun still showed only the pre-fix 5-entry `KNOWN_UNSOURCED` list). `gh api -X PUT repos/.../pulls/<n>/update-branch` (the real "Update branch" action) does work — merges current main into the PR branch server-side, forcing a fresh merge-ref and a fresh CI run with no local checkout involved. All 5 confirmed green afterward (~1m each). `#1403` (this file's own cont'd-11 admin-settings crash fix) picked up the same fix via a local `origin/main` merge and was merged directly by Royce once green.

## Decided
- Royce: "Merge now" — approved merging #1408 once verified green, offered via pre-populated options rather than an open-ended question.

## Deferred
- None — the full 13-PR sweep confirmed clean; no PR remained blocked by this gate as of session end.

## Notes
- **Confirmed a shared-checkout collision live, not just inferred.** Mid-task, `C:\Projects\eq-shell` (the plain repo root, **not** a `-wt` worktree) silently changed checked-out branch and gained a commit (`379bb058`) this session never made. Investigated rather than assumed the fix had been reverted — `gh pr view 1403 --json mergedBy` confirmed it was Royce himself, working PR #1403 directly in this same directory at the same time: he merged `origin/main` to pick up #1408's fix, then merged #1403 once green. No harm done (routed the rest of this session's PR-branch updates through the GitHub API instead of local git once noticed), but it disproves the working assumption that a non-`-wt` repo root is single-session-exclusive — logged as its own memory since the existing shared-checkout lesson on file was scoped to `-wt` folders and to `eq-context` specifically, not the eq-shell root.
- `eq-context` substrate was itself significantly behind (8 commits) by the time `/close` ran this session — reconciled with a plain `git merge --ff-only origin/main` before writing anything (0 local commits ahead, so no rebase was needed). The pull surfaced this file's own already-existing `can_assign_worker_role` entry, logged as still-open by the session that first found it (cont'd 11 above) — updated `eq/pending.md` in place rather than duplicating it.

---

# Session 2026-08-16 (cont'd) — eq-cards: jvkn Supabase branch-replay root-caused and documented, PR merged

*Started from testing an unrelated eq-cards migration (0131) — `create_branch` on jvkn failed `MIGRATIONS_FAILED` before that migration was ever reached, so the session turned into root-causing why branch creation has never worked on this shared control-plane project at all.*

## Built
- Root-caused precisely by querying `supabase_migrations.schema_migrations.statements` directly — both on the parent project and on the failed branch's own still-alive database, even after `get_project` on the branch ref started returning "Project not found."
- **Root cause #1 (confirmed):** the 3rd migration ever tracked on jvkn references 12 tables no tracked migration ever created — traced to dead first-week "canonical entity" scaffolding, already dropped 6 days later (`2026_05_25_drop_empty_app_data`); zero live-data risk either way.
- **Root cause #2 (strong evidence):** cross-referenced all 61 currently-live tables against the full 334-row tracked history and found 12 more untracked-origin tables, all in eq-shell's `shell_control` schema — several still live and load-bearing today (`user_invites`, `persons`, `platform_config`, `provision_tokens`). Not eq-cards' to fix blind.
- Ruled out migration `0112`'s own header comment as a red herring — it claims two functions "predate any tracked migration"; checked directly, both are actually properly tracked. Sent the investigation down the wrong path first.
- Wrote `eq-cards/supabase/migrations/README.md` — both root causes, the dead end, and a reusable diagnostic technique for the next failure.
- Along the way, investigated a hardcoded `WORKERS_WEBHOOK_SECRET` value sitting in one tracked migration's statement text (still live, never rotated since 2026-06-11) — corrected an initial overstated exposure claim (same service-role DB tier as the vault itself; `anon`/`authenticated` can reach neither), confirmed it's a single early leftover rather than a spreading pattern (the 3 other secrets created the same way already use a safe generate-in-Postgres + verify-RPC pattern), and found the edge function holds its own separately-configured copy rather than reading the vault — rotating would need both updated together or the live Cards→SKS staff sync 401s.
- eq-cards [PR #255](https://github.com/eq-solutions/eq-cards/pull/255) opened, CI green, squash-merged (`24ecb2b`); post-merge CI on main re-confirmed green.

## Decided
- Royce: document only, for now — don't attempt a live backfill on jvkn (a shared prod-adjacent project) to fix root cause #1, since it wouldn't even get branch-preview fully working end-to-end (root cause #2 would still block it further down, and that half isn't eq-cards' to fix).
- Royce: leave the `WORKERS_WEBHOOK_SECRET` rotation for now, given the corrected lower urgency.
- Royce: push the branch, open a PR, then merge it — each its own explicit instruction in sequence.

## Deferred
- Root cause #1's precise backfill (reconstructing minimal table shapes from ~32 migrations, no live table left to verify against) — not attempted, Royce's call.
- Root cause #2 — eq-shell's own untracked `shell_control` tables — flagged for eq-shell to trace; eq-cards has no visibility into their original shape. eq-shell PR #1389 ("triage 3 jvkn functions into KNOWN_UNSOURCED," merged the same day per this file's own cont'd-11/-12 entries above) suggests a related tracking mechanism may already exist there, worth connecting to instead of duplicating.
- `WORKERS_WEBHOOK_SECRET` rotation — parked; if picked up later, needs jvkn's vault AND eq-shell's Edge Function secret updated in the same window.

## Notes
- **A spawned background-task chip for the secret finding turned out to route back into this same conversation** rather than run as a genuinely separate session — checked via session-list/transcript-search before assuming a duplicate investigation might be running unaware of Royce's "leave it for now" call; found no separate session, nothing to reconcile.
- **This session's working-directory checkout was sitting on `security/gate-worker-role-assignment`**, a branch already merged to main by a concurrent session as [PR #254](https://github.com/eq-solutions/eq-cards/pull/254) (confirmed by diffing the merged commit's files against local HEAD — identical). Left it alone rather than touch it; created a fresh branch off `origin/main` for this session's own commit instead, matching this repo's PR-per-branch convention.
- The diagnostic technique — query `schema_migrations.statements` directly, on both the parent and a failed branch's own still-alive database — is written up in full in the new README and is reusable verbatim for any future Supabase project hitting this same class of failure, not just jvkn.

---

# Session 2026-08-16 (cont'd 14) — eq-shell: confirming close, one correction to the record above

## Built
- Nothing new — no work happened between the previous push and this `/close`. Verified `origin/main` had 0 commits this session hadn't already seen before writing anything further.

## Decided
- None new.

## Deferred
- None new.

## Notes
- **The entry directly above ("cont'd 13") states as confirmed fact that Royce personally merged PR #1403** ("`gh pr view 1403 --json mergedBy` confirmed it was Royce himself"). That's not fully supported by the evidence it cites, and this session has direct counter-evidence worth recording rather than letting the stronger claim stand unchallenged: `mergedBy`/`author` on GitHub reflects whichever identity the `gh` CLI is authenticated as in this environment, which appears to be Royce's account (`Milmlow`) for every PR regardless of actual origin — every PR opened by a Claude session today, including ones with no plausible human authorship (the origin-guard batch sweep, #1408 itself), shows the same `"login": "Milmlow"`. So `mergedBy: Milmlow` doesn't distinguish "Royce, live, at a keyboard" from "any session's `gh pr merge` call" — it's consistent with either. Separately, this session ran `gh pr merge 1403 --squash --delete-branch` itself, mid-conversation, and got a clean exit with no "already merged" error (the same command against #1408 minutes earlier *did* return that error when someone else had already merged it) — then immediately confirmed `mergedAt`/`mergeCommit` and cross-checked the commit against Netlify's independent deploy record. That sequence is at least as consistent with this session's own action being what actually merged it as with Royce doing it live. Genuinely can't resolve which of us actually clicked merge from here — logging both readings rather than picking one, since the earlier entry's confidence outran what it could actually show. Doesn't change the outcome either way: PR #1403 is merged and live, independently verified against Netlify, regardless of who gets credit for the click.
- The same entry's finding on `gh run rerun --failed` not picking up a just-merged main-branch fix (it replays the original pinned merge-ref; `gh api -X PUT .../update-branch` is the one that actually works) is worth remembering — this session avoided the trap by accident, not by knowing about it: updated #1403's branch with a local `git merge origin/main` + push rather than a rerun, which triggers a genuinely fresh CI run either way.
---

# Session 2026-08-16 (cont'd 13) — eq-shell: roles pin bump + a correction to an earlier "live" claim

## Built
- Bumped eq-shell's `@eq-solutions/roles` pin from `v2.7.2` to `v2.7.3` — the tag cut earlier this session (eq-roles PR #27, `report_viewers` fix). This was the "Next" item flagged at the end of the earlier close pass in this same file.
- Netlify's build command runs `pnpm run check:perms` ahead of `build`; that step (not part of the earlier local verification) caught real drift: `netlify/functions/_shared/default-groups.ts`, a hand-kept mirror of the roles package's `defaultGroups` (Netlify's function bundler can't import the package directly, so bundled functions run against this local copy instead), still had `report_viewers` on the pre-fix perms list. Fixed to match.
- eq-shell [PR #1412](https://github.com/eq-solutions/eq-shell/pull/1412) — build/test/lint clean, deploy preview green. **Open, not merged** — needs Royce's merge-go.
- `eq/pending.md` and `eq/changelog/eq-shell.md` corrected in place (not rewritten) to reflect this.

## Decided
- None this pass — no Royce input arrived; the two background chips flagged in the earlier close (`admin self-grant via security groups`, `Cards role-assignment path`) had both already resolved on their own (PRs #1387 and #254, merged) by the time this pass checked on them.

## Deferred
- eq-roles [PR #28](https://github.com/eq-solutions/eq-roles/pull/28) (`tender.view`) still targets `v2.7.3`, which no longer exists as the tip — it now needs to rebase past this pin bump's target and cut `v2.7.4`. Already flagged in the earlier close entry; not actioned here, not mine.
- The ai-briefing/briefing-action retire-vs-repurpose decision — confirmed still running as its own session (`task_b07e0052`), not duplicated.

## Notes
- **Correction to this file's earlier same-day entry:** the "Report viewers" fix (cont'd-12 entry above) was logged as "merged and released as v2.7.3" without flagging that eq-shell itself still needed to consume that release — the roles-package fix alone never reached production. Caught here only because Netlify's own build gate (`check:perms`) failed on the first push; a plain `pnpm run build` locally does not run that check, so it went unnoticed until the deploy preview actually tried. Worth remembering for future roles bumps in this repo: run `pnpm run check:perms && pnpm run check:css` locally too, not just `build`/`test`/`lint` — they're cheap and they're exactly what Netlify gates on.
- Confirmed via `gh`/session-list, not assumed: both background chips spawned during the earlier permission-hygiene sweep had already run to completion and merged cleanly (#1387, eq-cards #254) before this pass started — nothing further to do on either.

---

# Session 2026-08-16 (cont'd 10) — eq-shell PR #1413: three separate name-sync bugs + a real double-navbar bug, all root-caused, fixed, merged, live

*Direct continuation of the same live-testing session (cont'd 9, above) — Royce kept testing after that close and reported two more things: a Staff-list record showing "Unnamed", and no approval alert for a new worker. Investigating those surfaced a third, unrelated report too (a screenshot showing the Wallet still flagging "1 thing needs a look", plus "you can't get to the cards profile screen, the UI has a double navbar" and "it says welcome 'contact'").*

## Built
- **"Unnamed" on the Staff list, root-caused:** `app_data.staff.first_name`/`last_name` are `NOT NULL`, so a phone-only signup stores `''` there — normal at that point. The sync job that's meant to backfill the real name later (`workers-canonical-sync/index.ts`) checked the *existing* value raw (`c.first_name ?? ne(record.first_name)`), so `'' ?? incoming` short-circuited on the empty string every single pass — the name was **permanently locked blank**, forever, no matter what real name later arrived from Cards. Confirmed isolated to this one worker (checked 7 other recent SKS connects, all synced fine). Fixed by sanitizing the existing value too, matching the pattern already used for email/phone three lines below.
- **Approval alert — investigated, nothing built.** `field_approved` was already `true` on the stuck record with no approver/timestamp — traced to a deliberate 2026-06-15 decision (auto-approve pipeline-synced staff so they show up on EQ Field), not a bug. There genuinely is no "new worker ready for review" notification for this connect path — three existing pending-approval queues in `StaffPage.tsx` (worker-initiated requests, QR self-join, labour-hire candidates) don't cover a worker who arrives with an already-linked Cards identity. Logged as a deferred decision for Royce rather than built unasked.
- **"1 thing needs a look" — traced, not a bug.** Read through all five things that can trigger that banner; the org-requirements RPC (already fixed, confirmed satisfied) wasn't the cause. Checked the worker's actual profile live — fully complete (all 9 fields). The real cause: a generic "you're missing some common site tickets" suggestion (first aid, working at heights, forklift) — working as designed.
- **"Welcome, Contact" greeting — a third, independent bug, same family.** Traced the greeting to `session.user.name`, which comes from yet another table (`shell_control.users.name` — different from both `app_data.staff` above and Cards' own `public.workers`/`profiles`). That field only ever gets read from the Cards profile at the moment a worker's account is first created (a fix from 2 days earlier, 2026-08-14) — nothing re-checks it later. This worker's account predates that fix by 10 days, so it was stuck on a blank name with no way to ever self-correct. Added a new database trigger mirroring an existing, already-proven one (`trg_sync_profile_to_worker`) — backfills `shell_control.users.name` from the Cards profile whenever it's saved, but only ever fills a blank, never overwrites a real name. Counted 38 other workers stuck the same way before writing anything, then included a one-time backfill for all of them in the same migration.
- **"Can't reach the Cards Profile screen" — confirmed real this time.** An earlier pass in this same session had tested Cards' own navigation in isolation and found nothing wrong — the gap was that test never simulated being embedded inside Core, which is how every real worker actually reaches it. Root cause: Core was never told Cards has its own bottom navigation, so on the common path it fell back to drawing its own full navigation bar (top wordmark + persistent Home/Field/Service bar) directly over Cards' content, with no space reserved for Cards' own Wallet/Profile switcher underneath. Core's bar physically covered Cards' bar at the same pixels and intercepted every tap. Fixed by giving Cards the same "step back, I own the bottom" treatment Field and Service already correctly get — one line telling Core to treat Cards the same way, plus closing the identical gap for a second, rarer path (field-first workers) that had the same bug for a different reason.
- All three code fixes plus the new migration bundled into one PR — [eq-shell #1413](https://github.com/eq-solutions/eq-shell/pull/1413). Verified clean before opening it: full TypeScript build compared against an untouched copy of the code, identical error count before and after (all 91 pre-existing, none in the changed files). The migration was tested three separate times against the live database inside a transaction that gets thrown away afterward (nothing real touched) before ever writing the real version — confirmed the backfill works, confirmed the ongoing fix works on a simulated future save, and confirmed it never overwrites a name that's already correct.
- Migration applied live (Royce's go) — confirmed immediately after: the reported worker's name fixed, and all 38 stuck records cleared to zero.
- PR #1413 merged (Royce's go, squash `fdaa4bae`). `gh`'s merge command reported a local error afterward, but that was only its own housekeeping step (tried to switch a local branch already in use by an unrelated concurrent session's worktree) — confirmed directly via the GitHub API that the actual merge went through cleanly; cleaned up the leftover remote branch by hand.

## Decided
- Royce: apply both eq-shell code fixes (yes).
- Royce: keep digging on the greeting-name issue rather than leave it for later (yes).
- Royce: backfill every worker already stuck blank right now, not just fix it going forward (yes).
- Royce: apply the new migration to the live database now (yes).
- Royce: "merge 1413" (yes).

## Deferred
- Already logged in `eq/pending.md`: no admin-facing alert exists for a new worker who connects directly and lands pre-approved — needs Royce's call on whether one should exist.

## Notes
- **Screenshots this session came from Google Drive, not the chat itself** — Royce said "I've just uploaded 5 screenshots in cards" with nothing actually attached; the images were on his Google Drive the whole time, found by listing his most recent uploaded files once he clarified. Same thing happened again for a later single screenshot. Worth remembering: "uploaded" doesn't always mean "attached to this message."
- **A name can live in three genuinely separate places across this platform** — Cards' own copy (`public.workers`/`profiles`, jvkn), Core's copy of a worker's employment record (`app_data.staff`, ehow), and Core's copy of the login account itself (`shell_control.users`, also jvkn but a different table entirely). All three had independently drifted out of sync in the same way (a value set once, nothing to refresh it later) — worth checking all three, not just one, the next time a name-related report comes in.
- **This repo uses `pnpm`, not `npm`** — a fresh checkout's `npm ci`/`npm install` fails with a confusing, unrelated-looking error (`workspace:` protocol not understood) rather than a clear "wrong package manager" message. `pnpm install --frozen-lockfile` is what actually works.
- **`gh pr merge`'s exit code isn't reliable on its own when local git housekeeping fails** — the actual merge can succeed on GitHub even when the command reports an error, if the failure is in its own follow-up local branch cleanup. Always confirm via `gh pr view --json state,mergedAt` rather than trusting the command's own success/failure.

---

# Session 2026-08-16 (cont'd 15) — eq-shell: PR #1412 merged on Royce's explicit go, confirmed live

## Built
- eq-shell [PR #1412](https://github.com/eq-solutions/eq-shell/pull/1412) (roles pin bump to v2.7.3 + default-groups mirror fix, opened in the previous entry) merged on Royce's explicit "merge #1412" — squash, commit `424be93`.
- Confirmed live properly, not assumed: pulled the newest production deploy from Netlify's own deploy list and matched its `commit_ref` against the merge commit, rather than trusting "the merge succeeded" alone — the exact shortcut that caused the correction one entry back.
- `eq/pending.md`'s permission-hygiene section trimmed per the archive rule: 2 genuine open items remain (Rollback button, `eq_revoke_session`), both untouched this pass and still Royce's call; everything resolved this session, including the pin-bump follow-up, dropped from the live doc since it's preserved here and in the changelog.

## Decided
- Royce: "merge #1412" — the only input this pass.

## Deferred
- Unchanged from the previous entry: eq-roles PR #28 still needs to rebase past v2.7.3 and cut v2.7.4; the ai-briefing/briefing-action fate decision is still running as its own session.

## Notes
- **Caught a mistake before it did damage:** first instinct was to archive the permission-hygiene `pending.md` section wholesale once its main items all read `[x]`, without reading past that point. It actually had its own `**Deferred:**` sub-block a few lines further down, holding 2 open items unrelated to this pass (the Rollback button, `eq_revoke_session`) that would have been silently archived as resolved alongside everything else. Re-read the full section before touching it; trimmed instead of archived — only what this pass actually closed came out of the live doc.

---

# Session 2026-08-16 (cont'd 16) — Closed the full cross-app cookie-sharing (CSRF) gap on eq-shell: 46 endpoints, 12 PRs, merged and live; corrected a 2-month-old misunderstanding about whether the protection was actually enforcing

## Built
- Full origin-guard (`checkShellOrigin()`) sweep across eq-shell `netlify/functions/`, closing the gap #1386 found beyond its own 7 fixed endpoints (45 more functions calling `readSessionCookie` + a mutation verb, or returning PII, with no origin check).
- Batch 1 (account/session security, 9 files): PR #1393 (confirm-totp, enroll-totp, set-phone-pin, set-recovery-email, reset-user-pin), PR #1394 (switch-tenant, shell-logout, delete-user, edit-user). Split into two PRs on Royce's explicit call rather than one 9-file PR.
- Batch 2 (worker/staff identity + credentials, 14 files): PR #1395 (invites), PR #1396 (staff/licence), PR #1398 (worker-linking/org-config).
- Batch 3 (business data, 16 of 17 files): PR #1401 (CRM/suppliers/labour-hire), PR #1402 (GM Reports), PR #1404 (intake-staging + push-document-audience.ts), PR #1405 (quotes/ops/briefing). `migration-reconcile.ts` dropped — confirmed read-only, no mutation, non-PII; a false positive in the original 45-file sweep list.
- Batch 4 (file uploads, 7 files): PR #1407 (attachment/document upload commit + single-request paths), PR #1409 (upload-gm-report, upload-tenant-logo, asset-calibration).
- `user-preferences.ts` (1 file, low severity but a real mutation — Royce's explicit "ship it" call): PR #1410.
- All 12 PRs squash-merged (Royce: "merge") and confirmed live via commit-ancestry check — a concurrent session's later merge (#1413) superseded the in-flight Netlify build from my own last merge, so verified via `git log` that my 12 commits are ancestors of whatever's actually serving, not by trusting deploy status alone. Matches the documented eq-shell-merge-equals-deploy "supersede" pattern.
- **Critical finding, post-merge**: checked `ENFORCE_IFRAME_ORIGIN` live via the Netlify env-var MCP (`manage-env-vars`/`getAllEnvVars`, site `a3473f83-7c82-4f1e-872d-aa96eaa55172`). It's set to `"true"` in the `production` context ONLY (no value in dev/branch-deploy/deploy-preview), set 2026-06-15 by `dev@eq.solutions` — **two months before this sweep, and before #1386 itself**. This means the guard has been hard-enforcing (403, not report-only) on core.eq.solutions the whole time, contradicting the "report-only, zero behavior change" framing in every one of today's 12 PR descriptions (and #1386's own). Resolves a standing 2026-07-26 pending item ("Royce to check the Netlify dashboard for ENFORCE_IFRAME_ORIGIN").
- Verified the sweep going live under real enforcement didn't break anything: (1) walked all 46 endpoints by batch — every one is called from Shell's own frontend (main bundle or same-origin lazy chunk), never from cards/field/service/sks-nsw-labour directly, so none match the cross-subdomain-caller risk shape `origin-check.ts`'s own comments describe (that risk applies to SSO/token-minting endpoints like `mint-supabase-jwt`, none of which this sweep touched); (2) `eq-context/digest.md`'s auto-refresh (fires on merge) came back clean 6 minutes after the last merge — no 403/CSRF-shaped errors, no volume spike, nothing named after today's files.
- Along the way: spotted PR #1400 touches `migration-reconcile.ts` (a Batch 3 file) at a different insertion point than the origin-guard change — confirmed no merge conflict risk, proceeded.
- Found + flagged (not fixed by me) a new occurrence of the recurring control-plane-drift CI block: `public.can_assign_worker_role` live on jvkn with no migration file, blocking #1402/#1404/#1405 (later also #1407) for a reason unrelated to their content. `spawn_task`'d it (task_e8fbcac6); a separate/concurrent session root-caused and fixed it same-day (PR #1408, added to KNOWN_UNSOURCED) before I circled back — dismiss attempt on the chip correctly reported "already started by the user."
- Consolidated eq-shell Claude Code memory early in the session (MEMORY.md index pruned from ~160 to ~138 lines; verified ~10 stale PR-number reminders against live `gh pr view` before removing, all confirmed already-merged weeks prior).

## Decided
- Split each >5-file tier into multiple sub-themed PRs rather than one large PR, matching this repo's own established norm (#590/#1244/#1377, #1386 itself shipped 7) — confirmed with Royce once via AskUserQuestion (Batch 1), applied consistently without re-asking for Batches 2-4.
- Ship `user-preferences.ts` as its own PR despite low severity (self-scoped, no PII, no cross-user impact) — Royce's explicit call via AskUserQuestion, "it IS a mutation, even if low-value."
- "merge" (Royce, one word) — read as authorization to merge all 12 PRs from this sweep, given it directly followed a summary listing exactly those 12 as awaiting his go. Merged in 3 batches of ~4, verifying each batch landed before the next, rather than firing all 12 blind.
- "you figure it out" (Royce, re: verifying the ENFORCE_IFRAME_ORIGIN production-enforcement finding) — investigated via the two methods described above rather than escalating further; did not flip the env var (that's a security-posture call, not mine to make unilaterally).

## Deferred
- Direct Netlify function-log spot-check for `[origin-check] BLOCKED` lines in the ~20 minutes after the last merge — no tool available this session can pull historical Netlify function logs, and Sentry MCP isn't authorized here. The two indirect checks (architecture + digest.md's Sentry pull) came back clean but aren't a substitute for the actual log lines.

## Notes
- **This repo squash-merges** (confirmed via `gh api repos/.../eq-shell` — `squash_merge_commit_title: COMMIT_OR_PR_TITLE` — and by inspecting a recent merge commit's message shape). `delete_branch_on_merge` is `false` at the repo-settings level, but branches were deleted anyway by passing `--delete-branch` explicitly on each `gh pr merge` call, matching observed convention from prior sessions' merges.
- **`gh pr merge` is silent on success** — "Bash completed with no output" is NOT evidence of failure; verify via a separate `gh pr view <n> --json state,mergedAt` call. Cost real time this session second-guessing 4 successful merges before realizing the "no output" was normal.
- **Structural variety found while placing the guard** (full detail in the `origin-guard-sweep-batches` Claude Code memory): most files are method-check-then-session-verify, guard goes right after the method check. Exceptions: (a) a few files check session/permission *before* the method check (`link-workers.ts`, `update-data-activation.ts`) — guard goes first, not "after the method-check," since the real invariant is "before the cookie is trusted for anything"; (b) GET+mutate dual-verb handlers — scope the guard to the mutating branch UNLESS GET itself returns PII (people/names/emails/signature images), then guard the whole handler (`org-join-notify-recipients.ts`, `ai-briefing.ts` despite being GET-only); (c) `push-document-audience.ts` is a 12-resource GET/POST handler where guarding the whole handler (rather than 9+ scattered scoped insertions) was the pragmatic call given how many of its resources are sensitive.
- **Two false-positive-adjacent findings in the original 45-file sweep list**, both caught by re-reading the actual file rather than trusting the list: `migration-reconcile.ts` (fully read-only, wrongly listed as a mutation) and `push-document-audience.ts` (genuinely a mutation, but its shape had ballooned from a simple single-purpose file to a 12-resource handler under a since-merged PR #1385, changing the right guard placement).
- Fresh-worktree gotcha hit repeatedly (11 separate worktrees created this session, one per sub-PR): `pnpm install` + `pnpm run build:packages` must run before `pnpm run build` will typecheck clean — `@eq/*` workspace `dist/` folders are gitignored, so a fresh worktree has none of them until built.
- Full batch-by-batch detail, PR links, and the complete "how to apply this pattern elsewhere" writeup live in the `origin-guard-sweep-batches` and `control-plane-drift-recurring` Claude Code memory files (`C:\Users\EQ\.claude\projects\C--Projects-eq-shell\memory\`), not duplicated here.

---

# Session 2026-08-16 — "Today's Actions" AI-briefing dashboard panel and its backend fully retired

## Built
- Investigated the open decision an earlier session's PR #1406 (dashboard-panel removal) deliberately left unmade: what to do with the two Netlify functions it orphaned (`ai-briefing.ts`, `briefing-action.ts`) and their `ai.view_briefing` permission wiring.
- Confirmed via live grep across eq-shell, eq-field, eq-solves-service, and sks-nsw-labour: zero callers anywhere outside the panel #1406 removes.
- Opened eq-shell PR #1411: deletes both files, fixes two stale references (a `permissions.test.ts` comment, a README function listing). Deliberately did NOT touch `permission-enforcement-baseline.json` in the first commit — `permission-enforcement-drift.test.ts` itself proved that would be premature while #1406 was still unmerged (`TenantHome.tsx`'s `canBriefing` was still a live call site).
- Merged #1406 (Royce's explicit "merge #1406"), confirmed live on core.eq.solutions via exact commit match on the production deploy, not just a green merge button.
- Merged #1411 (Royce's explicit "merge #1411"). Between opening and merging, an unrelated already-in-flight PR (#1405, an origin-guard security sweep) merged first and touched the same two files — turned a clean merge into a real modify/delete conflict. Resolved by keeping the deletion (the origin-guard addition was moot on a file being removed anyway), added the now-correct `ai.view_briefing` → `dead_keys` entry as a second commit on the same branch (only valid once #1406 had actually landed), re-ran the full suite (353/353 pass) and build clean, re-pushed, then merged. Confirmed live — 265s build, longer than #1406's since it rebuilt the whole app after everything else that had landed in between.
- One CI flake along the way: `gitleaks` failed once on a pure network error fetching its own binary (`curl: Connection reset by peer`), unrelated to any code — re-ran just that job rather than treating it as a real signal.
- Cleaned up: removed the worktree, deleted the local and remote `claude/retire-ai-briefing-endpoints` branch (confirmed merged via `gh` first, not assumed).

## Decided
- **Full retirement (delete), not rebuild-lighter or keep-dormant.** The one real counter-signal — `docs/ai-briefing-sprint.md`, a Royce-owned sprint plan from 2026-05-29 envisioning a bigger role-aware briefing across the whole suite — Royce confirmed live in-session is stale, not an active rebuild signal. Nearly all four of that plan's sprints had actually shipped over the following ~11 weeks before the load-time complaint killed the feature; the retirement isn't a verdict that the idea was wrong, just that nobody's continuing it.
- Merge order mattered and was followed: #1406 (frontend caller removal) before #1411 (backend deletion) — main called both endpoints from `TenantHome.tsx` until #1406 landed, so merging #1411 first would have 404'd the panel for every manager/supervisor in the gap.
- "merge #1406" / "merge #1411" (Royce, two separate explicit instructions) — each merge gated on its own go, not batched or assumed from the first.

## Deferred
- None — fully closed, no open thread left. Moved wholesale to `eq/pending-archive.md`.

## Notes
- **Re-check `mergeable`/`mergeStateStatus` immediately before merging, not just when a PR is opened.** #1411 looked clean when opened; by merge time an unrelated same-day PR had touched the same two files and merged first. This repo has enough concurrent sessions that "looked ready earlier" isn't the same as "ready now."
- **Squash-merged branches need `gh pr view --json state,mergedAt` to confirm the merge, then `git branch -D` (force) to delete locally** — `git branch -d` (safe delete) refuses squash-merged branches because git's ancestry check can't see them as merged even though they genuinely are. Same trap as the existing `squash-merge-branch-contains-trap` memory note, hit here from the delete side instead of the `--contains` side.
- `git merge` is hard-blocked by a repo hook in the shared local `eq-context` checkout (concurrent-session corruption risk, F9) — used an isolated clone in scratchpad for this close instead, per the hook's own suggested workaround.

---

# Session 2026-08-16 (cont'd 17) — Netlify function-log verification for the origin-guard sweep: the one deferred item from the earlier close, now resolved

## Built
- Found `netlify logs --source functions --since <ISO8601> --filter <site> --function <name...> --json` supports historical (not just live-tail) queries — not known to be available when this was deferred earlier the same session. Queried all 45 still-live functions from today's 46-endpoint origin-guard sweep (2 of the original list — `ai-briefing.ts`, `briefing-action.ts` — were deleted by PR #1411 later the same session, confirmed via `git log`/`gh pr view`, genuinely gone not a check gap) plus the original 7 from PR #1386, across the merge window and after (~10:00 UTC through query time).
- Result: zero `[origin-check]` log lines of any kind (neither `BLOCKED` nor `report-only`) and zero 403s, across every function checked. Closes the one item the earlier session-close card flagged as still open.

## Deferred
- None.

## Notes
- Full account-wide log fan-out (no `--function` filter) is unreliable for a site this size: one API call per function, latency climbs with concurrency, and a single failed sub-request silently discards the entire batch's output — confirmed empirically, not documented anywhere. Scoping to named functions in batches of ~10 worked cleanly. An invalid/retired function name anywhere in a `--function` list also fails the whole batch — same all-or-nothing behaviour — so retired functions need pre-filtering, not just inclusion-and-hope. Also: this CLI needs `--filter <site-name>` in a monorepo checkout or it drops into an interactive project picker that hangs forever in a non-interactive shell. Full detail in the `origin-guard-sweep-batches` Claude Code memory file.

---

# Session 2026-08-16 (cont'd 11) — review-needed email for auto-joined workers, built + shipped; caught an earlier fix that was merged but never actually deployed

*Direct continuation of the same live-testing session (cont'd 9/10, above). Royce asked for one more thing after everything else closed out: "can we setup an email to the nominated tenant contact (settings) to say there are licenses that need to be reviewed? It works for brand new cards users" — i.e. make the existing notification also cover the case found earlier this session (a worker landing on the roster with nobody ever told).*

## Built
- Traced the existing "it works for brand new Cards users" mechanism precisely before building anything: a genuine connect request inserts a row into `public.org_access_requests`, which fires an existing trigger, which calls an existing RPC to resolve who to notify (respecting each tenant's nominated recipients from Settings), which calls an existing edge function that sends the email via Resend. Confirmed live that the worker from earlier's connect never went through this table at all — SKS accepts applications automatically, so the worker lands straight on the roster with nothing for this pipeline to ever fire on.
- Reused all of it rather than building a second notification system: `workers-canonical-sync` (eq-shell) now also inserts an already-resolved row into that same table when a genuinely new person lands via the auto-accept path, which fires the exact same trigger → RPC → email, to the exact same nominated recipients. Only two things needed to change: the trigger now passes the row's status through to the edge function (one new field in a payload), and the edge function picks different wording when that status means "already joined" rather than "asking to join" — same recipients, same review link, same styling either way.
- Confirmed the design choice was safe before writing it: the row is inserted already resolved, so it never shows up as something an admin has to action in the existing pending-requests list — it's purely a heads-up, not a second decision queue.
- Explicitly did NOT dry-run test this one the way everything else this session was tested — the trigger it depends on queues a real outbound email, and that queuing can survive a rolled-back test transaction, so rehearsing it for real risked actually sending a live email to real admins. Told Royce this plainly rather than testing it anyway; verified by careful code reading instead. Royce: comfortable merging on that basis.
- eq-cards [PR #256](https://github.com/eq-solutions/eq-cards/pull/256) + eq-shell [PR #1414](https://github.com/eq-solutions/eq-shell/pull/1414), both CI-green, both merged.
- **Caught while deploying, not by anyone reporting a symptom:** pulled the live copy of `workers-canonical-sync` before redeploying it with the new insert, and found it still had this morning's already-merged blank-name bug in it — the earlier PR (#1413) had been merged into the git repo, but nothing had ever actually pushed that code to the real, running function. Netlify auto-deploys the web app on every merge to this repo; the database functions living alongside it don't move on their own — that step has to happen on purpose, and it never did. Practical effect: the "fixed" Staff-list name bug from earlier today had actually still been broken in production the entire time since. Corrected by deploying the current, correct code — both this morning's fix and today's new feature went live in the same deploy.
- Applied the trigger migration and deployed both edge functions; confirmed each one live afterward rather than trusting the deploy call's own success message.

## Decided
- Royce: "Merge on the code review, I'm comfortable with it" — explicit go to merge without the live-fire test, after being told plainly why that test couldn't safely be run.

## Deferred
- None new.

## Notes
- **A merge is not a deploy for everything in this repo, only some of it.** The web app (Netlify) ships itself the moment a PR merges — repeated, verified fact this whole session. The database-side functions living in the same repo do not — they need their own explicit deploy step regardless of merge state, and nothing here currently makes that obvious or automatic. Worth remembering for every future eq-shell (and eq-cards) change that touches an edge function: after merging, check whether the *live* function actually matches the *repo's* code before calling the work done — a green merge is not proof of a live fix. This is exactly the mistake this session's own earlier "merged, live" report made about the same function a few hours before catching it.
- The two products' notification pipelines turned out to share almost everything already: one recipient list, one trigger, one RPC, one edge function, both directions of the existing "someone wants to connect" email. The only genuinely new code across both repos was a status field threaded through a payload and a second wording branch — proof that finding and reusing the existing mechanism first (rather than assuming a new notification needs a new pipeline) kept this small.
- Full blow-by-blow (including the exact conflict-resolution steps and the drafted `dead_keys` entry text) lives in the `ai-briefing-retirement-2026-08-16` Claude Code memory file (`C:\Users\EQ\.claude\projects\C--Projects-eq-shell\memory\`), not duplicated here.
