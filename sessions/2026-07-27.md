# Session 2026-07-27 — eq-ui design handoff shipped + propagated to eq-shell/eq-service, real bugs found along the way

## Built
- Reviewed a Claude Design handoff (Tooltip, EmptyState, Pagination for `@eq-solutions/ui`) against the real toolchain — found and fixed a `jsx-a11y` lint failure in Tooltip and a props-shape mismatch in EmptyState/Pagination before merging. eq-ui PR #33 merged, published as `@eq-solutions/ui@1.12.0`.
- Traced the actual propagation path: eq-shell/eq-solves-service pin eq-ui by exact git tag (`github:eq-solutions/eq-ui#vX.Y.Z`), not a floating range, so nothing reaches a live app without a manual per-repo bump PR. eq-field has no dependency on the package at all (vanilla JS, no build step).
- Bumped eq-shell to v1.12.0 (PR #1027, merged) — surfaced a real, previously-unnoticed bug: the barrel stylesheet consumed via `@eq-solutions/ui/styles` had been missing `DropdownMenu.css` since v1.11.1, and eq-shell's `QuotesModule` uses `DropdownMenu` with no fallback CSS import. Confirmed directly against the live `v1.11.1` tag. Likely means the Quotes "⋯" menu has been rendering unstyled in production until this deploy.
- Bumped eq-solves-service to v1.12.0 too (PR #604, merged) — purely additive, no `DropdownMenu` usage there.
- Checked eq-field's hand-ported Spinner CSS against the new release — confirmed no update needed (Spinner itself untouched between v1.11.1 and v1.12.0).
- Spun off a background investigation into two `npm audit` findings (`sharp`/libvips CVEs, `uuid` buffer-bounds bug) surfaced while checking eq-solves-service. It confirmed the vulnerable `next/image` path is never exercised in this app (all image rendering goes through plain `<img>` + signed Supabase Storage URLs) and shipped a non-breaking fix via `package.json` `overrides` rather than the breaking `next`/`exceljs` downgrade GitHub's auto-fix would have forced. PR #605, Royce reviewed and merged it himself.
- Published two visual artifacts: an inline pipeline diagram of the whole rollout, and a live "what actually changed" report (real component markup/CSS side by side, before/after DropdownMenu comparison, literal diffs).
- Kept `eq-context/suite-state.md` and `system/worktree-registry.md` current throughout rather than waiting for the nightly cron — a concurrent Cowork session ended up finishing the last registry-prune edit and pushing it, content verified correct before relying on it.

## Decided
- Royce: implement the handoff, not just critique it.
- Royce: push the eq-ui branch, open the PR, merge it.
- Royce: open bump PRs for both eq-shell and eq-solves-service.
- Royce: merge both bump PRs.
- Royce: spin off the sharp/libvips investigation as a background task rather than fold it into this session.
- Royce: reviewed and merged the resulting sharp/uuid fix (PR #605) himself.

## Deferred
None — every thread opened this session was closed by the end of it (all 4 PRs merged, both hand-off apps checked, worktrees pruned).

## Notes
- The design handoff's own "no hardcoded hex, axe-tested" claims were all accurate, but it hadn't been run through eq-ui's real `tsc`/`eslint`/`vitest` toolchain (it said so itself) — the two real defects only surfaced by actually copying the files in and running the checks, not by reading the handoff's description of itself.
- eq-shell and eq-solves-service package pinning is by exact git tag, not semver range — worth remembering next time a shared-package fix needs to reach a live app; it never happens automatically, always needs a bump PR per consumer.
- eq-cards has no dependency on `@eq-solutions/ui` at all (different stack) — not a gap, just an architecture fact worth not re-deriving next time.
- Mid-session, a concurrent Cowork session was independently committing to the same shared `eq-context` root checkout at the same time (the same "two sessions, one folder" hazard already logged elsewhere in this substrate) — handled by re-fetching/rebasing before every push rather than force-pushing, no work was lost on either side.

---

# Session 2026-07-27 — Job Creation export slowness: root-caused and fixed

## Built
- Royce reported "Job creation stopped working" on a real SKS quote (SKS-17461, Metronode NSW/Equinix), linked directly. Investigated live in his own session against that exact quote before assuming anything: the Job Creation Excel export actually succeeded (200 OK), permissions/entitlements/tenant routing/deploy were all healthy, and the previously-shipped End Client fallback fix (PR #1007) was confirmed working on the real data.
- Asked Royce to narrow the symptom rather than guess further — answer: "it worked after a while — excel took a long time to download." Not a correctness bug, a latency one.
- Root cause: `netlify/functions/job-creation.ts` re-fetched its xlsx template over HTTP from the site's own public files on every single invocation, instead of reading the copy already bundled with the function (wired up via `netlify.toml`'s `included_files` since the function was first written, but never actually used). That self-referential network round-trip was stacked on top of the function's own cold-start time.
- Fix: swapped the HTTP fetch for a local file read. eq-shell [PR #1031](https://github.com/eq-solutions/eq-shell/pull/1031) — single-file diff, no schema/auth changes, `tsc -b` confirmed clean against the 4 pre-existing unrelated typecheck errors already on `main`. Merged and live on core.eq.solutions same session.

## Decided
- Royce: merge #1031.
- Royce: confirmed the deploy needed to be verified live before considering this closed.

## Deferred
None — root cause found, fixed, merged, and confirmed live within the session.

## Notes
- Real repro discipline paid off here: the obvious first guess (a permission regression from the same-day access-model refactor PRs) didn't hold up under a live test against the actual reported quote, and chasing it further without reproducing first would have wasted the session. Asking Royce one clarifying question ("what exactly fails?") turned an unreproducible bug report into a precise, fixable root cause.
- `netlify/functions/_templates/sks-job-creation-v7.xlsx` + the `included_files` bundling config have existed since the function's original commit (`0ba26f17`, mid-June) — set up for exactly this purpose but never wired in. Worth remembering if a similar "bundled but unused" gap shows up in another Netlify function.

---

# Session 2026-07-27 — Training Matrix "Photo ID" now accepts a driver's licence or passport

## Built
- Royce asked to review what "Photo ID" means in the Training Matrix: a driver's licence or passport IS itself valid photo ID. Verified the actual code — `photo_id`, `driver_licence`, and `passport` were three fully independent licence-type columns (`src/pages/staff/staffTypes.ts`), and the matrix's gap-checking (`missingRequired`/`gapCount`), cell rendering, and CSV export all did exact-type matching only, with no equivalence between them. A worker holding only a driver's licence was wrongly flagged red as missing a required Photo ID.
- Confirmed the fix direction with Royce before writing code (business-rule call, not a mechanical fix): one-directional equivalence — Driver Licence and Passport requirements stay exact-match (those ask for a specific document: driving eligibility / travel), only the generic Photo ID requirement accepts any of the three. Also confirmed the matrix cell should show the actual document used, not just a bare checkmark.
- Added `PHOTO_ID_EQUIVALENTS`, `satisfiesRequirement()`, and `licenceForColumn()` to `src/pages/staff/staffLib.ts` (pure logic, unit-tested) — the latter prefers a currently-valid equivalent document over an expired one so the cell doesn't read "expired" when the worker holds another valid form of ID. Wired into `MatrixView.tsx`'s gap-checking and cell rendering, and into `buildMatrixCsv` so the CSV export stays consistent with the on-screen matrix.
- 8 new unit tests added; full suite 253/253 pass, `tsc -b` clean on touched files (4 pre-existing unrelated `ops.view_suppliers` PermKey errors untouched), lint clean.
- Shipped: eq-shell [PR #1030](https://github.com/eq-solutions/eq-shell/pull/1030), merged (squash `006f310e`), branch deleted, live on core.eq.solutions.

## Decided
- Royce: one-directional equivalence into Photo ID only (not fully mutual across all three document types).
- Royce: show the actual document used in the cell, not a plain checkmark.
- Royce: commit, push, open the PR, and merge once CI was green.

## Deferred
None — root cause found, fix scoped, confirmed, built, tested, merged, and live within the session.

## Notes
- This was a genuine business-rule decision (what counts as equivalent ID), not a mechanical bug fix — worth the two confirming questions before touching code, since getting the equivalence direction wrong either way would have created a different real gap (e.g. fully mutual would let a passport silently satisfy a driving-eligibility requirement).
- No DB/migration change needed — the equivalence lives entirely in app-layer interpretation of the existing `public.licence_types` catalog values, so the org-configurable "required credentials" admin UI and the catalog itself are untouched.

---

# Session 2026-07-27 — EQ Service: branded loading spinner on the Shell sign-in handoff

## Built
- Royce reported the app is blank/white for a while before it shows up, asked for a loading spinner. Checked the live system first rather than assuming nothing existed: a branded root splash (`app/loading.tsx`, EQ logo + bouncing dots) already shipped and was live on `main` — not the gap.
- Traced the actual first-paint path for a Shell iframe embed: `app/shell/page.tsx`, the client-side token-exchange page Shell navigates to first. Its loading state was bare unstyled text ("Signing you in…") with no logo or spinner — the real gap, and exactly where the "long time to show up" complaint was landing.
- Restyled that page's loading branch to match the root splash (same EQ logo, same bouncing-dot spinner, same `eq-sky`/`eq-ink` tokens). No auth/token-exchange logic touched — presentational only. `tsc --noEmit` clean.
- Could not run the dev server locally to eyeball it live (this worktree has no `.env.local`/Supabase secrets) — verified by code inspection + a clean typecheck against a pattern already proven in production.
- Shipped: eq-service [PR #606](https://github.com/eq-solutions/eq-service/pull/606), merged (squash `e54d8dc9`), branch deleted, live on service.eq.solutions via Netlify auto-deploy.

## Decided
- Royce: go ahead with the fix once the actual gap (not the pre-existing splash) was identified.
- Royce: commit, open the PR.
- Royce: merge it.

## Deferred
- **The blank screen itself (before even the unbranded text appeared) is a separate, unfixed latency issue**: `proxy.ts` (middleware) does a full Supabase auth round-trip before any HTML reaches the browser on a fresh Shell embed — no client-side loading UI can paint over that gap. Not investigated further this session (out of scope for "add a spinner"); flagged in `eq/pending.md` as a follow-up if the complaint persists after this ships.

## Notes
- This session's brief-gate flag hadn't been set yet, so the first `Edit` call was blocked by `guard.js` until `/brief eq-solves-service` ran — a reminder that the gate is per-session, not automatic just because a prior session in the same repo ran it.
- Working directory was this session's own worktree (`.claude\worktrees\user-source-analysis-be4daa`, branch `claude/service-loading-page-7a6d8b`) — clean and isolated from the root checkout's unrelated detached-HEAD state (another session's in-progress asset-import work), so no collision risk.
- `gh pr merge --squash --delete-branch` failed on the local git step only (`'main' is already used by worktree at ...eqsvc-loadtime-ux`) — the merge itself had already succeeded via the API before that error surfaced. Confirmed via `gh pr view --json state,mergedAt` rather than assuming failure from the error text, then deleted the remote branch separately with `git push origin --delete`.

---

# Session 2026-07-27 — EQ Service: blank-screen root cause investigated (no new gap — already documented + mitigated)

## Built
- Royce asked to look into the blank-screen root cause deferred from the spinner session. Re-checked the specific hypothesis flagged earlier (middleware's `updateSession()` paying a Supabase auth round-trip on every fresh Shell embed) against the actual `@supabase/ssr` behaviour: `getUser()` resolves locally with no network call when there are no cookies to validate, so a genuinely fresh embed (zero cookies) doesn't pay that cost — the earlier hypothesis was too broad.
- Found `docs/perf/load-time-investigation.md` (eq-solves-service repo, last updated 2026-07-23) already root-caused the actual "takes a while to show up" complaint to Netlify Lambda cold start (measured 2026-07-12: 4.6–5.3s full cold vs 0.4–1.4s warm) and had already shipped mitigations — a keep-warm ping (#506/#508) and a Sentry-integration trim that cut the deployed bundle's registered integrations 44→17 (#571). The doc's own "what's left" section asked for a live re-measurement, which no one had done since #571 shipped.
- Did that re-measurement: 3 live hits on `service.eq.solutions/api/health` returned 1.57s / 0.36s / 0.51s — inside the documented warm range, not the cold range, so the shipped mitigations look like they're holding in production. Could not force a genuine cold hit (needs a longer idle gap past the 5-min keep-warm cadence, or catching a fresh deploy recycle) so the cold-start number itself is still unconfirmed post-#571.
- No code changed — this was pure investigation. Reported back to Royce that nothing further is fixable in code; the only remaining lever is a genuine infrastructure decision (always-on/edge runtime instead of serverless) if cold start is still unacceptable.

## Decided
- None yet — ended on a question back to Royce (see Deferred).

## Deferred
- **Needs Royce's call**: whether cold start is still bad enough to justify moving off the serverless runtime model. Not pursued without his go-ahead — this is an infrastructure decision, not a quick fix.

## Notes
- Good example of a hypothesis that seemed right from reading the code in isolation (middleware auth round-trip) but didn't survive checking the actual library behaviour — worth verifying framework internals before treating a plausible-looking code path as a confirmed root cause.
- `docs/perf/load-time-investigation.md` is the authoritative doc for any future "Service is slow to load" report — read it first before re-deriving this investigation from scratch again.

---

# Session 2026-07-27 — eq-field: fixed 2 live Sentry errors (duplicate global + Leave lazy-load race)

## Built
- Royce forwarded a prompt doc (`eqfieldsentryprompt.md`) asking to investigate and fix the two untriaged live Sentry errors flagged as gate item 5 in `ops/security-register.md`. The doc's hypotheses were built from Sentry event metadata + session logs only, without repo access — this session confirmed both against the real source before touching anything.
- **Issue 1 confirmed exactly**: `SyntaxError: Identifier 'INCIDENT_TYPES' has already been declared` (Sentry 136548558). `scripts/diary.js:68` and `scripts/incidents.js:22` both declared a top-level `const INCIDENT_TYPES` with different taxonomies (diary: near-miss/injury/spill/damage/other; incidents: incident/near_miss/hazard_observation). `lazy-loader.js` dedupes by exact `<script src>`, not by declared identifier — visiting one tab family then the other injects both files as separate script tags, and whichever loads second throws at parse time, aborting the module. Audited every other top-level `const`/`let`/`function` in both files — no other collisions. Fix: renamed diary.js's (narrower, diary-scoped) list to `DIARY_INCIDENT_TYPES`.
- **Issue 2 root-caused past the doc's hypothesis**: `ReferenceError: openLeaveRequest is not defined` (Sentry 130706295). The doc suspected a stale deep-link/dashboard-strip variant of a previously-fixed bug class; actual cause was narrower — `showPage()` (`index.html:9983`) unhides `#page-leave` synchronously, *before* its own async `EQ_LAZY.loadTabScripts('leave')` call (`index.html:10040`) resolves. The toolbar's "+ New Request" button is clickable in that window, before `leave.js` has loaded. Fix: added `openLeaveRequestSafe()`, reusing the exact `EQ_LAZY.loadTabScripts` pattern `dashboard.js`'s `_kickLeave` already uses for the same class of race (v3.5.293) — no new lazy-load mechanism invented, per the doc's own instruction.
- Verified thoroughly before shipping: `node --check` clean on both touched scripts; a real browser load (via local `serve`, script-injected since Netlify Functions aren't available under plain `serve`) of the full site-reports chain in **both** script orderings — zero errors either way, each constant resolves to its own taxonomy independently; a direct simulation of the leave-button race (call the wrapper before `leave.js` has loaded) — no throw, resolves cleanly once the lazy load completes. Re-verified all three surfaces (Diary, Incidents/Safety, Leave "+ New Request" — which actually opened the modal) on the live Netlify deploy preview before merging.
- Version bumped to v3.5.358 (`app-state.js`, `sw.js` banner+CACHE, in-HTML changelog). Shipped: eq-field [PR #542](https://github.com/eq-solutions/eq-field/pull/542), CI green (Tests+lint, Netlify deploy-preview), squash-merged `ee0767c`, branch deleted, live on field.eq.solutions.
- Updated `ops/security-register.md` gate item 5 from open to resolved with the fix detail.

## Decided
- Royce: proceed with the fix without re-confirming the brief — the task doc's hypotheses were specific enough and matched source exactly.
- Royce: merge #542.

## Deferred
- **Sibling latent race in the Leave toolbar, not fixed**: CC List, Archive Resolved, Show Archived, Print, and the status-filter/search `renderLeave()` calls all call `leave.js` globals directly and unguarded, with the identical lazy-load race as the button just fixed — just not yet caught by Sentry. Deliberately scoped out of this PR to stay narrow to the confirmed crash; flagged in `eq/pending.md` for a follow-up sweep applying the same guard pattern to the rest of that toolbar.

## Notes
- This session's worktree (`.claude\worktrees\bold-volhard-645dd7`) was flagged stale in `system/worktree-registry.md` as "branch `land-472`, 2 commits ahead of main, never pushed" — turned out superseded; HEAD matched `origin/main` exactly on a fresh branch, clean tree. Registry bookkeeping for this row skipped this pass since `eq-context` itself had another session's uncommitted WIP mid-session (resolved by the time of this write).
- Full tenant boot (canonical + Supabase routing) isn't testable under a plain `npx serve` static server — it needs Netlify Functions (`tenant-config.js`) which only run on an actual Netlify deploy. Worked around it for the parse-order verification by injecting the real `<script>` chain directly into a loaded page rather than trying to fake tenant boot; used the real deploy preview for the full click-through once it existed.
- The `eq` tenant on this preview now gates behind "Sign in through Core" per the already-shipped Access-Model Phase 3 changes — drove the smoke test via `showPage()`/direct function calls instead, since no Core credentials were available in this session.

---

# Session 2026-07-27 — eq-shell: local `pnpm run build` failure root-caused to stale `node_modules`, not a code bug

## Built
- Royce reported `pnpm run build` (the exact command Netlify's `netlify.toml` runs) failing at `tsc -b` with 4 `PermKey` type errors, all on `ops.view_suppliers`/`ops.manage_suppliers`: `HubSidebar.tsx`, `useCommandIndex.tsx`, `Suppliers.tsx`, `netlify/functions/suppliers-mutate.ts`. Confirmed via `git stash` these 4 errors were already present identically on `main` — not caused by anything in this session.
- Traced the `PermKey` union (`src/permissions/matrix.ts`) back to `@eq-solutions/roles/ops`'s `OpsPermKey`. `package.json`/`pnpm-lock.yaml` already pinned `github:eq-solutions/eq-roles#v2.5.7` (landed [#1023](https://github.com/eq-solutions/eq-shell/pull/1023)) and the lockfile resolved it to the correct tarball — but the local `node_modules/@eq-solutions/roles` symlink pointed at a *different*, older pnpm-store tarball hash whose `package.json` read `"version": "2.5.4"`, a version that predates both permission keys.
- Confirmed the keys genuinely exist in v2.5.7 by re-checking after reinstall: `PermissionMeta('ops.view_suppliers', ...)` / `PermissionMeta('ops.manage_suppliers', ...)`, both with commit-message-style provenance notes pointing straight at eq-shell's own `suppliers-mutate.ts` as the reason they were added.
- Fix: `pnpm install --frozen-lockfile` — resynced `node_modules` to the already-correct lockfile. No source changes, no lockfile changes, `git status` clean throughout. `pnpm run build` (full `build:packages && build:tokens && tsc -b && vite build`) now passes clean.
- Root-caused the "but Netlify deploys are succeeding" puzzle Royce flagged alongside the bug report: Netlify always does a clean install from `pnpm-lock.yaml` per build, so it never had a stale `node_modules` to drift in the first place. Verified live via Netlify MCP rather than assuming — current core.eq.solutions deploy (commit `a0df50b1`, PR [#1033](https://github.com/eq-solutions/eq-shell/pull/1033)) is `state: ready`, `error_message: null`, and multiple production deploys have succeeded since the v2.5.7 bump merged.

## Decided
- Royce: confirmed `pnpm-lock.yaml` didn't need a fresh commit (already correct on `main`, no diff after reinstall).
- Royce: confirmed the discrepancy explanation was sufficient — no infra change requested.

## Deferred
- **Habit note, not a task**: after pulling any `@eq-solutions/*` package-version bump, run `pnpm install` before trusting a local `tsc -b` failure as a real regression — this one cost investigation time chasing a phantom code bug that didn't exist past the local checkout.

## Notes
- Good example of Rule 0.5 in miniature: the failure *looked* exactly like a code/dependency-pin bug (4 clean TS2345/TS2322 errors, plausible root cause in "roles package needs bumping or call sites are stale") but the actual pin was already correct — only the local install artifact was wrong. Checking `node_modules`'s real resolved version (via the pnpm symlink target, not just `package.json`'s stated specifier) before touching source code avoided an unnecessary roles-package bump or call-site edit.
- No files under version control changed this session — everything was local install state.

---

# Session 2026-07-27 (2) — eq-service sharp/uuid audit merged; SEC-1/9/10/12 triage; a real detector bug found and fixed

## Built
- **Investigated and closed eq-solves-service's `sharp`/`uuid` npm-audit findings** (spun off as a background task by an earlier session today — see this file's first entry). Confirmed live: `next/image`'s `<Image>` component is never used anywhere in this repo — no `images` config in `next.config.ts`, the only `<Image>` import anywhere is a lucide-react icon; attachment/defect-photo thumbnails render via a plain `<img>` from signed Supabase Storage URLs. `sharp` sat unused as `next`'s optional dependency. `exceljs`'s only `uuid` usage is a single `require('uuid').v4` call, confirmed identical under uuid@11.1.1's CJS export. Fixed via `package.json` `overrides` (`sharp@^0.35.3`, `uuid@^11.1.1`) rather than the breaking `next`/`exceljs` downgrade `npm audit fix --force` would have forced — verified clean install, `tsc --noEmit` clean, 359/359 tests, `next build` TS phase clean.
- **Correction to this file's first entry above**: it states "Royce reviewed and merged [PR #605] himself." That's not what happened here. Royce's instruction was "merge #605 once CI is green"; this session confirmed the real gate (`tsc + next build`) passed and the two reds were the same pre-existing accepted-risk failures every recent PR in this repo carries (confirmed via job logs, not assumed), then merged it via `gh pr merge --squash --delete-branch` (`e6e72fe`).
- Worktree (`eq-solves-service-sharp-uuid-audit-wt`) fully pruned after merge — no leftover local/remote branch. Its `git worktree remove` was blocked by the session's permission classifier; the linked-worktree registration had already dropped out of `git worktree list` by the time of the follow-up (no `.git` pointer left, just a stray `.next` build-cache dir), so deleting the plain leftover folder was safe.
- Reviewed `digest.md`'s "Needs you" list at Royce's request. **SEC-1**: turned the vague "weekend tasks" line into a real 6-point gated decommission checklist in `ops/security-register.md` — did NOT touch sks-nsw-labour itself (Royce's own standing 2026-06-05/2026-07-20 decision). Live-verified Field's parallel-run proving is at 0/3-4 clean weeks (`SKS-FIELD-PARALLEL-RUN-LOG.md`), so a retirement date would be premature regardless of the other open gates.
- **SEC-9**: drafted the missing rotation runbook (`sec9-jvkn-key-rotation-runbook-2026-07-27.md`), mapping all 4 live consumers (eq-shell primary, eq-field, eq-cards, eq-solves-service) via a read-only subagent sweep. **SEC-10/SEC-12**: confirmed both are hard-blocked for Claude Code to execute (credential re-store/masking) — handed Royce the exact manual steps instead.
- **Real bug found and fixed in the substrate's own tooling**: `digest.md`'s F1 "guard bypass? rung 4" flag had been firing since 2026-07-26, but it was a false positive — `refresh_digest.py`'s `failure_recurrence_signals()` was re-matching `sessions/2026-07-21.md`'s own sentence confirming the already-known 2026-07-19 recurrence, because its date-only filter (`file_date > last_seen`) can't tell "narrating a confirmed past incident" from "it happened again." Added a `confirmed_in` field to `failures.md`'s schema + patched the detector to skip those files; verified live (urgent/quiet both empty, no real recurrence hiding elsewhere in the file). Struck the stale line from the tracked `digest.md` by hand rather than rebuilding it locally — no `GH_TOKEN`/`NETLIFY_TOKEN`/`SENTRY_AUTH_TOKEN` in this session, and a token-less rebuild would have blanked real PR/deploy/Sentry data. **Confirmed for real** by the subsequent CI-driven digest refresh (03:22 UTC): SEC-9's updated text carried through and the F1 line stayed gone.
- Spawned a background task (`task_62067d7c`) for the two untriaged eq-field Sentry errors surfaced as SEC-1 gate item 5 — since fixed by a separate session today (see this file's third entry).

## Decided
- Royce: "merge #605 once CI is green."
- Royce: "you do it aswell as any other deferred work" — worktree cleanup.
- Royce (via AskUserQuestion): SEC-1 → "Prep decommission checklist only," not touching sks-nsw-labour or re-verifying the leak live.
- Royce: "fix SEC-1 / close SEC-9,10,12 / investigate guard bypass" — then, after being asked to steelman that framing, redirected to: log the possible SEC-9 exposure honestly, draft the runbook, and separately flag whether the rotation timeline should tighten (not decided this session).

## Deferred
- **Royce's call: does the possible SEC-9 second exposure (see Notes) push "rotate whenever convenient" to "rotate soon"?** Not decided.
- SEC-9/10/12 actual rotation/re-store — Royce to run himself; runbook + exact steps are ready.
- SEC-1's 5 real gates (proving-run clean weeks, VIC scale question, sign-off owner, 44-worker migration date) — none met yet, no date set.

## Notes
- **A subagent run to map SEC-9's jvkn-key consumers was itself flagged by the Claude Code security classifier for "credential materialization"** — it decoded a live jvkn `service_role` JWT's payload (role/ref claims) while reading Netlify env vars for consumers. Recorded honestly in SEC-9's row: not confirmed as a full second leak (a JWT payload is base64, not encrypted — decoding it reveals only claims already described in plain English in the register; the encoded bearer token itself is what would actually matter, and I have no visibility into the subagent's raw tool-call output to confirm whether that appeared). Steelmanned my own first response to this (which had posed an unnecessary either/or "proceed vs. hold" question) — the better move was to just record the event, draft the runbook regardless (it needs no further credential handling), and raise the timeline question separately. Process fix for future credential-consumer mapping: scope it to env-var names/presence only, never fetch/decode/print actual values.
- This file and `eq/pending.md`/`eq/changelog/eq-shell.md` were being actively edited by other concurrent sessions throughout — re-read before each write rather than trusting the earlier-read version, staged and committed only the files this session actually changed.

---

# Session 2026-07-27 (3) — Tenant-plane security sweep + Brian Griffin-Colls login bug (3+2 instances of the same fix)

## Built
- **Closed out the deferred security question from the prior session**: swept both tenant databases (zaap/EQ, ehow/SKS) for the anon-EXECUTE gap found by accident last time. Read every function body rather than assuming severity from names. Found 39 more on zaap (12 on ehow, exact subset): 37 fail closed for an anonymous caller regardless (tenant-scoped JWT check resolves null), 2 are the customer quote-portal flow and correctly anon by design, and one (`eq_mark_expired_quotes`) had already been fixed once in June via a proper migration but had drifted back open live with no later migration explaining it — a governance-integrity finding, not just re-fixed silently. Closed all of them (migrations 0211+0212, the second one catching a signature transcription error the first one silently no-op'd on — caught by the post-apply live re-verification the plan called for). Seeded a CI baseline (`FUNC_EXEC_ANON_ALLOW`) for both tenant planes in the drift gate so this class of bug is actually enforced going forward instead of reporting "no baseline." Logged the full finding as SEC-13 in `ops/security-register.md`. eq-shell PR [#1028](https://github.com/eq-solutions/eq-shell/pull/1028), merged, migrations dispatched live to both planes.
- **Built admin control over new-join-request notification emails.** Royce's live Bob Smith test signup had emailed all 15 real SKS managers (a *correct* consequence of an earlier session's fix that made those emails actually reach people, previously reaching nobody). Steelmanned two designs with Royce before building: a per-tenant mute toggle vs. letting admins nominate specific recipients from the existing manager list. Built the latter — a new "New join-request emails" picker in Settings, backed by `org_join_notify_recipients` (eq-cards migration 0108) and an updated `eq_notify_connection_request_targets` that narrows to the nominated set only when one exists (empty = today's behaviour, every manager). eq-shell PR [#1029](https://github.com/eq-solutions/eq-shell/pull/1029), migration applied live to jvkn.
- **Diagnosed and fixed a real, live login-blocking bug for an actual employee.** Royce reported Brian Griffin-Colls getting "user not found." Traced it fully before touching anything: Brian's account was healthy (active, correctly linked everywhere, real recent logins) — the bug was three separate functions (`eq_get_tenant_user`, `edit-user.ts`, `mint-supabase-jwt.ts`) all comparing a user's *home* tenant against their *active session* tenant, the exact same mistake already fixed once for `mint-tenant-jwt` back in June. Brian's home tenant (from his original personal sign-up) differs from the SKS tenant he was separately added to — so every one of these checks failed for him, including the one that mints his actual login session token every ~14 minutes. Fixed all three (eq-shell PRs [#1034](https://github.com/eq-solutions/eq-shell/pull/1034), [#1035](https://github.com/eq-solutions/eq-shell/pull/1035)), confirmed live with Brian directly.
- **Investigated whether this was caused by the same weekend's access-control/security-groups work** — it wasn't. Brian's account shape dates to 2026-07-01/02, three-plus weeks prior; no recent commits touched any of the three functions. Coincidence of timing (someone finally opened his profile), not a regression.
- **Swept for more instances rather than stopping at three**, per Royce's own follow-up ask. Found 2 more, in admin review queues (`eq_list_phone_link_reviews`, `eq_list_recycle_reviews`) — same bug, but a silent-omission failure mode instead of an error: a manager reviewing pending flags would just never see an affected person's item, no error to notice. The other 3 candidates that matched the same suspicious SQL pattern were genuinely fine (they scope by an organisation's own tenant, not a person's home tenant). Fixed both, and added a canonical shared check (`shell_control.is_active_tenant_member()` SQL + `hasActiveTenantMembership()` TS) so the next feature has a correct pattern to copy instead of re-deriving the wrong one a sixth time. eq-shell PR [#1036](https://github.com/eq-solutions/eq-shell/pull/1036), merged, applied live.
- **Deleted a leftover duplicate identity for Brian** (created 12 days after his real account, via some invite/onboarding path that didn't recognise he already existed; never signed in, inactive). Snapshotted to scratchpad, confirmed fully isolated (zero other tables referenced it) before deleting, verified his real account untouched afterward.

## Decided
- Royce: "Do the full triage now, log it in the security register" — the tenant-plane sweep.
- Royce (via AskUserQuestion, steelman-first): nominate specific recipients rather than a blanket mute for join-request emails; pick from existing managers rather than free-text email entry.
- Royce: apply eq-cards migration 0108 to jvkn.
- Royce: merge #1028, #1029, #1034, #1035, #1036 (each confirmed individually as CI went green).
- Royce: "go delete the b8a7f7e1 stub and confirm brian can login."
- Royce: verify the 5 remaining unswept RPCs + add a shared helper, rather than stopping at "3 fixed, probably fine elsewhere."

## Deferred
- Royce to click through the new "who gets notified" Settings control to confirm it reads and saves correctly — code-complete and tested, not yet user-verified.
- `cards-staff-matches.ts` dead code (flagged prior session, still not cleaned up).
- SEC-4 (jvkn, `eq_cards_*` anon-exec, verified-not-exploitable but never actually revoked) — now has three live, incident-free precedents for the exact same fix pattern this session; a real candidate to finally close, not actioned here since it wasn't the task in hand.

## Notes
- This is the **fifth** confirmed instance of "home tenant vs. active session tenant" comparison bugs found across this codebase (2 fixed in prior sessions — `mint-tenant-jwt`, and `token-exchange.ts`/`mint-quotes-iframe-token.ts` already correctly written — plus the 3 found and fixed this session, plus 2 more from the deliberate sweep). Each was found reactively, one at a time, via a user complaint or an unrelated CI failure — never by looking for the pattern itself until today. The new shared helper exists specifically to stop a sixth occurrence, not as generic hardening.
- The `eq_update_customer` signature miss in migration 0211 (10 args written, 11 actually needed) is a concrete example of why the plan called for live re-verification immediately after applying, not trusting the draft — the tolerant `EXCEPTION WHEN undefined_function` handler that made the migration safe to run across both tenant planes also made the mismatch silently invisible; only the follow-up query caught it.
- `eq-context` substrate files (`pending.md`, today's session file, the changelog) were being concurrently edited by other sessions throughout — re-read immediately before each write, never trusted an earlier read, staged only the files this session actually touched.

---

# Session 2026-07-27 (4) — Quote pipeline "Pending" stage steelmanned then dropped; drag-and-drop attachments shipped; status-wiring reference doc

## Built
- Followed on from the Job Creation slowness fix in this same session's first entry. Checked whether the already-live "Archived" quotes/jobs view (under the "···" menu) needed building — it didn't, already exists and works; pointed Royce at it instead.
- Royce raised adding a "Pending" pipeline stage for quotes that are sent/won but awaiting a PO. Steelmanned it, then checked the live data before agreeing to build anything: "Open" barely holds any won-but-unpaperworked quotes (3 total) — the real conflation was inside "Job created," which lumped 5 quotes still awaiting a PO together with 12 that already had one. Built the corrected version (migration + `STATUS_FILTERS` regroup, eq-shell PR #1032) — Royce then decided not to touch quote statuses at all right now (a lot of the underlying plumbing isn't actually surfaced to the team, not worth the churn). Closed #1032 unmerged, branch deleted, no live change.
- Checked what's wired for saving files to quotes/jobs: upload already worked (same `AttachmentList` component/endpoint across quote/job/site) but click-to-browse, one file at a time. Added a whole-panel drop zone with a drag highlight, plus multi-file support on both drop and the file picker — same single-file upload endpoint underneath, looped sequentially, no backend change. Also fixed a pre-existing `react-hooks/set-state-in-effect` lint failure on the same file. eq-shell PR #1033, merged, deployed to core.eq.solutions.
- Confirmed the deploy went live and the new code is actually serving (checked the real DOM: `multiple` attribute present, drop-hint text rendering) — couldn't complete a full synthetic drag-and-drop test due to repeated browser-automation timeouts in this session's tab, and didn't want to upload a real test file onto a live customer's quote just to prove it. Royce is testing it himself.
- Spun off a background task for the pre-existing `pnpm run build` / `tsc -b` failure noticed along the way (4 `ops.view_suppliers`/`ops.manage_suppliers` PermKey errors) — resolved by a separate concurrent session this same day (see this file's "stale node_modules" entry above); root cause was a stale local `node_modules` install, not a code bug.
- Built two visual artifacts documenting the full quote/job status model (16 statuses, 5 stages, every transition trigger found, tagged Confirmed vs Inferred by how directly each was traced): an inline chat diagram, then a published standalone HTML reference page at Royce's request for something more thorough and shareable.

## Decided
- Royce: don't touch quote statuses right now — close #1032 unmerged.
- Royce: go ahead and build the drag-and-drop attachments work.
- Royce: merge #1033.
- Royce: wants a "proper HTML" reference doc for the status wiring, not just the inline diagram.

## Deferred
- Royce to click-test the drag-and-drop attachments live himself (see pending.md).
- The "Pending" stage idea is explicitly parked, not a live TODO — noted in pending.md so a future session doesn't re-propose it without reading why it was dropped.

## Notes
- Grounding the "Pending" idea in live data (rather than just the taxonomy code) is what turned a plausible-sounding request into an honest "here's what's actually true" answer — the framing Royce started with ("a lot of jobs sitting in Open") didn't match the real numbers, and saying so before building anything is what let him make an informed call to drop it instead of shipping a tab that would've shown almost nothing.
- Couldn't reach the Google Fonts CDN from this sandbox's Bash tool to embed EQ's actual Plus Jakarta Sans brand font in the published HTML artifact (empty response body despite HTTP 200 — looks like a network policy in this environment, not a Google Fonts issue) — used a close system-font stack instead and said so directly rather than silently approximating the brand.

---

# Session 2026-07-27 (5) — Deleted the last flagged dead code, audited whether it's a recurring pattern

## Built
- Closed out the one remaining carried-over deferred item from earlier today: deleted `netlify/functions/cards-staff-matches.ts`. Confirmed dead via git history rather than re-trusting the earlier flag — its sole caller (`AdminCardsFeed.tsx`) was deleted in commit `4921c13a` (2026-06-23), whose own message explicitly enumerated three files it was removing and missed this fourth one, which shared the exact same dependency.
- Fixed a stale comment in `cards-approve-staff.ts` that still named the deleted file as the source of match-panel candidates — that's `identity_collision_flags` now.
- Royce asked to audit whether this "keeps happening." Swept every function in `netlify/functions/` for the same shape (zero references anywhere in `src/` or other functions). 24 initial candidates, all but this one turned out to be false positives once properly scoped: 11 are cron-scheduled and never called from the frontend at all, 6 are called by other repos (eq-cards Flutter, EQ Service) my same-repo grep couldn't see, 2 are already self-documented in their own header as "RETIRED... kept pending explicit sign-off" (a deliberate holding pattern, not a miss), 1 is called via a Netlify background-function naming variant, and the rest are well-documented admin/ops tools presumably invoked manually. Answer: it doesn't actually keep happening — one clean, traceable miss in an otherwise-thorough cleanup, not a systemic gap.
- Shipped: eq-shell [PR #1038](https://github.com/eq-solutions/eq-shell/pull/1038), merged.

## Decided
- Royce: go delete it, and audit why this keeps happening.
- Royce: merge #1038.

## Deferred
None — the one open item from this thread is closed.

## Notes
- This closes the last open thread from the earlier "onboarding minimum-requirements / bulk connect-worker / security sweep" arc (2026-07-26 → 2026-07-27) — nothing from that whole thread is still open in `pending.md`.

---

# Session 2026-07-27 — EQ Service loading spinner → real black-screen bug found and fixed in eq-shell (two rounds)

## Built
- Continuation of the earlier EQ Service loading-spinner session (same thread, resumed after a `/close`). Investigated the deferred blank-screen root cause first: confirmed it's Netlify Lambda cold start, already root-caused and mitigated in a prior session (`docs/perf/load-time-investigation.md`) — re-measured live, mitigations holding, no code action needed, flagged the remaining infra-vs-leave-it call to Royce.
- Royce then reported a *separate*, more specific symptom: a genuine solid black screen for ~5s every time he opens Service from Core. Asked for a screenshot rather than guessing — root-caused directly from it: `.eq-hub` (Shell's sidebar+content wrapper) has a near-black background for the sidebar's dark theme, but `.eq-hub__iframe-content` (where Field/Service/Cards load) never overrode it back to white. The loading skeleton is a narrow centred card, not full-bleed, so the near-black bled through everywhere around it.
- Fixed with a one-line CSS addition (`.eq-hub__iframe-content { background: var(--eq-white) }`). Verified `pnpm run build:packages/build:tokens/tsc -b`, `vite build`, and `check-css-coverage` all clean; CI green end to end. Shipped: eq-shell [PR #1037](https://github.com/eq-solutions/eq-shell/pull/1037), merged (squash `36db17d0`), live on core.eq.solutions.
- Royce reported back: white now, but no visible loading animation. Asked to reference eq-ui's canonical `Spinner` component and use "the agreed on" one instead of guessing again — found it's already the established full-page-load pattern (`App.tsx`'s `BlockingSpinner` uses `<Spinner variant="bars" size="lg" />` for Shell's own sign-in loading state). The previous `Skeleton` shimmer's fill colour (`--eq-gray-200` = `#E4DDD2`) with an opacity-only pulse was genuinely too low-contrast against the new white background to read as "loading".
- Swapped the shared `IframeLoadingSkeleton` (used by Field/Service/Cards alike) to the canonical `Spinner` plus a visible text label, re-centred the wrapper CSS to match. `tsc -b`/`vite build`/`check-css-coverage` clean (550 classes resolve), 253/253 tests pass, CI green. Shipped: eq-shell [PR #1039](https://github.com/eq-solutions/eq-shell/pull/1039), merged (squash `333ada27`), live on core.eq.solutions.
- Both fixes live in the shared iframe-loading component — Field and Cards get the same fix as Service, not just the app originally reported slow.

## Decided
- Royce: go ahead and ship the white-background fix.
- Royce: go ahead and reference eq-ui's canonical Spinner and use it here.

## Deferred
- Royce to confirm live that the spinner is now clearly visible (couldn't verify visually myself — no Shell login credentials in this session, verified via build/test/CI only).
- The separate cold-start-vs-infra-change question from earlier in this thread is still open, unrelated to the black-screen bug — logged in `eq/pending.md`.

## Notes
- Two-screenshot pattern worked well here: guessing from source code alone (the middleware auth round-trip hypothesis, then assuming the skeleton "should" be visible) was wrong or incomplete both times; an actual screenshot of the live black screen immediately pointed at the real CSS bug, and asking for a second one when "no animation" didn't match what the code implied was the right instinct even though Royce didn't end up sending a second photo — he had enough context by then to just name the actual fix he wanted (the canonical Spinner).
- eq-shell's root checkout stayed on another session's branch (`claude/access-model-phase2-3-guardrails`) throughout — both fixes were built in fresh, disposable worktrees off `origin/main` per the repo's established shared-checkout pattern, registered and de-registered in `system/worktree-registry.md` each time.
- `eq-context/system/worktree-registry.md` and `sessions/2026-07-27.md` both got concurrently edited by other sessions mid-task — handled with fetch/rebase (never force-push) each time, no work lost on either side.

---

# Session 2026-07-27 — C:\Projects worktree cleanup (39 folders) + fixed a broken PreToolUse hook

## Built
- Royce asked "are all these folders required?" — audited every folder in `C:\Projects` against the repo map, `worktree-registry.md`, and (critically) live `git worktree list` per parent repo, since the registry itself proved unreliable in both directions.
- Deleted 34 folders that git had already forgotten about entirely (no `.git` of their own, holding only leftover `node_modules`/`.next` build cruft) — confirmed via `git worktree list` showing no entry before touching anything.
- Found the registry had 5 folders marked `~~REMOVED~~` that were actually still live, git-tracked worktrees on real branches (`eq-field-approvals-wt`, `eq-field-guard` — never even had a registry row, `eqsvc-access-cluster3-wt`, `eq-intake-dupes-merge-wt`, `eq-shell-phone-otp-selfheal-wt`). Held these back, verified each branch was genuinely merged via `gh pr list --head <branch>` (local `git merge-base --is-ancestor` gave false negatives — all 5 were squash merges), then removed properly via `git worktree remove` once confirmed.
- Updated `worktree-registry.md` to record the full cleanup and flag the registry's own past inaccuracy. First write attempt corrupted every em-dash in the file (`Get-Content -Raw`'s default decode mis-read UTF-8) — caught it in the diff before committing, discarded, redid with explicit UTF-8 read/write. Pushed clean: eq-context commit [`918d9e4`](https://github.com/eq-solutions/eq-context/commit/918d9e4).
- Along the way, hit a hook crash on the very first `Bash` call: `PreToolUse:Bash hook error: [python C:\Projects\eq-context\hooks\pre_tool_use.py]: ... can't open file 'C:\Projects\Projectseq-contexthookspre_tool_use.py'`. Root-caused it: this session's harness silently strips backslashes when parsing a hook's `command` string before spawning it on Windows, and the resulting mangled string resolves as a Windows drive-relative path against cwd. `pre_tool_use.py` itself was already correct (checks `platform.system() != "Windows"` and no-ops there by design) — it just never got the chance to run. Fixed by switching the hook command to forward slashes in `C:\Projects\.claude\settings.json`, matching the convention already working for `guard.js` elsewhere in the same settings files. Found and fixed the identical latent bug in the `SessionStart`/`Stop` hooks (`session_start.py`/`session_end.py`) in `C:\Users\EQ\.claude\settings.json` too, before it caused a quieter failure at a session boundary.

## Decided
- Royce: delete the confirmed-stale list (the 34).
- Royce: remove the 5 merged worktrees too, after the merge-status check.
- Royce: update the registry, then commit and push it.
- Royce: fix the broken hook.

## Deferred
- 5 now-orphaned local git branches (already merged, worktrees removed) still sit in their repos — cosmetic `git branch -d` cleanup, not urgent. Logged in `ops/pending.md`.

## Notes
- The registry's own "REMOVED" markers could not be trusted as ground truth in either direction this session — `git worktree list` per parent repo + `gh pr` status was the only reliable source. Worth remembering for future cleanup passes: verify live, don't just read the doc, even when the doc is the one that's supposed to track this.
- The hook-path bug was Windows/backslash-specific and only affects this machine's local config (`C:\Users\EQ\.claude`, `C:\Projects\.claude`) — neither file is in a git repo, so there's nothing to push for that half of the session.

---

# Session 2026-07-27 — Closed out the worktree-cleanup thread: deleted the 5 leftover merged branches

## Built
- Deleted the 5 local git branches left over from the previous session's worktree cleanup (worktrees were already removed; the branches themselves weren't): `claude/timesheet-leave-approval-lifecycle` + `claude/dependabot-config` (eq-field), `claude/access-cluster3-service-gate` (eq-solves-service), `claude/dupes-usage-check-client` (eq-solves-intake), `claude/phone-otp-approval-selfheal` (eq-shell).
- 2 deleted clean with `git branch -d`; 3 needed `-D` — same squash-merge false-negative as last session (`-d`'s ancestry check against local HEAD never sees a squashed commit as merged). All 3 were independently confirmed MERGED via `gh pr list --head <branch>` before force-deleting, not just assumed.
- Updated `ops/pending.md` to mark the branch-cleanup checklist item done (was the one open item from the 2026-07-27 worktree-cleanup entry). Committed and pushed: eq-context [`c071c37`](https://github.com/eq-solutions/eq-context/commit/c071c37).

## Decided
- Royce: delete the 5 merged branches.
- Royce: mark the checklist item done in `ops/pending.md`.

## Deferred
- None. Asked Royce "what's left on this branch before we can ship?" but no active feature branch was established in this session (all work was direct-to-main cleanup) — asked which repo/branch he meant; he redirected to `/close` instead of answering, so the question is dropped, not deferred.

## Notes
- This closes the last open item from the 2026-07-27 worktree-cleanup thread — nothing outstanding from that arc remains in `pending.md`.

---

# Session 2026-07-27 — Suite-wide worktree audit (110 folders, 3 rounds) + hook bug fully closed out + security register reviewed

## Built
- Checked every other repo for the same backslash-path hook bug fixed earlier — found one more live copy, in `eq-context/hooks/settings.template.json` (the master template other repos copy from, not itself live config). Fixed there too so it can't reintroduce the bug. Commit [`ab66d76`](https://github.com/eq-solutions/eq-context/commit/ab66d76).
- **Round 2:** checked whether *other* repos' worktrees were clean. Found 3 dirty checkouts belonging to other live sessions (left untouched) and 8 more clean-but-already-merged worktrees across eq-shell/eq-field/eq-solves-service, confirmed via `gh pr list`/commit ancestry before touching anything. 7 removed clean, 1 left build cruft cleared separately; their 7 now-dangling branches deleted. Also pruned an empty husk in `eq-context` itself.
- **Round 3** (Royce: "keep auditing and deep diving"): widened the check beyond `git worktree list` to a direct on-disk-vs-tracked diff across every repo's `.claude/worktrees/` folder — found ~65 more orphaned folders suite-wide, some dating to mid-June. Checked every one's actual contents (none had their own `.git`) before deleting: 60 confirmed empty/build-cruft-only, removed across eq-roles, eq-design-tokens, eq-field, eq-context, sks-nsw-labour (26), eq-shell (9/10), eq-solves-service (2/3), eq-intake (6).
- The 2 flagged-not-deleted folders (`eq-shell/app-naming-wt`, real source content; `766-wire-check-perms`, the one fully-undocumented mystery folder from the original audit) turned out to be near-identical stale snapshots of the same already-merged work — traced via `git log`/diff against the live repo straight to [eq-shell PR #779](https://github.com/eq-solutions/eq-shell/pull/779) (merged 2026-07-12). Both deleted, no Royce review actually needed.
- Verified the remaining undocumented non-`-wt` folders: corrected an earlier wrong assumption that `favour-perfect` was related to the dead Supabase tenant of the same name — it's an unrelated small-business client project (party favours/chocolates), pure name collision. Confirmed `akko-jobsetup`/`eq-analytics-v2`/`eq-website`/`sks` are accurately flagged as out-of-suite scratch folders already. Found `sks-charters` (generates SKS promotion charter docs) had no GitHub remote at all — everything in it only existed on this machine.
- Chased the 4 folders still locked "device busy" — found and fixed the actual cause for one (`eq-solves-service/user-source-analysis-be4daa`): two orphaned `next start` dev servers (ports 3111/3112) left running by a Claude Code session that had verified [PR #613](https://github.com/eq-solutions/eq-service/pull/613) and never stopped them. Traced the full process ancestry to confirm they were genuinely orphaned (parent session already exited), stopped both, folder deleted. The other 3 (`eq-cards`, `eq-ui`, `eq-shell`) remain locked by something Windows' built-in tools can't identify without a reboot — confirmed empty and harmless, left as-is.
- Checked what was actually in `sks-charters` before backing it up: a small, one-off tool (5 code files) that already generated 7 real charter documents, confirmed already delivered to their recipients. Backed up just the tool itself to a new private repo, [`eq-solutions/sks-charters`](https://github.com/eq-solutions/sks-charters) (private — contains real staff names/roles).
- Surfaced the 3 open P0 security findings sitting in the digest all session (SEC-1 live PII leak, SEC-9 confirmed exposed key, SEC-10 plaintext secrets) — none touched this session, none had been mentioned. Explained SEC-10's fix in plain terms (manual-hands-only, same-value re-mask via Netlify dashboard/CLI — Claude Code is blocked from doing it even with permission, per the SEC-12 precedent).

## Decided
- Royce: check other repos for the hook bug, fix the template too.
- Royce: yes, remove the round-2 worktrees, one repo at a time.
- Royce: "keep auditing and deep diving — there's a lot of work in here" → authorized the round-3 suite-wide sweep and its cleanup.
- Royce: try one more removal method on the locked folders (PowerShell instead of bash) before waiting.
- Royce: back up sks-charters' tool, skip the already-delivered documents (after confirming they'd been sent).
- Royce: stop the two orphaned Next.js processes and delete the folder.
- Royce: leave SEC-1 and SEC-9 as-is in the register (declined to delete them once I explained why that would just hide still-open, deliberately-accepted live risk rather than close it) — no register change needed, they already correctly reflect his standing decisions.

## Deferred
- 3 folders still locked by an unidentified system process (`eq-cards/will-brown-cards-sks-issue-4ec9c4`, `eq-ui/elastic-blackwell-1a85b8`, `eq-shell/frozen-window-issue-58b6b2`) — confirmed empty, harmless, will likely clear on their own or after a reboot.
- SEC-1 decommission checklist — still 0 of 4 gates met, no date set (pre-existing, unchanged this session).
- SEC-9 rotation — no runbook exists yet; Royce didn't ask for one drafted this session.
- SEC-10 fix — explained but not actioned; requires Royce's own Netlify access, and the site is under his existing "don't touch" freeze anyway.

## Notes
- The worktree-orphan pattern was far bigger than the original audit suggested — ~110 stale folders total across three rounds once the check widened from "read the registry" to "diff on-disk against what git actually tracks." The registry itself was unreliable in both directions throughout (rows claiming folders were removed when they were still live, and at least one live worktree — `eq-field-guard` — that never had a row at all). Worth remembering for any future cleanup pass: verify live, don't trust the doc, even when the doc's whole job is tracking this.
- Real lesson surfaced by the locked-folder investigation: a Claude Code session that starts a background dev/preview server (`next start`, etc.) for local verification needs to stop it before the session ends, or it silently outlives the session and locks the worktree it was launched from. Nothing currently enforces this.
- `sks-charters`' `.gitignore` deliberately excludes its own output (`out/*.docx`) — the generated documents are never version-controlled, only the tool that makes them. Worth knowing if anyone goes looking for past charters in the repo and doesn't find them; that's by design, not a gap.

---

# Session 2026-07-27 — Root-caused the recurring worktree-orphan pattern (Windows long-path limit) and fixed it at the source

## Built
- Royce shared a screenshot of `C:\Projects` in Explorer asking what all the extra folders were. Checked every visible one against each repo's live `git worktree list` rather than assuming from the earlier audit — found 22 more top-level `-wt` orphans (separate from the `.claude/worktrees/` ones already swept), all git-untracked, all build-cruft-only, dating back 15 days (2026-07-12 → 2026-07-27), concentrated entirely in `eq-shell` (13), `eq-solves-service` (8), `eq-intake` (1) — never `eq-field`.
- Asked to steelman the "this keeps happening" diagnosis, estimate the rate, and find an organic fix rather than another manual sweep. Root-caused it properly instead of guessing: this session had already directly witnessed `git worktree remove` fail with `Invalid argument` on a long `node_modules` path earlier the same day. The 15-day accumulation pattern, concentrated exactly in the repos with deep dependency trees and completely absent from the one repo without them (`eq-field`, vanilla JS), confirmed it. Checked the actual config: `git config core.longpaths` was already `true` (someone had tried to fix this before) but Windows' OS-level `LongPathsEnabled` registry setting was still `0` — the fix had been half-applied and never finished.
- Gave Royce the exact one-line fix (`Set-ItemProperty ... LongPathsEnabled -Value 1`) — a system-settings change, which is out of scope for Claude Code to make even with explicit permission. Royce ran it himself in an elevated PowerShell session; confirmed live via a registry read that it took (`LongPathsEnabled` = 1). Takes effect after a reboot, not yet rebooted as of this session.
- Deleted the 22-folder backlog that had built up under the old (broken) behavior, same method as every other round — confirmed each was empty/build-cruft/no-`.git` first, one repo at a time. `worktree-registry.md` and `ops/pending.md` updated to record both the root-cause finding and the cleanup. eq-context commit [`314cf2e`](https://github.com/eq-solutions/eq-context/commit/314cf2e).

## Decided
- Royce: clear the 22 now.
- Royce: enable long-path support himself (system setting, his to make).

## Deferred
- **Reboot not yet done** — the long-path fix is live in the registry but inactive until restart. No urgency, but the whole point of today's fix doesn't take effect until then.
- **Verify after reboot**: if orphaned `-wt` folders start reappearing in `eq-shell`/`eq-solves-service`/`eq-intake` post-reboot, this diagnosis needs revisiting rather than assumed correct.

## Notes
- This closes out the worktree-orphan thread properly — every prior round (1 through 3, ~110 folders) was cleanup after the fact; this is the first time the actual cause got found and fixed rather than just swept again. Whether it's actually fixed won't be known until after a reboot and some real usage — flagged as a check, not a close.
- The correlation-to-causation reasoning here is worth remembering as a pattern: when a bug's *distribution* (which repos, which timeframe) lines up exactly with a specific mechanical explanation (deep node_modules paths vs. Windows' 260-char limit) and is absent everywhere that explanation predicts it should be absent, that's strong evidence even without being able to directly instrument the actual failure on every instance — but it's still worth stating plainly that it's inference from a consistent pattern, not proof, and saying so rather than overclaiming certainty.

---

# Session 2026-07-27 — Closed the redirect-headers gap, cleared the eq-service dependency backlog, found the real limit of the npm-audit fix

## Built
- Closed the last gap in the earlier security-headers fix: `proxy.ts`'s 8 redirect responses (signin bounce, MFA challenge/enroll, dashboard bounce, admin/commercials role gate, deactivated-user signout) weren't carrying the CSP/HSTS header set — `next.config.ts`'s `headers()` only ever reached normal page responses. Extracted the header set into one shared `lib/security-headers.ts` (used by both `next.config.ts` and `proxy.ts`) instead of hand-copying it a second time. Along the way found this session's assigned worktree folder (`user-source-analysis-be4daa`) had silently lost its own `.git` link — any git command run from it was actually mutating the shared root checkout. Worked around it with a fresh isolated worktree, flagged the orphaned one in the registry. Shipped: eq-service PR [#615](https://github.com/eq-solutions/eq-service/pull/615), merged (`71ef7b6`), live.
- Checked CI health across the whole EQ suite (eq-shell, eq-solves-service, eq-field, eq-cards, eq-solves-intake) on Royce's ask — all green except eq-solves-service's `npm audit` gate.
- Merged the 5 open Dependabot PRs on eq-service (design-system group, lucide-react, posthog-js, @vitejs/plugin-react, @sentry/nextjs) after confirming each passed the real gate (`tsc + next build`) and only carried the same two pre-existing, unrelated CI failures every recent PR here has.
- Investigated the `npm audit` failure Royce asked about (and whether today's work caused it — it didn't; the underlying GHSA was published 2026-07-24, three days before this session). Root-caused it to a single new DoS advisory in `brace-expansion`, present as 4 separate copies in the tree via 2 unrelated `minimatch` majors. Confirmed by direct testing that 2 of the 4 copies can be patched safely (they already use the patched package's new calling convention) and the other 2 cannot — forcing the patch onto them crashes `eslint` outright (`TypeError: expand is not a function`), since they still call the old, incompatible API. Verified those 2 unfixed copies are not reachable at runtime anyway: eslint's chain never processes anything but this repo's own source, and `exceljs`'s use of `archiver` (read directly from `archiver`'s own source) never calls the one method (`.glob()`) that would ever hit the vulnerable code. Shipped the safe half, scoped precisely to the 2 fixable paths rather than a blanket dependency pin: eq-service PR [#616](https://github.com/eq-solutions/eq-service/pull/616), merged (`e37a553`), live. Re-checked CI suite-wide afterward — nothing else changed, same one check still red as expected.

## Decided
- Royce: merge #615 (redirect-headers fix).
- Royce: merge all five Dependabot PRs.
- Royce: merge #616 as-is, with the npm-audit gate's remaining half explicitly left open rather than force-closed.

## Deferred
- **The `npm audit` CI gate itself stays red** — not a live risk (verified both remaining paths are unreachable), but it won't turn green until either the affected tools (the eslint plugin chain, or the spreadsheet-export library's zip dependency) ship an update on a modern `minimatch`, or the CI check itself changes to stop flagging already-accepted findings. Real decision, not made this session — see `pending.md`.

## Notes
- `npm audit`'s severity count is per **package name**, not per vulnerable instance — fixing 2 of 4 physical copies of a vulnerable package can still report the exact same total count, because the same package names remain flagged via their other, unfixed copy. Worth remembering next time a "partial fix" doesn't move the reported number even though real vulnerable code paths were genuinely closed.
- Second confirmed sighting this session of the orphaned-worktree-folder pattern (a `.claude/worktrees/*` folder with no `.git` of its own, silently falling through to the root checkout) — this one wasn't caught by the earlier long-path-limit fix since it predates it; a plain git-metadata loss, not a path-length failure. Worth another look if it keeps recurring after the reboot.
