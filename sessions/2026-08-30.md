# Session 2026-08-30 — Equinix SY5 recon closes out; SLDC triple-duplicate merged live; MOD10 display fix collides with a concurrent session

## Built
- Live DB fix on ehow: merged the SLDC/Telstra St Leonards triple-duplicate in `app_data.sites`. Survivor `2881305d`; backfilled `customer_id` from `f17c8070`; moved 20 `schedule_entries` + 1 `quote` + 1 `contact_site_link` + 1 `site_project` (`MOD10`) onto the survivor; retired `f17c8070` + `36ea03f2` with dated notes citing eq-shell#781. Verified zero rows left referencing either retired id.

## Decided
- Royce: SLDC survivor = `2881305d` — schedule volume (120 vs 20 entries) was the deciding signal over customer-linkage.
- Royce: build the MOD10 roster-display fix — confirmed, but execution paused after finding a live concurrent worktree already building it (see Deferred).

## Deferred
- **MOD10-typed roster cells still don't resolve to site info in the UI.** Root cause confirmed via source review: the save path works, the display path (`renderSchedule`'s `getSiteName`/`getSiteAddress`, the roster grid's `isKnownSite`) doesn't. NOT fixed this session — a concurrent worktree (`.claude/worktrees/supervisor-list-population-9e6715`, branch `claude/mod10-functionality-988cc6`) has a staged, uncommitted fix covering 5 of 6 affected call sites, file timestamps ~20-25 min old when found (live, not abandoned). Royce didn't confirm whether that's his own session before running `/close`. **Check that worktree/branch first next session.** Full detail in `eq/pending/eq-field.md` (2026-08-30 entry).
- The site-resolution advisory tool's 15-item review backlog (oldest 6+ weeks) — flagged, not actioned.

## Notes
- The Equinix SY5 recon (the session's starting task) was a closed loop: 5 rows existed for one physical site, not the 3 originally spotted, and all were already correctly resolved by prior work via eq-shell#781's `site_resolution_advisory`/`verdict`/`merge_log` tooling (console: Core's `IntakeHealthHome`) plus manual SQL. No action was needed there — the value was in the broader sweep it triggered.
- That broader sweep (grouping `app_data.sites` by duplicate `code`) found 9 more groups; SLDC was the only one still genuinely live — real rostering data split across 2 of 3 active rows, unlike every other group (which was already an empty-duplicate cleanup, just not yet soft-retired).
- The resolver tool is real and functions correctly (write-time trigger + a console), but its own execute/merge step is barely used: only 1 of 13 decided verdicts has an actual `merge_log` entry; the rest were done by hand outside it. Spot-checked correct everywhere checked, but the tool's own audit trail isn't a reliable completeness signal by itself.
- MOD10 (`app_data.site_projects`, code `MOD10`) is a *project* tag on a site, not a competing site identity — worth remembering if another "which of these rows is more real" question comes up: a project/job/quote riding on a row doesn't make that row more canonical, it's just a child record that needs to move with whichever site_id survives.
- eq-context substrate drift was severe this session — local clone reached 112 commits behind `origin/main` mid-session. Per this repo's own guard hook (F9), `git pull` is blocked in the shared checkout; this close was written from a fresh isolated clone instead, then pushed.
