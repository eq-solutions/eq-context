# Session 2026-07-13 — SKS plant & equipment restored after a manual asset-register wipe; 2FA grace unchanged

## Built
- Restored the 16 SKS calibrated instruments (`asset_type='plant_equipment'`) on ehow `app_data.assets` from `audit_log.old_record` — original IDs, all active, calibration certs still linked. Plant & Equipment page (`/:slug/equipment`) populated again.
- (via spawned task `task_4bfeb34a`) eq-shell #790 / migration 0176 — `plant_equipment` DELETE now requires a real user actor; applied + live on ehow + zaap (`tenant-migrate.yml` run 29196855141).

## Decided
- Restore only the 16 instruments; the 817 deleted customer asset rows are "not required" (Royce) — no restore.
- The destructive delete-all was MANUAL (service-role SQL), not the eq-solves-service importer. P&E is separate from EQ Service — no importer hunt.
- Approved applying 0176 to both tenant planes.
- 2FA enrolment grace stays at 14 days — no change.

## Deferred
- 2FA grace window (`TOTP_GRACE_MS` in `netlify/functions/_shared/totp.ts`) is a one-line change if Royce ever wants it shortened or dropped — not requested.

## Notes
- Root cause: a manual service-role delete-all+reload of the whole SKS asset register at 2026-07-12 09:53 wiped every row not in the incoming feed — 16 plant_equipment + 817 customer assets (914 deleted, 187 reloaded). Recoverable because `app_data.audit_log.old_record` preserves the full deleted row; cert PDFs live in the jvkn `asset-certs` bucket (untouched, so cert_urls survive).
- Restore recipe (idempotent, original IDs): `insert into app_data.assets select (jsonb_populate_record(null::app_data.assets, old_record)).* from (select distinct on (record_id) record_id, old_record from app_data.audit_log where table_name='assets' and op='DELETE' and old_record->>'asset_type'='plant_equipment' order by record_id, at desc) t where record_id::uuid not in (select asset_id from app_data.assets);`
- The manual delete's service-role JWT bypassed the older 0154 guard (which only blocked fully context-less deletes); 0176 closes that specifically for plant_equipment.
- 2FA (`totp.ts`): `TOTP_GRACE_MS = 14 days` from account creation; enforced roles = manager/supervisor + platform admin; fail-open on missing/invalid createdAt; wired into shell-login, shell-login-phone-otp, shell-login-magic-link, select-tenant, shell-handoff-provision, verify-shell-session. File == origin/main.
- If the P&E page empties again: check `audit_log` for a system/manual DELETE burst before rebuilding — it's a restore, not a re-entry.

---

## Session 2026-07-13 (cont.) — closed the two eq-solves-intake CI items deferred last session

## Built
- **eq-solves-intake PR #65, merged `f6f41e3`** — closes both gaps left open by the 2026-07-12 basic-CI PR (#64):
  - `pnpm ci:drift` (schema-generation drift check) — re-tested from a genuinely fresh clone (short path, `core.longpaths`, pinned pnpm 9.15.9): zero diff, exit 0, reproducible twice. The original "fails on a clean checkout" finding was contaminated local-test state, not a real bug — wired up as a real CI step.
  - `pnpm test:integration` (`@eq/ai`, real Anthropic API) — the suite already self-skips cleanly when `ANTHROPIC_API_KEY` is unset (`describe.skipIf(!HAS_KEY)`), so it now runs unconditionally in CI at zero cost and will start asserting for real the moment the secret exists.
  - Both verified on a real GitHub Actions run (not just the checkmark): drift step showed zero diff, integration step showed "1 skipped".

## Decided
- Merged PR #65 with Royce's go-ahead (asked first, per the merge-approval pattern established last session).
- Adding an `ANTHROPIC_API_KEY` GitHub secret (to make the integration test actually call the live API) is left for Royce to decide — real per-call cost, not something to add unasked.

## Notes
- Same failure class as the 2026-07-12 build-before-typecheck bug: a "verified locally" finding that only held up because the local test environment wasn't actually clean. Second time this exact pattern has shown up in this repo's CI work — worth remembering for any future "tested locally, it fails" claim here: re-test from a truly fresh clone before trusting it.

---

# Session 2026-07-13 — Leif's invite-accept 500 fixed (auth.users ordering vs worker-link trigger); email-report + Fortinet triaged

## Built
- eq-shell PR #800 (`c865dba`, MERGED + auto-deployed): `accept-invite.ts` mints the auth.users identity BEFORE the `shell_control.users` insert so the out-of-band BEFORE INSERT worker-link trigger's non-deferrable FK to `auth.users` resolves; removed the redundant post-insert auth mirror.
- Control-plane migration `2026_07_13_harden_link_worker_trigger_fk.sql` — APPLIED + VERIFIED on jvkn: guards the trigger's `UPDATE public.workers SET user_id=NEW.id` behind an `auth.users` existence check so it can never again abort user creation.
- `invite-user.ts` resend-branch email-report fix — made + typechecks, but UNCOMMITTED (awaiting ship decision).
- Leif how-to page: `scratchpad/leif-signin-howto.html` + published artifact `de35bebb` (accept steps + Fortinet fallback).

## Decided
- (Royce) Apply the jvkn trigger migration + merge PR #800 together (chose "apply jvkn fix + merge PR").
- (Royce) Fortinet/HSTS is NOT a systemic problem — right-sized to a single CA-less device; no categorization/exemption program unless it starts hitting many devices.

## Deferred
- `invite-user.ts` email-report fix: staged/uncommitted — Royce to decide ship.
- M365 deliverability: Resend accepted the invite (messageId `3d0e29d5`) but `sks.com.au` (M365) may quarantine; unverified.
- Fortinet durable options (FortiGuard/PA/Zscaler categorization + a published "Network Requirements" allowlist page) — only if it recurs across devices. Dropping the inert HSTS `preload` token = optional hygiene.
- Leif hasn't accepted yet (link + how-to being sent).

## Notes
- Diagnosis chain: "Could not accept the invite" = `AcceptInvite.tsx` fallback for an UN-mapped error → `server-error` 500, not a validation error. No user row = failed AT the insert.
- The trigger (`shell_control.fn_link_worker_on_user_create`) is out-of-band (not in the repo — only a REVOKE guard referenced it). Its `phone_link_review` FK was made DEFERRABLE to survive BEFORE-INSERT ordering; the `workers`→`auth.users` FK (non-deferrable) was the missed case. 28 SKS workers match the trigger's fire condition (email + phone + user_id NULL).
- HSTS added 2026-05-25 (#40, `bfbaf85`): `max-age=31536000; includeSubDomains; preload`, live on core + field. eq.solutions NOT on the preload list (status "unknown") — `preload` token inert.
- core.eq.solutions usage (jvkn audit_log): ~10 distinct SKS shell-login users over 90d, 1–8/day; the field/labour workforce authenticates via the `sks-nsw-labour` path (`core.eq.solutions/sks/…`), invisible to shell_control audit — reconciles Royce's "a lot of people".
- Memory written: `invite-accept-auth-users-ordering.md` + MEMORY.md index line.

---

# Session 2026-07-13 — EQ security red-team: 4 open Field endpoints found, fixed, shipped + a parked hardening plan

## Built
- **eq-field #463** (merged + DEPLOYED live to ehow + verified) — closed 4 anonymously-triggerable automation edge functions. New `supabase/functions/_shared/cron-auth.ts` gate (accepts the service-role bearer pg_cron already sends, or `x-cron-secret`); added to supervisor-digest, shift-events, tafe-weekly-fill; tafe tenant pinned to env (was body-overridable = cross-tenant write); ts-reminder got an interim hourly abuse-cap (real fix = Phase D identity). Deployed all 4 via `supabase functions deploy … --no-verify-jwt`; verified anon → 401 (×3) / 500 fail-closed (×1, missing `TENANT_UUID`).
- **eq-field #465** (merged) — `tests/edge-fn-auth.test.js` CI guardrail: fails the build if any edge function lacks a caller-auth marker. Rides the existing test loop, no workflow change.
- **eq-field #466** (open) — Dependabot for github-actions.
- **Deliverables** (`scratchpad/` + artifacts): CEO verified security one-pager, plain-English recap, gap-closing plan, Trust-page draft, `security-register.md`.

## Decided (Royce)
- Assurance angle for the CEO; fix all 4 functions incl. an interim ts-reminder guard; merge + ship (deployed live).
- Approve Phase 1; PARK the rest (SOC 2 / MDR / WAF) behind a trigger — a real deal, a 3rd tenant, or EQ going external.
- Steelman accepted: clean stop — security is at its right level; redirect attention to the key-person / business question.

## Deferred / parked
- Merge #466; turn on the alert click-list; rotate the jvkn service key; decide whether the Field reminder/digest/TAFE features are meant to run (missing `TENANT_UUID` etc. on ehow).
- SOC 2, rented 24/7 monitoring (MDR), Cloudflare WAF — parked behind the trigger (in `security-register.md`).

## Notes
- **Red-team result:** 10 vectors × 3 DBs, everything held except the 4 Field endpoints. Verified live: RLS 100% on app tables, 0 critical advisors, JWT signing-secret resisted a dictionary, no leaked service key in any shipped bundle (only the anon key), control-plane edge functions all properly gated.
- **Apps are served direct from Netlify, NOT behind Cloudflare** (only eq.solutions is on CF) — a Cloudflare WAF for the apps is a DNS/proxy change, not a toggle.
- **Production edge-fn deploy is gated by the harness** — needs Royce's explicit hand (or a Bash allow-rule); a chat "ship" wasn't enough until he authorized it. GitHub MCP app is read-only (403); `gh` CLI works.
- Control-plane anon-grant lockdown (companion chip) verified live: anon write grants ~23 tables → 0.
- 3 eq-field worktrees left for cleanup after their PRs land: `eq-field-sec`, `eq-field-guard`, and the dependabot branch.

---

## Session 2026-07-13 (cont.) — eq-solves-intake's last CI gap closed: the Anthropic key is live and verified

## Built
- Nothing new shipped to the repo — this was verification, not a code change.

## Decided
- Royce added `ANTHROPIC_API_KEY` to eq-solves-intake's GitHub Actions secrets himself (explained first what the key is for — the "AI-assisted Suggest" column-mapping feature on the Tidy tab — and where to add it, per the non-negotiable that Claude never enters credentials).

## Notes
- Confirmed the secret exists via the GitHub API (didn't just take it on word), then re-ran the existing CI workflow with `gh run rerun` (no new commit needed — secrets are injected at run time) and read the real log: the integration test made an actual Anthropic call this time instead of skipping — `Mobile -> phone (conf=0.90)`, `Type -> employment_type (conf=0.70)`, 12.8s round trip.
- Confirmed the local dev `.env` (`C:\Projects\eq-intake\eq-platform\.env`) already had the same key set, so local testing was never blocked either.
- Both CI gaps opened by the 2026-07-12 basic-CI PR (#64) are now fully closed with real, verified coverage — nothing deferred left on this thread.
- A Netlify MCP call to dump every env var on the live eq-shell site was correctly blocked by the auto-mode classifier as bulk credential materialization; resolved by asking Royce directly instead of working around it.
