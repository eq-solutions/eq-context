# Session 2026-08-11 — EQ Cards: removed dead CardScreen (710 lines), merged live

## Built
- Confirmed `CardScreen` (`lib/features/card/presentation/screens/card_screen.dart`, 710 lines) was genuinely dead — fresh grep across `lib/` and `test/` found zero references outside its own definition; `Routes.card` already redirects straight to the wallet (Licences tab) post 3→2 tab merge.
- Deleted the file (and its now-empty containing folder); left the `/card` legacy-deep-link redirect in `app_router.dart` untouched, as required.
- `flutter analyze` clean (no issues) after removal, confirming nothing else referenced it.
- Branched, committed, pushed, opened [eq-cards#222](https://github.com/eq-solutions/eq-cards/pull/222), waited for CI (Analyze and test + Migration hygiene both passed), squash-merged, deleted the branch. `main` advanced `b1db8b8` → `657de55`.

## Decided
- Royce confirmed "merge" — PR taken through to squash-merge on `main`, following this repo's existing squash-PR convention.

## Deferred
- None — task completed end to end this session.

## Notes
- `flutter pub get` (run as part of `flutter analyze`) incidentally drifted `pubspec.lock` (4 deps, unrelated to this change) — reverted before committing so the PR stayed scoped to the deletion only. Worth remembering: running analyze/pub get in this repo can dirty the lockfile even with no dependency changes intended.
- Flutter SDK on this box lives at `C:\FlutterSDK\bin` — not on PATH by default in either Bash or PowerShell; had to prepend it manually to run `flutter analyze`.

---

# Session 2026-08-11 (cont.) — Secrets sprint: two PR fixes merged, Cards broker reviewed and readiness-audited

## Built
- eq-service PR #688 (Sentry bump): diagnosed and fixed a real TS2322 — the bump left a transitive `@sentry/core` version mismatch (bundler-plugins pinned the old version, npm hoisted it over the newer direct copy). One-line fix in `sentry.server.config.ts`, verified via clean typecheck, full build, 393/393 tests, 0 audit vulnerabilities. Squash-merged to main.
- eq-service PR #687 (eslint-config-next bump): diagnosed its one failing check as a pre-existing, main-independent integration-test infra bug, unrelated to the bump — no code fix needed. Squash-merged to main.
- `ops/secrets-inventory.md`: documented why the planned masked-suffix comparison of 3 suspected shared-secret clusters was dropped before running it — the only comparable value is reachable through the same `dev`-context call that IS SEC-9's leak, so it would reproduce the exposure for a hygiene question. Needs Royce's direct one-line confirmation instead. Also confirmed live that eq-cards' Netlify project is genuinely active (GitHub Actions zip-uploads the Flutter web build there, real Netlify Functions still run on it) — not disconnected as it might look from Cards being primarily mobile.
- `ops/sec9-sec24-netlify-manual-fix-runbook-2026-08-11.md` (new): literal click-by-click checklist for Royce's ~15 min of Netlify dashboard work to close SEC-24 and SEC-9's `dev`-context leak.
- `eq/cards-info-density-scoping-2026-08-11.md` (new): scoped punch-list #4 — Wallet (home) tab is the real target (up to 7 stacked nudge cards for a new/mid-setup worker, urgent items rendering twice), fix sized Medium; one small companion fix on the licence-detail screen (uncapped metadata rows), sized Small. Research only, no code changed.
- `ops/security-register.md`: logged a SEC-9 addendum — a merge-readiness audit's own `netlify env:list --json` call briefly had unmasked values in a local file before deleting it; recorded honestly per the row's existing standard rather than assumed clean.
- eq-shell PR #1294 + eq-cards PR #221 (Cards SSO broker, built 2026-08-10): full code review + independent merge-readiness audit this session. Both mechanically clean — CI green on every required check, 1-2 commits behind main with zero file overlap, scope matches description exactly. Live-confirmed the real blocker: `EQ_CARDS_HANDOFF_KEY` is set on neither Netlify project yet.
- Two visual explainers built for Royce and saved to his Downloads folder: `secrets-map.html` (the redundancy/issues diagram) and `keys-explainer.html` (plain-picture walkthrough of the whole Cards broker fix).

## Decided
- Royce: "Yes, start now" on the sprint plan (verify shared-secret clusters, fix the two aging PRs, scope Cards density, draft the SEC-9/24 runbook).
- Royce: "merge both" — eq-service #688 and #687 squash-merged to main.
- Royce: plans to run the Cards broker deploy sequence + Netlify runbook from his Beelink rather than ad hoc — confirmed that's the right call, nothing here is time-sensitive.

## Deferred
- 3 suspected shared-secret clusters (ehow key under 4 different names, platform admin key on 2 apps, Resend key on 4 apps) — needs Royce's direct confirmation, not another Netlify read.
- SEC-9 `dev`-context clear (17 vars, 3 sites) + SEC-24 re-store — runbook ready, waiting on Royce's Netlify dashboard time.
- Cards SSO broker merge/deploy — reviewed and ready, waiting on the `EQ_CARDS_HANDOFF_KEY` manual step + Royce's Beelink session. Sequence matters: eq-shell first, key on both sides, then eq-cards, then delete the now-unused old secrets from eq-cards.
- Cards info-density build (scoped, not built) — punch-list #4 still active.
- Mobile polish remainder — punch-list #3, still needs Royce's phone.

## Notes
- Confirmed live: Netlify's safely-masked contexts (production/branch-deploy/deploy-preview) return a literal `****` with no suffix — there is no partial-reveal channel for comparing two secrets' values without hitting the same `dev`-context leak SEC-9 already logged twice. Any future "are these the same secret" question needs to go to Royce directly, not another API read — same conclusion whether the tool is the Netlify MCP or the `netlify` CLI (the CLI hit the identical gap this session, logged as a SEC-9 addendum).
- eq-service's `Integration tests (Supabase local)` check has been failing on `main` itself since at least 2026-08-03 (migration `0152` assumes the `app_data` schema exists before it's created in the local reset sequence) — unrelated to either PR touched this session. Someone already has uncommitted WIP fixing it on `fix/integration-ci-app-data-bootstrap` in the shared eq-solves-service checkout (already flagged in `digest.md`'s waiting-on-you list).
- eq-cards has zero branch-protection rules configured (`gh api .../branches/main/protection` → 404) — merges there are gated by human judgement only, not CI, unlike eq-shell's 5 required checks. Worth knowing before assuming a future eq-cards merge is CI-gated the same way.
- This session ran concurrently with at least 3 other eq-context sessions today (the CardScreen dead-code removal logged above, plus punch-list housekeeping and a simultaneous-users investigation visible in the commit history) — no conflicts hit, but the shared-checkout concurrency pattern is still very active; always re-read before editing.

---

# Session 2026-08-11 (cont.) — eq-shell readiness triage: EQ-SHELL-14 verified fixed and closed live

## Built
- Answered "top 3-5 actions for eq-shell production readiness at 65-70 daily users," grounded in live digest/security-register data — corrected a misattribution along the way: SEC-1 (P0 PII leak) is sks-nsw-labour, not eq-shell.
- Re-ran the function-grant audit scoped to migrations landed since the 2026-08-07 suite sweep — clean, none of the 4 eq-shell PRs merged 2026-08-10 (#1293/#1292/#1291/#1279) touched `supabase/migrations/`.
- Sentry triage on eq-shell's unresolved issues: EQ-SHELL-1J (Royce's own quote-download "Failed to fetch," 16h old) read as the #1291 fix correctly surfacing a previously-swallowed error, not a new bug; EQ-SHELL-1H (background `quote-job-consumer` cron, one upstream 500) confirmed self-healing on its 15-min schedule. Both left open/informational, not resolved.
- Verified EQ-SHELL-14 (dangling `app_data.staff.cards_worker_id` pointers) against live data: pulled all 92 non-null pointers on ehow, anti-joined against jvkn's `public.workers`, zero dangling. Confirms Emma Curth's nulled pointers held and PR #1292's adoption fix is preventing recurrence. Marked resolved in Sentry with the evidence in the issue comment.
- Ran `/decide` on "what's our best play from here" — recommended standing down on manufacturing further checks, with EQ-SHELL-14's verification as the one cheap exception worth taking.
- Logged all of the above in `eq/pending.md` under a new 2026-08-11 section, including the two gaps that stayed open.

## Decided
- Royce: "yes" to running the EQ-SHELL-14 check rather than continuing to hunt for more "simple remote" work.
- Closing assessment accepted without pushback: eq-shell is clean on everything actually checked tonight, but two production-readiness questions are genuinely unverified, not just "no issues found" — see Deferred.

## Deferred
- **EQ_SECRET_SALT rotation readiness** — flagged as the top suite-wide SSO risk (session cookie, tenant JWTs, Cards, quotes handoff, internal tokens all fall back to it), never actually checked this session.
- **Shift-start concurrency** — 65-70 simultaneous logins against a 60s iframe-token TTL, never load-tested.
- SEC-24 / SEC-9 `dev`-context leak — explicitly left to Royce's own in-progress env-var review, not duplicated here (runbook already exists per the earlier session's log above).

## Notes
- Royce was overseas on a secondary device this session — scoped everything to genuinely remote-friendly work (read-only DB queries via Supabase MCP, Sentry MCP, local `git log`), explicitly avoiding anything needing Netlify hands or his main PC.
- `secrets-map.html` (built in the earlier session today, saved to his OneDrive) was referenced back by Royce mid-session as already covering the Netlify/env-var side — confirms that artifact is actually getting used, not just filed away.

---

# Session 2026-08-11 — eq-field: docx-export fix + timesheets/apprentices/roster decomposition, both PRs merged

## Built
- Fixed a live Sentry bug (docx export since 2026-08-04): `incidents.js`/`site-reports.js`/`toolbox.js` passed `fetchTenantLogo()`'s whole `{base64,cx,cy}` object as `logoBase64` instead of `.base64`, crashing Incident/Prestart/Toolbox exports for any tenant with a logo (SKS). Verified against the real bundled JSZip build (reproduces the exact error pre-fix, clean post-fix).
- Decomposed the 3 largest files in the app, all over the repo's ~1,500-line convention: `timesheets.js` 3,217→2,298 lines (3 new companion modules), `apprentices.js` 2,500→1,482 (now under the default; 3 new modules), `roster.js` 2,272→1,616 (2 new modules). 8 new companion files total, each verified via repo-wide grep for external callers before moving anything, `node --check`, the full 26-test suite, eslint, and a live deploy-preview smoke test.
- Caught and fixed a real test regression along the way (`tests/week-window-guards.test.js` extracted `runCopyWeek`'s source by file path — repointed after the function moved) and 2 real CI/process gaps: `core-bundle-b1.js`/`core-bundle-a2.js` drift (lazy-loader.js/week-picker.js edits not regenerated into their bundles before push, twice) and stale `<script>` cache-buster version tags on 2 core files (one predated this session).
- `eq-field` [PR #675](https://github.com/eq-solutions/eq-field/pull/675) (docx fix + decomposition, v3.5.474-479) and [PR #676](https://github.com/eq-solutions/eq-field/pull/676) (ratchet tightening) — both squash-merged. `main` advanced `8beb80a` → `935d63e`. Production verified serving the new build on `field.eq.solutions`.
- Ran `/decide` on the file-size convention itself: traced the "1,500 lines" threshold to two multi-lens audits (`_reviews/multi-lens/2026-06-14.md` proposed 1,000; `2026-07-20.md` revised to 1,500 with no justification given for the change) — a round number, not a measured one. Found `eslint.config.js`'s `GRANDFATHERED_MAX_LINES` is a live CI gate (`max-lines` ESLint rule), not just prose — its ceilings for the 3 decomposed files were still 3-4x their new actual size, so tightened them to match, per the file's own documented rule ("if a file shrinks, lower its entry by hand").

## Decided
- Royce corrected a wrong assumption early: Field is not live and has no real users — SKS NSW Labour is the live system; Core/Shell integration is why Field hasn't been adopted yet. Redirected the session from "top 3 production-readiness things" (advisory) into "decompose now and fix the Sentry items" (build).
- Royce chose "Full sweep — all three now" (via AskUserQuestion) over stopping after one extraction round per file, once `apprentices.js`/`roster.js`/`timesheets.js` were confirmed still over the threshold after round one.
- Royce: "go" on the ratchet-tightening plan from `/decide`; "merge" on both PRs.
- Discussed rewriting the script architecture onto real ES modules + event delegation (would remove the `window.foo` exposure boilerplate and `onclick=""`-global pattern this session hit repeatedly). Recommended against doing it now — real value, but delivers nothing user-visible and directly competes with the actual blocker (get Field adopted). Royce didn't push back on that call.

## Deferred
- **Why did real usage never start** — the actual blocker to Field being prod-ready. SKS NSW Labour is what real workers use; Field's own parallel-run proving period sits at 0 consecutive clean weeks. Recommended pulling PostHog/`audit_log` data to find the real adoption friction rather than waiting for it to self-resolve — not started, the session went into file-size work instead.
- ES-modules + event-delegation rewrite — see Decided above. Revisit once Field has real daily use and there's slack for invisible cleanup.
- `EQ_SECRET_SALT` rotation — already tracked at the top of `eq/pending.md` from the earlier eq-shell session today; not duplicated here. Likely can't be done by Claude Code even with explicit go — same-value re-store/rotation on Netlify env vars has been manual-hands-only all session, blocked by the safety classifier as "modifying security settings."

## Notes
- Timesheets.js/roster.js are still over the plain 1,500-line default after this session (2,298 and 1,616 respectively) but now sit well inside their newly-tightened grandfathered ceilings (2,400/1,750) with real headroom — apprentices.js (1,482) is the only one of the three now under the bare default.
- The extraction pattern used throughout (IIFE-wrapped companion module, `window.foo = foo` only for confirmed external callers, pointer comment left at the original location) matches this repo's own pre-existing precedent (`roster-cell-picker.js`, `timesheets-rules.js`) — not a new convention invented this session.

---

# Session 2026-08-11 — Mohamed Hussain's licence OCR/expiry issue root-caused, record patched live

## Built
- Diagnosed Royce's report ("updated Mohamed Hussain's Open Cabling licence via mobile, it hasn't OCR'd, can't update the expiry") against live data rather than the code alone: confirmed via jvkn's `audit_log` that exactly one genuine UPDATE fired on the licence row at the time described, but `expiry_date`/`licence_number`/`photo_front_url` were all byte-identical to the pre-existing row — a real write that changed nothing.
- Traced root cause to eq-cards: mobile licence photos are read on-device (ML Kit), not via the Claude Vision `ocr-licence` edge function eq-shell's admin tools use — confirmed by `public.ocr_usage` (which the edge-function path always logs to, success or failure) having no entry near the failed attempt, only a 20-day-old one. When on-device OCR reads nothing off a card, `scan_ocr_flow.dart`'s "found nothing" branch offers "Fill manually", which lands on `licence_edit_screen.dart` with the OLD expiry pre-loaded and no signal it's stale — Save goes through as a silent no-op.
- Told Royce the existing eq-shell admin tool (Staff page → Add licence → Update existing) was the right immediate fix path, since it uses the more reliable Claude Vision OCR and writes to the same underlying record — not a new feature.
- Caught that "Replace photo" (a separate, narrower endpoint — `staff-licence-replace-photo.ts`) only swaps the evidence blob and never touches `expiry_date`/`licence_number`, before Royce relied on it thinking it would fix the expiry.
- Found Royce's emailed renewal photo already uploaded to Google Drive (`IMG_6342.JPG`) and read it via Drive's own text extraction ahead of running it through OCR — confirmed clean, high-contrast text (name, licence number T56844 unchanged, new expiry 28/08/2029, "Open" registration type).
- Proposed adding a persistent "Edit" action to eq-cards' licence detail screen (reasoning: only saw a time-gated "Renew" button) — then caught this was wrong before writing any code: an unconditional Edit button already exists in the screen's app bar. Deleted the task with no code touched.
- Patched Mohamed Hussain's Open Cabling Registration directly via SQL: expiry corrected 2026-08-28 → 2029-08-28 on **both** jvkn `public.licences` (id `42056fe8-…`, Cards/system-of-record) and ehow `app_data.licences` (id `94ec55de-…`, Staff-page copy) — confirmed via `information_schema.triggers` that no DB trigger syncs licence edits between the two planes (unlike `public.workers`, which does auto-sync on every write via an Edge Function trigger).
- When Royce pushed back ("are we 100% on this — important, control later is what stores the info"), verified by reading the actual code rather than restating the architecture doc: `cards-export-licences-background.ts` (the real compliance-pack generator) and the Field-access-unlock trigger (`sync_field_access_unlock_for_user`) both read jvkn `public.licences` directly, live. The ehow copy only feeds the Staff page's on-screen display. This corrected an overstated earlier claim (that compliance-pack export reads the ehow copy) — fixed in both the chat response and the memory file.
- Spun off a background task for the actual eq-cards fix (silent no-op renewal — Save should warn/block when a renewal's expiry wasn't actually changed, and shouldn't show "Renewed — new expiry saved" when nothing was read).

## Decided
- Royce: use the existing eq-shell admin licence tool now, rather than wait on a mobile fix — "I'm still not confident Cards works perfectly and people will give up but I need current licence information."
- Royce: don't build a new eq-cards "Add License" or rename buttons to "Add / Update License" — agreed mobile's existing "Add to wallet"/"Renew" split is clearer than a unified label once the real gap (the Edit button I'd wrongly thought was missing) was found to already exist.
- Royce: yes, spin off the eq-cards silent-no-op renewal fix as its own task.
- Royce: go ahead and patch Mohamed's record directly now, rather than route back through the app.

## Deferred
- **eq-cards silent no-op renewal bug — not fixed this session.** Background task created (`task_842b0929`): warn/block Save on a renewal when the expiry date wasn't actually changed from the pre-renewal baseline; don't show the false "Renewed" confirmation when OCR found nothing. Full repro + file:line detail in the task prompt and in `eq/pending.md` (2026-08-11).
- No other workers were checked for the same silent-renewal-no-op pattern — Mohamed's case was found via a direct report, not a sweep. Worth a broader check if it recurs.

## Notes
- **Real lesson, corrected mid-session:** which table is "the source of truth" architecturally and which table actually drives a real control (compliance pack, Field-access gate) can be different questions with different answers — verify the actual reading code path for anything described as a "control", don't infer it from an ADR or from which table a UI happens to display. Saved as `licence-sync-no-auto-trigger.md` in memory, corrected once already after an initial overstated version.
- Two near-miss wrong claims caught and corrected *before* any code was written or shipped, both by re-reading the actual file instead of trusting an earlier read or a memory: the "no Edit button on eq-cards" claim, and the "compliance-pack export reads ehow" claim. Worth noting as a pattern — Royce's two follow-up questions ("does Replace photo also break this" and "are we 100% on this") each caught a real gap in the prior answer.
- Real near-miss caught mid-session: a large exact-match `Edit` on `roster.js` failed silently on a mistyped section-header comment; switched to a Node-script line-truncation approach (verify exact line boundaries by printing them first, then `slice`/`splice` by line number) for every subsequent large-block move, avoiding the same risk.

---

# Session 2026-08-11 (cont.) — eq-solves-service production-readiness audit: real gap found, fixed, merged, applied live

## Built
- Asked "what would a leading developer do to get Service to peak production-ready levels" — ran the repo's own defined gates first (`tsc --noEmit && next build`, `npm audit --audit-level=high`, Supabase security+performance advisors): all clean, 0 ERROR findings, 0 audit vulnerabilities.
- Verified the migration ledger (`service._eq_migrations` vs local `supabase/migrations/`) had zero drift — 209/209 matched both directions, the known risk from `project_service_migration_ledger_governed_apply.md` wasn't live.
- Challenged on "is that comprehensive, are you happy" — went deeper: audited `requireUser()` coverage across all 34 server-action files (28 had it), chased the 6 gaps individually rather than assuming. All 6 turned out legitimate by design: `customers/actions.ts` is an intentional empty stub (Shell owns customer writes now), `portal/scope/actions.ts` is a read-only download gated by the portal's own session model, and the 4 `auth/*` files are necessarily pre-authentication.
- That same pass surfaced a real, previously-unflagged finding: 6 functions with a `function_search_path_mutable` WARN not covered by the existing advisor baseline in memory — `app_data.normalise_employment_type`, `staff_normalise_employment_type`, `to_au_e164`, `staff_normalise_phones`, `contacts_normalise_phones`, and `public.trigger_supervisor_digest` (the last is `SECURITY DEFINER`, the one that actually mattered for privilege-escalation risk).
- Verified all 6 functions' exact signatures and bodies live against `pg_proc` before writing anything — none rely on unqualified schema resolution, so `search_path = ''` (empty) is the correct, tightest fix, not a guess.
- Wrote `supabase/migrations/0204_pin_search_path_normalise_helpers.sql`, committed, pushed, opened [eq-service#698](https://github.com/eq-solutions/eq-service/pull/698).
- CI: every real gate passed (tsc+build, typecheck+audit, advisors, service invariants, canonical-types drift, data-quality audit, Netlify preview). `Integration tests` failed, but at migration `0152` — 52 files before mine ever runs — the same pre-existing `app_data`-schema-bootstrap gap already tracked as someone else's in-progress WIP on `fix/integration-ci-app-data-bootstrap` in the shared root checkout. Confirmed by reading the actual failure log, not assumed.
- Squash-merged #698 (`25b36bd`), dispatched `apply-service-migrations` manually. It ran to completion on its own — the "pauses for production-environment approval" message shown pre-merge didn't apply to this manual-dispatch path, so no click was actually needed despite being ready to do it.
- Confirmed live: migration applied to ehow at `2026-08-11 02:41:26 UTC`, all 6 functions now carry `search_path=""` via direct `pg_proc.proconfig` query. Re-ran the security advisor afterward: `function_search_path_mutable` findings 6 → 0, no new categories introduced (161 WARN + 9 INFO + 0 ERROR, same accepted SECURITY DEFINER/deny-all-table baseline as before, just larger).
- Corrected a stale project memory in the same pass: `project_advisor_security_baseline.md` said ~11-13 accepted WARN findings as of 2026-06-13; live is now ~160 for the identical accepted pattern (tracks the canonical write-layer's growth to 25 objects since then) — updated so a future session doesn't read the raw count as an alarm.

## Decided
- Royce: "Yes, write it now (Recommended)" on writing the search_path migration, via AskUserQuestion.
- Royce: "commit and push it as a PR" → "merge" → "dispatch the apply workflow" → "approve it in GitHub, then let me know" → "re-run advisors to confirm the finding is gone" — walked the full governed pipeline one explicit step at a time rather than batching it.

## Deferred
- Sentry runtime-error check for eq-service specifically — not run this session, the Sentry MCP connector needs Royce's auth (same standing gap noted in other sessions today).
- SEC-1/SEC-9/SEC-24 (flagged by the session-gate hook at start) — not independently re-verified beyond cross-checking `digest.md`'s health section, which attributes them to Field/eq-shell/eq-canonical, not eq-service. Reasonably resolved, not exhaustively.

## Notes
- The worktree used this session (`.claude/worktrees/report-sheet-job-codes-0c400b`) is checked out on branch `claude/production-readiness-f26290`, not a branch matching its own folder name — a prior task's worktree directory reused for this session, consistent with the documented dir-reuse pattern in `system/worktree-registry.md`. Not a fake-worktree collision (`git status`/`git branch` both confirmed clean and correctly tracked).
- The shared root checkout (`C:\Projects\eq-solves-service`, distinct from the worktree above) has unrelated uncommitted WIP on `fix/integration-ci-app-data-bootstrap`, 8 commits behind main — untouched this session, already tracked in `digest.md`'s waiting-on-you list, blocked on Docker.

---

# Session 2026-08-11 — Labour-hire licence intake pipeline built, consolidated with existing admin invite tool, 2 Sentry issues resolved

## Built
- Full labour-hire licence intake pipeline: a labour-hire partner emails licences for someone who's never touched Cards; EQ ops uploads at a new `/_platform/labour-hire-intake` tool; OCR (reusing `ocr-licence`, unmodified) creates a stub worker + `worker_credentials` on jvkn (the Cards control layer); the candidate is staged in a new `labour_hire_candidates` table, invisible to the tenant until their own admin reviews and approves it on the Staff page; approval creates a `worker_invites` row via exact `worker_id` FK, which the real person later claims via the existing, unmodified phone-OTP flow (`eq_cards_claim_invite`).
- Design ruled out two existing mechanisms before landing on this one: `org_access_requests` (its only reviewer screen, `staff-pending-connections.ts`, and its approval handler, `cards-approve-staff.ts`, both assume an already-authenticated worker throughout — structurally wrong for a candidate with no auth account) and eq-shell's Intake engine (`eq_intake_*`/`intake-stage/approve/commit` — bulk CSV import into a *tenant's own* data plane, not a shared-control-layer candidate concept).
- eq-cards migrations 0119 (`labour_hire_candidates` table + `eq_ops_review_labour_hire_candidate` RPC) and 0120 (vault secret + verifier) applied live to jvkn; `labour-hire-candidate-intake` edge function deployed (`verify_jwt=false`, ops-secret gated). eq-cards [#219](https://github.com/eq-solutions/eq-cards/pull/219), eq-shell [#1279](https://github.com/eq-solutions/eq-shell/pull/1279) (Staff-page review section + the upload tool, gated `is_platform_admin`, secret held server-side by a proxy — the page itself never sees it). Both merged.
- Caught a real gap on first real-world attempt: uploading 4 documents (driver licence, White Card, EWP, CPR) for one person would have created 4 duplicate stub workers instead of 1 worker with 4 credentials, since none of those document types reliably carry a phone/email to dedup on. Fixed same session — dedup now runs once per submission before any file is processed. eq-cards [#220](https://github.com/eq-solutions/eq-cards/pull/220), eq-shell [#1293](https://github.com/eq-solutions/eq-shell/pull/1293). Both merged.
- Mid-session audit (Royce: "is this basically a duplicate feature?") found real overlap — a platform admin could already create a live invite for any tenant via the existing `AdminWorkerInviteForm` (`is_org_admin()` short-circuits true for platform admins regardless of org). Consolidated rather than running two systems: `AdminWorkerInviteForm` gained optional multi-file OCR upload (via the existing `staff-licence-ocr.ts`, no new auth surface; phone stays required, unchanged), and `create-worker-invite.ts` now writes the identical `worker_credentials` shape the labour-hire path writes. Both converge on the same "N documents → 1 worker + N credentials, promoted to `public.licences` on claim" mechanism; trust model and phone requirement stay deliberately different, not accidentally duplicated.
- Hit and fixed a genuine control-plane drift-check failure along the way: functions created live via Supabase MCP need a matching migration file in eq-shell's *own* ledger (`scripts/check-control-plane-drift.mjs`, scans all live jvkn functions against `supabase/migrations/`) even when introduced by eq-cards — a different session had already independently found and fixed this exact gap (`eq-shell#1282`) between branch creation and PR push; rebased onto it rather than duplicating.
- Session summary saved as a standalone HTML file to the user's OneDrive root.
- Sentry investigation (after reconnecting the connector mid-session — it initially showed as needing auth, reconnected fine on retry): resolved `EQ-SHELL-Y` (`staff-licence-ocr: ocr-licence returned 401`) with evidence — last occurrence predates `f1763bb`/eq-cards#217 (the service-role auth-check rewrite) by ~40 minutes, zero recurrences in 6+ days since. Fixed `EQ-CARDS-W` (blob URL revoked on licence photo edit, Sentry substatus: regressed) — the `Image` widget rendering a freshly-picked photo had no `errorBuilder`, so Chrome Mobile revoking the underlying blob URL while the tab backgrounds (opening the OS photo picker) threw unhandled instead of degrading; added `errorBuilder` falling back to the same tap-to-pick placeholder already used for an empty photo slot. All 4 prior occurrences were one user retrying the same failed pick 10-15s apart. eq-cards [#223](https://github.com/eq-solutions/eq-cards/pull/223), merged.
- Investigated `EQ-CARDS-1F` (`LateInitializationError`, `main.dart`) — full stack trace is entirely inside Flutter/CanvasKit engine internals (`CanvasProvider.acquireCanvas` → `Surface.gaFr()`), zero first-party application frames, 0 users impacted, isolated to a 2-minute window, non-recurring. Not independently fixable in application code; no action taken, flagged for awareness only rather than guessed at.

## Decided
- Royce: "licenses belong in the cards control layer" — corrected an early framing that conflated the "licences → EQ Field only" durable architecture rule (which governs workflow/UI ownership) with data-plane ownership (jvkn/eq-canonical, unaffected by that rule).
- Royce: candidate creation is ops-secret gated with no separate consent step — the tenant-review gate is the safety check, not creation-time consent (via AskUserQuestion).
- Royce: build order was RPC + manual upload page first, email-forward front door deferred as a later fast-follow (not built this session).
- Royce: "merge both" at multiple points, each time after CI was confirmed green — no PR merged unreviewed.
- Royce: full consolidation with the existing admin invite tool (not just patching the immediate multi-file gap in isolation) — chose the larger-blast-radius option deliberately after the audit surfaced the overlap.
- Royce: reconnect Sentry and investigate properly rather than guess from truncated error titles, when the connector initially appeared to need auth.

## Deferred
- Real end-to-end click-through never run (upload → OCR → candidate → tenant approves → worker claims) — everything verified by code review, typecheck, and CI, not a live authenticated session. Test tenant: EQ Solutions (`eq`, `is_seed_demo: true` on jvkn) — not SKS Technologies (live pilot, real workers) or "Favour Perfect" (looks like a real customer, unconfirmed).
- Email-forward front door (partner forwards the email directly to an inbox, automatically triggers the same pipeline) — discussed early, deliberately deferred behind proving the manual-upload pipeline first.
- `EQ-CARDS-1F` — not independently fixable in application code (see Built); left open in Sentry, flagged for awareness.

## Notes
- `eq_cards_claim_invite`'s credential-promotion loop copies `worker_credentials.metadata` into the final `licences.metadata` on claim, but does **not** copy `photo_front_path`/`photo_back_path` — any source-document reference has to live in `metadata` (`{source_document_url: ...}`) or it silently vanishes at claim time. Easy to get wrong again if this area is touched without checking the live function body first.
- `organisations.slug` and `shell_control.tenants.slug` are separate namespaces (per eq-shell's own `admin-tenants.ts` comment) — any future org-picker UI must resolve by `tenant_id`, never by feeding a tenant slug into an `organisations.slug` lookup.
- The ops secret (`LABOUR_HIRE_INTAKE_SECRET`, vault-stored on jvkn) is a server-to-server credential and was deliberately never routed through a browser — the upload page calls a server-side proxy that holds it, following the same shape as `ADMIN_PHOTO_UPLOAD_SECRET`/`admin-attach-licence-photo`. The Claude Code permission classifier correctly blocked an attempt to read the raw secret value from the vault mid-session — expected behaviour, not a bug, and the right outcome for a credential-reveal action.
- Full design trail, including the two ruled-out mechanisms and the exact schema/RPC decisions, lives in memory file `labour_hire_licence_intake_design.md` for any future session touching this area.

---

# Session 2026-08-11 (cont.) — Closed both deferred items from the eq-solves-service audit: Sentry checked, SEC-1/9/24 independently confirmed, SEC-7 register updated

## Built
- Asked to continue outstanding items from the earlier eq-solves-service production-readiness session — worked both deferred items to a definitive conclusion rather than re-deferring them.
- **SEC-1/SEC-9/SEC-24 independently re-verified** against the primary source (`ops/security-register.md`, not just `digest.md`'s summary line): SEC-1 is sks-labour (PII leak, unrelated decommission-at-cutover decision), SEC-9 is eq-canonical/jvkn (leaked service_role key), SEC-24 is eq-shell (`QUOTES_CRON_SECRET` plaintext). None touch eq-solves-service.
- Found the Sentry MCP connector under a different, already-authorized server ID than the "sentry" plugin the session start message flagged as needing auth — tried it directly rather than telling Royce it was blocked, and it worked (`find_organizations` → `eq-solutions`, region `https://de.sentry.io`).
- **Sentry check on eq-solves-service, both unresolved issues triaged:**
  - `EQ-SOLVES-SERVICE-3` (stale server-action ID post-redeploy) already has a graceful-recovery UX fix live since [eq-service#596](https://github.com/eq-solutions/eq-service/pull/596) (merged 2026-07-23) — continuing appearances are expected background noise from routine deploys, not a new bug; the fix changes the user experience, not whether Sentry still records the event.
  - `EQ-SOLVES-SERVICE-D` (single "unexpected response from server" event, 2026-08-09) — checked whether it was the same deploy-skew class via `git log` between the user's loaded release commit and the error timestamp; ruled out, no deploy happened in that window. Single occurrence, zero repeat since, zero confirmed user impact, and the only stack frame is unattributable Next.js-internal minified code — left open rather than guessing at a fix with no real signal.
- **Bonus find while verifying SEC-1/9/24:** `ops/security-register.md`'s own **SEC-7** row (`function_search_path_mutable`, "several projects", still marked OPEN) is the exact finding class fixed on eq-service earlier this session and had no record of that closure. Updated the row + Detail section to note eq-service's 6 functions closed 2026-08-11 via PR #698/migration 0204, so the register doesn't drift for the next session that reads it. Pushed as `01adfb84`.

## Decided
- Royce: "continue with outstanding items" — no further scoping given; both deferred items were worked to completion using judgment on what "resolved" means for each (one closed via existing fix, one investigated and correctly left open for lack of evidence).

## Deferred
- None — both items carried over from the prior close are now either closed (SEC-1/9/24 confirmed unrelated, SEC-7 register updated) or explicitly determined non-actionable with current evidence (`EQ-SOLVES-SERVICE-D`).

## Notes
- The shared `eq-context` checkout's `git merge`/`git pull` is blocked by a repo hook (F9) even for a clean fast-forward — used the same isolated-clone-then-push pattern as the prior close's session-log commit, twice more this session (once for the /brief re-run's flag write is unaffected, but both substrate pushes went through the clone). Worth remembering as the default move for any future eq-context substrate write from this or another worktree, not just a one-off workaround.
- The brief-gate (Rule 0.6) re-armed itself between the two `/close` calls in this session, once for `ops/security-register.md` specifically — confirms the gate is per-write-target-repo, not just per-session-once; ran `/brief eq-context` inline rather than reaching for `EQ_SKIP_BRIEF=1`.
- The brief-gate hook (Rule 0.6) actually fired mid-session on a plain `Write` call — first real confirmation this session that `guard.js`'s brief-gate is enforced on worktree sessions too, not just root-checkout ones. Ran `/brief eq-solves-service` inline to clear it once the need became concrete, rather than pre-emptively at session start.

---

# Session 2026-08-11 (cont. 2) — Punch list drained to backlog, restore-drill proven on all 3 platform planes, suite-wide SPOF scoping, Cards SSO fix attempted and re-blocked

## Built
- Merged eq-context PR #148 (closed punch-list item 3, mobile polish — verified fine via a standalone browser-engine harness, no bug found).
- Ran `/decide` on "next best sprint": recommended and executed merging 2 aging eq-service Dependabot PRs (#687/#688) — found already merged by Royce directly, no action needed.
- Answered Royce's direct question ("how are backups/redundancy covered, what can go wrong") with a full plain-English explainer, then two authorized build items:
  - **eq-context PR #149** — new `restore-drill-eq-canonical.yml` / `restore-drill-eq-canonical-internal.yml`, extending the proven ehow restore-drill pattern to the other two platform databases. Expected-count gates calibrated against live counts queried fresh that session (jvkn: 4 tenants/110 memberships/73 auth.users; zaap: 50 customers/30 sites/0 auth.users).
  - **eq-context PR #150** — `system/infra-redundancy-scoping-2026-08-11.md`, a live-verified (Netlify MCP + Supabase MCP + code reads) suite-wide single-point-of-failure scoping pass. Two real findings: Netlify account has one team, one member, no recovery path; the auth hub (eq-shell/jvkn) cascades to Field+Service with no fallback if either is down, confirmed in `token-exchange.ts`.
  - Both merged, then **manually dispatched both new restore-drill workflows to prove them for real** (not just merge untested YAML) — both green: eq-canonical restored 4/4 tenants, 110/110 memberships, RTO 4s; eq-canonical-internal restored 50/50 customers, 30/30 sites, RTO 5s.
  - **eq-context PR #151** — recorded the actual first-run numbers in `dr-backups.md`, replacing "armed, not yet proven" with real results. All three platform planes are now restore-proven, not just ehow.
- Royce also authorized shipping the already-built Cards SSO broker fix (eq-shell #1294 + eq-cards #221) as an explicit exception to the standing overseas goal — attempted to verify `EQ_CARDS_HANDOFF_KEY` was set on both Netlify projects first (found no scoped single-key read exists on the Netlify MCP tool, only a full-dump `getAllEnvVars` — declined to run that given it's the exact broad-read pattern already logged 3× under SEC-9 as a real leak vector). Asked Royce directly instead: he's on mobile, can't complete the manual step. Held, unmerged, exactly where it was — logged in `eq/pending.md`.
- Built a comprehensive offline-readable HTML doc combining the DR/backup explainer with a full-mode `/gap` centering pass on "Platform DR" (grounded against live `git log`, not memory) — caught and fixed a real dark-mode contrast bug (white text on a near-white inverted card) and two borderline-AA label colors before delivery, verified via actual browser-engine contrast-ratio checks, not eyeballing. Delivered to Royce, then copied to his OneDrive (`eq-power.com.au`) root on his explicit request so it syncs to his laptop.
- Saved a new memory (`royce-overseas-window-2026-08.md`) — he's sometimes mobile-only through ~2026-08-22, manual Netlify/dashboard steps may need to wait; directly informed by the Cards SSO fix getting re-blocked this session.

## Decided
- Royce: "merge 148" / "merge 149" / "merge 150" / "merge 151" — every merge held for explicit instruction, none auto-merged.
- Royce: restore-drill extension + SPOF scoping pass both authorized via AskUserQuestion (multi-select).
- Royce: "Ship it now" on the Cards SSO fix — explicit, informed override of the standing goal's live/auth exclusion. Still didn't ship — blocked on his own manual step, not a judgment call on my end.
- Royce: OneDrive root (not Documents) for the HTML doc.

## Deferred
- Cards SSO broker fix (eq-shell #1294, eq-cards #221) — code ready, CI green, held on `EQ_CARDS_HANDOFF_KEY` needing to be set on both Netlify projects from a real computer. Royce's own stated plan (per a concurrent session's note in `eq/pending.md`) is to do this from the Beelink specifically, not ad hoc.
- P1 gap from the centering pass: the human game-day (auth-data restore into a real Supabase project + app-repoint) has never been run — only the throwaway-container drills are proven. Appetite explicitly set to "wait until back," not urgent.
- P2 gap: code repositories have zero backup beyond GitHub's own hosting durability — needs a decision (accept vs. add a mirror), not yet made.
- PITR still off on all 3 platform databases, inherited cost decision never independently re-confirmed at current scale — open 1-line question for Royce, unchanged from before this session.

## Notes
- Two more instances this session of the established pattern: (1) a claimed "armed, not yet proven" state get upgraded to a real proven result by actually running the thing, not just writing it and moving on; (2) an explicit "yes, do it" from Royce still hitting a real-world blocker that no amount of authorization can route around (he's not physically at a computer) — logged as its own memory since it's the second time this exact shape has mattered this session.
- The Netlify MCP's env-var tool has no scoped single-key read — only a full `getAllEnvVars` dump. Worth knowing for any future session needing to check a single secret's presence without repeating the SEC-9 broad-read exposure pattern: the only genuinely safe move is asking the human directly, not "checking carefully."

---

# Session 2026-08-11 (eq-intake) — Verified the eq-shell dedup-swap deploy, delivered the suite-role report as a durable file

*Light follow-up session, continuing from the 2026-08-08 eq-intake close. No new build or decision — just closing the loop on that close's one "needs you" item and getting the published report into Royce's hands as an actual file.*

## Built
- Verified via Netlify MCP that eq-shell PR #1287's deploy shipped clean: commit `4fa7646e` is an ancestor of current production `main`, current deploy `state: ready`, `error_message: null`, published 2026-08-10.
- Delivered the "EQ Intake — role across the suite" report as a real file. The session's own scratchpad copy of the final merged HTML had been cleaned up between resumes (the intermediate build pieces — template, SVG, embedded font data — survived), so regenerated it byte-for-byte from those pieces rather than re-authoring, then saved a durable copy to `C:\Users\EQ\Downloads\eq-intake-suite-role.html` since Royce needed it for another session and the scratchpad is this-session-only.

## Decided
- None this session — pure verification and delivery, nothing needed Royce's call.

## Deferred
- **Customers page duplicate-detection still not click-tested live** — deploy-health confirms the code reached production, not that the UI still behaves correctly post-swap. Needs a real click-through with an authenticated session. _(carried from 2026-08-08, still open)_

## Notes
- **Scratchpad files don't reliably survive a session resume** — this session's own final merged HTML was gone on resume even though the intermediate pieces that built it (template, SVG, base64 font data) were still there. Worth remembering: anything the user might need across a session gap belongs in a durable location (Downloads, `C:\Projects\`), not left in the scratchpad past the turn that created it.
- Same eq-context shared-checkout contention as every recent close — did this one in a fresh isolated clone from the start rather than touching the shared checkout's working tree at all.

---

# Session 2026-08-11 (cont.) — Control-plane drift check fixed; eq-context's recurring main-fork problem closed with a real guard, then a suite-wide staleness sweep

## Built
- eq-shell's scheduled "Tenant drift + anon-grant + policy-lint check" fixed — 2 live jvkn functions (`eq_ops_review_labour_hire_candidate`, `eq_verify_labour_hire_intake_secret`) had no matching migration file, red since 2026-08-07. Wrote the backfill migration byte-for-byte from live `pg_get_functiondef`, recorded in `CONTROL-PLANE-LEDGER.md`. eq-shell [PR #1282](https://github.com/eq-solutions/eq-shell/pull/1282), merged — confirmed clean on its own drift-check run (115 files, 144 functions, zero unsourced).
- Found this session's own eq-context root checkout had genuinely forked from `origin/main` (not just stale) — multiple concurrent sessions committing without syncing over 2026-08-08/09, several producing content-identical duplicate commits under different SHAs/authors (same real events logged twice independently). Audited every local-only commit's content against origin before touching anything, rather than assuming stale meant lossy; recovered the one genuinely unique piece (an Intake suite-audit decision log — tonight's Ops/Service/ABN/enrich-dedup/AI-key calls) via an isolated worktree, then reset local `main` to match `origin/main`. Backup branch (`backup/main-divergence-2026-08-08`) + a stash left in place, not deleted.
- Ran `/decide` on the standing "worktree-isolation vs accept-and-rebase" question — open since 2026-08-04/05 per multiple prior sessions' own logged incidents (see today's earlier entries above re: the F9 hook block and isolated-clone-then-push workarounds). Concluded bare policy text wasn't enough on its own: eq-field's identical rule (its own `CLAUDE.md`, PR #673) was already violated same-day by concurrent sessions before any guard existed. Built the structural fix instead — new `stale-main-gate` rule in `eq-guard` (`~/.claude/hooks/guard.js`) blocking a `git commit` directly on a shared checkout's `main`/`master` when local is behind its upstream (skipped entirely inside worktrees, `EQ_SKIP_STALE_MAIN=1` override). Tested against throwaway local+remote repo pairs before touching the real file: confirmed it blocks when stale, allows once synced, skips entirely in worktree paths, unlock works, no regressions on non-git/file-tool calls. Does a real `git fetch` (not just the last-known remote-tracking ref) — verified via the same throwaway pair that skipping the fetch would silently defeat the one case the rule exists for.
- Added the worktree-isolation guidance to `eq-context/rules/agentic-coding.md` §1 step 4, pointing at the guard as the actual enforcement — per the file's own §3 principle, "prefer a structural guard over a documented rule."
- Suite-wide staleness sweep once the guard existed to catch it going forward: eq-roles, eq-field, eq-shell, eq-cards, eq-solves-service, eq-ui, and SKS's sks-nsw-labour all had local `main` genuinely behind `origin/main` (never forked, just unsynced) — fast-forwarded all seven. eq-shell's and eq-solves-service's `main` were each checked out in a different, idle worktree rather than the root checkout (`auto-import-toggles-956005`, `eqsvc-loadtime-ux`) — inspected each directly (clean, 1.5–8 days idle, no local-only commits) before fast-forwarding. sks-nsw-labour's worktree had a genuine untracked `.claude/launch.json` (custom `npx serve` config) that collided with a newly-tracked generic one from upstream — preserved as `.claude/launch.json.local-backup-preexisting` rather than overwritten.
- Found `eq-solves-assets`'s `origin` remote points at a personal fork of a *different* repo (`Milmlow/eq-solves-service`) — not touched, since fetching/merging against it would pull unrelated commits into what looks like real, non-stale asset-capture-app history (`main` + 7 feature branches). Royce confirmed the repo is parked; saved to memory so a future session doesn't re-flag or "fix" it without asking.
- Root eq-context checkout's `sks/pending.md` edit was independently overwritten by another concurrent session mid-close (confirmed via a "modified, either by the user or a linter" notice — the section this session had just added was gone from the on-disk file). Direct, live confirmation of exactly the collision class this session's own fix targets. Reapplied from this session's own record of the edit into the isolated worktree rather than the shared checkout, and didn't touch whatever the other session wrote.

## Decided
- Royce: "Yes, build both now" (via AskUserQuestion) on the stale-main-gate guard + agentic-coding.md guidance, after a `/decide` pass recommending the hybrid over bare worktree-by-default policy.
- Royce: fix eq-roles' own staleness, then check/fix eq-shell + eq-field, then eq-solves-service + eq-cards, then "the rest of the repos" — incremental go-ahead at each step, not one bulk authorization.
- Royce: eq-solves-assets is parked, don't fix or re-flag the remote misconfiguration without being asked again.

## Deferred
- eq-context's `backup/main-divergence-2026-08-08` branch + stash — safe to drop once confirmed unneeded, not deleted this session.
- sks-nsw-labour worktree's `.claude/launch.json.local-backup-preexisting` — old custom dev-server config (port 4321, `npx serve`), worth a look or discard.
- eq-solves-assets remote misconfiguration — parked per Royce, not being fixed.

## Notes
- The eq-context main-fork found this session is the same class of incident logged independently by at least 3 other sessions today (see the "(cont.)" entries above — the F9 hook block, brief-gate re-arming, isolated-clone-then-push already adopted ad hoc by multiple concurrent sessions with no shared name or mechanical enforcement for the pattern). The `stale-main-gate` guard is the first structural fix for the actual failure mode, not another documented workaround joining the pile.
- Confirmed live (not assumed) before building the guard: `~/.claude/hooks/guard.js` already intercepts `git commit`/`git rebase` via `PreToolUse` and does cwd/path parsing for other rules (rule 9's `reflection-gate`) — extending it was additive, not a new mechanism.
- While testing the guard, it caught a real, live case on its own merits: `eq-roles`' root checkout was genuinely 1 commit behind `origin/main` at the time — not a synthetic test result, confirmation the rule fires correctly on real state.
- This session's own substrate writes hit the exact shared-checkout collision this fix targets, twice (`origin/main` moved once mid-close touching the same two files this session had just edited; the SKS edit was independently overwritten by a concurrent session before it could be committed) — both handled the same way, via an isolated worktree, not by fighting the shared checkout.
