# Session 2026-08-11 — EQ Cards: removed dead CardScreen (710 lines), merged live

## Built
- Confirmed `CardScreen` (`lib/features/card/presentation/screens/card_screen.dart`, 710 lines) was genuinely dead — fresh grep across `lib/` and `test/` found zero references outside its own definition; `Routes.card` already redirects straight to the wallet (Licences tab) post 3→2 tab merge.
- Deleted the file (and its now-empty containing folder); left the `/card` legacy-deep-link redirect in `app_router.dart` untouched, as required.
- `flutter analyze` clean (no issues) after removal, confirming nothing else referenced it.
- Branched, committed, pushed, opened [eq-cards#222](https://github.com/eq-solutions/eq-cards/pull/222), waited for CI (Analyze and test + Migration hygiene both passed), squash-merged, deleted the branch. `main` advanced `b1db8b8` → `657de55`.

## Decided
- Royce confirmed "merge" — PR taken through to squash-merge on `main`, following this repo's existing squash-PR convention.

## Deferred
- None — task completed end to end this session.

## Notes
- `flutter pub get` (run as part of `flutter analyze`) incidentally drifted `pubspec.lock` (4 deps, unrelated to this change) — reverted before committing so the PR stayed scoped to the deletion only. Worth remembering: running analyze/pub get in this repo can dirty the lockfile even with no dependency changes intended.
- Flutter SDK on this box lives at `C:\FlutterSDK\bin` — not on PATH by default in either Bash or PowerShell; had to prepend it manually to run `flutter analyze`.

---

# Session 2026-08-11 (cont.) — Secrets sprint: two PR fixes merged, Cards broker reviewed and readiness-audited

## Built
- eq-service PR #688 (Sentry bump): diagnosed and fixed a real TS2322 — the bump left a transitive `@sentry/core` version mismatch (bundler-plugins pinned the old version, npm hoisted it over the newer direct copy). One-line fix in `sentry.server.config.ts`, verified via clean typecheck, full build, 393/393 tests, 0 audit vulnerabilities. Squash-merged to main.
- eq-service PR #687 (eslint-config-next bump): diagnosed its one failing check as a pre-existing, main-independent integration-test infra bug, unrelated to the bump — no code fix needed. Squash-merged to main.
- `ops/secrets-inventory.md`: documented why the planned masked-suffix comparison of 3 suspected shared-secret clusters was dropped before running it — the only comparable value is reachable through the same `dev`-context call that IS SEC-9's leak, so it would reproduce the exposure for a hygiene question. Needs Royce's direct one-line confirmation instead. Also confirmed live that eq-cards' Netlify project is genuinely active (GitHub Actions zip-uploads the Flutter web build there, real Netlify Functions still run on it) — not disconnected as it might look from Cards being primarily mobile.
- `ops/sec9-sec24-netlify-manual-fix-runbook-2026-08-11.md` (new): literal click-by-click checklist for Royce's ~15 min of Netlify dashboard work to close SEC-24 and SEC-9's `dev`-context leak.
- `eq/cards-info-density-scoping-2026-08-11.md` (new): scoped punch-list #4 — Wallet (home) tab is the real target (up to 7 stacked nudge cards for a new/mid-setup worker, urgent items rendering twice), fix sized Medium; one small companion fix on the licence-detail screen (uncapped metadata rows), sized Small. Research only, no code changed.
- `ops/security-register.md`: logged a SEC-9 addendum — a merge-readiness audit's own `netlify env:list --json` call briefly had unmasked values in a local file before deleting it; recorded honestly per the row's existing standard rather than assumed clean.
- eq-shell PR #1294 + eq-cards PR #221 (Cards SSO broker, built 2026-08-10): full code review + independent merge-readiness audit this session. Both mechanically clean — CI green on every required check, 1-2 commits behind main with zero file overlap, scope matches description exactly. Live-confirmed the real blocker: `EQ_CARDS_HANDOFF_KEY` is set on neither Netlify project yet.
- Two visual explainers built for Royce and saved to his Downloads folder: `secrets-map.html` (the redundancy/issues diagram) and `keys-explainer.html` (plain-picture walkthrough of the whole Cards broker fix).

## Decided
- Royce: "Yes, start now" on the sprint plan (verify shared-secret clusters, fix the two aging PRs, scope Cards density, draft the SEC-9/24 runbook).
- Royce: "merge both" — eq-service #688 and #687 squash-merged to main.
- Royce: plans to run the Cards broker deploy sequence + Netlify runbook from his Beelink rather than ad hoc — confirmed that's the right call, nothing here is time-sensitive.

## Deferred
- 3 suspected shared-secret clusters (ehow key under 4 different names, platform admin key on 2 apps, Resend key on 4 apps) — needs Royce's direct confirmation, not another Netlify read.
- SEC-9 `dev`-context clear (17 vars, 3 sites) + SEC-24 re-store — runbook ready, waiting on Royce's Netlify dashboard time.
- Cards SSO broker merge/deploy — reviewed and ready, waiting on the `EQ_CARDS_HANDOFF_KEY` manual step + Royce's Beelink session. Sequence matters: eq-shell first, key on both sides, then eq-cards, then delete the now-unused old secrets from eq-cards.
- Cards info-density build (scoped, not built) — punch-list #4 still active.
- Mobile polish remainder — punch-list #3, still needs Royce's phone.

## Notes
- Confirmed live: Netlify's safely-masked contexts (production/branch-deploy/deploy-preview) return a literal `****` with no suffix — there is no partial-reveal channel for comparing two secrets' values without hitting the same `dev`-context leak SEC-9 already logged twice. Any future "are these the same secret" question needs to go to Royce directly, not another API read — same conclusion whether the tool is the Netlify MCP or the `netlify` CLI (the CLI hit the identical gap this session, logged as a SEC-9 addendum).
- eq-service's `Integration tests (Supabase local)` check has been failing on `main` itself since at least 2026-08-03 (migration `0152` assumes the `app_data` schema exists before it's created in the local reset sequence) — unrelated to either PR touched this session. Someone already has uncommitted WIP fixing it on `fix/integration-ci-app-data-bootstrap` in the shared eq-solves-service checkout (already flagged in `digest.md`'s waiting-on-you list).
- eq-cards has zero branch-protection rules configured (`gh api .../branches/main/protection` → 404) — merges there are gated by human judgement only, not CI, unlike eq-shell's 5 required checks. Worth knowing before assuming a future eq-cards merge is CI-gated the same way.
- This session ran concurrently with at least 3 other eq-context sessions today (the CardScreen dead-code removal logged above, plus punch-list housekeeping and a simultaneous-users investigation visible in the commit history) — no conflicts hit, but the shared-checkout concurrency pattern is still very active; always re-read before editing.

---

# Session 2026-08-11 (cont.) — eq-shell readiness triage: EQ-SHELL-14 verified fixed and closed live

## Built
- Answered "top 3-5 actions for eq-shell production readiness at 65-70 daily users," grounded in live digest/security-register data — corrected a misattribution along the way: SEC-1 (P0 PII leak) is sks-nsw-labour, not eq-shell.
- Re-ran the function-grant audit scoped to migrations landed since the 2026-08-07 suite sweep — clean, none of the 4 eq-shell PRs merged 2026-08-10 (#1293/#1292/#1291/#1279) touched `supabase/migrations/`.
- Sentry triage on eq-shell's unresolved issues: EQ-SHELL-1J (Royce's own quote-download "Failed to fetch," 16h old) read as the #1291 fix correctly surfacing a previously-swallowed error, not a new bug; EQ-SHELL-1H (background `quote-job-consumer` cron, one upstream 500) confirmed self-healing on its 15-min schedule. Both left open/informational, not resolved.
- Verified EQ-SHELL-14 (dangling `app_data.staff.cards_worker_id` pointers) against live data: pulled all 92 non-null pointers on ehow, anti-joined against jvkn's `public.workers`, zero dangling. Confirms Emma Curth's nulled pointers held and PR #1292's adoption fix is preventing recurrence. Marked resolved in Sentry with the evidence in the issue comment.
- Ran `/decide` on "what's our best play from here" — recommended standing down on manufacturing further checks, with EQ-SHELL-14's verification as the one cheap exception worth taking.
- Logged all of the above in `eq/pending.md` under a new 2026-08-11 section, including the two gaps that stayed open.

## Decided
- Royce: "yes" to running the EQ-SHELL-14 check rather than continuing to hunt for more "simple remote" work.
- Closing assessment accepted without pushback: eq-shell is clean on everything actually checked tonight, but two production-readiness questions are genuinely unverified, not just "no issues found" — see Deferred.

## Deferred
- **EQ_SECRET_SALT rotation readiness** — flagged as the top suite-wide SSO risk (session cookie, tenant JWTs, Cards, quotes handoff, internal tokens all fall back to it), never actually checked this session.
- **Shift-start concurrency** — 65-70 simultaneous logins against a 60s iframe-token TTL, never load-tested.
- SEC-24 / SEC-9 `dev`-context leak — explicitly left to Royce's own in-progress env-var review, not duplicated here (runbook already exists per the earlier session's log above).

## Notes
- Royce was overseas on a secondary device this session — scoped everything to genuinely remote-friendly work (read-only DB queries via Supabase MCP, Sentry MCP, local `git log`), explicitly avoiding anything needing Netlify hands or his main PC.
- `secrets-map.html` (built in the earlier session today, saved to his OneDrive) was referenced back by Royce mid-session as already covering the Netlify/env-var side — confirms that artifact is actually getting used, not just filed away.

---

# Session 2026-08-11 — eq-field: docx-export fix + timesheets/apprentices/roster decomposition, both PRs merged

## Built
- Fixed a live Sentry bug (docx export since 2026-08-04): `incidents.js`/`site-reports.js`/`toolbox.js` passed `fetchTenantLogo()`'s whole `{base64,cx,cy}` object as `logoBase64` instead of `.base64`, crashing Incident/Prestart/Toolbox exports for any tenant with a logo (SKS). Verified against the real bundled JSZip build (reproduces the exact error pre-fix, clean post-fix).
- Decomposed the 3 largest files in the app, all over the repo's ~1,500-line convention: `timesheets.js` 3,217→2,298 lines (3 new companion modules), `apprentices.js` 2,500→1,482 (now under the default; 3 new modules), `roster.js` 2,272→1,616 (2 new modules). 8 new companion files total, each verified via repo-wide grep for external callers before moving anything, `node --check`, the full 26-test suite, eslint, and a live deploy-preview smoke test.
- Caught and fixed a real test regression along the way (`tests/week-window-guards.test.js` extracted `runCopyWeek`'s source by file path — repointed after the function moved) and 2 real CI/process gaps: `core-bundle-b1.js`/`core-bundle-a2.js` drift (lazy-loader.js/week-picker.js edits not regenerated into their bundles before push, twice) and stale `<script>` cache-buster version tags on 2 core files (one predated this session).
- `eq-field` [PR #675](https://github.com/eq-solutions/eq-field/pull/675) (docx fix + decomposition, v3.5.474-479) and [PR #676](https://github.com/eq-solutions/eq-field/pull/676) (ratchet tightening) — both squash-merged. `main` advanced `8beb80a` → `935d63e`. Production verified serving the new build on `field.eq.solutions`.
- Ran `/decide` on the file-size convention itself: traced the "1,500 lines" threshold to two multi-lens audits (`_reviews/multi-lens/2026-06-14.md` proposed 1,000; `2026-07-20.md` revised to 1,500 with no justification given for the change) — a round number, not a measured one. Found `eslint.config.js`'s `GRANDFATHERED_MAX_LINES` is a live CI gate (`max-lines` ESLint rule), not just prose — its ceilings for the 3 decomposed files were still 3-4x their new actual size, so tightened them to match, per the file's own documented rule ("if a file shrinks, lower its entry by hand").

## Decided
- Royce corrected a wrong assumption early: Field is not live and has no real users — SKS NSW Labour is the live system; Core/Shell integration is why Field hasn't been adopted yet. Redirected the session from "top 3 production-readiness things" (advisory) into "decompose now and fix the Sentry items" (build).
- Royce chose "Full sweep — all three now" (via AskUserQuestion) over stopping after one extraction round per file, once `apprentices.js`/`roster.js`/`timesheets.js` were confirmed still over the threshold after round one.
- Royce: "go" on the ratchet-tightening plan from `/decide`; "merge" on both PRs.
- Discussed rewriting the script architecture onto real ES modules + event delegation (would remove the `window.foo` exposure boilerplate and `onclick=""`-global pattern this session hit repeatedly). Recommended against doing it now — real value, but delivers nothing user-visible and directly competes with the actual blocker (get Field adopted). Royce didn't push back on that call.

## Deferred
- **Why did real usage never start** — the actual blocker to Field being prod-ready. SKS NSW Labour is what real workers use; Field's own parallel-run proving period sits at 0 consecutive clean weeks. Recommended pulling PostHog/`audit_log` data to find the real adoption friction rather than waiting for it to self-resolve — not started, the session went into file-size work instead.
- ES-modules + event-delegation rewrite — see Decided above. Revisit once Field has real daily use and there's slack for invisible cleanup.
- `EQ_SECRET_SALT` rotation — already tracked at the top of `eq/pending.md` from the earlier eq-shell session today; not duplicated here. Likely can't be done by Claude Code even with explicit go — same-value re-store/rotation on Netlify env vars has been manual-hands-only all session, blocked by the safety classifier as "modifying security settings."

## Notes
- Timesheets.js/roster.js are still over the plain 1,500-line default after this session (2,298 and 1,616 respectively) but now sit well inside their newly-tightened grandfathered ceilings (2,400/1,750) with real headroom — apprentices.js (1,482) is the only one of the three now under the bare default.
- The extraction pattern used throughout (IIFE-wrapped companion module, `window.foo = foo` only for confirmed external callers, pointer comment left at the original location) matches this repo's own pre-existing precedent (`roster-cell-picker.js`, `timesheets-rules.js`) — not a new convention invented this session.
- Real near-miss caught mid-session: a large exact-match `Edit` on `roster.js` failed silently on a mistyped section-header comment; switched to a Node-script line-truncation approach (verify exact line boundaries by printing them first, then `slice`/`splice` by line number) for every subsequent large-block move, avoiding the same risk.
