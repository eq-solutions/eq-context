# Session 2026-08-05-d — F9 wiring-gap follow-up (task_94836df0): answered, fixed, and the shared checkout reconciled

## Built
- Investigated whether commit `2104668`'s sweep of `hooks/adversarial_test.sh` was genuinely invisible to Claude Code's own hooks (as `2a5288a`/`ecd0402c` had concluded) or a fixable gap. `guard.log` proved the "outside Claude Code's hooks entirely" theory wrong: `gate-outbound` fired for the exact command, timestamped to the second, from an ordinary Claude Code session — "via Cowork" is this environment's standard git identity, not a marker of the sandboxed product.
- Found and fixed three compounding, genuinely fixable bugs in `hooks/pre_tool_use.py`, none of them the "architectural blind spot" the earlier notes assumed:
  1. **Wiring** — registered in `PreToolUse` only at the `C:\Projects` umbrella-root `settings.json`, the identical "guard that isn't wired" gap `session_start.py` hit and fixed 2026-07-12 by moving to user scope. This hook never got that fix, so a session launched inside a repo or worktree (the common case) never invoked it at all. Fixed: wired into `C:\Users\EQ\.claude\settings.json` too, alongside `guard.js`. Root-scope `settings.json` now wires nothing (moved, not duplicated). `hooks/settings.template.json` corrected — it was also stale.
  2. **cwd tracking** — F7/F9 read the session's nominal `cwd` only, never an in-command `cd`/`-C`. New `effective_cwd()` helper fixes it, mirroring a fix `guard.js`'s own `reflection-gate` rule already made for itself 2026-07-26.
  3. **Verb matching** — found live while writing (2)'s regression test: `COMMIT_RE`/`REBASE_MERGE_PULL_RE` required "git" and the verb separated by whitespace only, so a `-C`-prefixed invocation never matched at all. Same shape, ported from `guard.js`.
- Added regression cases to both adversarial suites (the cwd-tracking case, a `-C` verb-matching case, and a settings-wiring presence check). Verified clean from correctly-mount-pathed `C:\Projects`-rooted clones throughout: 70/70 python, 32/32 bash.
- A concurrent session (`2a5288a`, `ecd0402c`) had independently investigated the same incident under the wrong premise and reached "no fix exists, tighten a convention instead." That work (restoring `adversarial_test.sh`, a real cross-platform test bug it caught, a `core.hooksPath` drift fix, a solid git-level-hook feasibility study) is good and stands — it just answers a different question than what actually happened. Reconciled both into `eq/pending.md` and `system/failures.md` rather than one overwriting the other. Pushed as `c0bd049f`.
- Discovered mid-close that the shared checkout (`C:\Projects\eq-context`) had 4 commits from an earlier session's `/close` that were committed locally but never pushed — real divergence from `origin/main`, not staleness, and the reason the fix above wasn't actually taking effect for sessions working directly in that checkout. Reconciled in an isolated clone (one real conflict in `eq/pending.md`, a structural duplicate — kept the fuller side), re-verified both suites, pushed (superseded by further pushes from other concurrent sessions since).
- Session-close archive rule applied: the now-fully-closed "shared-checkout git races / F9" section moved from `eq/pending.md` to `eq/pending-archive.md` (as a "part 2" entry — an earlier session had already rotated part 1 on 2026-08-04, before this session's and others' follow-up work accumulated under the same live heading).

## Decided
- None requiring Royce's call arose during the investigation itself — this was an autonomous fix under existing standing authority (reversible, in-scope substrate hardening). His decision is now needed on the one item below.

## Deferred
- **`C:\Projects\eq-context`'s own working copy still needs a manual sync** — full detail and the exact two commands are in `ops/pending.md`. A plain fast-forward there is blocked by this session's own new F9(b) check (can't distinguish "safe" from "will collide," blocks the verb outright by design); a plain hard reset — safe here specifically, verified clean tree plus a confirmed ancestor relationship — was blocked by the Claude Code permission classifier, correctly, since it can't take an agent's word for it. _(added 2026-08-05)_
- `auto_pr_guard.py` is documented (`hooks/README.md`, `hooks/settings.template.json`) as a real, built rung-4 guard but isn't wired anywhere — found while auditing the same settings files, out of scope for this fix since it's inert without `EQ_AUTO_PR_MODE=1` and has no scheduled run yet. _(added 2026-08-05)_
- `guard.js`'s `detect-fake-worktree` rule only fires on file Edit/Write, not Bash — considered as a possible mechanism early in this investigation, ruled out as the actual cause (the worktree involved was real, not fake), but the gap itself is real and separate. Not fixed here. _(added 2026-08-05)_
- `pre_tool_use.py`'s F9(a) bare-commit block has no exemption for completing an in-progress merge (committing with `--no-edit` after resolving conflicts) — hit this directly while reconciling the shared checkout above, worked around with an explicit `-C` flag rather than redesigning the exemption. Minor, low-frequency edge case. _(added 2026-08-05)_

## Notes
- **The core lesson**: a same-day note attributing an incident to "ran outside Claude Code's hooks" was inferred from the commit's "via Cowork" author string and a standing sandbox rule — never checked against `guard.log`, which had the answer the whole time. Worth remembering as a standing check before accepting that specific conclusion again: `guard.log` is authoritative for "did a hook actually fire," author-identity strings are not.
- **This checkout's git activity tonight was extraordinarily dense** — at least 4 distinct concurrent sessions/threads touched `eq-context` during this investigation alone (this session, the `2a5288a`/`ecd0402c` session, session `-c`'s adversarial_test.sh work, and whatever produced the final commits landing during close). Every git operation in this session that touched the shared checkout was either read-only or done from an isolated clone, per the F9 guard's own recommendation — confirmed necessary, not just theoretical, by directly hitting two more live divergences mid-session.
- `pre_tool_use.py`'s F9 checks scan the raw text of a bash command for git-verb patterns — including text inside a heredoc that's only ever written to a markdown file, never executed. Hit this directly writing this very close (a plain-English mention of the two blocked verbs, inside prose, tripped the block twice). Worked around by rephrasing rather than fighting the guard; not filed as a bug, since a naive text scan having this shape is a reasonable, known tradeoff for a rung-4 guard that must run before a command executes and can't first determine whether git-looking text inside it will actually run.
