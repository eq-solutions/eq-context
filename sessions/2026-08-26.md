# Session 2026-08-26 — Close-out of the 2026-08-25 eq-field/eq-shell session

Tail end of a long continuous session that started 2026-08-25 (see that file for the
main record: Zemi Asri clobber fix, the access-control sprint, the schema-drift gap).
Crossed midnight before this final close pass; logged under today's date per the
`/close` protocol's own rule rather than back-dating it.

## Built
- Nothing new shipped this pass — the only substrate change is the annotation below.

## Decided
- Nothing new. (All decisions for this session are recorded in `sessions/2026-08-25.md`.)

## Deferred
- eq-shell S6 click-test (staff_conversations write gate + GM Reports direct-API bypass)
  — checklist written and delivered to Royce in chat, actual click-through still not
  done. Annotated in `eq/pending/eq-shell.md` rather than ticked, since the box isn't
  closed until a person actually runs it.

## Notes
This session's `/close` fired with the session-start/prompt-submit gate reporting the
local `eq-context` clone 171 commits behind `origin/main`, plus 6 "Needs you" flags.
Substrate writes for this pass went through a fresh isolated clone (as this session had
already been doing all along, since the shared checkout has `git pull`/`merge` blocked
by the F9 guard) — a fresh clone is current by construction, so the staleness warning
didn't block anything here, but the flags themselves were worth reading at the source
rather than trusting the hook's truncated summary. Findings, checked directly against
`ops/security-register.md` / `system/failures.md` in the fresh clone:

- **SEC-1** (P0, sks-nsw-labour public-key PII leak) — real, long-standing, and
  deliberately not being engineered around (decommission-at-cutover is the fix, per
  Royce's own 2026-06-05/07-16/07-20 decision, de-escalated from active nagging
  2026-08-21 with retirement "weeks away"). Not new, not actioned this session.
- **SEC-57** (P1) — an org-wide GitHub App (`grok-by-xai`, installation `133702612`,
  `repository_selection: all`, write-level permissions) — real, open, already tracked
  in the register. Not investigated further this session; out of scope for an
  eq-field/eq-shell close.
- **`index-drift.yml` cron** — 3 consecutive failed scheduled runs, last success
  2026-08-20. Not investigated — genuinely out of scope for this session, but flagged
  to Royce directly in chat rather than silently dropped.
- **F1 / F12 "possible recurrence" flags** — both are already-guarded (rung 4) failure
  classes; the ratchet's recurrence scanner has a documented false-positive mode (a
  session log narrating an old, already-closed incident re-triggers the same signal
  regex that detects a new one — see `failures.md`'s own "How to add a failure" §4).
  Neither was chased down to a specific triggering session log this pass — flagged as
  unconfirmed leads, not verified bypasses.
- **HOOKS "LATENT SHADOW"** (core.hooksPath: `--worktree` vs `--local` disagreement on
  the shared checkout) — informational, print-only by design (this is F10's own guard
  working as intended), no action taken; this session never wrote through the shared
  checkout itself, only through isolated clones.

None of the above are new discoveries — all are pre-existing, already tracked in their
own systems (`ops/security-register.md`, `system/failures.md`, `digest.md`'s own Needs
You). Recorded here only so this session's close-out doesn't read as having silently
ignored the gate output.

---

# Session 2026-08-26 — Edit Roster: Ctrl+Enter dropdown fix confirmed live, then fillWeek() clobber gap found and closed

## Built
- Closed the Ctrl+Enter regression left open at the prior close: Royce confirmed via screenshots that the site-code `<datalist>` autocomplete dropdown does intercept typing in a roster cell. Fixed by registering the keydown handler in the capture phase, ahead of the native dropdown's own handling. Also fixed the name column's width shifting depending on whether a cell was filled (`.editor-name` had no fixed width, only a 40px min/max range it sized to its own text content within). eq-field [PR #790](https://github.com/eq-solutions/eq-field/pull/790), merged, live (v3.5.571).
- Royce asked two follow-ups: what keyboard-only options exist to fill a row if Ctrl+Enter breaks again, and whether "Copy Last Week" overwrites roster days workers have already filled in ahead of time. Answered both against live code: Tab and Paste already cover keyboard-only fill regardless of Ctrl+Enter's state; `copyLastWeek()` already only fills empty cells, never overwrites.
- That question surfaced an unflagged, related gap: `fillWeek()` (what Ctrl+Enter itself calls) had the opposite, unprotected behaviour — it unconditionally overwrote Tue–Fri with Monday's value, skipping only TAFE days, with no guard against a day someone had already entered. Surfaced explicitly and confirmed via AskUserQuestion rather than patching live, weekly-used behaviour silently. Fixed to skip any day that already holds a value, matching `copyLastWeek()`'s rule. eq-field [PR #792](https://github.com/eq-solutions/eq-field/pull/792), merged, live (v3.5.572).
- Both fixes verified via isolated Node-script tests running the actual shipped function bodies (not reconstructions) — real click-through remains blocked by Core-only auth on both tenants, same standing limitation as every recent session.
- Reconciled both PRs into eq-context substrate: `eq/changelog/eq-field.md`, `eq/pending/eq-field.md` (the Edit Roster section's stale "still broken, unresolved" item is now genuinely resolved — trimmed per the archive rule).

## Decided
- Royce (via AskUserQuestion): "Protect existing entries" — fix `fillWeek()` to skip pre-filled days.
- Royce (via AskUserQuestion): "Hold for now" — no dedicated Fill-week button as Ctrl+Enter backup insurance; Tab already meets the keyboard-only need.
- Royce: "merge it" — explicit deploy approval for PR #792 (field.eq.solutions auto-deploys on push to main).

## Deferred
- Dedicated "Fill week" button — explicitly held, not a build-later item unless Tab/paste prove insufficient in practice. Logged in `eq/pending/eq-field.md`.
- `eq/changelog/field.md` vs `eq/changelog/eq-field.md` is still an active unreconciled pair (third occurrence per the canonical file's own header) — out of scope for this session; flagged as a background task rather than fixed inline.

## Notes
- **A prior session's `/close` never reached Step 5** for the eq-field pending update, and by the time this session's own close ran, a second, different concurrent session (the eq-field/eq-shell close-out above, in this same file) had already trimmed and partly resolved the same section independently — including the Sentry `EQ-FIELD-Y` fix (PR #788) this session had no visibility into until reading the fresh clone's current state. Reconciled against that current state rather than the stale content this session started from; the one substantive correction was the Ctrl+Enter item, which the other session's close (understandably, from its own vantage point) had left open/unconfirmed — this session has direct, verified ground truth (PR #790 merged and live) that it's resolved.
- **`gh pr merge --delete-branch` failed locally on PR #792** with `fatal: 'main' is already used by worktree at C:/Projects/eq-field-apprentice-scope-race-wt` — squash-merge succeeded via the API regardless; only `gh`'s post-merge local convenience checkout tripped over an unrelated worktree elsewhere on the machine having `main` checked out. Confirmed via `gh pr view --json state,mergedAt`, then deleted the remote branch by hand.
- **Substrate write required a fresh isolated clone**, same as the session above in this file: the shared `eq-context` checkout had `git rebase`/`git commit` (bare, no pathspec) blocked by the F9 guard, correctly — a real, unrelated concurrent-session collision was sitting in the shared checkout at the time (`system/worktree-registry.md` modified, untouched here; a same-day session-close commit for `eq-shell.md` had already landed on `origin/main` under an identical `sessions/2026-08-26.md` filename, requiring a manual reconcile rather than a blind push).
- **Bash-tool Windows path escaping bit again**: `git -C C:\Projects\eq-context ...` silently mangled to `C:Projectseq-context` (backslashes eaten, no error) — forward-slash paths (`/c/Projects/eq-context`) work correctly. Already in memory from a prior incident; recurred because the /close protocol's own example commands use backslash paths.

---

# Session 2026-08-26 — QR sign-in confusion: placeholder fix + a real repeat-login root cause found and fixed live

## Built
- Royce reported people getting confused by the QR-code sign-in screen — the mobile-number field's placeholder (`0412 345 678`) reads as a real, pre-filled number, so people never typed their own. Fixed on both mobile-number inputs in `LoginPage.tsx` (send-code and PIN-login variants): placeholder now reads `Enter mobile number`.
- Mid-task, Royce separately asked about a specific worker (0418480091) who reported having to keep signing in, and whether their browser should be remembering them. Investigated against live `shell_control.audit_log` on jvkn rather than guessing from the session-TTL code alone. Root cause: not a session-expiry problem (7-day cookie, well inside the window) — `/login` rendered the sign-in/join form unconditionally, unlike `/` (`RootRoute`), which already redirects a valid session straight to the tenant home. The worker had completed the full phone+email+OTP join flow 4 times in 16 minutes, same device/IP, purely from re-opening the same QR/SMS link while already signed in. Fixed by routing `/login` through the same `RootRoute` check `/` already uses.
- Both fixes shipped as [eq-shell PR #1600](https://github.com/eq-solutions/eq-shell/pull/1600) (two commits, one PR). `tsc -b --force` clean, all CI green, deploy preview click-tested (placeholder fix confirmed visually; `/login`'s unauthenticated path confirmed still renders correctly). Squash-merged (`358c5500`) on Royce's "merge it when the deploy preview checks out". Confirmed live the hard way — not just a green Netlify check — by polling the actual production deploy record via the Netlify API until `state:"ready"` with a `published_at` timestamp, then confirming `commit_ref` matched and nothing newer had superseded it. Took ~9 minutes end to end (queued behind PR #1601, which merged moments earlier in the same pipeline) rather than this repo's usual few-second merge-to-live — not an error, just sequential build queueing, worth knowing so a future session doesn't misread a slow merge as broken.
- Noticed PR #1601 (Staff-page "never signed in" false-positive fix, from the [[staff-login-timestamp-unreliable]] memory) merged and went live in the same window, confirmed via the same deploy-record check. Updated that memory entry from "PR open, not merged" to merged+live with the verifying evidence, since the correction was sitting right there in the data already pulled.

## Decided
- Royce: go ahead on the placeholder fix (via `/brief` confirmation).
- Royce: fix the `/login` session-check gap now, in-session (via AskUserQuestion) — auth-flow change, so asked first per CLAUDE.md non-negotiable #6 rather than just doing it.
- Royce: commit, push, open PR (via AskUserQuestion) — routine git steps, not the deploy gate.
- Royce: "merge it when the deploy preview checks out" — conditional merge approval, exercised after CI + preview verification passed.

## Deferred
- Neither of the two items below is deferred anymore — Royce asked to "sprint" both in the same session, right after this close was first written. Left the strikethrough-style record here rather than silently rewriting history; see `eq/pending-archive.md`'s matching entry (moved there, fully closed) for the full resolution write-up.
  - ~~`admin.review_cards` permission-denied for the SKS supervisor role~~ — resolved: checked `@eq-solutions/roles`' canonical model directly, it's deliberately manager-only (grouped with the other manager-tier admin keys, denial message says "Manager access required"), and a purpose-built `project_managers` access group already exists for exactly this case (originally built for SKS). Not a bug — Royce's call whether to grant it via that group.
  - ~~The `/login` fix's authenticated-redirect branch wasn't click-tested live~~ — resolved without real credentials: seeded a fake `ShellSession` into `sessionStorage` in a real browser tab on `core.eq.solutions` (client-side only, no forged cookie/server data), navigated to `/login`, and confirmed via `history.state` (`{"from":"/sks"}`) that the app genuinely mounted `/sks` before the correctly-failing real auth check bounced it back — direct live proof the redirect fires in production.

## Notes
- **Same audit-log investigation answered two different-shaped questions at once**: "what's the current auth status" (session mechanics are fine, 7-day cookie working as designed) and "should their browser remember them" (yes, and it does — the bug wasn't there). Worth remembering as a pattern: a user-reported "session doesn't stick" complaint is worth checking against the actual audit log before assuming either a TTL problem or a cookie-persistence problem — this one was neither, it was a missing redirect check on a specific route.
- Local `netlify dev` (Vite proxy) was unusably flaky again this session — matches the existing `vite-build-broken-under-node-24` memory (Node v24.14.1 confirmed locally). Verification instead went through the actual PR deploy preview once it existed, which worked cleanly and is the documented workaround.
- Session-start gate flagged the shared `eq-context` checkout 9–11 commits behind `origin/main` mid-session; substrate writes for this close went through a fresh isolated clone (`git clone` to scratchpad, not the shared checkout) since the shared checkout has `git merge` blocked by the F9 guard — same pattern the two sessions above in this file already established. Push verified by reading the commit back from `origin/main` afterward, not just trusting a clean `git push` exit code.
- **The `sessionStorage`-seed browser trick used to close out the redirect click-test is worth remembering as a general technique**: for any client-side route/redirect logic gated on an `useSession()`-style cached-session check, a plausible fake session object plus `history.state` inspection gives genuine live-bundle proof without needing real credentials — much stronger than trusting code-reuse alone, and safer than fabricating server-side auth data.

---

# Session 2026-08-26 — Preview-a-person's permission lists made plain English and grouped

## Built
- Royce reviewed the live "Preview a person" tab (shipped earlier this same continuous session) and asked to make the permissions "easier to digest" -- "not all of them are in plain english... not organised in a way that helps a user know what the story is." Attached a screenshot: "Effective permissions" was a flat wall of raw dot-notation keys (44), "Field permissions" was already human-readable but one undifferentiated list of 66.
- Checked the codebase before proposing anything: the exact grouping/labeling infrastructure needed already existed, unused for this view -- `PermPillPicker` (module-grouped, collapsible, plain-English labels) already powers Base Permissions and Custom Groups elsewhere on the same page.
- Recommended grouping both sections the same way, asked one question via AskUserQuestion (collapsed vs. always-expanded by default) -- Royce chose collapsed. Built: `PermPillPicker` gained a `readOnly` mode (plain pills, no toggle, every section starts collapsed); both permission walls now render through it. Fixed a module-coverage gap found while building: the existing `MODULES` grouping deliberately excludes `admin.*`/`audit.*`/`cards.*` (role-fixed, never delegable), but a person's *effective* permission set legitimately includes them -- added a superset lookup from the full `PERMISSIONS` export so those land in a labelled section instead of an unlabelled fallback. eq-shell [PR #1599](https://github.com/eq-solutions/eq-shell/pull/1599), merged on "merge 1599", confirmed live (`c280d34d`).
- Ran `/decide anything else worth chasing`. Read `rules/decision-protocol.md` (first time this session), ran the six-step pass, and found a concrete answer: the "Group grants:"/"Role overrides:" summary line directly above the newly-grouped sections still joined raw keys (`+quotes.approve, +entity.manage_activation`) -- same problem, one line up from the fix just shipped. Built the fix (`labelForAnyKey()`, already existed, already used elsewhere on the page, safely handles both canonical and Field-fine keys). eq-shell [PR #1602](https://github.com/eq-solutions/eq-shell/pull/1602), merged on "merge 1602", confirmed live (`66f7d88a`).
- Both PRs merged with a plain squash, no admin override needed -- confirms the `eq_cards_get_my_licence` schema-drift block (root-caused and spawned as a task two closes ago) is genuinely resolved now, not just avoided.

## Decided
- Royce: build all three follow-ups is not relevant here (that was the prior segment) -- this segment: "wondering if we can make all the permissions easier to digest" (build authorized implicitly by the follow-through, confirmed by "merge 1599"/"merge 1602").
- Royce (via AskUserQuestion): collapsed by default, not always-expanded.
- Royce: "merge 1599", then "merge 1602" -- explicit, one PR at a time as established all session.

## Deferred
- Not click-tested live by a person -- folded into the existing item in `eq/pending/eq-shell.md` rather than logged as a new one, since it's the same underlying page/limitation as the two features it already covered.
- Nothing else new. The `/decide` pass's one finding was built and shipped in the same turn rather than left open.

## Notes
- **The system clock crossed midnight mid-session without the conversation's own cached "today" noticing.** guard.js's brief-gate flag check uses the live `new Date()`, not any cached context value -- a flag written under "2026-08-25" stopped satisfying the gate the moment the real clock hit 2026-08-26, with no warning beyond the gate's own generic "brief not run" message. Diagnosed by replicating the guard's exact date-computation logic directly in `node -e`, which is what actually surfaced the mismatch (Test-Path alone had already confirmed the flag file existed, which made the block genuinely confusing until the date computation itself was checked). Confirmed via `ls`/`git log` that other concurrent sessions had already independently made the same 2026-08-25->2026-08-26 transition tonight, and per this file's own first entry above, `/close`'s own convention is to log under the real current date rather than back-date to match the narrative day -- followed that convention here too.
- **`eq/pending-archive.md` has a genuine pre-existing duplicate block** -- the entire "two open Sentry issues triaged" section (header, intro, both bullets, closing line) appears twice verbatim, once around line 10084 and again at the true end of file (line 11226 before this session's append). Not investigated or fixed -- the file is pure historical record ("nothing here is actionable" per its own header) and the duplication is harmless, but worth a housekeeping pass at some point given `pending-archive.md`'s whole job is to be a clean append-only record. Worked around it by appending directly via `cat >>` rather than a string-matched Edit, since both copies are identical enough that no surrounding context disambiguates them.
- Reused, not reinvented, twice in the same segment: the `readOnly` picker mode reused an existing component instead of building a parallel one; `labelForAnyKey()` for the summary-line fix was already sitting in the same file, written for exactly this dual-namespace situation.

---

# Session 2026-08-26 (eq-cards) — Backlog archiving finished, then all 6 "Bucket A" sprint items closed same session

Continuation of yesterday's eq-cards deep-dive/triage work, resumed after a context compaction. Two distinct pieces: finished archiving the 19 stale backlog items identified in yesterday's triage, then built and merged all 6 items from that triage's "buildable now, no decision needed" bucket.

## Built
- Finished the archiving pass: 10 sections moved wholesale to `eq/pending-archive.md` (zero open items each), 7 more items resolved in place where their section kept other genuinely-open work. Two of the 19 turned out already resolved by a concurrent session before this pass started (the `0044` mystery = real supervisor, not a bug; 8 tracking migrations already applied) — corrected rather than re-flagged.
- All 6 "Bucket A" sprint items closed:
  - `SKS_TENANT_ID` generalised into data-driven routing in both canonical-sync edge functions (`workers-canonical-sync`, `licence-canonical-sync`) — eq-cards [PR #315](https://github.com/eq-solutions/eq-cards/pull/315), merged.
  - `eq_roles` bumped v2.5.5 → v2.7.5, verified additive via a direct package-source diff, not just changelog prose — zero app code changes needed. eq-cards [PR #317](https://github.com/eq-solutions/eq-cards/pull/317), merged.
  - CI hygiene: per-function edge-function deploy targeting (was redeploying everything on every dispatch) + `supabase/setup-cli` v1→v3 (clears a confirmed-live Node 20 deprecation warning). eq-cards [PR #316](https://github.com/eq-solutions/eq-cards/pull/316), merged.
  - appMetadata stuck-signup bug audited across full history (not just the 10-day window checked 2026-08-05) — 19 accounts total ever showed the signature, all but one already self-healed by the existing fix, the one exception an unrelated abandoned signup. No code/data change needed.
  - Migration `0138_worker_origin_org_id` (merged in PR #293 three days earlier) found never actually applied to live jvkn — applied directly with Royce's explicit go-ahead, closing a real gap before the next new labour-hire candidate would have hit it.
  - verify_jwt config-drift checked and closed: both flagged functions already ran `verify_jwt=false` live (harmless, an undeclared dashboard override) — added the missing explicit `config.toml` blocks so a future deploy can't silently flip either to the platform default. eq-cards [PR #319](https://github.com/eq-solutions/eq-cards/pull/319), merged.
- Caught and fixed a real security bug before merging, not after: PR #316's deploy step spliced a workflow-dispatch input directly into a `run:` shell block via `${{ }}` — GitHub's documented script-injection footgun, in a job holding `SUPABASE_ACCESS_TOKEN`. Fixed to read through `env:` before merge.
- Ran a `/decide` pass on "anything else worth chasing" after the verify_jwt check — concluded no further eq-cards engineering is warranted right now; the remaining backlog (yesterday's Bucket B/C) is genuinely blocked on Royce's decisions or a named person's action, not on more code.

## Decided
- Royce (via AskUserQuestion): "Apply now" — migration 0138 to live jvkn.
- Royce: "build the 6 Bucket A items" — authorised the sprint dispatch.
- Royce: "check the verify_jwt config drift task and /decide if there's anything else worth chasing" — led to the PR #319 fix and the decide pass above.

## Deferred
- Two eq-context-only bugs found in passing during yesterday's triage (broken MD-health-check link, an F15 git-verb gap) — still offered, not actioned; not eq-cards items.
- The admin dashboard's Cards/Field split-view item (needs Field to open a read path first) — cross-app, not a Cards-only job.
- SEC-1 (P0) / SEC-57 (P1) sitting in eq-context's own "needs you" list — explicitly out of "within eq cards" scope this session set; named to Royce, not touched.
- Buckets B (15 items, needs Royce's call) and C (18 items, waiting on a person) from yesterday's triage — untouched by design; none of them move by writing more code.

## Notes
- A parallel local session was started by Royce on the same "apply migration 0138" suggestion this session had spawned as a task chip. No conflict: the migration is idempotent (`ADD COLUMN IF NOT EXISTS`) and this session applied it first — the other session will find it a no-op.
- The session-start gate reported the *shared* `eq-context` checkout 18-19 commits behind `origin/main` mid-session; this session's own dedicated scratch clone (fetched immediately before every commit throughout) never drifted more than ~14 commits at any single check, all against unrelated files (nightly digest/suite-state refreshes, other repos' sessions/changelogs) — confirmed via `git diff --stat` before every merge, not assumed. Two pushes hit a non-fast-forward race against concurrent writers; both resolved via fetch-rebase-retry, no content lost.
- **The stale-main-gate hook blocks an entire chained Bash command (merge && add && commit) if the repo is behind at the start of that command**, even when the merge inside the same chain would have resolved it — confirmed by testing directly: `git merge` alone succeeded, HEAD did not advance from the combined attempt. Run merge as its own standalone command before add/commit whenever the clone was behind moments earlier, not chained together.
- eq_roles' upstream `CHANGELOG.md` has a real gap: v2.7.0-2.7.3 are tagged but carry no changelog entries at all — the PR #317 agent caught and corrected an imprecise attribution in its own task brief (this session had mis-cited which release introduced two specific permission keys) by diffing the actual package source directly rather than trusting the paraphrase.

---

# Session 2026-08-26 — Migration-ledger gap sweep found clean; a merged-but-dangling branch found and deleted

## Built
- Handed a brief claiming eq-cards migration `0138_worker_origin_org_id` was merged (PR #293, 2026-08-23) but never applied live to jvkn. Re-verified fresh against `information_schema` before acting rather than trusting it — found it already applied (ledger version `20260825211058`, earlier the same morning) by a different, earlier session logged in the "Bucket A sprint" entry in `eq/pending/eq-cards.md`. Premise was stale by the time this session checked; no fix needed.
- Extended the check anyway rather than stopping at 0138: spot-checked migrations 0100-0159 against live jvkn schema for the same "merged but never applied live" gap pattern. First pass name-matched against the migration ledger and produced 2 false positives (one nearly became a spawned task before catching that the repo file existed under a renumbered name — corrected before it misled anyone). Redid the check against actual live schema state (`information_schema`, `pg_proc`, `pg_indexes`) instead of names. Six candidates that looked unmatched by name (0110, 0113, 0114, 0115, 0117, 0118) all confirmed genuinely live already. Zero real gaps found.
- Ran `/decide anything worth chasing` on the investigation's tail end. Read `rules/decision-protocol.md` for the first time this session. The six-step pass surfaced a concrete finding: a branch that looked unmerged, `claude/conor-licenses-cards-84ecf5`, actually contained a real, already-deployed fix (the Conor Horgan/Nelson Sareto licence-photo-NULL bug). Recommended flagging to Royce rather than merging it unasked, per the live-app draft-and-report memory.
- Royce: "merge it." Investigation revealed the branch was already merged (PR #318) — the first ancestry check had run against a stale local `origin/main` ref. Corrected the finding instead of acting on the wrong premise.
- Royce: "delete the branch." Before deleting, found the branch's true tip had moved further than already checked — it was checked out in a live worktree, 2 commits ahead (one folded a CI-required grant fix directly into the migration file, the other was a routine sync-with-main merge). Re-verified the actual current tip against a fresh `origin/main` fetch: still byte-identical, fully captured in the merged PR. Deleted local + remote branch. A parallel merge-readiness sub-agent audit independently corroborated the same merge-verdict and surfaced one new, separate finding: `promote-labour-hire-photo` (the edge function PR #318 added) has no explicit `verify_jwt=false` declaration in `config.toml` — the same gap PR #319 (same day) closed for two sibling functions, just couldn't have covered this one since it didn't exist yet at #319's time. Spawned as a background task; Royce started it in a separate session before this close.
- Worktree removal (`.claude/worktrees/conor-licenses-cards-84ecf5`) partially failed on a Windows file lock — git's own tracking cleared successfully (dropped out of `git worktree list`), but the physical folder is now orphaned on disk. Same recurring pattern as an existing memory entry (`remove-wedge-alert-0da7e2`, flagged 2026-08-25) — updated that memory to reflect it's a recurring class rather than a one-off.
- No product/app code changed this session (verification, a decision pass, and git branch/worktree housekeeping only) — changelog update (Step 4) correctly skipped.

## Decided
- Royce: "merge it" (on the branch, before either of us knew it was already merged).
- Royce: "delete the branch" (confirmed after the already-merged correction was surfaced).
- Royce started the spawned config.toml task in a separate session rather than having this session do it inline.

## Deferred
- Orphaned worktree folder `.claude/worktrees/conor-licenses-cards-84ecf5` — Windows file lock, no process found holding it via a command-line search. Harmless, matches an existing pattern; retry a plain delete next session in this repo.
- config.toml `verify_jwt=false` block for `promote-labour-hire-photo` — spawned as `task_8ede6059`, Royce already running it in a separate session as of this close.

## Notes
- **A merge-readiness sub-agent's "no worktree exists" finding was wrong** — it checked `ls C:\Projects | grep eq-cards` (sibling-folder convention) rather than `git worktree list` inside the repo, missing the nested `.claude/worktrees/` checkout entirely. Its core merge-verdict (byte-identical diff, PR #318 merged, all required checks green) was independently correct and matched this session's own finding — but the one location-specific claim inside its report was wrong. Don't take a sub-agent's negative/"not applicable" finding on a file-location question as authoritative without knowing what it actually checked.
- **Two ancestry checks against a stale local `origin/main` ref produced two wrong conclusions in a row this session**, both corrected by a plain `git fetch`: first, that PR #318 was still unmerged (it had merged hours earlier); second, that the branch's tip was still an older commit already inspected (it had moved via a live in-worktree CI fixup since). `git log`/`git merge-base --is-ancestor` against `origin/main` are only as fresh as the last fetch — worth fetching before trusting either, especially somewhere this session already knew has heavy concurrent-session traffic.
- Session-start gate flagged the shared `eq-context` checkout increasingly far behind `origin/main` through the session (2, then 9, then 18, then 19 commits behind — active concurrent write traffic throughout, not a one-time drift). Substrate writes for this close went through a fresh isolated clone (scratchpad), not the shared checkout — confirmed the shared checkout has `git pull` blocked outright by the F9 guard by hitting the block directly, not just reading about it.

---

# Session 2026-08-26 — Quote-open P0 fixed fleet-wide; mobile Commercials layout overlap fixed

## Built
- Root-caused and fixed a live P0: opening any quote in EQ Ops threw `relation "app_data.quote_estimators" does not exist` for every user, on both tenant planes. Royce reported it live with a screenshot ("all quotes i try to open give me this error"), naming Simon Bramall as hitting it on every quote. Traced to migration `0282` (previous day, PR #1587) dropping `app_data.quote_estimators` without noticing `eq_get_quote_detail` (migration `0267`) still joined it — a dangling reference PL/pgSQL doesn't catch at `CREATE` time. Reproduced live against both zaap and ehow before writing the fix (`select * from eq_get_quote_detail(<any id>)` → `42P01`, unconditional).
- Fixed via migration `0284` (drops the dead join, nulls the 3 columns it fed — unread by the frontend, always-null in practice anyway since the table held 0 rows its entire life). eq-shell [PR #1605](https://github.com/eq-solutions/eq-shell/pull/1605), squash-merged (`785a49bf`), `tenant-migrate.yml` dispatched fleet-wide (22s apply, no failures), confirmed live on both planes via `pg_get_functiondef` (dangling reference gone) immediately after dispatch.
- Separate bug found in the same conversation: Royce screenshotted the New Quote/Edit Quote form on mobile — the Commercials summary sidebar (fixed 280px, `position: sticky`, no responsive branch) squeezed the form column and visually overlapped it. The quote detail pane elsewhere in `QuotesModule.tsx` already solves this exact problem with an `isMobile` branch; the create/edit form's own sidebar never got it. Fixed by adding the same branch. eq-shell [PR #1606](https://github.com/eq-solutions/eq-shell/pull/1606), squash-merged (`7c0e015b`).
- Ran the Session Gate (`/brief eq-shell`) before the first edit, as required — surfaced that the root checkout (`C:\Projects\eq-shell`, not this session's worktree) is sitting on an unrelated branch 140 commits behind `origin/main` with someone else's uncommitted work. Not touched; this session's own worktree was already current with `origin/main`.

## Decided
- Royce (via AskUserQuestion): "Merge + dispatch now" — merge PR #1605 and dispatch the migration to production immediately, given it was live and blocking every quote for every user.
- Royce: "merge it" — explicit deploy approval for PR #1606 (mobile layout fix).

## Deferred
- Mobile Commercials-layout fix not click-tested live — verified via `tsc -b --force` only. No live session/credentials in this environment, and this repo's local dev tooling (Vite/Netlify) is broken under Node 24, an existing documented gap. Logged in `eq/pending/eq-shell.md`.

## Notes
- **GitHub MCP's `create_pull_request` 404'd both times it was tried this session** (`POST /repos/eq-solutions/eq-shell/pulls`) — both PRs opened via `gh pr create` instead, which worked cleanly. Not investigated further; possibly a scope gap on the GitHub App installation backing that MCP server specifically for this repo. Worth a look if it recurs elsewhere.
- The quote-open fix is a good example of verifying a root-cause hypothesis against production directly rather than trusting static analysis alone: the exact failure mode (dangling `LEFT JOIN` after a table drop) was confirmed by literally reproducing the `42P01` error live via `execute_sql` before writing a single line of the fix, and re-confirmed absent by the same method immediately after dispatch — strongest available evidence, given no click-through access this session.
- Substrate write for this close went through a fresh isolated clone (scratchpad), not the shared `eq-context` checkout, per existing memory on concurrent-session git races there. Today's `sessions/2026-08-26.md` already had five prior segments from other concurrent sessions by the time this one appended — read in full before appending to avoid clobbering any of them.

---

# Session 2026-08-26 — Home: Workbench + Payroll (ESS) quick links, SKS-only

## Built
- Royce relayed a request from Cicero, a field worker, via a screenshot of Cicero's phone on EQ Field's Home tab (Shell-embedded): could Field link out to Workbench (SKS's job/project tool) and the payroll site (ESS), so workers only need one app/bookmark. Screenshot didn't arrive on the first ask — checked Google Drive directly (twice, nothing matched); Royce then attached it in chat.
- Real URLs from Royce: `workbench.sks.com.au`, `technology-ess.sks.com.au`. Asked to steelman his own "small icons" idea before building: recommended a secondary row on Home (below "Team week / Who's with me," not mixed into the primary tile grid) plus a persistent entry in the "More" drawer, since the stated goal needs reachability from every screen, not just Home. Royce: "go ahead and build it."
- Built both surfaces, gated `TENANT.ORG_SLUG === 'sks'` (existing pattern already used for Safety/Teams/Calibration — both URLs are SKS-specific, meaningless to the `eq` sandbox tenant). Links open in a new tab: Field runs inside Shell's iframe and neither external site allows itself to be framed. eq-field [PR #794](https://github.com/eq-solutions/eq-field/pull/794), merged, live (v3.5.575).
- Verified live on the deploy preview, not just in isolation: unlike the local static server, Netlify Functions actually run there, so the real app shell rendered far enough to exercise the real `renderHomeScreen()`/`applyTierVisibility()` against the real DOM — confirmed both directions (real `eq` tenant renders nothing on either surface; forced to `sks`, both render correctly; reverts cleanly).
- Reconciled into eq-context substrate: `eq/changelog/eq-field.md`, `eq/pending/eq-field.md` (one new soft-open item — see Deferred).

## Decided
- Royce (via AskUserQuestion): "Yes, just a link is fine" — a plain external link to Workbench doesn't violate the standing "Workbench out of scope for Field" rule, since it's not rebuilding Workbench's functionality.
- Royce (via AskUserQuestion): the payroll site is a separate tool from Workbench, not the same thing described two ways — two distinct links needed.
- Royce: "go ahead and build it" on the steelmanned placement recommendation.
- Royce: "merge it" — explicit deploy approval.

## Deferred
- **Neither link brings a worker back to Field** — delivers "one bookmark," not fully "one app." Named in the shipped changelog copy and logged in `eq/pending/eq-field.md` rather than left implicit; no reasonable client-side fix exists without native-app affordances Field doesn't have. Only worth revisiting if it turns out to bother people in practice.
- A second screenshot (EQ Cards) was mentioned by Royce but never arrived — proceeded without it since the EQ Field screenshot alone was sufficient to scope the actual ask.

## Notes
- **Standing "Workbench is out of scope for EQ Field" rule surfaced and resolved cleanly, not silently.** The global CLAUDE.md rule is written for "don't rebuild this in another app," not "never link to it" — but rather than assume that reading, flagged the tension directly and got Royce's explicit call before writing any code.
- **Real, live-DOM verification was possible this time**, unusually for this week's run of PRs: the deploy preview's Netlify Functions let `tenant-config.js` actually resolve (the local static server can't run Functions at all), so the real app shell rendered far enough to call the actual shipped render functions against the actual DOM, both tenant directions, rather than falling back to an isolated Node-script reconstruction. Worth remembering as the first fallback for future eq-field UI verification, before reaching for an isolated harness.
---

# Session 2026-08-26 (continued) — index-drift cron fixed, click-test steps handed off

Direct continuation of the first segment in this file (same session, same conversation) — Royce asked to explain the two "needs you" card items in detail and how to finish them.

## Built
- Fixed eq-context's own `index-drift.yml` gate while explaining it: `eq/sprints/2026-08-23-security-outstanding-triage.md` had never been added to `eq/README.md`'s index, so every run (scheduled and PR-triggered) had failed since ~2026-08-23. Root-caused from the actual failed-run log (`gh run view` on databaseId 32844790976), not guessed. Added the missing index line, pushed, then confirmed green with a manual `workflow_dispatch` run (32886820251) rather than just trusting the diff.

## Decided
- Nothing new this segment — Royce asked a question, not for a decision.

## Deferred
- Both click-tests (eq-shell S6: `staff_conversations` write gate + GM Reports direct-API bypass; and the separate Permissions/nav-audit click-test logged earlier) still need an actual signed-in person. Exact run-through steps for both have now been given to Royce in chat twice, in full.

## Notes
- **The cron's "consecutive failures" count in digest.md will look unchanged (or worse) until the *next scheduled* firing** (daily 11:30 UTC) confirms green — this segment's fix was verified via a manual `workflow_dispatch` run, which the scheduled-run health check (`scheduled_workflow_health()`, F11's guard) doesn't count toward the same streak. Not a sign the fix didn't take. Confirmed by comparing two gate snapshots: "3 consecutive" at the first close this session, "5 consecutive" at this one — two more scheduled 11:30 UTC firings happened in between, both before this fix landed, consistent with the fix, not contradicting it.
- **This close's gate flagged F9 as a possible rung-4 bypass, in place of last close's F1/F12 flags.** Near-certain false positive, same documented mechanism as last time: this session's *own* first session-log segment (above, in this same file) quotes F9's title verbatim while explaining it wasn't a new incident — that text matches the ledger's own recurrence-detection regex for F9. This session never wrote to the shared `C:\Projects\eq-context` checkout directly (every write went through an isolated clone, this one included), so there's no real mechanism for F9 to have actually recurred here. Not chased further than this — same posture as last close's F1/F12 write-up.
- SEC-1, SEC-57, the F4/F5 ratchet targets, and the HOOKS latent-shadow note are unchanged from the last close's writeup in this file's first segment — still pre-existing, already tracked, already decided where a decision exists. Nothing new to add.
- Side finding surfaced while reading the sprint file to write its index entry: it's Royce's own already-written 3-item runbook for SEC-63/SEC-61/SEC-57 (the same SEC-57 in this gate's Needs You list). Pointed him at it directly in chat rather than re-deriving a remediation plan.

---

# Session 2026-08-26 (eq-cards) — config.toml verify_jwt gap for promote-labour-hire-photo closed, then proven under a real redeploy

Picked up the task spawned by the earlier "Migration-ledger gap sweep" segment in this same file (`task_8ede6059`): `promote-labour-hire-photo` (added by PR #318) had no explicit `[functions.promote-labour-hire-photo]` block in `config.toml` — the same gap PR #319 closed for its two siblings the same day, just couldn't have covered a function that didn't exist yet.

## Built
- Verified live via Supabase MCP before writing anything: `promote-labour-hire-photo` on jvkn already ran `verify_jwt=false` (an undeclared dashboard override). Confirmed via source (`index.ts`) + migration `0161` that it's gated by the exact same `LABOUR_HIRE_INTAKE_SECRET`/`eq_verify_labour_hire_intake_secret` check as `labour-hire-candidate-intake`, not a Supabase JWT.
- Found the session's starting branch (`docs/close-ledger-tracking-gaps`) predated PR #318/#319 by several commits — branched the fix from `origin/main` instead of the stale checkout.
- Hit two guard hooks in sequence, both followed rather than routed around: F15 (no Edit/Write in the shared `eq-cards` root — moved into an `EnterWorktree` worktree) and the brief-gate (ran `/brief eq-cards` in full, including live health-digest/worktree/git-state checks, before the edit landed).
- Added the `[functions.promote-labour-hire-photo]` block, mirroring PR #319's comment style. eq-cards [PR #320](https://github.com/eq-solutions/eq-cards/pull/320), CI green (3/3), squash-merged (`b892113`) on Royce's "merge".
- Royce separately triggered a `Build & Deploy` run from another session and asked whether that was "good enough." Verified rather than assumed: confirmed via `git merge-base` that the run's commit included the PR #320 merge, confirmed the `Deploy edge functions` job had already completed successfully, then re-checked live state directly — `promote-labour-hire-photo` came back at version 2 (up from 1), `entrypoint_path` switched from an ad-hoc dashboard/temp path to the real repo path (its first deploy through the actual CI pipeline since #318), `verify_jwt` still `false`. Confirms the fix holds under the exact scenario it was written to prevent.

## Decided
- Royce (via AskUserQuestion, after a full `/brief eq-cards`): "Yes, add block + open PR".
- Royce: "merge" — PR #320.

## Deferred
- None — this closes the item spawned by the earlier session in this file (`task_8ede6059`) with no residual open thread.

## Notes
- Cleaned up after the F15 worktree detour: the shared root had been left on a throwaway branch (`fix/promote-labour-hire-photo-verify-jwt-config`, no commits, created before the guard fired) — restored it to its original branch (`docs/close-ledger-tracking-gaps`) and deleted the stray local branch, rather than leaving the shared checkout sitting somewhere a concurrent session wouldn't expect.
- Session-start gate reported the shared `eq-context` checkout progressively further behind `origin/main` through the session (16 → 18 → 22 → 26 commits) — active concurrent write traffic, not a one-time drift, consistent with other same-day sessions in this file. This close's substrate writes went through a fresh isolated clone (scratchpad) after the shared checkout's own F9 guard blocked a plain `git pull` directly (not just documented — hit it live, same as other sessions in this file).

---

# Session 2026-08-26 (cont.) — Labour-hire licence photos root-caused across 3 claim paths + fixed; Cicero's placeholder email explained + corrected; "You're ready for site" removed and deployed

## Built
- **Labour-hire licence photos.** Royce: "i got conor to login but his licenses dont load in cards," clarified to specifically photos, missing in both Shell and Cards, for Conor Horgan and Nelson Sareto. Traced live rather than assuming: the intake pipeline genuinely uploads every photo/PDF at intake time (files intact in `licence-photos/pending-credentials/{worker_id}/...`, correct byte counts) — nothing was lost. The gap was at claim time, in THREE independent places: this repo's `eq_cards_claim_invite` RPC, and eq-shell's `shell-join-tenant.ts`/`accept-invite.ts` (which had ported the same promotion logic on 2026-08-23 rather than sharing it, inheriting the identical gap). None of the three ever extracted the file reference into `photo_front_url`/`document_url`, and a raw path copy wouldn't have worked anyway — the storage RLS policy on `licence-photos` needs the licence's own id already in the file path, impossible for a file uploaded before that row exists.
- Built one shared fix instead of three: new edge function `promote-labour-hire-photo` (jvkn) does the actual copy-to-correct-path-and-repoint, idempotent, callable from all three claim paths and for backfill. eq-cards side: migration `0161` wires `eq_cards_claim_invite` to dispatch it via `pg_net`, fire-and-forget. eq-shell side (delegated to a background agent, full context + the mandatory live-deploy verification method): both `shell-join-tenant.ts` and `accept-invite.ts` call the same function with a bounded-await pattern (this repo has a documented Netlify-freezes-on-response issue with true fire-and-forget). eq-cards [PR #318](https://github.com/eq-solutions/eq-cards/pull/318), eq-shell [PR #1603](https://github.com/eq-solutions/eq-shell/pull/1603), both merged and confirmed live via exact commit-ancestry match.
- **Real CI catch, fixed before merge**: eq-cards' `check-function-grants.mjs` correctly flagged the first draft of migration 0161 — a `CREATE OR REPLACE FUNCTION` with no trailing `GRANT` would have been silently stripped to service_role-only by this repo's own grant-stripping event trigger on any future replay, regardless of what a live spot-check happened to show immediately after applying. Fixed by folding the grant into the same file (a separate follow-up migration doesn't satisfy the check by design — confirmed by trying that first and having CI reject it a second time with an explicit "in this same file" message).
- Backfilled Conor's and Nelson's 8 existing licences via direct calls to the new function (all succeeded, byte-exact). Branch protection required rebasing onto `main` twice mid-session (heavy concurrent commit traffic on eq-cards today).
- **Live-verified, not just deployed**: opened `core.eq.solutions/sks/staff` as Royce (already-authenticated Chrome session, no credentials entered), searched and opened both Conor's and Nelson's rows directly — both show real, rendering licence photos/PDF previews. This also closed out two pre-existing tracked open items in `eq/pending/eq-shell.md` from 2026-08-23 that were waiting on exactly this click-through.
- **Cicero's placeholder email, explained (not a bug) and corrected.** Royce, mid-tangent, asked about a worker's Field profile showing `93b24e16-...@cards.eq.solutions` as their email, expecting his real icloud address. Traced to eq-shell's `mint-cards-otp.ts`: a deliberate, documented fallback that synthesizes `{user_id}@cards.eq.solutions` when a phone-only-signup user has no real email, purely to give GoTrue something to key a magic-link on — confirmed working-as-designed (Royce himself verified this exact behaviour live 2026-08-03, Sentry EQ-SHELL-13). Cicero's `shell_control.users.email` had simply been null since signup (2026-07-01) — never overwritten, never lost. Royce supplied the real address; updated `shell_control.users.email` + `public.profiles.email` + `public.workers.email` directly (checked for a collision first). Deliberately did not hand-edit `auth.users`/`auth.identities` — confirmed via reading `ensureAuthUser`'s own logic that it self-heals the auth row automatically the next time he opens Cards.
- Spawned a background task (Royce started it running in a separate session before this one closed) for the systemic follow-up: stop the synthetic placeholder ever displaying as a real email, and add a non-blocking nudge encouraging phone-only-signup workers to add one.
- **"You're ready for site" removed outright, deployed live.** Royce: "we keep seeing that... get rid of it in eq cards." A 2026-08-19 fix (archived this session) had already addressed one specific recurrence cause (a Shell-iframe cold-boot race defeating the once-ever flag) but the banner itself remained. Rather than chase further recurrence causes, deleted the whole feature from `licences_list_screen.dart` — method, call site, SharedPreferences key, all dead state, verified no dangling references. eq-cards [PR #321](https://github.com/eq-solutions/eq-cards/pull/321), merged, then **explicitly deployed** on Royce's separate "deploy" instruction — confirmed the dispatched run's commit SHA (`dd199c8`) exactly matches the removal commit, both jobs (Flutter web build, edge functions) succeeded independently.

## Decided
- Royce (AskUserQuestion): "Fix + backfill now" for the eq-cards photo-promotion fix, over draft-only or fix-without-backfill.
- Royce (AskUserQuestion): "Fix all three, merge now" for the eq-shell side, pre-authorizing that merge/deploy explicitly — worth recording plainly since the background agent's own result came back flagged by an automated safety classifier for merging to eq-shell's main with "no visible authorization" in its own isolated transcript. It didn't see this conversation's actual approval; the merge was correctly authorized twice over (the AskUserQuestion answer, then Royce's own later "merge it once CI's green").
- Royce: confirmed Cicero's real email (`cicero.junior@icloud.com`).
- Royce: "we dont want that happening - we want to encourage people to fill in their email" — confirmed scope for the spawned placeholder-masking/nudge follow-up.
- Royce: "can you get rid of that in eq cards" then "deploy" — explicit, separate instructions for the removal and its deploy, kept as two distinct asks per the deploy non-negotiable rather than assuming one implied the other.

## Deferred
- Placeholder-email masking + real-email nudge for phone-only signups — spawned as background task, already running independently (Royce started it in a separate session before this session closed). Investigate whether PR #1125's existing email-capture nudge already covers part of this before building anything new.
- The one open item left in eq-shell's "resend-worker-invite" pending section (Nelson's specific duplicate-key-error banner hypothesis) — moot for Conor/Nelson themselves (both claimed successfully), but the resend button's own behaviour in that edge case was never directly re-tested. Left open, trimmed section down to just this item.
- eq-cards' own `[functions.promote-labour-hire-photo]` config.toml `verify_jwt` gap — already found and fully closed by a concurrent same-day session (PR #320, see this file's earlier "migration-ledger gap sweep" entry) before this segment reached close; not duplicated here.

## Notes
- **This worktree's `.git` link was severed mid-session** by the concurrent session logged earlier in this same file ("migration-ledger gap sweep...") — it found and deleted this session's branch/worktree after PR #318 merged, while this session was still actively using it. Caught by the `detect-fake-worktree` guard rather than silently walking up to and mutating the parent repo. Switched to a different dedicated worktree (`cards-remove-ready-for-site`) rather than attempting an in-place repair, per existing memory on this exact recurring pattern (now an 8th instance).
- **Substrate write contention, textbook case, twice over.** By the time this segment reached `/close`, the concurrent session above had already logged its own eq-cards.md entry referencing this session's own PR #318 and had already found and closed a real follow-up gap (config.toml, PR #320) in it. Read in full before writing, to add this session's substantive root-cause entry as a companion rather than a duplicate. Then, mid-`/close`, a THIRD concurrent push landed between this segment's first commit attempt and its rebase, moving/archiving a section this segment was cross-referencing by relative position ("directly below") — resolved by aborting the rebase, resetting to the new `origin/main` tip, and re-applying this segment's actual edits fresh against current content rather than fighting merge-conflict markers on prose.
- `eq-context`'s shared checkout was clean but 26 commits behind at close time; the F9 guard correctly blocked an in-place `git merge` there (has corrupted this checkout before per its own message) — used an isolated scratch clone instead, per the guard's own prescribed remedy, verified by reading content back before treating any pending-file content as current.
- Ticked/archived three eq-shell.md sections and one eq-cards.md section this close, all genuinely resolved by this session's own live verification (not assumed): the "Staff list showed None recorded" click-test, the "labour-hire licence promotion gap" claim-door click-tests, the "field-access unlock trigger" Conor/Nelson-still-unclaimed item, and (via full removal rather than further debugging) the 2026-08-19 "You're ready for site" recurrence fix.

---

# Session 2026-08-26 — Synthetic cards.eq.solutions email hidden across all three apps, merged + deployed live

## Built
- Investigated every place eq-shell's synthetic `${user.id}@cards.eq.solutions` placeholder (minted for phone-only-signup workers so a magic-link token has something to key on — GoTrue requirement, working as designed) could display as if it were a real email. 3 parallel Explore agents swept eq-cards, eq-shell and eq-field (29 read/display sites found); traced the full DB sync chain live (`auth.users` → `profiles.email` → `workers.email` → `app_data.staff.email`); confirmed live that `profiles`/`workers`/`shell_control.users` currently hold zero synthetic values (only `auth.users` does, 13 rows) — this was prevention, not data repair.
- Checked PR #1125's existing email-capture nudge before building anything new, as asked: confirmed merged, live, working — but scoped only to workers who log in through eq-shell. Cards' own standalone "personal wallet" signup path has no coverage at all.
- Presented findings + a concrete, scoped plan and held for explicit go-ahead before writing any code, per the task's own instruction.
- Implemented and shipped across all three repos: eq-cards (closed the one write-back path that could have made this a real data-corruption bug, plus a model-layer choke point covering 8 downstream screens at once, plus the workers-canonical-sync guard — moved into the correct repo after a mid-session misattribution was caught and corrected), eq-shell (session-response sanitization, both admin Users surfaces, two real outbound-email guards, a greeting-fallback hardening), eq-field (one row-mapper choke point covering every consumer).
- All three PRs merged — eq-cards [#322](https://github.com/eq-solutions/eq-cards/pull/322), eq-shell [#1604](https://github.com/eq-solutions/eq-shell/pull/1604), eq-field [#795](https://github.com/eq-solutions/eq-field/pull/795). eq-shell and eq-field deploys verified **live** via direct Netlify commit-ancestry checks against the actual merge SHA, not assumed from a green merge alone.

## Decided
- Royce: defer building a matching email-capture nudge for Cards' standalone signup path — PR #1125 already covers the population that matters most today (90/10 SKS focus).
- Royce: push branches and open PRs for review rather than hold as local commits.
- Royce: merge all three PRs, with eq-shell and eq-field confirmed individually (both auto-deploy on merge/push; eq-cards' `main` is gated).

## Deferred
- Standalone Cards personal-wallet email nudge — explicit defer, Royce's call, revisit if that signup path grows.
- eq-field's pre-existing `build-bundles.mjs --check`/`check-cache-busters.mjs` CI drift, found while verifying this fix, confirmed unrelated (reproduces on `origin/main` itself) — spun off as background task `task_bc389479`, already running in a separate session Royce started.

## Notes
- **This session is the direct fulfillment of the deferred item another concurrent session logged earlier in this same file** ("Placeholder-email masking + real-email nudge for phone-only signups... Investigate whether PR #1125's existing email-capture nudge already covers part of this before building anything new" — see above). Same underlying incident, same worker (Cicero Goncalves Da Silva Junior), same real email already confirmed by Royce (`cicero.junior@icloud.com`) in that earlier segment.
- eq-context's shared checkout was 14 commits behind at close time; the F9 guard correctly blocked an in-place `git pull` (not just `merge`) there. Used an isolated scratch clone per the guard's own prescribed remedy.
- Netlify deploy-state verification needed a cross-check to trust: the Netlify MCP's `get-deploy-for-site` returned an identical, stale `state`/`updated_at` twice in a row for the same in-flight deploy; the direct `netlify api getDeploy` CLI call is what actually showed fresh progression (`building` → `uploading` → `ready`). Worth remembering next time a live-deploy check matters — don't trust a single MCP read of in-flight state without a fresh cross-check.

---

# Session 2026-08-26 — Weekly Supervisor Digest moved into Manage → Email Templates (eq-field)

## Built
- Moved the digest's two ad-hoc panels (21-manager recipient checklist, 4-section on/off + intro editor) off the Supervision page into the existing Manage → Email Templates page as a new card, same governed surface the 3 leave emails already use.
- Re-gated from `reports.subscribe_digest` to `field.manage_email_templates` — verified first that manager/supervisor (the only two roles with digest access) already hold both keys at the same tier, so nobody's access changed.
- Added "Send test to myself" for the digest via a new Netlify function (`send-digest-test.js`): verifies the caller's session, resolves their own email server-side from `app_data.field_managers` (never client-supplied, since a real digest carries other people's operational data), then calls the cron-only `supervisor-digest` edge function scoped to exactly that one recipient. Reuses the existing `AUDIT_SB_KEY` (the ehow service-role key this Netlify site already holds) as the bearer `assertCronCaller` already accepts — no new shared secret provisioned.
- `supervisor-digest` edge function extended to accept an optional `testRecipient`, bypassing the `digest_opt_in` filter for that one send and using real computed data (not synthetic samples).
- eq-field [PR #791](https://github.com/eq-solutions/eq-field/pull/791), v3.5.573, squash-merged, confirmed live (`field.eq.solutions/sw.js`).

## Decided
- Royce (AskUserQuestion): full consolidation, not sections-only or hold-as-is.
- Royce (AskUserQuestion): build the test-send safety net now, not as a fast-follow.
- Royce: "merge" — explicit go, given after the deploy preview's checks were confirmed green.

## Deferred
- None new. (One inherited, unchanged limitation carried over from the existing 3-leave-template test-send: it can only preview the logged-in caller's own digest, not a different supervisor's — named in this session's `docs/reflection-log.md` entry as a real but out-of-scope gap, not something Royce asked for.)

## Notes
- **The 2026-08-16 "digest editability — on hold" pending item is now resolved**, not overridden silently: re-checked its own trigger condition (real edits to the 3 existing email templates) live before touching any code — still zero, 12 days on — surfaced that to Royce explicitly, and proceeded on his explicit go since he was the one raising it again. Archived to `eq/pending-archive.md` with the resolution noted.
- **Version-number collision, in the wild, exactly as this repo's own CLAUDE.md already warns about**: fetched `origin/main` at session start (free at v3.5.571), but a concurrent session merged its own genuine v3.5.571 (then a follow-up v3.5.572) before this PR was ready to merge. Caught by `gh pr view --json mergeable` showing `CONFLICTING` right before merge — rebased onto the new tip, renumbered to v3.5.573 throughout (comments, changelog banner, version stamps), regenerated `core-bundle-b1.js` (a bundle source, `lazy-loader.js`, had changed — caught by `build-bundles.mjs --check`, not assumed), bumped 2 cache-buster tags flagged by `check-cache-busters.mjs`, and added `reports.subscribe_digest` back to `tests/permission-enforcement-baseline.json`'s dead-keys list (its only call site was the panel this PR removes) after the permission-enforcement-drift test caught it as newly-dead. All of this only surfaced by actually running the repo's own CI-equivalent commands locally before pushing, not by reasoning about the diff.
- **`gh pr merge --delete-branch` hit the exact "main already checked out in a concurrent worktree" local-housekeeping failure this repo's `worktree-registry.md` already documents from PR #773** — confirmed the merge itself went through server-side (`gh pr view --json state,mergedAt` showed `MERGED`) before treating it as done; deleted the remote branch separately via `gh api`.
- `gh`'s stored GitHub token went invalid mid-session (unrelated to this work) and was fixed outside this session; retried PR creation successfully once `gh auth status` showed it healthy again.
- `eq-context`'s shared checkout was 16 commits behind at close time and its own `git merge` guard (F9) correctly blocked an in-place fast-forward — used an isolated scratch clone instead, per the guard's own prescribed remedy. Also caught that `eq/changelog/field.md` is retired (superseded by `eq-field.md`, per its own header) before appending to the wrong file.

---

# Session 2026-08-26 — edit-user.ts role-change audit trail + fail-loud on membership write failure

Fully-scoped brief from Royce: `edit-user.ts`'s `patch.role` handler was the only one of the file's four mutable-field handlers (role/active/phone/unlock_pin) with no `writeAuditLog` call, and the only one that swallowed its own write failure while still returning `ok:true`.

## Built
- Confirmed live against jvkn (`shell_control.user_tenant_memberships`, `shell_control.audit_log`) that the schema matched the code's assumptions before writing anything.
- `netlify/functions/edit-user.ts`: `user_tenant_memberships.role` write failure now returns `500 {ok:false, error:'server-error'}` instead of logging a warning and continuing. On a genuine role change, writes `writeAuditLog({event:'user.role_changed', detail:{previous_role, next_role}})`, mirroring the `active`/`phone` handlers' exact shape (same repeated 3-line `ip` derivation, no shared helper — matched the file's existing style rather than refactoring it).
- `npx tsc -b --force` clean, `npx eslint netlify/functions/edit-user.ts` 0 errors (4 pre-existing "unused eslint-disable" warnings, same pattern already present at the file's other 3 write sites, unrelated to this change).
- [eq-shell PR #1607](https://github.com/eq-solutions/eq-shell/pull/1607), squash-merged (`979aa9c7`). Deploy verified the hard way: polled the Netlify deploy record to `state:"ready"` + `published_at` (318s build — this repo's usual "merge-to-live in 2-4s" pattern didn't hold this time, just a slower build, not an error) and confirmed the deploy's own `commit_ref` was the merge commit itself, then live-smoke-tested `verify-shell-session` (401 as expected, `Age: 1`).
- Worktree (`C:\Projects\eq-shell-role-audit`) and the merged branch (`claude/edit-user-role-audit-log`, local + remote) both removed on request.

## Decided
- Royce: "merge it" — explicit deploy approval (this repo's merge-to-main auto-deploys).
- Royce: "clean up the worktree", then "delete the branch too" — sequential explicit cleanup instructions.

## Deferred
- Not click-tested live — no live Shell session/credentials in this environment. Logged in `eq/pending/eq-shell.md`.

## Notes
- **Root checkout avoided, correctly.** `/brief eq-shell` surfaced the default checkout sitting 143 commits behind `origin/main` on an unrelated branch (`claude/platform-console-cards-core-reconcile`) with another session's uncommitted work in it (`entity-patch.ts`, `AdminDocumentUpload.tsx`, 2 untracked migrations). Not touched. Worth noting the nuance: the repo-level staleness (143 commits) did NOT mean the specific target file was stale — `git diff HEAD origin/main -- netlify/functions/edit-user.ts` came back empty, so the file itself was current. Built from an isolated worktree anyway, since the branch's identity and dirty state were wrong to commit onto regardless of this file's own freshness.
- **Duplicate-work check paid for itself even when it came back negative.** A local branch named `claude/user-role-change-check-cbae09` looked like it might already contain this exact fix — checked before writing anything (`git diff origin/main...claude/user-role-change-check-cbae09 -- netlify/functions/edit-user.ts`, empty; `git log`, empty) and it turned out to be an unused, unrelated branch. Cheap to check, would have been expensive to skip.
- **`git worktree remove` reported success and unregistered the worktree, but left `node_modules/` and `eq-intake/` behind on disk** (both untracked — `pnpm install` + `pnpm run build:packages` output from this session's own typecheck verification). No error surfaced from the remove command itself; only a follow-up `test -d` check caught it. Force-deleted the remainder by hand. Same failure class as this file's "Migration-ledger gap sweep" segment (`conor-licenses-cards-84ecf5` worktree orphaned on disk after removal, further above in this file) and eq-cards' documented `remove-wedge-alert-0da7e2` memory — a third instance, now clearly a recurring class on this machine rather than a one-off, most likely Windows file-locking on deeply-nested `node_modules` right after a build/install ran in that same directory. Worth always following a `git worktree remove` with an existence check before calling cleanup done.
- **Self-inflicted tooling mistake, worth remembering:** used `ScheduleWakeup` (built for `/loop` dynamic-interval pacing) to wait on a one-off background `pnpm install`, instead of just letting the Bash tool's own background-task notification drive the next step. The scheduled wakeup fired ~3 minutes later, re-submitting its queued prompt as if it were a fresh user message — by then the real notification had already arrived and the work was done, merged, and deployed, so it read as a confusing, stale instruction to redo finished work. Recognised and treated as non-authoritative rather than acted on. Don't reach for `ScheduleWakeup` outside actual `/loop` sessions — for a single background command, its own `run_in_background` notification is the whole mechanism.
- **Substrate write hit the same contention this file documents repeatedly today, twice over.** First push rejected (non-fast-forward); the rebase then conflicted against a concurrent session's own close (all three touched files, against the "Synthetic cards.eq.solutions email" segment above); resolved via `rebase --abort` + `reset --hard origin/main` + re-apply. Retried, rejected again; second rebase conflicted on just this session-log file (this time against the "Weekly Supervisor Digest" eq-field segment above); resolved the same proven way. Two full abort/reset/reapply cycles in one close, both against genuinely different concurrent sessions — not a one-off today.

# Session 2026-08-26 (eq-field) — field_job_number_overrides migration-parity task: built, then closed as duplicate of a concurrent PR

Direct task from Royce: write an on-disk migration file for `public.field_job_number_overrides`
(live on ehow since 2026-07-05, never had a repo migration — pure ledger-parity, no live DB
change intended).

## Built
- Isolated into a worktree (`eq-field-job-number-overrides-wt`, branch `claude/job-number-overrides-reconcile`), pulled the live schema/RLS/grants fresh from ehow via the Supabase MCP (not reconstructed from client code), wrote the migration file, opened eq-field PR #799.
- Registered the worktree in `eq-context/system/worktree-registry.md`'s Active section per its own documented protocol — the first row added there in a long while; the table was found empty despite 23 other concurrent eq-field worktrees existing at the same moment, none registered.
- PR #799 came back `mergeStateStatus: DIRTY` against `main`. Checked why rather than assuming a normal conflict: eq-field PR #798 (authored by Royce's own GitHub account, a different concurrent session) had merged minutes earlier — 09:11 UTC — doing the identical task against the same table. Independent verification value: both sessions pulled live ehow schema separately and landed on functionally equivalent DDL/RLS/grants.
- Presented the finding and three options via AskUserQuestion; Royce chose "close #799, clean up now." Closed PR #799 with a comment pointing to #798, removed the worktree and both local/remote branches, and updated the eq-context registry row to record the real outcome (superseded, not merged) rather than leaving a stale "OPEN" row.

## Decided
- Royce (via AskUserQuestion): close #799 as superseded by #798 and clean up immediately, rather than leave it open or rebase it down to a smaller diff.

## Deferred
- None — the underlying documentation-parity gap is fully closed (by #798); nothing left open on this thread.

## Notes
- **The worktree registry's "Active (do not touch)" section is aspirational, not actually followed.** Before this session it held zero rows while `git worktree list` on eq-field showed 23 checked-out worktrees. PR #798's own worktree (`.claude/worktrees/infallible-benz-f958af`, branch `claude/field-job-number-overrides-migration`) was never registered either — so even a by-the-book registry check before starting would not have caught this specific collision. The registry shouldn't be trusted as a real coordination mechanism until adoption is closer to universal; `mergeStateStatus: DIRTY` on a fresh, otherwise-unremarkable PR turned out to be the actual signal that caught this one.
- Session-start gate reported the shared `eq-context` checkout progressively further behind `origin/main` through the session (4 → 11 → 22 commits) — active concurrent write traffic throughout, consistent with every other segment in this file today. All three substrate writes this session (register, then prune, then this log) went through fresh isolated scratch clones rather than the shared checkout, each re-fetched immediately before push; one of the three (the initial registry registration) hit a real non-fast-forward race (an unrelated nightly-digest bot commit) and was resolved by rebase-and-retry, confirmed conflict-free by checking the intervening commit's changed paths before rebasing rather than assuming.

---

# Session 2026-08-26 — eq-field CI-drift ruled out (stale ref); `/decide` surfaces SEC-1's un-mechanized de-escalation

## Built
- Nothing shipped — investigation plus an advisory `/decide` pass, no product code touched.

## Decided
- Nothing new confirmed by Royce this segment — the `/decide` pass ended on a question for him, not yet answered.

## Deferred
- SEC-1 digest de-escalation convention — see `ops/pending.md` (added this segment). Needs Royce's steer: build a dated de-escalation token, leave the nag deliberately blunt on purpose, or just hand-adjust today's display.
- eq-field local branch `fix/hide-synthetic-cards-email` (commit `5293989`) on Royce's own machine — redundant, never pushed; its content is already live via PR #795 (built by the concurrent session logged in this file's "Synthetic cards.eq.solutions email" segment above). Safe to abandon/delete locally next time that checkout is open; not touched here since another session may still have it checked out.

## Notes
- Investigated a reported eq-field CI-guard failure (`build-bundles.mjs --check` + `check-cache-busters.mjs --base origin/main`, both said to fail against `origin/main` itself). Dispatched a bug-fixer agent, then independently re-verified in a separate scratch worktree: both checks pass clean on current `origin/main` (`8f61dd59`). This corroborates rather than duplicates this file's own "Synthetic cards.eq.solutions email" segment above, which already hit the identical drift and spun it off as background task `task_bc389479` (already running, Royce-started) — most likely a stale local `origin/main` ref during a high-merge-volume window, not genuine unfixed drift. Incidentally confirmed one real, narrow gap: `cache-buster-drift.yml` triggers on `pull_request` only, no `push: branches: [main]`, unlike its sibling `build-bundles-drift.yml` — worth folding into `task_bc389479` if that task doesn't already cover it; not spawned as a separate competing task.
- Ran `/decide anything worth doing` (no target given) — chose to triage SEC-1 as the single highest-severity open gate item. Near-miss caught before acting on it: a private memory from 2026-08-23 reused the label "SEC-1" for an unrelated, already-fully-closed ehow RLS cluster (see this file's 2026-08-23 entries) — the canonical `ops/security-register.md` SEC-1 is the older, separate sks-nsw-labour finding. Corrected the memory rather than triaging the wrong item. The real SEC-1 is a standing, deliberately-parked decision (decommission-at-cutover, reaffirmed 2026-06-05/07-16/07-20, de-escalated-from-nagging 2026-08-21) — but the digest generator never got a way to encode that de-escalation, so it's fired as an identical top-line P0 across at least 4 sessions since (08-23 through this one). Full `/decide` verdict recorded in `ops/pending.md`; crux is genuinely Royce's call, not a clear-cut fix.
- Substrate writes for this close went through a fresh isolated scratch clone, not the shared `C:\Projects\eq-context` checkout — session-start gate reported it progressively further behind `origin/main` (11 → 22 commits) over the course of this segment, consistent with every other same-day session in this file citing heavy concurrent write traffic.

---

# Session 2026-08-26 — eq-cards RLS/FK-index hardening (0162) shipped; 0131 header found stale, corrected without touching the ledger file

## Built
- Migration 0162: reapplied the 2026-05-31 RLS-initplan/FK-index fix to 8 policies + 15 foreign keys created or recreated since (incl. `workers.origin_org_id` from 0138), plus hardened `eq_format_au_mobile`'s search_path. [PR #323](https://github.com/eq-solutions/eq-cards/pull/323), merged, applied to live jvkn, verified clean via a fresh advisor scan (all 8 `auth_rls_initplan` + all 15 unindexed-FK warnings gone).

## Decided
- Royce: apply migration 0131 if still pending — turned out moot, see Notes.
- Royce: don't fold the wallet info-density rebuild into this sprint — stays parked while overseas.
- Royce: apply migration 0162 to live jvkn — taken, after the auto-mode classifier blocked the first attempt (same gate that caught 0138 earlier today).

## Deferred
- `organisations_anon_bootstrap_read` (unconditional anon SELECT on the whole `organisations` table) and 3 tables with overlapping permissive SELECT policies (`organisations`, `org_credential_requirements`, `org_join_requirements`) — found while scoping 0162, deliberately excluded (policy-logic judgment call, not a mechanical rewrite). See `eq/pending/eq-cards.md`.

## Notes
- Migration 0131 ("gate worker role assignment") has actually been live since 2026-08-16 (ledger: `gate_worker_role_assignment`, version `20260816091039`) — its file's header comment wrongly said "NOT YET APPLIED" the whole time, surfaced only because this segment's own earlier ratings pass had cited it as an open Security blocker. Started fixing the header comment, but CI (`check-function-grants.mjs`) correctly flagged that touching an already-applied migration's `CREATE OR REPLACE FUNCTION` statements looks like a fresh redefine with no grant statement — the exact bug pattern that broke these two functions' `authenticated` grant the first time 0131 actually ran (restored later by #295/#296). Reverted the header edit rather than force it through a real gate; the correction lives in the changelog instead of the ledger file. Lesson: an already-applied migration's file isn't safe to edit even cosmetically without checking whether this repo's grant-preservation CI treats any diff to it as a fresh redefine.
- Session-start gate on the shared `C:\Projects\eq-context` checkout reported it progressively further behind `origin/main` over this segment (4 → 22 commits) — all substrate writes here went through direct GitHub API calls against `origin/main` instead (fetch current sha, modify locally, PUT with that sha), never the stale local clone.
- The eq-cards worktree this session started in (`remove-wedge-alert-0da7e2`) had no `.git` of its own (a known recurring pattern) — resolved via `EnterWorktree` rather than repairing it in place, per standing guidance.

---

# Session 2026-08-26 (eq-shell) — staff/shell active-sync reverse direction: found, fixed, live-verified (PR #1608)

Handed a specific research question: is PR #1550's staff-active drift healer
direction-agnostic (would it catch a Shell login deactivated via Admin > Users
while the linked staff record stays active), given live evidence it wasn't
catching Mark Brame's case (SKS). Investigated rather than assumed.

## Built
- Corrected eq-shell's own stale memory first: PR #1550 was already MERGED
  (2026-08-23T08:14:09Z, commit `6e7d0692`) and live for 3 days — both the task
  brief and this repo's memory store still said "OPEN, not merged."
- Confirmed via code (not just the PR's own description) that both of #1550's
  layers — `entity-patch.ts`'s sync hook and `check-shell-staff-active-drift.mjs`
  — check staff→shell only. `edit-user.ts` (the real Admin > Users deactivate
  path) never touches `app_data.staff` at all; the drift script's own
  `WHERE active = false` predicate structurally excludes the reverse shape
  before the Shell side is ever read.
- Asked Royce the product question directly (cascade / alert-only / keep
  independent / not a priority) rather than guessing — he chose alert-only.
- Built and shipped [PR #1608](https://github.com/eq-solutions/eq-shell/pull/1608):
  a second, independent detect-only query in `check-shell-staff-active-drift.mjs`
  (control-plane deactivated logins → still-active linked staff, either tenant
  plane), surfaced via the existing 15-min sweep's `[Security]` GitHub issue.
  No write path added — `entity-patch.ts`'s forward hook and the forward
  remediation loop are untouched.
- Merged (explicit go confirmed via AskUserQuestion after the instruction's
  phrasing was ambiguous), deploy verified live via commit-ancestry
  (`a5c9a1b2`, Netlify `ready`, production). Dispatched the workflow by hand
  in `dry_run=true` afterward at Royce's request — ran clean against real
  production data, zero errors, correctly found nothing (the live case had
  already self-corrected hours earlier) and correctly left the GitHub issue
  untouched.

## Decided
- Royce: reverse-direction drift is detect + alert only — do not auto-archive
  `app_data.staff` from a Shell-side login deactivation. Archiving has a much
  bigger blast radius (rosters, dispatch, Field's write-guard triggers,
  compliance-pack/licence visibility) than logging someone out.
- Royce: merge PR #1608 now (confirmed explicitly after clarifying an
  ambiguous "once you give the go-ahead" instruction — treated as Royce
  authorizing the merge in that message, not delegating merge authority
  generally).

## Deferred
- **Unidentified writer**: Mark Brame's `app_data.staff.active` flipped to
  `false` on its own at 2026-08-26 05:10 UTC, hours before this was
  investigated — ruled out PR #1550's script (never writes `app_data.staff`)
  and `staff-shell-login-timestamp-sync.yml` (only syncs `last_login_at`, read
  it directly). Tagged `source: 'system'` in the tenant audit log, a single-row
  write, not the ~93-row nightly batch that touches most staff rows around
  02:35 UTC most days. Didn't chase into eq-cards (no matching workflow name
  there either — if it's `workers-canonical-sync` it likely runs as a Netlify
  scheduled function, not a GH Action, unconfirmed). Both sides are consistent
  now, so nothing urgent — worth a follow-up look at what that job actually is.

## Notes
- Session gate (`/brief`) triggered mid-task by the repo's own `eq-guard`
  brief-gate hook on the first Edit attempt — ran it properly rather than
  setting `EQ_SKIP_BRIEF=1`. Caught a useful fact in the process: a dead local
  branch `worktree-staff-shell-active-sync` (leftover from building #1550
  itself, one commit ahead of its squash-merge, no live worktree attached) —
  confirmed harmless, left alone.
- Caught and fixed a real bug in the new script before committing: the first
  draft gated the new reverse query on the FORWARD query's own `controlError`
  flag, which would have silently reported a false "clean" (empty
  `reverse_drift`) if the forward control-plane query ever failed, instead of
  surfacing the actual error. General lesson: don't reuse one independent
  check's error flag to gate a different independent check.
- Used an isolated `git worktree add` (plain sibling dir, no `-wt` suffix — the
  suffix trips a `detect-fake-worktree` guard on the `add` itself) off clean
  `origin/main` for the eq-shell fix, since the root checkout had unrelated
  uncommitted work on a different branch. Removed after push.
- This close's own substrate writes went through an isolated scratch clone,
  not the shared checkout — `git pull` in the shared eq-context checkout is
  now hard-blocked by an F9 guard hook (pre_tool_use.py) that fires even on
  a clean fast-forward; clone fresh instead.

---

# Session 2026-08-26 — Contacts off-roster default-visibility fix (v3.5.577)

## Built
- eq-field PR #800 (v3.5.577): Field's Contacts "On Roster" filter now defaults to checked, so off-roster people (e.g. Mitchell Forsyth, Subcontractor) no longer sit visually indistinguishable from active people in the default list view. Weekly Roster/Edit Roster/Timesheets already excluded them correctly — this was Contacts-only.
- Same pass: fixed the filter's own `on_roster` check from opt-IN (`=== true`) to opt-OUT (`!== false`), matching the convention every other consumer in the codebase already uses. Without this, defaulting the checkbox on would have emptied the Contacts list entirely on the SEED/demo tenant (no `on_roster` field in that data at all).

## Decided
- Royce confirmed via AskUserQuestion: default-hide off-roster people in Contacts, rather than (a) just adding a visual badge while still showing everyone, or (b) it turning out to be a different screen (Weekly/Edit Roster) entirely. Verbatim: "Hide him if shell says off roster - no need to confuse things with inactive people."
- Royce confirmed merge explicitly ("merge it") after CI + deploy preview went green.

## Deferred
- None — single, fully-scoped fix, built and shipped same session.

## Notes
- Root-caused via live Supabase query before writing any code: Mitchell's canonical record on ehow (`app_data.staff`) has `employment_type=Subcontractor`, `on_roster=false` — Shell's display was accurate. Also found (unrelated, cosmetic) his surname is stored as "Forsyrh" in that table, not "Forsyth" — not fixed this session, out of scope.
- The two screenshots in the bug report were from different apps (Shell's Staff/Users admin, Field's Contacts) — confirmed which Field screen was pictured by fingerprinting its exact badge-rendering helper functions (`_cGroupBadge`/`_missingRequiredBadge`/`_personActions`, unique to `people.js`) rather than assuming from layout alone.
- Real merge conflict hit and resolved: `main` picked up an unrelated commit (#798) mid-session that happened to touch the same `docs/reflection-log.md` tail my own reflection-gate entries landed in. Rebased, resolved by keeping both entries in landing order (same resolution pattern this file already documents twice for prior sessions), re-ran the full local verification suite post-rebase before force-pushing.
- `gh pr merge --squash --delete-branch` partially failed on the branch-delete step — `'main' is already used by worktree at .../eq-field-apprentice-scope-race-wt` (a concurrent session had `main` checked out elsewhere in the shared clone). Same exact failure shape already recorded in this file for PR #796 earlier today. Confirmed the squash-merge itself succeeded via `gh pr view --json state,mergedAt,mergeCommit` rather than assuming failure from the local error, then cleaned up the remote branch with a plain `git push origin --delete` (no local checkout needed).
- Deploy verification didn't stop at `sw.js` matching the new version string — cross-checked via the Netlify API (`listSiteDeploys`) that the live production deploy's `commit_ref` was the actual merge commit (`f05ea5f6`), polling deploy state until `ready` rather than assuming a plain `curl` hit was already the new build.

---

# Session 2026-08-26 — sks-nsw-labour vs EQ Field account reconciliation

## Built
- Reconciled Royce's uploaded `EQ_People.csv` (Downloads) against EQ Field's live SKS-tenant people list (`app_data.field_people`+`field_people_removed`, ehow) — turned out the CSV was an unlabelled two-part export of sks-nsw-labour's own `people` (84) + `managers` (21) tables pasted together, confirmed by exact 1:1 name match.
- Built a live, stronger reconciliation directly against sks-nsw-labour's own Supabase project instead of the CSV: matched on canonical worker ID (`people.canonical_id` = `field_people.worker_id`) first, then phone, then email. Delivered as a colour-coded Excel workbook (green/yellow/orange/red by account status), 96 unique people, sortable/filterable.
- No code or DB changes — this was entirely read-only investigation + a spreadsheet deliverable.

## Decided
- Royce: the 3 people active in sks-nsw-labour but removed/archived in EQ Field (Dean Francis, Mark Brame, Matthew Khreich) have all left SKS — sks-nsw-labour's own `archived` flag is just stale for them, not a real gap.
- Royce: will re-add Mark Brame to EQ Field himself, once he trusts EQ Field's security/permission groups. Deliberately held, no timeline, not delegated.
- Royce accepted "an active EQ Field record will eventually get logged into" as good enough for now — the 16 people with a Field record but no linked login yet need no action.

## Deferred
- Mark Brame's EQ Field re-add — Royce's own action, gated on trusting the security/permission model, no date. _(added 2026-08-26)_

## Notes
- Matching on canonical worker ID + phone + email (rather than name alone) automatically resolved every nickname/full-name variant between the two systems — Bruno Pedrosa/Bruno Vita Pedrosa, Cicero Junior/Cicero Goncalves Da Silva Junior, Benjamen Ritchie/Ben Ritchie, Matt Miller/Matthew Miller, and Koos Otto/Johannes Otto (confirmed via matching phone + email, not a guess) — with no manual alias list needed.
- **Open question for a future session, not resolved this session:** only 16 of 74 active `app_data.field_people` (ehow) rows have no linked login (`user_id IS NULL`) today. That reads like very good news against `eq/pending`'s tracked "38 of 99" / "44-47" unlinked-staff backlog (2026-08-16/19, see eq-context memory `project_worker_identity_mobile_login`) — but that backlog is measured against `app_data.staff.user_id`, a different table with a different total population (99-107 vs 74 here). Did not check whether `field_people` is a subset of `staff` or a differently-scoped view. Don't assume the backlog shrank from ~45 to 16 without reconciling that relationship first.
- LibreOffice recalc (`scripts/recalc.py`, xlsx skill) fails in this Windows sandbox (`module 'socket' has no attribute 'AF_UNIX'`) — worked around by shipping the workbook with zero formulas (pre-verified hardcoded summary counts) rather than an unverifiable `COUNTIF`. Worth knowing for any future xlsx-skill use from a Windows session.
- `git merge` is hook-blocked in the shared `C:\Projects\eq-context` checkout (guard F9) — this close was run from an isolated clone per the hook's own printed instructions, not the shared root.

---

# Session 2026-08-26 — Labour-hire offboarding: "Remove from roster" doesn't touch Cards/Shell connection records

## Built
- Nothing — read-only architecture verification, no code changed.

## Decided
- Nothing new decided; reported as a finding for Royce's future call on whether it needs fixing.

## Deferred
- Whether "Remove from roster" should cascade to `org_memberships`/`org_access_requests`/`shell_control.user_tenant_memberships` — open decision, not yet made. See `eq/pending/cross-repo.md`.

## Notes
- Prompted by Royce's own architecture question: what happens to a labour-hire worker's Cards access and Shell login when the tenant removes them from the roster. Traced Cards' Connections UI to `org_access_requests` (not raw `org_memberships`) via `connections_repository.dart`, then verified live against a real case rather than reasoning from code alone.
- Real test case: a worker pulled off SKS's EQ Field roster this morning (`app_data.staff.active` → `false` on ehow, 05:10:15). Checked three separate jvkn/shell-side tables that actually govern what the worker and Shell see — all three still showed the pre-removal state, none touched by the roster removal: `org_memberships.status` (still `active`), `org_access_requests.status` (still `approved`), `shell_control.user_tenant_memberships.active` (still `true`).
- Practical read: the worker's own Cards app still displays "connected" to SKS, and the Shell/Core login gate for that tenant workspace is still open — "Remove from roster" only ever touched the tenant's own employment record (`app_data.staff`), never the cross-database identity/connection layer. Nobody has decided whether it should.
- The test case turned out to be the same person as the "sks-nsw-labour vs EQ Field account reconciliation" session above in this file — Mark Brame. That session's independent finding (Royce plans to re-add him to EQ Field once he trusts its permission groups) confirms his removal was a real, known, deliberate state, not test noise — but doesn't change the architecture gap found here, which is about the mechanics of the removal action itself, not this particular person's status.
- This is this session's second, independent thread — the first (synthetic `@cards.eq.solutions` placeholder email leaking into worker-facing/admin UI) was already closed out earlier today; see this file's earlier 2026-08-26 entry above.

---

# Session 2026-08-26 (eq-shell) — root-caused what flipped Mark Brame's staff record (no bug: Royce, via EQ Field)

Direct follow-up to this file's own earlier "staff/shell active-sync reverse direction" segment above, which left one open thread: Mark Brame's `app_data.staff.active` had flipped to `false` hours before that investigation, via an unidentified `source: 'system'` writer. Asked to find out what actually changed it.

## Built
- Nothing shipped — pure investigation, no code or DB changes.

## Decided
- Nothing new needed Royce's call this segment — fully resolved without one.

## Deferred
- Fixing Field's audit-attribution gap (see Notes) — spun off as background task `task_66de20f0`, started independently by Royce, running in a separate session as of this close.

## Notes
- **Root cause, confirmed conclusively**: `app_data.field_people_iud()` (ehow) recomputes `active = NOT NEW.archived` on every Field-side edit to a person's row, gated to `field.manage_people` (manager/supervisor). EQ Field's "Remove from roster" action sends `archived: true`, which fired this. Identified the actor as **Royce Milmlow** via `app_data.staff.updated_by` (a real column value) — not from the generic `app_data.audit_log`, which showed `source: 'system'`, `actor_id: null` for this write.
- **Real, separate finding surfaced along the way**: Field's PostgREST write path never sets the `x-eq-actor` header `app_data.fn_audit()` looks for, so every Field-driven staff write is anonymous in the generic audit trail — this one was only attributable because `updated_by` happened to be populated by some other mechanism (not yet identified which trigger sets it). Filed as `task_66de20f0` rather than fixed inline — a design question (fix the trigger's fallback vs. fix Field's client), not a one-line patch, and out of scope for what was asked.
- **Ruled out, with evidence, before finding the real cause** (worth remembering the elimination path, not just the answer): PR #1550/#1608's own scripts (neither writes `app_data.staff`, confirmed by reading both), the unrelated `staff-shell-login-timestamp-sync.yml` workflow (only syncs `last_login_at`), ehow's `quality-guardian` pg_cron job (17:00 UTC daily — reads licences/health-scores/orphans only, never writes `staff`), and jvkn's `workers-canonical-sync` edge function (v22 — fetched its actually-deployed source directly, confirmed byte-identical to the eq-cards repo; its merge branch for an existing matched worker *structurally cannot* write `active`, by design, since two earlier incidents — PRs #993 and #293 — deliberately removed that capability; only its DELETE and brand-new-INSERT branches touch `active` at all, neither applicable to an existing, non-deleted worker).
- **Independent corroboration, found after the fact**: a concurrent session's own entry earlier in this file (the "sks-nsw-labour reconciliation" segment above) recorded Royce independently telling them Mark Brame is one of 3 people "active in sks-nsw-labour but removed/archived in EQ Field" who have genuinely left SKS — confirming this session's technical finding (Royce archived him deliberately) matches Royce's own stated understanding, from an unrelated conversation, same day. Not contradictory with that segment's separate note about Royce planning to "re-add Mark Brame to EQ Field himself" later — read together, the most consistent picture is a deliberate archive now with a possible future re-onboarding under a different arrangement, not an accidental or disputed removal.
- Investigation was entirely live-system reads (Supabase MCP against ehow + jvkn, GitHub API against eq-cards/eq-shell) — no repo code touched.

---

# Session 2026-08-26 (eq-shell) — Merge-burst check (expected, no action) + F9's digest false-positive actually closed this time (`confirmed_in`, not just narrated)

Royce asked, while merging eq-shell PR #1610, whether the concurrent-merge activity he was seeing (#1611/#1610/#1612 within ~3 minutes, one Netlify production deploy coming back `Skipped`) meant sessions should coordinate merges more. Separately asked to dismiss the gate's "F9 possibly recurred" flag as a false positive.

## Built
- Closed the F9 false-positive gap for real: added `sessions/2026-08-26.md` to F9's `confirmed_in` list in `system/failures.md` (joining the 3 dates already there) and bumped the file's `last_updated`. Did not touch `last_seen`/`recurrences` — nothing here indicates an actual recurrence, and per this file's own stated design that bump stays a human call regardless.

## Decided
- Royce: "dismiss the F9 guard-bypass flag as false positive."

## Deferred
- None new.

## Notes
- **Merge-burst question, answered from GitHub + Netlify directly, not from eq-context alone** (its shared local clone was 36-37 commits behind all session). Confirmed the burst is real — Netlify's deploy-preview timestamps show the three PR branches being pushed within under 2 minutes of each other, *before* the merges, so it's genuinely 3 concurrent sessions finishing around the same moment, not one session merging three ready PRs back to back. Checked it against today's own `sessions/2026-08-26.md`, which already carried 10+ segments from concurrent sessions across eq-shell/eq-field/eq-cards before this one — this is this repo's normal current load, not an anomaly localized to one 10-minute window. No process change recommended: the suite already has purpose-built tooling for it (F9's guard on the eq-context side; this repo's own ancestry-verified-deploy convention on the eq-shell side), and both were doing exactly their job.
- **Root-caused the false-positive mechanism before touching anything** — read `.github/scripts/refresh_digest.py`'s `failure_recurrence_signals()` directly rather than assuming. It scans `sessions/*.md` files dated after a failure's `last_seen` for that failure's `signal` regex, and skips only files already listed in `confirmed_in` — nothing else suppresses a hit. A session's own Notes *explaining* the flag is false (which necessarily quotes the failure's title/signal wording to do so) reads to the scanner exactly like a fresh recurrence, and re-fires on every subsequent digest run until someone actually edits `confirmed_in`. Narration alone was never going to stop this, which is presumably why it was still firing today despite several earlier sessions in this same file already writing "false positive, not a new incident" into their own Notes (see this file's "index-drift cron fixed" segment, and the very first segment at the top of this file) — none of them had closed the loop on the ledger itself.
- **This is at least the third time this exact shape has needed a manual `confirmed_in` fix** — the function's own docstring cites F1 (2026-07-26) as the incident that motivated building `confirmed_in` in the first place, and F9 already had 3 prior dates in the list before today's addition. Didn't touch the scanner itself this session (out of scope, not asked) — but if a rung-4 detector keeps needing a human to notice and hand-edit a YAML list every time its own guard's success gets narrated in prose, that's arguably still a rung-2 remedy wearing a rung-4 badge. Worth a look if it recurs a fourth time: e.g. recognizing an explicit dismissal marker in the session log itself, rather than relying on someone finding this exact function again.
- **New MSYS/Git-Bash gotcha, distinct from the existing backslash-`-C` one already in eq-shell's own memory store**: `git show <ref>:<path>` silently mis-tokenizes when `<path>` has more than one `/`-separated segment — MSYS's automatic path-conversion reads the whole `ref:path` argument as if it were a colon-separated PATH-style list and rewrites it slashes→backslashes, `:`→`;` (`origin/main:.github/scripts/x.py` → `origin\main;.github\scripts\x.py`), which git then rejects as an ambiguous revision. Reproduced on `.github/scripts/refresh_digest.py` and `hooks/pre_tool_use.py` — not limited to dotfiles, a single-segment path like `origin/main:digest.md` is unaffected. Fix: `MSYS_NO_PATHCONV=1` on the call — but that also kills the (separately legitimate) `/c/Projects/...`→Windows-path conversion `-C` relies on, so pair it with a native-form `-C "C:/Projects/eq-context"` rather than the POSIX `-C /c/Projects/eq-context` form, or the `-C` itself starts failing instead.
- Substrate write went through a fresh isolated clone (`gh repo clone`, scratchpad), not the shared checkout, consistent with the concurrency finding above and F9's own guard.

---

# Session 2026-08-26 — Job-numbers Ops→Field link verified, 7-week eq-field/eq-shell migration drift found and closed

## Built
- Verified live that Field's Job Numbers board reads status straight from EQ Ops's `app_data.quote.status` (mapped: invoiced→dropped from board, complete→"Complete", on_hold→"On Hold", else→"Active"). Working as intended; currently reads as mostly binary in practice because Ops's live statuses today don't populate the Complete/On Hold branches.
- Found eq-field's own committed migration for `app_data.field_job_numbers_src()` had been silently wrong for 7 weeks: eq-shell's Ops pipeline renamed the column it reads (`workbench_job_no`→`job_number`, 2026-07-12) and added an invoiced-quote auto-exclude, neither ever committed to eq-field. Fixed and applied (documentation parity, zero behavior change — verified byte-identical before/after). eq-field PR #796, merged.
- Found and fixed a second, distinct gap in the same area: `public.field_job_number_overrides` (the retire/restore table) had zero migration file in eq-field despite being live since 2026-07-05. Recorded it. eq-field PR #798, merged.
- Registered `field_job_numbers_src` in eq-field's `tests/migration-shared-fn-guard.test.js` shared-function registry, and (via a coordinated peer session) in eq-context's `IDENTITY-MODEL.md` §3.3.3 — commit `ae0033f2`. This closes the SECOND confirmed instance of the eq-field/eq-shell two-pipeline drift pattern (first was `field_people_iud`, 2026-08-23/24); the registry built specifically to catch this had missed this second instance for 2 days.
- Verified both merges are live in production on `field.eq.solutions` via Netlify's own API — exact `commit_ref` match on the live production deploy, not just timing.

## Decided
- Royce: reconcile eq-field's migration file to match live reality (via AskUserQuestion, chose the plain-reconcile option over also widening the status-detail mapping).
- Royce: "merge it" for both PR #796 and PR #798, explicit per-PR as usual.
- Royce: "start task_0ef5369f" — done directly in the same session rather than spun off separately, since the session was already mid-thread on the exact topic.

## Deferred
- Widening the Job Numbers board's status mapping so Ops's `won-job-created`/`po-matched`/`draft` render as distinct statuses instead of all reading "Active" — offered, Royce didn't take it. Logged in `eq/pending/eq-field.md`.

## Notes
- **`gh pr merge --delete-branch` local cleanup reliably fails on this repo right now** when a *different* concurrent session has `main` checked out in another worktree of the same shared clone (`fatal: 'main' is already used by worktree at ...`) — hit twice, identical error, both times on otherwise-clean merges. The squash-merge itself always succeeds server-side regardless (confirmed via `gh pr view --json state,mergedAt,mergeCommit` both times) — only the local branch-delete step fails. Fix: `git push origin --delete <branch>` directly (a plain remote delete, no local checkout needed) rather than leaving the branch dangling.
- **eq-field's Netlify deploys are NOT visible via GitHub's API** — both `repos/.../commits/{sha}/status` and `repos/.../deployments` return empty for this repo (unlike some other integrations). The reliable verification path is Netlify's own API directly: site id `554a0f1f-d524-4677-98c6-08e7c1edc92b` (custom domain `field.eq.solutions`), check the current production deploy's `commit_ref` for an exact match against the merge SHA — not just "site responds" or timing, which don't by themselves prove which commit is actually live.
- Two peer sessions (`eq-field-33`, `eq-field-81`) were coordinated with directly via `ListAgents`/`SendMessage` mid-session to avoid duplicate migration work on the same function — worked cleanly: one flagged in-progress work, the other verified independently before trusting it rather than taking the flag on faith, and reported back with a checkable commit hash. Worth repeating as the pattern for future concurrent-session overlap on hot files/functions in this repo.
- Eq-context substrate was 41-43 commits behind origin/main for most of this close pass, consistent with F1/F9 warnings firing all session — `git pull` in the shared checkout was correctly blocked by the F9 guard hook; did this close from an isolated scratchpad clone instead, per the guard's own instructions.

---

# Session 2026-08-26 (eq-shell) — Mark Brame role-change check resolved + record archived; Staff-page licence-expiry hide feature built, shipped, live-fixed

Two threads in one continuous conversation: Royce's original question ("did changing Mark Brame's role from manager to employee actually do anything, given I've since deleted him") drove an investigation that spawned two of this file's own earlier segments (`edit-user.ts` audit trail / PR #1607, staff-shell reverse active-sync / PR #1608); then, mid-session, Royce separately asked for a way to hide a licence with a garbage expiry date from the Staff page's metrics.

## Built

**Mark Brame investigation:**
- Confirmed live, not guessed, that the manager→employee role change genuinely worked: `shell_control.users.role` and `user_tenant_memberships.role` both read `employee`, consistently, and the permission matrix was actively enforcing it — Mark's only real session (2026-08-20) hit five explicit `permission.denied` audit rows for admin-tier actions.
- Found the actual reason it "didn't look like anything changed": Admin > Users' `EqRole` (the login/permission gate) and the Staff page's `is_supervisor`/`supervisor_role` field ("NSW General Manager" on Mark's `app_data.staff` row) are two unrelated fields, on two different databases, edited on two different screens — changing one never touches the other. Saved as memory `eqrole-vs-staff-supervisor-two-manager-concepts`.
- Chased Royce's follow-up worry ("it sounded like he could see everything as a manager") to ground: zero security-group membership at the time (his one group, a calendar-digest mailing list, carried no permission grants and was removed before his first login); what *was* broader than a plain employee were SKS-tenant-wide role overrides (service job create/close/reopen, prestart/toolbox reports) applying to every SKS employee, not personal to Mark. Royce accepted this as the likely explanation rather than asking for deeper Field-side verification.
- Archived Mark's stale `app_data.staff` record on Royce's explicit confirmation (`active=false`, `on_roster=false`) — his Shell login had been deactivated twice (2026-08-20, 2026-08-23) but the staff/roster record had never followed, so he was still showing as an active NSW General Manager on the SKS roster.
- Two real gaps found along the way, both spawned as background tasks, both built and merged by separate sessions within this same file: `edit-user.ts`'s unaudited/silently-failing role-change write (PR #1607) and the staff/shell reverse active-sync direction (PR #1608, which also root-caused what actually flipped Mark's own record — Royce himself, via Field's "Remove from roster," not a bug — see this file's own later segment on that).

**Staff-page licence-expiry hide feature:**
- Found the zero-migration fix before proposing anything: `public.licences` (jvkn) already has a `deleted_at` column, already filtered by every read path (`staff-canonical-licences.ts`, `worker-licences.ts`, `staff-licence-replace-photo.ts`) — no function had ever written to it. Memory: `licences-deleted-at-already-wired-soft-hide`.
- Built `staff-licence-remove.ts` (mirrors `staff-licence-replace-photo.ts`'s exact `admin.review_cards` + org_memberships tenant-scope pattern), sets `deleted_at`; added a "Remove" control (two-step inline confirm) next to the existing Replace photo/back buttons in `SplitPanel.tsx`'s `LicGroup`. No change needed to `licStatus()` or the slicers. [PR #1612](https://github.com/eq-solutions/eq-shell/pull/1612), `tsc -b --force` + eslint clean, squash-merged on Royce's "merge it into main," deploy-ancestry-confirmed live (`d0ec2b9c`) via `listSiteDeploys` polling — caught and correctly waited out a concurrent session's own deploy being `Skipped` mid-poll rather than assuming the first "ready" row was necessarily mine.
- Royce click-tested it live within minutes and found a real bug: the new third button pushed the action row past the detail panel's width, worst when Remove's confirm state expanded it further and clipped the Confirm button. Fixed with `flexWrap` + shortened confirm copy ("Remove this licence?" → "Remove?"). [PR #1614](https://github.com/eq-solutions/eq-shell/pull/1614), same verification rigor, squash-merged on Royce's second explicit "merge it into main," deploy-ancestry-confirmed live (`108d6d6`).
- Confirmed the write scope directly when Royce asked ("does it delete it from jvkn or just from the tenant UI?"): real write to jvkn's `public.licences.deleted_at`, not a client-only hide — soft-delete, not a hard `DELETE`, nothing touched on ehow.

## Decided
- Royce (via AskUserQuestion): archive Mark's stale `app_data.staff` record now.
- Royce: "go" — confirmed the brief for the licence-hide feature before any code was written.
- Royce: "merge it into main" — twice, explicit, one PR at a time (#1612, then #1614).

## Deferred
- The #1614 overlap fix itself hasn't been visually re-confirmed live by a person — verified via deploy-ancestry only. Logged in `eq/pending/eq-shell.md`.
- Everything else this thread surfaced (audit-log gap, staff/shell reverse-sync gap, the unidentified-writer mystery) was already fully closed by other sessions within this same file — see their own segments above rather than re-logged here.

## Notes
- **Session Gate (`/brief eq-shell`) caught the same root-checkout hazard this file's other segments today also hit** — the default checkout 143+ commits behind `origin/main`, on an unrelated branch, with another session's uncommitted work in it. Not touched; built from this session's own worktree, merged forward from `origin/main` first.
- **`gh pr merge` requires waiting for required checks before it will merge** — both #1612 and #1614 hit `mergeStateStatus: BLOCKED` immediately after opening (CI still running); polled via a background Bash task rather than blocking synchronously, matching this file's own established pattern for CI-wait/deploy-wait steps.
- **`ScheduleWakeup` misuse, same mistake this file's own "edit-user.ts" segment already documents** — briefly reached for it as a fallback wakeup while waiting on a backgrounded CI poll; caught and cancelled (`stop: true`) once realized it's `/loop`-only tooling, not a general-purpose one-off waiter. The background task's own completion notification was the correct (and, once corrected, the only) mechanism.
- Local `netlify dev` confirmed unusably broken again this session (attempted once, genuinely, before falling back to `tsc -b --force` + eslint) — matches the existing `vite-build-broken-under-node-24` memory, not a new finding.
- Both PR merges were deploy-verified the hard way (Netlify `listSiteDeploys`, matching commit SHA against the newest `state:"ready"` production row, not just a green merge) — consistent with this file's own repeatedly-reinforced convention today, and directly caught real concurrent-deploy skip/supersede activity in progress both times.

---

# Session 2026-08-26 — eq-cards anon organisations-read dropped, admin/member policy overlap resolved (PR #324)

## Built
- Dropped `organisations_anon_bootstrap_read` (anon, unconditional `SELECT` on the whole `organisations` table, no tracked origin migration) after confirming it has zero live consumers — both real find/browse-company RPCs are `SECURITY DEFINER` and already restricted to `authenticated` at the grant level. Anon confirmed at zero visible rows post-fix.
- Split `org_credential_requirements`/`org_join_requirements`'s admin "manage" (`FOR ALL`) policies into insert/update/delete, removing the redundant `SELECT` overlap with each table's member-read policy — same permissions, fewer policies evaluated per read.
- [PR #324](https://github.com/eq-solutions/eq-cards/pull/324), merged, both migrations (`0163`, `0164`) applied to live jvkn. `get_advisors` (performance) confirms all 3 `multiple_permissive_policies` warnings cleared — zero left in the public schema.

## Decided
- Royce: "create a sprint to action them" — go-ahead for both items surfaced on the prior session-close card (the anon-read finding and the 3-table policy overlap).
- Royce, once findings were in hand: drop the anon policy entirely rather than narrow it or leave it — nothing depends on it today, and a future public browse-companies feature would get its own properly-scoped policy when actually built.

## Deferred
None — this closes both items opened in this file's earlier 0162 segment today.

## Notes
- The "plausibly deliberate pre-login find-my-company read" hypothesis from the earlier 0162 segment did not survive investigation — same failure shape as that segment's 0131 mistake (assuming from a name/comment instead of tracing actual consumers), caught this time before presenting it as settled or leaving it unquestioned. Traced every path that touches `organisations`: `eq_cards_find_invites_by_phone` (phone-based invite lookup) doesn't query this table at all, it reads `tenants`; the actual company-directory RPCs (`eq_cards_list_discoverable_orgs` and siblings, documented in 0153) are `SECURITY DEFINER` with grants restricted to `authenticated` — confirmed via `information_schema.role_routine_grants` per 0153's own header. A full grep of `lib/` and `supabase/functions/` found no direct client-side read of the table either.
- `organisations` carries `supabase_url`/`supabase_anon_key`/`hostname` columns per row — infra routing fields, not just display data — which is what made the anon full-table exposure worth closing rather than just narrowing down to safe columns.
- No migration file in this repo's history ever created `organisations_anon_bootstrap_read` (absent from `git log --all`) — same untracked-live-object class as 0152-0159, except this one was a live access grant rather than schema/RPC drift.

---

# Session 2026-08-26 (cont.) — Digest test-send auth fix (eq-field, follow-up to PR #791)

## Built
- Diagnosed and fixed "Send test to myself" (shipped minutes earlier in #791) failing "unauthorized" on first real click. Root cause was two-layered: the edge function's `testRecipient` code was never actually deployed to Supabase (separate deploy path from Netlify/git), and separately the Netlify function's credential (`AUDIT_SB_KEY`) doesn't match what the edge function's `assertCronCaller` checks against.
- Verified the real Friday cron was unaffected (live dry-run through pg_cron's own SQL, 200/sent:1) before concluding the bug was scoped to the new button only.
- Fixed by switching to the vault's own `digest_cron_secret` (the same one pg_cron already uses via `x-cron-secret`) and deploying the missing edge-function code directly.
- eq-field [PR #802](https://github.com/eq-solutions/eq-field/pull/802), v3.5.578, squash-merged, confirmed live.

## Decided
- Royce (AskUserQuestion): go ahead with all three fix steps (env var, direct edge-function deploy, Netlify-function PR) rather than hand him manual dashboard steps.

## Deferred
- None new.

## Notes
- Real production actions taken beyond the usual PR-preview flow: set a new Netlify env var (`DIGEST_CRON_SECRET`) and deployed edge-function code directly to Supabase, plus one live `net.http_post` diagnostic call (dry-run) against production to test the real cron's credentials. All with Royce's explicit go; edge-function deploys have never gone through git/CI in this repo regardless (no workflow touches `supabase/functions/*`), so this was the normal manual-deploy convention, just exercised mid-debugging rather than as a separate later step.
- `gh pr merge --delete-branch` hit the same "main already checked out in a concurrent worktree" local-housekeeping collision as PR #791 earlier this session — confirmed server-side merge via `gh pr view --json state,mergedAt` before treating it as done, deleted the remote branch separately via `gh api`.

---

# Session 2026-08-26 (cont.) — Don Milmlow blank-name bug: root-caused, fixed, deployed, backfilled

## Built
- Royce reported his own test account (Don Milmlow, added to try EQ Field/Shell) was invisible by name everywhere in EQ Field despite signing in fine. Traced live via Supabase: his Shell login (`shell_control.users.name`) had the right name, but the canonical worker record created 2 seconds later (`public.workers`) had `first_name`/`last_name` hardcoded blank, and that blank propagated into EQ Field's own people list (`app_data.field_people`, ehow) as a name-less record.
- Root cause: `eq-shell/netlify/functions/shell-join-tenant.ts` resolves a joiner's name from an existing Cards profile and correctly uses it for the Shell login's own name — but the variable went out of scope before the later step that creates the canonical worker row, which hardcoded blank names unconditionally regardless of whether a name was already known.
- Checked this wasn't a duplicate of two very recently merged, related fixes (#1542/#1543, 2026-08-23) by reading both diffs directly — those fixed the Shell login's own name self-healing on repeat sign-ins; neither touches the canonical worker record's name at all, which is what EQ Field actually reads from. Genuinely a third, separate gap.
- Built and shipped the fix: eq-shell PR #1616, squash-merged via admin override (branch protection had a separate, unrelated required check freshly broken — a live anon-grant issue on the control plane, confirmed pre-existing drift and not caused by this change, before Royce authorized bypassing it). Confirmed live in production via commit-ancestry against the real Netlify deploy record (not just a green merge).
- Backfilled Don's own existing blank records by hand on Royce's request — both the canonical worker record and the EQ Field record now show his real name.

## Decided
- Royce: fix the underlying gap properly (not just patch Don's one record) — chosen explicitly over "leave it, he's just testing."
- Royce: "merge it", then after being told about the unrelated failing check, explicitly "admin override it, merge it now" — informed authorization to bypass branch protection given the specific, unrelated context.
- Royce: "yes, backfill his name too" — approved the one-off manual data fix for his own existing test records.

## Deferred
- The newly-broken "Schema drift + anon-grant + policy-lint" required check is still red on `main` — a live anon-grant regression on the control plane, unrelated to this session's fix, worked around via admin-override rather than investigated. Table not yet identified. _(added 2026-08-26)_
- 2 other blank-name records already sit in EQ Field's removed-people list on ehow (found earlier this session during the sks-nsw-labour reconciliation) — same shape as Don's, not yet identified or backfilled. _(added 2026-08-26)_
- Not click-tested live by a person — verified via typecheck, lint, live database tracing, and a production deploy-ancestry check, not a real join-flow click-through as a brand-new user. _(added 2026-08-26)_

## Notes
- This is now the third distinct blank-name-provisioning gap found in this exact subsystem in a single week (2 on 2026-08-23, this one 2026-08-26) — worth a step back at some point to ask whether the join-provisioning flow needs a structural fix (one name-resolution step, threaded everywhere it's needed) rather than another one-off patch next time a fourth shows up.
- Branch-protection lesson: a required check failing for reasons completely unrelated to a given PR's diff can still block that PR's merge outright — worth checking a required check's own recent history on `main` (not just the PR's own run) before assuming a red check means the PR itself is broken.
- Also this session: accidentally triggered a filesystem-access permission grant (`C:\Projects` via the Claude Desktop directory-share mechanism) with no corresponding intent or need — flagged to Royce directly rather than left unexplained; he was not asked to grant it and can revoke freely.

---

# Session 2026-08-26 — eq-field audit-attribution breadcrumb: PR #803 shipped, merged, live

## Built
- Investigated eq-shell/ehow's `app_data.fn_audit()` audit-attribution gap (task handed in with a stale premise — corrected it): eq-field's `sbFetch()` already sets `x-eq-actor` since 2026-07-30 and it works for most `staff` writes, confirmed via a live `app_data.audit_log` query.
- Ruled out `field_people_iud()`, eq-shell's `entity-patch.ts`/`entity-actions.ts`/`staff-create.ts`, and all 10 live triggers on `app_data.staff` as the source of `updated_by` on the one unattributed write (Mark Brame's archive) — root cause of that specific miss still unknown.
- Considered and rejected a `fn_audit()` fallback to `auth.uid()`/JWT `sub`: eq-field's data-plane JWT sets `sub` to the tenant id (not the caller), so the fallback would misattribute writes; `auth.uid()` also raises on the leave-canonical magic-link JWT. Left `fn_audit()`/`field_people_iud()` unchanged.
- Shipped eq-field [PR #803](https://github.com/eq-solutions/eq-field/pull/803): a one-per-tab `EQ_OBS` breadcrumb in `sbFetch` for the next time a tab that previously had a real actor id produces a JWT write without one. Squash-merged to `main` on Royce's explicit "merge it into main" — live (auto-deploys to field.eq.solutions on merge).
- Found and fixed a real CI gate the change tripped: `check-cache-busters.mjs`'s drift guard (`scripts/supabase.js` changed, its `?v=` tag in `index.html` hadn't moved — `/scripts` is served immutable, so a missed bump means already-cached browsers never see the fix). Bumped `3.5.508` → `3.5.579`, same PR.
- Saved eq-shell memory `field-audit-attribution-x-eq-actor-already-exists` with the full investigation trail, so the corrected premise and rejected direction don't get re-derived or re-proposed.
- Updated `eq/pending/eq-shell.md` (corrected its own stale premise on the pre-existing item) and `eq/pending/eq-field.md` (new pointer section) — see Notes.

## Decided
- Royce: "Add the sbFetch breadcrumb for the null-actorId case" — build the diagnostic, not a `fn_audit()`/`field_people_iud()` change.
- Royce: "Commit this" / "Push it and open a PR" / "Merge it into main" — each step confirmed explicitly and separately.

## Deferred
- **Root cause of the original Mark Brame miss is still open.** The pre-existing eq-context record (see Notes) says the write came through EQ Field's "Remove from roster" action, but no code path in either repo explains why `x-eq-actor` didn't ride along on that one write despite the mechanism otherwise working. Supabase Studio's own SQL Editor query history (UI-only, not MCP-queryable) is the one remaining place to check for that specific timestamp.
- **Overlaps `task_66de20f0`**, Royce's independently-started background session investigating the same gap — this session's findings should feed into it rather than be re-derived by it.

## Notes
- **Substrate correction, found only at session-close, not during the investigation.** `eq/pending/eq-shell.md` already carried an entry for this exact incident (added earlier the same day, 2026-08-26) asserting "Field's PostgREST path never sets [x-eq-actor]" — the same stale premise this session was independently handed and independently disproved via live code + a live `audit_log` query. It also already stated definitively that the write came through EQ Field's "Remove from roster" action — information this session never had and could not re-derive from code alone. The shared eq-context checkout was flagged out-of-sync for this session's entire duration (behind 37 commits at session start, 56 by session end), so that existing entry was never consulted mid-investigation. Corrected in place at close rather than left to drift further — a real instance of the digest's own F1 guard-bypass flag ("substrate read path served stale content, no error"), not just a hypothetical.
- Separately: the section header carrying that pending item ("staff/shell active-sync — reverse direction... PR #1608") does not describe the audit-attribution bullet beneath it — looks like a mismatch from an earlier close, not something caused or fixed this session. Flagged here rather than guessed at.
- Wrote to `eq-context` from an isolated scratch clone, not the shared `C:\Projects\eq-context` checkout — `git pull` there is hard-blocked by the F9 guard (concurrent-session git races corrupting the shared checkout). Pushed from the isolated clone instead, per the guard's own instructions.
- A background CI-status watcher (Bash `run_in_background`, polling `gh pr view` in a loop) reported all-green while one check (`Tests + lint`) was still genuinely `IN_PROGRESS` — a timing race between the loop's own exit check and its final status print, not a harness bug. Re-verified live before merging rather than trusting the notification; a "task completed" signal describes the watcher process exiting, not necessarily the condition it was watching for.

---

# Session 2026-08-26 (cont.) — Week picker: "This week" marker (eq-field, PR #801)

## Built
- Royce asked where the roster week-picker's existing bold highlight was built (from a screenshot showing "Week Starting 31st August 2026" bolded). Found it in `scripts/week-picker.js` — it marks `STATE.currentWeek`, the persisted VIEWED week, not the real calendar week. Royce then asked to add a marker for today's actual week too.
- Added a new tag on the popover's matching row (`_wpTodayWeekKey()`, a local Monday-of-week calc — this codebase already keeps ~8 separate copies of that exact one-liner across other files rather than sharing one, so a 9th local copy matches convention rather than introducing a new shared util), independent of the existing highlight — the two read distinctly when they differ and stack on one row when they coincide.
- eq-field [PR #801](https://github.com/eq-solutions/eq-field/pull/801), v3.5.579, squash-merged on Royce's explicit "merge #801", confirmed live via `field.eq.solutions/sw.js`.

## Decided
- Royce (AskUserQuestion): "Commit + push + open PR" over leaving it uncommitted or committing without pushing.
- Royce: "merge #801" — explicit merge go.

## Deferred
- None new. The one open item already logged against this same popover (`eq/pending/eq-field.md`'s "pin a Current row when paged away" entry, 2026-08-25 — not click-tested through a rendered popover) is untouched; this session's change is additive to a different code path and wasn't verified against that specific scenario either, so it's left exactly as already logged rather than silently marked closer to done than it is.

## Notes
- **Session Gate (`/brief eq-field`) caught a real duplicate-work risk before any code was written**: a dormant local branch `claude/week-picker-current-highlight` (no random worktree suffix, unlike this session's own auto-generated branch name) sat in the shared root, last touched under a self-numbered "v3.5.567". Diffed it against `main` before writing anything — its `week-picker.js`/`base.css` changes turned out byte-identical to what's already live (the pinned-row feature, actually shipped as v3.5.562 through a different PR) — a version-numbering race from an earlier session, fully superseded, safe to ignore. Confirmed via `git diff main...<branch>`, not assumed from the branch name alone.
- **Mandatory `/reflect` pass earned its keep, twice, before this ever reached CI**: (1) an early draft's CSS/changelog comment claimed the new tag "repaints correctly per tenant" via the canonical branding pipeline — traced the actual CSS variables in `base.css` and found that's wrong: the tag's background is a fixed constant on both tenants, only its text colour shifts, via an unrelated static class rule. Corrected before it shipped as a permanent (wrong) claim in the in-HTML changelog banner. (2) the tag was initially labelled "Today" — grepped the rest of the app first and found it already has an established word for this exact concept elsewhere (Safety, Timesheets, Tender Pipeline all say "This week", never "Today") — relabelled for consistency before shipping, not after a user pointed it out.
- **CI's cache-buster drift-guard caught a real miss on the first push**: `scripts/app-state.js` had its own separate `<script src=...?v=>` tag in `index.html` that hadn't moved, distinct from the two bundle/stylesheet tags already bumped — a known failure class (already in this repo's own memory) that got missed anyway on the version-stamp file itself. Fixed, re-verified locally against the exact base commit CI used, pushed again.
- **A live version-collision hit at merge time, not before**: `main` had moved between opening the PR and merging it — a concurrent session's PR (#802, a digest test-send fix, unrelated) landed first and had also claimed v3.5.578. Rebased onto the new `main`, resolved 2 real conflicts (the changelog banner insertion point in `index.html`, the reflection-log tail), renumbered this release to v3.5.579 throughout (APP_VERSION, `sw.js`, all 3 `index.html` `?v=` tags, the changelog header), and re-ran the full local check suite (tests, lint, bundle regen, cache-busters) before force-pushing — same recurring pattern this repo's own session logs document repeatedly, handled the same documented way.
- Verification method for a UI-only change with no way to boot the real app locally (this repo's tenant/auth boot depends on a live Netlify Function that a plain static file server can't run): built a throwaway harness page serving the REAL `base.css`/`utils.js`/`week-picker.js` under a local static server instead of trying to force a full app boot, and confirmed both the independent-marker and stacked-marker cases via `getComputedStyle` on the actual rendered popover. Stronger than this repo's usual "function-level only" fallback, though still short of a real authenticated click-through — same standing sandbox limitation as most entries in this file. Harness file deleted before commit, never part of the diff.
- `gh pr merge --delete-branch`'s local cleanup step hit the same recurring "`main` already checked out in a concurrent worktree" collision this file's other segments today also document — confirmed the server-side squash-merge succeeded regardless (`gh pr view --json state,mergedAt,mergeCommit`) before treating it as done, then deleted the remote branch separately.

---

# Session 2026-08-26 — Self-join QR link fixes, plus a live security regression found and fixed along the way

## Built
- eq-shell: deactivated self-join links a user has actually joined through can never be deleted (real Postgres FK, protects join history) — UI showed a Delete button on those rows anyway, which always 409'd and silently reverted. Now shows "Can't delete — already used" instead. PR #1610, merged, live.
- eq-shell: join-links list defaulted to showing every link ever created. Default view now shows only Active + not-expired; Deactivated and expired links collapse behind a "Show deactivated & expired (N)" toggle. A `/decide` pass caught that `active` never auto-flips false on expiry (checked only at join-time), so a naive "hide deactivated" fix would have left expired-but-active links quietly regrowing the list. PR #1615, merged, live.
- eq-shell: restored the anon pre-login bootstrap-read policy on `public.organisations` (jvkn control plane) — third occurrence of this exact regression (see Notes). Paper-trailed as `supabase/migrations/2026_08_26_restore_organisations_anon_bootstrap_read_again.sql`, PR #1618, merged, live.

## Decided
- Royce: hide the Delete button rather than disable it or just improve the error message (recommended option from a `/decide` pass).
- Royce: "both deactivate promptly" — confirmed the expiry-cruft edge case is real and worth building the fuller fix for.
- Royce: fix the organisations anon-read regression now (before merging #1615) rather than defer or admin-override past it.
- Royce: hand-applied the restore SQL himself via the Supabase dashboard after the Supabase MCP's own `apply_migration` call was blocked by the Claude Code auto-mode classifier for a direct production DDL write.

## Deferred
- WHO/WHAT applied migrations `0163_resolve_admin_read_policy_overlap` and `0164_drop_orphaned_anon_organisations_read` directly to jvkn (10:34 UTC today, no corresponding file anywhere in this repo's git history) is still unknown — not investigated, wasn't blocking the fix. No durable guard yet against a 4th occurrence of this same failure mode on this same table.
- Spawned `task_873144b9` (background, separate session) to look into today's unusually heavy concurrent-merge/deploy-skip churn on eq-shell `main` — ended, outcome not reviewed by this session.
- Neither PR click-tested live by a person (no live Shell session/credentials in this environment) — see eq-shell pending.md for exact steps.

## Notes
- **`public.organisations` losing its anon bootstrap-read policy is now a 3-time recurrence** (originally established PR #787 2026-07-12; restored as incident #1530 on 2026-08-23; broken again today by `0163`/`0164`). All three times the failure shape is the same: something touches this table's RLS policies, doesn't know about the dedicated anon-only `organisations_anon_bootstrap_read` policy's reason for existing, and drops or narrows it. Worth a standing guard beyond CHECK 10 (which catches it after the fact, correctly, but only when *something else's* PR happens to run CI) — maybe a comment on the policy itself, or a check that runs on a schedule independent of unrelated PRs.
- **The Supabase MCP's `apply_migration` gets blocked by the Claude Code auto-mode classifier when targeting what looks like a live production write** — confirmed this session. For jvkn control-plane hotfixes specifically (which have no CI apply path at all — precedent from incident #1530), the actual working path is: give Royce the exact SQL, he runs it via the Supabase dashboard SQL editor, then write the migration file afterward as a paper trail. Don't try to route around the classifier block.
- Concurrent-merge churn on eq-shell `main` was extremely heavy this session (eq-context substrate went from 11 to 68+ commits behind `origin/main` over the course of one conversation) — two of this session's own merges got their Netlify deploy `Skipped` by a later concurrent merge, and PR #1615 hit a genuine merge conflict against `main` from the session's *own* earlier PR (#1610) landing as a squashed commit. Commit-ancestry verification (per eq-shell's CLAUDE.md deploy note) was load-bearing more than once.

---

# Session 2026-08-26 — Per-user sidebar simplification ("Interface: Simple") built, merged, live

Royce: "Sharon as an example is our office manager but easily overwhelmed, she only needs to see Records and EQ Field to look at timesheets. Whats a good way of doing this?" Sketched the UX, ran `/decide` on whether anything more sophisticated was worth considering, confirmed the admin-control placement (AdminEditUser.tsx over AccessControlPage) via AskUserQuestion, then built.

## Built
- New `nav_scope` axis: a per-(user, tenant) sidebar-visibility override, fully independent of the existing permission system (`useCan`/`PermKey`) — it only hides items a user's permissions already allow, never grants anything. `shell_control.user_tenant_memberships.nav_scope` (jsonb, nullable — null means Full/unrestricted), `eq_get_tenant_user` RPC updated to return it, both applied live on jvkn.
- Canonical item list + visibility filter centralised once in new `src/lib/navScope.ts`, consumed identically by `HubSidebar` (the real sidebar), `AdminEditUser.tsx` (the new admin control — Full/Simple toggle + grouped checkboxes, gated `admin.manage_groups`, one write per click via new `nav-scope` Netlify function), and `AccessControlPage`'s existing permission-preview panel (now also shows "Interface: Simple (N shown)").
- Checkbox saves are optimistic with rollback on failure — no separate Save button, matching the existing Security-groups section's UX exactly (confirmed with Royce mid-session: "Does this auto save?" — yes, by design).
- eq-shell [PR #1617](https://github.com/eq-solutions/eq-shell/pull/1617), CI green, squash-merged on "merge it then do the migration", deploy-confirmed live via Netlify commit-ancestry (not just a green merge).
- Both control-plane migrations (the `nav_scope` column add, and the `eq_get_tenant_user` RPC update re-asserting its grants) applied via the Supabase MCP directly to jvkn and verified live — this repo's control-plane migrations have no CI automation, unlike tenant-plane's One Pipe.

## Decided
- Royce: "Yeah, sketch it out" — visual mockup before building.
- Royce (via `/decide`): "i like that idea, is there anything world leading that we could consider? then build" — ran the six-step decision pass, concluded the straightforward per-user override was the right call over anything more elaborate, then build was authorized in the same instruction.
- Royce (via AskUserQuestion): "AdminEditUser.tsx" — the edit control lives on the user's own edit page, not a separate picker on Access Control.
- Royce: confirmed the interactive mockup matched what he expected before build started.
- Royce: "merge it then do the migration" — explicit authorization to merge PR #1617 (a production deploy on this auto-deploying repo) and hand-apply both control-plane migrations.

## Deferred
- Not click-tested live by a person — verified via `tsc -b`, the access-control preview, and production deploy-ancestry only. Logged in `eq/pending/eq-shell.md`.
- Sharon's own account not yet switched to Simple — this session built the mechanism; applying it to her specifically, and which items she keeps, is Royce's call.

## Notes
- **Merge was blocked mid-task by an unrelated live regression**: PR #1617's required "Schema drift + anon-grant + policy-lint" check came back red — root-caused (not assumed) by comparing against other PRs failing identically around the same time and direct `pg_policy`/`information_schema` queries against live jvkn: the anon pre-login bootstrap-read policy on `public.organisations` had silently disappeared for a third time. Not this PR's fault — confirmed and reported before touching anything, per the principle that "merge it" didn't anticipate this new information. Full root-cause (two out-of-band migrations, `0163`/`0164`, applied directly to jvkn outside git entirely) was written up by a concurrent session working PR #1615/#1618 — see that entry in this same file and in `eq/pending/eq-shell.md`, not duplicated here.
- **Claude Code's own permission classifier blocked a DB write even after explicit in-chat authorization**: attempted `mcp__supabase__apply_migration` for the anon-RLS-policy fix specifically (with Royce's explicit go-ahead already given via AskUserQuestion) — blocked by the harness's own classifier, distinct from and in addition to chat approval. Per the tool's own guidance, did not attempt to route around it via `execute_sql` or any other tool; stopped, explained clearly, handed Royce the exact SQL. He applied it directly via the Supabase dashboard SQL editor. Notably, this session's own two `nav_scope` migrations (an additive column, and a `SECURITY DEFINER` function replace that preserves existing grants) went through the same tool without being blocked — the classifier reads as specifically sensitive to anon-facing RLS/policy changes, not additive schema DDL in general. Worth remembering: this class of fix needs a human at the keyboard, not a more creative tool call.
- Substrate writes for this close went through a fresh isolated clone (scratchpad), not the shared `eq-context` checkout — the session-start gate reported it 66-73 commits behind `origin/main` and climbing through the session, consistent with the heavy concurrent-session traffic every other entry in this file reports today.

---

# Session 2026-08-26 — Dependabot high-severity alerts on eq-shell: confirmed already-resolved (SEC-23)

## Built
- Nothing shipped — investigation only, no code changes.

## Decided
- No action taken on the 2 open GitHub Dependabot alerts (#193/#194) beyond re-verification — confirmed they still match the existing SEC-23 posture exactly.

## Deferred
- None new. Offered (not actioned) to dismiss the 2 alerts on GitHub with a note linking to PR #1288 — didn't do it unprompted since it's a visible state change on a shared repo; left as Royce's call.

## Notes
- Prompted by GitHub's push-time notice ("2 vulnerabilities... 2 high") surfacing on an unrelated branch push (`claude/upload-aiden-profile-image-db18e2`). Re-verified from scratch rather than trusting memory alone: live `gh api repos/eq-solutions/eq-shell/dependabot/alerts` confirms both are still `image-size@2.0.2` (GHSA-w3rx-r6r6-pgpr, ICNS parser; GHSA-5p2g-fcmc-qvqq, JXL/HEIF parsers), both `first_patched_version: null` (upstream repo stays archived, no fix will ever ship). Live repo check confirms the `pnpm patch` fix (PR #1288) is still wired (`package.json` `patchedDependencies`, applied in `pnpm-lock.yaml` with a matching patch hash) and the patch content itself still matches both advisories exactly — break-guards in the icns/heif/jxl parsers' `.cjs`/`.mjs` output. Cross-checked against eq-context's own `ops/security-register.md` (SEC-23) and `eq/pending/eq-shell.md` (already carries a prior-session `[x]` line covering this exact pair) — all four sources (live GitHub, live repo, register, pending) agree: permanently-open-by-design, a scanner limitation, not a real gap. No `pending/eq-shell.md` edit needed this session — already correctly closed there from before.
- Substrate writes for this close went through a fresh isolated clone (scratchpad), not the shared `eq-context` checkout — the session-start gate reported it climbing from 66 to 77 commits behind `origin/main` over the session, and the shared checkout's own `git pull` guard (F9) fired when first attempted mid-session, confirming the isolated-clone path was the right call (consistent with every other entry in this file today).
---

# Session 2026-08-26 (cont.) — eq-field: Preferred Name saved identical to Full Name, fixed

## Built
- `savePerson()` (`scripts/people.js`) now treats a Preferred Name that case-insensitively matches Full Name as unset — saves `null` instead of a literal duplicate. Modal input (`index.html`) also gains `autocomplete="nickname"` as a defensive measure against possible browser-autofill misfires.
- eq-field [PR #806](https://github.com/eq-solutions/eq-field/pull/806) (v3.5.581) — CI green (Tests + lint, drift x2), not yet merged (Royce's go received for the fix + data clear, merge/deploy not yet requested).
- Live data fix: Marcus De La Fuente's `preferred_name` on ehow cleared from a literal duplicate of his full name back to `null` (Royce's explicit go), via a scoped `UPDATE ... WHERE ... AND preferred_name = 'Marcus De La Fuente'`.
- Corrected a same-day-stale local memory (EQ Field's own `project_preferred_name_wiring.md`) that claimed Roster/Timesheets don't read `preferred_name` — they do, via both adapters delegating to the Leave adapter's `staffIdToName()`; the memory predated PR #783 by ~11 hours and was never revisited.

## Decided
- Royce: confirmed the session brief, then "Confirmed — go ahead, and clear Marcus's value too" — explicit go for both the code fix and the live data correction.

## Deferred
- Root cause of how Marcus's row got into that state is unconfirmed — see the new pending item in `eq/pending/eq-field.md` (same file, same section as the 2026-08-25 "full click-through save round-trip... still not completed" item, which is exactly the gap that let this ship unnoticed).
- No toast/notice added for the silent-normalization case (typing your own full name into Preferred Name now silently reverts to unset) — considered, declined as disproportionate scope for a single-line guard; consistent with other silent normalizations already in the same function (phone formatting, trimming).
- PR #806 not merged — code fix + data clear were authorized explicitly; merge/deploy was not requested in the same instruction, per the standing rule that "fix this" isn't "deploy this."

## Notes
- **Live-verified before writing any code, not assumed**: only 1 of 110 `app_data.staff` rows on ehow had `preferred_name` literally equal to `first_name + last_name` — ruled out a systemic data-pipeline issue before treating this as a narrow guard fix. A rollback-protected direct SQL transaction (`BEGIN; UPDATE app_data.field_people SET preferred_name = '...'; SELECT ...; ROLLBACK;`) proved the view + `field_people_iud()` trigger already handle a partial `preferred_name`-only PATCH correctly (first/last untouched) — the higher-risk half of this investigation, confirmed rather than assumed. Searched every Postgres function on ehow whose source references the column (`pg_proc.prosrc ILIKE '%preferred_name%'`) plus `cron.job` — nothing else touches it besides the (irrelevant, 7-year-deactivated-only) leaver-retention anonymiser.
- **Caught a real cache-buster drift via the repo's own automated check, not manual review**: `scripts/app-state.js` is loaded via a static `<script src=...?v=>` tag (unlike the lazy-loaded `people.js`), and its tag hadn't moved — `node scripts/check-cache-busters.mjs` caught it before push. Same failure class already named twice elsewhere in this file/this repo's `docs/reflection-log.md`.
- **Local click-through attempted, hit the same standing limitation every other eq-field entry in this file reports**: a plain static server can boot the app's static assets fine, but `?tenant=demo`'s own tenant-config resolution still needs a Netlify Function (`tenant-config.js`), which 404s without `netlify dev`. Verified the fix at the DB/logic level instead (see above) rather than skip verification.
- **This session's local eq-context clone read 73→77 commits behind `origin/main` and climbing** (session-start gate + live re-checks) — consistent with every other entry in this file today. Substrate writes for this close went through a fresh isolated clone (scratchpad), not the shared checkout — `git merge` in the shared `C:\Projects\eq-context` is now a hard-blocked guard action (F9, rung 4) after repeated corruption incidents; the isolated-clone path is what the guard's own error message directs to.

---

# Session 2026-08-26 — organisations anon-read regression: independent verification, then the real cross-repo root cause traced at close

Handed the CI failure cold (table name truncated in the log the task was briefed with — a real bug in CHECK 10's own printer, see below). Worked it independently before discovering eq-shell PR #1618 already existed, drafted by Royce in a parallel session; then, at `/close`, reading this file end-to-end surfaced the actual full chain, which no single session so far had seen — see Notes, this is the substantive finding.

## Built
- Nothing shipped directly — this session's own value was verification, task-spawning, and (at close) cross-repo reconciliation. The code fixes it triggered (PRs #1622, #1623) were built and merged by the two spawned sessions, not this one.
- Independently root-caused the anon-read table (`public.organisations`) by cross-referencing `check-tenant-drift.mjs`'s `INTENTIONAL_ANON_READS` allowlist against live jvkn grants, before finding PR #1618 already existed — arrived at the same table via a different method (Supabase MCP `execute_sql`, not a prior session's writeup).
- Confirmed via jvkn `postgres_logs` (not just "no matching repo file") that `0163`/`0164` were applied live via the Management API's `POST /v1/projects/:ref/database/migrations` (the same call `apply_migration` makes), `pat:9984107`/`dev@eq.solutions`, 10:34:23–10:34:50Z. Reviewed PR #1618's migration file for correctness before recommending merge (idempotent, existence-guarded, correctly scoped to leave `organisations_read` untouched) — Royce had already merged it by the time this session tried.
- Spawned two follow-up tasks from what this session found: CHECK 10's console printer crashing before naming the table (uncaught exception, exit 1 not the deliberate 2), and paper-trailing `0162`/`0163` since they looked untracked in this repo. Both picked up in separate sessions same day — see `eq/pending/eq-shell.md`'s own entry for outcomes (PRs #1622, #1623).
- **At `/close`, reading this file's earlier entries end-to-end (not just this session's own transcript) found the actual complete chain**, which neither this session's own investigation nor PR #1618's had: this file's own "eq-cards RLS/FK-index hardening (0162)" and "eq-cards anon organisations-read dropped... (PR #324)" entries, above, show `0163`+`0164` were BOTH eq-cards PR #324 — a deliberate, Royce-approved change, investigated thoroughly within eq-cards' own scope (traced every Cards-side consumer, found none), just with no visibility into EQ Field's cross-repo dependency on the same policy. Not an unidentified actor, not a pipeline bypass — normal practice (jvkn control-plane has no CI apply path anywhere) plus a genuine cross-repo blind spot.
- Corrected three pieces of substrate that had all inherited the "unidentified actor" framing from PR #1618's own (reasonable, but incomplete) investigation: this eq-shell memory (`organisations-anon-bootstrap-read-recurring-incident.md`, corrected twice as the picture sharpened), `eq/pending/eq-shell.md`'s own entry (trimmed per the archive rule; open items now correctly describe a cross-repo visibility gap, not a mystery actor), and `eq/pending/eq-cards.md`'s PR #324 entry (added a same-day correction note — it currently reads as clean/final with no hint it broke Field in production a few hours later).
- Flagged in memory that this session's own `task_65f538db` ask ("paper-trail `0162`/`0163` into eq-shell") was built on a false premise — they're not untracked drift, they're real eq-cards migrations (PR #323/#324) against the shared jvkn plane. Writing a competing eq-shell file for them would be wrong, not just redundant. If the spawned session is still working toward that goal, it should stop short of new migration files.

## Decided
- Royce (via AskUserQuestion): merge PR #1618 now — moot by the time this session acted (already merged), but the recommendation was independently reached and correctly scoped as needing his go given eq-shell auto-deploys on merge.

## Deferred
- **The actual remaining gap**: nothing makes an eq-cards (or any other repo's) change to the jvkn control plane check eq-shell's CHECK 10 before merging. This is what a 4th occurrence needs to be prevented — logged precisely as this in `eq/pending/eq-shell.md`, replacing the old "identify the actor" framing.
- `task_873144b9` (unrelated concurrent-merge/deploy-skip churn investigation, spawned by a different session earlier the same day) — not reviewed by this session, unclear if related.
- PR #1623 (CHECK 10 fixes) not click/CI-tested against a real live violation yet — no live credentials in this environment.

## Notes
- **The core lesson of this session is methodological, not technical**: two independent, individually competent investigations (this session's own postgres_logs trace, and PR #1618's) both stopped at "no corresponding file in *this* repo" and reasonably (but wrongly) concluded "unidentified actor." The actual answer was sitting in this exact session-log file the whole time, written by a third session working a completely different repo, a few hundred lines above. Neither investigation had a reason to read eq-cards' own entries while working an eq-shell CI failure — cross-repo incidents don't announce themselves as cross-repo. Worth remembering next time a control-plane regression's "author" comes back as a generic PAT/service credential rather than a named person: check *every* repo's same-day session-log entries for a matching migration number before concluding it's out-of-band, not just the repo the symptom appeared in.
- Also learned via a memory-changed notification mid-close: the `task_97bd23d6` follow-up (CHECK 10 printer crash) independently converged on the same fix as PR #1623 about 10 minutes apart and correctly closed its own redundant PR (#1624) in favour of the already-merged one, rather than both landing. No harm done, just worth the pattern: re-check adjacent/recent PRs immediately before merging, not only at task start, given today's exceptionally heavy concurrent-session volume.
- Substrate writes for this close went through a fresh isolated clone (scratchpad), not the shared `C:\Projects\eq-context` checkout — session-start gate reported it 75→77 commits behind `origin/main` and climbing, and a plain `git pull` there was hard-blocked by the F9 guard directly (hit it live, not just documented), consistent with every other same-day entry in this file.

---

# Session 2026-08-26 (cont.) — Sharon Maroni's simplified sidebar confirmed live

Direct continuation of the nav_scope build logged earlier in this same file ("Per-user sidebar simplification... built, merged, live"). Royce: "Sharon should be simple now."

## Built
- Nothing new — verification only.

## Decided
- Looked up Sharon Maroni's live record on jvkn (`shell_control.users`/`user_tenant_memberships`, SKS Technologies tenant) rather than assuming: she was already switched to Simple, `nav_scope = ["customer","site","contact","staff","licence","equipment","field","suppliers"]` — Royce had set this directly himself while click-testing the feature earlier in the session (the screenshot he'd sent asking "Does this auto save?" was already her real admin-edit page, not a test user).
- One item didn't match the original ask ("Records and EQ Field" only) — `suppliers` was also checked. Flagged it via AskUserQuestion rather than silently changing a real person's live access. Royce: "Keep Suppliers" — her live sidebar is correct as-is, no write needed.

## Deferred
- Not click-tested live by a person — still open, now narrower: the data side is fully confirmed correct (Sharon's own row, direct query), only the actual signed-in visual (does her sidebar itself narrow) remains unverified. Trimmed in `eq/pending/eq-shell.md`.

## Notes
- No code or schema touched this round — purely a live-data lookup (read-only query against jvkn) plus a scope confirmation. Worth recording because "should be simple now" could have been read as an instruction to write a change; checking live state first caught that the write had already happened, avoiding a redundant/conflicting update.
---

# Session 2026-08-26 (cont.) — eq-field: Preferred Name fix merged + deployed live

## Built
- Merged eq-field [PR #806](https://github.com/eq-solutions/eq-field/pull/806) on Royce's "merge when safe" — re-verified immediately before merging: `origin/main` unchanged since PR open (no rebase/renumber needed), PR state `MERGEABLE`/`CLEAN`, all checks green (Tests + lint, drift x2, Netlify header/redirect rules, `netlify/eq-field/deploy-preview`).
- Confirmed live: `field.eq.solutions/sw.js` serves `v3.5.581` post-merge.

## Decided
- Royce: "merge when safe" — merged once every check above came back clean, no further confirmation sought (matches the standing convention: an explicit merge instruction on this repo is treated as deploy authorization, Netlify auto-deploys on push to `main`).

## Deferred
- Unchanged from the entry above — root cause of Marcus's original bad value still unconfirmed, live click-test of the save round-trip still outstanding. Both remain open in `eq/pending/eq-field.md`.

## Notes
- **`gh pr merge --delete-branch`'s local branch-delete step failed, the merge itself didn't**: `fatal: 'main' is already used by worktree at 'C:/Projects/eq-field-apprentice-scope-race-wt'` — a concurrent session had `main` checked out in a different worktree on this shared machine. Checked `gh pr view 806 --json state,mergedAt,mergeCommit` directly rather than assume failure from the error text alone — confirmed `state=MERGED` with a real SHA. Cleaned up the now-orphaned remote branch via a plain `git push origin --delete claude/preferred-name-bug-a0b7af` (API-level, no local checkout needed) — identical recovery already documented in this repo's own `docs/reflection-log.md` for PR #796. Second confirmed instance of this exact failure shape; worth generalizing if a third one turns up.
- A `gh`/network blip (`error connecting to api.github.com`, then a DNS resolution failure on a raw `curl`) briefly blocked the first merge attempt — transient, resolved on retry a short time later, not investigated further since it cleared on its own and both `curl` and `gh` recovered together (consistent with a local network hiccup, not a GitHub-side outage).

---

# Session 2026-08-26 (cont.) — Dismissed the 2 image-size Dependabot alerts on GitHub

Direct continuation of the Dependabot re-verification logged earlier in this same file ("Dependabot high-severity alerts on eq-shell: confirmed already-resolved (SEC-23)"). Royce: "close those 2 warning."

## Built
- Nothing code-side — a GitHub-state change only. Dismissed alerts #193 and #194 on eq-solutions/eq-shell via the Dependabot API (`dismissed_reason: inaccurate`), each with a comment linking to PR #1288 and eq-context SEC-23 so the reasoning is visible on GitHub itself, not just in this substrate. Live-verified both now read `state: dismissed`.

## Decided
- Royce: "close those 2 warning" — explicit go-ahead for the GitHub-side dismissal that was flagged as optional on the prior close's session card.

## Deferred
- None.

## Notes
- Hit a transient network blip on the first attempt (`gh api` TLS handshake timeout on both PATCH calls, same window `/compact` also failed with ENOTFOUND) — confirmed it wasn't GitHub-side via a direct `curl` to `api.github.com` (200) before retrying; cleared within the same minute.
- `dismissed_comment` has an undocumented-in-practice 280-character hard limit (HTTP 422 on the first retry — both comments were 800+ chars) — trimmed to ~235-240 chars each and resubmitted successfully. Worth remembering for any future Dependabot-alert-dismissal via this API.

---

# Session 2026-08-26 — entity-patch/cards-api CORS fix (#1609/#1613) verified live; swept for other process.env.CONTEXT instances, none found

Handed a bug report for eq-shell's `entity-patch.ts`: Field's cross-origin OPTIONS preflight (`field.eq.solutions` → `core.eq.solutions`) returning bare 204 with no CORS headers, blocking every Field→Shell write ("Failed to fetch"). Investigated live before writing any fix, per Rule 0.5 — this repo wasn't this session's primary repo (started from an eq-field session), so verification leaned on `git show origin/main:...` / `gh pr list` / live curl rather than a checked-out worktree.

## Built
- No code changes — the described bug was already fixed by a concurrent session, [PR #1609](https://github.com/eq-solutions/eq-shell/pull/1609) (merged 2026-08-26T11:03:19Z): `isProdRequest()` was reading `process.env.CONTEXT`, which Netlify doesn't reliably propagate to Function runtime, so it evaluated false in real production and silently fell back to a dev-only CORS allowlist. Fix derives prod-ness from the request's own `Host` header instead (new `_shared/entity-patch-cors.ts`, with unit tests pinning the regression).
- First live curl still showed the bug (bare 204, zero CORS headers) — traced to the fix's own production deploy still being mid-build (`state:"uploading"` / `state:"new"` on the two queued deploys, via `listSiteDeploys`), not a residual code bug. Polled until published (~9 min after merge), then re-confirmed: `Access-Control-Allow-Origin: https://field.eq.solutions`, `-Methods`, `-Headers`, `Vary: Origin` all present. Cross-checked the actual client call (eq-field's `scripts/managers.js` → `saveManagerCategoryToCanonical()`) matches what's now allowed — no live SKS data touched to verify this.
- Follow-up ask: sweep eq-shell for other functions with the same `process.env.CONTEXT` pattern. Found exactly one sibling, `cards-api.ts` — already fixed same day in [PR #1613](https://github.com/eq-solutions/eq-shell/pull/1613) (merged earlier, 10:14:06Z), identical Host-header pattern, confirmed live via the same curl method (`cards.eq.solutions` origin). Checked for variant forms too (bracket/destructured env access, `ctx.deploy`, any other `isProd`-named helper anywhere in `netlify/functions`) — none found. The only surviving `process.env.CONTEXT` reference in `netlify/functions` is `_shared/sentry.ts`'s environment/release tag, a non-security use (wrong value there mistags an error report, can't defeat CORS).

## Decided
- Nothing — no decision point reached; the investigation concluded the bug (and its one sibling) were already fixed and live.

## Deferred
- Nothing new.

## Notes
- **Good example of a stale task premise caught by Rule 0.5**: the bug report's root-cause guess ("CORS was apparently never added") had already been superseded by the time it reached this session — a concurrent session had already found the real root cause (`process.env.CONTEXT` unreliability, not a missing handler) and shipped the fix. Live curl, not code reading alone, is what caught that the fix was merged-but-not-yet-deployed on the first check rather than genuinely broken — `listSiteDeploys` showing both relevant deploys still `"uploading"`/`"new"` was the tell.
- Substrate write for this close went through a fresh isolated clone (scratchpad), not the shared `eq-context` checkout — same F9-guard pattern as every other entry in this file today; the shared checkout's `git pull` was hit live (blocked with the standard F9 message), not just assumed blocked.
