# Session 2026-09-14 — field_people_iud() null-tenant guard: fixed, applied live, merged

## Built
- [eq-field PR #977](https://github.com/eq-solutions/eq-field/pull/977): fixed the hardcoded-SKS-UUID fallback in `app_data.field_people_iud()` on ehow — root cause of the 2026-09-09/10 incident where 6 Madagins onboarding test profiles leaked into SKS's live roster. Migration pulls the live function body via `pg_get_functiondef` at apply time and splices a null-tenant guard around a verified-stable anchor line, rather than hand-reconstructing a full body — this exact function has caused two prior silent-revert incidents from committed-history reconstruction, and this session's investigation found a 3rd candidate-for-disaster: two eq-shell migrations both numbered `0274` had silently overwritten each other live (fully explained afterward by a concurrent session's follow-up, PR #980 — see Claude memory `incident_field_people_iud_provenance_gap.md`).
- Applied live via the Supabase MCP and verified directly: ehow and madagins both confirmed patched (guard present, everything else in the function byte-for-byte untouched, no new security-advisor flags). zaap has no `field_people_iud()` at all — confirmed nothing to fix there.
- Fixed a same-day CI regression on the PR: the tenant-provision generator's self-test (a 5th gate on `supabase/migrations/*.sql`, distinct from the 4 migration-guard tests) hard-fails on any unclassified migration file — missed on first push 3 days earlier, caught by re-checking PR status before a later retry, fixed and verified against the full local suite before re-pushing.
- PR #977 squash-merged into `main` (`be8a982`), by explicit request.
- Hard-deleted the 6 "Demo Candidate" test rows from eq-canonical (jvkn) `public.workers` — superseding the original incident session's "re-tag, don't purge" call. Checked all 8 FK-referencing tables first; two had real rows (`worker_credentials` ×10, `ON DELETE CASCADE`; `labour_hire_candidates` ×6, `NO ACTION`, deleted explicitly first). Verified 0 remaining across all three afterward, and that the 2 real Madagins employees (Nelson Sareto, Conor Horgan) were untouched.

## Decided
- Royce: apply the fix live now ("go"), not just PR it.
- Royce: purge the 6 demo rows outright ("safely, remove those demo users") — reverses the original incident session's re-tag decision once re-tagging turned out to just relocate the risk (auto-promotion into Madagins's own real roster) rather than remove it.
- Royce: apply directly via the agent rather than paste the SQL into Supabase Studio himself ("do it yourself").
- Royce: merge PR #977 once both live planes were confirmed fixed ("merge and commit").

## Deferred
- Nothing genuinely deferred from this session's own work. The one open thread this session itself flagged — provenance of `field_people_iud()`'s undocumented `v_actor_id`/self-write addition — was picked up and closed by a concurrent session before this one ended (PR #980, comment-only backfill, no behavior change). That investigation also answered a question this session had left open (whether the Restore button's reactivation stamp survived the `0274` collision): no regression — the button is backed by a different function, `field_people_removed_iud()`, already fixed and tracked via migration `0311`.

## Notes
- The Claude Code auto-mode classifier blocked `apply_migration` (DDL) against both ehow and madagins on first attempt, with no reason given beyond "Blocked by classifier." Retrying the *identical* call later in the session succeeded both times, with no code or settings change in between — no root cause identified. Plain `execute_sql` DML (a data DELETE, dependencies checked first) was never blocked in the same session. Per the tool's own denial instructions, did not attempt to route around the block via another tool — surfaced it to Royce and retried only when explicitly told to. Detail in Claude memory `feedback_no_live_supabase_access_eq_field_session.md`.
- `field_people_iud()` on madagins was, as of a 2026-09-09 check, not wired to any trigger on `field_people` (inserts there fail outright rather than reaching the function). This session's fix patches the function itself regardless — safe and idempotent whether or not a trigger is wired — but didn't independently re-verify the trigger status today. Worth checking before assuming madagins's roster-write path is actually protected end-to-end, vs. just "correctly patched, not yet reachable."
- A WebFetch summary of eq-context's `IDENTITY-MODEL.md` invented a different filename (`UNIFIED-IDENTITY-AND-PERMISSIONS.md`) that would have broken the shared-fn-guard CI check had it gone uncaught — caught by cross-checking via `gh api` before it reached a commit. Treat WebFetch summaries of exact technical strings (filenames, identifiers) as unverified until independently confirmed, especially when a CI gate depends on getting the string exactly right.

---

# Session 2026-09-14 (cont.) — `field_people_iud()` provenance gap traced, documented, and shipped to live ehow

*Concurrent with the session above — same function, different thread. That session fixed the Madagins hardcoded-tenant-fallback bug itself (PR #977); this one traced and documented a second, unrelated undocumented change on the same function (a `v_actor_id` worker self-write allowance) found while investigating the first. Both landed live today; confirmed at close that they coexist cleanly on the current function body.*

## Built
- Root-caused eq-shell's 2026-08-24 `0274` migration collision with exact timestamps (live `app_data._eq_migrations` query) — two same-day PRs claimed the same migration number, second silently overwrote the first's `activated_by`/`activated_at` reactivation stamp on `field_people_iud()`.
- Confirmed (both ledgers, exhaustively) that `field_people_iud()`'s live `v_actor_id`/`user_id = v_actor_id` worker self-write allowance has no committed source in either eq-field or eq-shell — hand-applied, provenance not fully traceable.
- Confirmed the Contacts "Restore" button is unaffected (it calls `field_people_removed_iud()`, already correctly tracked via migration `0311`) — not a live regression.
- Shipped [eq-field PR #980](https://github.com/eq-solutions/eq-field/pull/980): a comment-only migration documenting the `v_actor_id` provenance, deliberately not a body edit (PR #977 was concurrently open on the same function at the time). Built in an isolated worktree per this repo's shared-checkout rule.
- Fixed a real CI gap surfaced by #980: `generate-tenant-provision-sql.mjs --self-test` requires every migration file classified in its MANIFEST — added the missing entry.
- Rebased #980 through a real git conflict when PR #977 merged mid-session (conflict was in the shared MANIFEST array only, by design — the migration content itself never touched the same ground as #977).
- Merged #980 (`6efc56c`) and applied its `COMMENT ON FUNCTION` live to ehow, both on Royce's explicit instruction. Verified both independently: Netlify production deploy via the Netlify API (commit `6efc56c`, live 13s after merge) before confirming the DB comment separately via `obj_description()` — never conflated "merged" with "applied."
- While verifying, confirmed PR #977 (the session above) had already been dispatched to ehow — live body carries both its null-tenant guard and this session's `v_actor_id` documentation, coexisting cleanly on the current function.

## Decided
- Royce: add Supabase MCP access (via connector UI) to unblock the investigation.
- Royce: backfill the provenance as a comment-only migration rather than waiting on PR #977 or a full body rewrite.
- Royce: merge PR #980.
- Royce: apply the migration live to ehow.

## Deferred
- None from this thread — fully closed. See `eq/pending-archive.md` for the full write-up.

## Notes
- The 0274 numeric-collision hygiene check (`check-migration-hygiene.mjs`) didn't exist yet on 2026-08-24 — it was only added after a *later* collision (0298/0299, 2026-08-31→09-01). Worth remembering if another pre-08-31 same-numbered pair ever surfaces.
- `field_people_iud()` being `SECURITY DEFINER` means a same-table RLS policy (like Royce's own `staff_own_or_manager_write`, PR #956) is inert against this function's own internal writes — a recurring subtlety worth flagging early if this function comes up again.
- Full technical trail (exact SQL, ledger rows, PR-by-PR reasoning) lives in eq-field's own Claude memory (`incident_field_people_iud_provenance_gap.md`), not duplicated in the substrate — that memory file is now the canonical reference if this function's history needs re-deriving.
- If a `spawn_task` chip referencing "field_people_iud v_actor_id provenance" (per PR #977's own migration header — see the session above) is still sitting in Royce's queue, it's resolved by this session and can be dismissed.

---

# Session 2026-09-10 — Certificate "who's included" picker folded into one mechanism, merged + live

## Built
- Reviewed the document sign-off certificate flow (`CertificateActions.tsx` / `CertificateAudienceModal.tsx` / `push-document-audience.ts`) after Royce flagged likely "wiring from previous attempts." Found two separate "who's included" mechanisms doing the same job: a per-site dropdown (filters via `covered_sites` tags, only shown once a signer had been tagged) and a newer team/hand-pick modal built the same week. Confirmed live in the actual backend filter branch (`buildCertificate`) that folding the dropdown into the modal needed zero backend changes — `site_id` already only filters when `signer_ids`/`crew` is absent, the same branch the dropdown used.
- `CertificateAudienceModal.tsx`: added a third "By site" tab (shown only when the document has a tagged signer), reusing that exact filter path. Site stays a label-only field on the other two tabs.
- `CertificateActions.tsx`: dropped both `DropdownMenu`s; the plain whole-document download links are now always visible instead of hidden behind a dropdown.
- Verified via `pnpm run build` (clean) and `eslint` (clean). Committed (`404580f4`, two files only — left three unrelated pending JWT-auth files already sitting modified in the shared checkout untouched) and pushed directly to `main`.

## Decided
- Royce: fold the per-site dropdown into the modal rather than keep both mechanisms.
- Royce: confirmed the session brief before any edit — a worktree name-match false alarm and the pending JWT files were both checked and cleared first.
- Royce: commit just the two certificate files, not the pending JWT files sitting in the same checkout.
- Royce: push it — asked immediately after being told push triggers eq-shell's auto-deploy.

## Deferred
- Live click-test not done, partially confirmed since — Royce viewed the live Document sign-off list and the row UI matches what shipped exactly (no more per-site dropdown, "Choose who's included…" and the plain download links always visible; SWMS-008's "Required at 1 site" badge confirms it's the one live document with a tagged signer). Still open: nobody has clicked into the modal itself and exercised the new "By site" tab.
- The direct push bypassed branch protection's 5 required status checks (GitHub's own push output said so) — local build+lint covered the same ground, but this commit never ran the repo's actual CI on GitHub.
- Both logged to `eq/pending/eq-shell.md` this close (2026-09-14) — the work itself landed 2026-09-10, this session log and pending entry were written 4 days later at close time.

## Notes
- Found three worktree branches (`worktree-certificate-site-label-only`, `-site-refinement`, `-audience-picker`) that looked like live competing work on the exact same feature — checked and ruled out: each is a stale single-commit leftover from the same-week PRs (#1888/#1889/#1890) that had already landed, not active. Worth the reflex of verifying a name match before treating it as a collision.
- Three unrelated files (`_shared/supabase-jwt.ts`, `mint-tenant-jwt.ts`, `tenant-data-proxy.ts`) sat modified-but-uncommitted in the shared eq-shell checkout throughout this session — matches digest.md's own "waiting on Royce's sign-off" auth-adjacent item. Left untouched; staged and committed only the two files this task needed.
- Royce later shared a screenshot of the live Document sign-off page and asked what he needed to do there — answered directly (explained each action, tied "Required at 1 site" to `countRequiredSites` in `DocumentListView.tsx`). No code change; folded the resulting live-confirmation fact into the Deferred item above rather than opening a new session block for a pure Q&A.

---

# Session 2026-09-14 (cont. 2) — SEC-77 hardcoded-tenant-literal RLS sweep closed out; 2 real bugs found, fixed, dispatched, verified live

## Built
- Investigated eq-shell's `tender_enrichment` RLS coverage in `tenant-migrate.yml` (direct ask) — found the whole out-of-band tender/nominations/apprentice table family is deliberately excluded from the One Pipe (misfiled control-plane migration, tombstoned 2026-07-11), not a gap. All 3 real tenants (ehow/zaap/madagins) confirmed correctly scoped as of that check.
- Extended the same live-verification to every table in `ops/security-register.md` SEC-77's ~31-table hardcoded-literal footprint, both eq-shell's ~10-table share and eq-field's ~15+6-shared share (~37 table-checks total across 3 tenant planes). Found and fixed 2 real, live bugs; everything else was already correct or a dead/nonexistent concept:
  - **`public.nominations` on zaap** — RLS enabled, zero policies, live silent lockout (not hypothetical — any authenticated EQ-tenant user got nothing back). Fixed: [eq-shell#1878](https://github.com/eq-solutions/eq-shell/pull/1878) (migrations `0316`/`0317` — the latter also revoked a superfluous anon grant on madagins's `nomination_clashes` view). Merged via admin override past an unrelated, confirmed-pre-existing control-plane drift-check regression (proved unrelated via a second, docs-only PR that failed the identical check). Dispatched fleet-wide, live-verified.
  - **`public.field_job_number_overrides` on madagins** — full CRUD `anon` grant (not currently exploitable, no anon policy exists, but broader and more real than the `nomination_clashes` cleanup — a live write grant on a populated table, and ehow's own copy has no such grant). Fixed: [eq-shell#1893](https://github.com/eq-solutions/eq-shell/pull/1893) (migration `0318`). Merged clean (no override needed), dispatched, live-verified.
- Corrected the SEC-77 ownership-attribution methodology: the original `CREATE TABLE`-grep-based "eq-shell owns ~10 tables" claim matched a family of dead, 0-row `app_data.*` stub tables (from `0002_remaining_tables.sql`, later orphaned by a 2026-06-28 out-of-band cleanup) instead of the real, live `public.*` tables — held on all 6 originally-named tables without exception. Recorded as a standing caveat against future grep-based ownership claims in this repo.
- Updated `ops/security-register.md` SEC-77 and `system/tenant-identity-drift-scoping-2026-09-09.md` four times across this arc with live-verified findings, replacing every "task running" / "not checked" / "likely covered" hedge the docs carried with a definitive, dated answer.
- Added a section 4 + summary row to `eq/sprints/2026-09-09-provisioning-completeness-followup.md` covering this whole thread.
- **Found the same 3 files this repo's other two sessions today already independently flagged (see the certificate-picker entry above, and PR #1895's own pending.md entry) sitting uncommitted a third time — confirmed at close it's the identical, already-tracked `task_b3a9c0f1`/PR #1895 fix, not new work.** Parked safely on `wip/tenant-jwt-secret-resolver-recovered-dd4b9417` before recognizing this (diffed directly against #1895 at close time: byte-for-byte the same `resolveTenantJwtSecret()` implementation, #1895 is the newer/complete version). Cross-referenced in `eq/pending/eq-shell.md` — the parked branch is redundant, safe to delete once Royce confirms. Worth noting for whoever looks at this next: these same 3 files have now been independently found uncommitted in this one shared checkout by at least 3 different sessions across 2026-09-10 through 2026-09-14.

## Decided
- Royce: record the `tender_enrichment` structural finding (no tracked provisioning mechanism for a future tenant) rather than build a fix for it now — no live risk today.
- Royce: fix the zaap `nominations` lockout now (over: record it, fix later).
- Royce: merge PR #1878 with `--admin` past the unrelated drift-check regression, once confirmed unrelated via the second-PR check (over: hold, or investigate the regression first).
- Royce: fix the madagins `field_job_number_overrides` anon grant now (over: record it, fix later).
- Royce: park the orphaned JWT-secret-resolver WIP on its own branch rather than pick it up or discard it (over: leave it uncommitted, or discard outright) — this was *before* it turned out to be redundant with already-tracked work; parking rather than discarding is exactly why nothing was lost in the meantime.

## Deferred
- The structural gap behind both fixes — no tracked mechanism creates the out-of-band tender/nominations/apprentice tables or their policies for a *future* 4th tenant plane — remains open. Only the 2 live instances found this session were fixed, not the mechanism itself.
- The other ~35 findings in `system/tenant-identity-drift-scoping-2026-09-09.md` (§1–§5, Category A/B/C) remain entirely unbuilt — pre-existing deferred status, untouched this session.

## Post-close follow-ups (same thread, after the card above)
- `wip/tenant-jwt-secret-resolver-recovered-dd4b9417` — deleted, both local and remote, on Royce's explicit instruction, once he'd seen the redundancy-with-#1895 finding.
- **Corrected the "control-plane drift-check regression" flagged above — it wasn't one.** Checked directly against the most recent scheduled `tenant-drift.yml` run (2026-09-14T04:44): function drift (the half that actually gates the build, per `check-control-plane-drift.mjs`'s own exit-code logic — `failed: args.strict && unsourced.length > 0`, scoped to functions only) is clean, run concluded `success`. Table drift (the 14 `shell_control` tables) is unchanged and still correctly informational-only, exactly as the script documents. What actually failed PR #1878 was a real, temporary function-drift violation live on jvkn at that moment — since resolved by unrelated work, not a bug in the check. Nothing to fix; this was a misdiagnosis from reading only the table section of #1878's own log, never checked whether a function violation was also present at the time.

## Notes
- The brief-gate flag is dated (`eq-brief-<date>-<session-id>.flag`) — this session crossed two date rollovers (2026-09-10→13→14) mid-thread and had to re-run `/brief eq-context` each time before any `eq-context` substrate edit would go through. Same friction logged independently elsewhere this same week — a real, recurring cost of the current gate design, not a one-off.
- This close hit a genuine three-way concurrent-append collision on this exact session log file — two other sessions each independently appended their own "2026-09-14 (cont.)" section at nearly the same time, and this section is the third. Resolved by re-fetching the freshest content each time and appending after, never overwriting — same convention already established for `docs/reflection-log.md`'s own first collision (2026-09-13). Worth treating this file's append point as genuinely contended right now, not a one-off.
- Every migration this session touched was live-verified via `BEGIN...ROLLBACK` against its real target tenant plane before being written to a file — the same discipline used throughout this repo's other RLS work this week, held consistently here too.

---

# Session 2026-09-14 — Fixed a cross-tenant Field data-invisibility bug, closed the stale `0311` dispatch question, reviewed all 18 org-wide Sentry issues

*Spans 2026-09-10 → 2026-09-14, one continuous eq-field-rooted conversation closed out today.*

## Built

- **eq-shell PR #1884, merged + live.** `token-exchange.ts`'s platform-admin Field picker minted the JWT's `tenant_id` claim from the admin's own Core session tenant instead of the tenant they actually picked — Field's RLS then silently returned zero rows for the picked tenant, with no error anywhere in the chain. Fixed: the picked tenant's own shell row is resolved and a real membership in it is required (same bar as everyone else — Field has no platform-admin RLS escalation), and the JWT + its enrichment reads are scoped to that tenant/role. A picked tenant with no real membership now gets a clear `403 not-a-member-of-tenant` instead of a silently-empty session; `FieldIframe.tsx` shows its own message for it. 2 new regression tests added; `tsc -b`/`eslint`/full suite (634 tests) clean. Merged `7816faa`, confirmed live via commit ancestry.
- Marked [EQ-SHELL-27](https://eq-solutions.sentry.io/issues/EQ-SHELL-27) resolved in Sentry after confirming the connect-worker status-badge crash was already fixed live by #1870/#1887 (co-authored with Royce, 2026-09-09) — commit-ancestry-verified against the current production deploy.

## Decided

- **Do not dispatch `0311` to madagins** (Royce's explicit ask) — investigation found the file is deliberately ehow-only (`-- Plane: ehow ONLY`, added after a real fleet-wide dispatch crashed on madagins with `42P16`) and already applied on ehow. Nothing to run on either tenant. The stale "dispatch to madagins" bullet in `eq-shell.md` (carried since 2026-09-09) is now closed and archived.
- The original bug report's own root-cause theory (a token-exchange 401 silently degrading to an empty-looking Field session) was rejected after a direct code read of `select-tenant.ts`/`verify-shell-session.ts`/`FieldIframe.tsx` — all three already fail loud on that path. The real mechanism was a JWT scoping bug one layer over, only found by tracing the platform-admin cross-tenant picker specifically.

## Deferred

- **[EQ-SHELL-23](https://eq-solutions.sentry.io/issues/EQ-SHELL-23)** — Royce's own 6 licences on the madagins tenant have been invisible since 2026-09-08 (missing `org_membership`, flagged by the deliberate alert-only `check-missing-org-memberships` monitor). Needs an operator decision to grant — the checker never does this itself by design. Not built this session; added to `eq-shell.md`.
- **[EQ-SHELL-1V](https://eq-solutions.sentry.io/issues/EQ-SHELL-1V)** — 1 stale `org_membership` remaining on SKS (down from 12 at the check's launch). Needs an operator decision to revoke. Added to `eq-shell.md`.

## Notes

- **Supabase MCP was added mid-session** (Royce, via the connector UI, same mechanism as 2026-09-09) specifically to verify the original bug report's live-membership claims — without it the investigation would have stalled on unverifiable assumptions. Org-wide reach (`sqjyblkiqonyrdobaucn`), not scoped to one project.
- **Sentry MCP (the UUID-keyed connector, distinct from the named per-repo `sentry` MCP entry that needs separate `claude mcp` auth) was already available this session without any extra setup** — used for the full 18-issue org-wide review. Worth remembering next session it doesn't need re-adding.
- Full org-wide Sentry triage (18 unresolved issues, 2026-09-13 snapshot): beyond the two deferred items above, everything else reviewed is either already-fixed-and-verified-live (EQ-SHELL-27, EQ-SHELL-22/newer-1P via `chunkReload.ts`), deliberate self-healing telemetry (the Field-handoff-stall family EQ-SHELL-20/21/26/29/2A, `EQ-FIELD-1R`'s canon-read fallback), low-volume network noise, or a Flutter/CanvasKit engine-level bug in Cards not fixable in app code (EQ-CARDS-1P). Full detail in `eq-shell.md`'s top section — not repeated here. Found at close (via `eq/changelog/eq-shell.md`, not this session's own review): a separate, more thorough pass on the exact same handoff-stall cluster had already landed [PR #1896](https://github.com/eq-solutions/eq-shell/pull/1896) the same day, fixing one genuine false-alarm (EQ-SHELL-29) inside it — this session's own "self-healing, no action" read of that cluster held up, just less deeply than that other session's.
- Confirmed the general verification pattern this substrate keeps re-teaching held up in practice twice this session: (1) a terse digest bullet ("dispatch 0311") had gone stale behind a fuller, more careful finding that was never back-propagated into it — the live migration file's own header was the actual source of truth, not the pending-item text; (2) "merged" and "live" are genuinely different claims — both PR #1884 and the #1887 connect-worker fix were confirmed live via commit-ancestry against the actual deployed Netlify commit, not inferred from merge time.

---

# Session 2026-09-14 (cont.) — Closed both deferred Sentry items; caught and reversed course on one before acting

*Continuation of the session above, later the same day. Royce asked to grant EQ-SHELL-23 (done), then complete EQ-SHELL-1V the same way (revoke) and confirm everything else still holds.*

## Built

- Nothing new shipped — this pass was data/verification only, no code changed.

## Decided

- Royce: grant `EQ-SHELL-23` — done. Inserted the missing `public.org_memberships` row (jvkn) for `85e30693-...` on madagins (`role=member`, `eq_role=manager`, `status=active`), matching the shape `shell-join-tenant.ts` already uses for a normal grant. Verified live: `shell_user_active`, `tenant_membership_active`, the `workers.staff_id` link, and the new `org_membership` are all true/present together, and the 6 licences count as visible under the same filter the Staff page uses. Marked resolved in Sentry.
- Royce: "complete that [EQ-SHELL-1V] and then confirm everything else is 100/100" — the revoke did NOT happen. Pulled the actual flagged row before acting on the earlier session's own "stale/leftover" characterization, and that characterization was wrong: `138ba173-...` (wayne.rowe@sks.com.au) had both his `shell_control.user_tenant_memberships` row and his `org_memberships` row created together 2026-09-10 as part of a real, active onboarding — a genuine, current SKS team member, not an orphaned grant. A live, fleet-wide re-check (recomputing the checker's own stale_grant logic directly against jvkn, not trusting the Sentry snapshot) found zero actual stale grants anywhere right now, and the issue hadn't fired since 2026-09-12 — it had already self-resolved before this session touched it. Nothing was revoked. Resolved in Sentry with a note correcting the earlier mischaracterization.

## Deferred

- None — both items from the prior close are now closed, one by action (grant), one by correction (no action needed).

## Notes

- **The near-miss is the actual finding here.** The previous close's own pending.md write-up called EQ-SHELL-1V "held by an identity no longer a live member of the tenant" — a label inherited from the check's own drift-class name (`stale_grant`) without re-verifying the underlying row before repeating it as fact. Mechanically applying the "mirror of the grant" fix (revoke) would have cut off a real, currently-working SKS supervisor's access. Caught only because this pass pulled the live row instead of trusting the earlier summary — the same discipline this substrate names elsewhere (verify against live, the label can lie), just this time the stale claim was one this session's own predecessor had written, not an old doc.
- **100/100 re-check, all confirmed independently against current live state, not reruns of the original checks:** PR #1884's fix commit (`7816faa`) re-confirmed as an ancestor of the *current* production deploy (`1ed81ea6...`, published 2026-09-14T05:15 UTC) — a different, later deploy than the one checked at the original merge, so this confirms the fix survived at least one more deploy cycle, not just that it once went live. `EQ-SHELL-27` reconfirmed `resolved` in Sentry, no recurrence. The `EQ-SHELL-23` grant reconfirmed present and unchanged since insert.

---

# Session 2026-09-14 (cont. 3) — SEC-77 fully closed (eq-service side); WIP branch deleted; drift-check finding corrected

## Built
- Deleted `wip/tenant-jwt-secret-resolver-recovered-dd4b9417` (both local and remote) on Royce's explicit instruction, once he'd confirmed the redundancy-with-PR-#1895 finding from the earlier close. Closes that thread for good.
- **Re-investigated the "control-plane drift-check regression" flagged in the earlier close as a real, chronic, worth-a-look item — it wasn't one.** Checked directly against the most recent scheduled `tenant-drift.yml` run rather than trusting the earlier same-day diagnosis: function drift (the half that actually gates the build) was clean, run concluded `success`. Table drift (14 `shell_control` tables) was unchanged and still correctly informational-only, exactly as `check-control-plane-drift.mjs`'s own exit-code logic documents (`failed: args.strict && unsourced.length > 0`, scoped to functions only). What failed PR #1878 was a real but temporary function-drift violation live on jvkn at that exact moment, resolved by unrelated work before this re-check — not a bug in the check itself. Corrected the record (this session's own earlier log entry, `ops/security-register.md` was never touched by the wrong claim).
- **Checked eq-service's own 3-table share of SEC-77** (`acknowledgments`/`app_config`/`audit_log`) live across ehow/zaap/madagins — the one piece of the ~31-table sweep not yet independently re-verified this pass. All clean. One moment that looked like a real bug — zaap's `app_config` policy hardcodes an `org_id` matching the register's own documented "Demo tenant ID" — resolved by checking jvkn directly rather than assuming: EQ's own `organisations.id` genuinely is that value, not a copy-paste error from ehow's Demo-tenant context. `service.audit_logs` turned out to be the best-designed policy in the entire investigation (fully dynamic, no hardcoded literal, plus a live membership check) — just stale (66 rows vs. `public.audit_log`'s 5,886), a separate already-tracked question (`task_53ac5191`) untouched here.
- **SEC-77 is now fully closed across all three repos with a live database footprint** (eq-shell, eq-field, eq-service). Final tally across the entire ~35-table sweep: `nominations` on zaap was the only real live bug found; everything else was already correct, deliberately excluded for a tenant not using that feature, or never a live thing.
- Updated `ops/security-register.md` SEC-77 and `eq/sprints/2026-09-09-provisioning-completeness-followup.md` (section 4 now marked CLOSED, summary table row updated) to record all of the above.

## Decided
- Royce: delete the WIP branch (explicit, once shown the redundancy finding).
- Royce: "/decide what worth doing" on the drift-check regression — led to re-verifying live first rather than assuming the earlier diagnosis was still current, which is what actually resolved it.
- Royce: "check the eq-service side too" — explicit instruction to complete the SEC-77 sweep's third and final repo.

## Deferred
- Nothing new from this stretch. The structural gap (no tracked mechanism provisions the out-of-band tender/nominations/apprentice/acknowledgments-family tables or their policies for a future 4th tenant) remains open, unchanged from the earlier close — still nobody's asked for it to be built, and nothing live depends on it today.
- The other ~35 findings in `system/tenant-identity-drift-scoping-2026-09-09.md` (§1–§5) remain entirely unbuilt — pre-existing, untouched again this stretch.

## Notes
- Deliberately left `eq/pending/eq-shell.md`'s `task_b3a9c0f1` (JWT-secret) section untrimmed despite touching one line in it (the now-`[x]` stray-copy bullet) — that section's remaining open items and narrative belong to a different, still-active investigation thread (PR #1895, driven by another session), not this one. Trimming or archiving it risked destroying context that thread still needs; flagging here rather than guessing, per the archive rule's own carve-out for mixed sections.
- This is the third same-day close for this session's own thread (see the two entries above). All three landed cleanly via `safe_commit.py` this time — no concurrent-append collision on this file this round, unlike the first close.

---

# Session 2026-09-14 — EQ-SHELL-21 root-caused and fixed (draw-notice threshold); EQ-FIELD-1N dropped as resolved; EQ-SHELL-1P/22 sequenced for next

*Continuation of the eq-field/eq-shell reliability thread running since 2026-09-10 (see `sessions/2026-09-10.md`, `sessions/2026-09-13.md`). Picks up the 3 items the 2026-09-13 Sentry review deferred rather than fixed same-session.*

## Built
- Re-verified all 3 of 2026-09-13's deferred items live before sprinting any of them, per instruction ("ensure they are worth chasing first") — one (EQ-FIELD-1N) was dropped, see Notes.
- Wrote `eq/sprints/2026-09-14-eq-shell-reliability-followups.md` scoping the remaining two (EQ-SHELL-21, EQ-SHELL-1P/22), each re-checked against current Sentry state rather than carried forward on stale numbers.
- Root-caused EQ-SHELL-21 ("Field accepts the handoff but never reports 'rendered'") with real evidence, not a guess: pulled all 9 live event traces (an initial 2-example iOS-only theory was disproved once all 9 were in — 6 of 9 are Windows desktop) and ran a 684-sample PostHog percentile query on accepted→rendered timing. Finding: the existing 10s notice threshold was calibrated for the handoff's mint/boot leg (fixed separately via #1896, 2026-09-13), not the draw leg — real draw times on production traffic run genuinely longer than 10s.
- Shipped [eq-shell#1899](https://github.com/eq-solutions/eq-shell/pull/1899): `DRAW_NOTICE_MS = 15_000` as its own constant (kept separate from the 10s `STALL_NOTICE_MS` on purpose — different legs, different real timing). Merged.
- Verified live via Netlify: `commit_ref` on the current production deploy matches the merge commit exactly (`1a81981f`), state ready, published 08:32:32Z (~6 min after merge, matching this repo's known deploy lag), secret scan clean.
- Checked both affected Sentry issues directly rather than assume the fix's effect: EQ-SHELL-21 has had no new occurrence since the fix went live, but it's only been a few hours against a historical ~1-3 day recurrence rate — left `unresolved` deliberately, too soon to credit as confirmation. EQ-SHELL-29 (fixed by #1896 the day before) got a fresh event today at 07:44Z, *after* that fix deployed — traced the event instead of assuming regression: it's a genuinely slow load (zero iframe-load progress ~8s after a fast 2.34s mint), correctly surfaced by the new code's `minIframeLoadWindowMs` floor, not the old mint-timing artifact recurring.

## Decided
- Royce: check the deploys and create a sprint for all 3 deferred items, but only after confirming each is still worth chasing.
- Royce: start on EQ-SHELL-21 first (per the sprint doc's own sequencing — actively growing vs. EQ-SHELL-1P/22's dormant-but-real status).
- Royce: merge eq-shell#1899.

## Deferred
- **EQ-SHELL-1P/22** (stale JS chunk after a deploy crashes instead of recovering) — diagnosed, fix shape scoped (reload-on-chunk-failure handler wrapping the router's lazy imports), not built. Sequenced second, behind EQ-SHELL-21, in the sprint doc. Untouched this session.
- **EQ-SHELL-21 recurrence confirmation** — the fix is live but not yet confirmed by silence; worth a look again in a few days rather than treated as closed today.

## Notes
- **EQ-FIELD-1N dropped, not sprinted** — re-checked live: zero new occurrences since 2026-09-10T06:55Z, unchanged across 4+ days and past two subsequent production deploys. Same CSP-gap shape already fixed elsewhere (eq-field#976); reads as a stale service-worker cache outliving a deploy by a few hours on one device, not a code gap. Marked resolved in Sentry rather than carried into the sprint on a hypothesis nothing since has supported.
- The EQ-SHELL-29 fresh-event check is worth remembering going forward: that Sentry issue ID is a generic "iframe didn't progress within its floor window" signal, not specifically a "mint-timing" signal — now that the false-alarm cause is fixed, it will keep legitimately collecting real slow-load events under the same ID. A future occurrence there isn't automatically the old bug back; check the trace before assuming either way.
- Same discipline held again this session that this substrate keeps re-teaching: "merged" and "live" are different claims, checked separately via Netlify `commit_ref` ancestry rather than elapsed time or a generic "ready" state; and a first theory (iOS-only) built from too few examples was corrected once the full event set was pulled, not defended.

---

# Session 2026-09-14 (cont.) — EQ-SHELL-1P/22 checked before building — already fixed, closed the loop instead of duplicating it

*Continuation of the entry above. Royce asked to start building the stale-chunk fix scoped in today's sprint doc; checking live branches first (per the substrate's own duplicate-work rule) found it already done.*

## Built
- Nothing new shipped — no code needed. Corrected `eq/sprints/2026-09-14-eq-shell-reliability-followups.md` and `eq/pending/eq-shell.md` to record EQ-SHELL-1P/22 as already fixed rather than open, with the original (wrong) write-up kept inline for the record rather than deleted.

## Decided
- Royce: close EQ-SHELL-1P/22 out, no code, once shown the evidence (over: one more verification pass first, or build a UX addition anyway).

## Deferred
- None — this thread is now fully closed. Nothing left open in the 2026-09-14 sprint doc.

## Notes
- **Found before writing any code, not after.** `git branch -a` plus a `gh pr list` search on "chunk"/"mime" surfaced three already-merged PRs (#1523, #1528, #1826) that the prior sprint-scoping pass never found — it had only ruled out one plausibly-named branch (`chunk-prefetch-catch`) and stopped there. [#1826](https://github.com/eq-solutions/eq-shell/pull/1826) (merged 2026-09-08, three days before this session's sprint doc was written) explicitly named EQ-SHELL-1P/22 as its motivation and added the missing Firefox MIME-type wording to the shared `ChunkErrorBoundary`/`installChunkLoadRejectionGuard` self-heal matcher (`src/lib/chunkReload.ts`).
- Confirmed fixed, not just merged, before trusting it: both Sentry issues already `resolved`, zero occurrences since 2026-09-11 across several deploys since, and eq-shell's full unresolved-issue list (checked live) has nothing chunk/MIME-related. The occurrences between the fix merging and 2026-09-11 read as tabs that already had the pre-fix bundle loaded before that deploy — the one population this exact class of fix structurally can't reach until that tab reloads on its own.
- Worth remembering next time a doc says "no existing coverage": one branch-name check isn't a search. This is the second time this exact thread corrected an "unrelated" finding (the first was `chunk-prefetch-catch` itself, checked properly at the time) — the lesson that's actually holding up is to search by the failure's own shape, not by a guessed branch name.

---

# Session 2026-09-14 (cont. 4) — GitHub MCP connector fixed: root cause was a never-completed GitHub App install, not credentials

*Rooted in eq-cards, not eq-context — no product code changed. Started from a spawned task ("MCP can't see eq-cards, worked around via `gh pr create`, is the GitHub App even installed?"), ended several turns later live-fixing a connector Royce said had "never worked."*

## Built
- Diagnosed and fixed the GitHub MCP connector's 404s on all 4 private flagship EQ repos (eq-cards/eq-shell/eq-field/eq-service). Root cause: the backing GitHub App ("Claude Github MCP Connector" by `anthropics`, `github.com/apps/claude-github-mcp-connector`) had only ever been **authorized** (identity confirmation) on this GitHub account, never **installed** (the separate step that actually grants repo access) on eq-solutions. Every earlier "disconnect/reconnect" attempt only ever re-ran the authorize step, which is why nothing changed no matter how many times it was retried. Fixed by going straight to the app's own page and clicking Install, targeting eq-solutions. Verified live immediately after: `list_pull_requests` against all 4 repos, all successful (eq-cards/eq-field empty-but-real, eq-shell/eq-service returned real PR data — eq-service's response was large enough to overflow the tool's own size limit, the clearest possible confirmation).
- Full GitHub credential inventory taken along the way (10 fine-grained PATs, 2 classic PATs, 11 authorized OAuth apps, 4 org GitHub App installs) — none of it was ever the actual mechanism; recorded so a future session doesn't re-walk the same ground. Also ruled out both real org-level gates that could have explained "it's because we're an org" (third-party OAuth policy: "No restrictions"; no GitHub Copilot Business/Enterprise, no SAML SSO) before finding the real cause — org membership was a coincidence throughout, not the mechanism.
- Surfaced, not built: `system/infrastructure.md`'s 3-compromised-workstation-PAT item (open in `ops/pending.md` since 2026-09-03) — confirmed the local `.git-credentials` file mechanism it describes no longer exists anywhere on this machine at all (git now authenticates via `gh auth git-credential`). Royce independently deleted 5 fine-grained/classic PATs as unrelated hygiene cleanup, including one named `eq-solutions` matching one of the 3 originally-flagged tokens — likely closes that one, though the exact date match was never confirmed before deletion; the other 2 ("Milmlow", "Milmlow alt") remain completely unaccounted for under those names. Detail + updated status in `ops/pending.md`.
- Verified no collateral damage from Royce's PAT deletions before or after: `notify-substrate.yml` still succeeding across all 4 repos throughout, CI green, the MCP connector's own `get_me` still authenticating the whole time.

## Decided
- Royce: deleted 5 GitHub PATs himself (the expired one, the unused one, and the `eq-solutions`/`eq-solutions-latest` pair across both token types) once shown nothing depended on them.
- Royce: went through the GitHub App Install flow himself — an account-level OAuth/consent action, not something Claude performs on a user's behalf regardless of how clearly the fix is identified.

## Deferred
- `system/git-automation.md`'s "Credential Helpers (Per-URL)" section needs a rewrite — describes a mechanism confirmed dead this session. Added to `ops/pending.md`.
- eq-service `ci.yml` has 2 pre-existing, unrelated CI failures noticed in passing while checking for collateral damage — never investigated, never previously flagged to Royce. Added to `ops/pending.md`.
- 3 remaining fine-grained PATs (`eq-context-code`/`-suite-bot`/`-token`) look redundant with each other — minor hygiene, added to `ops/pending.md`.
- The "Milmlow"/"Milmlow alt" compromised-token half of the 2026-09-03 item remains genuinely open — see the updated entry in `ops/pending.md`.

## Notes
- Declined twice to pass GitHub's sudo-mode re-authentication (passkey/password/2FA) wall when it appeared on token-detail and app-authorization pages mid-investigation — every finding that would have needed to go past that wall was left for Royce's own hands instead of worked around.
- The investigation moved through several wrong theories before landing on the real one — GitHub App installation gap → fine-grained PAT stale repo picker → public-repo-only OAuth scope → the actual Authorize-vs-Install distinction — each ruled out with live evidence (API calls, browser checks, or a direct behavioral test) rather than assumed away or left standing. Full arc preserved in Claude's own memory (`project_github_mcp_no_eq_solutions_access.md`, `project_github_credential_inventory_2026_09_14.md`) for whichever session needs this ground again.
- Used Claude in Chrome (driving Royce's own logged-in browser) for every GitHub/claude.ai settings check in this thread — all read-only navigation except the one moment Royce clicked Authorize and Install himself, live, mid-conversation.
- This session log entry hit the file's own well-documented contention problem firsthand: `safe_commit.py`'s safety check caught a 5th concurrent session's fresh append (the EQ-SHELL-21 entry above) between this worktree's fetch and its push. Re-fetched `origin/main` directly, replaced the working copy with the current upstream content, and re-appended this section after it — no `--force`, nothing else touched.
