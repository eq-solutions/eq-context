# Session 2026-08-01 — Confirmed the "no Supabase connector" finding, then closed a real EQ-tenant roster gap found via a backlog sweep

## Built
- eq-field v3.5.394 — `scripts/roster-adapter.js` `toWideList()` collision handling: a genuinely-entered roster row now displaces a stale imported one on a `(staff, date)` collision instead of undocumented first-arrival-wins; fixed a self-contradicting code comment; 3 new tests, full 21-file suite green. eq-field [PR #587](https://github.com/eq-solutions/eq-field/pull/587), merged.

## Decided
- No new open Royce decision this session — picked up a build-ready backlog item after confirming nothing else was queued and no live-click-through/Royce-gated item was available to work on instead.

## Deferred
- Nothing new deferred this session — the roster-adapter fix was fully closed (built, tested, merged) with no open follow-up.
- Pre-existing deferred items untouched: eq-shell PR #1134's live attribution check (still waiting on `reconcile-worker-sync`'s next 02:35 UTC fire on jvkn), and everything else already logged in `eq/pending.md`/`sks/pending.md` from the prior close.

## Notes
- **Re-confirmed, with a fresh tool call, that no Supabase MCP connector is linked to the claude.ai account** — Royce disputed this ("yes there is - try again"), so it was independently re-verified via `mcp__mcp-registry__list_connectors`, not just re-asserted. Explained the real distinction: this session's working Supabase access comes from a project-scoped MCP server (`claude mcp add ... --scope project`, per this repo's own CLAUDE.md instructions), which is invisible to a cloud `RemoteTrigger` routine — that needs an actual claude.ai-account connector (`claude.ai/customize/connectors`), which doesn't exist yet. No cloud routine was built; the reconcile-job check stays a manual local re-check on request.
- **Backlog-sweep finding, not previously documented in the substrate**: `app_data.schedule_entries` carries `UNIQUE(staff_id, date)` on ehow (SKS) but **not on zaap (EQ tenant)** — verified live via `pg_constraint` on both. Worth remembering for any future EQ-tenant roster work: duplicate-row protection that's assumed suite-wide because it's true on SKS is not actually true on EQ.
- **Merge collision with a concurrent session**: PR #586 (a different eq-field session, mobile drawer relabel) merged as v3.5.393 while this session's branch was in flight — both picked the same next version number independently. Resolved by rebasing onto the new `main` and renumbering to v3.5.394; not a conflict in substance, just two sessions incrementing the same counter at once.
- Ran `/brief eq-field` and `/reflect` mid-session per this repo's guard gates (brief-gate on the first Edit; reflection-gate on the first commit) — both are session-scoped, not carried over from the prior close.

---

# Session 2026-08-01 (cont.) — Traced Sentry EQ-CARDS-1C to its actual root cause; documented the landmine so it doesn't repeat

## Built
- **eq-shell CLAUDE.md now documents the function-grant landmine.** `eq_enforce_function_privacy` (SEC-16) strips `authenticated`/`anon` EXECUTE on every `CREATE OR REPLACE FUNCTION` in a guarded schema on jvkn/zaap/ehow and never re-grants it — so editing an existing RPC silently breaks it for real users unless the same migration re-asserts the grant. Also corrected a stale CLAUDE.md line claiming in-DB event triggers weren't feasible on these planes (SEC-16 already disproved this). eq-shell [PR #1147](https://github.com/eq-solutions/eq-shell/pull/1147), merged on Royce's explicit "merge PR #1147."
- **eq-context substrate updated**: SEC-16 in `ops/security-register.md` now cross-references this incident; `eq/changelog/eq-shell.md` and `eq/changelog/eq-cards.md` both carry the full record. Also committed an eq-cards `pending.md` write-up that was sitting uncommitted in the shared local checkout (authored by a different/earlier session that had already found and fixed the actual bug — eq-cards PR #191, live grant restore — before this session reached it).

## Decided
- Session opened with "what's next" (no queued task) — surfaced the 5 items flagged in the session gate's NEEDS YOU list (SEC-1, SEC-9, 2 guard-bypass findings, this Sentry error) via AskUserQuestion; Royce picked the eq-cards Sentry error.
- Given the choice of (a) add the missing grant, (b) document the landmine, (c) just resolve the ticket, or (d) something else — Royce picked (a)+(b). Turned out (a) was already done by another session; only (b) remained, plus verifying (a) was genuinely correct rather than assuming.
- Royce then explicitly asked to merge PR #1147 — merged, branch cleaned up (remote + local).

## Deferred
- Nothing newly deferred this session. The eq-cards session's own deferred items (Royce to confirm EQ-CARDS-1C stops recurring, a one-off `mint-cards-otp` 500, an eq-field duplicate-script-declaration Sentry error, an access-changed-nudge design decision, an untestable Security Group scenario) are unchanged — this session didn't touch any of them, see `eq/pending.md`.
- SEC-1, SEC-9, and the two virtiofs guard-bypass findings (F6/F7) remain open — not selected this session, no new information gathered on them.

## Notes
- **Confirmed the eq-intake 4-part Intake UX plan (trades settings, merge-gate fix, data-gaps context, dupes archive/dismiss) from the prior close was still fully shipped** — re-verified independently (local branch content-matches the merged eq-solves-intake PR #97) before moving on to the "what's next" question. No further work needed there.
- **Root-cause chain, for the record**: eq-cards migration `0111` (`CREATE OR REPLACE FUNCTION public.eq_cards_auto_provision()`, applied live to jvkn 2026-07-30 08:10 UTC) had no trailing `GRANT`. The `eq_enforce_function_privacy` event trigger fired on that DDL and stripped `authenticated`'s EXECUTE, with only `service_role` re-granted. First real signup hit it 2026-07-31 00:14 UTC; a second user retried 7 times over 3 minutes at 08:50–08:53 UTC before the pattern was caught. The eq-cards session's own migration comment attributes this to "collateral damage from a broader EXECUTE-revoke pass" — plausible-sounding but not what actually happened; SEC-16's trigger is the precise, confirmed mechanism, live-verified via `pg_get_functiondef`/`has_function_privilege` on jvkn before writing this up. Didn't correct the merged migration's comment (low-value churn on a live-applied auth migration) — wrote the accurate account into the security register and CLAUDE.md instead.
- **Same event trigger confirmed present on ehow (sks-canonical) too**, not just jvkn — direct `pg_event_trigger` query. Any repo authoring functions against either plane is exposed to the same landmine, not just eq-cards/eq-shell.
- A `system/worktree-registry.md` change was sitting modified-but-uncommitted in this checkout, not made by this session — left untouched, not staged.

---

# Session 2026-08-01 (cont.) — SKS leave-approval email: fixed the broken magic links, declined the logo optimization

## Built
- eq-field v3.5.389 — fixed `netlify/functions/approve-leave.js`'s config guard 500ing every Approve/Reject email click since `LEAVE_CANONICAL=on` went live 2026-07-29 (guard required legacy env vars unconditionally, canonical mode never set them). PR #582, merged, verified live on `field.eq.solutions`.

## Decided
- **Logo re-optimization declined.** Prepared a 4.4KB replacement for the 350KB `SKS_Logo_White_Text_Clean.png`, but Royce flagged the `sks-assets` R2 bucket as a deliberately-preserved clean-copy reference library with consumers outside this repo — steelmanned the "add new key, repoint 2 references" compromise, concluded even that touches shared canonical branding config on unproven safety, and the resize doesn't fix the actual reported bug anyway (Outlook's own image-blocking, not file size). Recommendation: leave the bucket and canonical config untouched. No upload happened.

## Deferred
- Nothing code-owed. The bucket/logo question is closed for now (declined), not open-and-waiting.

## Notes
- **Missing email logo root-caused as Outlook's default "block external images" client setting**, not a code bug — confirmed via identical markup rendering fine elsewhere and a correctly-served R2 image (200, correct `Content-Type`). Not fixable from app code.
- **Mapped real usage of the 4 logo files in `sks-assets`** — only 2 are referenced in eq-field: `SKS_Logo_Colour_Arrows_Clean.png` (app header + docx report headers, printed ~240px) and `SKS_Logo_White_Text_Clean.png` (email + sidebar + pipeline UI, 32-38px). The other 2 have no reference found in this repo.
- **Authenticated the `cloudflare-api` MCP connector** (`claude mcp` → `/mcp` OAuth flow) — confirmed it's **read-only for R2**: `list`/`get` on buckets and objects work, every `PUT` (even a 4-byte test object) fails with an auth/permission error. Can't be used for R2 uploads as currently scoped, independent of the decision above.
- Corrected my own initial guidance mid-session: first told Royce to authorize `cloudflare-api`/`cloudflare-observability` via claude.ai connector settings (wrong — those are claude.ai account-level connectors, identified by UUID-style names); caught the naming-pattern mismatch and redirected him to `claude mcp` / `/mcp` in an interactive terminal instead, which was correct.

---

# Session 2026-08-01 (cont.) — F6/F7 digest bookkeeping fix, then SEC-9 rotation risk correctly recalibrated

## Built
- `system/failures.md` — added this session's own investigating session to F6/F7's `confirmed_in`, closing the F1-pattern self-flagging loop (the session that fixes/investigates a failure was never being excluded from the recurrence scanner, so it kept re-flagging itself in every future digest).
- `ops/security-register.md` SEC-9 row + `sec9-jvkn-key-rotation-runbook-2026-07-27.md` — recorded a materially safer rotation path: jvkn already has `sb_publishable_`/`sb_secret_`-style keys provisioned (confirmed live), which per Supabase's docs are independent of the project's JWT secret and sign out zero users on rotation — unlike the runbook's original Step 1 ("rotate the JWT secret"), which would sign out every live session suite-wide. Confirmed in code (`eq-shell/netlify/functions/_shared/supabase-jwt.ts`) that every Shell/Field/Service/Cards session token is HMAC-signed with exactly the secret that action would rotate. Also live-confirmed no encrypted copy of jvkn's own key sits in `shell_control.tenant_routing` (0 rows).

## Decided
- Royce, given the now-low-cost safe path, explicitly chose to **defer SEC-9 anyway** — same calibration Royce already applied to SEC-3: real-world marginal risk assessed low since this exposure was local-machine chat only, never confirmed to have left that trust boundary. Logged as a reasoned deferral, not silently dropped.
- Royce asked whether the local chat transcript containing the original 2026-07-12 leak could just be found and deleted instead of rotating. Declined to delete it: the transcript isn't the vulnerability (the still-valid key is), and deleting a local record of a confirmed credential exposure destroys the audit trail without reducing any real risk. Located the session only via `sessions/2026-07-12.md`'s existing summary line — deliberately did not open the raw `.jsonl` transcript itself, to avoid re-reading the actual key value into another context.

## Deferred
- SEC-9 rotation itself — explicitly deferred per Royce's call above, not urgent, safe path documented and ready whenever he wants it done.
- SEC-1 and the eq-cards-adjacent items from earlier in the day remain untouched by this thread.

## Notes
- Delivered an SVG infographic (via the visualize/Imagine widget) explaining the SEC-9 risk trade-off in plain terms for Royce — old disruptive path vs. the newly-found safe path — per his request for an ELI10 explanation; not duplicated in chat text per the tool's own instructions.
- **`/close` housekeeping**: rotated `ops/pending.md`'s now-fully-resolved "Security register triage (2026-07-27)" section to `ops/pending-archive.md` (its last open item — does the possible SEC-9 second exposure change the rotation urgency — closed by this session's finding above), and corrected the stale "SEC-9 rotation runbook doesn't exist yet" line in the "Full substrate audit" section (it's existed since 2026-07-27; just never got ticked off). Both `ops/pending.md` edits raced a concurrent session's writes to the same shared checkout and were silently reverted twice before landing — same hazard the previous session's close just below already flagged; staged each file with `git add` immediately after editing to close the race window.

---

# Session 2026-08-01 (cont.) — eq-solves-service: site photos wired into the reports that needed them, a real blank-page bug found and fixed

## Built
- Built a plain-English "Reports, Explained" artifact (private page) covering all 9 report types, how tenant branding flows in from Shell, the per-tier section matrix, and where to find every generate button — Royce's opening ask this session.
- **eq-service PR #661 (merged)** — site photo wired into the PM Check Report cover (`lib/reports/pm-check-report.ts`, `lib/reports/generate-and-store.ts`). Added `siteAddress`/`sitePhoto` to `PmCheckReportInput`; cover branches to a photo-hero + site/customer caption strip when a Media Library photo exists for the site, falls back to the pre-existing flat brand panel otherwise. This is the report ~95% of customers actually receive — the highest-value gap of the report types that didn't have it.
- **eq-service PR #662 (merged)** — site photo wired into NSX Test Report (`lib/reports/nsx-report.ts`, `lib/reports/nsx-report-input.ts`), mirroring ACB's existing pattern exactly (`sitePhoto` field, hero image after the "Prepared for" block, `fetchSitePhoto()` call in the input builder). Confirmed ACB Test Report already had this working correctly — no change needed there.
- **Real bug found + fixed in the same PR**: `pm-check-report.ts` builds two Word `sections[]` entries (cover + content). Word/OOXML starts every new section on its own page by default (no `type: CONTINUOUS` set), but the content section's first child was an explicit `PageBreak()` paragraph — a second, redundant break stacked on the automatic one, producing a genuinely blank page 2 in every report with a cover page, in every complexity tier. Not a TOC-fields-not-updated rendering illusion (ruled out: reproduced even in the `summary` tier, which has no TOC at all). Verified directly against `document.xml` before and after — confirmed the cover section's closing `<w:sectPr>` now flows straight into the "Check Summary" heading paragraph with nothing in between. Removed the redundant leading `PageBreak()`.
- Checked the remaining 5 report types (Compliance Report, Field Run-Sheet, Work Order Details, Customer Scope Statement, Customer Renewal Pack) for the same site-photo treatment via a research subagent, then spot-verified its findings directly (grepped for the button label/route in `app/(app)/reports/`). None are a fit: Compliance Report is usually multi-site/customer-wide rollup (its `site_id` filter is optional and rarely set); Field Run-Sheet and Work Order Details have no cover page at all (compact print layout / one-page-per-asset repeated); Customer Scope Statement and Customer Renewal Pack both span a customer's whole contract across potentially many sites, not one site.
- Generated sample DOCX files (PM Check Report × 3 tiers, NSX) with a synthetically-drawn placeholder switchboard photo (clearly watermarked "SAMPLE PHOTO") to let Royce eyeball the new cover treatment without needing a real production upload.

## Decided
- Royce confirmed via AskUserQuestion: do **not** build the Compliance Report single-site edge case (photo would only show when the report happens to be filtered to exactly one site) — not worth the new plumbing for a corner case that has no existing customer/site-logo pattern in that cover to mirror anyway.
- Both PRs merged on explicit "merge" from Royce.

## Deferred
- Royce to spot-check a live PM Check Report and NSX Test Report from a site that has a real uploaded photo — everything so far verified via generated samples with a placeholder image, not a real production report.

## Notes
- **The Word "update fields" dialog the user saw is expected, unrelated to the blank-page bug** — the report's Table of Contents is a Word field (auto-computed page numbers/entries); Word always prompts to confirm recalculation on open. Explained this distinctly from the blank-page bug so Royce wouldn't conflate the two — the blank page was real and reproduced even in the tier that has no TOC at all, which is what confirmed it wasn't the same root cause.
- **`gh pr merge --squash --delete-branch` failed with the known local-worktree conflict both times** (`'main' is already used by worktree at eqsvc-loadtime-ux`) — verified both merges actually succeeded via `gh pr view --json state,mergedAt` before treating them as done, per the established pattern from the prior session's close.
- Trimmed the 2026-07-31 "photo uploads were failing" pending.md section per the archive rule (dropped its italic intro + `[x]` lines now that its remaining open items are just the 2 still-deferred ones) since this session resolved its 3rd deferred item (site photos) and folded that into a new section instead of leaving a stale duplicate.

---

# Session 2026-08-01 (cont.) — Self-join Field access: closed the loop and merged (continuation of 2026-07-31's gate work)

## Built
- Confirmed live on jvkn that `public.licences` and `shell_control.users` are the same database, so the still-missing "document-check trigger" from yesterday's close could be an ordinary same-database trigger rather than a cross-plane call. Also found yesterday's assumption that eq-cards needed new work to recognise `photo_id`/`white_card` licence types was wrong — both already exist as built-in types, confirmed both in eq-shell's own compliance-matrix code and live in the shared `licence_types` table Cards' own upload screen reads from (unfiltered).
- New control-plane migration (`2026_07_31b_licences_field_access_unlock_trigger.sql`): trigger on `public.licences` that auto-unlocks a self-joined worker's Field access once a current Photo ID and White Card are both on file, and only ever re-locks an account it granted itself — never an admin-invited or grandfathered one. Applied to jvkn, verified registered live.
- Confirmed end-to-end that eq-cards' upload screen already supports both document types today (reads from the same live `licence_types` table, no hardcoded filtering) — the one open question left on PR #1145.
- eq-shell [PR #1145](https://github.com/eq-solutions/eq-shell/pull/1145) merged (squash `a6c72045`) on Royce's explicit go. All CI green beforehand. Remote branch cleanup needed a manual `git push origin --delete` after `gh pr merge --delete-branch` hit the same local-worktree conflict another session logged earlier today.

## Decided
- Royce agreed with two low-friction options for giving Apprentice/Labour hire their own role automatically (role-tagged join links; bulk-inviting labour-hire crews from the agency roster) but explicitly held off building the join-links piece until PR #1145 was live — now unblocked, logged as the next candidate.
- Royce, via AskUserQuestion mid-session: confirmed hold-off-on-merge once more (a screenshot made it look like he might want an immediate merge before the trigger existed).
- Royce: "check if eq-cards lets workers upload photo ID and white card" then "merge PR #1145" — both explicit, executed in order once the check came back clean.

## Deferred
- **No "blocked pending documents" screen in Field** — a gated self-join worker still just hits a dead end.
- **Users-tab invite form still has no Field-access checkbox** — Workers-tab form got this 2026-07-30, Users-tab hasn't.
- **Live smoke test** — Royce said he'd run this himself with a test phone number; not yet confirmed done.
- **Role-tagged self-join links** — now unblocked, not yet started.

## Notes
- **This session's own session-log append was silently lost/reverted at least twice during this close**, almost certainly because `eq-context` is one shared local checkout, not isolated per session — a concurrent session's `git stash`/`rebase`/`reset` in the same directory can wipe another session's uncommitted working-tree edit with no warning beyond the Edit tool's own stale-read check (which only catches a stale *read*, not a *silent* overwrite between edits). Told Royce directly in chat rather than assuming this note alone would survive to be read.
- Same local-worktree branch-delete conflict as another session hit earlier today (`gh pr merge --delete-branch` fails when `main` is checked out in a sibling worktree) — confirmed the actual merge still succeeded via `gh pr view --json state,mergedAt` before treating it as done, then cleaned up the remote branch manually.

---

# Session 2026-08-01 (cont.) — Sprinted the prior close card's 4 "Next" items; two turned out to be false gaps, caught before writing code

Royce: "sprint those 4 items, steelman and let's build a 100/100 solution" — the 4 items were: eq-shell's mint-cards-otp 500, eq-field's duplicate-script Sentry error, a "your access changed" nudge, and a Security Group test case. Ran a steelman/decision pass first rather than building all 4 blind.

## Built
- **Corrected claim #1**: told Royce the "assign a user to a Security Group" UI didn't exist and got his go to build it. Before writing code, found it already fully exists — `AccessControlPage.tsx`'s `GroupDetailModal` (add/remove-member UI) wired to `netlify/functions/security-groups.ts` (correct upsert/delete into `user_security_groups`, with audit logging). The earlier grep that said "missing" had hit `AdminSecurityGroups.tsx`, a thin re-export stub, not the real 1301-line implementation. Zero live rows in `user_security_groups` just means nobody's clicked "Add" yet, not that the feature is broken. Nothing built — stood the task down.
- **Corrected claim #2**: told Royce (twice — once earlier in the day, once again mid-sprint) that role/entitlement changes only take effect on next login, sourced from `IDENTITY-MODEL.md` §6.3. False. `eq-shell/src/App.tsx`'s `SessionProvider` has run a 5-minute background poll since 2026-05-24 that silently re-verifies and rewrites the session in place — a role change already reaches an open tab automatically, no login required. The doc's "next login only, live propagation would require polling" framing was written six weeks after that poll shipped, contradicting it. Corrected `IDENTITY-MODEL.md` §6.3 inline (original text retracted, not deleted, matching the doc's own existing correction convention) — separate commit in `eq-context`, `c18ec1b`.
- **The actually-missing piece, once the premise was corrected**: a toast in `App.tsx` that fires only when the existing 5-min poll detects a real same-tenant role diff — "Your access just changed — refreshed automatically." No new polling, no forced re-auth, self-contained inside `SessionProvider`. `tsc -b`/`eslint`/`pnpm test` all clean (274/274, 4 pre-existing lint findings confirmed unchanged via `git stash` diff against `origin/main`). eq-shell [PR #1148](https://github.com/eq-solutions/eq-shell/pull/1148), merged (squash `8581b24d`), Royce's "merge #1148" go.
- Left `mint-cards-otp` (Sentry EQ-SHELL-13) and eq-field's duplicate-script error (EQ-FIELD-W) untouched — both single-occurrence, no repeat, no reproducible signal to build against. Watching, not fixing blind.

## Decided
- Royce, via AskUserQuestion: build the Security Group UI (superseded once found to already exist) and use an in-app banner (not forced re-auth) for the access-change nudge — the banner became a smaller toast once the real mechanism surfaced.
- Royce: "merge #1148" — merged despite one failing required check (`Schema drift + anon-grant + policy-lint`), confirmed unrelated first: it was flagging `public.sync_field_access_unlock`, a function from a different in-flight PR (#1145) applied live without its migration file landing yet. This PR touches zero schema.

## Deferred
- Nothing new — the three items already logged in `eq/pending.md`'s "eq-cards + eq-shell" section (EQ-CARDS-1C 24h confirm, mint-cards-otp watch, eq-field script-error watch) carry forward unchanged.

## Notes
- **Two Rule-0.5-class near-misses in one sprint, both caught before code was written, not after.** Neither would have been caught by re-reading old context — both required going back to the live repo (`git show origin/main:<file>`, `git log -S` on the poll's introduction) rather than trusting an earlier grep or an earlier session's own doc. Worth the pattern: when a "gap" is about to become a build task, verify the gap against current source one more time immediately before writing code, not just at initial discovery.
- **Live concurrent-session collision during close, handled without data loss.** Pushing the final worktree-registry update raced against a different session's substantial backlog-dive work (`scripts/rotate_pending.py`, `ops/pending-archive.md`, `eq/pending.md`, `eq/changelog/eq-shell.md`) landing in the same shared `eq-context` checkout. Stashed their in-progress files narrowly (never touched via `git add -A`), rebased, pushed. The first stash pop conflicted; rather than force it, diffed every contested file against the now-current `HEAD` (which already contained their own properly-committed, more advanced version, e.g. a newer `scripts/rotate_pending.py` with 'verify' bullet classification their stashed draft didn't have yet) and confirmed every one of my stashed copies was stale/superseded before discarding — restored from `HEAD`, not from the stash. No content lost either direction; their finished work stayed exactly as they'd committed it.
- Session date rolled from 2026-07-31 to 2026-08-01 partway through (Sydney time) — this file continues under today's date per the convention other concurrent sessions in this same checkout were already using tonight.

---

# Session 2026-08-01 (cont.) — Read-only: SEC-9 recap, eq-ui worktree confirmed unused

## Built
- Nothing — no Edit/Write this session, no repo touched.

## Decided
- Nothing new.

## Deferred
- Nothing new. SEC-9, SEC-1 remain open per the "F6/F7 digest bookkeeping / SEC-9 recalibrated" entry above — no new facts gathered this thread.

## Notes
- Answered "list out SEC-9" for Royce by reciting the already-current register row — no new investigation.
- Investigated `eq-ui/.claude/worktrees/elastic-blackwell-1a85b8` (recycled mid-session to `blissful-rosalind-75c061`, same branch): confirmed zero commits ahead of `main`, no PR, clean tree — the same empty/unused folder `worktree-registry.md` already flagged on 2026-07-27. Told Royce directly rather than assuming it held real work.

---

# Session 2026-08-01 (cont.) — Secrets governance recap + SEC-1/SEC-9 plain-English summary

## Built
- **Memory file updated** (`C:\Users\EQ\.claude\projects\C--Projects\memory\project_secrets_governance_audit.md` + MEMORY.md index) — corrected several stale claims from the June audit: `eq-field-notifications` Resend key confirmed deleted (was "to be deleted"), "pre-go-live" framing updated to "post-go-live — SKS is live as of 2026-07", SEC-1 and SEC-9 added as outstanding P0 findings.
- **Pre-compaction**: RESEND_API_KEY rotated on eq-service (Netlify + .env.local) and eq-field (Netlify). CRON_SECRET and UNSUBSCRIBE_SECRET rotated on eq-service. GitHub `production-ops` environment created on eq-solutions/eq-service with `main`-only branch rule — `backup.yml` jobs now actively gated.
- **eq-service redeploy triggered** (`netlify deploy --trigger`, deploy `6a40e906fddd64d93a55b577`) to pick up the Resend key rotation.

## Decided
- Nothing new. SEC-1 and SEC-9 standing decisions confirmed via security register review (read-only).

## Deferred
- All existing outstanding items unchanged. See `ops/security-register.md` for full status of SEC-1 (decommission at cutover, no date set) and SEC-9 (rotate whenever convenient, safe path now documented).

## Notes
- Royce asked "what are they again?" post-compaction about SEC-1 and SEC-9. Read `ops/security-register.md` directly (not the compacted session summary) to ensure the answer was current. Both findings have clear standing decisions attached — no new action triggered.
- **SEC-1**: sks-nsw-labour anon key exposes people/timesheets/leave_requests/audit_log. Decision: no interim hardening; fix = decommission at cutover. Blocked on 0/3-4 clean parallel-run weeks.
- **SEC-9**: jvkn (eq-canonical) service_role key pasted into a chat transcript 2026-07-12. Safe rotation path exists (sb_secret_ keys, no session wipe). Royce's call: defer, same window as SEC-3.

---

# Session 2026-08-01 (cont.) — Closed out the 3 error reports carried from the prior close: 1 confirmed fixed, 1 already fixed, 1 genuinely fixed here

Royce: "check EQ-CARDS-1C for recurrence and fix the three error reports" — direct instruction to act, not just watch.

## Built
- **EQ-CARDS-1C** — re-checked Sentry: zero new occurrences since the grant fix landed (last event 2026-07-31T08:53Z, well before the fix). Marked resolved in Sentry with the fix explained in the comment.
- **EQ-FIELD-W** (eq-field's duplicate `INCIDENT_TYPES` declaration) — turned out already fixed. eq-field [PR #542](https://github.com/eq-solutions/eq-field/pull/542) (v3.5.358, merged 2026-07-27, one day after the only report) renamed `diary.js`'s copy to `DIARY_INCIDENT_TYPES` to stop it colliding with `incidents.js`'s own top-level const. Confirmed live on `field.eq.solutions` directly (`curl` against the served `diary.js`/`incidents.js`, not just trusting the deploy timestamp). Marked resolved in Sentry, no code change needed.
- **EQ-SHELL-13** (`mint-cards-otp` returned 500) — the one real bug. Root-caused: `mint-cards-otp.ts` calls Supabase's `generateLink({type: 'magiclink', email: user.email})` with no check that `user.email` is actually set. Phone-only self-join workers can legitimately have no email (confirmed nullable at the DB level; the email-capture nudge, PR #1125, is a dismissible nag, never a gate) — hitting this with no email produced an opaque 500. Added an explicit guard returning `422 {error: 'no-email'}` before the call, and updated `CardsIframe.tsx` to treat that distinctly from a real mint failure (no Sentry alert fired for an incomplete profile, only for genuine faults). `tsc`/`eslint`/`pnpm test` all clean, 2 pre-existing lint findings confirmed unchanged via `git stash` diff. eq-shell [PR #1150](https://github.com/eq-solutions/eq-shell/pull/1150), merged (squash `8d5dc9bf`).

## Decided
- Royce: "check EQ-CARDS-1C for recurrence and fix the three error reports" — executed directly, no clarifying question needed (each item's prior write-up already specified what "fix" would mean).
- Royce: "merge #1150" — merged after all required checks passed (schema-drift check, which had blocked the previous PR for unrelated reasons, passed clean this time).

## Deferred
- Nothing — all three items fully closed this session. Archived the whole "eq-cards + eq-shell" pending.md section to `pending-archive.md` (zero open items left), per the file's own rotation rule.

## Notes
- **Live-verified rather than inferred** on both "already fixed" claims (EQ-FIELD-W's live deploy, EQ-CARDS-1C's Sentry event history) instead of trusting a commit merge date alone — matches the pattern this session already learned the hard way earlier tonight (merge ≠ applied, code existing ≠ code deployed).
- `mint-cards-otp`'s current blast radius is 0 — all 4 active `sks` supervisors have an email today (confirmed live) — the fix closes the code-level hole for whoever hits it next, not an active incident.

---

# Session 2026-08-01 (cont.) — eq-field: safety photos were silently never reaching Storage — dead JWT reference fixed, merged, deployed

Incoming task brief flagged `scripts/safety.js`'s `_photoJwt()` calling `_ensureDataJwt` — asked to verify live, trace its history, fix, and PR (not auto-merge, given it's a real data path).

## Built
- **Verified the bug live**: full-repo grep confirms `_ensureDataJwt` is referenced exactly once anywhere in this repo (`scripts/safety.js`) and defined nowhere. `typeof _ensureDataJwt !== 'function'` was always true, so `_photoJwt()` silently returned `null` on every call — every Prestart/Toolbox photo has used inline base64 the entire time, never once reaching the `safety-photos` Storage bucket.
- **Traced the root cause via `git log -S`**: it was never a real function that got renamed — it's a typo baked into the very first commit of the photo-Storage feature (v3.5.237/238, PR #403, 2026-07-04). A separate, independent confirmation: a 2026-07-10 ESLint `no-undef` hardening pass (PR #438) actually caught this exact undefined reference via its source-derived globals scan, but misclassified it as one of five *intentional* feature-detected no-ops and whitelisted it in `eslint.config.js` instead of fixing it.
- **Fixed**: `_photoJwt()` now calls the real `_getDataJwt()` (`scripts/supabase.js`) — same direct cross-file reference pattern already used without a guard by `scripts/realtime.js` and `scripts/supabase-rpc.js`; confirmed `safety.js` lazy-loads well after `supabase.js` so load order is safe. Removed the stale `_ensureDataJwt` whitelist entry from `eslint.config.js`.
- **Verified the fix's targets live**: queried `storage.buckets`/`pg_policies` on ehow (SKS live tenant) via Supabase MCP — the `safety-photos` bucket and its 4 RLS policies (from PR #403) are unchanged and already gate correctly on `auth.jwt().app_metadata.tenant_id`, exactly the claim `_photoJwtTenantId()` extracts from this JWT. No DB/RLS change needed.
- Bumped to v3.5.396 (`app-state.js`/`sw.js`/`index.html` banner), added the `docs/reflection-log.md` entry the repo's commit guard requires. 23/23 local tests pass; `npx eslint@9` diffed against the unmodified baseline via `git stash` shows zero new errors/warnings.
- Opened eq-field [PR #590](https://github.com/eq-solutions/eq-field/pull/590), smoke-tested the deploy preview (clean boot, no console errors, footer confirms v3.5.396) before flagging it for review.

## Decided
- Royce: "merge" — merged (squash, branch deleted). Netlify auto-deployed; confirmed live on `field.eq.solutions` via the production `sw.js` banner showing v3.5.396.

## Deferred
- **Live click-through not done** — sign in via Core, add a photo to a Prestart/Toolbox, confirm it actually lands in the `safety-photos` bucket rather than staying inline base64. Needs a real login only Royce can do. _(added 2026-08-01)_

## Notes
- Branched fresh off `origin/main` rather than continuing on the session's assigned prior branch (`claude/verify-pin-core-only-serverside`) — checked first and found that branch's own PR (#575) had already merged, so it was dead weight; avoided bundling an unrelated fix onto a stale branch.
- This is a good example of a bug class worth remembering suite-wide: a `typeof`-guarded reference to a genuinely nonexistent name degrades gracefully (no crash, no log, no Sentry event) by design — which is exactly why it can go undetected for a month even after an ESLint pass was specifically built to catch undefined references. The eslint pass caught the symptom (undefined name) but a human had to misjudge it as intentional for the whitelist to paper over the real bug; worth double-checking any future `HAND_GLOBALS` addition actually corresponds to a deliberately-absent helper, not a typo for one that exists under a different name.

---

# Session 2026-08-01 (cont.) — Photo-Storage fix confirmed live by Royce; read-only SEC-1/SEC-9 recap

## Built
- Nothing new — closed the loop on the PR #590 fix above. `eq/pending.md`'s write-up rotated to `eq/pending-archive.md` (zero open items left, per the archive rule) with the confirmation appended as its final line; `eq/changelog/field.md`'s PR #590 entry updated to drop the "still needs Royce" caveat.

## Decided
- Nothing new. Royce reported the deferred live click-through succeeded ("photo saved - well done") — no further action needed on this item.

## Deferred
- Nothing new. The PR #590 thread is now fully closed, no open items remain.

## Notes
- Royce also asked to check SEC-1 and SEC-9 — answered read-only, directly from `ops/security-register.md`, no new investigation or state change. Both remain open by Royce's own standing decisions, not by neglect: SEC-1 (sks-nsw-labour anon key exposes PII) stays unfixed until Field replaces that app at cutover, per his 2026-07-20 "do not touch sks-nsw-labour" instruction; SEC-9 (a jvkn service_role key pasted into a 2026-07-12 chat transcript) has a cheap, session-safe rotation path documented since 2026-08-01 but Royce explicitly re-deferred it same day, same risk calibration as SEC-3 (local-machine-only exposure, never confirmed to have left that boundary). Nothing about either finding changed this turn — recap only.

---

# Session 2026-08-01 (cont.) — pending.md rotation overhaul, two stale-item closeouts, and a real Suppliers access gap found + fixed

## Built
- **Closed two stale claims found while picking work off `eq/pending.md`**: eq-shell PR #1117 (archived-staff dashboard leak) was logged as "not yet merged" — it had already shipped and was live; eq-shell PR #973's one flagged behavior change (the "Overdue follow-up" quote filter) was logged as "needs a second look before merge" — already merged too, and the flagged change turned out to make zero difference on real data (1/1 SKS, 0/0 eq, identical either way). Both closed out with live evidence, not just re-reading the merge.
- **Rebuilt `scripts/rotate_pending.py`'s rotation rule** (Royce's `/decide`, chose "separate verify-queue file"): a `- [ ]` line whose only blocker is Royce's own live click-through now moves to a new `verify-queue.md` per tier instead of pinning an otherwise-finished write-up in the live file forever. 15 sections in `eq/pending.md` alone were stuck live by exactly one such line. Full test coverage added (43/43 passing), ran for real: `eq/pending.md` 2966 → 2822 lines, new `eq/verify-queue.md` (14 items) and `sks/verify-queue.md` (1 item) created. Also fixed the nightly workflow, which didn't know about the new files and would have silently stopped updating them after this one run.
- **Verification pass across the 14-item verify-queue** (Royce: "i think most of this is finished?"): checked for revert commits (none, 5 repos), Sentry regressions (none), and prior confirmation (none) — "unfalsified," not fully confirmed, since a silent UI bug wouldn't show up in any of those. Flagged one item — the Suppliers workspace-switch fix — as worth an actual look rather than trusting the quiet, since it was a data-leak-class bug that wouldn't necessarily throw an error even if it recurred.
- **Explained the Suppliers workspace-switch bug in full** and confirmed the fix (`clearTenantJwt()`) is still wired into `TenantSwitcher.tsx` on current `main` — code-verified, upgraded from "probably fine" to "actually confirmed."
- **Found and fixed a real access gap** (Royce: "check if we wired in security settings for suppliers... dont want apprentices, labour hire, sub contractors able to see"): the database function behind the Suppliers directory correctly hid login/password from those roles, and the app correctly hid the whole page from them — but the function itself had no role check on the rest of the directory (names, contacts, emails, phones), so a direct call bypassing the app would have handed it over to anyone signed in, any role. Fixed to match the app's own rule, mirroring the exact pattern already used for login/password. eq-shell [PR #1151](https://github.com/eq-solutions/eq-shell/pull/1151), merged, dispatched to both SKS and EQ, confirmed live via direct database check on both.
- **Checked the other two Suppliers permission keys too** (Royce: "check the other suppliers permission keys too") — both clean (the edit/delete gate covers all three write actions in one place; the login/password gate is unchanged and correct). Chasing the "individually granted extra access" exception on the read gate surfaced a suite-wide finding: the login-token field that's supposed to carry an individual access grant, or an explicit block, is never actually written onto the token by any of the 15+ places a login token gets issued — so database-level checks that read it (this one and the original login/password one) can never actually see it.

## Decided
- Royce, `/decide` on managing `pending.md`: chose the separate verify-queue-file approach over time-boxing or doing nothing.
- Royce, on PR #973's flagged behavior change: keep the new, stricter definition — confirmed after seeing it made zero live difference either way.
- Royce, `/decide "then build"` on the Suppliers directory gap: ship it. Built, merged, and dispatched same session.
- Royce, "may as well fix now?" on the deeper token gap found while checking the other permission keys: raised, investigated, and **not** built this session — see Deferred. Explained the real scope (touches every login path in the app, a genuine auth change) before proceeding, rather than building under momentum from a casual "may as well."

## Deferred
- **The login-token gap is a real login-system change, not a quick patch** — fixing it properly means adding a field to every login token the app issues, which needs its own deliberate pass, not a same-session follow-on to a Suppliers fix. Recommended holding it until there's an actual live reason to use either mechanism (today: zero people ever placed in an access group, zero denial rules touching Suppliers) rather than touching how every person logs in to close a currently-theoretical gap. _(added 2026-08-01)_

## Notes
- **Real near-miss on data loss, not caused by this session's own actions**: mid-edit on `scripts/rotate_pending.py`, a concurrent session's own git operation on this same shared checkout silently reverted this session's uncommitted changes. Nothing was actually lost — the concurrent session had safely stashed everyone's dirty files first, matching an established pattern in this repo — but reconciling that stash would have mixed in unrelated sessions' work, so the edits were simply redone directly and committed after each file instead of batched, which avoided the problem recurring.
- **The 7 real `tenant_role_overrides` rows checked while investigating the token gap are unrelated to Suppliers** — one explicit denial (apprentice can't see the Intake tab) and six explicit grants (mostly labour-hire-adjacent employee/supervisor extensions on the SKS tenant). Confirmed none touch any Suppliers permission key, which is why today's fix is unaffected by the broader gap.

---

# Session 2026-08-01 (cont.) — eq-shell: audit-logged every permission denial suite-wide, then reconciled with a concurrent session that built the same foundation

Task brief described `requirePerm()` already extended to audit-log denials, with 12 files migrated. Live repo check (any branch, any worktree) found none of it existed yet — surfaced the gap to Royce via AskUserQuestion rather than assuming the brief was simply wrong; Royce chose "build it from scratch."

## Built
- Extended `requirePerm()` to fire-and-forget log every permission denial to the audit log, then migrated all 63 remaining screens/actions still on the old silent-403 pattern to use it. Full design/decision pass (`/decide`) run before building.
- 6 parallel background agents split the 63-file migration into batches (each in its own scratch worktree to avoid file collisions); merged their output back into one branch, added test coverage, re-ran the full test/build/permission-matrix-check suite clean, opened PR #1154.
- **Mid-session discovery**: a concurrent session had merged PR #1153 with the *same* `requirePerm()` foundation and the *same* 12 "priority" files 20 minutes into this session's own build — confirming the original task brief was accurate, just not yet landed on `main` when first checked. Rather than force a conflicting merge, reconciled: took #1153's version of the 12 shared files (its design is cleaner — the session/JWT principal carries its own user-id/tenant-id instead of threading them through every call site), then adapted this session's other 51 files to match. Re-verified everything green, then merged PR #1154 on top of #1153.
- Net result live on `core.eq.solutions`: every hard-gated action in the app (admin, staff, ops, reports, intake, etc.) now logs a "denied" row to the audit trail when someone without the right role tries it. Zero screens left on the old silent pattern.

## Decided
- Royce: "Build it now, from scratch" (via AskUserQuestion, once the brief/live-repo mismatch was surfaced) — one of three offered options (the others: point to where the missing work actually lives, or skip the logging and just wire up plain call sites).
- Royce, `/decide "then build"`: ran the full decide pass (frame/steelman/pre-mortem/value/feasibility) before the 63-file build — recommendation was to build the whole batch in one pass rather than defer the non-priority 51, which is what happened.
- Royce: "merge #1154" — merged after all 4 required checks passed clean (typecheck·test·lint, schema drift, gitleaks, migration hygiene) plus a working deploy preview.

## Deferred
- **Royce to click through live**: sign in as a non-manager, trigger a denied action, confirm the audit-log row actually appears. Needs a real login Claude can't perform on Royce's behalf. _(added 2026-08-01)_

## Notes
- **This is a genuine two-session race, not a stale brief.** PR #1153 merged at 00:31 UTC; this session's own repo verification (before starting the build) ran earlier in that same window and correctly found nothing on `main` yet. Neither session did anything wrong — worth remembering that "verify against live main" can still race a concurrent session's in-flight merge, especially on a fast-moving foundational file like `permissions.ts` that multiple sessions might reasonably pick up the same night.
- **6-way parallel-agent worktree cleanup**: each migration batch ran in its own isolated worktree (`isolation: "worktree"`) to avoid file-write collisions across agents; all 6 were torn down (`git worktree remove` + branch delete) immediately after their output was copied into the main branch, so nothing was left behind in `eq-shell`'s worktree tree.
- `gh pr merge --squash --delete-branch` hit the same local-worktree conflict several other sessions logged today (`'main' is already used by worktree at ...`) — confirmed the merge actually succeeded via `gh pr view --json state,mergedAt` before treating it as done, then deleted the remote branch manually via the GitHub API. Same established pattern as the rest of today's sessions.

---

# Session 2026-08-01 (cont.) — eq-shell PermKey build report: false alarm, watched a concurrent session's fix land instead of duplicating it

Incoming report claimed `pnpm run build` failed on `origin/main` because `ops.view_suppliers`/`ops.manage_suppliers` weren't in the `@eq-solutions/roles` `PermKey` union, and recommended a two-repo release bump.

## Built
- Nothing — no Edit/Write this session. The report didn't reproduce and the real build issue found while checking it was already being fixed live by another session.

## Decided
- Nothing needing Royce's call. Asked only a process question (how to handle a concurrent session actively editing the same checkout) — Royce said wait and re-check; did so via a background `Monitor` poll rather than manual sleeps.

## Deferred
- Nothing new. The one open item from the concurrent session's own work (Royce to click through and confirm a denied action lands in the audit log) is already logged just above — not duplicated here.

## Notes
- **The reported PermKey gap was a phantom, same class as the 2026-07-27 "stale `node_modules`" false alarm logged earlier in this file — but a different cause this time.** Checked live: `package.json`/`pnpm-lock.yaml` on `origin/main` were already pinned to `@eq-solutions/roles#v2.5.8`, and `node_modules` already had both `ops.view_suppliers`/`ops.manage_suppliers` in the `PermKey` union (added upstream 2026-07-26, [eq-roles PR #14](https://github.com/eq-solutions/eq-roles/pull/14)). No reinstall was even needed this time — the environment was already correct; the report itself was stale.
- **While checking, found the actual live problem: `claude/perm-audit-logging` was mid-merge with real conflict markers in several auth files**, and it resolved itself while under observation (`MERGE_HEAD` cleared, `HEAD` advanced to a merge commit) — this was the concurrent session logged just above landing PR #1154's reconciliation with PR #1153. Post-merge the build genuinely failed for an unrelated reason (`requirePerm()` opts type didn't yet accept `actor_id`/`tenant_id`, which ~45 call sites were passing) — watched that get fixed live (commit `7ab9d847`, "reconcile with #1153's requirePerm() signature") rather than starting a duplicate fix, then re-ran `pnpm run build` to independently confirm green.
- Deliberately did not touch `eq-shell` or `eq-context`'s `pending.md`/changelog beyond this log entry — the substantive work and its record both belong to, and were already fully written by, the concurrent session.
