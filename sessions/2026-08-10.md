# Session 2026-08-10 — Delete an approved leave request (SKS), then found + fixed a live Calendar regression in EQ Field

## Built
- **SKS NSW Labour** ([PR #78](https://github.com/eq-solutions/sks-nsw-labour/pull/78), merged `5aecc42`, v3.10.111): manager-only Delete action for leave requests (`confirmDeleteLeaveRequest`/`deleteLeaveRequest` in `scripts/leave.js`). For an Approved request it first reverses the roster write-back (`clearLeaveFromSchedule()` — inverse of `writeLeaveToSchedule()`, only clears a day if the cell still holds exactly that request's leave code) then hard-deletes the `leave_requests` row. Confirmed live on `sks-labour` (`nspbmirochztcjijmcrx`) that RLS already permits the DELETE, same shape as every other write. `realtime.js` already handled the DELETE event server-side — this was the missing UI trigger.
- **EQ Field** ([PR #674](https://github.com/eq-solutions/eq-field/pull/674), merged `8beb80a`, v3.5.473): fixed `scripts/calendar.js`'s `_getCodeForDate()` to overlay approved leave live via the existing `approvedLeaveCode()` (roster-rules.js), matching the pattern `roster.js`/`dashboard.js` already use. Added the same `_ensureLeaveLoaded()` self-heal kick `dashboard.js` has, for a Calendar-first deep link. Also corrected `hardDeleteLeaveRequest()`'s confirm copy, which had gone stale post-migration ("roster entry not affected" — no longer true of the display, only of the underlying DB cell).

## Decided
- Royce: "should be good to go" → pushed SKS branch + opened PR #78; confirmed "Merge now" via AskUserQuestion → merged.
- Royce: "triple check and audit the truth then build the feature — we will do eq field after this" → full audit done before building (no delete path existed anywhere in SKS; realtime DELETE handling already wired but unused).
- Royce, via AskUserQuestion on eq-field scope: **"Fix Calendar gap + tighten delete confirm copy"** (over "fix Calendar gap only" or "nothing today") — after being told eq-field already had delete/roster parity (nothing to mirror there) but a real, previously-undiscovered Calendar regression was found while tracing it.
- Royce: "merge PR #674" — explicit merge instruction, executed.

## Deferred
- **Live click-through not done on either PR** — this sandbox's browser can't reach the real Supabase project (SKS) or the canonical config service (EQ Field, blocks even the demo tenant from booting). Both were verified instead via standalone harnesses running the actual edited code (stubbed network calls for SKS; a `vm`-loaded real `calendar.js` + real `roster-rules.js` for EQ Field) plus full test suites and eslint. Logged in `sks/pending.md` and `eq/pending.md` for Royce to confirm on a real device/tenant.

## Notes
- **EQ Field's leave/roster architecture has fully diverged from SKS** — not something to port between the two apps going forward. SKS still writes approved leave into `schedule` cells and now has to reverse that on delete. EQ Field removed the write-back entirely in v3.5.281/282 (PR #433, 2026-07-10): `leave_requests` is the single source of truth, and the roster/calendar overlay it live at render time — a delete needs nothing reversed. If SKS ever merges into EQ Field, SKS's write-back model is the one that should die. Recorded in memory (`project-sks-eqfield-sync.md`).
- **The EQ Field Calendar gap was a real, live, previously-untracked regression** — not something flagged in `eq/pending.md` or the digest before this session. PR #433 updated `roster.js`/`dashboard.js` for the new overlay model but missed `calendar.js`; anything approved after 2026-07-10 silently stopped appearing on the main Calendar page (Roster grid and the Leave tab's own mini-calendar were unaffected, since they don't share that code path). General lesson for future migrations of this shape: grep for every other consumer of the assumption being changed, not just the pages the PR's author happened to be looking at.
- Root checkouts of both `sks-nsw-labour` and `eq-field` were dirty with other concurrent sessions' work throughout — neither was touched. All work done in isolated worktrees (`sks-nsw-labour`: existing dedicated worktree for this session; `eq-field`: fresh `calendar-leave-overlay` worktree off `origin/main`, removed after merge).
- Confirmed live on `ehow` (`ehowgjardagevnrluult`): eq-field's `leave_requests` is `app_data.leave_requests` (base table); `app_data.field_leave_requests` is a separate read view, unrelated to this change.

---

# Session 2026-08-10 — Closed the live `next_variation_number` cross-tenant gap, audited the whole phantom-migration batch behind it

## Built
- **eq-service**: closed the cross-tenant info-disclosure gap flagged 2026-08-08. Deleted the phantom `0140_harden_next_variation_number.sql` ledger row on ehow (`service._eq_migrations`), dispatched the governed `apply-service-migrations.yml` workflow (run [31381351162](https://github.com/eq-solutions/eq-service/actions/runs/31381351162), 16s). Live-verified: `service.next_variation_number` now takes a single JWT-derived `p_year integer` arg — the old caller-supplied-tenant-UUID signature is gone.
- Audited all 172 migrations phantom-marked by the 2026-07-03 ledger-bootstrap backfill (`checksum: null, applied_by: 'backfill-2026-07-03'`) before treating `0140` as an isolated case, via a background agent. Found nothing else open: 4 others (`0102`/`0104`, `0126`/`0129`, `0089`) were already independently fixed by eq-service PR #614's own `--verify` tooling between 2026-07-08 and 2026-07-28; one more (`0136_lock_internal_identity_helpers.sql`) is dead code with nothing live to expose.

## Decided
- Royce, via `/decide` with no scope specified: picked the `next_variation_number` fix-path decision myself (most decision-shaped open item on the board) after he answered "no preference."
- Royce, choosing among 4 options for the 172-row finding: **"Audit all 172 phantom rows first"** — over fixing 0140 alone and parking the rest, or leaving everything for him to action by hand.
- Royce: **"Yes — fix ledger row + dispatch now"** — explicit go to actually write to production (delete the ledger row, dispatch the live migration workflow).

## Deferred
- **`task_808f61f4`** (background task chip, not started): extend eq-service's `--verify` migration-audit tooling to check function *signatures*, not just object existence — the exact blind spot that let `0140` through PR #614's own earlier audit undetected.

## Notes
- **Substrate correction**: the 2026-08-08 entry for this gap recorded `0140` as merged 2026-07-03. Actual merge date is **2026-06-20** (PR #321, commit `c65b9c3`) — 13 days before the backfill that wrongly grandfathered it as "already applied." The backfill itself (triggered by `0168_service_migration_ledger.sql` introducing the ledger table) is a normal, expected bootstrap pattern for pre-existing schema history, not a bug — `0140` was a genuine one-off case of a merged-but-never-actually-run migration getting caught up in it.
- Two early-session questions were pure recap, no new work: "whats left?" (re-confirmed all of the 2026-08-08 close-out's deferred items were still open, nothing had silently resolved) and "where did we get with the original QR code question" (re-confirmed the self-join QR account-creation flow itself was clean and production-ready — everything that followed in that earlier session was adjacent hygiene/security work, not a blocker to issuing QR codes).

---

# Session 2026-08-10 — RCD testing: industry comparison → implementation plan → built, shipped, dispatched live

## Built
- **eq-service**: RCD (safety switch) circuit pass/fail, computed and auto-defected for the first time. Asked to check the current RCD testing flow and compare against industry practice/leaders — found RCD was the only test module with zero automated compliance signal (ACB/NSX/generic tests all auto-create a defect from a failed reading via migration 0148; RCD stored trip-time ms values but never compared them to a limit). Scoped option 1 from that comparison ("compute pass/fail from data already stored") as a concrete implementation plan, confirmed, built: new migration adds a `BEFORE INSERT OR UPDATE` compute trigger on `app_data.rcd_test_circuits` (30mA circuits only, per AS/NZS 3000 Table J1 — 300ms @ rated current, 100ms @ 5×) and an `AFTER INSERT OR UPDATE` auto-defect trigger on `app_data.defects`, mirroring the existing `fn_acb_reading_to_defect` pattern exactly, including a real gotcha that pattern already solved (`AFTER UPDATE OF is_pass` would never fire since `is_pass` is trigger-derived, not written by the app's own UPDATE statement — migration 0148 already had to drop that column-list restriction for the same reason). Report (docx) and the live editor both shade pass/fail with the same green/red convention ACB/NSX already use. [PR #694](https://github.com/eq-solutions/eq-service/pull/694), merged `4fd704d`.
- Dispatched migration 0203 to ehow via the governed `apply-service-migrations.yml` workflow (run applied in ~2s, single transaction, real checksum recorded in the ledger). Live-verified after: `is_pass` on both `app_data.rcd_test_circuits` and the `service.*` view, `source_rcd_circuit_id` on `app_data.defects`, both triggers present.
- While dispatching, found `apply-service-migrations.yml` contradicted itself about whether dispatch pauses for approval — the file's header comment said no required-reviewer rule exists (confirmed live 2026-07-28), an inline job comment said the opposite ("Milmlow as required reviewer"). Re-verified live via `gh api repos/.../environments/production`: still branch-policy only, no approval pause. Fixed the stale comment. [PR #695](https://github.com/eq-solutions/eq-service/pull/695), merged `ed984ee`.
- Checked on the previously-spawned background task (`task_549910ee`, "recreate app_data schema for fresh-bootstrap CI") — it made real progress (root-caused `app_data` as eq-shell's to own, not this repo's; wrote a drift-check script + weekly CI workflow; captured the live `app_data` shape via SQL, 147 tables/2378 columns) but is blocked on Docker Desktop failing to start in that environment, and paused itself after asking the user how to proceed. Work sits uncommitted on branch `fix/integration-ci-app-data-bootstrap` in the shared root checkout — not lost, not resolved either.

## Decided
- Royce, via `/decide` (on whether the pass/fail thresholds needed sign-off before shipping): ship the PR now with the threshold caveat flagged explicitly in the description, rather than holding for review before opening it — nothing about the design was irreversible, and the numbers are a one-line fix if wrong.
- Royce, via `AskUserQuestion` (RCD implementation plan): 30mA-only computation, other trip-current ratings get `is_pass = NULL` rather than a guessed pass/fail — over holding the whole feature for full verification, or having Royce supply the other ratings' limits himself.
- Royce: "build it", then "yes" (merge + dispatch), then "dispatch the migration to ehow", then "merge PR #695" — each an explicit go for its specific action.

## Deferred
- **Threshold values not independently checked against primary AS/NZS 3000 text** — web-verified only, flagged in the PR for Royce's direct confirmation before they're trusted as the sole gate on a real compliance defect.
- **Not click-tested live in a browser** — verified end-to-end instead via a rolled-back transaction against live ehow (every case: untested/pass/fail/nuisance-trip/unverified-rating, create-on-fail, auto-resolve-on-refix) and by inspecting the generated docx's actual XML for the expected shading.
- **RCD wizard restructure + de-Jemena-ing the schema** — scoped as options 2 and 3 in the original comparison, not built this session.
- **`task_549910ee`** — still blocked on Docker; needs Royce to get Docker running, hand over a `supabase db dump`, or greenlight a pure-SQL fallback.

## Notes
- **Schema discovery that reshaped the plan mid-build**: `service.rcd_test_circuits` and `service.defects` turned out to be *views* over `app_data.*` with `INSTEAD OF` triggers (the canonical write layer, migration 0148), not the plain tables the original 2026-04-27 schema (migration 0069) defined — caught by checking live triggers before writing any DDL, not assumed from the older migration file. Postgres also expands a view's `SELECT *` into a fixed column list at `CREATE VIEW` time, so the new `is_pass` column needed `CREATE OR REPLACE VIEW` on both `service.rcd_test_circuits` and `service.defects` to actually become visible through them — silent trap if missed, no error, the column would just never show up in app reads.
- **Worktree hygiene, twice over**: root checkout (`C:\Projects\eq-solves-service`) stayed occupied by the paused `task_549910ee` session's uncommitted work throughout the build — all RCD work done in a fresh worktree off `origin/main`. When a later doc-fix edit accidentally landed in the shared root checkout (mixed in with that other session's changes), recovered by stashing only that one file, moving the stash into a clean worktree, and dropping it from the root — the other session's uncommitted files were never touched. Then hit the same class of problem again during this close: concurrent sessions kept landing commits on `eq-context`'s shared `main` (an automated nightly refresh, and at least one other session running its own `/close` in parallel with near-identical wording on an overlapping note) while this close was still writing — cost several rounds of fetch/rebase/resolve before the push actually landed. Recovered each time the same way: isolated worktree off fresh `origin/main`, redo any lost edit, resolve real conflicts by keeping both sides' distinct content, dedupe near-identical overlapping content instead of keeping both, push directly.
- **Substrate gap noticed, not fixed**: the 2026-08-08 assignee-picker fix (PR #692) and the `next_variation_number` gap have `eq/pending.md` entries but no matching `eq/changelog/eq-solves-service.md` entry for 2026-08-08 — the changelog jumps from 2026-08-06 straight to 2026-08-10. Not backfilled this close (wrong date to write under now, and unclear whether that was a genuine miss or an earlier session's deliberate scoping); flagging so it isn't mistaken for "already covered."
- **Changelog naming collision confirmed live**: `eq/changelog/` has three files that could plausibly be "the" eq-solves-service changelog — `eq-solves-service.md` (active, matches this repo's actual recent PRs), `eq-service.md` (stale, last entry mid-500s PR range, unrelated dates), `service.md` (ancient, single paragraph about the original urjh→ehow migration). Used `eq-solves-service.md`. Worth a cleanup pass to retire or clearly mark the other two dead, so a future session doesn't guess wrong.
