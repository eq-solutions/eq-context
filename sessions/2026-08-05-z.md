# Session 2026-08-05 (z) — index-drift fix landed; digest F6/F7 false-positives investigated and one real bug fixed

## Built
- Fixed the index-drift CI failure (open on `main` since 2026-08-01): added `eq/documents/internal-signoff-register-sprint-2026-08-04.md` to `eq/README.md`'s index. Verified with `INDEX_DRIFT_STRICT=1 python scripts/index_drift.py` — all 7 tiers clean. Committed `b51da2fd`.
- Investigated both digest "Guard bypass? rung 4" flags from earlier today (F6 and F7) at depth — read `.github/scripts/refresh_digest.py`'s actual `failure_recurrence_signals()` matching logic rather than guessing. Found they're two different mechanisms, not one:
  - **F6**: a real, reproducible signal-regex precision bug — the bare `NUL.?(fill|byte)s?|nul-fill` pattern matched a session log merely citing "the open F7 NUL-fill risk" as workflow rationale, not a new incident. Designed and empirically tested a tightened regex (requires an incident verb near the phrase) against all ~22 of today's real session logs (0 false positives after, vs 1 before) and against F6's own historical incident text verbatim (still matches). Committed `f93dcef`.
  - **F7**: investigated, concluded it's NOT a precision bug — the old regex matched zero times across every session log today; the digest's citation of `sessions/2026-08-05.md` was stale, reflecting content from a highly volatile, frequently-rewritten base file that had already changed by check time. Tested the same tightening approach on F7 and found it measurably hurt recall without fixing anything real. Left F7's regex unchanged.
- `ops/pending.md`: both sections above are fully closed — rotated wholesale to `ops/pending-archive.md` rather than left open.
- Dismissed both spawned task chips (`task_ef924474`, `task_5b1d3d56`) — both already completed by the time they were "started."

## Decided
- Royce: "commit it" for the F6 regex change (via AskUserQuestion) — data-only change to `system/failures.md`, direct commit to main with explicit pathspec.
- Royce: "go" to resume the paused index-drift fix from an earlier brief.

## Deferred
- Nothing new from this session's own work — both threads fully closed, tested, and shipped.

## Notes
- Both fixes were done via a fresh isolated clone rather than the shared checkout, each time the shared checkout was found diverged/behind at the moment of checking (routine same-day concurrent-session churn) — matches the pattern established earlier today; F9 blocks `pull`/`rebase`/`merge` in the shared checkout unconditionally regardless of how safe a given fast-forward would actually be, so a fresh clone is the standing workaround, not a one-off.
- **Two new items surfaced by this session's gate checks, not investigated or actioned (out of scope for what was asked):**
  - HOOKS now reports a **LATENT SHADOW**: effective `core.hooksPath` resolves correctly (`.githooks`) right now, but `--worktree` and `--local` scope disagree on this checkout — worktree silently wins. Gate flags this as "the exact shape of F10's 2026-08-05 recurrence" (the same core.hooksPath-drift failure class already proposed for rung promotion earlier today). If the worktree override is ever cleared, this reverts to the wrong value with no warning. Worth a look given F10 is already a known repeat offender.
  - NEEDS YOU grew to 7 items this session, including a new one not seen earlier today: `jwt-contract-drift.yml` cron failing, 10 consecutive runs, no success in recent history. Not touched — flagging for visibility only.
