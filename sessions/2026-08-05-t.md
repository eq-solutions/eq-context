# Session 2026-08-05-t — Fixed guard.js worktree-detection false positives on Git-Bash-style paths (all 3 rules)

## Built
- Root-caused and fixed a path-normalization bug in `C:\Users\EQ\.claude\hooks\guard.js` rules 1 (block-worktree-write), 1b and 1c (detect-fake-worktree): splitting a Git-Bash-style absolute path (`/c/Projects/...`) and filtering empty segments silently dropped the leading slash, so every legitimate worktree reached via a Unix-style cwd/cd-target/file_path — exactly what the Bash tool's own convention produces — was misidentified as foreign or fake.
- Fix: normalize the fake `/x/` drive segment to a real drive letter (`c:/...`) before comparing/splitting, applied consistently across all three rules.
- Extended `hooks/selftest.js` (the existing canonical test harness — found and reused rather than writing a parallel script) with real-`.git` fixture setup/teardown and 3 new mixed-path-style regression cases. Needed real fixtures, not synthetic non-existent paths, because rule 1b's `fs.existsSync` check force-blocks unconditionally on any non-existent worktree-shaped path — a synthetic path would have confounded rule 1's own cases.
- Verified methodically: reproduced each false positive live before fixing; confirmed the new tests actually fail with the fix disabled (not vacuous) before confirming they pass with it restored; 14/14 passing after. `node --check` clean on both files.
- Committed to `C:\Users\EQ\.claude` (its own separate git repo, not eq-context) as `f42ed85`, scoped to exactly the two touched files — left other pre-existing uncommitted work in that repo (`README.md`, `settings.json`, `plans/`, `agents/`) untouched.

## Decided
- Royce: "commit this" — committed. Not pushed (no push requested, and no evidence this personal config repo has an active remote in use).

## Deferred
- None from this session's own work — self-contained, fully landed.

## Notes
- This session traces back to `sessions/2026-08-05-o.md`, which found the guard.js bug live, root-caused it, and spun it off as a background task (`task_ec3c7cf0`). This session picked that up, fixed it, then self-flagged a follow-up audit of a related rule (`task_ed0ad60c`) which this same session also completed. Royce started the work directly — confirmed via a `dismiss_task` call returning "already started by the user" rather than "withdrawn."
- **Process mistake, caught and corrected within this same close.** Session `-o`'s notes also described a second, unfixed hook bug (`pre_tool_use.py`'s `effective_cwd()`, same *shape* — regex `^` anchoring — as the guard.js bug just fixed here) and said it hadn't been spun off. Trusting that description without re-checking, spawned `task_a6cdad01` for it — but the fix (commit `f3cc8cb`, `re.MULTILINE`, 4 new adversarial-test cases, 79/79 python + 36/36 bash) had *already landed on origin* by another concurrent session, visible in a `git log` this same close had already pulled minutes earlier but hadn't cross-checked against the task before spawning it. Also got the file location wrong — it only exists at `C:\Projects\eq-context\hooks\pre_tool_use.py` (repo-tracked; likely eq-context's own git-level hook, wired via `core.hooksPath` → `.githooks`, not a Claude-Code PreToolUse hook alongside `guard.js`), never at `C:\Users\EQ\.claude\hooks\pre_tool_use.py` as assumed — confirmed that path doesn't exist. Caught by re-checking before finishing this close; task dismissed. Lesson: cross-check a "found but not yet fixed" claim from a session log against the *current* git log before spawning a task on it, not just against the log entry's own timestamp.
- **No `pending.md` update this session, deliberately.** This is Claude Code hook/tooling infrastructure, not EQ/SKS/OPS product work — none of the three tier `pending.md` files are the right home for it (session `-o` itself only mentioned the discovery as a footnote inside its own eq-shell write-up, not as a standalone entry). Reinforced by risk: at the time of this write-up, `eq/pending.md`, `eq/changelog/eq-shell.md`, and `sessions/2026-08-05.md` (the bare file) were all briefly staged mid-flight by a different concurrent session's own close sequence — editing or committing those risked interfering with it, on the same day a real `pending.md` clobber incident already happened once (see `2026-08-05-o` Notes, recovered in commit `e20df0f`).
- **Extreme same-day concurrency, hit twice by this close alone.** 17 separate `sessions/2026-08-05-*.md` files (`-a` through `-r`) existed before this session started; used the actually-observed convention (a per-session suffixed file, confirmed via `git log` + a directory listing — Rule 0.5, live over doc text) rather than this skill's literal "append to the bare file" instruction. First attempt picked `-s` — by the time of push (isolated clone, to avoid `git stash`/`git rebase` in the shared checkout given the open F7 NUL-fill risk on this mount and F9's by-design rebase block there) `-s` had *already been claimed* by another session in the gap between listing the directory and pushing. Re-picked `-t`. The local orphaned commit this raced attempt left behind in the shared checkout (adding a now-conflicting `2026-08-05-s.md`) was undone with `git reset HEAD~1` (mixed — moves the branch pointer and unstages, never touches the working tree) rather than anything `--hard`, specifically so it wouldn't disturb the *other* concurrent session's own uncommitted edits to `hooks/pre_tool_use.py` and `hooks/adversarial_test.py` sitting alongside it. Confirmed those files' diffs were byte-identical before and after the reset.
