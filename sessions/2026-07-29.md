# Session 2026-07-29 — eq-receipts: fixed a duplicate-detection blind spot, added invoice number as a stronger match

## Built
- Traced a live bug: two identical Anthropic charges ($12.02 USD) weren't flagged as duplicates because the duplicate check compared the AUD-converted total, and the currency conversion rate applied can drift a few cents between two scans of the same document.
- Fixed the check to compare the original as-charged amount instead, for foreign-currency receipts — no longer sensitive to which day's conversion rate got used.
- Added invoice/receipt number as the strongest match when a document prints one — more reliable than amount+date for recurring subscription charges.
- Re-ran the corrected check against every existing receipt — surfaced a second real duplicate that had been hiding the same way (two GitHub charges, $68.64 USD, both already approved, 2026-07-22).
- Shipped end-to-end: eq-receipts [PR #14](https://github.com/eq-solutions/eq-receipts/pull/14) merged, frontend live via Netlify auto-deploy, backend (extract-receipt, poll-batch, receive-email edge functions) redeployed to Supabase.

## Decided
- Royce confirmed the session brief before any code was written (Rule 0.6 gate).
- Royce: fix both the FX-instability bug and add invoice_number as a stronger signal, rather than just the narrower fix.
- Royce: push, merge, and deploy the edge functions — all explicit go-aheads, done in sequence this session.

## Deferred
- Royce to review the two now-flagged duplicate pairs in the app (Anthropic + GitHub) and decide if either was a genuine double-charge worth pursuing.
- No UI field yet to correct a misread invoice number by hand — falls back cleanly to the amount+date check, but flagged for later.

## Notes
- Correction to an earlier finding: the 2026-07-28 eq-receipts session logged "Netlify doesn't auto-deploy on push to main, needs manual trigger" as an open item. This session merged via `gh pr merge` and Netlify auto-deployed cleanly within a minute, no manual trigger — confirmed live via the deploy record (commit ref matched, build succeeded). Only the PR-merge path was retested, not the original direct-push repro, so the pending.md item is annotated rather than closed.

---

# Session 2026-07-29 — eq-solves-service: Field Run-Sheet asset headers now show the maintenance plan's Job Code

## Built
- Royce shared a generated Run-Sheet (SY3, standard) and asked for the maintenance plan job code to display in each asset's header.
- Traced the report to `lib/reports/maintenance-checklist.ts` (`buildAssetSection`) + `lib/reports/maintenance-checklist-input.ts` — confirmed via extracted docx text that no job/plan code appears anywhere in the current output.
- `job_plans.code` (the "Job Code" field, e.g. "SJPNL1", per the Job Plans form) was already being fetched for the check-level plan but not per-asset — the per-asset job-plan lookup only selected `id, name`.
- Added `code` to that per-asset select, mapped it to a new `ChecklistAsset.jobPlanCode`, and rendered it into the existing `ID | Location | WO` info line as `Job Code: {code}`.
- Scoped to the standard PPM checklist path only, per Royce's explicit choice — ACB/NSX breaker cards and RCD synthesized assets untouched.
- `tsc --noEmit` — 0 errors. `next build` could not run locally (worktree had no `node_modules` or `.env.local`); installed deps, left env/secrets to CI.
- Committed (`4256cb0`), pushed, opened eq-service [PR #638](https://github.com/eq-solutions/eq-service/pull/638). Royce said "merge" — CI was still running (`tsc + next build`, `Typecheck + audit`, integration tests, Netlify preview) as of session close; not yet merged.

## Decided
- Royce, via AskUserQuestion: fold the Job Code into the existing info line (not a new standalone line), and PPM checklist only — do not extend to ACB/NSX breaker cards this session.
- Royce: merge PR #638 (given, but CI hadn't resolved by session close — did not force a merge on pending/red checks).

## Deferred
- PR #638 needs to actually merge — CI was still pending when this session closed. Whoever picks this up next should check `gh pr checks 638` and merge once green (or investigate if anything failed).
- ACB/NSX breaker-card and RCD run-sheets have the same missing-Job-Code gap, not built this session.
- `pm-asset-report.ts` and `work-order-details.ts` have the identical gap (job plan fetched, only `name` surfaced, never `code`) — flagged, not touched.
- Royce to spot-check a freshly generated Run-Sheet once merged/deployed and confirm the Job Code line reads as expected.

## Notes
- The 16 `npm audit --audit-level=high` findings hit during this session are all one pre-existing chain (`brace-expansion` → `minimatch`/`glob` → `eslint`/`archiver`/`exceljs`) already documented in digest.md as an accepted residual (fix needs a breaking `eslint` major bump) — not introduced by this change, noted as a waiver in the PR body rather than fixed.
- This worktree (`report-sheet-job-codes-0c400b`) had no `node_modules` and no `.env.local` on first use — ran `npm install` to get `next build` working locally; did not fabricate Supabase credentials, left full-build verification to CI which has its own secrets.
- Root cause detail for the next session: `VerifyCard.tsx`'s save-time hash recompute was using the post-FX-conversion AUD total, while the extraction-time hash used the pre-conversion original — the two disagreed for any foreign-currency receipt. Both now consistently use the pre-conversion amount.
- A one-off backfill recomputed the duplicate-check value for every existing receipt with the corrected rule — that's what surfaced the second (GitHub) duplicate pair; it wasn't a new receipt, it had been sitting unflagged since 2026-07-22.

---

# Session 2026-07-29 (cont.) — EQ Field: team pills as the single crew picker, plus a lazy-load race and a column-sizing bug

## Built
- Royce asked for a team selector on Edit Roster, "same as SKS NSW". Investigation found the pill UI already existed (`teams.js`, ported from SKS once before, v3.5.27) but was missing from Editor, and — everywhere it did render — never actually filtered anything (`personInActiveTeam()` was never called from any page's render function). A separate "My crew / a team / Everyone" dropdown was the only thing actually scoping fetches (Roster + Timesheets), also absent from Editor and Contacts.
- Consolidated onto the pill row as the single picker everywhere (eq-field [PR #563](https://github.com/eq-solutions/eq-field/pull/563), v3.5.375): wired `personInActiveTeam()` into Editor/Roster/Contacts/Timesheets' render functions; `getCrewPersonIds()` (permissions.js) now reads `STATE.teamFilters` instead of the retired `EQ_CREW_VIEW`; added three-state persistence (never-chosen / explicit-All / team ids) so a supervisor who's never touched the pills still defaults to their own crew, matching the old dropdown's default; removed the dropdown UI; rewrote `tests/crew-scoping.test.js`'s manual-widen section for the new mechanism (35/35 pass).
- Real behavior change flagged up front: Contacts and Editor previously always showed everyone regardless of crew — this is the first time either page gets any default narrowing.
- Royce reported the pills still weren't showing on `core.eq.solutions/sks/field?tab=editor` after a hard refresh. Root-caused (not assumed): the deployed code was correct, but `renderTeamPills()` had a lazy-load race — only ever called before `teams.js`'s async lazy-load necessarily resolved, or (post-load) only for the `'teams'` page specifically. Same bug class this file has already fixed twice for `leave.js`. Invisible before because normal in-app navigation usually pre-loaded `teams.js` from an earlier tab; Core's cold `?tab=editor` deep link has nothing to paper over it. Fixed by calling `renderTeamPills()` from inside `renderCurrentPage()`, which only ever runs after the lazy-load resolves, for every page (eq-field [PR #565](https://github.com/eq-solutions/eq-field/pull/565), v3.5.377).
- Found and fixed a second bug in the same screenshots: Labour Hire rows' 4th action-column icon (📦 archive, from an earlier session's v3.5.371) overflowed `.editor-actions`' fixed 80px width, which was never widened when that icon was added — misaligning Labour Hire rows against the header and every other group. Widened to 108px, same PR.
- Answered two follow-up questions by reading the actual code rather than guessing: Copy Last Week / Copy Week → copy the whole roster's schedule data for the source week, not whatever's filtered by a team pill; TAFE Holidays is a date-range blocklist that stops the "Apply TAFE Day" button and the Sunday automated cron from auto-filling apprentices' TAFE days during those ranges — adding a range doesn't touch anyone's roster by itself.

## Decided
- Royce: pills become the one true crew-picker mechanism across all four pages, retiring the dropdown — asked directly given building both properly would've left two competing pickers on some pages.
- Royce: "merge" — both PR #563 and PR #565, each confirmed separately.

## Deferred
- Copy Last Week / Copy Week → whole-roster-vs-filtered-view behavior — flagged to Royce, no decision yet whether that's desired or needs changing. See `sks/pending.md`.
- Batch Fill's Team quick-toggle vs. Timesheets' Team filter inconsistency (compose-with-groups vs. narrow-the-view) — pre-existing gap, not touched this session, still open from 2026-07-27's reflection log.

## Notes
- Two live version collisions during this session — both PR #563 and (separately) the rebase for PR #565 landed on top of other same-day merges that had already claimed the "next" version number (v3.5.373 taken by PR #560, v3.5.374 by PR #562, v3.5.376 by PR #564). Each time: rebased onto `origin/main`, resolved the changelog-banner conflict by keeping both entries and renumbering mine, re-ran the full verification pass, force-pushed with `--force-with-lease`. `gh pr merge --squash --delete-branch` threw the same recurring local "'main' is already used by worktree at ..." error both times — the GitHub-side merge completed fine both times, verified via `pull_request_read`/`pr view` before treating it as done, remote branch deleted manually afterward.
- Real lesson from the mid-session correction: a "confirmed no console errors on the demo path" verification claim in an earlier response was misleading, not just incomplete — the `demo`/`eq` tenant never loads `teams.js` at all (triple-gated to `sks`), so that check structurally could not have exercised the bug Royce then found. A negative result from a code path that can't touch the feature isn't evidence the feature works. Caught and stated plainly in the follow-up reflection-log entry rather than left uncorrected.
- The brief-gate flag is per-session-per-day; this session crossed midnight (system clock rolled to 2026-07-29 mid-session) and the previous day's flag silently stopped satisfying the gate, blocking an Edit mid-rebase with no obvious cause until checked directly — rewriting the flag with the current date fixed it immediately. Worth remembering: an unexplained brief-gate block mid-session is worth checking the actual system date before assuming a deeper problem.
- `docs/reflection-log.md` entries this session explicitly named and corrected the previous entry's own verification gap rather than quietly building on top of it — matches this repo's established self-critique convention (see the 2026-07-27 audit-log entries doing the same).

---

# Session 2026-07-29 (cont.) — Maximo PDF import: job-plan matching + export bugs fixed, feature closed out end-to-end

## Built
- Fixed job-plan matching showing every real maintenance plan as "not found in EQ" during PDF import preview — the importer was matching on the E-number (e.g. `E1.25`) instead of the short plan code (e.g. `LVACB`); Maximo's raw text carries both, wrong one was being used as the DB key. Verified against all 5 real PDF files Royce provided. Deployed live via direct edge function deploy (v9); documented in eq-service [PR #630](https://github.com/eq-solutions/eq-service/pull/630).
- Fixed the Maintenance Plans page's CSV export only downloading the current page (26 rows) instead of the full filtered list (58 rows) — confirmed against a CSV Royce actually exported and compared to the live DB count. Added a second, unpaginated query for export only. eq-service [PR #630](https://github.com/eq-solutions/eq-service/pull/630), merged.
- Fixed a near-match suggestion bug Royce caught from a live screenshot: Maximo code `MVSWBD` was fuzzy-matching to `SWBD` instead of the obviously-intended `MVSWDB`. Root cause: the edit-distance check scored a 2-letter swap (`BD`→`DB`) the same as an unrelated bigger difference (dropping the `MV` prefix), so the tie fell to whichever plan the DB happened to return first — arbitrary, not the actual typo target. Switched to Damerau-Levenshtein so a swap counts as 1 edit, not 2 — the real target now wins outright. eq-service [PR #631](https://github.com/eq-solutions/eq-service/pull/631), merged.
- Merged eq-solves-intake [PR #79](https://github.com/eq-solutions/eq-solves-intake/pull/79) — catches up git history on everything already deployed live to the edge function across this whole thread (text-first extraction, both `WORKER_RESOURCE_LIMIT` fixes, the 5A frequency mapping, the job-plan-mnemonic fix).
- Full write-up of the whole Maximo PDF thread (spans multiple sessions) is in `eq-context/eq/pending-archive.md`, same date — the feature is now fully live and working against real production data.

## Decided
- Royce: "do we need to spend anymore time planning this or are you 100%" — direct instruction that once root cause is confirmed by hard evidence (live SQL + code trace), skip further ceremony and just fix it. Applied for the rest of the session.
- Royce confirmed each merge explicitly ("merge it") for PRs #630, #631, and eq-solves-intake #79 — none merged without an explicit go-ahead.

## Deferred
- Spun off as its own background session (task `a787e8ea`, started by Royce directly): the same "export only grabs the current page" bug also exists on Sites, Customers, Instruments, and the Audit Log. Tracked in `eq/pending.md` — check its outcome next session.
- Residual data-quality mismatches the mnemonic fix surfaced (e.g. Maximo's `MVSWBD` vs. the DB's registered `MVSWDB` — likely a genuine naming inconsistency, not a parsing bug) are left for Royce to resolve via the existing fuzzy-match/alias review UI, not fixed blindly.

## Notes
- `job_plans.code` is the short mnemonic (`LVACB`, `ATS`, `SWBD`); `job_plans.name` is the E-number (`E1.25`). This was the root cause of the job-plan-matching bug and is worth remembering for any future work on this importer.
- `lib/utils/levenshtein.ts` now implements Damerau-Levenshtein (optimal string alignment), not plain Levenshtein — adjacent transpositions cost 1 edit, not 2. Relevant if anything else in the codebase reuses `closestMatch()`.
- postgrest-js query builders mutate the same instance in place (`.range()`, `.eq()`, etc. return `this`) — caught mid-fix in an earlier part of this thread; `buildJobPlansQuery()` factory pattern in `app/(app)/job-plans/page.tsx` is the fix, worth following as the pattern for any other paginated-list export fix (including the Sites/Customers/Instruments/Audit-log one now running separately).

---

# Session 2026-07-29 (cont.) — Export bug closed on 4 more pages, Excel export added to 3 pages

## Built
- Reviewed and merged the background session Royce started for the Sites/Customers/Instruments/Audit Log export-pagination bug (task `a787e8ea` from the prior entry) — read the full diff before merging rather than merging on trust; it followed the exact same fix pattern already established for Maintenance Plans, correctly. eq-service [PR #632](https://github.com/eq-solutions/eq-service/pull/632), merged.
- Royce asked whether Excel export was feasible. Checked first: the app already has an Excel-writing library in use elsewhere (ACB test collection import/export), so this was composition, not new infrastructure — ran the decision protocol, then built it the same turn.
- New `lib/utils/xlsx-export.ts` (`exportToXlsx`) — takes the exact same column definitions as the existing CSV export, so every export button can offer Excel for free. `ExportButton` gained an optional prop that turns it into the existing dropdown-button component (CSV is still the default click, Excel is a second option) — every other export button in the app is unaffected.
- Wired Excel export into Assets, Job Plans, and Maintenance Checks.
- While wiring Maintenance Checks: found it had the *same* page-only export bug just fixed on the other four pages — fixed it in the same change (a second unpaginated query, matching the established pattern; live row count for the tenant is 30, well under the database's row-return cap).
- Also found and fixed a second, unrelated bug on that same page: the "tasks completed" count next to every check has been showing "x/0" instead of "x/y" — the total was hardcoded to zero and never actually calculated. One-line fix once spotted, done in the same pass since it was the same lines.
- eq-service [PR #633](https://github.com/eq-solutions/eq-service/pull/633), merged.
- Answered a live question: verified `core.eq.solutions/sks/service/contacts` is fully canonical — `service.customer_contacts`/`service.site_contacts` are views with full read/write triggers over the same `app_data.contacts` table Shell and Field use, confirmed live against ehow. No separate Service-local copy.

## Decided
- Royce: "fix that export issue" (the Sites/Customers/Instruments/Audit Log bug) — reviewed the already-open PR from the background session rather than rebuilding, merged after confirming it matched the established pattern.
- Royce: "Assets, job plans, and maintenance checks" — scoped which 3 pages get Excel export, out of the ~16 pages with an export button.
- Royce: "merge it" for PR #633, after CI cleared.

## Deferred
- Royce to click through Assets/Job Plans/Maintenance Checks once the deploy lands and confirm the Excel option works and the Maintenance Checks progress count is no longer "/0" — not click-tested live this session, no login credentials available in this dev environment. Verified via `tsc --noEmit`, the full 370-test suite, and code review only.

## Notes
- `components/ui/SplitButton.tsx` already existed (primary action + dropdown caret, used for Field Run-Sheet/Print Blank) — reused it for the Export button's CSV/Excel choice instead of building a new dropdown component.
- `exceljs` was already a dependency, already used client-side (lazy-imported per an earlier bundle-size fix) — `exportToXlsx` follows the same lazy-import pattern so pages that only ever export CSV don't pay for it.
- CI on this repo currently takes noticeably longer to leave "pending" than earlier in the session (checks sat at 0s elapsed for several minutes before genuinely progressing) — confirmed via `gh run view`'s per-step breakdown that it was a real, progressing run, not a stuck job. Worth checking step-level detail before assuming a stall next time this happens.


---

# Session 2026-07-29 — Suppliers Login/Password saga closed (3rd attempt, real root cause); wallet credential-equivalence fix re-verified live

## Built
- **eq-shell [PR #1092](https://github.com/eq-solutions/eq-shell/pull/1092)** (merged, squash `625829b8`, confirmed live via Netlify — production deploy verified serving this exact commit): third and final fix in the Suppliers Login/Password saga. Royce reported the columns "show and then disappear" and asked if it was a security setting — it wasn't. Root-caused to `notes` being the only column in the table with no width cap: the loading skeleton has no real content yet so it always fits within the visible area, but once real (sometimes long) note text loads, the table widens past the visible area and pushes Login/Password off-screen — that's the "show then disappear." Capped `notes` at 160px with ellipsis truncation + hover tooltip, matching the existing pattern in `AdminAuditPage.tsx`. Royce explicitly declined two heavier options (two-line rows, column merging) and asked for the simplest fix.
- Re-verified the eq-cards credential-gaps fix (PR #185 / jvkn migration 0109, applied yesterday) is still live — `pg_get_functiondef` confirms the Driver Licence/Passport equivalence logic is intact, and the same real SKS worker test case still resolves correctly (`held=true` under the live function vs. `held=false` under the old exact-match logic).

## Decided
- Royce: "merge #1092" — explicit go, same pattern as every other merge this week (check CI, merge, confirm live).
- Royce twice declined more thorough/architecturally-correct fixes (column reorder, then two-line rows / column merging) in favour of the simplest change that solves the reported symptom — see Notes below, this is a real, recorded preference now.

## Deferred
- **Royce's own click-through of the Suppliers page** — three fixes across two days (#1079, #1082, #1092) have each been verified live via deploy/build checks, but none has been confirmed by an actual human look yet. Logged in `eq/pending.md`.

## Notes
- **Royce's stated preference, worth remembering for any future "make this UI element show up" report**: when a display bug has multiple possible fixes ranging from a one-line change to a page-level rebuild, default to proposing the simplest one first and only escalate in scope if asked — don't lead with the "most correct" architectural option. This session specifically involved two rounds of AskUserQuestion proposing heavier fixes (column reorder → two-line rows / column merge / rebuild) before landing on the one-line width cap that was actually right; Royce's exact words: "This feels complicated... keep the solution as simple as possible." The eventual real fix (cap the one unbounded column) was in fact the simplest available lever the whole time — it just took two escalations to get back to it after the first two attempts turned out to be the wrong *mechanism*, not the wrong *scope*.
- **General pattern worth watching for elsewhere**: an `@eq-solutions/ui` Table's loading-skeleton state (narrow, header-only-driven width) and its loaded-data state (real content-driven width) can differ enough that a column visible during loading vanishes once real data arrives, with zero visual transition to explain why. Any page using this Table with an unbounded-width column (no `width` prop, no truncation) is a candidate for the same bug. Worth a proactive check across other pages using this component if it resurfaces.
- Netlify production-deploy verification (same method as yesterday: `get-project` → `currentDeploy.currentDeploy.id` → `get-deploy-for-site` → grep the sliced response for `commit_ref`) again showed a stale deploy for several minutes after merge before the real one landed — consistent with yesterday's finding, not a fluke. Budget a few minutes before confirming "live" after any eq-shell merge.

---

# Session 2026-07-29 (cont.) — eq-solves-service: Sites/Customers/Instruments/Audit Log export-pagination fix closed out (task `a787e8ea`)

## Built
- This was the background session Royce spun off from the Maximo PDF import thread (task `a787e8ea`, tracked in `eq/pending.md`). Verified the same "export only grabs the current page" bug from PR #630 (Maintenance Plans) also existed on Sites, Customers, Test Equipment, and Audit Log — confirmed by reading each `page.tsx`/`*List.tsx` pair, not just trusting the earlier flag.
- Checked live row counts on ehow before picking a fix approach: sites 258, customers 48, instruments 18, audit_logs 49 — all comfortably under PostgREST's ~1000-row cap, so the simple second-query pattern from #630 (not the RPC-bypass used for `/assets`) was the right fit for all four.
- Fixed all four: each page now runs a second, unpaginated query in parallel with the paginated one via a `buildXQuery()` factory (fresh query-builder call per query — postgrest-js mutates a builder in place rather than branching one before/after `.range()`), and passes the full result down as a dedicated `allX` prop that Export uses instead of the paginated prop.
- Also fixed the Export button's visibility gate on Instruments and Audit Log to check the full filtered set instead of the current page, so it doesn't wrongly hide itself when viewing an out-of-range page.
- `tsc --noEmit` clean. eq-service [PR #632](https://github.com/eq-solutions/eq-service/pull/632): committed, pushed, opened, then rebased onto latest `main` (which had picked up #630 and #631 in the meantime) at Royce's request, force-pushed clean (no conflicts — disjoint files). Royce merged it himself. Confirmed `main`'s CI post-merge shows only the same two pre-existing red checks as #630 (flaky integration tests; an `npm audit` finding on a deep transitive dependency already logged in `eq/pending.md` as an accepted residual) — the real gate (`tsc + next build`) passed, nothing regressed.

## Decided
- Royce: commit + push + open PR (same flow as #630).
- Royce: rebase #632 onto latest main after #630/#631 landed.
- Royce merged #632 himself.

## Deferred
- None new. This closes the item logged in `eq/pending.md` from the previous session — full write-up moved to `eq/pending-archive.md`.

## Notes
- This confirms the `buildXQuery()` factory pattern (noted in the previous session's entry above) generalizes cleanly to simpler paginated lists, including ones with per-row enrichment (Instruments' assignee names, Audit Log's user names) that also needed to run against the union of paginated + full row IDs, not just the paginated set.

---

# Session 2026-07-29 (cont.) — EQ Field: Copy Last Week / Copy Week made team-filter-aware

Direct follow-on from the team-pills session logged earlier today in this file — closes the open item that session flagged (Copy Last Week / Copy Week → operated on the whole roster regardless of what a team pill filtered).

## Built
- Ran `/decide` against the flagged gap before building anything: six-step pass (frame / steelman / pre-mortem / value / feasibility / call) recommended keeping whole-roster as the default but making the scope explicit either way — not a silent flip in either direction.
- Royce: "make it filter-aware with an explicit scope note" — built exactly that (eq-field [PR #566](https://github.com/eq-solutions/eq-field/pull/566), v3.5.378): new `scripts/teams.js` helpers (`activeTeamScopeLabel()`, `activeTeamScopeIds()`) shared by both actions; Copy Last Week now scopes to the active team filter and always shows a confirm dialog stating the scope + person count when a filter is active (previously the dialog only appeared if the target week already had data); Copy Week → shows the same note directly in its modal on open. Both fall back to today's whole-roster behaviour when no filter is active — zero change for anyone not using the pills.
- Merged and confirmed live on production (`field.eq.solutions/sw.js` → v3.5.378).

## Decided
- Royce: filter-aware with an explicit scope note, over the two alternatives raised in the `/decide` pass (leave whole-roster as-is / make it filter-aware with no note).
- Royce: "merge" — confirmed after CI green.

## Deferred
- None new — this closes the one item the earlier session in this file left open. See `sks/pending-archive.md` (moved there, zero open items left in that section).

## Notes
- Named a real, deliberately-accepted trade-off rather than hiding it: Copy Last Week now confirms every time a filter is active, even the previously-silent empty-target-week fast path — a genuine extra click for anyone who filters routinely, accepted because a silent partial copy going unnoticed for a week is worse.
- Flagged (not silently absorbed) that the new scope filter matches schedule rows to people by name, not id — an assumption already baked into the whole schedule data model (every existing read/write already keys rows by `name+week`), not a new risk this change introduces.
- No live click-through possible from this sandbox (same limitation as the two team-pills PRs earlier today) — verified via `node --check`, a `new Function()` syntax pass over every inline `<script>` block in `index.html`, and the full existing test suite + eslint. Asked Royce to confirm the actual scoped-copy behaviour next time he's in the app with a team filter active.

---

# Session 2026-07-29 (cont.) — eq-shell: compliance-pack "compressing" spinner added; filename bug confirmed already fixed

## Built
- Royce attached a real compliance-pack download and asked to strip the ugly filename and make the "compressing" progress more obvious. Checked live git state first rather than assuming: the ugly-filename bug was already root-caused and fixed the previous day in [PR #1071](https://github.com/eq-solutions/eq-shell/pull/1071) (merged, live on `main`) — the file Royce had was downloaded from a job that ran before that fix deployed. No filename code touched this session.
- The "compressing" progress text (`Downloading photos (N of M)…` → `Compressing…`) already existed on the export button, just as easy-to-miss plain text. Added a spinning icon next to it using the app's existing `eq-spin`/`Loader2` pattern (same one used in `AdminWorkersPage.tsx`/`AdminTenantsPage.tsx`). eq-shell [PR #1087](https://github.com/eq-solutions/eq-shell/pull/1087), merged (`7a30a76d`), live via Netlify auto-deploy.

## Decided
- Royce picked "spinner + existing label" over "spinner + toast banner" when asked how prominent the compressing indicator should be.
- Royce: push, open PR, merge — explicit go-ahead, done in sequence.

## Deferred
- Royce to re-download a compliance pack once the deploy lands and confirm the filename reads correctly, the contact-details fix holds, and the spinner shows while it builds — folded into the existing deferred item in `eq/pending.md` rather than duplicated.

## Notes
- No click-through verification this session — the export flow needs a real signed-in tenant session with staff/licence data to trigger; typechecked (`tsc --noEmit` clean on `StaffPage.tsx`) and code-reviewed against the existing spinner pattern instead. Said so plainly rather than claiming a live test that didn't happen.
- Worth remembering as a recurring pattern: a user-reported bug can already be fixed-but-not-yet-deployed-when-they-hit-it, or fixed-and-deployed-but-they're-looking-at-a-stale-artifact (this case — the zip itself, not the page, was stale). Checking `git log` / PR history before touching code caught it in under 10 minutes instead of re-fixing something already shipped.

---

# Session 2026-07-29 (cont.) — eq-shell: removed 6 dead, never-linked import pages

## Built
- Asked to investigate a set of 6 `intake/{core,field,quotes,cards,service,review}` routes in eq-shell that a companion staging-gate fix had surfaced as possibly dead. Confirmed via fresh grep across `src/` that only `App.tsx`'s own route table referenced them — no `HubSidebar`/`MobileTabBar` entry, no in-app link anywhere, only reachable by typing the exact URL. Built 2026-05-21 ("Unit 7"), never wired in the ~2 months since.
- Checked safety before recommending removal: no reference to these URLs in eq-cards, eq-field, or eq-solves-service; the only hits in eq-context were the original 2026-05-21 design/sprint docs for this exact feature, not external bookmarks. Also confirmed the "duplicate importer" was no longer a safety differentiator — `EntityImportPanel.tsx` already routes through the same `intake-stage` gate the live Health-console import screen uses (the companion fix from PR #1088).
- Presented Royce three options (remove / wire into nav as a second surface / park) via AskUserQuestion, plain tradeoffs, no recommendation baked into the framing beyond noting duplication cost. Royce chose remove.
- Removed the 6 routes + their lazy imports from `App.tsx`, deleted `DomainLanding.tsx`, `EntityImportPanel.tsx`, `ReviewQueue.tsx` (1,040 lines, zero remaining callers), and corrected two stale comments in `stageCommit.ts`/`modules/intake/index.tsx` that referenced `EntityImportPanel` as a live caller.
- `pnpm run build` (build:packages + tsc -b + vite build) clean. eq-shell [PR #1090](https://github.com/eq-solutions/eq-shell/pull/1090): pushed, all CI green (typecheck/test/lint, gitleaks, tenant-drift + anon-grant + policy-lint, migration-ledger hygiene, Netlify deploy preview), merged squash `484b1b5`, remote branch deleted. Deploying to core.eq.solutions via the standard Netlify auto-deploy-on-push-to-main trigger.

## Decided
- Royce: remove the dead routes, over wiring them into nav or parking the decision.
- Royce: push + open PR.
- Royce: merge the PR.

## Deferred
- Royce to click through the Import screen (`/intake`, the Health console — untouched by this change) on core.eq.solutions once the deploy lands, confirming it still loads and commits normally. Logged in `eq/pending.md`.

## Notes
- This was a separate, out-of-scope cleanup item spun out of a different session's staging-gate fix (PR #1088) — not a continuation of that work, just something it surfaced.
- The repo's brief-gate hook (`guard.js`) blocked the first Edit on eq-shell mid-session because `/brief eq-shell` hadn't been run yet this session — ran it (digest + worktree registry + git state), which folded cleanly into work already done as part of the investigation. Branched off fresh `origin/main` rather than reusing the session's other unrelated eq-shell branch (`claude/lockdown-anon-eq-enforce-function-privacy`), since this was explicitly flagged as a separate cleanup.

---

# Session 2026-07-29 (cont.) — eq-shell: EQ Ops — view archived quotes without restoring them

## Built
- Royce asked whether staff could see an archived quote's contents without un-archiving it — the only path today was clicking Restore (which put it back live), then remembering to Archive it again. Investigated first: `eq_get_quote_detail` (the RPC the detail panel already uses) has no `deleted_at` filter, so it already returns data for archived quotes — this was a pure frontend gap, no backend change needed.
- Ran the session in the pre-existing worktree already named for this exact task (`dropdown-filtering-capabilities-11ebbd`, branch `claude/view-archived-quotes-f85a5d`) — clean, up to date with `main`.
- Offered Royce two scope options via AskUserQuestion: a bare View button leaving the full edit panel active, vs. View + locking the lifecycle controls so viewing genuinely can't reactivate it. Royce picked the locked version.
- Built in `src/modules/quotes/QuotesModule.tsx`: Archived-quotes rows are now clickable (plus an explicit View button) and open the standard detail panel via the existing `openDetail()`. When the opened quote is still archived (cross-referenced against the loaded Archived list), the status pill becomes a plain read-only "Archived <date>" badge instead of the editable stage dropdown, and the action bar is trimmed to Restore / Download / Delete only — Edit, Duplicate, Revise, Mark as Sent, Close as Lost/Cancelled, and Archive are all hidden. Line items, notes, activity, contacts, and attachments stay fully visible.
- `tsc --noEmit` clean. Checked the file's pre-existing ESLint noise (25 pre-existing `react-hooks/refs`/`react-hooks/purity` errors on `main`, not part of any CI gate — confirmed no workflow runs eslint) before concluding two shifted-line matches after the edit were the same pre-existing pattern moving position, not a real regression.
- eq-shell [PR #1093](https://github.com/eq-solutions/eq-shell/pull/1093): pushed, all CI green (typecheck/test/lint, gitleaks, schema-drift + anon-grant + policy-lint, migration-ledger hygiene, Netlify deploy preview), squash-merged (`2b01bf3b`), branch deleted.
- Confirmed live the same way as yesterday's Suppliers fix: `get-project` → `currentDeploy.currentDeploy.id` → `get-deploy-for-site` → sliced response's `commit_ref` matched `2b01bf3b`, `state: "ready"`, `context: "production"`, published `2026-07-29T01:39:30Z`.

## Decided
- Royce: lock the lifecycle controls while viewing an archived quote, not just add a bare View button.
- Royce: commit + open PR, then separately: merge the PR, then separately: confirm live once the deploy lands — three explicit go-aheads, each given after the previous step completed.

## Deferred
- Royce's own click-through: EQ Ops → "…" → Archived → View a quote, confirm the read-only badge/trimmed actions render correctly and Restore still works from there. Verified only via the Netlify deploy record this session, no production login session available in this environment. Logged in `eq/pending.md`.

## Notes
- This worktree's branch name (`claude/view-archived-quotes-f85a5d`) already matched the task before any investigation started — a useful confirmation signal that this was the right scope, not a coincidence worth re-deriving.
- Ran `/brief eq-shell` before the first Edit per the repo's Rule 0.6 gate; worktree wasn't in `worktree-registry.md` but wasn't created this session either, so no new-worktree registration step applied.

---

# Session 2026-07-29 (cont.) — eq-service: import silently generating nothing — found and fixed a real, always-broken constraint

## Built
- Royce reported the Maximo PDF importer "generating nothing even after skipping questionable stuff", with a screenshot showing `Failed to create alias: there is no unique or exclusion constraint matching the ON CONFLICT specification`.
- Traced it live rather than guessing: `service.job_plan_aliases` (what the app actually writes to) only ever had a PK on `id` — no unique index on `(tenant_id, source_system, external_code)`, despite the import commit code upserting on exactly that conflict target whenever a group needs Accept/Nominate/Create resolution. Confirmed the table has 0 rows — this write path has never once succeeded in production, for any tenant, since the table came into being. A single failed alias write aborted the *entire* commit before any maintenance checks were created, which is why skipping the unresolved rows didn't help.
- Found a second, orphaned table while investigating: `app_data.job_plan_aliases` has the correct constraint and 2 rows, but the app never talks to it — unrelated to the fix, flagged separately, not touched.
- Wrote migration 0195 adding the missing unique index. Dry-ran it twice in rolled-back transactions against live ehow before committing — once for the DDL itself, once simulating the exact app upsert — both succeeded cleanly with nothing left behind. eq-service [PR #635](https://github.com/eq-solutions/eq-service/pull/635), merged.
- First dispatch of the migration-apply pipeline failed its own pre-flight check: the file had a top-level `BEGIN;`/`COMMIT;`, which the runner (it owns the transaction wrap itself) rejects before touching anything — confirmed nothing was applied. Fixed and re-opened as eq-service [PR #636](https://github.com/eq-solutions/eq-service/pull/636), merged.
- Re-dispatched successfully. Verified live against ehow: the index now exists on `service.job_plan_aliases`.

## Decided
- Royce: "yes, write the migration" — after the brief.
- Royce: "merge it" for both PR #635 and PR #636, each after CI cleared.
- Royce: "yes" — explicit go-ahead to dispatch the production migration-apply workflow, asked separately from the merge since dispatching is the actual live-DB step.

## Deferred
- None new. The orphaned `app_data.job_plan_aliases` table (2 rows, correct constraint, never read by the app) is worth a look eventually but isn't blocking anything — noted here rather than in pending.md since it's not actionable on its own.

## Notes
- This bug had been live since the table was created — every real import that needed a job-plan match resolved via Accept/Nominate/Create has been silently failing, tenant-wide, this whole time. Worth remembering: an `ON CONFLICT` upsert failing with "no unique or exclusion constraint" is *never* a data problem — it means the constraint the code assumes was either never created or got dropped, and the fix is always schema-side, not code-side.
- `scripts/migrate-service.mjs` rejects migration files containing top-level `begin;`/`commit;`/`rollback;` — it owns the transaction wrap per file. Older migrations (0043-0053, including the original 0049 that first defined this table) used top-level tx control and are grandfathered in the ledger seed, but any *new* file must not follow that old style. Worth checking new migration files against this before the first dispatch attempt, not after.
- Confirmed live twice in this session that dry-running DDL in a rolled-back transaction only proves the SQL runs — it does not catch runner-level format rules like the tx-control guard, which only fires on the real dispatch path. Belt-and-braces here would be running the file through `scripts/migrate-service.mjs --plan` locally before opening the PR, not just a raw SQL dry-run.

---

# Session 2026-07-29 (cont.) — eq-solves-intake: Reconcile silently skipped phone/ABN cleanup for customer and contact rows

## Built
- Royce handed over a pre-diagnosed bug report naming the exact file and lines. Confirmed by direct code read before touching anything: `ReconcileModule.tsx`'s `PHONE_FIELDS`/`normaliseRow` keyed off plural entity names (`customers`, `contacts`), but `classifySheet` (via `ROLE_REGISTRY` → `x-eq-entity` in `simpro-schemas.ts`) returns singular names (`customer`, `contact`, `staff`, `site`). The lookup never matched for customer or contact rows — only `staff` happened to work since singular and plural are identical there.
- Fixed the three keys to singular. `tsc --noEmit` clean on the file (remaining package-wide errors are pre-existing, in unrelated files — `EntityDrillDown.tsx`, `IntakeHealthHome.tsx`).
- Branch was 14 commits behind `main` (PR #79's earlier work had already squash-merged as `139f515`, plus newer unrelated work landed since). Merged `origin/main` into the branch before opening the PR so the diff showed only the real 4-line change instead of a false ~2,259-line diff from stale history.
- eq-solves-intake [PR #82](https://github.com/eq-solutions/eq-solves-intake/pull/82): pushed, CI green (Build/typecheck/test), squash-merged, both local and remote branch deleted (local `-d` false-negatived on the squash-merge, confirmed `MERGED` via `gh` first, then `-D`).

## Decided
- Royce: commit, push, open PR, merge, delete branch — five explicit go-aheads in sequence.

## Deferred
- Royce to reconcile a customer CSV with a messy phone number/ABN and confirm normalization now applies live — verified in code + typecheck only, not click-tested. Logged in `eq/pending.md`.

## Notes
- Bug report arrived fully pre-diagnosed (exact file, lines, root cause named) — verified independently against the actual `x-eq-entity` values in `simpro-schemas.ts` rather than trusting the report at face value, per this repo's own "verify against live/actual state" convention. Confirmed correct.
- Stale-branch detection (Rule 0.6 Step 3) caught the 14-commits-behind gap before opening the PR, not after — avoided shipping a confusing false-diff PR.

---

# Session 2026-07-29 (cont.) — eq-solves-service: closed the loop on the pending npm audit CVE

## Built
- Royce asked to close the loop on eq-service's recurring red `npm audit --audit-level=high` CI check (16 findings, all one CVE — `brace-expansion` DoS, GHSA-mh99-v99m-4gvg). `package.json` already had two surgical `overrides` from an earlier session closing two dependency chains (Sentry bundler plugin, typescript-eslint); investigated whether the rest could close the same way.
- Fixed a third, previously-missed chain: `exceljs → archiver → readdir-glob → minimatch → brace-expansion` (the xlsx export path). Added a matching override; verified live that `readdir-glob`'s own `minimatch` now resolves `brace-expansion@5.0.8` (patched) instead of `2.1.2` (vulnerable).
- Directly tested the remaining chain rather than assuming: all other findings trace to one shared `minimatch@3.1.5` node used by both `eslint` and `archiver-utils`/`zip-stream`/`rimraf`. Forcing the same override onto that node crashes `eslint` outright — `minimatch@3.1.5` calls `brace-expansion`'s old exported-function API, which 5.x removed. This confirms, with a real repro instead of inference, that the remaining findings genuinely need an `eslint` major-version bump — out of scope, and now a tested decision instead of an assumption.
- Verified no regressions: `tsc --noEmit` clean, `next build` clean, full `vitest` suite (370/370) passing, and a live `exceljs` workbook write → buffer round-trip confirmed the xlsx export path still works.
- eq-service [PR #634](https://github.com/eq-solutions/eq-service/pull/634): committed, pushed, opened with the CVE reasoning documented in the PR body (per `AGENTS.md`'s "0 findings or an explicit waiver" clause), CI confirmed matching expectations (real gate green, audit red only on the documented residual), merged by Royce. `gh pr merge --squash --delete-branch` hit the same known local-worktree conflict documented in `worktree-registry.md` (`main` is checked out by a concurrent worktree) — confirmed the merge completed on GitHub's side via `pr view` before treating it as done, remote branch deleted manually. Confirmed `main`'s CI post-merge shows only the expected residual, nothing new or regressed.

## Decided
- Royce: "close the loop on the pending audit fix" — investigate + fix what can genuinely be fixed.
- Royce: "merge #634" — explicit go.

## Deferred
- None new. The remaining `eslint` major-version migration needed to fully clear the audit check is a real, out-of-scope, deliberately-not-picked-up piece of work — not tracked as an open to-do, since nobody has decided to take on that migration.

## Notes
- Caught and self-corrected two real mistakes mid-session before committing: (1) a full `npm install` after deleting `package-lock.json` produced an unrelated 4,151-line lockfile diff from picking up today's-latest registry patch/minor versions across ~190 packages, unrelated to the actual fix — re-diffed and confirmed no major-version bumps hid in the noise before accepting it (a full re-resolution turned out to be unavoidable to get npm to honor a changed `overrides` entry at all — incremental `npm install` silently no-ops on override changes if the existing lockfile already looks self-consistent); (2) a targeted `npm install <pkg>` invocation meant to force re-resolution of a transitive dependency instead added `readdir-glob`/`minimatch`/`brace-expansion` as new *direct* dependencies in `package.json` — caught by re-reading the diff, reverted.
- Real technique worth reusing: when a "fix available via `npm audit fix --force`" warns of a breaking change, don't just accept the vendor's claim — reproduce it directly (apply the override, run the actual affected tool) to get a real yes/no instead of an assumption. That's what turned eq-shell's earlier "not verified against `minimatch`'s newer API" residual note into a confirmed, evidenced one for eq-service specifically.
- The eq-shell `pending.md` entry for this same CVE (`eq-shell: full Dependabot sweep — 146 alerts down to 6 known/deferred`, 2026-07-28) is a **separate instance in a different repo** that happens to hit the identical dependency chain — left untouched, not the same tracked item as this session's eq-service work.

---

# Session 2026-07-29 (cont.) — eq-cards: STATUS.md auth-flows doc corrected, phone OTP not email OTP

## Built
- Verified live code (routes.dart, app_router.dart, email_entry_screen.dart) against STATUS.md's auth-flows table: `/auth/email` (`Routes.email`, `EmailEntryScreen`) collects a mobile number and calls `sendPhoneOtp` — there is no email-OTP screen anywhere in the repo. The route/file/class names are legacy leftovers from before the flow flipped to phone-only.
- Fixed STATUS.md in two places: the auth-flows table row, plus a matching "direct email-OTP" reference in the Key architecture notes section. Added a short note that the `/auth/email` naming is legacy, kept for URL stability only.
- Committed (`docs(status): correct auth-flows table`), rebased onto a same-day merge (PR #186, unrelated netlify.toml/welcome.html cleanup) that landed between commit and push, pushed clean to `origin/main` as `447ba9f`.

## Decided
- Royce: "commit this", then "merge" — pushed straight to `main` (no PR; single docs-only commit, branch already on main).

## Deferred
- None new.

## Notes
- Docs-only change, no code touched, no deploy triggered (eq-cards `deploy.yml` only runs on `workflow_dispatch`/`release/v*` tags, per existing project memory — a plain push to `main` does not deploy).
- STATUS.md still has other stale items open from earlier sessions (SW claim, Email OTP dashboard-mode check) — untouched this session, already tracked in `eq/pending.md` under the 2026-07-06 close entry.

---

# Session 2026-07-29 (cont.) — eq-receipts: added an editable invoice number field, resolved the two flagged duplicate pairs

## Built
- Added an "Invoice / receipt number" field to Verify (only shown once a value exists, or on request via "+ Add invoice number") so a misread or missing value can be corrected by hand — closes the gap left by the earlier duplicate-detection fix, where `invoice_number` had no way to be edited. Also fixed the save handler silently dropping edits to it. eq-receipts [PR #15](https://github.com/eq-solutions/eq-receipts/pull/15), merged, deployed (frontend only — confirmed via Netlify deploy record matching the merge commit).
- Went back and actually checked the two duplicate pairs the earlier fix had flagged, reading the real line-item data instead of trusting the hash match alone:
  - **Anthropic ($12.02 USD, 2026-07-28) — genuine duplicate.** Identical "Auto-recharge credits" line item, same ABN, same payment method — the same real charge uploaded twice (once as a phone photo, once as a pasted screenshot). Deleted the unverified duplicate row (line items + receipt), kept the already-approved one.
  - **GitHub ($48 USD / $68.64 AUD, 2026-07-22) — false positive.** Line items showed two genuinely different subscriptions: an annual Team plan billed to the eq-solutions org, and an annual Developer plan on Royce's personal account — coincidentally the same renewal date and price. Dismissed as not-a-duplicate (`dupe_dismissed_at` set), both charges are legitimate and kept.
- Confirmed via a direct query that zero unresolved duplicate groups remain in the live table after both fixes.

## Decided
- Royce: delete the Anthropic re-upload, dismiss the GitHub pair — the recommended option, after seeing the line-item evidence for both.
- Royce: leave the one orphaned image file (from the deleted Anthropic duplicate) alone rather than have a one-off admin edge function built and left permanently in the project for a single-use, low-value cleanup — direct SQL deletion of `storage.objects` is blocked by Supabase itself ("use the Storage API instead").

## Deferred
- None new — both duplicate-pair follow-ups from the earlier session are now resolved (moved to `eq/pending-archive.md`).

## Notes
- **Real finding, not just a fix**: the GitHub pair proves the vendor+date+amount dedup check has a genuine blind spot in the other direction — it can false-positive on two real, different transactions that coincidentally share an amount and date (annual subscription renewals are exactly the case most likely to collide like this). Not fixed; would need comparing line-item descriptions too. Logged in `eq/pending-archive.md` under the corrected eq-receipts entry so the next session doesn't have to rediscover it.
- The earlier eq-receipts pending.md entry (2026-07-29, duplicate-detection fix) had one factual error by the time this session re-checked it — it described the GitHub pair as "a second real duplicate," which turned out to be wrong. Corrected before archiving rather than leaving a wrong record as the permanent one.
- Brief-gate flag had to be re-written mid-session (once after the first `/close` cleared it, since the next task needed a fresh Edit) — expected behavior per the gate's own design, not a bug.

---

# Session 2026-07-29 (cont.) — eq-cards: mobile "old email login" audit — real cause was device-side, but found a genuine dead-config bug along the way

## Built
- Royce reported the old email-login screen opening on his phone (Brave) instead of the current phone-OTP flow. Full live audit before touching anything: confirmed the production entry screen (`/auth/email`, file still named `email_entry_screen.dart` from before the flip) is phone-only; confirmed via `curl` that `main.dart.js`/`index.html`/`flutter_service_worker.js` all carry correct `no-cache`/`must-revalidate` headers live; downloaded and read the actual deployed `flutter_service_worker.js` — it's a self-unregistering kill-switch (`skipWaiting()` + forced `client.navigate()` on activate), working as designed. Conclusion: production is clean. The real cause is a Flutter service worker on Royce's specific phone, registered before the auth-hardening phone-OTP flip, still serving its own cached copy of the old build — a known PWA update-lifecycle trap (an old SW intercepts the very requests that would let the browser discover a newer one). Fix communicated directly: fully close (not just background) the Cards tab/PWA, clear Brave's site data for cards.eq.solutions, reopen.
- While verifying live headers, found `netlify.toml` had a `[[headers]]` block duplicating/conflicting with `web/_headers` (`main.dart.js` cached 7 days there vs. `no-cache` in `web/_headers`) — same bug class that broke CSP once before (`00fcf30`, 2026-05-25), silently reintroduced 2026-06-08 (PR #49). Verified live `web/_headers` was winning (no active harm), flagged it.
- Royce asked to remove it and check for more. Broader sweep found `netlify.toml`'s entire `[[redirects]]`/`[[headers]]` config is dead for this deploy pipeline — the GitHub Actions job zip-uploads `build/web` only, root `netlify.toml` is never shipped (confirmed both by `web/_redirects`'s own comment and by direct live behaviour: hitting `/` serves the Flutter app directly, not the `welcome.html` "smart front door" `netlify.toml` still claimed to redirect to). Traced that specific rule: it was correctly retired from the *live* sources (`web/_redirects`, `web/_headers`) on 2026-06-23 (`b37b4c5`, GoRouter took over root routing), but nobody realised `netlify.toml` held its own dead copy that was never cleaned up.
- Stripped `netlify.toml` to just `[build]`/`[functions]` plus an explanatory comment. In the process found `web/_headers` (the file that's actually live) was missing a `main.dart.wasm` cache rule that only existed in the dead netlify.toml copy — added it, so nothing was lost. Fixed a stale comment in `web/flutter_bootstrap.js` misattributing the SW cache header to netlify.toml.
- Deleted the now-fully-orphaned `web/welcome.html` on Royce's explicit go — confirmed zero remaining references anywhere in the repo first.
- eq-cards [PR #186](https://github.com/eq-solutions/eq-cards/pull/186): committed, pushed, opened, auto-merge (squash) set pending CI, merged clean.
- Spawned a background task (`task_814bdaef`) to refresh STATUS.md's auth-flow table, which still documented the old email-OTP flow — Royce ran it in a separate session; it landed as its own commit (`447ba9f`), logged separately above in this file.

## Decided
- Royce: remove the stale netlify.toml headers block, and check for other similar "easter eggs" while in there.
- Royce: delete `web/welcome.html`.
- Royce: commit, push, open PR, merge — given explicitly as a single go, then formalised as "how do I make this one command" — answered directly (no native one-liner exists; either say "ship it end to end" in future, or use `gh pr create --fill && gh pr merge --auto --squash`) rather than assumed.

## Deferred
- Royce to clear Brave's site data for cards.eq.solutions on his phone — the actual fix for the reported symptom, can't be done from this environment. Logged in `eq/pending.md`.

## Notes
- Good example of a report that felt like a live bug but wasn't: every server-side signal (headers, entry screen, service-worker code) checked out correctly live, which is what pushed the diagnosis toward "stuck client, not stuck server" rather than continuing to hunt for a production cause that didn't exist. Worth remembering as the general pattern — when live verification of the reported surface comes back clean, the next suspect is the reporter's own device/cache, not a deeper server bug.
- The dead-netlify.toml-config finding is a good instance of "docs and even code comments can describe intent that was never true in production" — `flutter_bootstrap.js`'s own comment confidently attributed a cache header to netlify.toml, which had never been the live source since this deploy pipeline (zip-upload via GitHub Actions) was set up. Worth treating source-file comments about *deploy-time* behaviour with the same live-verify skepticism as separate docs, not as automatically more trustworthy just because they're in code.
- `web/_redirects` already carried the correct warning ("netlify.toml is not shipped") as an inline comment — it just wasn't cross-referenced from netlify.toml itself, so the dead config kept accumulating there anyway. The fix folds that warning into netlify.toml directly this time.

---

# Session 2026-07-29 — Intake redesigned (5 tabs → 4), plus two bugs found and fixed along the way

## Built
- Reviewed the live EQ Intake app end-to-end, confirmed actual wiring (not just docs), and found a real overlap-risk bug before scoping any redesign work: `/intake`'s "Into EQ" commit path bypassed the staging/conflict-detection gate the other importer already enforced.
- Fixed that gap: eq-solves-intake [PR #80](https://github.com/eq-solutions/eq-solves-intake/pull/80) + eq-shell [PR #1088](https://github.com/eq-solutions/eq-shell/pull/1088), merged, live. Also relabelled the `/intake` nav link "Import"→"Intake" (Download icon→Activity icon) while in there.
- Root-caused a false "Re-review" licence badge to the Cards→Shell licence sync bumping `updated_at` on every no-op run (confirmed via live SQL: 24 licence rows across 4 staff sharing one exact automated-sync timestamp). Fixed with a fetch-existing-then-diff guard + 5 new unit tests — eq-shell [PR #1091](https://github.com/eq-solutions/eq-shell/pull/1091), merged, live.
- Built a clickable HTML mockup (Artifact) of a 3-tab Intake redesign after Royce found the live 5-tab layout overwhelming, walked it through with him, then shipped the real thing:
  - PR1 (eq-solves-intake [#81](https://github.com/eq-solutions/eq-solves-intake/pull/81)): Health + Queue → Overview + To Do. Extracted the site-merge decision widget (`SiteAdvisoryPanel`/`MergePanel`) out of `IntakeHealthHome.tsx` into a new `DuplicateMergePanel.tsx`, mounted inside `RemediationQueue`'s tab instead (it's a decision, not a diagnostic). Unified the two tabs' pending-count badges. Collapsed the six-metric breakdown behind a `<details>` disclosure.
  - PR2 (eq-solves-intake [#83](https://github.com/eq-solutions/eq-solves-intake/pull/83)): Import + Reconcile → Bring Data In. `IntakeDropZone` is now the single entry point; each classified file slot gets an optional "Check for conflicts" action that opens `ReconcileModule` scoped to just that slot (new `initialSlot`/`onClose` props, tracked by object reference not array index to survive slot removal). SimPRO-specific copy replaced with generic wording. Deliberately did NOT route resolved reconcile rows into the shared multi-entity `CommitView` bundle — flagged as separate future work, not attempted.
  - Hotfix (eq-solves-intake [#84](https://github.com/eq-solutions/eq-solves-intake/pull/84)): PR1 left an unused `useCallback` import that eq-intake-demo's own `tsc --noEmit` doesn't catch but eq-shell's stricter `tsc -b` does — caught while re-vendoring, fixed at the source.
  - eq-shell re-vendor (eq-shell [#1094](https://github.com/eq-solutions/eq-shell/pull/1094)): picked up all three, plus a lockfile update initially missed in the first commit. Merged — live on core.eq.solutions via Netlify auto-deploy.

## Decided
- Ran `/decide` before scoping the redesign: ship the tab-collapse now as a focused pass, explicitly defer bulk-approve/standing-rules/trend-view/deeper-Ask — Royce's stated pain this session was legibility ("I built it and don't understand what it does"), not throughput, and bundling in the deeper mechanics would have doubled the build for unconfirmed value.
- Royce confirmed each merge explicitly and separately: #1091, #80+#1088 ("merge and go from there"), #81, #83, #84, #1094 — none merged without an explicit go, consistent with the deploy rule.
- Chose "Guard the upsert" (fetch-then-diff) over the alternative fix location for the licence-resync bug, via AskUserQuestion.
- For the Bring Data In merge, chose the scoped-down "unify the entry chrome, keep Reconcile's engine as-is" approach over a full commit-path rewrite, based on a Plan-agent feasibility pass that found the full version (multi-file bundle vs. single-file state, bypassed shared commit path) was real, underscoped work — not a re-sequencing.

## Deferred
- Live end-to-end proof that `/intake` now parks a genuinely conflicting row instead of committing it — blocked all session on the file-upload tool refusing to attach a test file not shared directly by Royce in chat. Two paths still open: Royce drags the file into chat, or does the drop himself while I check the Queue/To Do tab.
- Click-through verification of the final redesigned `/sks/intake` screens on live core.eq.solutions — the Netlify deploy preview is login-gated (no credentials in this environment), so this shipped verified only via full build + test suite + code review, not an actual click-through.
- Bring Data In's per-slot "Check for conflicts" still commits on its own scoped path, separate from the shared multi-entity `CommitView` bundle the rest of the screen uses — routing those rows into the same bundle is real, separate work, named but not started.
- The deeper "why is this still exhausting" tier, discussed but not scoped into this session: bulk-approve (still one row at a time in To Do), standing rules for recurring conflict types, a trend/history view for the health score, and a real ask-anything grounded across the whole suite (today's Ask tab is a thin preview of that per the north-star vision doc).
- A pre-existing bug in Reconcile's phone/ABN normalisation (plural/singular entity-key mismatch) was found and flagged via `spawn_task` mid-session — resolved independently by Royce in a separate session as eq-solves-intake #82, confirmed via a concurrent pending.md entry; no action needed from this session.

## Notes
- This session's own eq-intake worktree (`app-polish-improvements-8c03dd`) was found 4 commits stale on `IntakeModule.tsx`/`IntakeHealthHome.tsx` mid-session — always verify a worktree against `origin/main` before trusting it as a source for either recon or a vendor copy, don't assume a worktree from earlier in the session is still current if other PRs may have merged since.
- The Health tab's "duplicates" story was actually two unrelated systems that the original mockup conflated: `DuplicateStrip` (an on-demand, read-only `detectAllDuplicates` scan — diagnostic, stayed on Overview) vs. `SiteAdvisoryPanel`/`MergePanel` (the real `readSiteAdvisory`/`previewSiteMerge`/`executeSiteMerge` decision flow — moved to To Do). A Plan-agent pressure-test caught this distinction before the build started.
- `RemediationQueue` already had its own, unrelated `category:"duplicate"` signal (from `eq_queue_list`, explicitly "never auto-merged") sitting in the same tab the real merge tool now lives in — relabelled to "Other duplicate flags" so the two don't read as contradictory.
- eq-shell's vendoring of `@eq/intake-demo` is a manual, git-committed copy (not a submodule, not an npm package) — `eq-solves-intake` is private and Netlify's build has no credentials to clone it, so the vendored tree under `eq-shell/eq-intake/` is the actual source of truth Netlify builds from. Re-vendor procedure is documented in eq-shell's own README ("Updating the vendored packages").
- `pnpm install` regenerates `eq-schemas/src/generated/*` deterministically — the vendor script's instruction to strip that directory before copying is specifically to avoid a spurious delete+recreate diff in the PR, not because those files are stale.

---

# Session 2026-07-29 (cont.) — Suppliers table: real root cause found and fixed

## Built
- Root-caused why Login/Password still weren't visible after PR #1095's width-budget/truncation fix: `.eq-table-card` (the `@eq-solutions/ui` package's own wrapper directly around `<table>`) has `overflow: hidden`, sitting one DOM level inside eq-shell's `.eq-rc-tablescroll` scroll wrapper — so the table's overflow was getting clipped there before the outer wrapper's `overflow-x: auto` ever got a chance to run. No scrollbar ever appeared; columns just vanished off the edge.
- Fix: override `overflow-x: auto` on `.eq-table-card` itself, scoped under `.eq-rc-tablescroll` so no other table in the app is affected. Dropped the per-column ellipsis truncation added in #1092/#1095 back to plain, comfortable widths — the table now grows to its natural content width and scrolls in place instead of hiding data. eq-shell [PR #1096](https://github.com/eq-solutions/eq-shell/pull/1096), squash `cd7ce734`, merged and confirmed live on core.eq.solutions (published 2026-07-29T07:21:31Z).
- Cleaned up: removed the `eq-shell-suppliers-width-budget-wt` worktree husk (git had already untracked it; only leftover `node_modules`/vendored `eq-intake` remained, no unique source) and the `eq-shell-suppliers-scroll-real-wt` worktree used for this fix. `eq-context/system/worktree-registry.md` updated to match.

## Decided
- Royce's framing ("extend the table to the width and then make it scroll") was the correct diagnosis — confirmed by tracing the actual CSS clip rather than continuing to tune column-width budgets.

## Deferred
- Royce to click through Suppliers live and confirm all 8 columns are reachable via scroll with nothing truncated (added to `eq/pending.md`).

## Notes
- This closes out the Suppliers saga (5 PRs total across two days: #1079, #1082, #1092, #1095, #1096) — #1096 supersedes #1092/#1095's truncation approach with a structural fix, so their column-width tuning is no longer live code (replaced, not just added-to).
- Confirmed via Netlify MCP (`get-project` → `currentDeploy.id` → `get-deploy-for-site` → `commit_ref`), same verification method used all session — GitHub commit-status/check-runs still carry no signal for this repo's Netlify deploy state, only the Netlify MCP does.

---

# Session 2026-07-29 (cont.) — eq-solves-service: Contacts service_enabled leak fixed; monthly PM sheet now imports directly

## Built
- Royce asked to double-check the Service Users and Contacts lists are sourced from canonical, not a separate list. Users already was (Shell's `list-members` endpoint, with a documented local fallback). Contacts wasn't fully — traced the actual bug: `service.customer_contacts`/`service.site_contacts` (canonical views since migration 0167) filter on link/contact `active` only, never the parent customer/site's `service_enabled` toggle that `service.customers`/`service.sites` already gate on. Confirmed live: 93/197 customer contacts and 13/31 site contacts belonged to a customer/site switched off for Service in Shell, yet still rendered on `/contacts`.
- Wrote migration 0196 reusing the already-correct `service.customers`/`service.sites` views as the enablement filter (`EXISTS (...)`) instead of a third copy of the `service_enabled` logic. Verified in a rolled-back transaction on live ehow before opening the PR (counts dropped exactly as expected, INSTEAD OF triggers intact). eq-service [PR #637](https://github.com/eq-solutions/eq-service/pull/637): merged (real gate green; two pre-existing failures — flaky integration tests, the tracked `brace-expansion` audit residual — correctly not treated as blockers).
- Learned mid-session that merging this repo's migration PRs does **not** auto-apply to ehow — `apply-service-migrations.yml`'s `apply` job only fires on an explicit `workflow_dispatch`, and (despite the auto-posted PR comment claiming otherwise) that dispatch has no approval pause; it's a live-DB write the moment it's triggered. Flagged this to Royce explicitly before dispatching rather than assuming "merged" meant "live." Royce confirmed; dispatched; verified live (counts now 104/18, ledger row present).
- Royce then shared "August PM.xlsx" — his monthly hand-copied work-order sheet — asking if it could import directly instead of manual entry. It couldn't: the existing Delta/Maximo parser (`lib/import/delta-wo-parser.ts`) only recognised row-1-headered, headerless (Metronode), and SY9x-schedule shapes; this file has a free-text title (site + month) above an offset header row, no `Site`/`Target Start` columns.
- Extended the parser with a fourth shape ("titled PM sheet"): site and target month inferred from the title text when there's no dedicated column for either; every inferred-date row carries a warning so the import preview surfaces it before commit. Refactored three now-duplicated grouping blocks into one `groupRows()` helper along the way.
- Royce then asked a sharp follow-up: would a per-row `Site` column (same shape, but with Site as a real column) already work? It wouldn't have — the new code always used the title-derived site for every row, silently ignoring a real Site column. Fixed before shipping: a per-row Site value now wins whenever the column exists; the title is only the fallback.
- Verified against the real `August_PM.xlsx` file as a test fixture (12/12 rows, 3 job-plan groups: BTCHGR ×1, XFMROIL ×5, SUUPS ×6, zero errors) — found and fixed an `exceljs` `actualRowCount`-vs-`rowCount` off-by-one bug via this real-file test that a synthetic-only test suite would likely have missed (the shape's intentionally-empty title row undercounts `actualRowCount` by one). 58/58 parser tests pass, `tsc --noEmit` clean.
- Checked live against ehow before calling it done: site `SY7` resolves correctly (two `SY7`-coded rows exist, the canonical view already filters to the live one); job plans `XFMROIL`/`SUUPS` match exactly; `BTCHGR` doesn't match exactly (closest is `24VBTCHGR`) — will go through the wizard's existing fuzzy-match/alias flow on first real commit, not a bug.
- eq-service [PR #640](https://github.com/eq-solutions/eq-service/pull/640): before merging, caught the branch was 4 commits behind `main` — including an unrelated security fix (#634) and an unrelated report feature (#638/#639) — merged `main` into the branch first so the PR's diff couldn't silently revert either. Merged after CI (same two pre-existing failures as #637, correctly non-blocking). Confirmed live via Netlify MCP (`commit_ref` matched, `state: ready`, published) after Royce reported seeing the old error — the report was a stale page load caught mid-race with the immediately-prior deploy, resolved ~90 seconds later; no actual bug.

## Decided
- Royce: extend the parser for the titled-PM shape, over asking for the original unfiltered Maximo export (AskUserQuestion) — accepting the inferred-date tradeoff since every row gets a visible warning.
- Royce: dispatch the migration-apply workflow now (AskUserQuestion, given the no-pause behaviour was disclosed first).
- Royce: commit + open PR + merge, for both PR #637 and PR #640, each confirmed separately ("merge it once CI's green").

## Deferred
- First real "August PM"-style import: `BTCHGR` will need confirming/nominating against `24VBTCHGR` (or a new plan) in the wizard's fuzzy-match step — flagged in `eq/pending.md`, not a blocker.

## Notes
- `apply-service-migrations.yml`'s `production` GitHub Environment has no required-reviewer rule (confirmed live 2026-07-28, per the workflow's own header comment) — its auto-posted PR comment text ("pauses for production-environment approval") is stale/inaccurate. Worth re-flagging to Royce before every future dispatch until that comment or the environment protection is fixed for real, since the gap between what it says and what it does is exactly the kind of thing that causes an accidental live write.
- `exceljs`'s `Worksheet.actualRowCount` is a *count* of populated rows, not a max row index — differs from `rowCount` whenever a leading row (row 1, in this case) is empty. Any new parser code walking a sheet by row number should use `rowCount` as the loop bound, not `actualRowCount`, unless every row from 1 upward is guaranteed populated.
- Two live `SY7`-coded rows exist in `app_data.sites` (one legacy "Equinix SY7", `service_enabled=false`/`active=false`; one live "SY7", both true) — not a new finding, matches the already-tracked canonical-site-dedup work, just re-confirmed live in passing. Not touched.

---

# Session 2026-07-29 (cont.) — eq-field: closed the load-time gap (version-tagged immutable URLs), fought through a production deploy stall

## Built
- **eq-field [PR #567](https://github.com/eq-solutions/eq-field/pull/567)** (v3.5.379, merged `69e56fa`, live on field.eq.solutions): Royce asked to see the load-time follow-up options as a picture, then to build the recommended one. Every `/scripts/` and `/styles/` request now carries a `?v=<APP_VERSION>` query string — `index.html`'s own `<script>`/`<link>` tags, the dynamic injection in `scripts/lazy-loader.js` (`loadScript()`), and a third injection point found by grepping every `createElement('script')` call site in the repo (`index.html`'s inline `jszip.min.js` idle-loader — would have been missed by only checking lazy-loader.js). `netlify.toml` now caches those paths `public, max-age=31536000, immutable`. `sw.js`'s `PRECACHE` list is versioned to match, via a version string *derived* from `CACHE` rather than a second literal. Also fixed a small adjacent gap noticed while touching the list: `styles/spinner.css` + `styles/field-v8.css` were live in `index.html` but missing from `PRECACHE`.
- Verified live on the deploy preview before merge: 0 unversioned local script/style tags, all 80 assets precached under correct keys, 42/42 resources served via Service Worker on repeat load (DOMContentLoaded 131ms), and the same-day TAFE fix (v3.5.373) still resolves correctly stacked on top.
- Ran `/reflect` before committing (this repo's `reflection-gate` blocks eq-field commits without a paired `docs/reflection-log.md` entry) — flagged honestly that this trades a self-healing caching failure mode (v3.5.374: broken for at most a day) for one that doesn't self-heal at all if the version-bump ritual is ever skipped (immutable: broken for up to a year), landing the same day a sibling bug (PR #564) already proved that ritual gets missed in practice.

## Decided
- Royce, via `AskUserQuestion` after seeing the visual comparison: ship the version-tag URL fix ("build this fix now").
- Royce, when the production deploy stalled after merge: try the Netlify MCP's manual deploy trigger; when that failed once (transient-looking 500), try again; when the retry surfaced a real, reproducible root cause instead, accepted the recommendation to use the Netlify dashboard's manual trigger instead of continuing to retry the broken path.

## Deferred
- 3 of the original 4 load-time follow-ups remain open (concatenating boot scripts, auditing which scripts truly need to block first paint, Netlify Early Hints) — logged in `eq/pending.md`.
- Root cause of *why* this account's Netlify projects don't reliably auto-deploy on push (this is the second repo — after eq-receipts — to need a manual kick) not investigated; worked around, not fixed.
- CLI-proxy deploy-from-a-git-worktree is now a confirmed-broken path, not just occasionally flaky — noted in `eq/pending.md` so a future session doesn't re-attempt it and burn the same time.

## Notes
- **Mid-task staleness catch, exactly the scenario the /brief protocol exists for**: had already built the version-tag fix once (targeting `v3.5.375`) before checking git state, only to find `main` was 5 commits behind — a concurrent session had shipped an unrelated "Team pills" feature that had *already claimed* `v3.5.375-378`, and, more importantly, had shipped [PR #564](https://github.com/eq-solutions/eq-field/pull/564) — a version-only bump needed because a script was edited without bumping the version, and since `/scripts/` is cache-first (yesterday's fix), the edit silently never reached already-active users. Discarded the stale work, pulled fresh, rebuilt against `v3.5.379`, and used PR #564 directly as the risk case named in this fix's own commit message and reflection entry. Skipping the staleness check here would have shipped a version-number collision on top of a caching change that needed to be reasoned about *especially* carefully that day.
- **Production deploy stall, root-caused not just worked around**: after merging PR #567, `field.eq.solutions` didn't pick up the new build for several minutes (vs. ~20-30s on the three earlier merges that day). Confirmed via GitHub API that `main` was correct at the right commit the whole time — this was purely a Netlify-side lag/stall, not a bad push. First manual-deploy attempt via the Netlify MCP's CLI-proxy path failed with a generic-looking 500 (assumed transient); the retry instead surfaced the real, reproducible cause: `open /opt/build/repo/.git: is a directory` — the proxy zips up the local checkout including `.git`, and this session's checkout is a git *worktree* (`.git` is a small pointer file there), which collides with a real `.git` *directory* Netlify's build environment already has cached at that path from prior native GitHub-integration builds. This is structural, not transient — it will fail identically every time run from a worktree, and retrying it again would have been pure waste. The dashboard's manual "Trigger deploy" button (re-clones from GitHub directly, no local zip) worked immediately. Also correctly declined to route around this by switching to the root `eq-field` checkout — it was on a different branch (`claude/copy-week-team-scope`) with its own uncommitted state, not mine to touch.
- **A background task notification arrived mid-session and was correctly NOT treated as user input** — the harness's own system-reminder made this explicit, but worth reinforcing in this log: a `<task-notification>` completing (the poll-for-v3.5.379 background check) is not the user saying anything, confirming anything, or approving anything. Verified the actual claim (curled production directly) before reporting it as live, rather than trusting the notification's summary text alone.

---

# Session 2026-07-29 (cont.) — eq-solves-service: ACB/NSX Test Report shipped, Report Settings toggles extended to 3 more reports

## Built
- Two background plans from earlier in this session (dispatched per Royce's "can we do much in the background and stage the implementation?") came back. Presented a synthesis via AskUserQuestion as three separate yes/no calls rather than one bundled decision.
- **ACB/NSX Test Report** — built the full three-file pattern (input-builder / route / download button) for both kinds, following the existing `pm-asset-report.ts` triplet exactly. Reused `resolveBreakerIdentity()` and the logo/photo helpers already shared across every other generator.
- **Mid-build stop, not silently absorbed**: live QA against the one real NSX dataset on ehow showed 0/12 checklist labels matching between the dormant report template and the actual technician workflow screens (`AcbWorkflow.tsx`/`NsxWorkflow.tsx`), and the CB protection-setting attributes were being looked up from a dead `readings` array instead of their real typed DB columns. Stopped, ran a second AskUserQuestion laying out the finding plainly (ship broken / fix first / hold off). Royce: fix first.
- Rewrote both report generators' checklists verbatim against the real workflow screens (23 ACB items, 12 NSX items) and rewired ~20 breaker-attribute fields per generator to their real DB columns. Re-verified against the live NSX dataset: 12/12 label matches, up from 0/12.
- eq-service [PR #644](https://github.com/eq-solutions/eq-service/pull/644), merged.
- **Report Settings toggles extended**: sign-off (Field Run-Sheet), cover page + executive summary (PM Check Report) — both trivial, same `?? true` default-safe pattern already used on the Customer Report. eq-service [PR #645](https://github.com/eq-solutions/eq-service/pull/645), merged.
- **Compliance Report cover toggle** — required extracting the inline cover-page construction into its own `buildComplianceCover()` function first (the approved plan's recommended approach, rather than a riskier inline conditional). eq-service [PR #646](https://github.com/eq-solutions/eq-service/pull/646), merged.
- All three confirmed live via Netlify MCP (`get-project` → `currentDeploy.id` → `get-deploy-for-site` → `commit_ref` matched `65c4695`, `state: ready`, published 09:52:12Z) — Royce separately asked to "deploy this to Netlify" after the merges; auto-deploy-on-push had already picked it up, no manual trigger was needed.

## Decided
- Royce, via AskUserQuestion: "Yes, build it" (ACB/NSX report), "Yes, wire the trivial ones" (Run-Sheet sign-off + PM Check cover/summary), "Yes, do the refactor + gate" (Compliance Report cover).
- Royce, via a second AskUserQuestion when the label mismatch was found mid-build: "Fix labels before shipping" over shipping as-is or holding the whole feature.

## Deferred
- Secondary Injection / Protection Test Results readings (`SI: Long Time - Setting` style labels) still don't match the report generators' lookup keys — same bug class as the checklist mismatch just fixed, found during the same QA pass, left out of scope for this pass. Logged in `eq/pending.md`.
- ACB Test Report verified correct by code symmetry with the NSX path (which has real data to check against), not yet against an actual completed ACB check — zero exist live yet. Logged in `eq/pending.md`.

## Notes
- The checklist-label and CB-attribute mismatch was a real, unplanned-scope finding, not a code bug — the dormant report template had simply never been built against the real technician workflow. Worth remembering as a pattern: "the generator exists and compiles" is not the same claim as "the generator's output is correct," especially for a code path with no live data to exercise it before this session.
- Electrical Testing readings (Contact Resistance, IR Closed/Open) already matched the report templates' hardcoded keys exactly — left untouched, not part of the mismatch.
- `gh pr merge --squash --delete-branch` hit the same known benign local-worktree error as several other sessions today (`'main' is already used by worktree at ...`) on PR #646 — verified the GitHub-side merge completed via `pr view --json state,mergedAt` before treating it as done, per the established pattern.

---

# Session 2026-07-29 (cont. 2) — Suppliers finished properly; found the real cause of the recurring false re-review flags

## Built
- **Suppliers page — the actual root cause, finally.** After #1096 (real scroll fix) and #1097 (pagination), Royce asked why the table wasn't using the full page width. Traced it to `Suppliers.tsx` never passing `fullWidth` to `<HubLayout>`, so it was stuck inside the package's default 860px reading-width cap the entire time — the same cap that made 8 raw columns need scrolling in the first place. Every other wide-table page in the codebase (StaffPage, equipment, FieldRosterPage, CustomersPage) already passes `fullWidth`. eq-shell [PR #1099](https://github.com/eq-solutions/eq-shell/pull/1099), merged, live. Once fixed, all 8 columns fit naturally — reverted #1098's column-hiding-by-default (no longer needed) in [PR #1100](https://github.com/eq-solutions/eq-shell/pull/1100), merged, live. Suppliers saga closed: 6 PRs (#1079, #1082, #1092, #1095 done in earlier sessions; #1096, #1097, #1098, #1099, #1100 this session) — #1099 supersedes all the truncation/hiding workarounds that came before it.
- **Found and fixed the real cause of the recurring "Re-review" badge false-flag** (Royce: "this happens alot, licenses keep required a re review for no reason"). PR #1091 (merged earlier today) was believed to fix this but doesn't — it guards a different table (`app_data.licences`, the tenant plane for Field) than what the Staff-page badge actually reads (`public.licences`, jvkn canonical). Root cause: a jvkn DB trigger (`licences_set_updated_at`) stamps `updated_at` on every UPDATE regardless of whether any real field changed. Found 2 confirmed cross-person batch-touch incidents (14 Jul, 20 Jul) explaining 3 of today's 9 flagged people. Built the proper fix (Royce's choice over 2 lighter options): record a content fingerprint (licence number/expiry/no-expiry/photo+document storage paths — deliberately the raw stable paths, not the signed URLs which re-mint every fetch) on each verified licence at review time, compare that instead of timestamps. Legacy entries with no fingerprint fall back to the old check unchanged. eq-shell [PR #1101](https://github.com/eq-solutions/eq-shell/pull/1101), merged, live — 274/274 tests pass, no migration needed (jsonb column).
- Merged an unrelated pending PR: eq-shell [#1065](https://github.com/eq-solutions/eq-shell/pull/1065) (lazy-load `@sentry/node` for local `netlify dev`), merged, live.

## Decided
- Royce, given the choice, wanted the proper content-fingerprint fix for the re-review badge, not a quick patch or a one-off manual clear.

## Deferred
- Royce to re-review Bruno Vita Pedrosa, Luke Wheeler, and Mohamed Ahmed — clears their currently-false flags and, now that #1101 is live, actually sticks this time.
- Royce to click through Suppliers live (all 8 columns, no scroll needed).

## Notes
- **Corrected a wrong diagnosis from earlier today's memory/pending.md**: PR #1091 does not fix the Staff-page re-review badge — it was never reading the table #1091 touches. `eq/pending.md`'s #1091 section rewritten to reflect this; memory file should be corrected too.
- Root-cause pattern across both fixes this session: don't trust "it's already fixed" claims (memory or otherwise) — verify the actual code path each fix touches against what's actually failing. In both cases the earlier assumption was one table/prop-name off from the real one.

---

# Session 2026-07-29 (cont. 3) — eq-solves-service: fixed Secondary Injection labels; found the same bug had also silently broken the checklist relabel from #644

## Built
- Royce asked to fix the Secondary Injection label mismatch flagged as deferred earlier today. Re-read the actual code on latest `main` first (this worktree's branch was 3 commits behind — the #644/#645/#646 merges weren't in it yet — so checked out a fresh branch off `origin/main` before touching anything).
- Traced the real stored label format directly from the save actions (`acb/actions.ts:491`, `nsx/actions.ts:305`): every electrical reading gets wrapped in `Electrical: ` server-side on top of whatever label the client sends, and the visual checklist is saved separately under `Visual Check: ` with no such wrapper.
- Found `findReading()`'s prefix-stripping regex only handled a bare `Visual:`, never `Visual Check:` — proved this with a plain Node regex test before touching any report code. This meant the #644 checklist relabel (making the report say the right item names) never actually worked: every row's Result/Comment has been silently blank the whole time, because the lookup could never find the reading behind the correct-looking label. Confirmed this against the one real live checklist dataset on ehow: 0/12 matched before the fix, 12/12 after.
- Fixed `findReading()` in both `acb-report.ts` and `nsx-report.ts` to strip `Visual Check:` too.
- Rewrote the Protection/Trip Test Results tables to look up the real `SI: <Function> - <Field>` readings (Current Levels, Trip Time, Min/Max Trip Time, Result) instead of a `Protection <name>`/`Trip <name>` label nothing had ever written.
- ACB's quick-items table pointed at three invented items (Operation Counter - Before/Castle Key Fitted/safety shutters) with zero backing data ever — replaced with the real Greasing/Op Counter/Racking fields from the Electrical Testing tab. Dropped two free-text lines ("Carry Out Secondary Injection...", "Operation Counter - After") that never had real data behind them in either app.
- No live check has reached the Electrical Testing/SI step yet (checked live — every NSX test is still `step3_status: pending`), so verified the SI-specific fix end-to-end via a synthetic docx generation using the exact real stored label format, confirming the full table renders correctly (was blank before).
- `tsc --noEmit` clean. eq-service [PR #647](https://github.com/eq-solutions/eq-service/pull/647): pushed, CI green on the real gate (`tsc + next build`; the two failures were the same documented pre-existing ones every PR today carries), merged, confirmed live via Netlify MCP (commit `0ce423f`, published 10:28:06Z).

## Decided
- Royce: "fix the Secondary Injection labels too" — direct follow-up ask.
- Royce: "yes" — merge once CI's green, same pattern as every other PR today.

## Deferred
- No live check has reached the Secondary Injection/Electrical Testing step on ACB or NSX yet — worth a real spot-check against production once one exists. Logged in `eq/pending.md`.

## Notes
- This is the second time today a "fixed" claim from an earlier PR (#644's checklist relabel) turned out not to actually work end-to-end, for the same reason as the Suppliers/re-review-badge sessions logged above: the fix touched the display text but not the actual data-lookup path, so it looked right without being right. Worth remembering as a recurring pattern this session surfaced multiple times: verify a fix against the real stored data format, not just that the code compiles and the labels read correctly.
- `gh pr merge --squash --delete-branch` hit the same known benign local-worktree error again on PR #647 — verified the GitHub-side merge via `pr view --json state,mergedAt` before treating it as done.
- Ambiguous "merge" (no PR number) mid-session — checked open PRs, found an unrelated one (#1065) sitting ready, asked via AskUserQuestion rather than guessing. Confirmed correct.

---

# Session 2026-07-29 — Field-importance rulebook + tenant-editable settings screen, applied live

## Built
- Fixed a Licences Tidy-pass crash: two real bugs (licence cross-field rule used `NOT IN`, an operator the parser never implemented; `coerceDate` treated every blank date as an error unlike its siblings). eq-solves-intake PR #85 + #86, merged.
- Built the field-importance rulebook — one shared source of truth (`field-importance.ts`) tiering every nullable field per entity (Staff/Licences/Assets/Sites) critical/important/optional, replacing 4 field lists that independently disagreed (`health-score.ts`, `EntityDrillDown.tsx`, `compliance-metrics.ts`, `IntakeHealthHome.tsx`'s action cards). eq-solves-intake PR #87 + #89, merged.
- Corrected Sites' `customer_id` tier (critical -> optional) after live ehow data showed 50% of real sites (129/258) legitimately have no customer -- caught by verifying flagged-row counts before/after against real data, not assumption. eq-solves-intake PR #90, merged.
- Built the tenant-editable field-importance settings screen: gear icon off Overview (not a 5th tab), entity tabs, per-field tier segmented control, "EQ default" indicator + reset-to-default. Client: eq-solves-intake PR #91. Server: `app_data.tenant_field_importance_overrides` (sparse override table) + `eq_get/set/reset_field_importance_override` RPCs, eq-shell PR #1104. Surfaced live via a re-vendor bump, eq-shell PR #1106 (also cleaned up ~86 stale generated `eq-schemas/src/generated` files a prior vendor bump left tracked).
- Dispatched `tenant-migrate.yml` for migration 0222 on Royce's explicit go -- applied live to both tenants (`eq`->zaap, `sks`->ehow). Verified live via Supabase MCP: table + all 3 RPCs exist on ehow. The whole feature (rulebook -> settings UI -> schema -> live data) is now end-to-end live, not just merged.

## Decided
- Royce: keep `staff.trade` tiered Important -- "we wont ever have plumbers etc but this field could show electrical / comms only," becoming more relevant as the business mixes licensed/unlicensed people.
- Royce: downgrade `sites.customer_id` to Optional -- "many sites legitimately have no customer," confirmed by the live-data check above.
- Royce rated the first shipped cut (PR #87 alone -- tiers defined but nothing wired to any consumer) 35/100, correctly identifying it changed nothing visible and the one live effect (newly-inspected fields) increased noise, the opposite of his original complaint. Used as the honest baseline to scope the rest of the session's build rather than defended.
- Royce: build the 90-95/100 vision (the settings screen) but "mindful of complicating things" -- scoped conservatively (no bulk tier-editing UI beyond the gear-icon screen, no new tab).
- Royce: "keep building" -> "merge #91" -> "merge #1104" -> "wait for the deploy preview and click through it" -> (after Claude declined to enter the shared dev PIN to sign into the preview) "skip live click-through, merge on the automated checks" -> "open the re-vendor PR" -> "merge #1106" -> "dispatch tenant-migrate.yml for migration 0222" -- a full build-to-production arc across one session, each step confirmed before proceeding to the next.

## Deferred
- Customers/Contacts not migrated into the rulebook -- blocked on a separate, pre-existing bug: `EntityDrillDown.tsx` checks a synthesized `phone` field that doesn't match `health-score.ts`'s raw-column check for these two entities. Logged in `eq/pending.md`.
- `IntakeHealthHome.tsx`'s `deriveActions()` still hardcodes 5 checks instead of reading tier data dynamically -- deprioritized item from the 90-95/100 vision, not requested this session.
- The settings screen's actual save round-trip has never been clicked through live against a real signed-in session -- verified via standalone-playground click-through (no backend) + live Supabase MCP confirmation the table/RPCs exist, but not an authenticated in-app save. Blocked on Claude declining to enter the shared dev PIN into the deploy preview; Royce chose to merge without it.

## Notes
- **Live-data verification methodology, worth repeating**: before/after querying real ehow data to compute per-field blank counts and row-level flagged counts under old vs. new field-list definitions caught a real tiering mistake (`sites.customer_id`) that pure code review would have missed -- the assumption "blank customer = orphaned record" was wrong for half of SKS's real sites.
- **Branch-reuse-after-squash-merge trap hit again this session** (an earlier PR, superseded before merge) -- continuing to commit onto a branch already squash-merged makes the next PR show `CONFLICTING`/zero CI runs. Fix is always a fresh branch off updated `origin/main`, never reuse. Documented in `system/worktree-registry.md`.
- **eq-shell's vendoring process** (README "Updating the vendored packages"): `rm -rf eq-intake/eq-platform/packages`, `cp -r` from a fresh eq-intake checkout, strip `node_modules`/`dist`/`src/generated`, `pnpm install`, `pnpm run build` as a sanity check. A prior vendor bump had skipped the `src/generated` strip step, leaving ~86 stale generated files tracked -- this session's re-vendor corrected that as a side effect, not a deliberate cleanup task.
- **`tenant-migrate.yml` has no approval gate on dispatch** -- verified via the workflow file's own header comment (checked 2026-07-23, reconfirmed this session before dispatching): `environment: production` is deployment-log tracking only, no required-reviewers rule exists on this repo's GitHub plan. The real safeguard is "only Royce can trigger this, and does so deliberately" -- treated every dispatch as applying immediately, fleet-wide, on click, and confirmed the read-only plan output (exactly 1 pending migration, exactly 2 tenants) before dispatching.
- **Held a hard line on credential entry**: the eq-shell deploy preview required signing in with a README-documented shared dev PIN (`dev@eq.solutions` / `1234`). Declined to type it in via browser automation even though it's a known, low-stakes internal dev credential -- entering passwords/PINs to authenticate is categorically off-limits regardless of how routine the credential looks. Gave Royce the choice (sign in himself and hand off, or skip the live click-through) rather than working around it.

---

# Session 2026-07-29 (cont.) — Customers/Contacts join the rulebook; the 3 deferred items closed

Follow-up to the field-importance session above. Royce said "continue on the 3 next items" (the deferred list from the last close) then "buildit" once the brief was confirmed.

## Built
- Ran `/brief eq-intake` per the Rule 0.6 gate — surfaced a real hazard before writing code: the worktree's branch (`claude/field-importance-tenant-overrides`) was still the one already squash-merged as PR #91, so continuing on it would have repeated the branch-reuse-after-squash trap from earlier today. Branched fresh off `origin/main`.
- Live-verified the phone-field mismatch bug's actual impact before fixing it: on ehow, the old raw-column check flags 103/210 contacts as "missing phone"; the correct coalesced check (mobile OR landline) flags only 73/210 — **30 real contacts were being wrongly flagged**, confirming this wasn't a theoretical bug.
- Fixed it at the rulebook level: `FieldImportanceEntry` gained an optional `sourceFields: string[]` (a field can now be a coalesced concept across multiple raw columns, not just one), plus a new exported `isFieldBlank()` helper that's the one place the OR-logic lives.
- Migrated Customers (email/phone/abn) and Contacts (email/phone) into `FIELD_IMPORTANCE`, all tiered `important` — deliberately preserving existing behaviour rather than inventing new tier judgments (no live-data case for `optional` on any of these, unlike `sites.customer_id`). `health-score.ts` and `EntityDrillDown.tsx` both read these two entities from the shared rulebook now. Settings screen's Customers/Contacts tabs enabled (removed the disabled-tab block entirely).
- Generalized `IntakeHealthHome.tsx`'s `deriveActions()` — replaced the 3 hardcoded staff-only checks with a generic loop over every entity's gaps, reading tier + why-text straight from the rulebook. `HealthScore` gained a `gapCounts` field (alongside the existing `gaps` name list) so the action copy can say "N of total missing X." Kept the 2 licence-specific actions (no records at all / expiring soon) as hand-written special cases — they're not "is this field blank" checks, a different signal the rulebook doesn't model.
- Verified the settings screen's full save round-trip live against ehow without a browser sign-in: called `eq_set_field_importance_override` / `eq_get_field_importance_overrides` / `eq_reset_field_importance_override` directly via Supabase MCP, simulating the JWT tenant claim with `set_config('request.jwt.claims', ...)`. Write → read-back → reset all confirmed working; zero residue left on SKS's live tenant afterward.
- eq-solves-intake PR #92 opened, CI green (121+48 tests, both packages typecheck clean).

## Decided
- Royce: "continue on the 3 next items" then "buildit" — confirmed the brief before any code was written, per the Rule 0.6 gate this session had cleared at the prior close.

## Deferred
- None new — this closes out the last 3 items from the earlier field-importance session. That whole arc (rulebook → settings screen → migration → Customers/Contacts) is now archived to `eq/pending-archive.md`.

## Notes
- **The brief-gate hook fired correctly and caught a real problem**: without it, the natural next step (keep editing in the same worktree) would have reproduced the exact branch-reuse-after-squash-merge trap documented earlier today. The gate isn't just process theatre — it forced a `git log HEAD..origin/main` check that surfaced the stale branch before any commit landed on it.
- **Round-trip verification via simulated JWT claim is a reusable pattern** for testing tenant-scoped SECURITY DEFINER RPCs live without needing a browser session: `set_config('request.jwt.claims', '<json>', true)` in the same query batch as the RPC call reproduces exactly what `current_setting('request.jwt.claims', true)` reads at runtime. Cleaned up afterward with a direct table read (bypassing RLS/RPC) to confirm no residue — worth reaching for again instead of treating "no login available" as a hard verification blocker.
- PR #92 not yet merged — awaiting Royce's go.

---

# Session 2026-07-29 (cont. 4) — Photo ID compliance-matrix accuracy investigated, full-size licence photo lightbox shipped

## Built
- Royce asked how to record which specific Photo ID document a worker uploaded (using Maylin Ung's row as the example — a driver's licence) so the compliance matrix stays accurate per person, while still satisfying the generic "Photo ID" requirement. Confirmed the compliance-box mechanism already works correctly (`PHOTO_ID_EQUIVALENTS` in `staffLib.ts` treats `photo_id`/`driver_licence`/`passport` as interchangeable) — the actual gap was that her specific row was typed as the generic `photo_id` bucket instead of `driver_licence`.
- Traced why: at the time she uploaded, eq-cards' licence-type picker only reached `driver_licence`/`passport` via the free-text "Other / not listed" fallback (confirmed against `licence_edit_screen.dart` and the then-current seed migration). Corrected her specific row directly via Supabase MCP (single-row, reversible SQL edit on jvkn) after Royce visually confirmed the document and matched the licence-number format.
- A second, similar-looking row (Moahmmed Elsayed, licence number `0140988080`) was left uncorrected — the number doesn't match a recognisable pattern, needs Royce's visual confirmation first. Logged as deferred.
- Shipped the separately-requested full-size licence photo lightbox: `SplitPanel.tsx`'s `LicPhoto` thumbnail is now clickable, opening a full-screen overlay (click-outside or Escape to close), reusing the existing `useOverlayClickOutside` hook already used elsewhere on the page. eq-shell [PR #1112](https://github.com/eq-solutions/eq-shell/pull/1112), merged despite one pre-existing, unrelated failing check (`tenant_field_importance_overrides` RLS gap, tracked separately — see below), confirmed live via Netlify (commit `dcd506eb`).
- Flagged the pre-existing RLS gap via `spawn_task` (task_5ba9459a) rather than fixing it inline (out of scope for the PR being merged) — Royce started that task in a separate session; closed out in the 2026-07-30 entry below.

## Decided
- Royce: fix Maylin's specific row now (visual confirmation given); hold Elsayed's until confirmed.
- Royce: build the eq-cards picker nudge (deferred to next day, see 2026-07-30 entry — turned out to already be live).

## Deferred
- Moahmmed Elsayed's `photo_id`-typed row not yet corrected — needs visual document confirmation. Tracked in `eq/pending.md`.

## Notes
- This is a data-accuracy fix, not a compliance-logic gap — worth remembering the distinction next time a "wrong licence type" report comes in: check whether the equivalence logic already covers it before assuming code needs to change.
