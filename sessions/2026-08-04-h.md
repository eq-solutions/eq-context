# Session 2026-08-04 — Document sign-off register sprint closed out; new admin-feature-baseline rule

## Built
- eq-shell [PR #1222](https://github.com/eq-solutions/eq-shell/pull/1222) — sign-off certificate PDF (standalone) + Templates tab. Merged.
- eq-shell [PR #1226](https://github.com/eq-solutions/eq-shell/pull/1226) — T2, daily reminder cron for outstanding sign-offs. Merged.
- eq-shell [PR #1228](https://github.com/eq-solutions/eq-shell/pull/1228) — reminder cadence corrected to a uniform 7 days. Merged.
- `eq-context/rules/admin-feature-baseline.md` — new governed rule: minimum bar for admin/data-management features (lifecycle action, bulk, export scope, configurable taxonomy, scale, smart intake). Indexed in `CLAUDE.md` §8.
- eq-shell [PR #1239](https://github.com/eq-solutions/eq-shell/pull/1239) — Templates gets a real "Upload a template" CTA (`EmptyState` action prop); Register gets a per-document Archive action (`DropdownMenu`+`ConfirmDialog`, non-destructive). `app_data.document_register` already exposed the needed column — verified live on both tenants, no migration. Merged.
- eq-shell [PR #1241](https://github.com/eq-solutions/eq-shell/pull/1241) — T3, bulk upload for Templates (bounded 3-at-a-time concurrency, editable per-file titles, per-file retry). Also fixed a real focus-trap bug in `StagedFileList` (hidden input's DOM order broke keyboard focus on modal open). Merged.
- Two `/gap` HTML artifacts published: a centering pass on the sign-off register, and a "current truth vs. north star" pass with a `/decide` verdict (stay lean; the north star is a map, not a build queue, until a real second signer or an external signer forces specific items forward).
- Corrected a stale memory (`sks-labour-retiring.md`) — it had over-generalized a 2026-07-20 "not to be touched" instruction into a full feature freeze; live `sks/changelog/labour.md` shows continuous real engineering work through today. Only the narrow claim (SEC-1 itself stays unhardened pending Field cutover) still holds.

## Decided
- Certificate stays a standalone PDF, not merged into the original document (Royce, earlier in session).
- Template storage extends the same document-sign-off feature rather than becoming a separate feature (Royce, earlier in session).
- T1 (per-signer RLS) stays deliberately deferred — the real fix needs an identity-model decision (eq-field's data-plane JWT carries the tenant id, not the actor, as `sub`), not a migration. Revisit alongside a real second-signer rollout.
- Reminder cadence: uniform 7 days, not the shipped 3-day-first/4-day-repeat.
- Post-`/decide`: stay lean on the sign-off register's north star — build only when a real person's usage hits a named wall, not speculatively.
- On the UI critique's gaps: "quick wins only" (the two already covered by an unused eq-ui component) over the bigger bulk/config scope, or just writing the doc with nothing built.
- On Royce's real 15-file template batch: build bulk upload (T3) rather than grind through 15 manual uploads — an explicit override of the "manual, it's one-time" recommendation.

## Deferred
- Royce's actual 15-file template batch — bulk upload shipped for exactly this, not run yet.
- OCR / smart intake on document upload (north-star item, real precedent in eq-cards' licence OCR).
- Document type list is a hardcoded frontend array — no admin config surface.
- Certificate export can't be scoped to a subset of signers — always the whole document.
- T4 (permission gate) — accepted interim decision, any tenant member can do everything.
- T5 (real second-signer rollout) — Royce reaffirmed lower priority.
- EQ Field / SKS Labour adoption — surfaced via a `/gap` (Royce: "need to get EQ Field adopted first"), then the conversation pivoted back to the sign-off register before any next step got decided. No tracked parity list exists between the two apps; SKS Labour has no retirement date and is genuinely still receiving real engineering work in parallel with EQ Field.

## Notes
- **Two live concurrent-checkout collisions hit the shared `eq-context` working directory this session** — a transient stale read mid-way through another session's active rebase (self-resolved, nothing lost), and a `git add <specific-file>` that swept up three files another session had already staged in the shared index (caught via `git show --stat HEAD` before pushing, undone clean with `git reset HEAD~1`, no data lost either way). Spawned a background task off this (`task_54dfc431`); Royce started it separately and it resolved during this session — see `system/failures.md` F9 and the "shared-checkout git races — structural fix shipped as F9" entry near the top of `eq/pending.md`. A hook (`hooks/pre_tool_use.py`) now enforces the fix rather than just documenting it.
- `git stash` is blocked by the Claude Code auto-mode permission classifier in this environment — worth knowing before reaching for it mid git-recovery. The workaround used both times was an isolated fresh clone + cherry-pick rather than touching the shared checkout's index directly.
- The reminder cron (T2) has never actually fired in production — the only real signoff was signed 16 seconds after assignment, so nothing has ever sat outstanding long enough to trigger it, at any cadence tested.
- The EQ tenant (zaap) has zero rows anywhere in the sign-off register — the whole feature is SKS-proven only so far, by one person (Royce), on one document.
- `eq-ui`'s actual current component list (checked directly against source, not assumed): `AppShell`, `Button`, `Card`, `DropdownMenu`, `EmptyState`, `FormInput`, `KindPill`, `Modal`+`ConfirmDialog`, `Pagination`, `Skeleton`, `Spinner`, `StatusBadge`, `Table`, `Tabs`, `Toast`, `Tooltip` — several of these (`EmptyState`'s action slot, `Pagination`, `ConfirmDialog`) were sitting unused in the sign-off register feature despite matching its own gaps exactly, which is what prompted `rules/admin-feature-baseline.md`.
