# Session 2026-09-13 — eq-solves-intake schema drift closed out; eq-shell JWT-secret root cause traced, fix drafted then lost to shared-checkout drift

## Built
- `eq-solves-intake`: reconciled `schemas/site.schema.json`'s `country` (added the real, wired `country-iso-alpha2` coerce hint) and `client_name` (removed a false `x-eq-deprecated` flag — verified live against ehow that the column is actively written by the current `eq_upsert_site` RPC) against eq-platform's already-accurate copy. `main` @ `8dccda0`, pushed.
- `eq-solves-intake`: swept all 34 schema files for the same drift pattern. 18 "eq-platform doesn't have this file" cases are deliberate canonical-subset curation, not drift. `asset.schema.json` was real drift — eq-platform's `x-eq-source-aliases` were a strict superset across ~17 properties. Reconciled root to match. `main` @ `9c6b542`, pushed.
- `eq-solves-intake`: deleted the stale `claude/fuzzy-match-reconcile` remote branch after confirming its real payload was already superseded by merged PRs #112/#113 (with main's version *better* — a perf fix the branch lacked), and its schema-file hunks were actively regressive.
- `eq-shell`: drafted a fix for the tenant JWT-secret gap (see Decided/Deferred below) — typechecked clean, all 27 existing unit tests passed. **Status at close: not present in the working tree** — see Notes.

## Decided
- Royce confirmed the site.schema.json brief before editing; confirmed both commits and both pushes.
- Royce confirmed deleting the stale branch after the supersession trace.
- Royce confirmed madagins is a real, active customer — the goal is getting their customer/site/contact data through Intake (his own import, or their self-serve) to evaluate it. This reframed the JWT-secret investigation from "interesting tangent" to "does this block the actual plan" — answer: no, the import/commit path is fully isolated from the bug.
- Ran `/decide` three times on the JWT-secret thread. First recommended per-tenant secrets (right instinct). Second, after reading `ARCHITECTURE-V2.md`, wrongly walked that back in favor of the doc's shared-secret design. Third, after checking Supabase's actual Management API docs, confirmed shared-secret isn't achievable at all — per-tenant was correct all along, for a different reason than first thought (not architectural taste, but the only thing the platform supports).
- Royce confirmed proceeding to draft the corrected per-tenant-secret code after the second `/decide` ("are we lost?") flagged the investigation had drifted well past the original ask.

## Deferred
- `eq-solves-intake` — none new; the one long-open deferred item from the 2026-09-09 close (eq-platform `site.schema.json` drift) is now closed, archived to `eq/pending-archive.md`.
- `eq-shell` — the JWT-secret fix itself, tracked as background task `task_b3a9c0f1`. Three sub-questions still open: (1) whether a tenant project's own secret can be fetched via the Management API at provisioning time, vs. dashboard-only; (2) whether zaap has the same gap (no Sentry signal either way); (3) actually setting madagins' own secret once (1) is answered. Full detail in `eq/pending/eq-shell.md`'s matching entry.

## Notes
- **The eq-shell draft vanished from the shared working tree between when it was written and this close** (a multi-day gap in wall-clock time within one continuous conversation). `C:\Projects\eq-shell` is a shared, non-isolated checkout with ~20 other concurrent sessions active at various points this session — the draft was never committed, and by close time `tenant-data-proxy.ts`/`mint-tenant-jwt.ts` were back to their pre-draft state. Most likely cause: unrelated concurrent activity in the same physical directory, not a deliberate revert. Lesson for next time: either commit a draft promptly even before final sign-off, or build it in an isolated worktree from the start — don't leave uncommitted changes sitting in a heavily-shared checkout across a long gap.
- Two background tasks were spawned and one superseded mid-session: `task_a01478d3` (fuzzy-match-reconcile branch — resolved directly, not left as a task), `task_2ab69cca` (JWT shared-secret fix — dismissed as wrongly-scoped once the Management API check disproved the premise; note it "was already started by the user" in a separate session at dismiss time, so that other session may be working from the stale framing — worth Royce checking which session that was), `task_b3a9c0f1` (the corrected replacement, still open).
- `eq-solves-intake`'s `eq-platform/packages/eq-schemas` is a deliberately curated canonical subset (shared cross-app entities only), not a full mirror of the repo's own `schemas/` — worth remembering before treating any future "file only exists in one location" finding as drift.
- This close required re-running `/brief` a second time mid-session (once for eq-solves-intake, once for eq-shell) because the gate is keyed to today's date + session id — a date rollover mid-session (2026-09-09 → 2026-09-10) invalidated the first flag.
