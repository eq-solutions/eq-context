# Session 2026-07-31 — Quote events now stamp app_source='ops' instead of the retired app name (continuation of 2026-07-30)

## Built
- eq-shell [PR #1132](https://github.com/eq-solutions/eq-shell/pull/1132) merged (squash `8c9dd4ee`, Royce's "merge it once CI's green" go): follow-up to `#1129` (which only corrected the Suite Activity tab's *display* label from "EQ Quotes" to "EQ Ops"). New tenant-migration `0227` changes `eq_update_quote_status` to write `app_source='ops'` for quote.sent/accepted/declined going forward — historical rows keep `'quotes'`, no backfill. Current live definition (`0214`) confirmed matching on both ehow and zaap before writing the migration, and the DDL was replay-tested in a rolled-back transaction against ehow first (confirmed it reverted cleanly). Paired frontend fix so the App filter and label keep working across both old and new rows: `tenant-events.ts` now maps both `quotes` and `ops` to "EQ Ops" and matches both when the App filter is set to "EQ Ops"; `AdminAuditPage.tsx`'s dropdown option value changed from `"quotes"` to `"ops"`.
- Migration `0227` dispatched (Royce: "dispatch the migration once it's merged"): read-only plan confirmed first — exactly 1 pending migration across exactly 2 tenants (`eq`→zaap, `sks`→ehow), fleet otherwise fully caught up. `gh workflow run tenant-migrate.yml` (no slug filter = whole fleet) ran green. Verified live via `pg_get_functiondef` on both ehow and zaap that `app_source` is now `'ops'`.

## Decided
- Royce: "merge it once CI's green" — merged once all required checks passed (Netlify deploy preview treated as informational, matching established precedent).
- Royce: "dispatch the migration once it's merged" — explicit forward go for the tenant-migration apply, executed once the plan job confirmed exactly the expected single pending migration across both tenants.

## Deferred
- None new. This closes out the "EQ Quotes doesn't exist?" thread from 2026-07-30 end to end — display label, filtering, and now the underlying data all correct.

## Notes
- This session spans a midnight rollover (started 2026-07-30, most of the migration/dispatch work landed 2026-07-31 by wall clock) — the brief-gate flag from the prior day stopped matching partway through and had to be re-written for the new date before any further `eq-context` edit would go through. Worth knowing for any long-running session: the per-session brief flag is date-stamped, not just session-stamped, so a session that crosses midnight needs a fresh flag mid-session even though nothing else about the session state changed.
- Same `eq-context/system/worktree-registry.md` collision pattern as 2026-07-30 recurred here too — an edit sat uncommitted in the shared working tree and needed a second pass to land, this time caught immediately (within the same turn) rather than a turn later. The convention (commit + push right after any registry edit, no delay) held up once actually followed.
- The "Apply to all tenants" job in `tenant-migrate.yml` genuinely has no approval gate (documented in the workflow file itself, `environment: production` is deployment-log tracking only) — dispatching it is an immediate, fleet-wide, irreversible-by-undo action the moment `gh workflow run` is called. Treated it accordingly: read-only plan checked first, DDL replay-tested in a rolled-back transaction before ever touching the real dispatch.

---

# Session 2026-07-31 — eq-shell mobile chrome: dropped redundant top bar on Field/Service, verified Ops-tab gating already live

## Built
- eq-shell [PR #1133](https://github.com/eq-solutions/eq-shell/pull/1133) merged (squash `4266310a`, Royce's "merge" go): removed the "Apps ←" mobile top bar from Field/Service (adapted iframe modules) since Shell's persistent bottom tab bar already provides Home navigation there, making the top bar's only real behavior redundant. `MobileTabBar.tsx` now conditionally skips the top bar (+ its account/admin sheets) for adapted modules only; `HubLayout.tsx` mirrors the same `ADAPTED_MODULES` check (newly exported) to drop the 48px top padding; `App.css` adds `.eq-hub__iframe-content--adapted`. Ops/Comms (non-adapted, no bottom bar of their own) keep the top bar unchanged. `pnpm run build:packages && tsc -b` clean, 274/274 tests pass, lint clean.

## Decided
- Royce: "Remove entirely" (chosen via AskUserQuestion over "slim it down" or "leave as-is") — the top bar should be dropped outright for the iframe-module case, not just the "Apps" button trimmed.
- Royce: "merge" — merged once all CI checks + deploy preview were green. Full click-through deferred to Royce (logging in requires entering a PIN, off-limits per the credential rule even for EQ's own product) — same wall every other pending live-click-through in the digest hits.

## Deferred
- **Click-through confirmation on the live mobile view** — Royce to sign in on a ~375px-wide viewport, open Field/Service (top bar should be gone, bottom tab bar only) and Ops/Comms (top bar unchanged), and confirm Settings/Sign-out are still reachable via the Home tab from Field/Service. _(added 2026-07-31)_
- **`shell.md`/`eq-shell.md` changelog duplication** — `shell.md` stops 2026-06-30 and is stale; `eq-shell.md` is the one actually kept current. Flagged inline in the file's own frontmatter already, not re-flagged as new — just noting it wasn't consolidated this session either. _(pre-existing, not added new)_

## Notes
- Two items originated from Royce's 2026-07-30 screenshot review of EQ Field/Ops — both turned out to be eq-shell chrome (the outer app frame around the embedded Field/Service/Ops iframes), not eq-field itself, despite the session starting in the eq-field repo. Confirmed via source read (no matching markup in eq-field's `index.html`) before switching repos.
- Second item ("only managers... see Ops") needed no fix — `useCan('ops.view_rates')` gating on the mobile Ops tab was already merged same-day via [PR #1109](https://github.com/eq-solutions/eq-shell/pull/1109), verified live on `origin/main` before doing anything else. Saved a redundant rebuild.
- Checked for a regression the top-bar removal could plausibly cause: `eq-field`'s own `styles/mobile.css` (`.shell-mode .page`) reserves top/bottom padding when embedded in Shell. Read it directly — the top padding is for Field's *own* internal section-nav strip, not Shell's top bar, and the bottom padding (reserved for Shell's persistent bottom app bar) is already coordinated against the same `ADAPTED_MODULES` set via a comment that names it explicitly (`mobile.css:736`, "Pairs with eq-shell MobileTabBar ADAPTED_MODULES"). No coupling issue — confirmed rather than assumed.
- `gh pr merge --delete-branch` failed its local branch-delete step (same recurring cause noted in `worktree-registry.md` history: another worktree — `auto-import-toggles-956005` — already had `main` checked out in this shared eq-shell checkout). Remote merge succeeded regardless; deleted the remote branch manually with `git push origin --delete`. Local root checkout was left on the now-merged feature branch rather than force-switching — harmless, and switching would have required unassigned worktree surgery out of scope for this session.
- SEC-1 (P0, live PII leak on sks-nsw-labour) and SEC-9 (P0, confirmed jvkn key exposure) remain the two highest-value open items in the suite, surfaced when Royce asked "anything else worth doing" — both already fully diagnosed with runbooks/decisions on file in `ops/security-register.md`, blocked purely on Royce's own action (a decommission-date call; picking a rotation window and running Step 1 in the Supabase dashboard himself). No new engineering scope there — not built or touched this session.

---

# Session 2026-07-30/31 — EQ Field screenshot review: 5 PRs shipped from Royce's own PDF review + Q&A pass

## Built
- eq-field [PR #572](https://github.com/eq-solutions/eq-field/pull/572) merged (v3.5.382): My Schedule's team-filter pill row hidden unconditionally (confirmed it had zero function on that page — the person dropdown lists everyone regardless of the filter); Job Numbers' BETA→Manage nav promotion (already live for SKS since v3.5.95) ungated for all tenants; Pipeline nav hidden outright on mobile (≤768px) regardless of role; Leave's CC List/Archive Resolved/Show Archived buttons were visible-but-blocked for non-managers (missing CSS rule for bare `.edit-only` on `.eqf-actbtn`) — added the missing rule.
- eq-field [PR #576](https://github.com/eq-solutions/eq-field/pull/576) merged (v3.5.383): root-caused and fixed a real staff-name-resolution bug — `loadCanonicalStaffMap()`'s fetch filtered `on_roster=eq.true` (added v3.5.151 to hide leavers from the roster grid), but that excluded anyone toggled off-roster from NAME RESOLUTION entirely, not just the display list. Affected 21 of 98 active SKS staff. Traced from a flagged-but-uninvestigated finding in #572 (a roster warning for "Tom Ivicevic," who turned out to be an active, non-removed person).
- eq-field [PR #577](https://github.com/eq-solutions/eq-field/pull/577) merged (v3.5.384): closed a real security finding found while investigating the CC List button — `app_config`'s SELECT policy is `USING(true)` for every role including anon (needed for the pre-auth PIN-code read), and `leave_cc_list` was piggybacking on that same wide-open table, making the manager-email mailing list anon-readable via direct REST regardless of the UI gate. Moved to a new table (`public.leave_cc_recipients`) with real RLS (authenticated + org_id + JWT tenant claim, no anon grant, mirrors the `acknowledgments` pattern), one-time backfill of the live list. Per Royce's follow-up answer on how the migration should work, also dropped free-text email entry — CC membership is now a pure multi-select over canonical managers only.
- eq-field [PR #578](https://github.com/eq-solutions/eq-field/pull/578) merged (v3.5.385): new "Roster Overview" summary card at the top of the Roster page — today's coverage %, sites with nobody rostered today, a tap-a-site-to-see-crew list, and the existing "Management Out This Week" banner folded in. The existing scrollable grid/mobile crew-strip views are unchanged, just no longer the landing view.
- eq-field [PR #579](https://github.com/eq-solutions/eq-field/pull/579) merged (v3.5.386): Leave toolbar's 3 manager-only buttons (CC List/Archive Resolved/Show Archived) collapsed into one "⋯" overflow menu; Print and +New Request (neither manager-only) stay prominent.
- Spawned `task_e8b60d22` for the two items that turned out to be eq-shell's chrome, not eq-field's (top bar, Ops-tab visibility) — completed same day in a separate session, see the two entries above this one.

## Decided
- Royce: "Do both in parallel" — investigate the security/data findings and ship the quick eq-field UI fixes in the same pass, rather than sequencing them.
- Royce, on the `app_config` finding: new table with proper RLS (over: scoped policy on the existing table / accept the risk / a bigger no-anon-reads redesign).
- Royce, on the Tom Ivicevic finding: investigate now, not deferred.
- Royce, on the eq-shell items: spawn a flagged follow-up task rather than leave a dangling note or drop it.
- Royce, on the CC list's actual data model (correcting my own narrower first-draft plan): "that list needs to be the canonical tenant staff list - 'managers' and then we need to be able to choose who is on the mailing list" — then, on a direct follow-up naming the two concrete UI paths already live: drop free-text entirely, canonical-managers-only.
- Royce, on the roster dashboard scope: "Build all 4 as described" (coverage %, zero-crew sites, Management Out promoted, tap-a-site drill-down) over three narrower options.
- Royce, on the Leave screen: move admin actions into an overflow menu (my recommendation, which I then had to correct against the actual permission model — Print isn't manager-only, so it stayed prominent rather than moving with the other three as I'd first described).

## Deferred
- **No live click-through on any of the 5 merged PRs** — the build sandbox has no Netlify Functions runtime, so full app boot isn't possible locally; every PR was verified instead via the real test suite + real browser execution of the actual edited functions against stubbed data (not CSS-only guesses, not reconstructions). Worth a manual pass on `field.eq.solutions`, especially the Roster Overview card on a real phone. _(logged in `sks/pending.md` + `eq/pending.md`)_
- **EQ Wallet — Licences critique** — gave direct feedback (expiring-soon count badge, lock-icon legend) but didn't build anything; Royce hasn't said whether he wants it built.
- **Timesheets mobile-entry strategy** — Royce asked whether mobile timesheet entry is even the right pattern; recommended checking PostHog's `timesheet_saved` device breakdown before deciding anything, not done this session.

## Notes
- This session ran the full loop end-to-end multiple times: screenshot review → triage → investigate → recommend → Royce decides → build → real (non-mock) verification → merge — five times in one sitting, each shipped and merged before starting the next rather than batched into one giant PR.
- Two genuine mid-build corrections, both caught before shipping, not after: (1) my own recommendation text for the Leave toolbar named Print as moving into the overflow menu — checked the actual `.edit-only` classes first and found Print isn't manager-gated, so it stayed prominent; (2) my first-draft CC-list migration plan assumed a relocated-but-otherwise-unchanged email array — Royce's actual answer described a different shape (canonical-managers-only), caught by asking a direct follow-up rather than guessing which of two live UI paths (the existing quick-add chips vs. the free-text box) he meant to keep.
- Live DB work this session was read-mostly plus one applied migration (`20260731_leave_cc_recipients.sql`, applied to ehow via Supabase MCP) — no destructive changes, no data loss, verified post-apply via direct queries (backfilled row resolves correctly, zero anon grant exists on the new table) rather than trusting the migration's stated intent.

---

# Session 2026-07-31 — eq-shell re-vendored from eq-solves-intake: merge-error display + duplicate-record archive

## Built
- Copied the two upstream fixes from eq-solves-intake `main` (PR #94: `DuplicateMergePanel` was swallowing `eq_site_merge_execute` errors with no on-screen feedback; PR #95: new `eq_archive_duplicate_record` RPC + Archive button on the Remediation Queue's "other duplicate flags" list, migration already applied live to ehow) into eq-shell's vendored `eq-intake/eq-platform/packages/` copy — 5 files, sourced individually via `git show origin/main:<path>` rather than a bulk copy, to keep the diff exactly matching upstream with no incidental CRLF churn.
- Full local gate green (install, `build:packages`, `tsc -b`, `check:css`, `check:perms`, tests) before opening the PR; remote CI green before merging. eq-shell [PR #1130](https://github.com/eq-solutions/eq-shell/pull/1130) merged (squash `ea42a65`), same pattern as the two prior re-vendors this month (#93→#1127, #91/#1104/#1106).
- Verified live: Netlify's production deploy for core.eq.solutions (`6a6b2aafe2d7be000876487c`, `ready`, published 10:50:15Z) builds from `76fd6be1` (PR #1131), one commit past the `ea42a65` merge — so the vendored fix ships in the same deploy. Site loads correctly to the sign-in screen.

## Decided
- Royce: "merge #1130 once CI is green" — merged once all checks passed.
- Royce: "check core.eq.solutions once deployed" — confirmed via Netlify deploy state + `git log` (branch-tip build includes both merged PRs), plus a direct page load. Did not attempt to log in and click-test the actual UI change — that requires a PIN, which falls under the hard rule against entering credentials on the user's behalf, same wall as every other pending live-click-through this week.

## Deferred
- **Click-through on the two actual UI changes** — trigger a failed site merge in the Duplicate Sites panel (error should now show) and Archive a duplicate flag from the Remediation Queue (record should go inactive and drop off the list). Neither has been interactively tested; static verification (build/typecheck/tests + a load of the sign-in page) is as far as this session could go. _(added 2026-07-31, needs Royce)_

## Notes
- `gh pr merge` hit the same shared-checkout collision documented for PR #984 and others (`'main' is already used by worktree at ...` from a concurrent session's worktree) — the merge itself succeeded via GitHub's API regardless; confirmed via `gh pr view --json state,mergedAt,mergeCommit` rather than trusting the CLI's non-zero exit. Cleaned up manually: remote branch deleted, local worktree removed.
- Worktree registration hit the brief-gate hook on the first attempt (blocked before `/brief eq-shell` had been run this session) — the blocked edit silently didn't apply, so the retry needed a fresh `Grep` check before re-inserting the row rather than trusting the first attempt had landed.

---

# Session 2026-07-31 — F6 vs F7: git-merge NUL corruption confirmed as a distinct, unguarded vector

Royce asked to investigate digest.md's flagged "possible F6 recurrence" (`sessions/2026-07-28.md`, the `scripts/sites.js` NUL-byte corruption during a git stash pop/merge round-trip) and decide whether it warranted its own ledger entry.

## Built
- Read `hooks/pre_tool_use.py` and `system/lessons.md` directly rather than trusting the digest flag — confirmed F6's hook and lesson are both scoped to Bash `>>` append only; neither has any visibility into git operations. The 2026-07-28 incident came from a `git stash pop`/merge round-trip, a mechanism the F6 guard structurally cannot see.
- Added `F7` to `system/failures.md` — rung 0 (honest "no guard yet"), target rung 4, proposing a git `post-merge`/`post-checkout` NUL-byte-scan hook (mirrors F2/F6's fail-closed posture). Not built — just logged, per the ratchet's "propose only" convention for a brand-new vector (same pattern as F4/F5).
- Added `sessions/2026-07-28.md` to F6's `confirmed_in` list so the digest recurrence scanner stops re-flagging that session against F6's already-closed rung-4 entry going forward (same fix already applied for F1's own self-confirmation false-positive).
- Committed directly to `eq-context` main (`fa83216`) — substrate-only doc edit, no product code touched.

## Decided
- Royce, via AskUserQuestion: track it as its own ledger entry (F7) rather than folding into F6 or leaving it untracked, after being walked through what each option meant in plain terms.

## Deferred
- **The actual F7 guard doesn't exist yet** — a git-level NUL-byte scan hook is proposed but not built. Parked at rung 0 until a second occurrence or Royce prioritizes it. Logged in `ops/pending.md`.

## Notes
- This was a "surface the analysis, don't silently edit" case per the ledger's own stated posture ("recurrences is a human call") — walked Royce through the mechanism difference and three concrete options (new entry / fold into F6 / don't track) before touching the file.

---

# Session 2026-07-31 (cont.) — F7 guard built: PR #123 merged, plus a correction to the prior entry

Royce asked to build the F7 guard. Before writing code, re-checked the premise from the previous entry and found it was wrong — worth a self-correction rather than building on a false floor.

## Built
- Found that `hooks/pre_tool_use.py` already had a blanket block on git write verbs (merge/stash/add/commit/push/etc.) in-sandbox, live since 2026-07-12 — it should have stopped the 2026-07-28 corruption outright. The previous session's ledger entry claiming "no guard exists" was wrong; corrected it in `system/failures.md` rather than quietly building around it.
- Also found `hooks/README.md` claiming `guard.js` is "the active write-guard" for this failure class on the Beelink — grepped `guard.js` directly and confirmed it has zero NUL-byte/truncation logic. Flagged as an open gap in the README rather than left standing.
- Walked Royce through three options (harden the existing block / investigate the sandbox-wiring gap first / build the originally-proposed standalone hook anyway) via AskUserQuestion — he chose hardening.
- Shipped `hooks/pre_tool_use.py` changes: widened tool matching to `Bash` + `PowerShell` throughout (was `Bash`-only); added an independent, NOT sandbox-gated NUL-byte scan of the working tree ahead of any git verb, on any platform — blocks fail-closed if the tree is already corrupted regardless of the sandbox flag.
- Added 6 new adversarial test cases (2 proving the widened PowerShell match, 4 proving the scan fires independent of `in_sandbox()`) — hit and fixed a Windows-specific test flake along the way (`shutil.rmtree` silently leaves `.git` read-only files behind without an `onerror` handler that clears the attribute first). Full suite: 51/51.
- Updated `system/failures.md`'s F7 entry (rung 0 → 4, corrected guard/note fields) and `ops/pending.md` (ticked the build, added the still-open "why didn't the pre-existing block fire" item) — all in the same PR as the code, not a separate docs pass.
- Shipped via PR (not direct push, since this is enforcement code): eq-context [PR #123](https://github.com/eq-solutions/eq-context/pull/123), merged (squash `4963cbc`). CI green on everything relevant (adversarial suite, honesty, health, ratchet, frontmatter, line-endings); one unrelated pre-existing failure (`index-drift`, a stale `system/incident-claims.md` indexing gap, already failing on `main` since 2026-07-30) — confirmed via `main`'s own recent run history before treating it as out of scope.

## Decided
- Royce: harden the existing block (widen matching + add the independent scan) over investigating the sandbox-wiring gap first, or building the originally-proposed standalone post-merge hook.
- Royce: squash-merge PR #123 once adversarial suite + all relevant checks were green.

## Deferred
- **Why the pre-existing git-verb block didn't fire on 2026-07-28** — still open. Two live candidates: the Cowork sandbox session that ran that day never had `hooks/pre_tool_use.py` wired into its own settings.json, or the git command reached it through a tool-name path this hook didn't match (now narrowed by widening to Bash+PowerShell, but not proven as the actual cause). Can't inspect that sandbox's settings from this machine — needs Royce's input or a live test next time a Cowork session is available. Logged in `ops/pending.md`.

## Notes
- Real self-correction, not just a new finding: the previous session's F7 ledger entry asserted "F6's guard has no visibility into git operations" without having read the git-verb block further down the same file. Caught it by reading the whole hook file line-by-line before writing new code, not by trusting the earlier summary — worth remembering as a general habit when building on top of a same-session finding, not just a prior session's.

---

# Session 2026-07-31 (cont.) — iOS spinner fix, then two more real bugs from Royce's own phone screenshots

Continuation after the prior close. Royce asked "do animated spinners work on iOS?", then reported a live contradiction ("spinner definitely isn't working on iOS and hasn't... works fine on pc and android") and a separate top-bar alignment complaint, both from his own phone. Root-caused and fixed the spinner first; the alignment complaint turned out to need a screenshot to pin down, which — once sent — also surfaced a second, unrelated piece of direct product feedback on the Roster Overview card shipped the day before.

## Built
- eq-field [PR #580](https://github.com/eq-solutions/eq-field/pull/580) merged (v3.5.387): root-caused the iOS-only spinner failure to a known WebKit bug — an already-declared `@keyframes` animation doesn't reliably restart when its element goes from `display:none` to visible via a bare class toggle (Chrome recomputes it correctly regardless, which is exactly the "works on PC/Android, not iOS" split reported). Fixed by forcing a reflow on each spinner bar (`animation:none` → read `offsetHeight` → restore) every time `showLoadingOverlay()` shows the overlay.
- eq-field [PR #581](https://github.com/eq-solutions/eq-field/pull/581) merged (v3.5.388): two fixes from Royce's own iPhone screenshots. (1) The "EQ FIELD" home eyebrow label was rendering clipped under Shell's fixed top strip on `core.eq.solutions` mobile — root cause was a CSS specificity bug, not a layout oversight: `home.css`'s `#page-home` ID selector was silently beating `mobile.css`'s `.shell-mode .page` top-clearance rule regardless of source order (verified live with a throwaway browser specificity test, not reasoned on paper). Added a scoped `.shell-mode #page-home` override. (2) Roster Overview's "sites with no one rostered today" panel — built the day before per Royce's own "build all 4" go — removed outright per his direct correction on seeing it live: "we dont need to show what sites arent being worked at - this view is meant to be an easy way to see where people are working." The "Sites today — tap to see crew" list (where people ARE working) is unchanged.
- Investigated eq-shell PR #1134 ("workers-canonical-sync attributes real actors") after Royce asked "merge once green" without specifying which PR — traced it to a different, unrelated background session (not `task_e8b60d22`, which had already completed and merged as #1133 before this). Recommended NOT auto-merging it: wrong repo for this session, CI was still in progress (not actually green), and its own PR body has an unchecked live-verification item. Left for Royce or that session to close out.

## Decided
- Royce, via screenshots and two short follow-up messages, confirmed the iOS spinner symptom and gave the exact "PC/Android fine, iOS never worked" contrast that pinned the root cause.
- Royce, via AskUserQuestion: the "EQ FIELD... slightly behind" complaint was on `core.eq.solutions`, mobile, iOS — sent a screenshot on request, which is what actually located the bug (grepping eq-field's own markup first had already ruled out eq-field owning any literal "EQ FIELD" string, correctly pointing at Shell-embedded chrome before the screenshot confirmed it was Field's own `home.js` eyebrow label, clipped by Shell's strip).
- Royce, unprompted in the same screenshot batch: drop the "sites with no one rostered" panel — direct correction of yesterday's own build, not something flagged as a question.
- Royce: "merge once green" (ambiguous — no PR was open at the time). Asked which PR he meant rather than guessing; his "what do you think?" put the call back to me, and the recommendation was not to merge #1134 blind.

## Deferred
- **Live click-through of v3.5.387/388 on Royce's actual phone** — not yet confirmed, same gap as every other mobile fix this week (sandbox has no path to a real Shell-embedded iOS session). _(added 2026-07-31)_
- **eq-shell PR #1134** — left open for Royce or its originating session; not something this session should auto-merge (wrong repo, CI incomplete, unfinished test plan in the PR's own body). _(added 2026-07-31)_

## Notes
- The top-bar alignment complaint is a good example of asking before guessing paying off: eq-field's own markup genuinely has no standalone "EQ FIELD" string anywhere (checked first), which correctly pointed at Shell before assuming — but the actual bug turned out to be Field's own `home.js`/`home.css`, just rendered inside Shell's iframe. A screenshot was the only way to tell the difference; asking for one rather than guessing avoided chasing the wrong repo a second time (the same class of mistake the earlier top-bar/Ops-tab task had already been careful about).
- Two of this session's fixes came from Royce actually looking at the app on his phone rather than a screenshot-review document — a live click-through catches things static review and stubbed-data testing can't (this sandbox still can't reproduce a Shell-embedded iOS session end-to-end).

---

# Session 2026-07-31 (cont.) — EQ Ops quote-status sync fixed for all 5 stages, new Target period badge

Royce reported (screenshot, quote SKS-17503) that changing a quote's status on the detail panel wasn't reliably moving the job into the right status. Mid-turn, also asked for ideas on managing the growing "Open" list, and separately asked whether future-dated/budget quotes (screenshot, SKS-17480, a 2027 budget submitted in 2026) should get some kind of flag without adding friction to archiving.

## Built
- Root-caused the status-sync bug: `savePipelineStatus()` only called `syncJobToCanonical()` for 2 of the 5 pipeline stages (`won-job-created`, `invoiced`) — `active`/`in-progress` and `complete` changes never reached the job record that Field/Service read. Replaced the two-case special-casing with a `JOB_STAGE_CANONICAL_STATUS` lookup covering all 4 stages that have a job (Open has none by definition), so every stage change now syncs.
- Built the "Target period" feature per Royce's answer to a clarifying question (manual passive month/year, not auto-detection from dates — the SKS-17480 example's `created_at` was 2026, which would've defeated any date-based heuristic): new optional month/year field on a quote, save/clear, zero validation friction; a quiet "Targeting Jan 2027"-style badge shown on the detail panel and the pipeline board card when set.
- New tenant-migration `0228`: nullable `target_period` date column on `app_data.quote`, new `eq_set_target_period` RPC (same tenant-scoped/audit-logged pattern as the existing field-update RPCs), both read RPCs (`eq_get_quote_detail`, `eq_list_quotes`) re-cut to return the new column (`DROP FUNCTION` + `CREATE`, required since it changes a `RETURNS TABLE` shape).
- `tsc -p tsconfig.app.json --noEmit` (not `tsc -p . --noEmit`, which silently checks nothing — root `tsconfig.json` has `"files": []`) caught one real error from the new field (a duplicate-quote stub object missing `target_period`); fixed. Confirmed remaining tsc errors are pre-existing in the vendored `eq-intake` packages via a `git stash` before/after comparison, not caused by this change.
- eq-shell [PR #1136](https://github.com/eq-solutions/eq-shell/pull/1136), merged (squash `85a930c5`). Migration `0228` dispatched fleet-wide (`gh workflow run tenant-migrate.yml` on this branch, no slug filter) and confirmed live via direct query against ehow (`target_period` column + `eq_set_target_period` both present) before merging to main.

## Decided
- Royce: "Just the passive future-dated badge for now" — scoped explicitly to the badge only, not the archive-friction reminder idea it grew out of.
- Royce, via AskUserQuestion (manual month/year vs. auto-scan vs. both): "Maybe a passive month / year (this is an edge case but as we grow it'll happen more and more)."
- Royce: "dispatch tenant-migrate.yml for this branch" then "merge PR 1136" — both explicit, executed in that order (migration applied and verified live before the merge, not after).

## Deferred
- **No build for the long "Open" list / no drag-and-drop from the bottom** — discussed only as ideas (lean on the existing board view which already supports drag/drop within view, add sort/filter to the flat list) since Royce didn't approve building it, just asked to investigate and suggest. Logged in `eq/pending.md`.
- **Live click-through** — Royce to change a quote through all 5 stages and confirm the job follows each time; set a Target period and confirm the badge on both the detail panel and board card. _(added 2026-07-31)_

## Notes
- Caught my own design flaw before building anything: my first instinct for "future-dated" was to diff `created_at`'s year against the current year, but the actual screenshot Royce sent (SKS-17480) was created in 2026 for a 2027 budget — same year as "now," so that heuristic would never have fired on the exact example shown. Surfaced this to Royce rather than building it, which led to the AskUserQuestion and the correct (manual, passive) design.
- `tenant-migrate.yml`'s apply job has no approval gate (documented in the workflow file) — dispatching it applies DDL to both live tenant planes immediately on click. Verified the plan/inputs and confirmed the column existed live on ehow before proceeding to merge, same discipline as the 2026-07-31 `0227` dispatch earlier this file.

---

# Session 2026-07-31 (cont.) — 4-part Intake UX fix from Royce's live screenshot review

Royce tested the Review Queue / Tidy / Dupes screens live on core.eq.solutions after the earlier re-vendor (#1130) shipped, and sent four screenshots: no way to see/edit the trades list, a site merge failing with a permission error, the Data Gaps table showing bare unhelpful labels, and the Contacts Dupes tab having no action at all. Investigated all four against the real code and, for the merge failure, the live database, before proposing anything.

## Built
- **Root-caused the merge failure as a real bug, not an access problem.** `eq_site_merge_execute`/`eq_site_advisory_flag_pair` gated on `service.tenant_members` by raw `auth.uid()` — live-checked ehow and confirmed Royce genuinely holds an active `manager` row there, so the RPC's own check was wrong. Every other manager-gated RPC in the database (`eq_get_quote_detail`, `eq_list_quotes`, `eq_set_job_number`, `eq_list_suppliers`, `eq_list_contacts_for_customer/site`, `eq__assert_pricing_role` — confirmed via `pg_get_functiondef`) reads the role off the session JWT's `app_metadata.eq_role` instead, per the suite's own identity model (`IDENTITY-MODEL.md` #10). New migration `0228_site_merge_manager_gate_fix.sql` (`CREATE OR REPLACE FUNCTION` only) fixes both to match. eq-shell [PR #1137](https://github.com/eq-solutions/eq-shell/pull/1137), merged.
- **Data gaps table now shows real context** — the Tidy tab's "Record" column previously showed a bare label ("Accounts", "Rafael") with no way to tell two same-labelled rows apart. Joined `GapItem.row_id` back against the already-fetched full row locally in `EntityDrillDown.tsx` to show email/phone/company as a subline — no `GapItem`/tidy-pass schema change, no new fetch.
- **Contacts/Staff Dupes tab gains an Archive button** — previously read-only (Sites already had a full Flag/Preview/Confirm merge flow). Reuses `archiveDuplicateRecord`/`isArchivableDuplicate` from the earlier #95. eq-solves-intake [PR #96](https://github.com/eq-solutions/eq-solves-intake/pull/96), merged, re-vendored via eq-shell [PR #1140](https://github.com/eq-solutions/eq-shell/pull/1140).
- **New Trades settings screen + persisted "Not a duplicate" dismiss.** New per-tenant tables `app_data.tenant_trades` (sparse additions over EQ's code-level default list, add-only — a tenant can't hide a default, only remove what they added) and `app_data.eq_duplicate_dismissals` (group-level dismiss, not pairwise like Sites — dismiss never merges anything so no survivor/loser concept needed), both modeled on `tenant_field_importance_overrides` (0222) with RLS + `tenant_iso` policy included from the start this time (0222 needed a follow-up migration, 0224, once `check-tenant-drift.mjs` caught the missing RLS). Migration `0229`, eq-shell [PR #1138](https://github.com/eq-solutions/eq-shell/pull/1138), merged. New `getFieldSuggestedValues()` in `@eq/intake` reads `staff.schema.json`'s `x-eq-suggested-values` directly, replacing `RemediationQueue.tsx`'s hardcoded copy. UI wiring: eq-solves-intake [PR #97](https://github.com/eq-solutions/eq-solves-intake/pull/97), merged, re-vendored via eq-shell [PR #1142](https://github.com/eq-solutions/eq-shell/pull/1142).
- **Found and fixed a real correctness bug while wiring the dismiss feature**: `EntityDrillDown`'s `duplicateRows` used each row's own *raw* field value for `_dupeKey` instead of the shared *normalized* group value — two differently-cased members of the same group (e.g. `John@x.com` / `john@x.com`) could silently disagree on their own group's identity. This also affects Sites' merge-candidate grouping (`siteMergeCandidates`), not just the new dismiss feature — fixed at the source (`duplicateRows` itself) so both benefit.
- **Migrations 0228/0229 dispatched fleet-wide** (`tenant-migrate.yml`, Royce's explicit "dispatch both now" go, chosen via AskUserQuestion over waiting) — both applied cleanly on ehow + zaap (2 applied, 239 skipped, each). Live-verified before the dependent UI PR (#1142) was even opened: `eq_site_merge_execute`/`eq_site_advisory_flag_pair` confirmed on the JWT `eq_role` gate (checked with SQL comments stripped first — the migration's own explanatory comment mentions the old check by name, which would otherwise false-positive a naive substring search), RLS + policy confirmed enabled on both new tables via `pg_policies`/`relrowsecurity`.
- Final production deploy confirmed live via Netlify (`core.eq.solutions`, commit `64ec4248`, state `ready`) and a direct page load.

## Decided
- Royce, via AskUserQuestion on scope: build the Trades settings screen now (not just fix the drift/leave it); persist "not a duplicate" as a real record like Sites (not session-only, not skip-and-ship-Archive-only); investigate the merge-permission error now (not defer it).
- Royce: "dispatch both now" — explicit go to apply both merged-but-undispatched migrations (0228, 0229) to live tenants in one dispatch, before the dependent UI PR was even built, since 0229's new tables are additive and safe to apply early and 0228 fixes a live blocker he'd just hit himself.

## Deferred
- **Live click-through on all four fixes** — trigger a failed-then-fixed site merge; archive a duplicate and dismiss another as "not a duplicate" from the Contacts/Staff Dupes tab; add a trade in the new settings screen and confirm it shows in the Review Queue's picker. None of this has been clicked through live — requires sign-in, off-limits for Claude to do on Royce's behalf. _(added 2026-07-31, needs Royce)_
- **Migration numbering collision (0228)** — this session's `0228_site_merge_manager_gate_fix.sql` and a concurrent session's unrelated `0228_quote_target_period.sql` both landed on `main` under the same leading number, discovered while checking the live ledger before dispatch. Non-blocking (ledger keys by full filename; the same pattern already exists twice for `0187_*` in this same migrations folder) but worth knowing the "check origin/main's latest number" step used all session isn't fully collision-proof against a same-day concurrent session. _(added 2026-07-31)_

## Notes
- Used Plan Mode for this one, unusually for a same-day multi-PR sprint — the combination of Royce's four answers landing on "build everything" plus two new eq-shell schema surfaces (auth-adjacent RPC fix + two new tables) crossed the threshold for a written brief + explicit approval before touching code, per the global Task Brief rule. The plan's own phasing (fast wins first, schema+auth fix before the UI that depends on it) held up exactly as written through to the end — no re-planning needed mid-build.
- Root-causing the merge-permission bug required going outside eq-intake's own repo entirely — the RPC itself, its manager-gate logic, and the fix all live in eq-shell (Rule 2 ownership), confirmed by direct `pg_get_functiondef` reads on ehow rather than trusting the client-side error text or eq-intake's own (accurate but incomplete) code comments about the gate's existence.
- Six worktrees in flight across this thread (2 for the eq-shell schema/fix work, 1 each for 3 separate re-vendor batches) — all registered, all cleaned up (`git worktree remove` + local branch delete) once their PRs merged, no stale entries left behind this time.

---

# Session 2026-07-31 (cont.) — Self-join Field access now requires "earned", not just "allowed"

## Built
- Full ONE LOGIN onboarding wiring reviewed end to end (six live doors + two retired), captured as a visual workflow map with every edge case Royce asked to see, including the self-join (QR/link) door's zero-vetting gap: it grants Field access instantly, no admin and no document check.
- Ran a structured steelman on Royce's own idea (gate Field behind Photo ID + White Card upload, keep phone+code signup itself frictionless) and scoped which onboarding doors already have a human vetting step (email invite, worker-invite claim, Cards staff approval) vs which don't (true self-join only).
- New database columns (`field_access_unlocked_at` / `field_access_unlock_reason`) added to `shell_control.users` on jvkn (core's login system), separate from the existing per-worker Field on/off switch — this one tracks whether access has actually been *earned*. Settled the grandfather question first: every account that already had Field access got backfilled and verified (70/70, zero gap) so nobody currently working got logged out.
- `token-exchange.ts` (the single chokepoint every onboarding door already goes through before minting a Field session) now requires both "allowed" and "earned" before letting someone into Field.
- `shell-join-tenant.ts`, `accept-invite.ts`, `cards-approve-staff.ts` updated so every admin-vetted door marks access earned immediately (a human already vouched) — only true self-join is left ungated, pending the document-check piece.
- Migration applied live to jvkn (Supabase MCP `apply_migration`, verified via a live count query). Code changes committed (`3623e6f0`), pushed, PR opened: eq-shell [#1145](https://github.com/eq-solutions/eq-shell/pull/1145) — not yet merged.

## Decided
- Royce: "Settle the grandfather cutoff first, then start the migration" — settled as a one-time backfill stamp at migration time (`grandfathered_at_launch`) rather than an ongoing date-cutoff comparison in app code.
- Royce: "Confirmed, go ahead" on the session brief before the migration file was written (this session hit the Rule 0.6 brief-gate hook on the first Write attempt — resolved by running `/brief eq-shell` properly rather than the `EQ_SKIP_BRIEF` bypass).
- Royce: "Apply the migration to jvkn" — explicit go, applied and verified same turn.
- Royce: "commit these changes" / "push it and open a PR" — both explicit, executed as asked.

## Deferred
- **PR #1145 should not merge/deploy on its own** — the piece that flips a self-joined worker's access back on (the document-check trigger) doesn't exist yet. Merging this alone doesn't just delay self-join Field access, it removes it entirely until that trigger ships. _(added 2026-07-31, flagged in eq/pending.md)_
- **Document-check trigger not built** — needs eq-cards (separate repo) to recognise Photo ID and White Card as upload types, plus a rule on `public.licences` that flips access on once both are present/current and back off if either lapses.
- **No "blocked pending documents" screen in Field** — a gated self-join worker currently hits an unexplained dead end.
- **Users-tab invite form has no Field-access checkbox** (`invite-user.ts`/`AdminInviteUser.tsx`) — relies on the DB default; the Workers-tab form already closed this same gap on 2026-07-30.
- **Live smoke test not run** — needs sign-in, off-limits for Claude.

## Notes
- The migration file's own header comments ended up being the most authoritative record of the final design (more so than an earlier build-spec summary written mid-session) — when the two disagreed on whether the self-join fix should flip the existing `has_field_access` boolean or add a second "earned" signal, went with the migration text since it was the artifact Royce had most recently and explicitly confirmed.
- Session crossed from the prior day's `/brief` flow into new code territory — the brief-gate hook did its job here, correctly blocking an auth-adjacent control-plane migration write until the full Rule 0.6 gate ran and Royce signed off, rather than letting it slip through on session momentum.

---

# Session 2026-07-31 (cont.) — Weekly digest opt-in panel root-caused and fixed (v3.5.390)

Royce asked where the weekly digest recipient list lives ("I thought it was under supervisors") on `core.eq.solutions/sks/field?tab=managers` — the panel wasn't there.

## Built
- Traced the recipient list to `app_data.field_managers.digest_opt_in` (read by `supabase/functions/supervisor-digest/index.ts`), and the missing toggle UI to `scripts/digest-settings.js`. Root cause: that file hasn't been touched since v3.4.59 and triggers the panel by monkey-patching `window.renderManagers`, polled for 5s after `DOMContentLoaded` then permanently abandoned. `scripts/managers.js` (which defines `renderManagers`) became lazy-loaded in v3.5.21 (`EQ_LAZY`) — it only fetches once Contacts/Supervision is opened, almost never within that 5s window — so the wrap silently never installed and the panel stopped rendering for anyone, with no error surfaced.
- Fixed by replacing the monkey-patch trigger with a `MutationObserver` on `#page-managers`'s `class` attribute — `showPage()` removes `hidden` synchronously, before the lazy bundle even starts fetching, so this fires reliably regardless of `managers.js` load timing. Confirmed the panel is a DOM sibling of `#managers-content` (not a child), so it was never actually at risk from `renderManagers()` re-renders — the wrap had no real job to do beyond the initial mount.
- Verified without live tenant access (canonical Supabase is network-isolated from the build sandbox): built an isolated harness reproducing the exact regression (`renderManagers` left undefined, page visibility toggled at t=7.9s — past the old 5s window) — confirmed old code fails this, new code passes. Also confirmed live on the deploy preview and, after merge, on `field.eq.solutions` directly (`sw.js` banner shows v3.5.390) and via a demo-tenant click-through straight to the Managers tab.
- eq-field [PR #583](https://github.com/eq-solutions/eq-field/pull/583) merged (squash, v3.5.390).

## Decided
- Royce: "go ahead" on the fix approach after reviewing the brief.
- Royce: "merge it" — merged once checks + deploy preview were green.

## Deferred
- **Live click-through with real SKS tenant data** — confirm the panel shows real supervisors (not demo-tenant seed data) and that toggling opt-in still saves, on `core.eq.solutions/sks/field?tab=managers`. Sign-in required, off-limits for Claude. _(added 2026-07-31, logged in `sks/pending.md`)_

## Notes
- `gh pr merge --delete-branch` hit the same recurring shared-checkout collision as other sessions this week (`'main' already used by worktree at ...`) — merge succeeded via GitHub regardless (confirmed via `gh pr view --json state,mergedAt,mergeCommit`); remote branch deleted manually via the GitHub API since `--delete-branch` never ran.
- A useful verification pattern for a bug that's fundamentally about *timing* rather than data: don't chase live auth just to prove it, build the smallest harness that reproduces the actual race (a bare DOM + stubbed globals + real unmodified source file), and show the old code fails it while the new code passes. Caught and proved the exact regression without needing the real tenant.

---

# Session 2026-07-31 (cont.) — Richard Brown's mobile crash, then usage-driven mobile nav simplification for supervisors

## Built
- **Fixed the crash** (`session.ts` typed `User.email` as non-nullable, but phone-only self-join workers get `email: null`): five unguarded `.email.split('@')` call sites across `TenantHome.tsx`, `WorkerHome.tsx`, `HubSidebar.tsx`, `MobileTabBar.tsx`, `AdminEditUser.tsx`. Fixed the type to `string | null` and null-guarded each site with plain-English fallbacks. eq-shell [PR #1143](https://github.com/eq-solutions/eq-shell/pull/1143), merged (squash `0d829e9e`).
- Confirmed via Sentry the matching error (EQ-SHELL-10, `auth-stall: chunk-error`, iPhone Mobile Safari on `/sks`) had no occurrences after the fix deployed (last event predates the merge), then marked it resolved.
- Investigated William Brown's routing (role=supervisor, has_field_access=true) landing on the dashboard instead of the Field-first view — confirmed working as designed: `FIELD_FIRST_WORKER_ROLES` only covers `employee`/`labour_hire`/`subcontractor`; supervisors/managers always keep the dashboard. No code change.
- Added a "My Card" row to the mobile account sheet for everyone who isn't field-first (supervisors/managers/office roles had no mobile path to their own licences at all — Cards was only ever a bottom tab for field-first workers). Gated on the same tenant `cards` entitlement the desktop sidebar already checks. eq-shell [PR #1144](https://github.com/eq-solutions/eq-shell/pull/1144), merged (squash `a93d77f3`).
- Before building a supervisor-specific mobile nav, pulled 30-day PostHog usage by `role=supervisor`: Ops 159 / Service 102 / Field 83 pageviews on **desktop** (supervisors are the heaviest Ops/Service users, more than Field) vs. every module in single digits on **mobile** (Field included) — mobile is a quick-status device for this cohort, not a work surface. This ruled out reusing `isFieldFirstWorker` (which also drives the desktop sidebar) as the lever, since it would have stripped supervisors' desktop access to the modules they actually rely on.
- Built `mobileSimplified` in `MobileTabBar.tsx` — mobile-only (manager/supervisor, excluding platform admins), trims the bottom bar to Home + Field; Service/Ops move to the account sheet's "Reports & admin" panel (gated on `service.view`/`ops.view_rates`) rather than disappearing. Desktop `HubLayout`/`HubSidebar` untouched. Required a follow-up commit to satisfy the repo's role-literal CI ratchet (`check-role-literals.mjs`) with an `eq-role-literal-ok` suppression comment, since this is a display-only layout choice, not an access gate. eq-shell [PR #1146](https://github.com/eq-solutions/eq-shell/pull/1146), merged (squash `be0bb56d`).
- Richard Brown reported he then couldn't access Service. Live-checked (not assumed): his role (supervisor) has `service.view`/`create`/`close`/`reopen`/`record_tests`, the `sks` org has `service` entitlement enabled, and no security-group membership or `tenant_role_overrides` row denies anything on Service for supervisors on that tenant. Conclusion: not a permissions gap — this is the expected result of the `mobileSimplified` change just shipped, Service is one tap deeper (account menu → Reports & admin) instead of a main tab. Told Royce where to find it, no code change.
- Confirmed the mobile/desktop breakpoint (`max-width: 767px`) means iPads (768px+ in both orientations) get the full desktop nav, not the new simplified mobile view — informational, in case a tablet-specific tier is ever wanted.

## Decided
- Royce: "merge it" (×3, once per PR) — explicit go for #1143, #1144, #1146, each only after confirming CI was green.
- Royce, via AskUserQuestion: "we will need to add in a cards icons then so its obvious how to see your licenses" — confirmed William's dashboard routing as working-as-designed, and turned the follow-on gap (no mobile path to licences for non-field-first users) into the My Card row.
- Royce: "the idea is to simplify for site guys... users of Ops/Service etc will be desktop" — confirmed building the mobile-only Home+Field tier after usage data supported it, explicitly scoped to supervisors/managers, desktop untouched.
- Royce: "mark it resolved" — explicit go to close the matching Sentry issue (EQ-SHELL-10) once the fix was live-verified.

## Deferred
- **Royce to confirm on Richard's own phone**: page loads clean, bottom bar shows Home + Field only, Service/Ops reachable via account menu → Reports & admin. _(added 2026-07-31, flagged in eq/pending.md)_
- **Whether supervisors need Service as a main tab after all** — open question if Richard's "couldn't find it" report turns out to be a pattern rather than a one-off; currently just relocated, not removed. _(added 2026-07-31, flagged in eq/pending.md)_

## Notes
- The William Brown routing question and the Richard "couldn't access Service" report both looked like bugs/permission gaps on the surface but weren't — both resolved by checking the actual permission matrix / usage data / live entitlements first rather than assuming and changing code. Same discipline paid off twice in one session.
- Real usage data (PostHog, 30-day pageviews by role) directly overturned the working hypothesis ("supervisors won't need Service/Ops on mobile so default them to field-first") — the desktop numbers showed the opposite. Building the mobile-only `mobileSimplified` tier instead of reusing `isFieldFirstWorker` avoided an unintended desktop regression for supervisors.

---

# Session 2026-07-31 (cont.) — EQ Service: per-tier report settings finished, a real storage bug root-caused, paste-import gets link/create/skip

Continuation from the prior close (canonical user-id fix + per-tier report settings had already landed). Royce asked where the report export buttons were on a maintenance check (status-gated, explained, no bug), then what images populate reports (tenant/customer logo vs. site photo — investigated and explained), then hit a real upload failure trying to add a site photo. Root-caused that instead of guessing, which led into a second, related ask: give the work-order paste-import screen the same link/create/skip choice an existing importer already has.

## Built
- Confirmed live via direct SQL that migration `0197` (12 new per-tier report-settings columns) is actually applied on ehow, not just merged — first dispatch attempt hit a transient 502 from the Supabase API, second attempt succeeded. eq-service [PR #658](https://github.com/eq-solutions/eq-service/pull/658).
- Explained (no bug): the "only Field Run-Sheet, no export buttons" question — Customer Report / Send Report / Test Report only appear once a maintenance check is marked Complete, by design.
- Explained (no bug, but one real gap found): tenant/customer logos are set in EQ Shell, not EQ Service — EQ Service just displays them. Site photos ARE uploadable in EQ Service (Admin → Media) but only render on 2 of the 8 report types (ACB Test Report, Customer/PM Asset Report) — not the PM Check Report, which is by far the most-used one.
- **Root-caused the actual upload failure**: Postgres logs showed `new row violates row-level security policy for table "objects"` at the exact moment of the attempt. The `logos` storage bucket (tenant/customer logos, Media Library site photos) never had an INSERT policy on `storage.objects` — confirmed via `pg_policies`, only `licence-photos`/`safety-photos`/`compliance-packs`/`attachments` had one. `public=true` on a bucket only exempts *reads* from RLS, not writes — every in-app upload through Admin → Media had been silently failing for every tenant since the bucket was created. New migration `0198` adds the missing tenant-scoped write policy. eq-service [PR #659](https://github.com/eq-solutions/eq-service/pull/659), merged; dispatched (also needed a retry past a transient 502) and confirmed live via `pg_policies`.
- **Scoped and built the paste-import reconcile feature.** Mapped what already existed first: the Delta/Maximo importer (`/maintenance/import`) already has a full link/create/skip reconcile UX; the lighter paste-to-match flow (`/maintenance/paste`) only had smart matching with unmatched rows silently skipped; a third, dumber positional-paste modal on the check-detail page has no matching at all. Royce chose `/maintenance/paste` via AskUserQuestion. Built: an inline searchable "link to existing asset" picker and a "create new asset" mini-form (site + name required, type optional — paste rows only carry Asset ID + WO#, no description/classification to derive a name from, unlike the Delta importer) for each unresolved row. Server-side, `commitPasteImportAction` now resolves link/create choices into the same row shape as auto-matched rows before the existing group-by-site commit logic; moved the duplicate-WO guard to run before any asset creation so an aborted commit never leaves an orphan asset behind. eq-service [PR #660](https://github.com/eq-solutions/eq-service/pull/660), merged. `tsc --noEmit` and full `next build` both clean.

## Decided
- Royce, via AskUserQuestion: build true per-tier report settings (the full migration + rework), not a quick copy-fix — already covered before this continuation's close entry.
- Royce: "yes, dispatch it and build the paste reconcile feature" — go-ahead for both the migration-0198 dispatch and the paste-reconcile build in one message.
- Royce, via AskUserQuestion: `/maintenance/paste` unresolved rows over the Assets CSV importer or the check-detail page's dumber paste modal, as the target for the link/create/skip treatment.
- Royce: "merge it" ×2 (PRs #659, #660) and "merge" for the earlier #658 — each after CI/build checks passed.

## Deferred
- **Royce to click through `/maintenance/paste`'s new resolve UI live** — paste a batch with an unmatched Asset ID, link one row and create another, confirm the resulting check comes out right. Not tested in a real browser this session (no Shell-JWT session available in this environment). _(added 2026-07-31)_
- **Site photos still only render on 2 of 8 report types** — flagged to Royce, not yet requested as a fix. _(added 2026-07-31)_
- **The dumber positional paste-WO modal (check-detail page) and the Assets CSV importer still have no reconcile option** — explicitly out of scope this round; same treatment could extend there later. _(added 2026-07-31)_

## Notes
- Both migration dispatches this session (`0197` retroactively confirmed, `0198` freshly applied) hit the same transient 502 from the Supabase management API on the first attempt and succeeded on an immediate retry — worth knowing this specific governed-apply pipeline has some flake, not necessarily a real blocker, before assuming a first-attempt failure means something is broken.
- The `logos` bucket RLS gap is a good example of a bug hiding behind partial success: EQ Shell's own logo writes "worked" the whole time because Shell uploads via its own service-role key (which bypasses RLS entirely) — a completely different code path from EQ Service's tenant-JWT upload, so the gap never surfaced until a user tried the in-app upload feature directly.

---

# Session 2026-07-31 (cont.) — SKS Toolbox Talk feedback: photo picker fixed, post-submit editing fixed, ported to EQ Field

Royce reported two Toolbox Talk problems live: couldn't upload an existing JPEG, and asked whether a talk should be editable after submitting. Root-caused both before touching code, then asked Royce to make the one genuine product call (lock vs. edit) rather than deciding it silently.

## Built
- **Photo picker fix**: the shared photo-add input (`_photoRenderList` in `scripts/safety.js`, feeds Prestart/Toolbox/Incident) carried `capture="environment"` alongside `accept="image/*"` — forces the camera open directly on many mobile browsers and hides the "choose from library" option entirely, so an existing JPEG couldn't be selected. Dropped `capture`. Verified live in-browser (rendered input, before/after) rather than just reading the source.
- **Post-submit editing fix**: a submitted Prestart/Toolbox/Incident left every field fully live in the DOM but had no Save button once status flipped to `submitted` — any edit typed after that point was silently discarded on modal close, no save path existed. All three forms now show a "Save changes" button post-submit, reusing the existing PATCH-by-id path that already worked for drafts (`status`/`submitted_at`/`submitted_by` untouched by a post-submit save). Verified via a direct in-page render check (fetched the live script, evaluated it, rendered the form with a submitted-status draft, confirmed the button set).
- sks-nsw-labour [PR #74](https://github.com/eq-solutions/sks-nsw-labour/pull/74) merged (squash `0e3377d`), v3.10.107. Live on sks-nsw-labour.netlify.app (deploy `6a6c5a0`, secret scan clean) — confirmed via Netlify MCP, not assumed.
- **Ported the photo-picker fix to EQ Field**: found the identical `capture="environment"` bug in the shared `createPhotoController.renderList` (`scripts/site-reports-shared.js`), used by Toolbox/Incidents/Prestart there too. Checked (not assumed) whether EQ Field also needed the post-submit-editing fix: its Toolbox already always shows a working Save regardless of status (no bug); its Prestart is *deliberately* locked (fields disabled) once submitted — a real fix from field supervisor feedback back in July (v3.5.247) — left untouched rather than reversing that decision. `scripts/safety.js`'s own duplicate copy of the buggy photo input is dead code (retired per the file's own v3.5.339/340 changelog comments, no live caller) — left as-is.
- eq-field [PR #584](https://github.com/eq-solutions/eq-field/pull/584) merged (squash `cbd1075`), v3.5.391. Live on field.eq.solutions (deploy `6a6c5db`, secret scan clean) — confirmed via Netlify MCP.
- Flagged a stale SKS brand colour (`#1F335C` vs. the corrected `#203060`) hardcoded in the incident-alert email while reading `_incNotifyManagers` — out of scope for this task, spun off as a background task (`task_6ab9ac44`) rather than bundled in; Royce started it in a separate session, which has since ended — outcome not visible from here.

## Decided
- Royce, via AskUserQuestion (lock it down vs. allow real editing): **allow real editing** — add a working post-submit Save rather than disabling fields to match EQ Field's Prestart precedent.
- Royce: "ship it — bump version and push" then "merge it now" — SKS PR #74, merged after confirming the branch was clean and un-stale against `origin/main`.
- Royce: "merge it now" — eq-field PR #584, same pattern, after confirming EQ Field's own analogous behaviour first rather than blindly porting SKS's UI decision.

## Deferred
- **Live phone click-through not done on either app** — camera vs. gallery picker, and that "Save changes" actually persists an edit after Submit. _(added 2026-07-31, logged in `sks/pending.md` + `eq/pending.md`)_
- **Stale `#1F335C` brand colour** — background task ran in a separate session and ended; whether it actually fixed the email template isn't visible from this session. _(added 2026-07-31, logged in `sks/pending.md`)_

## Notes
- This session ran the Rule 0.6 gate fresh for both repos it touched (SKS at the start, EQ Field again when the "check against eq field" ask came in) rather than assuming the earlier gate covered a second repo — caught that `C:\Projects\eq-field`'s root checkout was mid-work on an unrelated branch (`claude/verify-pin-core-only-serverside`), so used a fresh worktree there instead of touching root, same convention documented throughout `worktree-registry.md`.
- Deliberately did NOT mirror SKS's "allow editing" decision onto EQ Field's Prestart lock — checked the actual current behaviour in each app first rather than assuming parity meant "make them identical," since EQ Field's lock was itself a real, feedback-driven fix, not an oversight.
- Both Netlify deploys were confirmed via the Netlify MCP reading the actual deploy record (commit ref, secret-scan result, build status) rather than trusting `git push` + "should be live by now."

---

# Session 2026-07-31 — EQ Suite loading-perf sweep: 3 shipped + live-verified, 1 shelved on risk, security-register addendum logged

*Continuation of the 2026-07-11 nav-speed thread's open "residual pre-warm timing" item. Asked for a fresh round of creative cold-start/prewarm ideas beyond what was already tried; built the safe ones one at a time, verifying each live before moving to the next.*

## Built
- **`token-exchange` added to the existing warm-ping keep-warm list** (`netlify/functions/warm-ping.ts`) — it's the one session/auth-adjacent function NOT already covered, and it fires just once per session (Field/Service/Cards iframe prewarm) with long idle gaps, long enough for its Lambda container to recycle before the next mint. A bare GET 405s before any session/origin/DB work runs, so the ping costs nothing on the auth path and generates no Sentry noise. eq-shell [PR #1135](https://github.com/eq-solutions/eq-shell/pull/1135), merged (squash `ecf1b3f1`), confirmed live via the Netlify deploy record (commit ref match, not just "should be live by now").
- **Preconnect hints for Field/Service/Cards origins** (`src/lib/preconnect.ts`, wired into `FieldIframe.tsx`/`ServiceIframe.tsx`/`CardsIframe.tsx`) — `index.html` only ever preconnected to Google Fonts; each iframe component now fires the hint as soon as its real origin is known (not a hardcoded guess — see Notes). eq-shell [PR #1139](https://github.com/eq-solutions/eq-shell/pull/1139), merged (squash `aa5fda6`).
- **`fetchPriority="low"` on the three prewarmed iframes + visibility-gated prewarm timer** (`App.tsx`) — background prewarm now yields bandwidth to the active tab and pauses entirely via `document.visibilityState` if the tab is backgrounded at the 2.5s mark, resuming on `visibilitychange`. Needed a small `@types/react` module augmentation (`src/types/react-fetchpriority.d.ts`) — `fetchPriority` is typed on `img`/`link`/`script` but not `iframe` in the pinned version, despite the attribute being valid/supported there too. eq-shell [PR #1141](https://github.com/eq-solutions/eq-shell/pull/1141), merged (squash `9cc9164`).
- All three PRs: `tsc -b --force` clean, full `npm test` green (274/274), zero *new* lint findings — confirmed via `git stash`/`eslint`/`stash pop` that every finding touched pre-exists on `origin/main` (this repo's lint gate is documented `continue-on-error` pre-existing debt in `ci.yml`, ~3,095 errors repo-wide, not a blocking gate).
- Verified the final deploy live on core.eq.solutions two ways: cross-referenced the Netlify deploy record's `commit_ref` against both merge SHAs, and confirmed `#1139`'s commit is a git ancestor of `#1141`'s (so the deploy genuinely contains both, not just the latest).
- Logged a live secret-exposure finding as an addendum to **SEC-9** in `ops/security-register.md` (eq-context, commit `427234e`) — see Notes.

## Decided
- Royce: **"Warm-ping the Lambda instead"** (of porting `token-exchange` to a Netlify Edge Function) — chosen via `AskUserQuestion` after reading the actual handler and finding it hand-rolls `node:crypto` HMAC signing + `@sentry/node`, neither verified Deno-edge-compatible, sitting directly on the Field/Service SSO handoff. Real risk found by reading code, not assumed from the original brainstorm's "it's just a JWT mint" framing.
- Royce: **"Build #1"** → **"merge #1135"** → **"Keep going on those two"** → **"merge #1139 and #1141"** — each PR built, tested, and left unmerged for explicit confirmation before merging (per standing auth/deploy rules), one at a time.
- Royce: **"Log as a security-register addendum, rotate later"** (of the jvkn service_role key exposure) via `AskUserQuestion` — not an emergency rotation.
- Steelman run (on my own recommendation) on deferring the Sentry release-tracking gap: confirmed via `gh secret list` + workflow grep that no `SENTRY_AUTH_TOKEN` exists anywhere in the repo — the fix is genuinely blocked on Royce generating one, not just lower-priority. Called "defer," flagged for his 10-second gut-check before filing (not yet confirmed as of session close).

## Deferred
- **Edge Function port of `token-exchange`** — shelved on real auth-path risk (see Decided), not attempted.
- **Batch the three apps' token mints into one `verify-shell-session` response** — bigger structural change, not built.
- **Prefetch each app's JS bundle by role ahead of iframe mount** — bigger structural change, not built.
- **Sentry release tracking is unwired on eq-shell** — `find_releases` returns zero, and every error event in the last 90 days has `release: null`. Needs a new Sentry auth token (`project:releases` scope) that doesn't exist yet; recommended defer, not yet confirmed by Royce.
- **Cross-referenced but out of scope**: a "Needs you" Sentry alert (`EQ-SHELL-10`, "auth-stall: chunk-error") surfaced mid-session — checked and confirmed it first-seen 2026-07-29, unrelated to this thread's code, and (per a *different* concurrent session's same-day entry above) already root-caused and fixed via PR #1143.

## Notes
- **A routine `getAllEnvVars` Netlify read (verifying `VITE_FIELD_URL`/`VITE_SERVICE_URL` production values before building the preconnect fix) returned the live `SUPABASE_SERVICE_ROLE_KEY` for eq-canonical (jvkn) fully unmasked** — same key/project as the already-open SEC-9 finding, but this time via `is_secret: true` `dev`-context values not being masked at all, not an `is_secret: false` misconfiguration (worse than SEC-12/SEC-18's pattern). Five other `is_secret: true` dev-context secrets came back unmasked the same call. None of these values were printed or reused anywhere in this session's output. Logged as an addendum to SEC-9 in `ops/security-register.md`, isolated to that one file in a commit separate from four other files mid-edit by other concurrent sessions in the shared `eq-context` checkout.
- **Confirmed live (via the same env-var read) that `VITE_FIELD_URL` production = `field.eq.solutions`, not `eq-field.netlify.app`** — the latter is only the code's fallback constant if the env var were ever unset. This directly shaped the preconnect fix: a static hint hardcoded to the fallback would have warmed the wrong origin for most tenants, which is why the fix resolves the origin dynamically per-component instead.
- **`git stash`/`git stash pop` was used twice this session** (isolating pre-existing lint findings from new ones on `ServiceIframe.tsx`/`FieldIframe.tsx`, then again on `App.tsx`) — this is exactly the operation flagged by a live guard finding (F7: git merge/stash-pop round-trip can NUL-fill files on this mount). Ran a byte-level NUL scan (not just `wc -l`/`tsc`, per F6/F7's own detection notes) both times; both clean, in the working tree and the pushed commit.
- Corrected my own mid-session characterization: the 2026-07-11 pending.md note "Shell eager-mounts all 3 app iframes on boot" was already out of date before this session started — a delayed 2.5s background-prewarm mechanism (not true simultaneous eager-mount) has existed since before that date. Ticked the 2026-07-11 "residual pre-warm timing" deferred item closed in `eq/pending.md` rather than leaving stale.
- Ran into the same benign local-git conflict twice when merging (`gh pr merge` failing to sync local `main` because a sibling worktree, `auto-import-toggles-956005`, has it checked out) — both times confirmed the merge actually landed on GitHub via `gh pr view` before treating it as done, and manually deleted the resulting orphaned remote branch.

---

# Session 2026-07-31 (cont.) — EQ Field mobile drawer: Toolbox Talks (and Prestarts/Records/Incidents) had no mobile path at all

Continuation after the day's earlier close. Royce reported live: "mobile view for eq field doesnt allow navigation to toolbox talks."

## Built
- Root-caused before touching code: desktop's Safety nav group has 7 children (Prestarts, Toolboxes, Site Audits, Records, Report, Test Equipment, Incidents) in a collapsible sidebar section, but the mobile "More" drawer only ever had one flat, sks-gated `ditem-safety` item that calls `mobileNav('safety')` — which routes straight to Site Audits and nowhere else. Confirmed via direct read of `index.html`'s drawer markup, `mobileNav()`, and `applyTierVisibility()` in `app-state.js`: Prestarts/Toolboxes/Records/Incidents had zero mobile entry point, on either tenant — not a deliberate SKS-only gate working as intended.
- Added 4 new drawer items (`ditem-prestart`, `ditem-toolbox`, `ditem-safety-records`, `ditem-incident`) mirroring desktop's `nav-prestart`/`nav-toolbox`/`nav-safety-records`/`nav-incident` exactly — same `edit-only` (manager-mode) gating per item, ungated by tenant since only Site Audits/Report/Test Equipment are sks-only on desktop. Added `'prestart'`/`'toolbox'`/`'incident'` to `DRAWER_NAV_PAGES` so the new items get correct active-state highlighting.
- Verified structurally rather than assumed: this sandbox's browser can't complete EQ Field's tenant-config boot handshake (no real network to the config service, app shows a hard "Could not connect" screen instead of the shell), so live click-through wasn't possible. Instead fetched the built `index.html`, parsed it with `DOMParser`, and confirmed all 4 new items render with correct IDs/labels/`edit-only` flags and no ID collisions; confirmed `DRAWER_NAV_PAGES` picked up the 3 new page IDs via direct source-string match.
- eq-field [PR #585](https://github.com/eq-solutions/eq-field/pull/585) merged (squash `4a15fa0`), v3.5.392. Live on field.eq.solutions (deploy `6a6c625`, secret scan clean) — confirmed via Netlify MCP.

## Decided
- Royce: "merge it now" — merged once the structural verification was reported (no CI in this repo beyond Netlify's own build/secret-scan).

## Deferred
- **Live phone click-through not done** — open the More drawer, unlock manager mode, confirm Toolboxes/Prestarts/Incidents/Records each land on the right page. Same sandbox limitation (no real network to EQ Field's tenant-config service) blocked this from being done here. _(added 2026-07-31, logged in `eq/pending.md`)_

## Notes
- Ran the Rule 0.6 gate fresh again for this task specifically (root checkout still on the same unrelated stray branch as this morning, `claude/verify-pin-core-only-serverside`) rather than assuming the day's earlier eq-field brief still covered a new, unrelated task — used a fresh worktree off `origin/main` again, same convention as this morning's parity-fix session.
- `system/worktree-registry.md` row for this worktree was silently lost twice mid-session — added once, later found missing (confirmed via direct `grep`/`sed`, not the Read tool's stale-cache assumption, which incorrectly reported "file unchanged" when it demonstrably wasn't). Re-added and pushed immediately both times rather than batching, per the file's own documented "commit right after any registry edit" guidance — this is the same concurrent-write collision class the file's own history extensively documents, not a new failure mode.
- `git worktree remove` hit the same Windows locked-folder pattern documented elsewhere in the registry (`Permission denied` on the final directory delete) — but this time confirmed via `git worktree list` that the unregister half of the operation succeeded regardless; only the physical folder delete failed. Left as a harmless orphaned husk rather than fighting it further, consistent with how prior sessions have handled the same class of lock.
