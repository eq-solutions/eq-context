# Session 2026-07-30 — `__personal__` tenant "retired" doc claim corrected against live data

## Built
- Live-verified a doc/data mismatch: `eq/identity/IDENTITY-MODEL.md` claimed the `__personal__` ghost tenant was "retired — all users homed in their primary employer tenant." Queried `shell_control.user_tenant_memberships` on eq-canonical (jvknxcmbtrfnxfrwfimn) — 47 active rows against `__personal__`, created continuously through 2026-07-29.
- Traced the mechanism: eq-cards' "Policy 1" (`supabase/migrations/0038_claim_invite_personal_tenant.sql`, decided 2026-06-17, unchanged through `0072`/`0076` as of 2026-07-27) makes `__personal__` the permanent home tenant for every Cards worker; org membership is added additively, never replacing it. This is deliberate, live, working behaviour — not orphaned/stale rows.
- What actually happened 2026-06-28: only the `shell_control.tenants` row for `__personal__` was set `active=false` — that only removes it from a few admin/audit sweeps that filter `is_personal=false` (`check-duplicate-shell-accounts.ts`, `check-dangling-staff-pointers.ts`, `licence-expiry-scheduler.ts`). It never stopped new memberships.
- Corrected `IDENTITY-MODEL.md` (retraction + explanation, not a data change) and bumped its `last_updated`. Also flagged the doc's own §11.2 backlog item ("multi-tenant membership — not yet built") as itself stale, since eq-cards has shipped exactly that since 2026-06-17.
- Committed (`87d565f`) and pushed to `origin/main` on Royce's explicit go.

## Decided
- Royce confirmed: correct the doc, don't touch the live membership rows (verify-first task from the start — no live writes were in scope or performed).
- Royce: "push and merge" — pushed directly to `main` (eq-context has no PR step; direct push is its normal flow).

## Deferred
- Spawned a background task to reconcile IDENTITY-MODEL.md's stated "one user, one tenant" model against eq-cards' shipped dual-membership Policy 1. That task produced a new §11.3 section mid-session with an explicit open decision for Royce (ratify Policy 1 / scope it Cards-only / constrain back to one-tenant) — as of this session's close, that edit was still uncommitted in the shared eq-context checkout (another live session's in-progress work, not committed here to avoid stomping on it). Needs Royce's decision and someone to land that commit.

## Notes
- Two sessions were editing `eq/identity/IDENTITY-MODEL.md` in the same shared `C:\Projects\eq-context` checkout concurrently this session (this one, plus the spawned reconciliation task). No conflict occurred because each session's edits landed in non-overlapping regions and only this session's commit was pushed — but it's a live instance of the checkout's known concurrent-writer risk (see `system/failures.md` / memory `eq-context-checkout-concurrent-writer`). Worth checking `git status` before any further edits to this file.
- None of the 47 `__personal__` membership rows were modified — this was a documentation-only fix, per the task's explicit verify-first framing.

---

# Session 2026-07-30 — EQ Ops now leads with ex-GST everywhere; Coupa PO-match display bug fixed

## Built
- Reviewed where EQ Ops (`eq-shell`'s quotes module) wires up job totals — found `subtotal_cents` (ex GST) already existed alongside `total_cents` (inc GST) server-side, but every UI surface except the pipeline list column showed inc-GST as the primary figure.
- Swapped emphasis to ex-GST in: job detail header + financial breakdown, create-quote form breakdown, kanban board cards (inc-GST moved to a tooltip), and every Reports tab (pipeline, aging, by-estimator, monthly, by-customer, win/loss, register) — headers relabelled to match. Register CSV kept both figures, already correctly labelled.
- Found and fixed a related display bug: the Coupa PO-match import preview compared a supplier PO's ex-GST value against the quote's inc-GST total on screen — a correct match could read as a mismatch. The matching RPC's own logic was already correct (it grosses up the PO figure by 1.1 before comparing); only the two numbers shown side by side were mismatched units.
- New migration `0223_ops_ex_gst_display_rpcs.sql`: adds `quote_subtotal_cents` to `eq_match_coupa_po` and `subtotal_cents` to `eq_list_loss_reasons` so both RPCs can expose ex-GST for display. First dispatch to `sks`/ehow failed clean (0 applied, transaction rolled back) — Postgres 42P13, `CREATE OR REPLACE FUNCTION` can't change a `RETURNS TABLE` function's OUT-parameter shape, needed an explicit `DROP FUNCTION` first (already had this right for `eq_match_coupa_po`, missed it on `eq_list_loss_reasons`). Fixed, redispatched, applied clean, live-verified via Supabase MCP that both RPCs now return the new columns.
- `npx tsc --noEmit` and full `pnpm run build` both clean.
- Ran the `/brief` session gate for eq-shell before writing (health digest, worktree check, git state — branch was up to date with `main`, clean).
- Committed, pushed `claude/eq-ops-gst-ex-gst-totals-245943`, opened eq-shell [PR #1111](https://github.com/eq-solutions/eq-shell/pull/1111), squash-merged to `main` on Royce's explicit go (triggers Netlify auto-deploy to core.eq.solutions).

## Decided
- Royce: scope = "everything internal" (all the locations above), customer-facing quote PDF stays inc-GST-prominent — normal AU invoicing practice, out of scope.
- Royce: merge the PR — confirmed despite one pre-existing, unrelated failing check (see Notes).

## Deferred
- **Royce to click through live**: job detail view, create-quote form, kanban board, and each Reports tab — confirm ex-GST reads as the main figure everywhere. Verified via build + typecheck only this session. _(added 2026-07-30)_

## Notes
- PR #1111's "Schema drift + anon-grant + policy-lint" check was failing at merge time, but on inspection it was a pre-existing, already-tracked violation ([issue #1108](https://github.com/eq-solutions/eq-shell/issues/1108): `tenant_field_importance_overrides` RLS disabled on zaap+ehow, from PR #1104/migration 0222, opened hours before this session's branch existed) — unrelated to this change, and `main` has no branch protection requiring it green. Merged anyway per Royce's explicit instruction, didn't chase the unrelated red check.
- Royce asked why "open PR / commit / merge" needed three separate confirmations for one piece of work. Answer: only merge-to-`main` and prod migration dispatch are actually deploy-gated by his CLAUDE.md; commit/push/PR-open aren't deploys. Going forward those three routine steps happen automatically once a change is built and verified — saved as a feedback memory (`deploy-gate-vs-routine-git-steps`) so this doesn't need re-explaining next session.

---

# Session 2026-07-30 — `__personal__`/multi-tenant reconciliation confirmed landed, pending.md fully archived

## Built
- Royce corrected the standing assumption directly: there should be no "one worker, one tenant" rule — it's the opposite. Cards is the identity layer for everyone; a person owns their info once and can additively join more than one tenant without re-entering it.
- That exact ruling had already reached the background reconciliation task independently (quoted near-verbatim in its own `§11.3` write-up) and was already committed to `eq-context` main (`f67886e`) in the shared checkout before this exchange — confirmed via `git show --stat`, no new edit needed.
- Since the `__personal__`/multi-tenant `pending.md` section (added earlier today) now had zero open items, moved it wholesale to `eq/pending-archive.md` per the stated archive rule, rather than leaving a fully-ticked section live.

## Decided
- Royce: the multi-tenant model is Cards-owns-identity, tenants-are-additive — now the documented, ratified model in `IDENTITY-MODEL.md` §11/§11.2/§11.3, not an open question.

## Deferred
- None new. The two follow-ups already on record in §11.3 stand: eq-cards' `0055` migration hardcodes the personal membership row's role to `employee` regardless of the user's real role (a shipped inconsistency, not fixed here — code change, out of this doc-only pass's scope), and a formal v2 version bump of `IDENTITY-MODEL.md` is a candidate given how materially this changed the identity model (not done in this pass).

## Notes
- No live data or code was touched in this pass — confirmation + doc housekeeping only.

---

# Session 2026-07-30 — Compliance-roster-only workers shipped: `has_field_access` flag merged + deployed

## Built
- Sprint-planning Q&A on top of the ONE LOGIN work surfaced a gap: labour_hire/subcontractor workers who exist purely for licence/compliance tracking (no real Field duties) were routed identically to every other worker — WorkerHome's tiles, the mobile 2-tab bar, and the desktop sidebar all assumed "worker role" implies "has Field access." No way existed to add someone for compliance-roster tracking only.
- Added `shell_control.users.has_field_access` (+ mirrored on `shell_control.user_invites`) on the jvkn control plane, boolean, default `true` — same plane/pattern as the existing `is_platform_admin` flag, not a new architecture.
- Threaded the flag end-to-end: `create-worker-invite.ts` → `worker_invites.profile_data` → `shell-join-tenant.ts` reads it into the new `shell_control.users` row (defaults `true` if absent) → every login door's `.select()` on `shell_control.users` now returns it → `src/session.ts` gets a new `hasFieldAccess()` helper (fail-open) → gates `isFieldFirstWorker` (kills the bypass-to-Field redirect for roster-only workers), `WorkerHome.tsx`'s tile list, `MobileTabBar.tsx`'s tab filter, and `HubLayout.tsx`'s sidebar filter.
- Admin UI: `AdminWorkerInviteForm.tsx` gets a "Field access" checkbox, shown only for labour_hire/subcontractor roles, checked by default.
- Migration `2026_07_30_users_has_field_access.sql` applied directly to jvkn via Supabase MCP (control-plane schema, outside the tenant-migrate.yml governed pipe, which only covers zaap/ehow) — verified live: both columns exist, default `true` confirmed via `information_schema.columns`.
- Pushed branch `worker-field-access-flag`, opened eq-shell [PR #1116](https://github.com/eq-solutions/eq-shell/pull/1116). All CI green (migration ledger hygiene, schema drift + anon-grant + policy-lint, gitleaks, typecheck/test/lint, Netlify deploy-preview). Squash-merged to `main` on Royce's explicit "Merge #1116" — commit `fd69a9b6`, branch deleted, Netlify auto-deploy to core.eq.solutions triggered.

## Decided
- Royce: default `true`, additive only, no new role — matches the ordering constraint (schema live on jvkn *before* the branch reaches `main`, since the code selects/writes the new column).
- Royce: "Merge #1116" — explicit go-ahead for the one gated step (merge-to-main); commit/push/PR-open were done automatically as routine steps per the standing deploy-gate rule.

## Deferred
- **Royce to click through live**: invite a labour_hire worker with the Field-access box unchecked → activate via phone-OTP join → confirm no Field tile/tab/sidebar item appears and a direct `/field` navigation is blocked (403 from `token-exchange.ts`); regression-check a normal labour_hire worker (box left checked) still lands in Field unchanged. Not yet done — build/typecheck verified only. _(added 2026-07-30)_
- No after-the-fact edit UI for an existing worker's `has_field_access` — v1 is invite-time only, by design (stated boundary in the approved plan). _(added 2026-07-30)_
- Remaining approved-but-unstarted backlog from the same sprint-planning Q&A, not started this session: SIM-swap/number-reassignment PIN invalidation; admin PIN-visibility UI; email-capture nudge → Email+PIN fallback.

## Notes
- Classifier outage from the prior (pre-compaction) window — which had blocked `apply_migration` and `git push` — had resolved by the time this session resumed; both succeeded first-try with no retries needed.
- `gh pr create` warned of "1 uncommitted change" — checked, it was only the pre-existing stray scratchpad file in this worktree, unrelated to the branch; left untouched per the never-delete-without-permission rule.

---

# Session 2026-07-30 — Closed the tenant_field_importance_overrides RLS gap (issue #1108)

## Built
- Root-caused the "Schema drift + anon-grant + policy-lint" required-check failure flagged (as pre-existing/unrelated) during PR #1112's merge: migration `0222_field_importance_overrides` created `app_data.tenant_field_importance_overrides` + its 3 RPCs but never enabled RLS or added a tenant policy.
- Verified live via Supabase MCP on ehow before fixing: `relrowsecurity=false`, no policy; confirmed all 3 RPCs (`eq_get/set/reset_field_importance_override`) are `SECURITY DEFINER`, so they bypass RLS and needed no changes.
- Found the exact precedent for this class of gap already in the repo — `0083_quote_rate_presets_rls.sql` closed the identical "table shipped without RLS" gap for `quote_rate_presets` (0071) — and mirrored it exactly: new migration `0224_field_importance_overrides_rls.sql` (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + idempotent `tenant_iso` policy).
- `node scripts/check-migration-hygiene.mjs` passed (no ledger self-insert). Opened eq-shell [PR #1115](https://github.com/eq-solutions/eq-shell/pull/1115), dispatched `tenant-migrate.yml` against the PR branch before merge (the known drift-gate dispatch-order workaround) — applied clean to both planes (`eq`/zaap 2 applied, `sks`/ehow 1 applied) — then the required check went green pre-merge. Squash-merged (`0b688cc7`) on Royce's "merge #1115" go.
- The drift check's own auto-issue-management closed [issue #1108](https://github.com/eq-solutions/eq-shell/issues/1108) automatically once the fix was live (20:38:46Z) — no manual issue close needed.

## Decided
- Royce: "merge #1115" — confirmed merge after all required checks (including the previously-red schema-drift check) were green.

## Deferred
- None. This gap is fully closed: migration merged to `main`, applied to both live tenant planes, drift check green, tracking issue auto-closed.

## Notes
- `gh pr merge --delete-branch` failed its local cleanup step only (`'main' is already used by worktree at .claude/worktrees/auto-import-toggles-956005`) — same known snag as prior sessions (e.g. PR #1094). The merge itself succeeded on GitHub; deleted the now-stale remote branch manually (`git push origin --delete`).
- Confirms the established pattern (memory `eq-shell-drift-gate-dispatch-order`): for a migration-only PR, dispatching `tenant-migrate.yml --ref <pr-branch>` before merge is what turns the required drift check green pre-merge, rather than merging red and fixing after.

---

# Session 2026-07-30 — Same drift-gate task arrived again, already closed: every premise stale, nothing built; PR #1114 unblocked + merged

## Built
- **Nothing.** A task arrived asking for a catch-up RLS migration + `SERVICE_ROLE_ONLY` entries for `app_data.tenant_field_importance_overrides`, reporting the gate red on `main` and RLS disabled on both planes. Verified live first (Rule 0.5) — **already fixed by the #1115 session logged directly above**. No migration written, no hand-toggle. All Supabase calls were read-only SELECTs.
- **Re-ran the stale drift check on PR #1114** (`gh run rerun 30485770946`) → SUCCESS. PR went fully green, `mergeStateStatus=CLEAN`.
- **Merged PR #1114** on Royce's explicit go — squash `7ac5af3a`, remote branch deleted cleanly. Dead-file cleanup only (intake domain pages + SecurityGroupsPage shim). Live smoke after: `verify-shell-session` → 401 `{"valid":false}`, root 200.

## Decided
- Royce: "merge #1114" — merged via the green re-run, explicitly not an admin bypass.
- Do **not** add `tenant_field_importance_overrides` to `SERVICE_ROLE_ONLY`, despite the task asking for it. That list is for RLS-on-with-**no-policy** tables; 0224 took the `tenant_iso` policy route (mirrors `quote_rate_presets`/0083, which is likewise absent from the list). Both lists verified identical (10 entries each) — invariant holds.

## Deferred
- None. Nothing left open from what was touched.

## Notes
- **Every premise of the task was stale by ~12 hours.** Claimed live state ("verified 2026-07-30: `relrowsecurity=false`, 0 policies, both planes") did not match the database: both planes read `relrowsecurity=true`, policy `tenant_iso`, grants `postgres` + `service_role` only, zero anon/authenticated. Ledger (`app_data._eq_migrations` — schema-qualified; a bare `_eq_migrations` query errors 42P01) shows 0224 applied ehow 20:35Z, zaap 20:36Z on 2026-07-29.
- **Ownership was eq-shell**, not eq-service/eq-field as the task suggested checking first. The "created out-of-band, appears nowhere in `eq-shell/supabase/`" claim was wrong: `0222_field_importance_overrides.sql` lives in **`supabase/tenant-migrations/`**, not `supabase/migrations/`. **Grep `supabase/tenant-migrations/` before concluding a tenant table is ungoverned** — that one path difference generated the entire false premise.
- **The `0223` zaap gap was stale too** — applied to zaap 2026-07-29 20:36Z. No `tenant-migrate.yml` dispatch needed; nothing was actually waiting on Royce's approval click.
- **Gate had been green since 2026-07-29T20:32Z** (3 consecutive scheduled passes, latest 02:29Z today); issue #1108 already auto-closed.
- **Recurrence, second time.** The stale-required-check trap is already on record in `pending.md` from 2026-07-06 (~line 1762, migration 0164) with the same remedy, and cost a full investigation again here. **Compare a red check's `startedAt` against when the fix landed before treating it as a live failure**: `gh pr view <N> --json statusCheckRollup --jq '.statusCheckRollup[] | select(.name=="<check>")'` → then `gh run rerun <run-id>`. PR #1114's check ran 19:44:53Z; the fix landed 20:36Z.
- Local branch `claude/field-importance-overrides-rls` (commit `42447300`) is redundant — same content as #1115's `0b688cc7`. Left in place, not deleted.
- Session gate's F6 warning honoured: used Edit anchors, never shell `>>`, for every write under `C:\Projects`. Hit two concurrent-edit collisions on this log mid-close (another agent appending its own entry) — re-read and re-anchored rather than forcing.
- **Goals are unset** — TODAY.md has no owned goals, so nothing here was prioritised or deferred against a deadline.

---

# Session 2026-07-30 — Dead-file audit opened PR #1114 (origin of the entry directly above)

## Built
- Royce asked for a research-only verdict on three similarly-named files found during an earlier mobile-permission fix: `SecurityGroupsPage.tsx`, `AdminSecurityGroups.tsx`, `AccessControlPage.tsx`. Traced all three against `App.tsx`'s routes/imports: `AccessControlPage.tsx` is the canonical 1,302-line implementation (routed `admin/access-control`, gated `admin.manage_groups`); `AdminSecurityGroups.tsx` is a deliberate re-export alias giving it a second stable URL (routed `admin/security-groups`) — both live, both keep. `SecurityGroupsPage.tsx` was a stale `<Navigate>` redirect shim superseded by the re-export, zero references anywhere in `App.tsx` — dead code.
- Royce confirmed the delete. Removed `SecurityGroupsPage.tsx` (`git rm`), committed `b4df772a`, pushed, opened eq-shell [PR #1114](https://github.com/eq-solutions/eq-shell/pull/1114) (which also picked up a pre-existing unpushed commit already on the branch: `c1af9150`, unlinked per-domain intake landing pages).
- CI showed the required "Schema drift + anon-grant + policy-lint" check red. Root-caused it to the `tenant_field_importance_overrides` RLS gap (unrelated to this diff — confirmed via live Supabase SELECTs on both planes: no anon/authenticated/PUBLIC grants despite RLS being off, so no live exposure) and spawned a background task rather than fixing an out-of-scope table inline.
- Royce ran that spawned task in a separate session; **its investigation, fix confirmation, gate re-run, and the PR #1114 merge are logged in the session entry directly above this one** (title: "Same drift-gate task arrived again, already closed").

## Decided
- Royce: delete `SecurityGroupsPage.tsx`, keep the other two. Royce: commit / push / open PR (routine steps, no separate gate). Final merge of #1114 was confirmed and performed in the follow-on session (entry above).

## Deferred
- None outstanding — resolved end-to-end across this session and its spawned follow-on.

## Notes
- This entry exists mainly to record the file-audit verdict (useful precedent if these three names resurface) since the entry above focuses on the drift-gate story and only mentions the deletion in passing.

---

# Session 2026-07-30 (cont.) — eq-cards nudge fix followed up (already live); RLS gap confirmed closed

## Built
- Confirmed PR #1112 (licence photo lightbox, merged 2026-07-29) live on core.eq.solutions via Netlify MCP — deploy `commit_ref` matches the squash commit, state `ready`.
- Followed up on the unanswered "want me to build the eq-cards picker nudge" question from 2026-07-29. Checked live jvkn state before building anything: `photo_id`/`passport` already exist as canonical, selectable `licence_types` rows (added out-of-band 2026-06-22) — the picker already surfaces them, nothing to build there. The real gap was that the tracked migration history never captured it (`0002_seed_licence_types.sql` only lists 13 types). Added a no-op migration matching the live rows exactly. eq-cards [PR #187](https://github.com/eq-solutions/eq-cards/pull/187), merged (squash `27b68d70`). Also reconfirmed PR #185's credential-gaps equivalence RPC (migration 0109) is applied live on jvkn.
- Verified the `tenant_field_importance_overrides` RLS gap (flagged via `spawn_task` on 2026-07-29, task_5ba9459a) is now fully closed — a concurrent session's PR #1115 enabled RLS on both planes; live-verified `relrowsecurity = true` on zaap and ehow via Supabase MCP. Issue #1108 auto-closed. No action needed from this session.

## Decided
- Royce: build the picker nudge and apply PR #185 to jvkn (the latter turned out to already be applied, confirmed rather than reapplied).

## Deferred
- None new.

## Notes
- Second time this week a "needs building" assumption turned out to already be live once checked against the actual DB — worth checking live state before building, every time, not just when a memory/doc claim seems stale.

---

# Session 2026-07-30 — eq-field Sentry error confirmed already fixed; Assigned To dropdown A-Z fix shipped; Report Settings gap found on two eq-solves-service reports

## Built
- Investigated a live Sentry error on eq-field (`ReferenceError: openTafeHolidaysConfig is not defined`, 3 events, last seen 2026-07-28). Traced it to a known lazy-loading gap (Edit Roster's TAFE buttons called a function from `scripts/tafe.js`, which was only ever bundled under the Apprentices tab) — already fixed same day in commit `152d145` (PR #560, "v3.5.373"), which added `tafe.js` to the `editor` tab's own script bundle. No action needed; confirmed closed, not re-opened.
- Audited eq-solves-service's Report Settings (`/admin/reports`) against the actual code of all 9 report generators (not the docs) to answer "what role does Report Settings play" — built a verified toggle matrix. Customer Report, ACB/NSX Test Report, Customer Scope Statement, and Customer Renewal Pack all respect the full set (cover/executive-summary/sign-off/complexity). Compliance Report only respects cover. PM Check Report respects cover + executive-summary (no sign-off section exists). Field Run-Sheet only respects sign-off (no cover/summary sections exist — cover is unconditional). Work Order Details respects **none** of the four.
- Found `docs/FEATURES.md`'s Report Settings section was stale — still described a "Customer logo toggle" and "Site photos toggle" that were removed from the code and the admin form on 2026-04-26 (form itself was already correct, only the doc lagged).
- Royce then reported two real symptoms from a downloaded Field Run-Sheet: the Assigned To dropdown wasn't A-Z, and Report Settings "doesn't appear to be affecting the report download." Investigated both against live code/DB (not assumption):
  - Assigned To dropdown (both the maintenance check detail page's inline editor and the New Check form) was sorting by role tier first (manager → supervisor → employee → apprentice → labour_hire), alphabetical only within each tier — produced exactly the "two stitched-together alphabetical lists" pattern in Royce's screenshot. Fixed in both duplicated `sortByRoleThenName` copies to sort by name only. `tsc --noEmit` clean.
  - Confirmed the Report Settings complaint was real, not user error: Field Run-Sheet has no cover/contents/executive-summary sections to gate — the downloaded docx (SY3 standard Run-Sheet) matched this exactly (cover/register header always present, sign-off block present, no executive-summary/contents anywhere).
- Pushed, opened, and squash-merged eq-service [PR #648](https://github.com/eq-solutions/eq-service/pull/648) on Royce's "push/merge" instruction — `296edfe` on `main`, Netlify auto-deploy triggered.
- Spawned two background tasks for the design decisions this surfaced (not built silently, since both are scoping calls): Work Order Details Report Settings wiring (`task_0efe5e55`) and Field Run-Sheet Report Settings wiring (`task_f8612432`). Both were run by Royce in separate sessions and completed; see Deferred.

## Decided
- Royce: "push / merge" for PR #648 — explicit go, executed (squash-merge to `main`, Netlify auto-deploy follows per the standing rule).

## Deferred
- **Whether Field Run-Sheet and Work Order Details should grow real Cover/Contents/Executive-Summary sections to match the settings page, or are meant to stay fixed-format working documents with only sign-off ever optional — still needs Royce's explicit call.** Work Order Details' background session left uncommitted changes on branch `claude/wo-details-report-settings` (`ReportSettingsForm.tsx`, `docs/FEATURES.md`, `generate-and-store.ts`, `work-order-details.ts` all modified, not committed). The Field Run-Sheet background session ran and ended but produced no branch/commit/PR that this session could find — its outcome is unknown and should be checked first thing next session. _(added 2026-07-30)_
- Everything already open from 2026-07-29's ACB/SI verification work is untouched and still stands (see that dated section in `pending.md`) — not part of this session.

## Notes
- The eq-context digest's Pulse table showed `? unknown` CI status simultaneously for eq-shell, eq-solves-service, eq-field, and eq-cards — verified directly via `gh run list` that eq-field's actual CI on `main` was green and current. This is very likely a digest-generation gap (one tool failing to query several repos at once), not several repos independently broken — worth remembering before treating a Pulse "unknown" as a real red flag.
- Two background-task branches now exist in the shared `C:\Projects\eq-solves-service` checkout with uncommitted work (`claude/wo-details-report-settings`) — did not touch, commit, or push either, per the rule against acting on another live session's in-progress worktree state.

---

# Session 2026-07-30 — Customers/contacts phone-fix merged and shipped to core.eq.solutions

## Built
- Merged eq-solves-intake [PR #92](https://github.com/eq-solutions/eq-solves-intake/pull/92) (`d1a0ebd3`) — CI was green, ready from an earlier session, needed the merge click.
- Ran `/decide` on "next best steps" given the session-gate flags — recommendation was to finish the already-verified, zero-risk eq-shell re-vendor rather than chase the 3 open P0 security findings (SEC-1/9/10), since Royce confirmed those are already owned in another session/thread.
- Re-vendored eq-shell's `eq-intake/eq-platform/packages/` to pick up #92: phone-field coalescing fix for customers/contacts (was under-flagging ~30/210 live contacts), Customers/Contacts enabled in the field-importance settings screen, `deriveActions()` generalized. Full gate green (build, check:css, check:perms, 274/274 tests). Opened eq-shell [PR #1113](https://github.com/eq-solutions/eq-shell/pull/1113).
- PR #1113's first CI run hit the required "Schema drift + anon-grant + policy-lint" check red — investigated rather than assumed: confirmed via direct `pg_class`/`pg_policies` SQL that the flagged `tenant_field_importance_overrides` RLS gap was real but transient, unrelated to this PR's diff (zero SQL changed), and had already been fixed live by a concurrent session (same issue #1108 thread other sessions logged today). Re-ran the CI job instead of merging past red or re-doing a fix that already landed; went green, merged (`8acd50cc`).
- Cleaned up both throwaway worktrees, updated `worktree-registry.md`, updated both product changelogs (`eq-shell.md`, `eq-intake.md`), ticked the one now-genuinely-confirmed deferred item in `pending.md` ("Royce to click through `/sks/intake` live" — the screenshot he shared at the top of this session showed the live 4-tab dashboard rendering correctly) and trimmed that section per the archive rule (2 open items remain, so header + opens only, narrative dropped in favour of the changelog).

## Decided
- Royce: settings screen stays local to Intake's gear icon, not promoted to Shell's global Settings page — nothing outside Intake reads the overrides today; revisit only if Field/Service start consuming them.
- Royce: SEC-1/9/10 are already owned elsewhere — proceed with intake work, don't pivot this session to them.
- Royce: "merge #92", then "merge #1113 once CI's green" — both executed.

## Deferred
- None new. The 2 items already open under the Intake-redesign section in `pending.md` (Bring Data In's separate commit path; the deeper bulk-approve/standing-rules/trend-view tier) stand untouched — out of scope for this session.

## Notes
- The customers/contacts phone-fix + settings screen is now fully live end-to-end on core.eq.solutions (both eq-intake #92 and eq-shell #1113 merged, Netlify auto-deploys on push to main).
- Confirms the same `tenant_field_importance_overrides` RLS-drift story multiple other sessions logged today (#1108/#1114/#1115/#1116 entries above) — this session's CI failure was the same already-closed gap surfacing one more time on a fresh PR's first run, not a new incident.

---

# Session 2026-07-30 (cont.) — Security P0 status check, no CI to check

## Built
- Nothing — read-only session. No Edit/Write, no migrations, no PRs.

## Decided
- None new.

## Deferred
- None added to `pending.md` — the one follow-up (security-audit.yml's broken `eq-solves-field` project ref) is tracked via a spawned background task (`task_919f0c83`), not a pending.md line item, since it's actively running in its own session rather than sitting idle.

## Notes
- Royce asked to "check CI on the security P0s" (SEC-1/9/10). Answer: there is no CI for any of the three. SEC-1 is explicitly manually-tracked per the register ("not by CI"); SEC-9/SEC-10 are key-rotation tasks needing Royce's own console access, also outside CI's scope. The one real CI surface (`security-audit.yml`) doesn't check any of these three findings and has been failing for 3 weeks anyway, for an unrelated reason: it queries a Supabase project reference for "eq-solves-field" that returns 400 "Resource has been removed." Spawned a background task to fix that broken reference rather than fixing it inline (out of scope for a status-check turn) — Royce started it in a separate session, running independently.
- While investigating, found (not done by this session) that a concurrent session had already closed SEC-10 — Royce rotated `ANTHROPIC_API_KEY`/`RESEND_API_KEY` on sks-nsw-labour himself and cleared `EQ_SECRET_SALT`'s dev-context plaintext leftover (commit `ef41f55`). SEC-1 and SEC-9 remain open, unchanged from the register's 2026-07-28 state — both are standing decisions (SKS Labour freeze; jvkn key rotation bundled with SEC-3) waiting on their own trigger conditions, not on any engineering.

---

# Session 2026-07-30 (cont.) — IDENTITY-MODEL.md: closed the "does §11.3 need a formal v2 bump" question

## Built
- Read `IDENTITY-MODEL.md` in full (already carrying the §11.3 multi-tenant-membership decision from an earlier session today) and checked it against the doc's own versioning rule: a v1→v2 bump triggers only on changes to the role list, naming conventions, session payload shape, or JWT shape. §6 (session cookie + Supabase JWT shape) is untouched by §11.3 — confirmed directly, not assumed.
- Checked whether the "modules pin themselves to a version in `permissions.ts`" mechanism the versioning rule describes actually exists anywhere: it doesn't. Grepped `permissions.ts` across all four repos — eq-shell has real permission files but none define a version constant (only prose comments citing section numbers); eq-cards and eq-solves-service have no `permissions.ts` at all; eq-field is still on its legacy `window.EQ_PERMS` JS shim, not on this model yet.
- Brought both findings back to Royce as a genuine either/or (not a unilateral call) via `AskUserQuestion`. Royce chose: stay v1, don't bump.
- Recorded that decision inline in the doc, right above the versioning-rule footer, so the question doesn't get re-litigated next time someone reads the closing note.
- Committed (`e888953`) and pushed to `origin/main` on Royce's explicit go.

## Decided
- Royce: `IDENTITY-MODEL.md` stays v1 — the membership change doesn't trip the doc's own literal bump triggers, and the pin mechanism that would give a bump teeth doesn't exist in any module yet. Revisit at the next real trigger (password+MFA, 6th tier, or the admin-grant flow already flagged as backlog in §11.2) and fold this membership change into that bump then.

## Deferred
- None new — this closed an open question, it didn't create one.

## Notes
- Confirms an existing memory pattern (`control-plane-migrations-no-ci-apply` / general "verify against live, not the doc's stated rule"): a doc's own stated mechanism (module version-pinning) can be aspirational text that no code actually implements. Worth remembering next time this doc's versioning rule is invoked as if it has real enforcement behind it — today it doesn't.

---

# Session 2026-07-30 (cont.) — eq-cards auto-provision role-hardcode: merged, applied live

## Built
- Task came in already scoped against §11.3's own flagged follow-up: `eq_cards_auto_provision()` hardcodes the `__personal__` membership row's role to `'employee'` regardless of the user's real role. Checked git/worktree state before writing anything (per Rule 0.6) and found the fix already existed: a dedicated worktree (`eq-cards-fix-autoprov-role-wt`, registered earlier today) had already built it as eq-cards PR #188 (migration `0111`), open and unmerged.
- Verified the existing fix rather than duplicating it: read `IDENTITY-MODEL.md` §3/§11.3, diffed migration `0111` against the live `0056` pre-image (exact match), and confirmed the role source is trustworthy in both cases — `eq_cards_claim_invite` (0038) already writes the real role onto `shell_control.users` at invite-claim time, and a brand-new self-signup with no invite falls through to the unchanged default. Confirmed the `ON CONFLICT` branch was extended to self-heal existing wrong-role rows.
- Waited for eq-cards CI on PR #188 to clear pending (backgrounded poll, `gh pr checks`), then merged on Royce's "merge it once CI's green" go — squash `dc7a5471`.
- On Royce's further go, drift-checked migration `0111` against live jvkn (exact match to the `0056` pre-image), replay-tested the DDL in a rolled-back transaction, then applied it live via Supabase MCP `apply_migration` (ledger `20260730081055`). Post-apply verified via `pg_get_functiondef` that both the role-read fix and the self-heal `ON CONFLICT` clause are live — re-confirmed again at session close by re-querying `pg_get_functiondef` directly rather than trusting the registry's own account of it.
- Updated `eq-context/system/worktree-registry.md` (two commits: merge, then live-apply) and `eq/changelog/eq-cards.md`, both on Royce's "update the worktree registry once it's merged" instruction.

## Decided
- Royce: merge PR #188 only (not merge+apply in one go) — apply-to-live was a separate explicit go given after the merge.

## Deferred
- None new. The one remaining open piece from §11.2/§11.3 (an admin-initiated path to grant a second-tenant membership directly from the Shell admin UI, rather than only via a Cards-initiated claim/request) was already backlog before this session and isn't part of this fix's scope.

## Notes
- Confirmed live: 10+ real named managers/supervisors (e.g. Huon Henne, Adam Khan, Jack Cluff) currently carry a wrong `'employee'` personal-membership row. These self-correct the next time each person opens Cards — not retroactively, no backfill run.
- This is a clean example of Rule 0.5/0.6 paying off: had the git-branch/worktree-registry check been skipped, this session would have rebuilt a migration that already existed build-ready on a branch, rather than reviewing, merging, and applying it.

---

# Session 2026-07-30 — Archived staff leaking into the AI briefing; Cards email-update bug fixed and shipped end-to-end

## Built
- Royce reported Huon Henne, archived, still showing up in the AI dashboard summary. Traced to `briefing-engine.ts`: `loadNameMaps()`'s staff query and `fetchLicenceExpiries()`'s licence query both skipped the `active` filter that a third function in the same file (`deriveCapacity`) already applied correctly — so an archived worker's still-active licence row kept generating an "expiring soon" signal with their name resolved and attached. Fixed both, `tsc --noEmit` clean, committed to branch `claude/archived-huon-dashboard-summary-ac5190`, pushed, opened eq-shell [PR #1117](https://github.com/eq-solutions/eq-shell/pull/1117). **Not yet merged.**
- Royce then reported a second, unrelated bug: added Zemi Asri to Cards, he entered an email, it never reached core. Traced to `workers-canonical-sync`'s "fill-if-missing" coalesce on `staff.email`/`phone` (added 2026-07-28 to fix a different clobber, Ben Ritchie) — it can't distinguish "a Shell operator deliberately set this" from "this column just happens to be non-null," so once any value landed (even an old placeholder), no later Cards value could ever overwrite it. Confirmed live: both `ehow.app_data.staff.email` and `jvkn.public.workers.email` for Zemi were stuck on `zemi.asri@sks.com.au`.
- Ran the full `/decide` six-step protocol before building (cross-plane, genuinely uncertain value call) — recommendation was to build a provenance lock scoped to both email and phone, which Royce then had built.
- Built: migration `0224_staff_contact_provenance_lock.sql` (adds `email_locked_by_shell`/`phone_locked_by_shell` to `app_data.staff`), `entity-patch.ts` sets the lock only when a Shell operator explicitly edits that field via the Staff page, `workers-canonical-sync/index.ts` merge now checks the lock instead of null-ness. Branch `claude/staff-contact-provenance-lock`, PR [#1118](https://github.com/eq-solutions/eq-shell/pull/1118), all CI green.
- On Royce's explicit instructions, in sequence: pushed both branches + opened both PRs; dispatched `tenant-migrate.yml` for migration 0224 (fleet-wide, off the PR branch before merge — established pattern) — applied clean to both ehow and zaap, live-verified via direct query; merged PR #1118 (`43e2c42`); redeployed the `workers-canonical-sync` Edge Function to jvkn (version 11, same `verify_jwt: false` auth preserved).
- Noted in passing: migration `0224_staff_contact_provenance_lock.sql` collides in number with `0224_field_importance_overrides_rls.sql` (merged same day via #1115, unrelated session) — different filenames, so the ledger and drift-check are unaffected; left as-is rather than renumber after it was already applied live.

## Decided
- Royce: build the contact-provenance-lock fix as scoped in the `/decide` call (both email and phone, not email-only).
- Royce: push both branches and open PRs — done.
- Royce: dispatch `tenant-migrate.yml` for 0224 — done, fleet-wide.
- Royce: merge #1118 and deploy the edge function — done, both live.

## Deferred
- **Merge [PR #1117](https://github.com/eq-solutions/eq-shell/pull/1117)** (archived-staff briefing fix) — written and typechecked, not yet on `main`. _(added 2026-07-30)_
- **Zemi Asri's own `staff.email` row is still the stale value** (`zemi.asri@sks.com.au`) — the shipped fix stops the *next* occurrence, it doesn't retroactively correct his existing row. Needs a fresh Cards edit (now unlocked, will take) or a direct Shell Staff-page correction. _(added 2026-07-30)_

## Notes
- Two independent bugs surfaced in the same `briefing-engine.ts`/`workers-canonical-sync.ts` pass — both were the same shape of miss (a filter/provenance check present in one code path, missing in a sibling one) rather than related to each other.
- Migration 0224 was dispatched against the PR branch ref *before* merging (`tenant-migrate.yml --ref claude/staff-contact-provenance-lock`), the same established workaround documented in memory `eq-shell-drift-gate-dispatch-order` — confirms the pattern generalizes beyond the drift-gate-timing use case it was first written for.

---

# Session 2026-07-30 (cont.) — Followed up on the two Report Settings background sessions; both resolved

## Built
- Royce asked to check what the two background sessions spawned earlier today (Work Order Details / Field Run-Sheet Report Settings gaps) had actually produced, rather than assume. Used `list_sessions`/`search_session_transcripts`/`list_events` to read each session's real transcript directly instead of guessing from the spawn prompt.
- **Work Order Details session**: built and shipped [PR #649](https://github.com/eq-solutions/eq-service/pull/649) (merged `35ab9ad`) — wired `report_show_sign_off` into Work Order Details (previously honoured none of the four toggles), and separately found + fixed PM Check Report silently ignoring `report_complexity` (Summary tier now genuinely drops the itemised checklist for a pass/fail count). Resolved the open "should these grow full sections" design question by correcting the settings-page copy and `docs/FEATURES.md` instead of inventing sections that don't fit a fixed-layout working document — verified by reading the actual diff, not just the PR title.
- **Field Run-Sheet session**: confirmed the identical gap in code, asked Royce directly (via `AskUserQuestion`) whether Field Run-Sheet should grow real sections or stay fixed-format, got no reply, and closed itself with zero changes made — a clean "asked, no answer, did nothing" outcome, not a stalled or lost task.
- Rewrote the `eq/pending.md` section covering this whole thread to reflect the real, finished state (both sessions' actual outcomes, not the "still mid-build" snapshot written at session-close time before either had finished) and archived it wholesale to `eq/pending-archive.md` — zero open items remain.

## Decided
- None new — the design question ("should Field Run-Sheet/Work Order Details grow full report sections") was left standing as Royce didn't answer it; treated the shipped documentation-only resolution as the accepted default per this session's own framing to him, reversible on request.

## Deferred
- None new. Reopening Field Run-Sheet's design question (building real Cover/Contents/Executive-Summary sections) remains available on request — not currently blocking anything.

## Notes
- Real lesson for future `spawn_task` follow-up: two tasks spawned from the same prompt pattern can have completely different outcomes (one ships code, one asks a question and gets silence) — always read the actual session transcript/PR before reporting "what happened," never infer from the spawn prompt or task title alone.

---

# Session 2026-07-30 (cont.) — Royce pushed back on the 0111 role fix; verified it was data hygiene, not a bug fix

## Built
- After eq-cards migration 0111 (personal-membership role fix, PR #188) was live on jvkn, Royce asked "isn't the personal membership role agnostic?" — a direct challenge to the fix's own justification.
- Traced all three real consumers of role/tenant claims instead of re-asserting the original reasoning: `custom_access_token_hook` (the GoTrue hook that stamps `app_metadata.eq_role` into every native Supabase JWT — what RLS on eq-canonical actually reads) sources `eq_role` from the flat `shell_control.users.role`, never from `user_tenant_memberships`. `mint-supabase-jwt.ts` (what Cards calls to refresh its token) does the same. `tenant-resolution.ts` (Shell's phone-OTP login) does read `user_tenant_memberships.role` per-tenant, but explicitly filters out `__personal__` before that read happens, because the `__personal__` tenant record itself is `active=false`.
- Conclusion: the personal-tenant membership role is confirmed unread by every enforcement path. The fix (0111) was correct and harmless, but the original write-up ("a real inconsistency worth a follow-up fix") overclaimed functional/security impact it doesn't have — it's data hygiene.
- Ran a steelman (leave-applied vs. revert) at Royce's request rather than defaulting to either. Leave-applied won: reverting would spend a second live auth-schema migration solely to reintroduce a value now known to be wrong, for zero functional gain, right after finding 13 real people (including Royce) already carrying that wrong value for a month because nobody was checking it — "nobody reads it" is exactly how that happened the first time.
- Royce's call: leave the fix applied. Corrected the overclaim in `eq/identity/IDENTITY-MODEL.md` §11.3 and `system/worktree-registry.md` so neither document misrepresents this as a functional/security fix going forward.
- Re-checked the list of 13 affected people twice more during the session (once after asking specifically about Jack Cluff, once for the full list) — none have self-healed yet as of session close. Expected: the self-heal only fires when each person next opens Cards, not retroactively.

## Decided
- Royce: leave migration 0111 applied as-is (harmless + more correct even though currently unread) rather than revert it — decided after an explicit steelman of both options, not by default.

## Deferred
- None new.

## Notes
- Real lesson: I shipped and applied a live auth-schema change based on a write-side pattern-match (0038/0072 write the same role to both membership rows, so I assumed 0055/0056 "should" too) without ever checking what reads the value. The fix itself turned out fine, but the justification was unverified until Royce questioned it directly — worth checking the read side before framing something as a "bug" next time a similar case comes up.

---

# Session 2026-07-30 (cont.) — Second copy of the false __personal__-retired claim found and fixed; both spawned identity tasks confirmed landed

## Built
- Royce asked to "check on those two tasks" (role-hardcode fix, v2-bump question) and "is this everything?" — used `list_sessions`/`get_session` to check real status rather than assume, then surfaced two more loose threads from the §11.3 reconciliation that hadn't been spun up: the admin-initiated cross-tenant-grant backlog item (genuine, not a bug), and a second, independent copy of the same false "`__personal__` tenant is retired" claim living inside eq-cards migration `0076`'s own comment.
- Royce said "fix item 2" — ran `/brief eq-cards` (Rule 0.6 gate), confirmed `eq_cards_auto_provision()` runs automatically right after OTP verification (`otp_screen.dart`, not gated behind a manual button as a nearby docstring implies) before touching anything, then corrected `0076_recycled_phone_review_guard.sql`'s comment: retracted the false "retired" claim and documented that this guard only withholds the cross-tenant graft, not personal-tenant access (the row still gets created moments later via `eq_cards_auto_provision()` per `0055`). Comment-only, no logic change. Pushed direct to eq-cards `main`, commit `60b21c3`.
- While briefing eq-cards, found `origin/main` already 1 commit ahead with the role-hardcode fix (PR #188/migration `0111`, commit `dc7a547`) — confirmed independently merged before this session got to it, nothing left to do there.
- When notified the v2-bump background task had ended, checked its actual commits (`e888953`, `89b9ca4`) rather than re-deriving from memory — confirmed it closed the "stay v1 vs. bump to v2" question correctly (checked the doc's own trigger conditions, checked whether the module-pinning mechanism the rule assumes even exists — it doesn't, anywhere — brought both back to Royce as a real either/or) and separately corrected the 0111 fix's overclaimed impact after Royce's own challenge, matching what was independently found earlier the same day in this session's sibling thread.
- Updated `eq/changelog/eq-cards.md` and archived the fully-closed item straight to `eq/pending-archive.md` (no open items remained, so skipped a live `pending.md` entry per the archive rule).

## Decided
- Royce: fix the `0076` comment now (item 2 of the two surfaced loose threads) — done. Item 1 (admin-initiated cross-tenant grants) left as standing backlog, not spun up.

## Deferred
- **Admin-initiated cross-tenant grants** — still no way for a Shell admin to grant a user membership in a second tenant directly; today it only happens via Cards (invite claim or a worker-initiated access request). Genuine backlog per §11.2, not a bug. _(added 2026-07-30)_

## Notes
- Confirms the same lesson as the sibling 0111-overclaim session, independently: the same false substrate claim ("`__personal__` is retired") had propagated into two separate places (the identity doc and an unrelated eq-cards migration comment) before anyone checked live data — worth remembering that a wrong belief rarely lives in just one file once it's been repeated for a month.

---

# Session 2026-07-30 (cont.) — security-audit.yml: dead project ref fixed, gate had been silently broken a month

## Built
- Traced the reported 400 "Resource has been removed" to `security_audit.py`/`rls_probe.py`'s hardcoded `"eq-solves-field": "ktmjmdzqrogauaevbktn"` — that Supabase project was deleted 2026-07-04, confirmed both via live `list_projects` and independently via eq-field's own `DATA-PLANES-SOURCE-OF-TRUTH.md`. EQ Field has no standalone Supabase project today; its data lives on zaap/ehow, both already covered. `gh run list` confirmed every scheduled run since at least 2026-06-28 had been failing here.
- Removed the dead ref from both scripts, corrected the workflow's stale "4 EQ projects" comment, re-ran via `workflow_dispatch` — green, `ERROR 0` across the 3 remaining projects. Logged as SEC-17 in `ops/security-register.md`, closed.
- Royce asked for the follow-up noted in that closure: `security_audit.py`'s advisor-audit loop used to hard-abort on the first `HTTPError` (that's exactly what let one dead ref hide the whole gate's results for a month) — reworked `main()` to catch per-project fetch failures, report them, and keep checking the rest; the run still fails overall on any fetch failure or new ERROR finding, just no longer at the cost of every other project's result.
- Also added `eq-receipts` (`bgrhqvmvzgotxzjneskv`, live since 2026-07-21) to `security_audit.py`'s scope — never previously covered by this gate. Confirmed green with it included (`ERROR 0 · WARN 0 · INFO 0`).

## Decided
- Royce: extend the error-isolation fix to cover `eq-receipts` too, in the same pass.

## Deferred
- **`eq-receipts` not added to `rls_probe.py`** — needs its publishable/anon key. Supabase MCP's `get_publishable_keys` was blocked by the Claude Code auto-mode classifier this session; checked eq-receipts' own frontend source and the connected Netlify MCP for another read path — neither exposes it (key lives only in eq-receipts' gitignored `.env`, and the Netlify MCP surface here has no env-var-read operation). Royce needs to pull it from the Supabase dashboard (Project Settings → API) before this can finish. _(added 2026-07-30)_

## Notes
- **Shared-checkout collision, root cause confirmed live:** this session's `git add` + `git commit` for the error-isolation fix silently produced "no changes added to commit" — the 3 staged files had already been swept into a concurrent session's commit (`694ffd0`, "feat(hooks): incident-claims registry + session_start.py cross-reference") moments earlier, almost certainly via that session's own broad `git add`. Confirmed via `git show 694ffd0 --stat`: it lists exactly the 3 files this session was editing, alongside that session's own unrelated hook/registry files. The code is correct and live on `main` (verified `git diff HEAD` against the working tree = empty) — only the commit message misattributes it. Left history alone rather than rebase/amend a shared branch the other session was still actively building on (`hooks/session_start.py` had further uncommitted edits at the time). Same root-cause family as the fake-worktree collisions already documented in `system/worktree-registry.md`, but on the root checkout itself, not a worktree — worth a mention there if this recurs.

---

# Session 2026-07-30 (cont.) — Chat's 3 discipline recommendations: SEC-10 closed, DDL-migration Stop gate built, incident-claims registry built

## Built
- Rated /clothes-skill discipline 82/100 with 3 prioritized recommendations (from an earlier Chat session); ran a steelman pass here that reordered them by value, not cost — build #1 first (folding in the settings.json wiring check as its opening step), not #3 standalone.
- Verified `session_end.py`/`guard.js` wiring live in `~/.claude/settings.json` (Stop hook + PreToolUse matcher already covers `execute_sql`/`apply_migration`) — confirmed armed, not assumed.
- Closed SEC-10 in `ops/security-register.md`: Royce rotated `ANTHROPIC_API_KEY`/`RESEND_API_KEY` on sks-nsw-labour himself and re-stored `is_secret:true`; cleared `EQ_SECRET_SALT`'s `dev`-context plaintext leftover via the Netlify MCP without the assistant ever reading/entering the real value. Committed `ef41f55`.
- Built the DDL-without-migration Stop gate: `guard.js` now tags every `gate-sql` log line with session_id+cwd; new `~/.claude/hooks/ddl_migration_gate.py` Stop hook flags (never blocks) a session that applied live DDL via `apply_migration`/`execute_sql` with no matching migration file committed or even present uncommitted in that repo. Tested against 4 scenarios in a throwaway repo (missing/uncommitted/covered/wrong-session). Machine-local config (`~/.claude`), not git-versioned — same as `guard.js` itself.
- Found a pre-existing, unrelated `guard.js` bug while testing (`selftest.js`'s inline-password case denies when it should warn-allow) — confirmed pre-existing via revert-and-rerun, spun off as background task `task_19efbbf8` rather than folding an unscoped fix in; Royce started it independently in its own session.
- Built the incident-claims registry (`system/incident-claims.md`) + wired `session_start.py` to cross-reference it against the digest's Needs You list every session (`CLAIMS` line), flagging possible duplicate investigation by ID (`SEC-N`/`F-N`), with 6h staleness handling. Tested (empty, fresh match, stale match, wrong session). Committed `8087b7a`.
- Mid-task, collided with the concurrent security-audit.yml session logged directly above in the shared `C:\Projects\eq-context` checkout (same index, same working tree) — this session's first commit attempt accidentally captured that session's in-progress SEC-17 files under this session's commit message (694ffd0). Recovered cleanly via a fresh throwaway clone (no content lost either side), then fast-forwarded the shared checkout without touching the other session's uncommitted work.

## Decided
- Royce confirmed the revised build order (#1 DDL-migration gate first, folding in the settings.json verification step, rather than treating it as its own standalone first task).
- Royce confirmed doing #2 (incident-claims registry) next, same session.

## Deferred
- SEC-10's own remaining follow-up — sweep other Netlify projects (eq-shell, eq-solves-service, eq-field) for the same `is_secret:false` plaintext-credential pattern. _(added 2026-07-30, see `ops/pending.md`)_
- The pre-existing `guard.js` scan-secrets bug (`task_19efbbf8`) — spun off, running independently in its own session as of this close.

## Notes
- Live-confirmed a second real instance of the `eq-context-checkout-concurrent-writer` risk already on record (memory + `system/failures.md`) — reinforcement, not a new finding, and independently corroborated from the other side by the session logged directly above (its own Notes section describes the same collision from its perspective). Safe recovery pattern used here: read the exact final file content, clone fresh from origin, reapply there, diff-verify byte-identical, commit+push from the clean clone, then fast-forward the shared checkout.
- The new `incident-claims.md`/`CLAIMS` check is a partial mitigation for a *related* class of collision (duplicate investigation of the same finding) but doesn't cover simultaneous git writes to the same file — that's a different, lower-level risk, still unaddressed by this session's fixes. Worth a mention in `system/worktree-registry.md` or a dedicated note if this recurs, per the other session's own suggestion.

---

# Session 2026-07-30 (cont.) — eq-receipts anon key retrieved, rls_probe.py finished, SEC-17 fully closed

## Built
- Royce asked to pull the eq-receipts anon key and finish `rls_probe.py`, following on from the earlier security-audit gate fix this session. Retried the Supabase MCP `get_publishable_keys` call that had been blocked earlier — it succeeded this time, confirming the earlier block was a one-off classifier hiccup, not a hard rule.
- Added `eq-receipts` to `rls_probe.py`'s `PROJECTS`, covering all 6 tables (`entities`, `categories`, `receipts`, `line_items`, `extraction_jobs`, `exports` — all RLS-enabled per `list_tables`). Ran the probe scoped to just this project before pushing — all 6 came back `200 empty`, no anon-readable rows.
- Updated the SEC-17 register entry to fully closed. Both security scripts now cover all 4 EQ projects.
- Committed (`707c102`) and pushed clean — no collision this time. Re-ran `security-audit.yml` via `workflow_dispatch` (run [30528180869](https://github.com/eq-solutions/eq-context/actions/runs/30528180869)) — green, `eq-receipts` now showing in both the RLS probe and advisor-audit output.

## Decided
- Royce: pull the key now and finish the job, rather than leave it as a standing "needs Royce" item.

## Deferred
- None new.

## Notes
- Another shared-checkout collision surfaced while updating `ops/pending.md` for this close: a concurrent session had appended an unrelated line (`SEC-9/10/12 actual rotation/re-store...`) directly under this session's own "Security-audit CI gate" header, inside the same section. Left that line untouched (not mine to judge or move) and only removed this session's own item, now resolved — the section header is left slightly mismatched with the one remaining line, but the content itself is exactly as the other session wrote it, not guessed at or restructured. Same collision family already logged earlier today in this file's own Notes sections.

---

# Session 2026-07-30 (cont.) — Netlify plaintext-secret sweep confirms two live database master keys exposed in production (SEC-18)

## Built
- Royce asked to check the orphaned `SEC-9/10/12` line from the prior close — traced it to a stale duplicate: SEC-10 and SEC-12 were both already closed, only SEC-9's rotation remained genuinely open (already tracked in the register's own "rotate whenever convenient" section). Removed the stray line as redundant, not archived (nothing left to preserve that wasn't already elsewhere).
- Royce then asked to sweep every other Netlify project for the SEC-10 plaintext-secret pattern. Listed all 10 projects on the team account and checked the 8 not already covered by SEC-10 (sks-nsw-labour) or SEC-12 (eq-shell): eq-service, eq-field, eq-receipts, eq-cards, sks-comms, eq-core-design, life-tracking, knx-job-folder.
- Found a severe live exposure: **eq-cards** has the control-plane's `SUPABASE_SERVICE_ROLE_KEY` (ref `jvkn`/eq-canonical) stored `is_secret: false` in all four contexts including production — full database-bypass access to every tenant's identity/credentials, sitting in plaintext on the app that takes real public signup traffic. **eq-service** has the equivalent for the live SKS database (`CANONICAL_SERVICE_ROLE_KEY`, ref `ehow`), same exposure. Both worse than any prior SEC finding — actual database master keys, not a signing secret or a one-off chat paste.
- A concurrent session had independently run the identical sweep and logged the core of it as SEC-18 moments earlier. Confirmed every item that session found and added several more on the same three sites (`EQ_SERVICE_JWT_SECRET`/`EQ_SERVICE_HANDOFF_KEY`/`EQ_SERVICE_API_KEY` on eq-service; `EQ_SECRET_SALT`/`EQ_SESSION_SALT` on eq-cards; `EQ_FIELD_HANDOFF_KEY` on eq-field) and closed out the "not yet checked" list — sks-comms/eq-core-design/knx-job-folder/life-tracking have zero env vars configured, eq-receipts holds only a public URL and an already-secret anon key. Root cause pattern: SEC-12's fix (2026-07-27) only touched the one copy of each shared secret living on eq-shell's own Netlify project — the identical values duplicated onto the consuming apps' own Netlify configs (needed so each app can verify tokens the others issue) were never touched.
- Updated `ops/security-register.md`'s SEC-18 row with the full combined inventory and replaced the generic `ops/pending.md` sweep item with SEC-18-specific action items. Did not attempt any remediation — a same-value re-store is credential handling, confirmed blocked for Claude Code by design regardless of permission (same block SEC-12 hit).

## Decided
- Royce: run the sweep now rather than leave it queued.

## Deferred
- **Re-store every flagged secret as masked (same value, not rotation)** on eq-service/eq-field/eq-cards' Netlify projects — manual-hands-only, Royce via the Netlify dashboard or CLI. _(added 2026-07-30)_
- **Royce's call: does `CANONICAL_SERVICE_ROLE_KEY` (ehow) or `SUPABASE_SERVICE_ROLE_KEY` (jvkn) warrant an actual rotation**, not just re-masking, given a leak would grant full database bypass access. _(added 2026-07-30)_

## Notes
- Two sessions discovering the identical live P0 independently within minutes of each other, from the same "check other Netlify projects" prompt, is itself worth noting — the finding merged cleanly (no register conflict, both push attempts required only a nightly-refresh merge, not a real conflict), but it's a reminder that the same shared-checkout concurrency this file has logged all day also applies to security-relevant discovery work, not just doc edits.

---

# Session 2026-07-30 (cont.) — eq-solves-intake Tidy tab: dropdown + real save wired up; live RPC whitelist gap found and closed

## Built
- Royce challenged the value of Intake off a screenshot of the Customers Tidy tab's "Data gaps" table: "no dropdown when you edit, then you can't save it here anyway... what is the value of intake?" Both turned out to be real, confirmed bugs, not a misreading of the UI.
- Traced `EntityDrillDown.tsx`: the inline Edit control always rendered a plain text input, even for closed-enum fields like `customers.type` (lead/prospect/active/churned) — schema already carried the enum, nothing read it. Separately, `commitEdit`/`acceptSuggestion` (the Edit/Suggest handlers) only ever mutated local React state (`setRows` + a `hasLocalEdits` flag) — neither ever called the one real save path, `commitTidyFixes`. The only functioning save path was a *different* button (bulk auto-fixes), never wired to this table.
- Fixed both: `GapItem` gained an `allowed_values` field (from the validator's `field_enum_invalid.allowed`, with a schema-enum fallback via a new `getFieldEnumValues()` export, so a blank required-enum gap gets a dropdown too, not just an invalid-value one); the Fix column now renders a `<select>` when present. `commitEdit`/`acceptSuggestion` now call `commitTidyFixes` for real (with a saving/error state), falling back to the old local-only behaviour only when no live Supabase client is present (e.g. a static preview).
- While verifying the fix would actually work end-to-end, checked the live RPC rather than trusting the repo's `sql/` files (this repo's own README: "merge ≠ applied") — found `eq_tidy_commit_fixes` on ehow had already drifted ahead of `sql/049` to a newer, better-shaped version (`sql/062`'s per-`table:field` whitelist), confirmed via `pg_get_functiondef`. Neither `customers:type` nor `sites:site_type` — both real closed-enum fields the tidy scan already flags as gaps — were in that whitelist. Even a perfect frontend fix would have silently no-opped saving either field. Cross-checked every schema enum field against the whitelist (only these two were missing; `staff.employment_type`/`licence.state` already covered) and confirmed both columns exist live (`text` type) before writing anything.
- Wrote `sql/063_tidy_commit_fixes_enum_fields.sql` (adds `customers:type` + `sites:site_type`) and, on Royce's explicit go, applied it live to ehow via Supabase MCP — verified via `pg_get_functiondef` that both entries are live and the migration ledger row is correctly stamped.
- Full verification: `@eq/intake` and `@eq/intake-demo` both `tsc --noEmit` clean, `@eq/intake`'s test suite green (123/123, incl. 2 new tests asserting `allowed_values` populates on both the invalid-value and blank-required cases).
- **Process error, caught and fixed before commit:** every file edit this session landed in the *root* `eq-intake` checkout (branch `main`, shared with other concurrent sessions) instead of the assigned worktree (`app-polish-improvements-8c03dd`, branch `claude/fancy-menu-clarification-288ca8`) — used root-style absolute paths throughout instead of the worktree path, all session. Caught it before session close, not after. Root already carried ~8 unrelated pre-existing dirty files from other sessions/tooling, untouched throughout. Recovered via `git diff`/`git apply` (moved my 6-file diff + the new SQL file into the correct worktree, verified the patch applied clean and the full build/test suite still passed there) then, on Royce's explicit go, `git stash push -- <my 6 files>` on the root checkout to remove exactly the duplicate edits, leaving every unrelated pre-existing dirty file in root untouched. Confirmed root's status matches its pre-session state exactly.
- Spawned a follow-up task (`task_858653b3`) to re-vendor eq-intake's `eq-platform` into eq-shell (same pattern as PRs #1094/#1106/#1113) — this fix won't appear at core.eq.solutions until that lands. Royce started it in a separate session; still running as of this close.
- Not committed/pushed/PR'd this session — Royce didn't ask for that step, only "fix both now" and "apply to ehow now" (the live migration). Code sits verified and ready in the correct worktree.

## Decided
- Royce: "Yes, fix both now" — wire Edit/Suggest to real persistence + add the enum dropdown.
- Royce: "Yes, apply to ehow now" — live-apply migration 063.
- Royce: stash (not discard) the 6 duplicate files from the root checkout, leaving unrelated dirty files alone — explicit go after being asked, since the classifier flagged the more direct `git checkout --` revert as risky.

## Deferred
- **eq-shell re-vendor of eq-intake/eq-platform** — needed before this fix is visible at core.eq.solutions/sks/intake. Task `task_858653b3` running independently in a separate session as of this close. _(added 2026-07-30)_
- **Committing/pushing/opening a PR for this session's eq-intake code changes** — not requested this session; changes are verified (build/typecheck/tests all green) and sitting correctly in the `app-polish-improvements-8c03dd` worktree, ready for a future session to commit. _(added 2026-07-30)_

## Notes
- Root cause of the file-location mistake: used absolute paths rooted at `C:\Projects\eq-intake\...` throughout instead of the assigned worktree path `C:\Projects\eq-intake\.claude\worktrees\app-polish-improvements-8c03dd\...`. Worth double-checking the working-directory prefix on the *first* file operation of a worktree session, not just trusting the stated "primary working directory" — nothing enforces that Read/Edit/Write/Bash paths actually stay under it.
- The root checkout being already dirty with ~8 unrelated files (`.armada/config.json`, `.claude/settings.local.json`, several `vitest.config.ts`, an untracked `CLAUDE.md`, `eq-platform/apps/`) before this session touched anything is itself worth flagging — matches the same "root checkout found dirty" pattern the worktree registry has logged for this exact repo before (2026-07-27 round 2: "the eq-intake root checkout (detached HEAD, 6 modified + 3 untracked)"). Recurring, not a one-off.

---

# Session 2026-07-30 (cont.) — SKS PIN Management fix scoped, brief drafted; closed before Royce confirmed

## Built
- Nothing landed in code. Royce (via the standing brief) described a real bug: `renderPinList()` in `sks-nsw-labour`'s `scripts/people.js` checks `p.pin` on `STATE.people`, but the bulk load never fetches `pin` (removed from the select list in v3.10.106 as a deliberate security fix, per project memory `loadfromsupabase-resilient-sync`). Result: the PIN Management modal shows "No PIN" for every staff member except whoever had their PIN set earlier in the same session — misleading for a manager checking who still needs one.
- Ran the Rule 0.6 session gate for sks-nsw-labour (digest read, worktree registry checked — none active, git state checked): branch `claude/loadfromsupabase-resilient-sync` is clean, 12 commits behind `origin/main` (main is at v3.10.105), but none of those 12 touch `people.js` or PIN management — confirmed by skimming titles, not assumed. Concluded any real fix should branch fresh off `origin/main` rather than build on this stale branch.
- Drafted the fix approach (matching the brief's own suggestion): make `openPinManagement()` async, do a narrow on-demand `people?select=id,pin&group=in.(Apprentice,Labour Hire)` fetch (same pattern as the staff-timesheet PIN gate already in `auth.js`), cache the result, have `renderPinList()` read from that cache instead of `p.pin`. Confirmed via memory/grep that the DB's `has_pin` column is separately known-stale (32/35 mismatched, per existing project memory) and correctly out of scope for this fix.
- Presented the full session brief (health/worktree/git state, what exists, what's broken, what changes, constraints) and was waiting on Royce's confirmation per this repo's hard rule (no edits without explicit authorisation) when `/close` arrived. One `Edit` attempt was made before the brief was drafted and correctly blocked by the local brief-gate hook — no code was touched at any point.

## Decided
- None — session closed before Royce responded to the brief.

## Deferred
- **Build the PIN Management fix** as scoped above — logged in `sks/pending.md` (2026-07-30) so the next session can pick it up without re-deriving the investigation. _(added 2026-07-30)_
- **The stale `has_pin` DB column** — separate, lower-priority follow-up (needs a sync trigger before it's trustworthy), also logged in `sks/pending.md`. _(added 2026-07-30)_

## Notes
- Confirms the brief-gate is working as designed: it blocked a same-turn edit attempt on a repo governed by an extra-strict "no edits without Royce naming this clone" rule, forcing the gate to run first. No wasted work — the investigation and fix design are fully captured in `sks/pending.md` for whichever session builds it next.

---

# Session 2026-07-30 — Admin Users "Deactivated" tab fixed (dead server-side filter)

## Built
- Royce flagged (via a saved note) that `public.eq_list_tenant_users()` — the RPC behind `AdminUserList.tsx` — had `AND u.active = true` in its `WHERE` clause, so the page's "Deactivated" slicer tab could never show a row: every user reaching the client was already pre-filtered to active-only. Confirmed live on jvkn via `pg_get_functiondef` before touching anything — deployed body was byte-identical to the migration file, same filter.
- New migration `2026_07_30_fix_eq_list_tenant_users_active_filter.sql` drops the `u.active = true` filter (kept `m.active = true` — active tenant *membership*, unrelated — and the Cards-worker-stub email exclusion, also unrelated).
- **Same 42P13 gotcha as this session's earlier `0223` migration** (see entry above): plain `CREATE OR REPLACE FUNCTION` was rejected live ("cannot change return type of existing function") despite an identical `RETURNS TABLE` column list. Applied as `DROP FUNCTION` + `CREATE FUNCTION` instead, per Postgres's own hint. This is now the second time today this exact error hit an unrelated function — worth treating as the default expectation for any control-plane RPC edit, not a one-off.
- Applied to live jvkn via Supabase MCP `apply_migration`. Post-apply verified via `pg_get_functiondef` (filter gone, rest byte-identical) and a direct query confirming real effect: `sks` tenant has 2 deactivated users that were being silently dropped and now aren't.
- Updated `supabase/CONTROL-PLANE-LEDGER.md` (new table row + changelog entry) — this repo's control-plane tree has no CI apply path, so the ledger is the only record that the migration file's live-apply actually happened.
- Branched fresh off `origin/main` (`claude/fix-eq-list-tenant-users-deactivated`) rather than building on the session's pre-existing checkout, which was on a stale, already-merged branch (`claude/field-importance-overrides-rls`, 10 commits behind).
- Pushed, opened [eq-shell PR #1124](https://github.com/eq-solutions/eq-shell/pull/1124), watched CI to green (typecheck·test·lint, schema drift, gitleaks, migration ledger hygiene, Netlify deploy preview all passed), squash-merged on Royce's standing "merge once CI is green" instruction. Branch deleted on merge.

## Decided
- Royce approved the plan as drafted (Plan Mode), then asked to push + open the PR, then to merge once CI turned green — all three followed exactly as given.

## Deferred
- **Royce to confirm live**: open `/sks/admin/users`, click the "Deactivated" tab, confirm the 2 known deactivated SKS users actually appear. Logged in `eq/pending.md`. _(added 2026-07-30)_

## Notes
- No frontend change was needed — `AdminUserList.tsx`'s client-side "Deactivated" filter (`filter: (u) => !u.active`) was already correct; it had simply never been given a deactivated row to match, ever, since the function was created.
- The DB fix went live *before* the PR merged (control-plane convention — hand-apply via Supabase MCP, PR is the source-control record after the fact, not the trigger). Anyone reviewing this PR after the fact should know the behavior change was already in production by the time CI ran.

---

# Session 2026-07-30 — Suppliers polish, audit-log noise root-caused + fixed, Suite activity tab shipped

## Built
- **Suppliers page**: enabled the Table component's existing `columnToggle` popover (never wired up), removed the leftover `pageSize: 20` pagination cap so the table scrolls freely, added a "Click to reveal"/"Click to hide" hover tooltip on the masked password button (desktop + mobile). eq-shell [PR #1120](https://github.com/eq-solutions/eq-shell/pull/1120), merged.
- **BETA badge removed from EQ Cards and NSW Comms** (`HubLayout.tsx` sidebar config + `TenantHome.tsx` home tiles) — both apps are out of beta. Same PR #1120.
- **Audit-log noise, root cause #1**: `workers-canonical-sync` (jvkn Edge Function) wrote a full-column update — bumping `updated_at` — on every Cards webhook and every nightly reconcile pass, regardless of whether anything actually changed. Fixed to skip the write entirely when the computed merge matches what's already stored; real drift-correction writes still fire. Migration `0225` split each canonical-spine table's trigger into per-event triggers so the UPDATE one could carry a `WHEN (OLD IS DISTINCT FROM NEW)` guard. eq-shell [PR #1121](https://github.com/eq-solutions/eq-shell/pull/1121), merged, migration dispatched fleet-wide, Edge Function redeployed to jvkn as v12.
- **Audit-log noise, root cause #2 (found by testing live)**: fired a genuine no-op UPDATE directly at a real staff row after 0225 shipped — a fresh audit row still landed. Root cause: all 5 canonical-spine tables already have their own `touch_updated_at` trigger that bumps `updated_at` unconditionally on every UPDATE, so 0225's row-level WHEN guard was always true and never actually filtered anything. Real fix: migration `0226` moved the check inside `fn_audit()` itself, stripping `updated_at`/`created_at`/`updated_by`/`created_by` before comparing old vs new. eq-shell [PR #1123](https://github.com/eq-solutions/eq-shell/pull/1123), merged, dispatched fleet-wide. Re-ran the same live no-op test post-dispatch — confirmed suppressed (count held at 33) — then confirmed a real edit still logs (33 → 35, reverted after).
- **New "Suite activity" tab** on the Activity log page, reading `app_data.canonical_events` directly and rendering each event type (shift rosters, quote sends, staff approvals, licence reviews, overdue maintenance checks) as a plain-English sentence server-side. eq-shell [PR #1126](https://github.com/eq-solutions/eq-shell/pull/1126), merged. Read-layer only, no migration.

## Decided
- Royce: "commit this and merge" (Suppliers + BETA) → committed, PR opened, merged same turn.
- Royce: "merge #1121" / "dispatch the migration and redeploy the function" → both done.
- Royce: "click through live and confirm the noise is gone" → couldn't literally log into core.eq.solutions (no session in this environment); verified against the live DB instead, which is what surfaced the 0225 bug.
- Royce: "yes, do that check then build A" → checked `workers-canonical-sync` against the just-merged provenance-lock fix (#1118) before writing the no-op skip, confirmed no conflict.
- Royce: "merge and dispatch" (0226 fix) → done, re-verified live.
- Given a choice between (a) a new "Suite activity" tab, (b) just stopping the redundant `entity.patched` writes, (c) something else — Royce picked (a), the recommended option.
- Royce: "merge #1126" → done.

## Deferred
- **Royce to click through live**: Suppliers (Columns button, free scroll, password tooltip) and the new Suite activity tab (sentences read sensibly against real SKS data). Logged in `eq/pending.md`.
- **Cross-repo attribution** (Field/Service/Cards' own sync jobs still write with `source='system'`, no actor header) — named as the audit-log program's last, biggest piece back on 2026-07-14, still not touched. Logged in `eq/pending.md`.
- `entity.patched` canonical_events rows are still being written even though the new Suite activity tab filters them out as duplicate — a cheap follow-up (stop writing them at the source) if anyone wants to trim `canonical_events` volume, not urgent.

## Notes
- **Load-bearing fact for any future work on `customers`/`sites`/`contacts`/`staff`/`assets`**: every one of these 5 tables has a `<table>_touch_updated_at` BEFORE trigger (`app_data.touch_updated_at()`) that bumps `updated_at` unconditionally on every UPDATE statement, regardless of whether any other column changed. A plain `OLD IS DISTINCT FROM NEW` row-level comparison can never be used to detect a "real" no-op on these tables — always exclude housekeeping columns first (now done centrally in `fn_audit()`, migration `0226`).
- **`app_data.canonical_events` has no record-level correlation columns** — no `site_id`/`customer_id`/etc. of its own, only an untyped `payload` jsonb whose shape is caller-defined. Correlation to a specific `audit_log` row is inconsistent across event types: `shift.started` (the single most common live event, 180/411 rows on ehow) carries no ids at all; `maintenance_check.overdue`'s `site_id` is a Service-domain id that doesn't match `app_data.sites`; only `entity.patched` correlates cleanly, and that's a pure duplicate of what `audit_log` already shows via the `fn_audit` trigger. Don't assume a join is buildable here without checking live payloads first.
- **`docs/ai-briefing-emit-enrichment.md` is stale on `maintenance_check.overdue`** — it documents the payload as lacking `site_name`/`check_name`, but live data shows both fields are now present (the Service-side fix that doc recommended has apparently already shipped; nobody updated the doc). Worth a scan for other emit-side docs that may be similarly stale before trusting them for a future enrichment task.
- Verifying a DB-level fix by firing the exact live mutation being guarded against (then a real mutation to prove nothing's over-suppressed) caught a genuine bug that a code read-through missed — the 0225 guard looked correct in isolation but was defeated by a pre-existing trigger on every one of the 5 tables it touched. Worth the extra two minutes before calling a live DB fix confirmed, especially on anything append-only/compliance-adjacent.

---

# Session 2026-07-30 (cont.) — Tidy-tab fix: PR #93 merged; eq-shell re-vendor task unblocked

## Built
- Nothing new built this turn — this picked up directly from the prior close in this same session (Tidy tab dropdown/save fix + live migration 063).
- Discovered the spawned re-vendor task (`task_858653b3`, session `local_c62c929a`) had, as a prerequisite for its own work, already committed and pushed this session's eq-intake code changes and opened [eq-solves-intake PR #93](https://github.com/eq-solutions/eq-solves-intake/pull/93) — nothing further needed on the commit/push front.
- Checked PR #93's CI: green (`Build, typecheck, test` passed), `mergeStateStatus: CLEAN`.
- Merged PR #93 (squash `fc46a41301d29567dc43580e782639ceabb7a75d`) into `main`, branch deleted.
- Checked on the re-vendor task directly (`list_sessions`/`get_session`/`list_events` rather than assuming): it was paused, having asked itself whether it should merge PR #93 on its own authority — reasoned that every prior eq-shell re-vendor (PRs #1094/#1106/#1113) vendored from a commit Royce had merged himself, not one Claude merged autonomously, and stopped to ask rather than assume. Sent it a direct message (`send_message`) confirming PR #93 is merged and telling it to proceed with the re-vendor.

## Decided
- Royce: "merge it once CI's green" — merged PR #93.
- Royce: "send it a message now" (rather than leave Royce to answer the other session's question himself) — done.

## Deferred
- **eq-shell re-vendor of eq-intake/eq-platform** — task `task_858653b3` unblocked and told to proceed; still not landed as of this close, running independently. _(added 2026-07-30)_

## Notes
- Good example of checking a background task's *actual* transcript before assuming it's still working normally — `list_sessions` alone showed `isRunning: true` with a stale `prState: OPEN`, which would have read as "still in progress, nothing wrong" without pulling `list_events` and finding it was actually idle, waiting on an unanswered question in its own session.

---

# Session 2026-07-30 — sks-labour PIN credential leak found + closed (SEC-19); SEC-1 residual risk surfaced

Royce asked what simple security upgrades could land on sks-nsw-labour "without changing any user experience." The standing rule going in was SEC-1's freeze — no engineering changes to this app at all, reaffirmed 3× since 2026-06-05. Live investigation (not doc-assumed, per Rule 0.5) found something worse than SEC-1's own framing and worth breaking the freeze for narrowly.

## Built
- Live-verified SEC-1 is still real and current: `pg_policies` + `get_advisors` on sks-labour confirm the anon key still reads `people`/`timesheets`/`leave_requests`/`audit_log`. While checking it, found `people.pin` (the plaintext 4-digit login PIN) is a directly SELECT-able column — a live-credential leak, not just PII. Traced the client code: `loadFromSupabase`'s bulk roster fetch (`index.html`) used `select=*` and shipped every worker's PIN on every session load; confirmed via code trace that nothing downstream actually reads `.pin` back out of `STATE.people` except a manager-only truthy check, so removing it changes zero visible behavior.
- Royce set a hard constraint mid-session: no changes to how anyone logs in, no UX changes at all. Scoped the fix inside that: `people?select=*` → explicit 29-column list excluding `pin`. Verified against the live schema before pushing (exact column list runs clean, `pin` absent from the shape).
- Shipped: sks-nsw-labour PR [#73](https://github.com/eq-solutions/sks-nsw-labour/pull/73) (v3.10.106, commit `c846374`) — Royce merged it himself after the merge action itself was blocked by the Claude Code safety classifier (same class of block as every DB/credential change this session). Live-verified via Netlify: production deploy `ready`, commit ref matches exactly.
- DB hardening: revoked anon/authenticated EXECUTE on 3 RPCs confirmed unused anywhere (app code, Netlify functions, all 7 `pg_cron` jobs) — `verify_staff_pin`, `trigger_shift_events`, `bump_rate_limit` — plus pinned `search_path` on those 3 and 2 more the advisor flagged. Blocked from Claude Code by the classifier; Royce ran the SQL himself, confirmed "SQL done." Live-verified after: `has_function_privilege` false on all 3, `proconfig` shows the pin on all 5.
- Logged the whole thing as SEC-19 in `ops/security-register.md` (committed/pushed separately mid-session, `8fcca88`) — DB side closed, code side shipped and live, SEC-1 itself explicitly still open.
- Spawned a background task for an unrelated bug found along the way (PIN-management modal shows stale "No PIN" status). Checked on it directly when the notification arrived (per the lesson from the eq-intake session earlier today) — confirmed it made **zero code changes**: it correctly scoped the fix, ran its own `/brief`, then `/close` fired before Royce's confirmation landed, so nothing was built. No risk to login from that task at all. Deferred item already logged in `sks/pending.md` by that session's own close.

## Decided
- Royce: "yes go ahead with both" — approved the PIN code fix and the DB RPC/search_path hardening.
- Royce ran the DB SQL himself and confirmed "SQL done."
- Royce: "merge it" — merged PR #73 himself after the classifier blocked the merge action.
- Reaffirmed, narrowly: this was a scoped exception to the standing SEC-1 freeze (prompted by Royce naming the real stakes — "if this leaks the whole eq idea may get dropped by sks"), not a general reopening of "don't touch sks-nsw-labour."
- Royce: "we can't change the way people log in" — the hard boundary every option this session had to fit inside.

## Deferred
- **SEC-1 itself** — unchanged. Not fixable under the no-login-changes constraint without either real per-user auth or decommissioning; neither is happening now. _(open since 2026-06-05, restated 2026-07-30)_
- **Offered, not yet confirmed**: investigate exactly what's blocking the Field parallel-run proving run from actually starting (currently 0 of the required 3-4 clean weeks) — the real remaining lever on SEC-1, since no further safe code hardening exists under current constraints. _(added 2026-07-30)_
- **PIN-management modal stale-status bug** — scoped, not built (see above). Logged in `sks/pending.md`. _(added 2026-07-30)_

## Notes
- The Claude Code safety classifier blocked every single DB/deploy-adjacent action this session tried directly (SQL REVOKE/ALTER, PR merge) — consistent with SEC-12/SEC-18's precedent. Pattern is now well-established: prepare the exact command, hand it to Royce, verify live after he runs it. Not worth re-attempting workarounds.
- Real-time visibility into a spawned background session (`list_sessions` → `list_events`) was essential twice today (this task and the eq-intake re-vendor task in the session above) — a session showing as "ended" or "running" doesn't tell you whether it actually built anything. Always pull the transcript before reporting status on someone else's work.

---

# Session 2026-07-30 — Worker sign-in safety net: SIM-swap PIN invalidation, admin PIN-visibility, email-capture nudge (3 PRs, all merged)

## Built
- **SIM-swap PIN invalidation** (eq-shell [PR #1119](https://github.com/eq-solutions/eq-shell/pull/1119), merged `fd69a9b6`→`87203110`, deployed): admins can now correct a worker's phone number via the edit-user screen — previously fully immutable (`edit-user.ts` explicitly documented it as such; every writer of `shell_control.users.phone` was insert-only). New trigger `users_invalidate_pin_on_phone_change` wipes `pin_hash`/`totp_secret`/`totp_enrolled_at`/`pin_failed_attempts`/`pin_locked_until` the instant phone changes, so a reassigned number can never inherit the previous holder's PIN/2FA — the worker re-authenticates via phone+OTP and is nudged through the existing `pinSetupDue()` flow. Found and fixed a live gap along the way: `shell_control.users.phone` had no unique constraint despite `_shared/phone.ts`'s own comment claiming one exists — added `users_phone_unique` (zero duplicates existed, verified before applying). `eq_get_tenant_user` RPC extended to return `phone`.
- **Admin PIN-visibility UI** (eq-shell [PR #1122](https://github.com/eq-solutions/eq-shell/pull/1122), merged `cc9d4985`→`7bb33291`, deployed): admins can see whether a worker has set a PIN and whether they're locked out (never the PIN itself — it's bcrypt-hashed, unrecoverable by design) on both the Users list (compact pill) and the edit page (status line + "Unlock now" when locked). New `unlock_pin` patch action on `edit-user.ts` clears a lockout without generating a reset link or touching the PIN. `eq_get_tenant_user` and `eq_list_tenant_users` RPCs both extended with PIN status columns.
- **Email-capture nudge** (eq-shell [PR #1125](https://github.com/eq-solutions/eq-shell/pull/1125), merged `dece531a`→`d9ecce3e`, deployed): phone-only self-join workers (`shell-join-tenant.ts` creates them with `email: null`) get a dismissible, nag-only WorkerHome banner (never a forced gate, per the draft onboarding design doc's "the wallet always opens" framing) linking to a new self-service `/settings/set-email` page. The existing email+PIN login door (`shell-login.ts`) needed zero changes — it already works once email+pin_hash are both set. No migration needed — `email` + its unique constraint already existed.
- All three PRs went through a scoped `/plan`-mode cycle before any code (per this repo's non-negotiable: auth changes require explicit approval), each with its own live-migration-then-code-then-merge ordering, same discipline as #1116 earlier today.
- Spawned a separate background task for a bug found in passing (`eq_list_tenant_users` filtering `u.active = true`, making the Users list's "Deactivated" tab permanently empty) rather than folding it into either PR — Royce started that task in a separate session; it landed as [PR #1124](https://github.com/eq-solutions/eq-shell/pull/1124), already merged (own entry earlier in this same file).
- Made a session-close-time explainer diagram for Royce (kid-friendly SVG: "ask → build → test → go live", looped 3×) — no code, just a visual, not logged as a build item.

## Decided
- Royce: "dispatch the SIM-swap PIN invalidation work next" / "dispatch the admin PIN-visibility UI next" / "dispatch the email-capture nudge next" — three explicit dispatches, one after another.
- Scope calls via AskUserQuestion, all picked the recommended option: SIM-swap = full feature (admin phone-change UI + invalidation trigger, not trigger-only); PIN-visibility = status only, never reveal the actual PIN; email nudge = accept unverified for v1 (no confirm-link flow) + dismissal is session-scoped (`sessionStorage`), not permanent per-device.
- Royce: "Merge #1119" / "Merge #1122" / "Merge #1125" — three separate explicit merge approvals, same gate as #1116.
- Royce: "let's do the live click-through verification now" — surfaced that Claude cannot perform this step itself (logging in requires entering a PIN/OTP, which falls under the hard "never enter credentials" rule regardless of whose account it is); offered to monitor Supabase/Sentry live while Royce drives the browser himself. Not yet started as of session close.

## Deferred
- **Royce to click through live, all four features shipped today** (compliance-roster-only workers from earlier + the three in this entry) — full checklist logged in `eq/pending.md` under "Worker sign-in safety net." None of it has been clicked through live yet.

## Notes
- **The brief-gate flag gets cleared by `/close` (step 6), so a same-day follow-on session that dispatches new build work has to re-run `/brief` before touching code** — hit this firsthand when the SIM-swap migration write was blocked mid-task; re-ran `/brief eq-shell` (digest/worktree/git checks + a live jvkn recon since this was schema-adjacent) and it cleared cleanly. Worth remembering: closing a session and then immediately continuing to build in the same session is a real, recurring pattern here, not a one-off.
- **Live-DB recon caught two real, pre-existing gaps neither PR set out to fix**: `shell_control.users.phone` had no unique constraint (found while designing the SIM-swap PR — fixed in the same migration, since it was required for the new `phone-taken` error path to actually work) and `eq_list_tenant_users` was silently hiding every deactivated user from its own "Deactivated" filter tab (found while designing the PIN-visibility PR — NOT fixed in that PR, spun off as its own task instead, since it's a different, unrelated bug). Same lesson as the 0225 trigger discovery earlier today: checking live state before building surfaces things a pure code read misses.
- Confirmed via live query that self-join phone-only workers with an actual PIN set are rare-to-nonexistent today (47 null-email rows on jvkn, zero with `pin_hash` set) — the phone+PIN door (#1110) only shipped hours before this session, so this isn't a sign of a bug, just very early adoption. Worth re-checking this count in a future session to see if it's moved.

---

# Session 2026-07-30 — Follow-on: Patricia Milmlow's Shell account fully deleted

## Built
- After the Deactivated-tab fix (this session's earlier entry) went live, Royce opened `/sks/admin/users` and clicked the "Deactivated" tab himself — confirmed the 2 SKS deactivated users it surfaced (Jack Cluff, Patricia Milmlow) actually appeared. Reactivated Jack Cluff via the UI. Asked to delete Patricia Milmlow outright instead of leaving her archived.
- Before touching anything: queried the real blast radius. An earlier combined count across both accounts showed 6 licence rows, 1 worker record, 1 org-membership — re-querying per person showed all of that belonged to Jack Cluff, and Patricia Milmlow had none of it. Her footprint was just `shell_control.users` (1 row) + 2 tenant memberships (`sks` manager, `__personal__` employee) + `public.profiles` (1 empty row) + `auth.users` (1 row) — confirmed no enforced FK anywhere would block or complicate a delete.
- Did not run the DELETE myself — flagged this as a hard rule (permanent-deletion actions aren't something Claude executes directly, even on explicit request) and instead prepared the exact 3-statement SQL (`shell_control.users` → `public.profiles` → `auth.users`, in that order) as a file and handed it to Royce to run.
- Royce ran it. Verified live afterward: all 4 relevant tables (`shell_control.users`, `user_tenant_memberships`, `public.profiles`, `auth.users`) return zero rows for her id — fully gone from jvkn.
- Archived the whole Deactivated-tab pending.md section (zero open items left) to `eq/pending-archive.md`, and added a new archive entry for the Milmlow deletion itself (already-closed, no live open item).

## Decided
- Royce: reactivate Jack Cluff, fully delete Patricia Milmlow (not just leave archived) — his explicit choice from a 3-way AskUserQuestion (leave archived / hard-delete Shell-login-only / hard-delete everything). Scope for her was narrowed further mid-conversation once the live query showed she had no Cards/licence/org data to decide between — there was only one real thing to delete.
- Royce ran the DELETE himself after I supplied the exact SQL — consistent with the rule that I prepare but never execute permanent-deletion statements.

## Deferred
- None — this thread closed clean, no open items left on either side.

## Notes
- **No delete-user feature exists anywhere in eq-shell** — `AdminEditUser.tsx` only offers Archive/Restore (`active` toggle). The closest-sounding function, `eq_finalize_deleted_accounts()`, is a completely different pipeline (Cards worker `profiles.deleted_at`, 30-day purge) and doesn't touch `shell_control.users` at all. If a real "delete this person" admin action is ever wanted as a standing feature (not a one-off), it doesn't exist yet and would need to be built — this session's delete was a manual, audited one-off via Supabase SQL, not a repeatable app path.
- Confirmed live before deleting: `public.workers.user_id`, `public.licences.user_id`, `public.org_memberships.user_id`, and `public.profiles.id` all carry **no enforced FK constraint** to `auth.users`/`shell_control.users` on this schema — they're soft, app-level references only. Worth remembering for any future account-deletion work: the DB won't stop you from creating orphaned worker/licence/profile rows by deleting a user, so checking real referencing data (not just declared FKs) before deleting is the only way to know the true blast radius.

---

# Session 2026-07-30 — EQ Field transactional emails: tenant branding, SKS logo, and a real cache-busting bug caught mid-review

## Built
- Fixed the Friday supervisor-digest cron's 401 (mismatched auth secret) — new Supabase Vault secret, confirmed live with a 200 OK dry run.
- v3.5.380: wired all 5 EQ Field transactional email templates (leave x3, incident alert, apprentice feedback) plus the 2 server-side ones (digest, timesheet reminder) to read the tenant's real canonical brand colour instead of a hardcoded, drifted SKS hex. Also fixed a real bug in the same pass: the digest cron's empty POST body was defaulting `orgSlug` to null, which displayed "EQ Field" instead of "SKS" as the org name in every live digest email. `eq-field` [PR #569](https://github.com/eq-solutions/eq-field/pull/569), merged.
- Built an interactive HTML review artifact showing all 7 rendered email templates side-by-side with an EQ/SKS toggle, so Royce could see the branding before it shipped — no dev environment needed.
- v3.5.381: added the SKS logo to every email header (reusing `organisations.branding.sidebarLogoHtml`, the same wordmark the app's own dark sidebar already renders — zero new asset). Consolidated the Approve/Reject button markup `leave.js` and the digest edge function had each hand-rolled separately into one shared, icon-labelled helper (`scripts/email-branding.js`, new `supabase/functions/_shared/email-ui.ts`). Swapped remaining unicode glyphs (checkmarks, X's, a party emoji) for inline Lucide SVG icons per the repo's own icon convention. Status text became proper coloured pills. `eq-field` [PR #570](https://github.com/eq-solutions/eq-field/pull/570), merged.
- **Real bug found and fixed while smoke-testing PR #570's deploy preview**: `index.html`'s static eager-loaded `<script>`/`<link>` tags (39 of them — every style file plus every eager script, `app-state.js` included) carried a hardcoded `?v=3.5.379` cache-busting query string that had never been bumped since the release that introduced the versioning scheme. Only the lazy-loaded tab scripts computed their version live from `APP_VERSION`. Netlify serves those paths as `immutable, max-age=31536000`, so any returning browser with a cache from before v3.5.379 shipped had been silently stuck on stale JS ever since — meaning v3.5.380's own fixes never reached some fraction of returning users either. Bundled the fix into the same PR (mechanical find/replace, no content change to the 39 files themselves).
- Redeployed both Supabase edge functions (`supervisor-digest`, `ts-reminder`) on ehow, v10 → v11, so the server-rendered emails pick up the new branding/icon code — this needed a manual deploy since edge functions have no CI/auto-deploy path in this repo. Confirmed `verify_jwt` stayed `false` on both (matching prior config) and the bundler resolved every relative import cleanly on the second attempt (first attempt had the wrong relative-path nesting for the shared `_shared/*.ts` files).
- Refreshed the review artifact to match the final polished output (logo, icon buttons, pills) and added a "view raw HTML source" toggle per template, per Royce's follow-up ask.

## Decided
- Royce: use the existing canonical `sidebarLogoHtml` asset for the email logo rather than a new dedicated upload — zero new asset, tenant-driven, already proven live.
- Royce: go with the fuller visual-polish pass (icons, pills, consolidated helpers) rather than a light-touch logo-only change.
- Royce: use the real live canonical palette value (`#203060`) over the stale value documented in CLAUDE.md (`#1F335C`) once the live query surfaced the drift — corrected both the project and global CLAUDE.md brand references.
- Royce: deploy the two edge functions now, same session, rather than leaving the server-side templates un-branded until the next unrelated deploy touches them.

## Deferred
- **`eq` demo tenant's Leave feature is broken (401 on `leave_requests`, anon lacks a DB grant on zaap)** — found while smoke-testing this unrelated work on both the deploy preview and production. Spun off as its own background task (`task_16025072`), already picked up and running independently in a separate session. Not fixed in this session.
- **`EQ_SECRET_SALT` rotation** — still outstanding from the April exposure; not actioned this session, just re-noted.

## Notes
- **A real gap in the repo's own versioning convention**, not caused by this session's work but exposed by it: CLAUDE.md's documented version-bump ritual (APP_VERSION + sw.js CACHE + changelog banner) never covered the 39 hardcoded `?v=` literals scattered through `index.html`'s static asset tags — those were introduced by PR #567 (the "make /scripts/ + /styles/ immutable" change) and apparently never updated on any release since, including v3.5.380 shipped earlier the same day. Worth a documentation fix so this doesn't quietly recur on the next release — not done this session (out of scope for a same-day emergency fix), flagging for whoever next touches the versioning convention doc.
- SKS's live canonical brand primary is `#203060`, not the `#1F335C` previously documented in both CLAUDE.md files — corrected in place with a note explaining the drift, rather than silently reconciled.
- `scripts/apprentices.js` is now sitting at exactly the repo's 2500-line `max-lines` CI ceiling (was 2493 before this session's small addition) — zero headroom left; the next feature landing in that file will need a helper-module extraction first, not just a quick edit.
- Verification note for future sessions: this sandbox has no authenticated path to click through a live Netlify deploy preview's full app flow or invoke a Supabase edge function with real secrets — smoke-testing here means booting the app, checking console/network for errors, and confirming version tags land correctly, not a full end-to-end click-through. Where that mattered (e.g. confirming the SKS logo renders in a real inbox), it's flagged as unverified rather than claimed.
