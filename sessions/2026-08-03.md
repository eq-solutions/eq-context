# Session 2026-08-03 — EQ-FIELD-10 Sentry triage: tracked, not fixed (Royce's call)

## Built
- Nothing shipped — triage and tracking only, no code touched.

## Decided
- Tier: EQ.
- EQ-FIELD-10 (dashboard map "no-tenant-id" race, preview `deploy-preview-622--eq-field.netlify.app`): **track only, no fix yet.** Preview-only so far, graceful degradation ("Map unavailable right now", self-heals), TODAY.md goals still unset — nothing forces a fix now.

## Deferred
- The actual fix for EQ-FIELD-10 — two directions on the table if it recurs on production: widen the bounded JWT-tenant-id retry budget/interval (`dashboard.js`, currently 4 × 1500ms), or have the dashboard map card listen for the Shell→Field JWT-ready event instead of polling a fixed schedule. Logged in `eq/pending.md` under the existing "eq-field: Dashboard polish" section (same PR #622 the Sentry trace pointed at).

## Notes
- Verified live against `ops/security-register.md`: the other 2 flagged "needs you" items (SEC-1, SEC-9) both already sit under Royce's own standing deferral calls (2026-07-20 and 2026-08-01 respectively) — nothing new to decide, no action taken on either.
- A Sentry MCP (plus Supabase, Netlify, GitHub, Figma, Canva, Google Drive MCPs) became available mid-session — none were connected at session start (confirmed: no `.mcp.json` in eq-context, `ToolSearch "sentry"` found nothing at the time). Worth using next time Sentry needs a live pull instead of relying on digest.md's static snapshot.
- `eq/pending.md` is 384.6KB — exceeds the Read tool's 256KB single-shot limit; used offset/limit reads throughout instead of a full read.
- Applied the 2026-07-27 archive/trim rule to the "eq-field: Dashboard polish" section (the only section touched this session): removed the italic intro + 5 completed `[x]` items after confirming that exact narrative is already fully preserved in `eq/changelog/field.md` (2026-08-02 entry, v3.5.427→431) and `sessions/2026-08-02.md`. Section now holds only its header + the 2 remaining open items.
- `system/worktree-registry.md` has an unrelated uncommitted change (a different session's `contacts-dupe-merge` entry, added 2026-08-02) — left untouched, not staged.

---

# Session 2026-08-03 — eq-receipts: full-width nav + one-click Review from Inbox, merged to main

## Built
- Nav rebuilt from a horizontal-scroll strip into its own full-width wrapping row — every item (Inbox, Dashboard, Review, Exports, Settings, Sign out) now visible from the homescreen without swiping. [`src/components/Layout.tsx`](https://github.com/eq-solutions/eq-receipts/blob/main/src/components/Layout.tsx)
- Inbox photo imports now capture the receipt id back from `extract-receipt`, surfacing a per-item Review link (→ `/review/:id`) and a Review all button once anything's done — finishes a photo import from the Inbox in one click instead of navigating to Review separately. [`src/pages/Inbox.tsx`](https://github.com/eq-solutions/eq-receipts/blob/main/src/pages/Inbox.tsx)
- eq-receipts [PR #18](https://github.com/eq-solutions/eq-receipts/pull/18) opened and squash-merged to `main` (Royce's explicit "merge" instruction, given after being flagged that this would likely trigger an auto-deploy).

## Decided
- Tier: EQ.
- Royce confirmed "push, open PR, merge to main" after being told merging would likely auto-deploy eq-receipts to production (no branch restriction in `netlify.toml`).

## Deferred
- **Neither UI change has been clicked through live** — the app is gated behind Supabase OTP email auth, no test credentials available this session. Compounds the already-open `eq-receipts' react-router move hasn't been clicked through live` item in `eq/pending.md`. Worth doing both in one real-device pass: nav wrapping on a small screen, and the new Review / Review all links after a real photo import.

## Notes
- Confirmed this session was already running in the correct worktree for the task — folder name (`pdf-preview-image-537181`) was stale, but the checked-out branch (`claude/home-menu-photo-import-review-1c723a`) matched the actual ask exactly.
- Re-confirms the 2026-07-29 correction in `eq/pending.md` (eq-receipts: Exports archive/delete...): merging a PR via `gh pr merge` is the known-working Netlify auto-deploy path for this repo, distinct from a raw direct push to `main` (still unretested). This session used the same PR-merge path again.
- Did not verify the deploy actually completed (no Netlify MCP connected this session) — flagged to Royce as "likely" auto-deploy, not confirmed.

---

# Session 2026-08-03 — Cards→tenant consent/sync model: answered live, found a broken sync path, decided against per-tenant release scoping

## Built
- Nothing shipped — architecture Q&A against the live system, one background task spawned, one decision logged as memory.

## Decided
- Tier: EQ (eq-cards + eq-shell, SKS as the instance under discussion).
- **Credential visibility stays default-share-on-tenant-link** (claiming an invite / an approved company application is treated as the consent event), with `is_private` as a global opt-out. No per-tenant release granularity built. Ran via `/decide`; logged as memory `consent_release_model_decision`. Trigger to revisit: a worker with 2+ linked employers actually asking to withhold a credential from one but not another.

## Deferred
- **`credentials-canonical-sync` (jvknxcmbtrfnxfrwfimn edge function) is broken** — no live DB trigger calls it (confirmed: zero `pg_proc` bodies reference it, and the only trigger on `public.licences` calls something unrelated), and it hardcodes the pre-2026-06-15 wrong SKS tenant id (`dcb71d03`, actually EQ's tenant) instead of the corrected `7dee117c`. A worker's licence/credential edit in Cards today does not reach `ehow.app_data.licences` (the store behind the legacy SKS compliance pack / sks-nsw-labour Field roster) at all — the 115 existing synced rows there look like a one-time backfill, not a live feed. Spawned as `task_5687d06b`, already running in a separate session at close time — needs Royce's call on revive-vs-retire (in favour of eq-field's live-RPC read pattern, which has no copy to go stale).

## Notes
- Grounded the answer against live systems per Rule 0.5 rather than memory: confirmed `public.workers` is one row per person (97 rows) with tenant linkage via separate `user_tenant_memberships`/`org_memberships` tables (104/51 rows) — multi-tenant identity is already structurally solved, the gap is consent scoping, not identity.
- Confirmed `workers-canonical-sync` (the sibling, worker-profile sync) IS live and real-time: a genuine Postgres `net.http_post` DB trigger (`worker_canonical_sync` on `public.workers`), <10s, with a nightly reconcile job (02:35 UTC) as backstop. This is the sync path `credentials-canonical-sync` should probably be modeled on, if revived.
- `is_private` (licences) and `sensitive_consents` (profiles, APP 3.3 collection-consent gate) both reconfirmed present live — the 2026-06-26 audit memory's claims about these still hold 5+ weeks later.

---

# Session 2026-08-03 — EQ-FIELD-10 escalated from "track it" to a built, PR'd fix

## Built
- eq-field [PR #625](https://github.com/eq-solutions/eq-field/pull/625) (v3.5.432) — replaces the dashboard map's fixed 4×1500ms retry with a real `eq-field-session-ready` signal. `auth.js`/`core-bundle-b3.js` fire it (+ set `window.__eqFieldSessionReady`) at all 8 places a session becomes usable (5 `checkAccess()` paths + all 3 of `checkPin()`'s success branches). `dashboard.js`/`core-bundle-b1.js` await that signal before the map's first fetch instead of guessing a timeout; the surviving retry (now 2 tries, not 4) covers a genuinely different case — session ready, but the separate mint-data-jwt call itself flaky.
- Built in a dedicated worktree (`C:\Projects\eq-field\.claude\worktrees\eq-field-10-session-ready`, registered in `system/worktree-registry.md`) — the shared root checkout was on an unrelated branch, not touched.
- `node --check` clean on all 6 touched JS files. Line counts checked directly against `eslint.config.js`'s literal ceiling constants (no `eslint`/`node_modules` in this repo to run the rule itself): `core-bundle-b1.js` 1863→1879/1900, `core-bundle-b3.js` 1451→1475/1500.

## Decided
- Tier: EQ.
- Royce's call, given a corrected picture: build the proper event-driven fix, not another retry-timeout widen. Made **after** a live Sentry query (this session) overturned the earlier same-day "track only, preview-only" triage — 7 of EQ-FIELD-10's 26 events are production, spanning ~20 releases, including one real `sks`-tenant hit.

## Deferred
- **Not merged, not deployed.** Netlify auto-deploys eq-field on merge to `main` — needs Royce's explicit separate go, per standing rule and because this touches session-handoff-adjacent code (same-window event only, no auth/security-boundary change, but flagged for review regardless).
- **A separate, real gap found mid-build, deliberately not fixed here**: the `eq` tenant's own demo-mode login never mints a data-JWT at all — `checkPin()`'s demo branch only calls `_mintAndStoreEqToken` when `ORG_SLUG === 'demo'`, explicitly excluding `'eq'` (existing code comment: "eq tenant has no backend"). Since `'eq'` is the *default* tenant slug for any direct, non-SKS load of field.eq.solutions, this is likely why 6 of the 7 live production events are tenant `eq` specifically — a structural gap (the map assumes every tenant has a backend), not a timing race. This fix makes that failure faster and correctly diagnosed in Sentry going forward, but doesn't close it — that needs a product decision (give `eq` a backend? SEED-based map data instead? a demo-specific message?), logged in `eq/pending.md`, not defaulted on.

## Notes
- Reading the actual code (not the Plan agent's estimate) caught a real undercount before it shipped: the original plan said 6 ready-signal call sites (5 `checkAccess()` + 1 `checkPin()`); the true count is 8 — `checkPin()` has 3 distinct success branches (tenant-code, demo-mode, production), and missing the other 2 would have left standalone/non-Shell logins waiting the full 8s defensive ceiling on every single dashboard load. Caught during Phase 3 plan-mode review (reading the file directly) before any edit was made.
- Two research agents + one design agent ran during plan mode to map the real Shell↔Field handoff mechanism, the file-size CI ceiling, and a concrete line-budgeted implementation before anything was built — full detail in `eq/pending.md`'s EQ-FIELD-10 entry and PR #625's description, not re-duplicated here.
- Corrected this session's own earlier (same-day) pending.md/worktree-registry.md entries rather than leaving the stale "preview-only, track it" framing to stand once better data came in.

---

# Session 2026-08-03 — eq-shell: fixed the iframe pre-warm keeper's react-hooks/refs errors, merged

## Built
- Fixed 8 pre-existing `react-hooks/refs` eslint errors in `App.tsx`'s `TenantTree()` — `evermounted` (tracks which of Field/Cards/Service iframes have ever been active, so a visited one never unmounts) converted from `useRef<Set>` mutated during render to `useState<Set>` updated via a guarded render-time `setState` call (React's "adjust state during render" pattern), avoiding the one-render flash a `useEffect` port would have introduced on first navigation into a not-yet-prewarmed app.
- eq-shell [PR #1198](https://github.com/eq-solutions/eq-shell/pull/1198) — built in a dedicated worktree (root checkout was mid-work on an unrelated branch), verified clean (`npx eslint` 0/8 errors, `tsc -b`, full `pnpm run build`, 278/278 tests), opened, all 4 required CI checks green, **squash-merged to main by Royce** (commit `d5506d81`) — Netlify auto-deploys eq-shell to core.eq.solutions on merge.
- Worktree removed, local + remote branches deleted, `system/worktree-registry.md` entry closed out.

## Decided
- Tier: EQ.
- Royce confirmed the fix approach and worktree plan before building (in response to a full session brief), then gave the explicit "merge" go once CI was green.

## Deferred
- **Live click-through not done** — needs Royce to confirm on core.eq.solutions that Field/Service/Cards still pre-warm within 2.5s, switching stays fast, and a first-navigation-before-prewarm still mounts instantly with no flash. Real login is off-limits for me. Logged in `eq/pending.md`.
- **Repo-wide `pnpm lint` carries 438 pre-existing errors** elsewhere (mostly `react-hooks/set-state-in-effect`, same root cause — the `eslint-plugin-react-hooks@^7` upgrade — but far larger scope than this PR). Confirmed via `ci.yml` that lint is advisory (`continue-on-error`) specifically because of this debt, so nothing is silently gating on it; worth a dedicated burn-down session before flipping lint to blocking. Logged in `eq/pending.md`.

## Notes
- This finding originated as an explicit out-of-scope callout from the `document-signoff-shell-push` session (PR #1196) — a clean handoff pattern worth repeating: flag it precisely (file, lines, root cause, suggested direction) rather than scope-creeping the original PR.
- Caught a real mid-build mistake before it shipped: initially removed the `useRef` import assuming `evermounted` was its only use in the file — a `useRef\(` grep missed a second, legitimate use (`lastRoleRef = useRef<{...}>(null)` at line 306, a generic type parameter sitting between the name and the paren). `tsc -b`'s `Cannot find name 'useRef'` caught it; running the full build (not just a scoped `eslint` pass) is what surfaced this, not the narrower check.
- First attempt to merge via `gh pr merge` was blocked by the Claude Code auto-mode classifier (a production-deploy-adjacent action). Reported to Royce with the exact command rather than attempting a workaround via a different tool; Royce merged it himself.
- `system/worktree-registry.md` had a live concurrent writer throughout this session (another session merged `document-signoff-shell-push`/#1196 and advanced `eq-field-10-session-ready` mid-edit) — every edit needed a fresh `Read` immediately before `Edit` to avoid a staleness rejection. Also: `Grep`'s content-mode output displayed forward slashes as backslashes in one case, which caused an avoidable failed `Edit` attempt — `Read` is the reliable source for exact-match content, not `Grep`.

---

# Session 2026-08-03 — eq-shell: self-join's Cards "double sign-in" root-caused and fixed, worker-add nav trimmed further

## Built
- **Nav trim**: "Redeem invite (QR)" removed from the worker-add dropdown (a generic org-wide link, not really a distinct "how do I add someone" decision) and surfaced instead as a plain link next to the invite count. eq-shell [PR #1195](https://github.com/eq-solutions/eq-shell/pull/1195), merged. Discovered mid-plan that a concurrent session had already collapsed the header from 4 buttons to 1 + a dropdown earlier the same day (PR #1185, Royce: "amount of options is overwhelming") — adapted the plan to trim that dropdown rather than redo it.
- **Root-caused and fixed the Cards double sign-in.** `mint-cards-otp.ts` hard-required `shell_control.users.email` to mint the Cards session (422 otherwise). Self-join's collision-safety-net (PR #1188, 2026-08-01) silently drops the entered email whenever it collides with an existing account — confirmed live on the apprentice test via postgres logs (`users_email_unique` violation; the colliding email, `dev@eq.solutions`, belonged to Royce's own separate manager account, unrelated tenant). Fixed by falling back to a deterministic `<user.id>@cards.eq.solutions` address when there's no real email — never emailed, stable per-user, so GoTrue always resolves the same `auth.users` row; `ensureAuthUser` also now syncs that row's email forward if the real one changes later. `shell_control.users.email` itself untouched. eq-shell [PR #1197](https://github.com/eq-solutions/eq-shell/pull/1197), merged.
- **Bonus bug found while tracing the above, fixed same PR**: self-join's audit-only `worker_invites` insert never set `created_by` (NOT NULL, no default) — confirmed silently failing on every single self-join via the same postgres logs. Now set to the joining worker's own id, matching `create-worker-invite.ts`'s existing convention for admin-created invites.
- **Recovery-email nudge copy fix.** Confirmed live that the nudge does NOT normally double-ask (it reads `has_email`, computed straight off the same `shell_control.users.email` column self-join's own email field writes to) — the only case it re-asks is the collision-drop above, and the copy read as if the worker was never asked at all. Added a sessionStorage flag carried from the join response so the chip says "Add a different email — the one you entered is already linked to another account" specifically in that case. eq-shell [PR #1199](https://github.com/eq-solutions/eq-shell/pull/1199), merged.
- **Confirmed live (no build needed)**: unticking "Requires manager approval" on a self-join link does activate the joiner immediately, no pending row — traced directly through `self-join-codes.ts` → `shell-join-tenant.ts`'s `pendingApproval` computation.
- Test account (phone `0466118646` / `dev@eq.solutions`) deleted again — `auth.users` cascade + `shell_control.users` cascade, verified 0 rows across every dependent table.
- **Hotfix, same session, within the hour: #1197 had a real regression, caught live by Royce's own re-test.** Reported mid-`/close`: signed in, reached Field fine, EQ Cards spun forever. Root cause: `ensureAuthUser`'s email-sync condition (`existing?.email && existing.email !== email`) short-circuits when the existing row's email is `null` — exactly the case for a phone-only self-joined worker, the population #1197 was built to help. With the sync skipped, `generateLink` (which resolves by email, not id) couldn't find the real canonical row and GoTrue silently auto-provisioned a **disconnected orphan `auth.users` row** under a different id — Cards got a session with no `profiles`/`workers`/`licences` data behind it and hung. Confirmed via a direct `auth.users` query (found the orphan row, email `2fa032a4-...@cards.eq.solutions`, id `a7d5ca3c-...` — different from the real canonical id `2fa032a4-...`). Fixed by dropping the truthy gate (`existing.email !== email` now also catches null). Deleted the orphan row so the real row can claim that email cleanly on retry. eq-shell [PR #1203](https://github.com/eq-solutions/eq-shell/pull/1203), merged same session (`5a028f9`).

## Decided
- Tier: EQ.
- Royce approved building the Cards SSO fix ("Option A") after an explicit steelman pass covering: synthetic-email collision risk (none — keyed on the already-unique user id), GoTrue's email-lookup requiring the stored `auth.users` row to stay in sync if the real email changes later (added the sync step), and whether `worker_invites.created_by` pointing at the worker themself creates a new retention-purge problem (it doesn't — already a documented, generically-handled NO-ACTION FK case).
- Royce approved the recovery-email nudge copy polish as a small, low-risk follow-up once the SSO fix removed the functional harm.
- Royce's live re-test of #1197 is what caught the hotfix bug — approved shipping #1203 immediately once CI was green ("merge if green"), same auth-approval bar as the other PRs this session, just turned around fast given he was actively blocked.

## Deferred
- **#1195's nav trim and #1199's nudge copy still not clicked through live.** #1197 WAS clicked through live — and that's exactly what caught the regression fixed by #1203, which itself has not yet been re-confirmed (Royce was mid-retry when this session closed). Logged in `eq/pending.md`.
- **Photo ID still shows as needed after a driver's licence upload**, despite the 2026-08-02 equivalence migration (`2026_08_02b`) being live — reported by Royce, not re-investigated this session (explicitly deprioritised behind the Cards dive). Leading theory: the screen Royce is on reads `eq_cards_my_credential_gaps` (eq-cards) rather than the patched `eq_worker_compliance_status` (eq-shell/jvkn) — unconfirmed. Logged in `eq/pending.md`.
- **White card upload is camera-only, no photo-library option** — eq-cards' Flutter app, a different repo not available this session. Logged in `eq/pending.md`.
- **OCR-scanned name → `profiles.full_name` still unconfirmed** — carried open from the 2026-08-01/02 self-join work, never independently re-verified. Logged in `eq/pending.md`.
- The original 3-option nav-consolidation question from earlier in the session was superseded by the cross-tenant self-join bug investigation and never directly answered by Royce — resolved implicitly via the narrower PR #1195 trim instead, on his "simple is best, tech invisible" steer.
- **Worth a second look**: whether any other "sync if different" check in the auth path has the same falsy-`null` blind spot that caused #1203 — not swept this session, the one instance found was fixed reactively, not through a proactive audit.

## Notes
- The Cards SSO root cause was found entirely from live evidence, not code reading alone: pulling the actual DB row for the apprentice's phone number (had to search `+61466118646`, not `0466118646` — E.164 stored, not the raw local format the earlier query used) plus a targeted `get_logs` postgres pull for the exact join timestamp surfaced both bugs (the email collision AND the unrelated `worker_invites.created_by` failure) in two log lines.
- Confirmed via a second cross-tenant test earlier in the session (before this file's mid-session compaction): an existing Shell user scanning a *different* tenant's self-join QR was previously being silently signed into their **original** tenant only, never gaining real access to the one they scanned — found from the code's own stale comment, fixed (eq-shell PR #1193, merged prior to this entry), Royce approved via "Add them to the new org too."
- Every PR this session was built on a freshly re-cut branch off `origin/main` rather than continuing on the prior branch — the prior branch was already squash-merged each time, and continuing on it directly would have picked up the stale (pre-merge) base. This is now a recurring, deliberate pattern in this repo's sessions, not a one-off fix.
- **The #1203 hotfix is the clearest evidence yet for why the "click through live before calling it done" rule exists**: #1197 typechecked clean, linted clean, passed every CI check, and was reasoned through carefully at design time (steelmanned before building) — none of that caught a `null`-vs-falsy logic gap that only manifests for the exact real-world case (a phone-only worker) the fix was written for. Royce's own live re-test caught it within the hour; a same-day hotfix closed it.
- `pending.md` is 384.6KB+, over the Read tool's single-shot limit — used `Grep` to locate the right section, then a scoped `Read`/`Edit`, rather than a full read.

---

# Session 2026-08-03 — eq-shell + eq-solves-intake: no-explicit-any bucket closed, vendored unused-vars fixed at source, both PRs merged

## Built
- **eq-shell PR #1207** — all 117 eq-shell-owned `no-explicit-any` instances resolved (of 157 total; the remaining 40 live in the vendored `eq-intake/eq-platform/packages/` subtree). Triaged into three treatments rather than blanket-disabled: the systemic, already-documented `SupabaseClient<any,any,any>` generic-inference workaround got the repo's existing `eslint-disable-next-line` convention; genuinely fixable spots got real types (three dead `role as any` casts removed outright, an AI-extracted JSON field retyped, a tier-tuple check fixed to cast the array not the input, two test files given real types); two dead disable-comments removed from a `.mjs` script the TS rule was never active for.
- **eq-solves-intake PR #107** — the 13 `no-unused-vars` instances eq-shell's earlier PR #1204 found in its vendored copy, fixed at the real source instead, so a future re-vendor doesn't reintroduce them. Mostly dead imports/params; one dead `Map` removed that pointed at an unimplemented mapping-preference feature (flagged, not built).
- Both PRs: dedicated worktrees, full `pnpm run build` + test suite verification (eq-shell 278/278, eq-solves-intake 254/254 across 7 packages), CI green, squash-merged on Royce's explicit "merge both" go (`gh pr merge`). Worktrees removed, branches deleted, `system/worktree-registry.md` closed out for both.

## Decided
- Tier: EQ.
- Royce's "continue - sprint this" picked up two of the three "Next" items from the earlier same-day session-close map, overriding my own "maybe pause the lint treadmill" recommendation from the prior entry.
- "merge both" — explicit go to merge both PRs once CI was green.

## Deferred
- **eq-shell needs a re-vendor bump** to pick up eq-solves-intake PR #107's fixes into `eq-intake/eq-platform/packages/` — same routine as prior re-vendor PRs (#1156/#1159/#1169/#1187). Not started. Logged in `eq/pending.md` and both repos' changelogs.
- No live click-through needed for either PR — both are pure type-annotation/lint-suppression changes with no runtime behavior change, verified via full build + test suite instead.

## Notes
- **Real regression caught by the full build, not eslint**: an attempt to give two joined-relation query results (`admin-tenants.ts`, `list-worker-invites.ts`) proper row interfaces passed eslint but failed `tsc` (TS2352) — supabase-js's compile-time select-string parser infers embedded to-one relations as arrays regardless of real FK cardinality, on an any-generic client with no real generated `Database` types. Reverted both to the original any-cast-with-disable-comment pattern with an explanatory comment, rather than forcing a type that doesn't match actual compile-time behavior.
- **One instance missed on the first eslint pass** (`licence-expiry-scheduler.ts:419`) — caught by a recheck script re-parsing `eslint --format json` filtered to non-vendored paths, fixed immediately.
- **Substrate push hit a real concurrent-writer conflict**: `system/worktree-registry.md`'s `## Active (do not touch)` table got a non-fast-forward rejection from a concurrent session's own edit (a new row for `document-signoff-register-view`/eq-shell PR #1208, unrelated work). Resolved via `git fetch` + `git rebase origin/main`, manually keeping both sides' rows rather than picking one, then `git rebase --continue` and push — no content lost from either session.
- Changelog entries for both merged PRs (eq-shell.md, eq-intake.md) and this session-log entry were written after the merge, closing out the same-day gap left when `/close` ran before the sprint work and the "merge both" pass.

---

# Session 2026-08-03 — Wallet declutter, Show mode shipped, OCR dead-session bug root-caused + fixed

## Built
- **Removed a dev-only debug alert.** `licences_list_screen.dart` had a leftover once-ever SnackBar ("Tip: tap any field on a licence to copy it. That's the wedge.") — internal dev copy that was never meant to reach a worker. Deleted it plus its now-unused `shared_preferences` plumbing.
- **Reordered + deduped the wallet's stacked nudge cards.** Prior decluttering (PRs #139-141) had already fixed the big issues, but two remained: the install-to-home-screen nudge rendered *first*, ahead of an incoming company access request or the setup checklist — a convenience nudge outranking things that actually need a decision. And a real duplicate: `SetupChecklistCard` already ticks "Connect to your employer" done (struck through) the moment a pending application exists, but `OutgoingRequestsBanner` kept showing its own "Application to {org} — waiting" card right below it with no new information or action. Reordered by actual priority; added a `showPendingCard` flag to suppress the redundant pending card specifically where the checklist already covers it (the empty wallet, with no checklist yet, still shows it). eq-cards [PR #197](https://github.com/eq-solutions/eq-cards/pull/197), merged + deployed.
- **Wallet "Show" mode shipped.** Task described the target as `card_screen.dart:392`'s QR-sign-in button — verified against live routing first and found that screen is unreachable dead code (`/card` is a legacy redirect-only route to the wallet, `CardScreen` is never constructed anywhere). The real, live equivalent is the Wallet screen's own QR tap target in `licences_list_screen.dart`. Built there instead: a fullscreen `ShowScreen` — black on white for direct-sun readability, `screen_brightness` forced to max on entry/restored on exit, `wakelock_plus` keeps the screen on while active, worker name/licence type/number/expiry in words, swipe between licences via `PageView`, "EXPIRED" in red filling roughly a third of the screen. Zero network calls by design — reads `licencesListNotifierProvider`/`profileNotifierProvider`/`licenceTypesProvider` off whatever the Wallet screen underneath already has resident (a genuine `Navigator.push` keeps those providers alive), so it works with the device fully offline via the existing `wallet_cache_service` fallback. `wakelock_plus` capped `>=1.2.11 <1.6.0` — 1.6.0+ needs `win32 ^6.0.1`, conflicting with `file_picker ^11.0.0`'s `win32 ^5.9.0`. 8 new widget tests; `flutter analyze` clean; full suite (255 tests) passes; `flutter build web` succeeds and the built app boots with zero console errors (verified via a static `build/web` preview in-browser — not a real device). eq-cards [PR #198](https://github.com/eq-solutions/eq-cards/pull/198), merged + deployed.
- **OCR dead-session bug: root-caused live, then fixed.** Royce hit "OCR didn't run: sign-in expired" mid-test. Traced directly against `jvknxcmbtrfnxfrwfimn` Supabase logs (edge-function + auth + a targeted `execute_sql` against `auth.users`) rather than guessing: `ocr-licence`'s manual JWT check hit GoTrue's `/auth/v1/user` and got `403 user_not_found` — the device held a session for an `auth.users` row that no longer existed (confirmed it was neither of Royce's two active identities). This corrected an existing `eq-context/eq/pending.md` note from earlier the same day that had wrongly guessed a stale credential/key. Fix: `OcrService` now signs the session out immediately on this specific 401 and throws a new `OcrSessionDeadException`; `scan_ocr_flow.dart` treats that as already-handled (skips the old misleading "you can still fill it in manually" toast and the manual-entry navigation, both wrong mid-redirect) — `app_router.dart`'s existing signed-out redirect (reacting to `onAuthStateChange`) takes the worker to a real sign-in screen, no new navigation logic needed. `flutter analyze` clean; full suite (255 tests, +1 new) passes. eq-cards [PR #199](https://github.com/eq-solutions/eq-cards/pull/199), merged + deployed.
- **Non-corporate plain-English explainer of EQ Cards**, published as a Claude Artifact (not a repo commit) for Royce to share with a labour-hire-firm-owner mate for honest feedback — deliberately no pitch/CTA/pricing, framed as a personal note. Followed up with a short set of discovery-style questions (past behavior over future opinion, "what would need to be true") for the actual conversation, since marketing/validation isn't Royce's strength.

## Decided
- Tier: EQ (eq-cards specifically).
- Royce confirmed proceeding with all three eq-cards fixes end-to-end (build → merge → deploy) individually as each landed, rather than batching.
- Explicitly held PR #199 (the OCR sign-out fix) for a separate merge/deploy confirmation before acting, since it's an auth-flow change — per the standing rule requiring explicit approval before deploying auth changes. Royce then gave that separately.

## Deferred
- **Show mode not yet click-tested on a real device with network disabled.** Everything short of an actual live tap-through is verified (analyzer, full suite, web build + boot). Real login is off-limits for me to perform, so this needs Royce. Logged in `eq/pending.md`.
- **The underlying cause of the dead-session OCR bug is likely explained, not fully confirmed** — cross-referencing a concurrent eq-shell session's own log (same day) found it root-caused and fixed a `null`-vs-falsy bug (`ensureAuthUser`, eq-shell PR #1203) that made GoTrue auto-provision a disconnected orphan `auth.users` row for the *exact same shared test phone number* involved here, later deleted — which would 403 exactly like this bug. Not verified against the specific orphan id (already gone by the time this was cross-referenced). Logged in `eq/pending.md` as a strong correlation, not a confirmed single cause.
- Neither of the two `eq-context/system/worktree-registry.md` worktrees created this session (`remove-wedge-alert-0da7e2`, `wallet-show-mode`) has been torn down — both hold merged branches, safe to remove next time someone's doing worktree cleanup.

## Notes
- The "OCR expired" investigation is a good example of why live-verifying beats reading an existing note: `eq-context/eq/pending.md` already had an entry from earlier the same day confidently attributing this exact symptom class to a stale credential. Direct Supabase log/SQL evidence showed that was wrong — a completely different, more specific mechanism. Correcting a wrong prior note in the substrate, not just adding a new one, felt like the more important half of that fix.
- `card_screen.dart` being confirmed dead code (via `app_router.dart`'s `/card` legacy redirect + grepping for `CardScreen(` construction sites and finding none) is worth remembering for any future task that references that file by name — it's very likely stale, and the live equivalent for anything ID-card-related now lives in `licences_list_screen.dart`.
- Two fresh worktrees were used this session (`remove-wedge-alert-0da7e2` for the first fix, `wallet-show-mode` reused across both the Show mode PR and the OCR fix PR via a second branch) rather than continuing on an already-merged branch, matching the pattern the concurrent eq-shell session's log also independently notes as deliberate this session.

---

# Session 2026-08-03 — eq-shell react-hooks/refs cleanup finished (PRs #1200/#1202) + hex-colours (#1201) + unused-vars (#1204); EQ-FIELD-10 Sentry status checked and found stale

## Built
- **PR #1200** — second pass of the `react-hooks/refs` cleanup found while auditing #1198: 19 more instances fixed across `ServiceIframe.tsx` (2) and `QuotesModule.tsx` (16, three distinct clusters — a lazy-init ref converted to `useState`, six closure-freshness refs moved into a no-deps `useEffect`, one stage-picker `<select>` fixed by hoisting state out of an IIFE). One instance in `comms/index.tsx` deliberately left for a follow-up (deep in an actively-used inline-edit feature, needed a real component extraction rather than a quick port).
- **PR #1201** — mapped 8 hardcoded hex colours in `LabourHireRates.tsx`/`WorkerHome.tsx` onto `@eq-solutions/tokens` CSS variables, verified against the token file directly rather than guessed. One colour (`WorkerHome.tsx`'s Cards-app "My Card" tile accent) deliberately kept hardcoded with a scoped eslint-disable — matches an existing app-identity colour with no token yet.
- **PR #1202** — extracted `JobRow` as a named component out of the comms job table's inline `.map()` row renderer, closing the 1 `react-hooks/refs` instance deferred from #1200. **Closes all 28 `react-hooks/refs` errors across the whole eq-shell repo** (started at #1198).
- **PR #1204** — `no-unused-vars` cleanup: found 106 of 120 flagged errors were already-correct code (the repo's own `_context`/`_ctx` convention for intentionally-unused Netlify handler args) that the eslint config just wasn't exempting — fixed with one rule-option addition instead of touching ~90 files. One genuine case fixed (`encryption.ts`, a deliberately-unread caught error). 13 remaining instances live in a vendored subtree (`eq-intake/eq-platform/packages/`, sourced from eq-solves-intake) — out of scope for this repo, logged as a follow-up.
- All 4 PRs: dedicated worktrees, full verification (`eslint`, `tsc -b` via the full `pnpm run build`, test suite) before opening, all 4 required CI checks green, squash-merged on Royce's explicit "merge" go each time. Worktrees + branches cleaned up, `system/worktree-registry.md` closed out for all 4.
- **Investigated Sentry issue EQ-FIELD-10** ("check the sentry error"). Confirmed live against the Sentry MCP: the underlying bug is already fixed and deployed (eq-field PR #625 + #626, merged earlier the same day by a separate concurrent session, confirmed live via Netlify). But the Sentry issue itself still shows `unresolved`/`regressed` — neither fix commit's message used the `Fixes EQ-FIELD-10` keyword Sentry's GitHub integration needs to auto-close an issue, and no new events have landed since before the fix shipped (last event 2026-08-02, predates the 3.5.432 release), so there's no fresh evidence either way yet. Reported both findings to Royce plainly rather than either assuming the stale tracking meant the fix hadn't worked, or marking it resolved myself without proof.

## Decided
- Tier: EQ.
- Royce gave an explicit "merge" go for each of the 4 PRs individually.
- Mid-campaign, Royce asked "what are we actually achieving here?" — answered with an honest assessment rather than continuing to justify the work: real bugs were fixed (28 render-time ref violations, a stale eslint config hiding a real caught-error swallow), but the remaining lint buckets (`no-explicit-any`, 157 instances) are materially bigger and lower-value per hour than either finishing live click-throughs on what's already shipped, or working the three flagged "Needs You" items. Recommendation given, not yet acted on.

## Deferred
- **Live click-throughs still outstanding** for #1198 (iframe pre-warm), #1200 (Service embed, Quotes/Ops filter persistence + keyboard nav, stage picker), #1201 (visual colour check), #1202 (comms job table inline editing) — all need a real authenticated session, off-limits for me. Logged in `eq/pending.md` under each PR's own entry.
- **EQ-FIELD-10's Sentry issue status itself** — still shows unresolved/regressed despite the code fix being live; needs either Royce manually resolving it in Sentry, or waiting for real post-fix traffic to confirm before doing so. Not actioned this session — investigative only, no write to Sentry attempted.
- **`no-explicit-any` bucket (157 instances)** — not started, materially larger and needs real typing work rather than mechanical fixes. Flagged as lower-priority than the three "Needs You" items still open (SEC-1, SEC-9, and the now-resolved-in-code-but-not-in-Sentry EQ-FIELD-10) in the reflective exchange above.

## Notes
- Confirmed via direct `get_sentry_resource` pull (not memory/notes) before reporting anything to Royce, consistent with Rule 0.5 — the earlier session's own log entries (PR #625/#626 merged + confirmed live) were leads, re-verified against live Sentry data before concluding anything about the issue's current status.
- `sessions/2026-08-03.md` and `eq/changelog/eq-shell.md` both had a live concurrent writer throughout the day (multiple unrelated same-day session entries from eq-cards, Cards SSO, and EQ-FIELD-10-escalation work) — appended with a `---` divider rather than touching existing entries.
