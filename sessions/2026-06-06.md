# 2026-06-06 — EQ core go-live push: Field cutover (B5), Quotes SKS-tenant readiness, Shell auth + onboarding

**Built by:** Royce Milmlow + Claude Code (autonomous sprint, multiple agents)

> **Provenance:** the original session draft for this day was lost before it was
> committed (drafted in a session transcript that couldn't be recovered). This log was
> **reconstructed from verified git history** — every item below maps to a PR that
> merged to `main` on 2026-06-06. It captures *what shipped*, not the narrative nuance
> of the lost draft. `eq/pending.md` ticks and the `eq/active.md` line from that draft
> were **not** reconstructed (not recoverable from git — see foot of file).

## Shipped — EQ Field (`eq-field`)
- **#193** v3.5.79 — hardening sweep: mobile overflow, `clash_detected`, logging + docs.
- **#194** SKS→EQ Field **cutover (B5)** — verified plan, sprint, and repoint package
  (the SKS standalone Field is being replaced by the EQ Field build behind the tenant).
- **#195** v3.5.82 — SKS pipeline → authenticated JWT + RLS (B5 Track 2). **MERGED, dormant**
  (`DATA_JWT_ENABLED` off; EQ unchanged). Was "in flight" earlier in the day — see Addendum.
- **#196** v3.5.81 — Teams id-type fix for uuid tenants (EQ/melbourne/demo-trades). Parallel
  duplicate **#197 closed** in favour of it.
- **Canonical** — `organisations.hostname` `sks → sks-field.netlify.app` (eq-canonical control
  plane), so EQ Field resolves the SKS tenant at the new host. Pairs with eq-shell #182.

## Shipped — EQ Quotes (`eq-quotes`) — #16–#26
- **#16** attachment uploads fixed + export-button alignment + `audit_log.actor_uuid` (live as Fly v152).
- **#17** quote-machine fixes: editable scope/items, contacts, project name, paginator (live v155).
- **#22** per-browser memory of each user's export-template choice.
- **#23** sprint: grounding infra + critical-path guards + adoption signal.
- **#25** sprint: SKS-tenant readiness — ABR ABN lookup, security tidy, Phase 1 playbook.
- **#20 / #26** Phase 1 canonical-convergence design + the schema-subset finding + hardened playbook (WS0 Sentry confirmed clean).
- **#24** cron embed made rate-limit safe (stops on 429).
- **#21** reverted speculative multi-tenant routing (#18/#19) to keep `main` lean;
  **#18/#19** (per-tenant canonical routing without the master key) merged but **DORMANT until cutover**.

## Shipped — EQ Shell (`eq-shell`)
- **#171** forced TOTP on all 3 login doors + phone capture at invite.
- **#174** TOTP enrolment QR generated locally (CSP-safe, no secret leak).
- **#173** admin reconciliation view (expected vs landed + FK orphan scan).
- **#175** invite-migrated-staff bridge (onboarding).
- **#182** repoint SKS Field iframe to the EQ Field build (`sks-field.netlify.app`).

## In flight (open at session close)
- **eq-shell** — #183 (Cards→Field SSO sending-half finding, blocked upstream), #180 (`/_platform/*`
  operator space), #179 (stop ~85 phantom CRLF-churned eq-schemas files per build), #178 (TOTP
  back-link 404 fix), #177 (DRAFT — F1 tenant-config contract + schema-parity audit), #176 (SKS
  go-live launch-day execution sheet, Stage 1), #172 (unified Magic Link template, part 1).

## Cross-tier (SKS, separate repo/DB — logged for the seam only)
- **sks-nsw-labour #31** — v3.10.59 Cards→Field SSO: resolver (`canonical_id`→phone) + `canonical_id`
  re-sync. Pairs with eq-shell #182/#183 above (the EQ-side sending half).

## Theme
Convergence toward EQ-core go-live: collapse the SKS standalone Field onto the EQ Field build
behind the tenant (B5 cutover), harden EQ Quotes for SKS-tenant readiness, and tighten Shell auth
(forced TOTP) + onboarding (migrated-staff invite bridge) ahead of onboarding real users.

## Not reconstructed (lost with the original draft)
- **`eq/pending.md` ticks** — the day's code PRs don't map cleanly to the open Royce-actions in
  `pending.md` (downgrade ktm DB, `TENANT_ORG_UUID`, revoke PAT, drift CI secrets, HIBP toggle),
  so nothing was ticked. If items did close, tick them by hand.
- **`eq/active.md` line** — that file does not exist in the EQ tier (`active.md` is SKS-tier). The
  intended edit couldn't be verified, so it was not applied.

---

## Addendum — Track 2 + Teams landed (later 2026-06-06; live-session, not reconstructed)

Direct continuation of the day, captured live (not from git reconstruction):

- **eq-field #195 → MERGED as v3.5.82** (was "in flight" above; rebumped 3.5.80→3.5.82 because
  #196 took 3.5.81). SKS pipeline JWT+RLS carrier ships **dormant** behind `DATA_JWT_ENABLED` (off):
  per-tenant data-JWT secret resolver (verify-pin.js), in-place carrier `JWT_INPLACE_TENANTS={sks}`
  keeping `public.*` on SKS's own Supabase (supabase.js), `realtime:public:<table>` for in-place
  (realtime.js). `migrations/2026-06-06_sks_pipeline_rls.sql` **authored, NOT applied**.
- **eq-field #196 → v3.5.81** Teams uuid id-type fix (concurrent agent); my parallel **#197 closed**
  as a duplicate.
- **Canonical hostname repoint:** `organisations.hostname` `sks → sks-field.netlify.app`.

**Recon corrections (live > docs):** Tender Pipeline IS already on SKS live + populated (CLAUDE.md
TODO stale); SKS carrier tables key on `org_id` (not `tenant_id`); `nominations`/`tender_enrichment`
have no `org_id` (isolate via parent tender); JWT `sub` is synthetic (`eq-field:sks`) so per-nominee
RLS is inert until a per-user-identity model exists.

**SKS-LIVE gate — Track 2 stays dormant until done (each needs snapshot + per-action OK):** apply
the RLS migration to `nspbmirochztcjijmcrx`; add `SKS_JWT_SECRET` to the SKS Netlify site; flip
`DATA_JWT_ENABLED=on`; then move `AUDIT_SB_KEY` to a service_role key and drop the `audit_log`
anon-insert carve-out. Ticks added to `eq/pending.md`.

**Process note:** #195 and #196/#197 were worked by two agents concurrently → version collision +
a duplicate teams PR (tell: the worktree-name clash on `claude/teams-uuid-fix`). Check
"who's touching this file?" before parallel fan-out.

---

## Addendum 2 — Track-2 staged + SKS LIVE on EQ Field (later 2026-06-06)

Continued the same day toward go-live:

- **Snapshot + branch dry-run** of the Track-2 RLS: validated on a disposable Supabase branch
  (deleted after) — tenant isolation, supervisor-vs-confirmed, anon revoke, audit_log carve-out all
  passed. Migration split into STEP1 (additive) + STEP2 (lockdown) for zero-downtime.
- **STEP 1 (authed policies) APPLIED to SKS prod** (`sks_pipeline_rls_step1_additive`): 22
  `field_authed_*` policies; anon intact (verified anon still reads 60 people). PR [#200](https://github.com/eq-solutions/eq-field/pull/200)
  records the split SQL + snapshot.
- **`DATA_JWT_ENABLED` confirmed ON** (probed the mint endpoint). ⇒ post-login SKS runs on the
  **authed** path (in-place `public.*` + STEP-1 policies), and **STEP 1 is load-bearing, not
  dormant — do NOT roll it back.** STEP 2 (anon revoke) stays DEFERRED until the standalone is
  retired (the revoke breaks the standalone's anon soak-rollback).
- **Gate lock-out found + fixed (v3.5.83, PR [#199](https://github.com/eq-solutions/eq-field/pull/199), merged + live).** SKS on EQ Field showed an empty
  "who are you" gate (0 names) → nobody could log in: the gate's pre-login `sbFetch('people'/'managers')`
  hits JWT_TABLES → carrier tried to mint a JWT → no session pre-auth → threw → empty list. Fix:
  `sbFetch` falls back to the anon path when the JWT can't be minted. **Verified live: gate now lists
  69 SKS names.** Also fixed melbourne/demo-trades throwing on the JWT path.
- **Result: SKS tenant is usable on the EQ Field build** at `field.sks.eq.solutions` (v3.5.83).
  Remaining = Royce visual smoke → soak (standalone warm) → retire standalone → Track 2 STEP 2.

**Carrier learning:** any real tenant on the EQ Field build that's in JWT_TABLES needs the data-JWT
to mint; the gate is pre-auth so it MUST tolerate no-session → anon. The v3.5.83 fallback makes the
carrier degrade to anon whenever a mint isn't available, which is the correct behaviour for pre-login
and for the anon-parity phase before a tenant's flag/secret are set.
