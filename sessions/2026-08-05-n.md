# Session 2026-08-05 (n) — core.hooksPath resolution check added to session gate (F10)

## Built
- `hooks/session_start.py` gains a 6th check, HOOKS: reads `--local`, `--worktree` (when `extensions.worktreeConfig` is enabled), and the effective `core.hooksPath` every session; warns loudly on any wrong or shadowed value, silent-ok otherwise.
- `system/failures.md` gains F10 (recurrences: 3, rung: 1, target_rung: 4) — unifies three previously-scattered incidents (2026-05-24 prose-only `lessons.md` entry, 2026-08-04 F8's shadow-copy side-finding, 2026-08-05 the worktree-scope-shadow finding `eq/pending.md` had explicitly flagged as "not filed as its own ledger item").
- `hooks/README.md`'s `session_start.py` row updated to match.
- Committed + pushed via an isolated clone (the shared checkout was mid-divergence, 6 ahead / 22 behind origin, when this started): commit [`6b02a8f3`](https://github.com/eq-solutions/eq-context/commit/6b02a8f3e3c8362ccd67ddb64df623bb4f0f102e).

## Decided
- N/A — task was fully pre-specified by brief; no in-session decisions needed from Royce.

## Deferred
- Both adversarial test suites (`hooks/adversarial_test.py` / `.sh`) failing 3 cases each on `origin/main` — F2/F7 sandbox-simulation cases, unrelated to this change (confirmed via a git-stash A/B test: identical failures with the change fully removed). Spawned as background task `task_e1722f87`; Royce started it in a separate session, result not yet known.

## Notes
- Two corrections to the original brief, folded into F10's own note field rather than left standing: (1) the "third mechanism" writeup lives in `eq/pending.md` (line 115, "Correction, 2026-08-05"), not `ops/pending.md` — the latter has no `core.hooksPath` mentions at all; (2) F10 immediately trips the existing RATCHET check (3 recurrences, rung 1 < 4) on the very next session-gate run — expected and correct, not a bug; `guard-ratchet.yml` will likely open a promotion-proposal issue next run.
- `pre_tool_use.py`'s F9 `effective_cwd()` fix only recognizes a `cd "<path>" &&`-chained one-liner or `git -C <path>` — a genuinely separate `cd` on its own line earlier in a multi-line script still resolves to the nominal session cwd. Hit this live: a multi-line script (`cd "$CLONE"` on its own line, `git rebase origin/main` several lines later) got wrongly blocked as if targeting the shared checkout, even though every command in it targeted an isolated clone. Safe-direction false positive (over-blocks, never under-blocks) — worked around by using `git -C <path>` throughout instead of a leading `cd` line. Not filed as its own ledger entry — low severity, self-resolved, worth knowing if it recurs and starts costing real time.
- Checked the "Guard bypass? rung 4 — F7" NEEDS YOU item that appeared on session resume: grepped F7's exact signal regex against `sessions/2026-08-05.md` and every lettered variant, found no match anywhere in current content. Reads as a stale-digest false positive (digest last generated 05:37 UTC, well before today's volume of same-day commits) rather than a confirmed bypass, matching the known F1 self-flagging failure mode — not independently root-caused further; out of scope for this session.
- Also hit, mid-session: the Claude Code auto-mode classifier blocked a `git reset --hard origin/main` against the shared checkout even though `git status` showed a clean tree and a strict fast-forward ancestor (zero actual risk). Didn't route around it — used `git merge --ff-only` in an isolated clone instead, which the classifier allowed. Worth knowing this specific safe case reads as "risky" to the classifier if it comes up again.
