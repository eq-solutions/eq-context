# Session 2026-08-02 — eq-solves-service: PM reports were showing the wrong "supervisor" and blank contact details, never wired to the real site-supervisor feature

## Built
- Royce spot-checked a generated PM Check Report for site SY1 and asked whether reports actually reflect the site-supervisor feature (migration `0201`/PR #681) — screenshot showed Supervisor / Contact Email / Phone all rendering `—`. Traced the field instead of assuming it was just missing data.
- Found `buildPmAssetReportInput` (`lib/reports/pm-asset-report-input.ts`) was never wired to `sites.supervisor_id` at all: `supervisorName` was sourced from `maintenance_checks.created_by` (whoever internally created the check — a tech/admin, resolved via `userMap`), and `contactEmail`/`contactPhone` were hardcoded literal `'—'` on every report, unconditionally. The site query itself didn't even select `supervisor_id`.
- Fixed: site query now also selects `supervisor_id`; a new parallel fetch joins `service.site_contacts` on that id and reports its real `name`/`email`/`phone`, replacing both the wrong `created_by` lookup and the hardcoded dashes. Left `reviewerName` (the separate internal "Technician / prepared by" sign-off field) untouched — different concept, already correctly wired to `created_by`.
- Verified live against `ehow`: SY3 (has a supervisor assigned) now resolves Pradeep Singh's real name/email/phone through the new query path; SY1 (genuinely has no supervisor contact assigned) correctly still resolves all three to `—` — confirming the screenshot that triggered this was accurate data, not a symptom of the bug for that particular site.
- `tsc --noEmit` clean; `pm-asset-report.smoke.test.ts` + `pm-asset-report-with-tests.smoke.test.ts` 4/4 pass (unaffected — they feed hardcoded input directly). eq-service [PR #683](https://github.com/eq-solutions/eq-service/pull/683) — merged.

## Decided
- Branched fresh off `origin/main` for this fix rather than stacking on the worktree's original branch (`claude/fix-sites-assets-deleted-at-trig`), which had already been merged as PR #682 earlier the same session — avoids confusing history on an already-shipped branch.
- Merged directly on Royce's "merge" instruction, no further confirmation needed.

## Deferred
- No sweep done of the pre-visit tech-brief path (`lib/notifications/send-pre-visit-brief.ts`) or `lib/reports/generate-and-store.ts` — both call `buildPmAssetReportInput` directly, so they inherit this fix automatically, but neither was independently re-verified live this session.

## Notes
Same underlying feature (site-supervisor, PR #681) as the trigger-bug session logged below — this is a second, independent gap in the same feature's rollout (the report layer never consumed it), not a regression from that session's own fix.

---

# Session 2026-08-02 — eq-solves-service: root-caused "Could not update site access" to a 6-day-old dead trigger, fixed and verified live

## Built
- Royce reported a real save failure via screenshot: assigning site supervisor Pradeep Singh on site SY3 (Equinix) failed with "Could not update site access — please try again." Root-caused via Postgres logs (`get_logs`) rather than guessing/retrying: `service.sites_update_trig()` and `service.assets_update_trig()` both reference `OLD.deleted_at` in their `deleted_at` CASE expression, but neither `service.sites` nor `service.assets` exposes a `deleted_at` column in its SELECT list — plpgsql doesn't type-check `OLD`/`NEW` field access at `CREATE FUNCTION` time (generic `record` type), so this compiled clean and only threw at runtime: `record "old" has no field "deleted_at"`.
- Confirmed blast radius via `service.audit_logs`: zero successful `site`/`asset` update entries anywhere since migration `0193` (2026-07-27) — both triggers have been fully dead for 6 days, just never exercised until Royce's own live test of the new supervisor picker (migration `0201`) became the first-ever UPDATE through `service.sites`.
- Identical bug, already fixed once: migration `0199` (2026-08-01, unrelated PR #663 — customer-logo write-through) had already hit and fixed the same class of bug for `customers_update_trig()`. Wrote migration `0202_fix_sites_assets_update_trig_deleted_at.sql`, applying the identical fix pattern (read `deleted_at` live from `app_data.sites`/`app_data.assets` via a `v_old_deleted_at` local variable, instead of the never-valid `OLD.deleted_at`) to the two triggers `0199` didn't cover.
- Verified twice via a rolled-back live transaction against `ehow` (`BEGIN` → fake tenant JWT claim via `set_config` → real `UPDATE service.sites` → `ROLLBACK`, zero persistent risk): once before writing the fix (confirming the bug), once after deploying it — the second run exercised the actual live (fixed) trigger against the exact site (SY3) and supervisor (Pradeep Singh) from Royce's failed screenshot, and it succeeded.
- eq-service [PR #682](https://github.com/eq-solutions/eq-service/pull/682) — merged; migration applied live via the governed `apply-service-migrations.yml` dispatch (run `30726591629`), confirmed `"Apply to ehow: completed success"`.

## Decided
- No explicit "go"/"merge" needed beyond the standing precedent this session already established for bug fixes surfaced by Royce's own live testing — proceeded to root-cause, fix, and deploy directly since it was blocking the feature just shipped, then reported the outcome.

## Deferred
- Royce to retry the actual save in the browser to confirm end-to-end — DB-level fix is verified via dry-run, only the real click-through confirms the full path.
- No sweep done across the other ~22 canonical objects (`defects`, `contract_scopes`, `job_plans`, `maintenance_checks`, etc.) for the same "trigger references a column the view doesn't expose" bug class — this fix only covers the three objects (`customers`/`sites`/`assets`) touched by migration `0193`. Worth a dedicated audit session, not assumed clean.

## Notes
This is not a bug introduced by this session's own supervisor-field work (PRs #679–681) — it's a pre-existing regression from `0193`, six days old, that simply had no live exerciser until Royce's own test of the new feature. `pending.md` and this repo's changelog updated in the same pass (this entry follows below the supervisor-field session log for the same day).

---

# Session 2026-08-02 — Fixed stale SKS brand color in the customer-facing quote portal

## Built
- `src/portal/QuotePortal.tsx` (eq-shell): replaced all 12 occurrences of the stale SKS navy `#1F335C` with the verified-live `#203060`. Re-checked the live value against `organisations.branding.palette.primary` on eq-canonical (jvkn) before changing anything — confirmed still `#203060`, not stale. eq-shell [PR #1173](https://github.com/eq-solutions/eq-shell/pull/1173), merged.
- Left the file's existing hardcoded-hex styling pattern intact (it's deliberately exempt from `@eq-solutions/tokens` via its own `no-restricted-syntax` eslint-disable) — corrected the value only, did not migrate to tokens.

## Decided
- Scope stayed to the one named file (QuotePortal.tsx). Found the same stale color in 3 more files while verifying scope completeness (`netlify/functions/quote-email.ts`, `src/components/MobileRecordsDrawer.tsx`, `netlify/functions/_shared/quote-pdf.tsx`) — flagged rather than silently expanded into, per the authority model (real issue, out of current scope → spawn_task, not a fix-and-warn dangle).

## Deferred
- 3 files above still carry the stale `#1F335C` — dispatched as background task `task_b6ad6f19`, Royce started it in a separate session. Not confirmed done as of this session's close.

## Notes
This closes out the second half of a pending.md item from the 2026-08-01 UI/UX audit session, which had already found and deferred this exact QuotePortal.tsx issue (as `task_2d3f2710`) without building the fix. This session did the actual fix and re-verification.

---

# Session 2026-08-02 — Closed out the remaining 3 stale-brand-color files (eq-shell)

## Built
- `netlify/functions/quote-email.ts`, `src/components/MobileRecordsDrawer.tsx`, `netlify/functions/_shared/quote-pdf.tsx`: replaced the remaining `#1F335C` occurrences with the verified-live `#203060`, matching each file's existing styling pattern (inline email CSS, avatar-color array, PDF constants block) — no migration to `@eq-solutions/tokens`.
- Repo-wide case-insensitive grep for `1F335C` returned zero matches after the edits — full sweep confirmed clean.
- eq-shell [PR #1173](https://github.com/eq-solutions/eq-shell/pull/1173) — same PR as the QuotePortal.tsx fix above (all 4 files landed together), merged to `main`, live-verified on `origin/main`.

## Decided
- Royce confirmed both "go" (commit) and "merge" (land on `main`) — merge triggers Netlify's auto-deploy to core.eq.solutions per repo governance, surfaced explicitly rather than silently.

## Deferred
- None — this was the last open piece of the brand-color sweep.

## Notes
**Merge race condition, recovered cleanly:** the shared branch (`claude/site-advisory-flag-pair-grant-fix`) carried both an unrelated pre-existing commit (`719713cc`, its own PR #1171) and this session's color-fix commit (`559b889d`) stacked on top. Royce merged PR #1171 directly via GitHub moments after this session pushed — GitHub auto-deleted the branch on merge, orphaning the un-merged color-fix commit. Recovered by branching `claude/sks-navy-brand-color-fix` off the still-intact local commit, opening a fresh PR #1173, and merging that. Verified live by reading `quote-pdf.tsx` directly off `origin/main` post-merge (not just trusting the merge API's success response) — confirms `NAVY = '#203060'`. Lesson: when a shared branch carries someone else's independent commit, a squash-merge on their PR can silently orphan work stacked on top if the branch auto-deletes — worth pushing to a dedicated branch rather than a shared one when stacking unrelated work.

---

# Session 2026-08-02 — eq-solves-service: closed the last two lint follow-ups (next/image conversion), correctly re-scoped the other

## Built
- **`next.config.ts`**: added `images.remotePatterns` for the ehow Supabase storage host (`logos` bucket) — the only remote image source this app ever renders.
- **8 `<img>` → `next/image` swaps** across `components/ui/Sidebar.tsx` (brand logo x2), `components/ui/MediaPicker.tsx` (preview/dropdown/thumbnail x3), and `app/(app)/admin/media/MediaLibraryClient.tsx` (grid tile + edit-modal preview x2, incl. the SVG-specific branch). Fixed-pixel thumbnails use numeric `width`/`height`; variable-aspect containers use `fill`+`sizes` (added `position: relative` to their parents). All use `unoptimized` — this app allows SVG logo uploads, and next/image's built-in optimizer refuses SVG without a relaxed image CSP not worth opening on this app's tightly-scoped CSP. `tsc --noEmit` clean, `next build` succeeds, full-repo eslint re-scan: 8 → 0 findings, zero regressions. eq-service [PR #676](https://github.com/eq-solutions/eq-service/pull/676), merged (real gate `tsc + next build` green; `Integration tests (Supabase local)` red as the standing accepted pre-existing failure).

## Decided
- Royce chose "do item 2 now, defer item 1" after I corrected my own earlier framing of item 1 (the Supabase `no-explicit-any` insert/update typing gap) — it's already-known, already-documented team debt (`docs/30-day-plan.md` B6), not a fresh bug, and the available tooling here can't regenerate multi-schema types to fix it properly.
- "merge" — merged PR #676 to `main`, which auto-deploys `service.eq.solutions` via Netlify on push.

## Deferred
- Item 1 (Supabase insert/update typing gap, B6) — left as already-tracked team debt, not attempted. Needs the Supabase CLI's multi-schema type generation (not available via the MCP tool used this session, which only pulls `public`) plus per-site re-verification of ~17 call sites, since the underlying table (`service.assets`) is a view with its own write trigger, not a plain auto-updatable view.
- Royce to click through live: sidebar logo + admin Media Library page (grid + edit modal) for a tenant with a logo set, to confirm the swap renders correctly.

## Notes
Re-investigating item 1 before building it turned up a materially different picture than how it was first described (a fresh typing bug) — surfaced the correction to Royce via a question before proceeding rather than either silently attempting a risky multi-schema regen or silently deferring without explanation. Substrate (changelog + pending.md) for this PR was already pushed to `main` earlier in this session, ahead of this log entry.

---

# Session 2026-08-02 — eq-shell: Sentry sweep, fixed 3 real bugs + confirmed a 4th already fixed

## Built
- Triaged all 8 unresolved eq-shell Sentry issues via the Sentry MCP before touching any code — 3 were scheduled data-quality monitors (`check-never-invited-workers`, `check-identity-collisions`, `check-duplicate-shell-accounts`) correctly firing via `captureServerError` on real data, not bugs; 1 (mint-cards-otp 500, EQ-SHELL-13) was already fixed by merged PR #1150 before the reported event occurred.
- `src/App.tsx` — `ChunkErrorBoundary.componentDidCatch` now also calls `Sentry.captureException(error, { contexts: { react: { componentStack } } })` for genuine "stuck-crash" cases (EQ-SHELL-10). Previously only the error's `.message` string reached Sentry via `captureAuthEvent`/`captureMessage`, discarding the real stack trace — every occurrence of "Cannot read properties of null (reading 'useMemo')" on `/sks/intake` was undiagnosable beyond that one line. Ruled out a duplicate-React-copy cause first (checked `@eq/intake-demo`'s `package.json main` points at raw `.ts` source compiled into eq-shell's own Vite build, not a separately-bundled dist; lockfile resolves a single `react@19.2.8`). Root cause of the crash itself is still open — Sentry's Seer analysis returned HTTP 402 (no budget), so this fix only makes the next occurrence traceable.
- `netlify/functions/verify-shell-session.ts` — folded the `tenant_role_overrides` read into the existing `Promise.allSettled` batch (chained off the memberships read, which is all it depends on) instead of awaiting it as a trailing sequential round-trip after the batch resolved. That tail latency was still occasionally clearing the client's 15s deadline even after PR #888's earlier parallelization (EQ-SHELL-T, "regressed" substatus — first fix cut most but not all occurrences).
- `src/pages/CardsIframe.tsx` — added one retry (500ms backoff) on a network-level `fetch` failure (TypeError, e.g. Safari's generic "Load failed") in the `mint-cards-otp` REQUEST_SHELL_TOKEN handler (EQ-SHELL-12). Never retries a completed 4xx/5xx response, to avoid burning GoTrue's magic-link rate limit on a genuine server-side failure.
- Marked EQ-SHELL-13 resolved in Sentry with a comment citing the fixing PR and confirming the event predates it (verified `8d5dc9bf` is an ancestor of current `main` and its timestamp precedes the Sentry event).
- eq-shell [PR #1174](https://github.com/eq-solutions/eq-shell/pull/1174) — `pnpm run build`, `tsc` (both `tsconfig.netlify.json` and `tsconfig.app.json`), `eslint`, `check:perms`, `check:css` all clean (confirmed zero *new* lint issues via `git stash` diff against the pre-existing baseline). CI green, squash-merged to `main` (`b7f809b`) on Royce's explicit "merge" instruction — Netlify auto-deploys `core.eq.solutions` from `main`. Source branch deleted (had to finish the delete manually — `gh pr merge --delete-branch` partially failed because another local worktree on this machine had `main` checked out, which blocked `gh`'s local git sync; the GitHub-side merge itself succeeded).

## Decided
- Royce: "merge" — explicit instruction to land PR #1174 on `main`, understood as authorizing the resulting Netlify auto-deploy per repo governance.

## Deferred
- **EQ-SHELL-Y (ocr-licence 401)** — not code-fixable from eq-shell. Traced via Supabase MCP (`get_edge_function` on jvkn/`jvknxcmbtrfnxfrwfimn`) to the `ocr-licence` edge function's own manual trust check: a literal string equality between the caller's bearer token and the function's own `SUPABASE_SERVICE_ROLE_KEY` env var (gateway `verify_jwt` is off, so this is the function's only auth gate for server-to-server calls). Classic rotation-drift risk between eq-shell's Netlify copy of that key and jvkn's live one. Notably the *second* distinct root cause behind this same 401 symptom — 2026-07-23's version (`task_d94af51d`) was a stale CORS deploy, unrelated. Needs Royce to confirm/rotate the key; flagged in pending.md rather than guessed at.
- **EQ-SHELL-Z (1 identity collision on jvkn) and EQ-SHELL-11 (1 duplicate Shell account)** — working-as-designed data-quality alerts with real row IDs already in the Sentry event payload. Needs an operator decision on the underlying data, not a code change — flagged in pending.md.
- EQ-SHELL-X (44 never-invited workers past grace) — same underlying backlog as the existing 2026-07-21 pending.md item (was 45, now 44 — one resolved naturally); updated that item rather than duplicating it.

## Notes
Session-brief gate (`/brief eq-shell`, project CLAUDE.md Rule 0.6) fired mid-task via the `eq-guard` hook on the first `Edit` call — ran it retroactively (digest + worktree registry + git staleness check on the actual target repo, since the skill's own `$ARGUMENTS` path substitution broke on Bash's backslash handling and needed a manual path fix) before continuing, per the hook's hard block rather than the tool-permission-style soft ask.

---

# Session 2026-08-02 — eq-solves-service: two fixes prompted by a Royce screenshot (upload limit, report cover layout)

## Built
- **Media Library upload limit raised 2 MB → 5 MB** — Royce hit the old cap uploading a real site photo (a building exterior shot, ~2-3 MB typical). Updated `MEDIA_MAX_SIZE` in `admin/media/actions.ts`, its error string, and the upload form's helper text in `MediaLibraryClient.tsx` together. eq-service [PR #678](https://github.com/eq-solutions/eq-service/pull/678), merged.
- **PM Asset Report cover: brand band no longer disappears when a site photo is uploaded.** Royce compared two generated report covers side by side (with/without a site photo) and asked to keep the blue branded band and put the photo underneath it instead of the two being mutually exclusive. `buildCoverPage()` in `lib/reports/pm-asset-report.ts` previously branched on `input.sitePhoto`: present → photo + a caption strip with a duplicate customer-logo plate; absent → flat brand-colour band with the customer logo centred. Collapsed to: band always renders, photo (if present) renders below it with a simplified site name/address caption only — dropped the duplicate customer-plate since the band above already carries that identity. eq-service [PR #677](https://github.com/eq-solutions/eq-service/pull/677), merged.
- Both verified `tsc --noEmit` clean; PR #677 additionally re-ran `pm-asset-report.smoke.test.ts` + `pm-asset-report-with-tests.smoke.test.ts` (4/4 pass) since it touched report-generation logic, not just config/copy.

## Decided
- "merge" — Royce's instruction covered both fixes raised in the same exchange; merged both PR #677 and PR #678 after confirming the real CI gate (`tsc + next build`) went green on each (polled rather than assumed).
- Each fix went on its own branch/PR off `origin/main` rather than stacking on the other's branch — unrelated changes, and the repo's `main` checkout was already held by a sibling worktree (`eqsvc-loadtime-ux`), so new branches were created directly off `origin/main` instead of checking out `main` locally.

## Deferred
- Royce to spot-check a generated PM Asset Report live for a site that has a photo on file — confirm the band + photo layout reads correctly in practice, not just in the smoke tests.

## Notes
Branching off `origin/main` while a sibling worktree already had `main` checked out caused a `git stash` / branch-switch / `stash pop` sequence to transiently show the *other* branch's already-merged files (media upload actions.ts/MediaLibraryClient.tsx) as reverted to their pre-fix state in the diff — expected (those files' fix lives only on the sibling branch, not yet on `main` at that point), not a regression, but worth remembering the sequence can look alarming mid-flight. Also opened PR #678 for the upload-limit branch a step later than intended — had referenced its PR number in pending.md before actually creating it, then had to create it and fix the branch-vs-active-checkout mismatch (`gh pr create` failed once against the wrong currently-checked-out branch) before the number resolved for real.

---

# Session 2026-08-02 — eq-shell: fixed 2 duplicate-identity Sentry alerts live, found their likely shared root cause

## Built
- Investigated the two flagged data-integrity alerts left over from the earlier Sentry sweep this session, rather than taking their "canonical"/"duplicate" labels at face value — traced actual account/worker/staff records on jvkn (eq-canonical) and ehow (sks-canonical) via the Supabase MCP for both.
- **EQ-SHELL-11 (Zemi Asri)**: found the alert's labels were backwards — the account it called "canonical" was an empty, never-logged-in stub (`fb20ce14`), while the one it called "duplicate" (`ffe1de8e`) was his real, actively-used account (real name/email, real Field/Service logins as recently as the day before). Something had silently repointed `public.workers.user_id` (and, via what looks like a near-real-time sync, `app_data.staff.user_id` on ehow) from his real account to the empty stub, with no audit-log entry recording who or what did it. Royce confirmed "fix now" via AskUserQuestion. Repointed `public.workers.user_id` back to `ffe1de8e` (confirmed the ehow sync picked it up within ~2 seconds on its own), deactivated the stub account and its 2 tenant memberships, logged the correction as `shell_control.audit_log` id 4183, marked the Sentry issue resolved with a comment.
- **EQ-SHELL-Z (Collin Toohey)**: simpler case — his real account (`1becb454`) and its worker/staff links were correct throughout; the flagged "new_user_id" (`38859cae`) was a completely empty orphaned stub (no email/phone actually saved, never logged in) created in a different tenant alongside a duplicate `public.workers` row, both artifacts of a failed signup attempt that shared his email. Royce confirmed via AskUserQuestion. Deactivated the stub + its membership, set `resolved_at`/`notes` on the underlying `identity_collision_flags` row (this check reads that table directly, not account-active status, so deactivating the account alone wouldn't have cleared it), logged as `shell_control.audit_log` id 4186, marked the Sentry issue resolved.
- **Found the likely shared root cause, not yet fixed at the source**: phone numbers are stored inconsistently between `public.workers.phone` and `shell_control.users.phone` — confirmed 3 separate examples (`+61408164924` vs `61408164924`; `+61439109013` vs `0439109013`). Whatever matches an incoming phone number to an existing worker during signup/linking most likely fails silently across these formats, which is consistent with both accounts above being created as brand-new instead of recognized as existing. The exact function/line responsible is still unidentified (leading candidates: `eq_cards_link_or_create_worker`, which writes `identity_collision_flags`, or `workers-canonical-sync`) — flagged in pending.md as needing its own investigation session, since neither of today's fixes touched the actual cause.
- Checked whether the `ocr-licence` 401 (the other deferred item from the earlier sweep) had recurred — no new events since 2026-07-30, inconclusive either way. Confirmed I have no way to read or compare the actual service-role-key values via any available tool (by design), so this genuinely stays with Royce.

## Decided
- Royce: "let's fix these issues" — authorized investigating and fixing both deferred data-integrity alerts.
- Royce, via AskUserQuestion: "Fix now" for Zemi Asri's account repoint + stub deactivation.
- Royce, via AskUserQuestion: "Yes, deactivate it" for Collin Toohey's orphaned stub.

## Deferred
- Find and fix the actual phone-number-normalization bug causing duplicate identities (see Built above) — needs its own session, not a quick follow-up.
- `ocr-licence` 401 — unchanged from the earlier sweep, still needs Royce to check/rotate the jvkn service-role key himself.

## Notes
No code changes this round — both fixes were direct, audited database corrections via the Supabase MCP (not a migration; matches the precedent "manual merge runbook" pattern already used for similar identity fixes in prior sessions), each logged into `shell_control.audit_log` for traceability since neither the repoint nor the stub-deactivation would otherwise leave any record of who/why. Updated `eq/pending.md` inline as the fixes landed (ticked the two deferred items, added the phone-format root-cause item) rather than waiting for this close. Added a new memory file (`phone-format-inconsistency-identity-bugs.md`) documenting the confirmed pattern across both accounts for the next session that hits this. Also compacted `MEMORY.md` from 161 to 137 lines per the harness's line-limit prompt — pruned ~17 fully-closed index entries (topic files remain on disk per the file's own stated pruning policy), kept everything with any open thread or reusable lesson.

---

# Session 2026-08-02 — built the /gap centering skill (light mode default)

## Built
- New Claude Code skill `/gap` — "the centering process" for when a project's status has drifted from gut feel. Two files, same convention as `/decide`/`/brief`: full protocol in `eq-context/rules/gap-protocol.md`, thin trigger in `~/.claude/commands/gap.md`.
- First draft was the heavier version: a self-contained HTML artifact with Hero / Current Reality (strict status labels) / Desired State (100/100 moments) / Gap Analysis (P1-P3 table mapped to named moments) / Appetite & Kill Criteria / Centering Action, plus a documented "revise mode" for terse section-keyed follow-up annotations (infer intent — correct wording / reframe status / delegate ownership / act now / explain further — from phrasing, not a tag syntax).
- Royce then asked to compare that against a much shorter prompt he'd written by hand (one-sentence problem + 4 blunt questions: Current Reality / Desired Outcome / Gaps / Next Move with a time box, chat output, under a page). Recommended it as a genuine efficiency win for the common "I feel lost" case, with one caveat: it dropped the live-verification step, which is exactly what caught two wrong claims (a "Parked" feature that was actually live+deployed, a "3 profiles" count that was actually 12) the one time this process ran for real (EQ Intake gap analysis, same day).
- Royce: "let's just do the light version for now, we can always expand on it later — steel man then build." Steelmanned in one line (light mode matches actual usage, ships today, nothing lost since the heavy spec gets kept not deleted), then restructured `gap-protocol.md`: light mode (the 4-question shape, one cheap `git log` check baked in before "Current Reality") is now the default; the full HTML/artifact/gap-table spec is kept in the same file under a clearly marked "Full mode (parked)" section for later expansion, not built as the active path.
- Committed and pushed: `eq-context` [0d82bf5](https://github.com/eq-solutions/eq-context/commit/0d82bf5) → `origin/main`. `~/.claude` commands repo has no remote — committed locally to `master` only.

## Decided
- Royce: ship light mode only for now; full artifact mode stays parked in the file, not built out further, until actually needed.
- Royce: "merge" — confirmed committing + pushing both files.

## Deferred
- None actionable — "full mode" is a deliberate scope decision (documented, not built as default), not a to-do waiting on anyone.

## Notes
This session (not eq-shell, eq-service, eq-field, or eq-cards) touched no product code — nothing to add to Step 4's product changelogs. `~/.claude/commands` is a separate local-only git repo (no origin remote); its commit is not pushed anywhere, which is expected — there is nowhere to push it to.

---

# Session 2026-08-02 — EQ Intake "centering" gap analysis found two of our own docs were wrong

## Built
- Ran a live-grounded gap analysis on EQ Cards + Intake at Royce's request ("I feel lost in the progress"). First pass built from `suite-state.md`/`digest.md`/STATUS docs; Royce's revised prompt added one hard requirement — ground every claim in git/DB/deploy state, not docs or memory, live wins if they disagree. Re-ran on that basis.
- That re-check found two real, live-contradicted claims: **Cards→Field SSO sending-half**, documented as unbuilt, has actually been live since PR #812 (Jul 13)/#1145 (Aug 1); **EQ Intake reshape-out**, documented as ~3 SimPRO profiles, is actually 12 registered profiles (SimPRO/Xero/MYOB/4 Equinix formats) per `eq-format-ui/src/derive/registry.ts`.
- Live DB query (jvkn) surfaced a genuinely new, previously-unmeasured number: 44 of 97 `public.workers` rows have no linked `shell_control.users` row — none stale (0 >90 days, 10 created in the last 30), so not legacy debt. Royce's read: expected, team's being onboarded in tranches, not a bug.
- Two HTML artifacts published (build-review, then the corrected "centering" version) reflecting the above, plus Royce's own follow-up corrections (genericized "payroll" language since SKS runs Workbench not Xero; reframed the 44-worker gap as in-progress per his call).
- Corrected `EQ-CARDS-INTAKE-BRIDGE.md` (the stale doc both wrong claims traced back to) — done in a fresh worktree off `origin/main` since the root `eq-intake` checkout had unrelated uncommitted work from another session on `claude/trades-settings-and-dismiss`. eq-solves-intake [PR #103](https://github.com/eq-solutions/eq-solves-intake/pull/103), squash-merged `dddbe4e`. Worktree removed, registry row closed out.
- Updated stale memory (`cards-field-sso-sending-half.md`) that had been asserting the SSO gap was still blocked.
- Spun off the prompt Royce refined across this session as a background task; it landed as the new `/gap` skill (see the entry above this one — same day, different session).
- **Follow-up correction, same session:** explained the 44-workers gap and the "auto-invite on staff creation" question in plain terms; Royce pushed back — "I'm almost certain there's a whole ONE LOGIN design built and tested for this." He was right. Found `admin/users/migrate` (`AdminInviteMigratedStaff.tsx`, PR #175, refined by #201 worker-identity-linker and #278 `backfill-worker-links.ts`) — live, routed, actively maintained (touched as recently as yesterday's router upgrade). `staff-invite-candidates.ts` classifies a tenant's staff as ready/already-user/already-invited/no-email; `invite-users-batch.ts` sends the invites. Corrected the memory again — this wasn't an unbuilt policy question, it's a real shipped tool (manager-reviewed bulk invite, deliberately not silent full automation).
- **Closed the reshape-regression-proof deferred item** ("fix the exported worker issue"): ran `/brief` properly this time (caught myself skipping straight to a worktree/registry edit before presenting it — stopped, wrote the flag, presented the brief, waited for "confirm"). Added 8 tests to `derive-profiles.test.ts` for `xero-payroll-timesheets` — the literal "Friday problem" profile — using fixtures conformant to `@eq/schemas`' `timesheet.schema.json` on purpose (that schema has no name field, only `staff_id`). Built the package (`pnpm -r build`, needed for `@eq/ai`/`@eq/validation` — fresh worktree, gitignored dist), ran the suite for real (not just written blind) — first pass had 1 failing assertion, my own bug (expected `.toFixed(2)` formatting on the overtime note, the real code uses the raw `hours` number), fixed and re-ran to 50/50 green. `tsc --noEmit` clean. **Finding: no silent data loss.** `employeeName()` already has a deliberate, labelled `Staff:<uuid>` fallback for name-less canonical rows — confirmed live, not assumed from the comment. eq-solves-intake [PR #104](https://github.com/eq-solutions/eq-solves-intake/pull/104) — CI green, Royce said "merge," squash-merged `e8e1982`, branch deleted.

## Decided
- Royce: Desired-State language should stay generic ("payroll") rather than naming Xero/MYOB specifically, since SKS's actual system is Workbench.
- Royce: the 44-unlinked-workers number is expected tranche rollout, not a defect — don't build anything against it, just watch the pace.
- Royce: "merge and continue" on PR #103 — explicit go, merged same session.
- Royce: "merge" on PR #104 — explicit go, merged same session.

## Deferred
- Royce investigating himself: whether Intake can push timesheets into Workbench. Not something this session could check — no Workbench access/API surface found in the 12 existing reshape profiles.
- No reshape regression proof exists (12 profiles tested against fixtures, never a real tenant-shaped record) — flagged, not built.
- The worker-pool vs. control-plane architecture question the bridge doc was originally written to answer is still open — explicitly not resolved by today's correction, needs its own re-audit.
- Unconfirmed whether `admin/users/migrate`'s SKS candidate count matches the 44 exactly — it reads `app_data.staff` and matches by email, a different angle than `public.workers`; `#175`/`#201` don't themselves write `workers.user_id` (that's `backfill-worker-links.ts`, called separately), so even a fully-invited staff list doesn't guarantee the Cards-side link is backfilled.

## Notes
- Two live-verification catches this session (Field SSO, profile count) are the concrete case-study for why the `/gap` skill's live-grounding step matters — referenced directly in the other session's write-up above.
- `eq-intake` root checkout being dirty (another session's in-progress work) is exactly the shared-checkout risk this file's own root-cause note (2026-07-23, eq-solves-service) describes — routed around it with a fresh worktree rather than touching the dirty tree, per that lesson.
- **Second lesson this session, this time on me, not the docs:** framed a real built feature as an "open policy question" purely from not having looked for it — the inverse mistake of the doc-drift catches above (there, docs said "not built" when it was; here, I said "undecided" when it was already decided and shipped). Both failure modes come from the same root cause the `/gap` live-grounding rule targets: check the actual system before asserting state, in either direction.

---

# Session 2026-08-02 — eq-solves-service: retired a dead sprint doc, built site supervisor, caught a wrong data source before it shipped

## Built
- **`docs/SPRINT-2026-06-golive-hardening.md`**: added a "SUPERSEDED — do not action" banner. Its Phase 2 "Import from Canonical" feature was never built and is now architecturally incompatible with the read-only canonical view model (urjh tenant project it assumed is deleted; live DB is ehow). Direct commit, doc-only.
- **`CLAUDE.md`**: noted the Sites admin list stays a flat table by design — live count is 11 active SKS sites, not the ~554 the old sprint doc assumed. Revisit only past ~40-50 active sites.
- **Sites list**: removed the always-static `is_active` "Status" column, replaced with a URL-param-driven "Show archived" checkbox (mirrors `SearchFilter`'s pattern) and an inline "Archived" tag next to the site name.
- **Site supervisor field** (`supervisor_id`, migration `0201_site_supervisor.sql`): plain uuid on `app_data.sites`, no FK (verified live that the established `maintenance_checks.assigned_to` precedent also carries no FK to `auth.users`, since Plan B JWT auth never creates a Supabase session/`auth.users` row). Passed through `service.sites`'s canonical view + `INSTEAD OF` update trigger. Editable via a new picker on `SiteAccessCard.tsx`.
- eq-service [PR #679](https://github.com/eq-solutions/eq-service/pull/679) (site supervisor + Status/Source fix) — merged.
- **Migration dispatch failed on first attempt**: `42P16: cannot change name of view column "canonical_id" to "supervisor_id"` — the new column was inserted before the `canonical_id`/`canonical_field_id`/`canonical_synced_at` trio, shifting their position (`CREATE OR REPLACE VIEW` requires every pre-existing column to keep both name and position). Verified live the failed transaction rolled back cleanly (no ledger row, no column, view/trigger unchanged) before touching anything further. Fixed by moving `supervisor_id` to genuinely be the last column. eq-service [PR #680](https://github.com/eq-solutions/eq-service/pull/680) — merged, migration re-dispatched via `apply-service-migrations.yml`, verified live: ledger row + base column + view column all present.
- **Royce caught the real design bug**: the supervisor picker was wired to `getTechnicianRoster()` — the SKS/tenant staff roster — but a site supervisor is the *customer's* own on-site contact (e.g. Equinix's site lead), not SKS's own employee. Rewired `SiteAccessCard.tsx`/`page.tsx` to source from `site_contacts` (already fetched for the same page's Contacts card) instead. Removed the now-unused `Technician`/`getTechnicianRoster` helper from `lib/canonical-members.ts`. Updated the `Site.supervisor_id` type comment to match. eq-service [PR #681](https://github.com/eq-solutions/eq-service/pull/681) — merged, no schema change needed (column was already a plain uuid with no FK).

## Decided
- Royce: "3 then 1" — build the site-supervisor field first, then the Status/Source column honesty fix, in that order.
- Royce: "go" (commit + open PR), "merge" (land PR #679), then again for #680 and #681.
- Royce: site supervisor must be a customer-side contact, not a tenant/SKS staff member — corrected mid-flight after seeing the live dropdown populated with SKS names.

## Deferred
- Royce to click through live: assign a supervisor from a site's own contact list, save, reload, confirm it persists; toggle "Show archived" and confirm it filters/tags correctly. Standalone localhost testing is blocked by the Shell-auth iframe handshake screen in this environment, confirmed via console logs — not something this session could work around.

## Notes
- `apply-service-migrations.yml`'s documented "pauses for production-environment approval" behavior wasn't observed in either dispatch run (both completed in ~15s with no visible manual gate) — flagged, not investigated further; possibly an environment-protection-rule config gap or aspirational doc language.
- `Canonical types drift` and `Integration tests (Supabase local)` CI checks failed on PR #679 for pre-existing, unrelated reasons (confirmed via `gh run list --branch main` history and reading the actual failure logs) — not blocking per repo convention for the latter, independently verified for the former.

---

# Session 2026-08-02 — Corrected a Grok-authored EQ Intake doc, then automated the vendor-sync check and verified it live end-to-end

## Built
- Rewrote `eq-intake-current-gap-analysis-2026-08-02.html` (delivered to Royce's Downloads, not a repo file) correcting an externally-authored (Grok) EQ Intake gap-analysis doc against live substrate: EQ Cards moved Partial → Live (real self-signup/claim traffic); "EQ Import" retired as a separate product label (it's the file-drop tab inside EQ Intake, now "Bring Data In"); EQ Capture corrected from "built then parked" to "never built" (schema-only, forward-looking columns, no product ever shipped); every SimPRO/named-client-portal reference removed (contradicted by the real 2026-07-05 genericization work that stripped client names from the product).
- Ran a `/decide` pass on the doc's P1/P2 items before building anything. Verified live rather than trusting the doc: the AI duplicate-adjudicator (`adjudicateDuplicateWithAI`) turned out already fully wired into the Sites dupes UI (an earlier claim in-session that it wasn't was wrong — corrected immediately once found); the "schema drift" P2 item turned out to be a deliberate, already-documented decision (`tenant-drift.yml`'s own comment: non-blocking until EQ-plane quoting go-live), not unfinished work — migration `0035` had already fixed the 5 real column diffs months ago.
- Built `eq-shell/.github/workflows/intake-revendor-check.yml` (scheduled + `workflow_dispatch`) + `eq-shell/scripts/check-intake-version-pins.mjs` (+ `check:intake-pins` script) — automates the previously-manual `revendor-intake.mjs` step that caused two real production incidents from react version-pin drift (eq-solves-intake #102, eq-shell #1161). Opens a PR only on real drift; never auto-merges.
- Insisted on watching it run live end-to-end rather than trusting the build, and that caught 3 more real bugs in sequence, each fixed and re-verified live before moving on:
  - `pnpm/action-setup` couldn't auto-detect its version because the job checks eq-shell out to a custom subpath — fixed with `package_json_file`.
  - PRs opened with the default `GITHUB_TOKEN` don't trigger `pull_request`-gated workflows (GitHub's anti-recursion protection) — the first auto-opened PR (#1178) silently got zero real CI, only Netlify's checks. Also surfaced, in passing, that eq-shell's `main` has no branch protection at all (flagged in pending.md, not fixed). Fixed by authenticating as a PAT instead.
  - The PAT used (`EQ_CONTEXT_PAT`, reused from `notify-substrate.yml`) had Contents access but no Pull-requests scope — replaced with a new dedicated `INTAKE_REVENDOR_PAT` (Contents + Pull requests, read/write, scoped to just eq-shell + eq-solves-intake).
  - Final failure: `INTAKE_REVENDOR_PAT`'s resource owner was Royce's personal GitHub account, not the `eq-solutions` organization that actually owns both repos — fine-grained PATs need Resource owner set to the org to see org-private repos at all. Royce fixed this directly in GitHub's UI; re-dispatch confirmed it live.
  - Also needed one org-level GitHub setting flipped (`Allow GitHub Actions to create and approve pull requests`, off by default org-wide) — Claude was blocked from doing this via API by the harness's own auto-mode classifier (org-wide security-setting changes need more than an in-chat "go ahead"); Royce flipped it via the GitHub UI.
- Final live dispatch (`30725961956`) produced a real PR (eq-shell [#1183](https://github.com/eq-solutions/eq-shell/pull/1183)) that got checked by all 4 real required workflows on its own — confirms the whole safety design actually works, not assumed.
- All 4 fix PRs merged: eq-shell [#1175](https://github.com/eq-solutions/eq-shell/pull/1175), [#1176](https://github.com/eq-solutions/eq-shell/pull/1176), [#1179](https://github.com/eq-solutions/eq-shell/pull/1179), [#1182](https://github.com/eq-solutions/eq-shell/pull/1182). PR #1178 (opened before the CI-trigger fix, never actually CI-verified) was closed as superseded rather than merged blind.
- 5 short-lived eq-shell worktrees created and removed cleanly over the course of the fix cycle; several stray auto-generated branches from failed automation runs cleaned up as they appeared. `eq-context/system/worktree-registry.md` updated throughout, final entry summarizes all 4 PRs + the live-verification chain.

## Decided
- Royce, via `/decide`: build the vendor-sync automation now; verify (don't build blind against) the AI-adjudication and schema-drift items first — both turned out to need correction, not a build.
- Royce: reuse `EQ_CONTEXT_PAT` initially rather than minting a new secret up front (later replaced once it proved under-scoped).
- Royce: commit locally, stop before pushing → then explicitly "push item 1 and watch the first live run" once the local build was ready.
- Royce: "merge and then continue" / "merge and then start building the next sprint" — explicit go on each of the 4 fix PRs in turn, including the org-wide "allow Actions to create PRs" setting and the PAT resource-owner fix, both done directly by Royce after Claude was blocked or unable to do them.
- Royce, on branch protection being missing: didn't ask for a fix, just acknowledged — left as a flagged, deliberate follow-up.

## Deferred
- eq-shell #1183 itself (the actual re-vendor content the automation found) — still needs Royce's normal review/merge, never auto-merges by design.
- eq-shell `main` branch protection — flagged, not fixed; needs Royce's call on which checks to require before setting it up.
- Extending AI-adjudication from Sites to Contacts/Staff dupes — real gap, but those work on a structurally different mechanism (grouped field-match duplicates, not paired same/different/unsure), needs its own design pass before building.
- "Next sprint" scope — asked, not yet answered as of this session's close.

## Notes
- The whole value of this session was in the live-verification discipline, not the first build: every one of the 4 real bugs found (pnpm auto-detect, GITHUB_TOKEN recursion guard, PAT scope, PAT resource-owner) was invisible from reading the workflow YAML alone and only surfaced by actually dispatching it and watching the real run fail in a new way each time. A "build it, believe the design doc's own claims, ship it" pass would have shipped a workflow that silently never worked.
- Claude attempted to flip the org-wide GitHub Actions PR-creation setting directly via `gh api` after Royce's explicit in-chat go — blocked by the harness's own auto-mode classifier (a separate, harness-level safety layer beyond in-chat confirmation) for an org-wide security-relevant change. Did not attempt to route around it via another tool; explained the block and had Royce do it via the GitHub UI instead. Worth remembering for future sessions: some org-level settings changes need UI action even with explicit chat authorization.
- This session ran in parallel with at least one other concurrent session also doing EQ Intake/Cards gap-analysis work (see "EQ Intake 'centering' gap analysis" entry above, and the eq-solves-intake PR #103 doc-correction entry) — different specific focus (external Grok doc + CI automation here, vs. the `/gap` skill + Cards/Field SSO + export-profile-count corrections there), no file conflicts, but worth noting both landed the same day on adjacent ground.
