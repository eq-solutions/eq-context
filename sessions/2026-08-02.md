# Session 2026-08-02 — Fixed stale SKS brand color in the customer-facing quote portal

## Built
- `src/portal/QuotePortal.tsx` (eq-shell): replaced all 12 occurrences of the stale SKS navy `#1F335C` with the verified-live `#203060`. Re-checked the live value against `organisations.branding.palette.primary` on eq-canonical (jvkn) before changing anything — confirmed still `#203060`, not stale. eq-shell [PR #1173](https://github.com/eq-solutions/eq-shell/pull/1173), merged.
- Left the file's existing hardcoded-hex styling pattern intact (it's deliberately exempt from `@eq-solutions/tokens` via its own `no-restricted-syntax` eslint-disable) — corrected the value only, did not migrate to tokens.

## Decided
- Scope stayed to the one named file (QuotePortal.tsx). Found the same stale color in 3 more files while verifying scope completeness (`netlify/functions/quote-email.ts`, `src/components/MobileRecordsDrawer.tsx`, `netlify/functions/_shared/quote-pdf.tsx`) — flagged rather than silently expanded into, per the authority model (real issue, out of current scope → spawn_task, not a fix-and-warn dangle).

## Deferred
- 3 files above still carry the stale `#1F335C` — dispatched as background task `task_b6ad6f19`, Royce started it in a separate session. Not confirmed done as of this session's close.

## Notes
This closes out the second half of a pending.md item from the 2026-08-01 UI/UX audit session, which had already found and deferred this exact QuotePortal.tsx issue (as `task_2d3f2710`) without building the fix. This session did the actual fix and re-verification.

---

# Session 2026-08-02 — Closed out the remaining 3 stale-brand-color files (eq-shell)

## Built
- `netlify/functions/quote-email.ts`, `src/components/MobileRecordsDrawer.tsx`, `netlify/functions/_shared/quote-pdf.tsx`: replaced the remaining `#1F335C` occurrences with the verified-live `#203060`, matching each file's existing styling pattern (inline email CSS, avatar-color array, PDF constants block) — no migration to `@eq-solutions/tokens`.
- Repo-wide case-insensitive grep for `1F335C` returned zero matches after the edits — full sweep confirmed clean.
- eq-shell [PR #1173](https://github.com/eq-solutions/eq-shell/pull/1173) — same PR as the QuotePortal.tsx fix above (all 4 files landed together), merged to `main`, live-verified on `origin/main`.

## Decided
- Royce confirmed both "go" (commit) and "merge" (land on `main`) — merge triggers Netlify's auto-deploy to core.eq.solutions per repo governance, surfaced explicitly rather than silently.

## Deferred
- None — this was the last open piece of the brand-color sweep.

## Notes
**Merge race condition, recovered cleanly:** the shared branch (`claude/site-advisory-flag-pair-grant-fix`) carried both an unrelated pre-existing commit (`719713cc`, its own PR #1171) and this session's color-fix commit (`559b889d`) stacked on top. Royce merged PR #1171 directly via GitHub moments after this session pushed — GitHub auto-deleted the branch on merge, orphaning the un-merged color-fix commit. Recovered by branching `claude/sks-navy-brand-color-fix` off the still-intact local commit, opening a fresh PR #1173, and merging that. Verified live by reading `quote-pdf.tsx` directly off `origin/main` post-merge (not just trusting the merge API's success response) — confirms `NAVY = '#203060'`. Lesson: when a shared branch carries someone else's independent commit, a squash-merge on their PR can silently orphan work stacked on top if the branch auto-deletes — worth pushing to a dedicated branch rather than a shared one when stacking unrelated work.

---

# Session 2026-08-02 — eq-solves-service: closed the last two lint follow-ups (next/image conversion), correctly re-scoped the other

## Built
- **`next.config.ts`**: added `images.remotePatterns` for the ehow Supabase storage host (`logos` bucket) — the only remote image source this app ever renders.
- **8 `<img>` → `next/image` swaps** across `components/ui/Sidebar.tsx` (brand logo x2), `components/ui/MediaPicker.tsx` (preview/dropdown/thumbnail x3), and `app/(app)/admin/media/MediaLibraryClient.tsx` (grid tile + edit-modal preview x2, incl. the SVG-specific branch). Fixed-pixel thumbnails use numeric `width`/`height`; variable-aspect containers use `fill`+`sizes` (added `position: relative` to their parents). All use `unoptimized` — this app allows SVG logo uploads, and next/image's built-in optimizer refuses SVG without a relaxed image CSP not worth opening on this app's tightly-scoped CSP. `tsc --noEmit` clean, `next build` succeeds, full-repo eslint re-scan: 8 → 0 findings, zero regressions. eq-service [PR #676](https://github.com/eq-solutions/eq-service/pull/676), merged (real gate `tsc + next build` green; `Integration tests (Supabase local)` red as the standing accepted pre-existing failure).

## Decided
- Royce chose "do item 2 now, defer item 1" after I corrected my own earlier framing of item 1 (the Supabase `no-explicit-any` insert/update typing gap) — it's already-known, already-documented team debt (`docs/30-day-plan.md` B6), not a fresh bug, and the available tooling here can't regenerate multi-schema types to fix it properly.
- "merge" — merged PR #676 to `main`, which auto-deploys `service.eq.solutions` via Netlify on push.

## Deferred
- Item 1 (Supabase insert/update typing gap, B6) — left as already-tracked team debt, not attempted. Needs the Supabase CLI's multi-schema type generation (not available via the MCP tool used this session, which only pulls `public`) plus per-site re-verification of ~17 call sites, since the underlying table (`service.assets`) is a view with its own write trigger, not a plain auto-updatable view.
- Royce to click through live: sidebar logo + admin Media Library page (grid + edit modal) for a tenant with a logo set, to confirm the swap renders correctly.

## Notes
Re-investigating item 1 before building it turned up a materially different picture than how it was first described (a fresh typing bug) — surfaced the correction to Royce via a question before proceeding rather than either silently attempting a risky multi-schema regen or silently deferring without explanation. Substrate (changelog + pending.md) for this PR was already pushed to `main` earlier in this session, ahead of this log entry.

---

# Session 2026-08-02 — eq-shell: Sentry sweep, fixed 3 real bugs + confirmed a 4th already fixed

## Built
- Triaged all 8 unresolved eq-shell Sentry issues via the Sentry MCP before touching any code — 3 were scheduled data-quality monitors (`check-never-invited-workers`, `check-identity-collisions`, `check-duplicate-shell-accounts`) correctly firing via `captureServerError` on real data, not bugs; 1 (mint-cards-otp 500, EQ-SHELL-13) was already fixed by merged PR #1150 before the reported event occurred.
- `src/App.tsx` — `ChunkErrorBoundary.componentDidCatch` now also calls `Sentry.captureException(error, { contexts: { react: { componentStack } } })` for genuine "stuck-crash" cases (EQ-SHELL-10). Previously only the error's `.message` string reached Sentry via `captureAuthEvent`/`captureMessage`, discarding the real stack trace — every occurrence of "Cannot read properties of null (reading 'useMemo')" on `/sks/intake` was undiagnosable beyond that one line. Ruled out a duplicate-React-copy cause first (checked `@eq/intake-demo`'s `package.json main` points at raw `.ts` source compiled into eq-shell's own Vite build, not a separately-bundled dist; lockfile resolves a single `react@19.2.8`). Root cause of the crash itself is still open — Sentry's Seer analysis returned HTTP 402 (no budget), so this fix only makes the next occurrence traceable.
- `netlify/functions/verify-shell-session.ts` — folded the `tenant_role_overrides` read into the existing `Promise.allSettled` batch (chained off the memberships read, which is all it depends on) instead of awaiting it as a trailing sequential round-trip after the batch resolved. That tail latency was still occasionally clearing the client's 15s deadline even after PR #888's earlier parallelization (EQ-SHELL-T, "regressed" substatus — first fix cut most but not all occurrences).
- `src/pages/CardsIframe.tsx` — added one retry (500ms backoff) on a network-level `fetch` failure (TypeError, e.g. Safari's generic "Load failed") in the `mint-cards-otp` REQUEST_SHELL_TOKEN handler (EQ-SHELL-12). Never retries a completed 4xx/5xx response, to avoid burning GoTrue's magic-link rate limit on a genuine server-side failure.
- Marked EQ-SHELL-13 resolved in Sentry with a comment citing the fixing PR and confirming the event predates it (verified `8d5dc9bf` is an ancestor of current `main` and its timestamp precedes the Sentry event).
- eq-shell [PR #1174](https://github.com/eq-solutions/eq-shell/pull/1174) — `pnpm run build`, `tsc` (both `tsconfig.netlify.json` and `tsconfig.app.json`), `eslint`, `check:perms`, `check:css` all clean (confirmed zero *new* lint issues via `git stash` diff against the pre-existing baseline). CI green, squash-merged to `main` (`b7f809b`) on Royce's explicit "merge" instruction — Netlify auto-deploys `core.eq.solutions` from `main`. Source branch deleted (had to finish the delete manually — `gh pr merge --delete-branch` partially failed because another local worktree on this machine had `main` checked out, which blocked `gh`'s local git sync; the GitHub-side merge itself succeeded).

## Decided
- Royce: "merge" — explicit instruction to land PR #1174 on `main`, understood as authorizing the resulting Netlify auto-deploy per repo governance.

## Deferred
- **EQ-SHELL-Y (ocr-licence 401)** — not code-fixable from eq-shell. Traced via Supabase MCP (`get_edge_function` on jvkn/`jvknxcmbtrfnxfrwfimn`) to the `ocr-licence` edge function's own manual trust check: a literal string equality between the caller's bearer token and the function's own `SUPABASE_SERVICE_ROLE_KEY` env var (gateway `verify_jwt` is off, so this is the function's only auth gate for server-to-server calls). Classic rotation-drift risk between eq-shell's Netlify copy of that key and jvkn's live one. Notably the *second* distinct root cause behind this same 401 symptom — 2026-07-23's version (`task_d94af51d`) was a stale CORS deploy, unrelated. Needs Royce to confirm/rotate the key; flagged in pending.md rather than guessed at.
- **EQ-SHELL-Z (1 identity collision on jvkn) and EQ-SHELL-11 (1 duplicate Shell account)** — working-as-designed data-quality alerts with real row IDs already in the Sentry event payload. Needs an operator decision on the underlying data, not a code change — flagged in pending.md.
- EQ-SHELL-X (44 never-invited workers past grace) — same underlying backlog as the existing 2026-07-21 pending.md item (was 45, now 44 — one resolved naturally); updated that item rather than duplicating it.

## Notes
Session-brief gate (`/brief eq-shell`, project CLAUDE.md Rule 0.6) fired mid-task via the `eq-guard` hook on the first `Edit` call — ran it retroactively (digest + worktree registry + git staleness check on the actual target repo, since the skill's own `$ARGUMENTS` path substitution broke on Bash's backslash handling and needed a manual path fix) before continuing, per the hook's hard block rather than the tool-permission-style soft ask.

---

# Session 2026-08-02 — eq-solves-service: two fixes prompted by a Royce screenshot (upload limit, report cover layout)

## Built
- **Media Library upload limit raised 2 MB → 5 MB** — Royce hit the old cap uploading a real site photo (a building exterior shot, ~2-3 MB typical). Updated `MEDIA_MAX_SIZE` in `admin/media/actions.ts`, its error string, and the upload form's helper text in `MediaLibraryClient.tsx` together. eq-service [PR #678](https://github.com/eq-solutions/eq-service/pull/678), merged.
- **PM Asset Report cover: brand band no longer disappears when a site photo is uploaded.** Royce compared two generated report covers side by side (with/without a site photo) and asked to keep the blue branded band and put the photo underneath it instead of the two being mutually exclusive. `buildCoverPage()` in `lib/reports/pm-asset-report.ts` previously branched on `input.sitePhoto`: present → photo + a caption strip with a duplicate customer-logo plate; absent → flat brand-colour band with the customer logo centred. Collapsed to: band always renders, photo (if present) renders below it with a simplified site name/address caption only — dropped the duplicate customer-plate since the band above already carries that identity. eq-service [PR #677](https://github.com/eq-solutions/eq-service/pull/677), merged.
- Both verified `tsc --noEmit` clean; PR #677 additionally re-ran `pm-asset-report.smoke.test.ts` + `pm-asset-report-with-tests.smoke.test.ts` (4/4 pass) since it touched report-generation logic, not just config/copy.

## Decided
- "merge" — Royce's instruction covered both fixes raised in the same exchange; merged both PR #677 and PR #678 after confirming the real CI gate (`tsc + next build`) went green on each (polled rather than assumed).
- Each fix went on its own branch/PR off `origin/main` rather than stacking on the other's branch — unrelated changes, and the repo's `main` checkout was already held by a sibling worktree (`eqsvc-loadtime-ux`), so new branches were created directly off `origin/main` instead of checking out `main` locally.

## Deferred
- Royce to spot-check a generated PM Asset Report live for a site that has a photo on file — confirm the band + photo layout reads correctly in practice, not just in the smoke tests.

## Notes
Branching off `origin/main` while a sibling worktree already had `main` checked out caused a `git stash` / branch-switch / `stash pop` sequence to transiently show the *other* branch's already-merged files (media upload actions.ts/MediaLibraryClient.tsx) as reverted to their pre-fix state in the diff — expected (those files' fix lives only on the sibling branch, not yet on `main` at that point), not a regression, but worth remembering the sequence can look alarming mid-flight. Also opened PR #678 for the upload-limit branch a step later than intended — had referenced its PR number in pending.md before actually creating it, then had to create it and fix the branch-vs-active-checkout mismatch (`gh pr create` failed once against the wrong currently-checked-out branch) before the number resolved for real.
