---
title: Session — 2026-06-08
scope: EQ Field — Sentry crash fixes (v3.5.99 + v3.5.100)
date: 2026-06-08
---

# Session — 2026-06-08

## Summary

Sentry check on eq-field → 3 unresolved issues found. Fixed all 3 and shipped
two releases. Both Sentry issues from the previous session (EQ-FIELD-3, EQ-FIELD-4)
were already live in v3.5.99 (previous session). A new issue appeared (EQ-FIELD-5)
and was fixed in v3.5.100. All 3 issues resolved in Sentry.

---

## v3.5.99 — Sentry EQ-FIELD-3 + EQ-FIELD-4 (shipped in previous session, resolved today)

**EQ-FIELD-3** — `ReferenceError: isLeave is not defined` (dashboard.js:77, 15 events)
- Root cause: `isLeave()` in lazy-loaded `roster.js` called from core-loaded `dashboard.js`
  before `roster.js` had loaded (landing on dashboard tab, or EQ_LAZY .catch fallback).
- Fix: Added `_isLeave` inline fallback using `LEAVE_TERMS` from eagerly-loaded `app-state.js`.
  Same pattern as the `updateTopStats` guard added in v3.5.84.

**EQ-FIELD-4** — `ReferenceError: auditLog is not defined` (timesheets.js:419, 1 event)
- Root cause: `auditLog()` called unguarded from 14+ modules; `audit.js` was lazy
  (only loaded on Audit tab). Any module calling `auditLog()` before the Audit tab
  was visited crashed.
- Fix: `audit.js` (233 lines) core-loaded in `index.html`; removed from `TAB_SCRIPTS`
  in `lazy-loader.js` to prevent duplicate `let` redeclare crash on second inject.

Both resolved in Sentry with reason notes. Last seen: 8h and 14h before v3.5.99 deployed
— no new occurrences since.

---

## v3.5.100 — Sentry EQ-FIELD-5 (shipped 2026-06-08)

**EQ-FIELD-5** — `ReferenceError: siteColor is not defined` (dashboard.js:157, 1 event)
- URL: `https://field.sks.eq.solutions/` (EQ Shell embedding Field, embedded=true)
- Root cause: `siteColor()`, `getSiteName()`, and `isAbsence()` all live in lazy-loaded
  `roster.js`, but are called from core-loaded `dashboard.js` before `roster.js` loads.
  Same class of bug as EQ-FIELD-3.
- Fix: Added inline fallbacks for all three functions in `dashboard.js` using only
  eagerly-loaded constants from `app-state.js`:
  - `_siteColor` — uses `SITE_COLOR_MAP`, `EDUCATION_TERMS`, `LEAVE_TERMS`, hash fallback
  - `_getSiteName` — uses `STATE.sites`
  - `_isAbsence` — uses `_isLeave` + `_isEducation`
- Pre-empts the next two crashes in the same render path (getSiteName:158/104/115/116,
  isAbsence:228/231).
- PR #230, merged, live. Smoke confirmed: `dashErr=null` with all 3 functions undefined
  (roster.js not loaded) — fallbacks fired cleanly.

**PR:** [#230](https://github.com/eq-solutions/eq-field/pull/230) — merged, live.
**Production verified:** `sw.js` shows `v3.5.100`.

---

## EQ-FIELD-3 + EQ-FIELD-4 + EQ-FIELD-5 — all resolved in Sentry

---

## State after this session

- **EQ Field live version:** v3.5.100
- **Sentry eq-field:** 0 unresolved issues
- **dashboard.js lazy-load race:** fully closed for all roster.js dependants
  (`isLeave` v3.5.99, `siteColor`/`getSiteName`/`isAbsence` v3.5.100,
  `auditLog` moved to core v3.5.99)

## Deferred / no-action

- **Deploy preview auth gate (zaap anon-revoked):** `demo-trades` tenant on previews
  still 401s at the name-list (zaap anon SELECT revoked, Phase 2). Pre-existing.
  Deferred per 2026-06-06 decision. Use `?tenant=demo` to bypass for smoke.
- **eq-context sks/ edits:** `sks/README.md` and `sks/products.md` edited locally in
  a prior session — still need a git commit + push (emit .bat or use GitHub web UI).

---

## Session Part 2 — SKS Go-Live Sprint (autonomous, ~3h)

**Instruction:** "full approvals, no mistakes, greatest sprint of all time."

### What was executed

**Field Cron Wiring — nspb (SKS live) — FULLY DONE ✅**
- Set Supabase fn secrets on `shift-events` fn: `CANONICAL_API_URL=https://core.eq.solutions`, `CANONICAL_API_KEY_FIELD`, `CANONICAL_TENANT_SLUG=sks`
- Seeded `public.app_config`: `shift_events_fn_url` + `shift_events_fn_token`
- Applied migration `shift_events_cron_2026_06_02` → pg_cron 20:30 UTC daily (06:30 AEST)
- **Dry-run verified live:** 50 staff rostered Monday 2026-06-08, response clean, dryRun:true
- First real emit: 20:30 UTC 2026-06-08 (tomorrow 06:30 AEST)

**Track 2 — SKS CMMS go-live operational items**
- ✅ Netlify secret hygiene (Service): SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, CRON_SECRET → marked secret via Netlify CLI
- ✅ EQ_PLATFORM_ADMIN_KEY generated (`eq_admin_101bfbad36bcd2bdd54c9a20cf48fa8441c99b8fc188d55f03d0e41f58b58b92`) and set on both eq-shell and eq-solves-service Netlify envs
- ⚡ PRE_VISIT_BRIEF_CRON_ENABLED flip and dress rehearsal remain as Royce-only items

**Track 3 — Roles Sprint verification audit (surprise: much already done)**
Audited against live code. Finding: Phases 0, 1, 2, 3 are already complete on `main`.

| Phase | Status | Where |
|---|---|---|
| 0 — Trunk | ✅ main IS trunk | @eq-solutions/roles v2.3.0 in package.json |
| 1 — Roles v2.3.0 | ✅ On main | `github:eq-solutions/eq-roles#v2.3.0` |
| 2 — Session extra_perms | ✅ Done | `verify-shell-session.ts` → `getUserSecurityGroupPerms()` → `extra_perms` on every verify |
| 3 — AdminSecurityGroups | ✅ Done | `AccessControlPage.tsx` + `security-groups.ts` full CRUD |
| 4 — E2E walk | ⚡ Royce | Assign user to group, verify perm in session — creates first user_security_groups row |
| 5 — Hardening | ✅/PR | See below |

**Phase 5 hardening**
- Applied `ccl_tenant` WITH CHECK directly to ehow (sks-canonical) — previously relied on USING-as-fallback
- **PR #231** `claude/phase5-hardening` on eq-shell:
  - `supabase/tenant-migrations/0044_contact_customer_links_with_check.sql` — clean, hygiene CI passes
  - `scripts/check-orphan-perms.mjs` — validates perm keys in security_group_perms against @eq-solutions/roles

### Remaining — Royce only (can't be delegated)

1. **Pre-visit brief dry-run** (2 min):
   `curl -s -X POST https://service.eq.solutions/api/cron/pre-visit-brief -H "Authorization: Bearer $CRON_SECRET" | jq`
   → Expect `{ok: true, mode: "dry_run", total: N}`

2. **Phase 4 E2E** (15 min): Admin → Access Control → create group → assign user → sign out/in → verify perm appears

3. **PR #231** review → merge → One Pipe dispatch (production environment approval)

4. **go-live day**: `PRE_VISIT_BRIEF_CRON_ENABLED=true` on eq-solves-service Netlify

5. **Dress rehearsal** (P0-P5 phone pass)

### Live changes made (no deploy needed — env + DB only)

| System | Change |
|---|---|
| nspb Supabase secrets | shift-events fn: CANONICAL_API_URL/KEY/SLUG |
| nspb app_config | shift_events_fn_url + shift_events_fn_token seeded |
| nspb schema_migrations | shift_events_cron_2026_06_02 applied |
| ehow app_data | contact_customer_links ccl_tenant WITH CHECK added |
| Netlify eq-solves-service | 3 vars → secret; EQ_PLATFORM_ADMIN_KEY set |
| Netlify eq-shell | EQ_PLATFORM_ADMIN_KEY set |
