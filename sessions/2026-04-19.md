---
title: Session — 2026-04-19
owner: Royce Milmlow
scope: Session log
status: complete
---

# Session — 19 April 2026

**Chat:** Cowork (EQ Solves Service IP hardening + GitHub PAT unblock)

## What was done

### IP Protection & Commercialisation amendment (EQ Solves Service)

Applied the 18 Apr 2026 amendment from the EQ-IP-Register across
`eq-solves-service` on a feature branch `feat/ip-hardening` (commit
`8a47994`, pushed to `origin`).

- `components/ui/EqFooter.tsx` — copyright footer mounted in all three
  route-group layouts `(app)`, `(auth)`, `(portal)` with long-form
  attribution + link to `/terms`.
- `components/ui/EqAttribution.tsx` — sticky bottom-right "Powered by EQ"
  logo anchor, mounted at root layout; uses the R2-hosted blue transparent
  logo.
- `app/layout.tsx` — Next Metadata populated with `applicationName`,
  `authors`, `publisher`, and `other.copyright`. Mounts `<EqAttribution />`.
- `app/(auth)/layout.tsx` — brand-panel copyright upgraded from
  "© {year} EQ Solutions" to long-form with ACN/ABN; form panel converted
  to flex column with footer.
- `app/loading.tsx` — root loading splash: centred EQ logo + "Loading EQ
  Solves Service…" label + bottom attribution line. Paints before any
  tenant skin resolves so product identity always lands first.
- `app/terms/page.tsx` — 9-section plain-English Terms of Use. Draft —
  flagged for Webb / SaaS-lawyer review before any external-customer use.
- `package.json` — `license: UNLICENSED`, `author`, `description`
  populated with canonical ownership strings.
- File headers added to top-level entry points only (`app/layout.tsx`,
  `proxy.ts`, `lib/supabase/middleware.ts`, `app/loading.tsx`,
  `supabase/migrations/0048_eq_meta.sql`). Full-repo backfill deferred
  to a dedicated session.
- `supabase/migrations/0048_eq_meta.sql` — singleton `public._meta`
  table, seeded with CDC Solutions Pty Ltd / EQ / EQ Solves Service
  ownership row. RLS enabled, SELECT-only policies for `authenticated`
  and `anon`, no write policies. Applied to `eq-solves-service-dev`
  project (`urjhmkhbgaxrofurpbgc`); Supabase advisors returned no new
  findings.
- `CHANGELOG.md` entry written for the IP hardening pass.

### GitHub access unblocked from Cowork

First-time success pushing directly from Cowork to GitHub. Previously
every push had to be done via Claude Code on the Beelink.

- Two fine-grained Personal Access Tokens generated, 30-day expiry
  (expire 2026-05-19):
  - **Milmlow PAT** — scoped to `eq-solves-service` + `eq-field-app`,
    Contents/Pull requests/Issues/Workflows RW, Actions/Commit
    statuses/Deployments/Metadata R.
  - **eq-solutions PAT** — scoped to `eq-context` + `eq-solves-assets`,
    same permission set.
- `feat/ip-hardening` verified on origin (commit `8a47994`).
- This session log + `state/pending.md` update pushed directly from
  Cowork using the eq-solutions PAT — proves the pipe end to end.

## Decisions

- PATs are the Cowork-layer credential for GitHub writes. `claude.ai`
  chat still requires the OAuth MCP connector; unblocking that is a
  separate thread (auto-login loop on consent screen).
- `feat/ip-hardening` stays as a branch — PR review required before
  merge. No auto-merge of IP-related changes.
- Full-repo file-header backfill is scoped as its own session —
  touches too many files to bundle into this pass.

## Outstanding

- **Webb TM brief** for software classes 9 + 42 (EQ-IP-Register P1 #3).
- **IP-clarity email to SKS Technologies** (P1 #1) — formalising the
  arm's-length commercial relationship.
- **Repo visibility audit** (P1 #2) — confirm `eq-solves-service` and
  `eq-solves-assets` are private; flip any that drifted.
- **Full-repo header backfill** (P2 #7 scope A) — dedicated session.
- **OAuth GitHub MCP connector** — consent-screen auto-login loop still
  blocks re-authorisation with org picker. PATs unblock Cowork meantime.
- **PAT rotation** — both tokens expire 2026-05-19. Calendar reminder
  set for 2026-05-16.
- `feat/ip-hardening` PR — open at
  `https://github.com/Milmlow/eq-solves-service/compare/main...feat/ip-hardening`,
  awaiting review.

## Files touched (this push)

- `sessions/2026-04-19.md` (this file)
- `state/pending.md` (updated — ticks, new items, `last_updated` bumped)
