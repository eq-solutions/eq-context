# Session 2026-07-12 — Shell→Field handoff: cookie mode retired, recurring Sentry issues cleared

## Built
- **eq-shell #755** (merged+deployed) — self-healing Field handoff: a cookie-mode `rejected` falls back to token mode (`#sh=` JWT via token-exchange); background pre-warm frames (`active=false`) no longer emit error-level Sentry. Admin-merged (Royce-authorized) past the main-velocity race.
- **eq-shell #757** (merged+deployed) — unblocked frozen production deploys. `netlify/functions/invite-users-batch.test.ts` sat at the functions root; Netlify derives the function name from the filename, `invite-users-batch.test` has an illegal dot → every deploy errored since #749, freezing prod ~2 releases behind main (GitHub CI stayed green throughout). Moved the test under `_shared/`.
- **eq-shell #768** (merged+deployed, **closes #756**) — retired cookie-mode Field handoff; Field now uses token mode for every tenant. Removed the cookie-src branch + #755's `forceToken` machinery; dropped `tenantUsesCookieAuth`/`buildFieldCookieSrc`. Session cookie unchanged.
- Filed **#758** (CI guard for bad function filenames). Filed then **closed #756** in favour of Path B.
- **Sentry triage**: resolved EQ-SHELL-Q (31-event `app_data.tenders` PGRST205 — already fixed+deployed by #720), EQ-SHELL-P (retired path), and EQ-SHELL-R/S (root cause gone via #768); annotated SERVICE-9. Created daily scheduled watch `eq-shell-field-handoff-fallback-watch`.

## Decided
- Fix the handoff with a **token-mode fallback** (not a doomed cookie re-mint) — Royce's pick (#755 Gap B).
- **#756 → Path B** (token mode primary for every tenant), NOT the literal `SameSite=None; Secure; Partitioned` — recon showed SameSite=None strips the `SameSite=Lax` CSRF shield off ~50 unguarded mutating cookie-authed endpoints (95 read the cookie; only 15 have an origin guard, report-only). Path B leaves the session cookie untouched.
- Explicit merges of #755, #757, #768; admin-merge authorized for #755.

## Deferred
- **#758** CI guard — filed, not built (fail on dotted/`*.test.ts` at `netlify/functions/` root).
- **Service SameSite** — SERVICE-9 `cookie_absent` is the Service twin; Service already token mode so residual canary, but confirm it can be muted/removed.
- Broader CSRF hardening (enforce `checkShellOrigin` / `ENFORCE_IFRAME_ORIGIN` across the ~50 unguarded mutating endpoints) is now decoupled from the cookie change and can be done on its own merit whenever.

## Notes
- **eq-shell prod-deploy health ≠ GitHub CI.** A single misplaced test file at the functions root froze production deploys for hours while GitHub CI (typecheck·test·lint) stayed green; only reading the Netlify deploy state (prod commit behind main) revealed it. #758 is the guard.
- **Cookie-in-cross-origin-iframe is the fragile seam.** `Domain=.eq.solutions; SameSite=Lax` is not reliably delivered to an embedded `field.eq.solutions` iframe under Safari ITP / Chrome 3p-cookie phase-out. Token mode (URL-fragment JWT) sidesteps it entirely — same conclusion ServiceIframe reached earlier, and eq-service #474 (CHIPS `SameSite=None; Partitioned`) is the parallel Service-side approach.
- **Shared-checkout race**: a concurrent session hijacked the `C:\Projects\eq-shell` working tree mid-#768 (moved HEAD to `claude/crm-add-site-contact-link` with uncommitted `crm-write.ts`). Merged #768 entirely GitHub-side (`gh pr update-branch` + squash on required-checks-green) — never touched the other session's work.
- Live-verified on core.eq.solutions (deploy `578bac0`): `/eq/field` (now token mode) and `/sks/field` both reach the signed-in state, no overlay/error.
