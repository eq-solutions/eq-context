# Session 2026-07-15 — Unblocked new-user logins for the SKS demo; made the invite-accept fix merge-ready

## Built
- **Unblocked Emma Curth's login** — her email invite kept failing because she was already a phone-only worker sharing one sign-in id. Merged the invite onto her real record (jvkn `shell_control.users` `d5eac318`: set email + PIN + SKS employee, added membership, marked invite accepted). Verified live via a real `shell-login` call. No duplicate account.
- **accept-invite phone-stub code fix** — the decider now skips `createUser` when the stub id already exists in `auth.users` (phone-only accounts have email NULL, so the email RPC misses them). Added `authUserExistsById()` (GoTrue `getUserById` probe) + optional `stubIdExists` fact + 2 tests. Commit `b20f0a3`, pushed to PR #862.
- **Made PR #862 merge-ready** — merged main in (was 9 behind), renumbered its two colliding migrations `14c/14d` → `2026_07_15a/b` (clashed with #853/#859 audit migrations). Build + 12/12 tests green. Commit `3cae64e`. **NOT merged/deployed.**
- **Email → dev@** (earlier in session, pre-compaction) — changed sender + reply-to to `dev@eq.solutions` (`resend.ts`) + 3 in-app "contact us" links; set `EMAIL_FROM` env on Netlify. **STAGED** on branch `claude/email-new-users-levers-baab69`, uncommitted, **NOT deployed**.

## Decided (Royce)
- Emma: merge onto her real account + temp PIN (not a new duplicate).
- Emma role: **employee** (skip forced manager 2FA for the demo).
- Email: sender → dev@, replies → dev@.
- #862: push the phone-stub fix; then **HOLD the merge — make it ready, no deploy** (the three new users don't need it).

## Deferred
- **#862 deploy** — apply the two jvkn helpers (`resolve_invite_auth_identity` + `list_orphan_auth_identities`) THEN merge #862 → auto-deploy. Royce-gated (auth/prod). Not applied yet (verified absent on jvkn).
- **Email → dev@** — commit/PR/deploy decision on the staged branch; `EMAIL_FROM` env needs a redeploy to take effect.
- Emma's temp PIN `414691` — she should change it once in.

## Notes
- **Root cause of "could not accept the invite" for a phone-only invitee:** phone-OTP / Cards onboarding creates BOTH an `auth.users` row AND a shell stub sharing one id (auth email NULL); accept-invite's stub-promotion path called `createUser` on that existing id → GoTrue "Database error creating new user" 500. Fix = probe by id, skip create. Distinct from the orphaned-identity 500 and the email-collision reuse (both handled by `58cbaca`'s email-keyed decider, which is blind to phone-only accounts).
- Earlier "link expired" for Emma was a red herring: **resending an invite regenerates the token in place**, so every older email link dies — only the newest works.
- **0 orphaned `auth.identities`** remain platform-wide (swept).
- **Rob Baird / Matt Jinks / Tim Watson triple-checked clean-slate** (0 on every collision vector: auth-by-email, identity-by-email, shell-by-email, phone stub, phone-in-auth) — accept + sign in on **current** prod without #862.
- #862 had migration-number collisions (`14c/14d`) with audit migrations that merged since — renumbered to `15a/b`. Neither migration is applied anywhere, so no ledger reconciliation.
- `pnpm run lint` (`eslint .`) is **broken in a fresh worktree** — a vendored eq-intake sub-app's `eslint.config.mjs` can't resolve `eslint-config-next`. Real gate is `tsc` + `vite build`.
- Isolated worktree left in place: `.claude/worktrees/accept-invite-phonestub` on `claude/invite-accept-auth-id-reuse` (for #862 follow-up).

---

# Session 2026-07-15 — Fixed Jack Trusler's timesheet total, invisible mobile roster header, hid Pipeline/Resources nav

## Built
- **Timesheet total double-count fix (sks-nsw-labour v3.10.97, PR #66, merged, live).** Root cause: a day's leave status comes from the roster (`schedule` table) but its hours come from the timesheet — a roster A/L day renders as a read-only 🌴 pill that hides any timesheet entry still saved underneath it, yet the total kept counting both the hidden hours and the paid-leave 8h for the same day. TAFE days already guarded this (v3.10.84); paid leave (A/L/SICK, v3.10.41) never did. New `_tsWorkedHrs(name, week, entry)` in `scripts/timesheets.js` skips roster-leave days, wired into the row total, apprentice total, in-place cell-save, and CSV export. Verified against the real functions with 6 cases (Jack's exact bug, post-clean, normal week, split-day, unpaid RDO, weekend) — all pass. Cleared the one stale DB row causing it (Jack's Friday `26184/8`, the only occurrence found across every week in the table).
- **Invisible mobile Weekly Roster header fix (same PR, v3.10.97).** `base.css` sets white header text for the desktop navy header; mobile's sticky-header CSS re-backgrounded the header cells to near-white for the scroll effect but never reset the inherited white text, so it vanished. Added `color: var(--navy)` to both sticky rules in `styles/mobile.css`. Verified via computed styles at 375px (fixed) and 1280px (desktop unchanged).
- **Hid Pipeline/Resources nav from all managers (v3.10.98, PR #67, merged, live).** Was only gated by `edit-only` (visible to any manager). Hard-hid the desktop sidebar section + mobile drawer buttons in `index.html`; pages/data/scripts untouched, no other link points at those pages.
- **Spawned a follow-up task** (`task_19ab53ac`) to audit EQ Field for the same invisible-header CSS pattern — Royce started it as an independent session mid-close, still running.

## Decided (Royce)
- Timesheet bug fix = code fix + clean the stale DB row, both, not one or the other.
- Pipeline/Resources hide = from all managers, not a role-restricted subset.
- Explicitly waived PR review to merge #66 and #67 (had to be said fresh per PR — a waiver on one PR doesn't carry to the next under the auto-mode classifier).
- Explicitly named the force-push of `claude/jack-trusler-table-math-223dde` to resolve a squash-merge orphaning conflict.
- The very first push attempt was blocked because the commit message + CHANGELOG named Jack Trusler alongside his specific leave/hours discrepancy — Royce said push it as-is ("someone's name in a changelog isn't the end of the world"); pushed only after that explicit call.

## Deferred
- **EQ Field audit for the same CSS pattern** — task_19ab53ac running independently, not reported back by session close.
- **`eq-solutions/sks-nsw-labour` repo visibility** — the auto-mode classifier's block confirmed this repo is PUBLIC. Not previously documented as a known fact in this substrate; worth a deliberate decision (leave public vs flip private) given it now holds a named worker's leave/hours detail in commit history.

## Notes
- Squash-merge trap recurred (documented before, hit again): after PR #66 squash-merged, the source branch's own follow-up commit looked like it was redoing the whole diff from the pre-merge base, because the squash commit has no parent link back to the branch. Fix is `git rebase --onto origin/main <last-commit-before-squash>`, not manual conflict resolution — clean, zero-conflict rebase once pointed at the right base.
- The auto-mode classifier enforces per-action, per-instance consent: "merge without review" on PR #66 did not authorize merging PR #67 without review, and did not authorize the later force-push — each needed the specific action named again, even within the same conversation and same immediate task.

---

# Session 2026-07-15 — Sentry inbox triage: three clusters fixed, PRs merged + deploying

## Built
- **eq-shell #858 MERGED (deploying core.eq.solutions)** — fixed the auth-stall Sentry pair EQ-SHELL-T/V. Real bug: `SessionProvider.refresh()` used `fetchWithTimeout`, which clears its abort timer the instant headers arrive, leaving `await res.json()` unbounded — a stalled body wedged `loading` true forever with no recovery. Replaced with one AbortController deadline covering headers + body. Also re-arm the 20s spinner watchdog on tab foreground and gate both markers on `!document.hidden` (a backgrounded mobile tab flushing its timers on resume was the false-positive source). One file `src/App.tsx`, no auth-contract change. Direct follow-up to #833.
- **eq-cards #155 MERGED (deploying)** — corrected `web/index.html` renderer config: the comment claimed `renderer:'auto'` gives an HTML renderer on mobile (removed in Flutter 3.29; app is on 3.41). Pinned `renderer:'canvaskit'` explicitly — functional no-op today (not cross-origin isolated → `auto` already resolves to canvaskit). Does NOT fix the engine crash (none can from app code).
- **Sentry resolutions** — EQ-SOLVES-SERVICE-9 (`cookie_absent`, dead code post-CHIPS), EQ-CARDS-12 + EQ-CARDS-13 (one transient Flutter engine crash) all resolved with evidence comments. Auto-regress on recurrence.
- **Merges driven this session** — merged #155 (clean) and #858 (after updating from main to pick up the drift-gate fix that #849 already landed).

## Decided
- **Cards engine crash = resolve-and-monitor** (Royce). EQ-CARDS-12/13 are ONE transient CanvasKit/OffscreenCanvas WebGL-context-loss on mobile Safari — not app code, 0 users, 1 incident. No app-side fix; escalation path is a Flutter engine bump if it recurs with user impact.
- **Merge #858 + #155** (Royce: "merge and fix this") — authorised the auth + config deploys; the "fix" was the red drift gate on #858, resolved by updating the branch off main.

## Deferred
- **eq-shell #863 open (background task's PR)** — the login/OTP/provision twin of #858 (same body-read-not-under-timeout gap in `shell-login` / `shell-login-phone-otp` / `shell-handoff-provision`). Auth code, needs review + merge.
- **Netlify publish confirmation** — no Netlify token here; #858 deploy-preview built clean so prod mirrors it, but the actual core.eq.solutions + cards.eq.solutions publishes weren't confirmed from this session.

## Notes
- **The Cards issue was misdiagnosed by the initial recon agents** — the obfuscated frame `Dz.b07(main.dart)` reads as app code, and two agents proposed a three-screen `DropdownButtonFormField` refactor. The symbolicated stack (SDK source maps) proves it's the Flutter engine (`canvaskit/surface.dart:81` → `throwUnnamedLateFieldNI`, via `CanvasProvider.acquireCanvas` + `OffscreenCanvas`). Ground truth over the recon lead — did NOT make the speculative edit.
- **Calibration drift-gate lineage correction (matches pending #110):** the drift registration landed via **PR #849**, not my earlier **#852** (a race-duplicate, correctly closed unmerged). The DDL lives in **eq-solves-service migrations 0184+0185**, NOT "0181" as the original brief/#852 body stated. This session #858's drift gate went green purely because main already carried #849.
- **`cookie_absent` is dead code** — removed from the auth-handoff canary during the 07-07/08 CHIPS hardening; current reason set is secret_mismatch|expired|malformed|no_email|slug_unresolved|cookie_unusable, and the replacement only fires when a cookie IS present but fails verify. Can't regress.
- Cards is **not cross-origin isolated** (COOP set, COEP absent, and it's iframed cross-origin under core.eq.solutions) → skwasm unavailable → CanvasKit + OffscreenCanvas on every device, including the crashing mobile-Safari path.

---

# Session 2026-07-15 — EQ Field header-contrast audit (task_19ab53ac): one live bug found, fixed, merged, deployed

## Built
- **Closed out `task_19ab53ac`** (spawned by the earlier SKS Weekly Roster session today) — audited eq-field for the same invisible-header CSS pattern (background overridden on a `th`/ancestor without resetting the inherited text colour). Checked every `th`/`thead` rule in all of `styles/*.css` and every inline-styled `<th>` across `scripts/*.js`, covering Contacts, Sites, Supervision, Job Numbers, Safety Report, Timesheets, Roster, Leave, Dashboard, Pipeline, Audits, and Calibration.
- **Found one real match: Forecast page's sticky "Project" column header** (`scripts/forecast.js:232`) — set `background:var(--surface)` (white) inline with no `color`, inheriting `color:white` from base.css's global `thead tr` rule. Not mobile-scoped like the SKS bug — a plain inline style, so broken at every viewport width. Verified the exact cascade in an isolated local harness before and after: `color: rgb(255,255,255)` on `background: rgb(255,255,255)` → `color: rgb(41,134,180)` (navy) on the same background.
- **Fixed, PR'd, merged, deployed.** One-line fix (`color:var(--navy)` added to the inline style) on branch `claude/mobile-th-contrast-audit`, off `origin/main`. eq-field **PR #492**, CI green, squash-merged into `main` at `8142119`. Netlify build `6a57215` confirmed `state:ready`, `context:production`, live on **field.eq.solutions**.
- Every other page audited already pairs `background` + `color` together on the same element — none reproduce the bug.

## Decided (Royce)
- "Push and deploy" — explicit go-ahead to push the fix branch and deploy it.
- "Merge" — explicit second confirmation to squash-merge PR #492 into `main` with CI-only (no human) review, after the auto-mode classifier blocked the self-authored no-review merge and asked for it by name.

## Deferred
- **Reverse-direction contrast issue on two other pages, not fixed.** `scripts/leave.js:1188` (Leave mini-calendar header) and `scripts/sks-pipeline-import.js:199` (Pipeline import preview) set a dark `color` (ink-3/ink-4, meant for light backgrounds) on a `th` that still inherits the navy background from `thead tr` — low-contrast dark-on-navy text, not invisible. Different bug shape from the requested pattern; logged in `eq/pending.md` for a later pass.

## Notes
- The auto-mode classifier requires the specific action ("merge without review") to be named and confirmed per PR/action, even mid-conversation — a generic "push and deploy" wasn't enough consent for a self-authored no-review merge to `main`; had to ask explicitly and get "merge" back before it went through. Consistent with the pattern from the earlier SKS session today (line 52 above).
- Verification used an isolated local static-file harness (copied `tokens.css` + `base.css` into scratch, served over `python -m http.server`, checked `getComputedStyle`) rather than the live app, since the Forecast page sits behind Supabase auth this session had no credentials for. Screenshot tool was unresponsive on that harness; computed-style JS check substituted and gave a definitive answer.
