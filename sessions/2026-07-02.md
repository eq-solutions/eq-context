# Session 2026-07-02 — Token lint ratchet merged; staff licence resync endpoint shipped

## Built
- PR #585 merged — StaffPage Phase E: MatrixView/SplitPanel extracted to `src/pages/staff/` sub-modules
- PR #586 merged — Semantics pass: raw semantic hex → CSS token vars across codebase; LicGroup colour props migrated
- PR #587 merged — Security: `worker_dedup_archive_20260630` on jvkn — RLS enabled, policies locked to service_role
- PR #588 merged — Token lint ratchet: `no-restricted-syntax` warn→error; 24 legacy files get file-level eslint-disable markers (3 had incorrect `eslint-disable-next-line` that didn't cover multi-line objects — replaced); `netlify/functions/staff-resync-licences.ts` (new); SplitPanel "Re-sync from Cards" button
- Jack Cluff 6 licences backfilled — direct SQL to ehow `app_data.licences`: white_card, working_at_heights, driver_licence, ewp, electrical_licence, open_cabling

## Decided
- Lint ratchet implementation: file-level disable markers on 24 legacy files (not a config allowlist) — functionally equivalent, simpler to enforce
- Resync endpoint guards: looks up `cards_worker_id` server-side, resolves `workers.user_id`, upserts with `ON CONFLICT cards_credential_id DO NOTHING`
- Cicero "Re-review" badge: correct as-is (June 29 was programmatic bulk approval, not a real licence sighting); no code fix

## Deferred
- Armada/Lighthouse on eq-shell: Armada IS already set up (commit `f5ede5f` on main). `/lighthouse` fails in worktree sessions — run from `C:\Projects\eq-shell` main checkout. Royce to share Calum's repo URL if further wiring needed.
- Cicero: Royce to click "Re-review licences" in Staff panel manually

## Notes
- Design-system chain (A–F) now fully done: B (Warm Sand approved), C (hex→tokens complete), D (staffLib tests), E (Phase E extract), F (ratchet). Only A (token source unification) remains open.
- `eslint-disable-next-line` pattern fails silently on multi-line objects/arrays — it only suppresses the single next line. File-level block disables are the correct pattern for any multi-line declaration.
- Jack Cluff root cause: `cards-approve-staff.ts` syncs licences at approval time only (`ON CONFLICT … DO NOTHING`). Licences uploaded post-approval are never auto-synced. `staff-resync-licences.ts` is the durable fix for this class.
- Armada/Lighthouse: `/lighthouse` is an ARMADA skill resolved from `.claude/skills/`. Worktree sessions don't inherit the main checkout's `.claude/` — skills are invisible. Must run from `C:\Projects\eq-shell` directly.

---

# Session 2026-07-02 (part 2) — eq-service lighthouse: budget bump, 2nd recon pass, 9 issues built + merged

## Built
- Lighthouse budget bumped (eq-service + eq-shell): `maxIssuesPerRun` 3→6, `maxRuntimeSec` 300→600, `maxFindings` 20→30 — PR #397 + PR #589 merged
- eq-shell lighthouse scheduled daily 8am (`eq-shell-lighthouse` task) — explicitly `cd`s to the main checkout before invoking `/lighthouse` to route around the worktree skill-resolution gap noted above
- In-flight batch #1 finished: PR #394 (dead `/api/shell-sso` path), #395 (Zod validation on report settings action), #396 (7 idempotency unit tests)
- 3 pre-existing open PRs merged: #363 (docs project-ID fix), #368 (netlify/functions dev-dep bump), #370 (React patch — fixes a Server Actions `FormData` regression)
- 2nd lighthouse pass on eq-service found + chartered 6 issues (#398–403), all Sentry-coverage or test-coverage gaps
- All 6 armed, built, verified (tsc clean, #409's 7 tests passing), merged: PRs #404–409
- 9 worktrees cleaned up post-merge

## Decided
- Lighthouse budget of 6 issues / 600s runtime is now the standing config on both eq-service and eq-shell
- Merge-all-immediately is the preferred pattern for lighthouse-sourced fixes once validation is clean — no extra review gate for small, scoped, mechanical changes

## Deferred
- Verify `eq-shell-lighthouse`'s first scheduled fire actually runs end-to-end (created this session, not yet observed)

## Notes
- Two of the 6 charter-drafted issue specs were wrong in ways only visible from the source, corrected during build rather than built blind:
  - **#406** — issue guessed Sentry `pipeline` tags as `maintenance`/`pm-asset`; actual functions are `generateAndStoreReport` (PM check report) and `generateAndStoreWorkOrderDetailsReport` — tags corrected to `pm-check-report`/`work-order-details`
  - **#408** — `check-completion.ts` already had `Sentry.captureException` wired from an earlier PR sitting right next to the flagged `console.error`; the real fix was removing the redundant duplicate log line, not adding a second Sentry call
  - **#409** — issue spec assumed `isMutationProcessed` used a count query; it actually uses `.select('id').maybeSingle()` — tests written against the real implementation
- Pattern worth remembering: lighthouse/charter issue specs are hypotheses formed from a grep-level survey, not verified against full source. shipwright must re-verify against the actual file before implementing, not implement blind from the issue body.
- Windows bash backtick-in-heredoc gotcha recurred: `node -e "..."` with backtick template literals inside gets partially eaten by the outer shell even when escaped — 3 of the automated replacements silently failed and needed follow-up Edit-tool fixes to actually land. Prefer the Edit tool over inline node scripts for replacements containing template literals.

---

# Session 2026-07-02 (part 3) — Equipment cert-delete recovery + assign-to-staff dropdown

## Built
- Diagnosed missing equipment certs on `/sks/equipment`: 13 `app_data.assets` rows (plant_equipment, ehow/SKS tenant) uploaded 2026-06-30/07-01 via the Shell cert-import flow, then hard-deleted by an unattributed direct SQL statement at 2026-07-01 09:37:18 UTC (`actor_id: null`, `source: 'system'` in `app_data.audit_log` — not the app, not a tracked migration in eq-shell or eq-solves-service)
- Restored all 13 rows from `app_data.audit_log.old_record` — same asset_ids, sites, calibration dates, `cert_url`s; underlying PDFs were untouched in the `asset-certs` bucket. Verified live via `tenant-audit` endpoint.
- Spawned background investigation (chip `task_9b113311`, session `local_9c384ff7`) to trace the delete's source — still running at session close
- PR #592 merged — equipment table "Assigned to" column is now an inline dropdown for `equipment.edit` users (reassign without opening the drawer, optimistic update via existing `asset-calibration` endpoint); drawer button relabelled "Reassign custodian" → "Assign to staff member"

## Decided
- Restore-from-audit-log first, investigate the delete's source second — Royce prioritized getting the data back over root-causing immediately

## Deferred
- Find source of the unattributed `app_data.assets` delete on ehow — investigation session running independently, not yet reported back at session close (needs Royce's call once it lands — may recommend a guard against unattributed service-role DELETEs)

## Notes
- **Process error, caught and fixed within the session**: edited `src/modules/equipment/index.tsx` in the wrong checkout — `C:\Projects\eq-shell` (main folder, on a stray branch `claude/token-lint-ratchet` with another concurrent session's uncommitted work) instead of the assigned worktree at `.claude\worktrees\objective-bell-bc744d`. Caught when `git status` showed a clean worktree but a dirty main checkout. Fixed by diffing the stray checkout, applying the patch to the correct worktree, and reverting the stray checkout — without touching the other session's unrelated uncommitted files (later confirmed as PR #591, merged separately). Lesson: always confirm `pwd`/`git branch --show-current` matches the assigned worktree before editing when multiple Claude sessions may share `C:\Projects\eq-shell` as a working directory.
- `gh pr merge --squash` failed once on a stale branch (GitHub's "Update branch" had already fast-forwarded the remote branch past local HEAD via an equivalent merge commit) — resolved by fetching, confirming the tree diff was empty, and `git reset --hard` to the remote rather than force-pushing a duplicate merge.
- Root cause of the original data loss remains open — the restore is not a fix, just a recovery. The audit trail can currently be fooled by any hand-run service-role SQL (mine included: the restore itself is stamped `actor_id: null, source: 'system'`, indistinguishable from the original delete).
