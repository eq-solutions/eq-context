# Session 2026-08-05-c — hooks/adversarial_test.sh: deleted, then restored on Royce's direct call; live F9 Cowork-gap found

## Built (this session's own work)
- Deleted `hooks/adversarial_test.sh` (legacy bash test subset, F1/F2/F3/F6 only, no F7/F9) rather than syncing it — zero functional dependents found via repo-wide grep (no CI workflow, Makefile, or pre-commit config referenced it).
- `hooks/README.md`'s Testing section updated to drop the dead pointer to the deleted file.
- `eq/pending.md`'s 2026-08-04 deferred item ("bring it in sync or formally deprecate it") checked off with the reasoning.
- Verified: `python hooks/adversarial_test.py` — 65/65 passed, unaffected by the deletion.
- Session-close archive rule applied: the now-fully-closed "shared-checkout git races / F9" section moved wholesale from `eq/pending.md` to `eq/pending-archive.md` (rotated 2026-08-05).
- Spawned background task `task_1f1ce5cf` to investigate why F9's guard didn't catch a concurrent Cowork-labeled session's bare `git commit`/`git rebase` running directly in this shared checkout.

## Superseded — read before trusting the "Built" section above
This session's own delete decision **did not stand**. `task_1f1ce5cf` reported back mid-close as commit `2a5288a` — Royce gave it a direct instruction to **restore `adversarial_test.sh` and bring it in sync instead of leaving it deleted**, overriding this session's (b). That session:
- Restored `hooks/adversarial_test.sh` from git history and ported F7 + F9 bash-native fixtures into it, so it now has real parity with `adversarial_test.py`.
- Forced `EQ_FORCE_GUARD=1` for the sandbox-scoped checks in the bash suite, since they were silently no-op'ing 9 assertions on native Windows.
- **Caught a real, separate bug while doing it**: the F9 "allowed" test cases added the day before (pathspec commit, `--amend`, `rebase --continue`/`--abort`) only passed because `in_sandbox()` happened to be `False` on Windows — on Linux CI, where it defaults `True`, the pre-existing sandbox git-blanket-block would have overridden F9's own correct verdict and these would have read as false regressions on the next CI run. Fixed by pinning `EQ_FORCE_GUARD=0` on every F9 case in both suites.
- Reconciled `hooks/README.md` and `eq/pending.md` to describe "restored + synced," not "retired" — this session's own edits to those two files are now stale/overwritten. My own delete reasoning wasn't wrong given what I knew at the time (it's described in `2a5288a`'s own message as "also legitimate, also documented") — it was just superseded by Royce's actual preference once he weighed in directly.
- Both suites verified green from a clean clone: `adversarial_test.py` 65/65, `adversarial_test.sh` 32/32.

**Net effect on `eq/pending-archive.md`**: the archived "shared-checkout git races / F9" section (rotated by this session, see Built above) still accurately describes the *original* 2026-08-04 F9 fix — that part is untouched and still correct. It's this session's *own follow-up* (the adversarial_test.sh delete) that got overridden downstream, after archiving had already happened.

## Decided
- Royce: restore + sync `adversarial_test.sh`, not delete it — final call, made directly to the investigating session, not to this one.

## Deferred
- **Root F9-Cowork-gap is still open.** `2a5288a` confirmed the mechanism (a commit authored "via Cowork" landed outside Claude Code's own tool-call hook entirely, so `pre_tool_use.py` never saw it — not a logic bug in F9, a visibility gap) and documented it as a known scope boundary in `pre_tool_use.py`'s docstring and `system/failures.md`'s F9 entry, explicitly NOT counted as a recurrence since the guard was never positioned to see it. A further follow-up was spawned separately: `task_94836df0` (distinct from this session's own `task_1f1ce5cf`, which is now resolved). Whoever picks this up next should start from `2a5288a`'s message and `system/failures.md`'s F9 entry, not from this log.

## Notes
- **This is a direct, concrete example of why the guard-gap investigation matters**: while this session was mid-close, it collided at least 3 more times with concurrent sessions sharing this exact checkout (`C:\Projects\eq-context`) — including, at one point, a `git checkout` this session ran landing as a no-op because another session had already silently moved the shared `HEAD` back to `main` from a branch (`temp-close-cherry`) that a different concurrent session was using for its own reconciliation work. One of this session's own commits briefly landed on that branch instead of `main` as a result — recovered cleanly (nothing lost, the commit object stayed reachable throughout), but only by explicitly checking `git branch --show-current` after the fact rather than assuming. Multiple sessions' git commands sharing one physical `.git/HEAD` in this checkout is a real, demonstrated hazard, not a theoretical one.
- Given that, the rest of this session's own reconciliation (this file included) was finished from an isolated clone in the scratchpad rather than by continuing to commit/branch-switch directly in the shared checkout — same pattern this repo's own F9 fix already recommends, applied here after directly hitting the failure mode it exists to prevent.
- `git pull --ff-only` failed repeatedly this session on genuine divergence, not staleness — automated and concurrent commits landed on origin faster than this session's own small, sequential commits could land cleanly.
