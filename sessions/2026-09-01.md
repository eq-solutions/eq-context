# Session 2026-09-01 — eq-solves-service: migration-governance review (task_38071324) closed — DB-first PR split adopted, --reconcile tooling shipped (PR #820)

## Built
- Picked up `task_38071324`, spun off by the prior day's P0/P1 sprint close (see `sessions/2026-08-31.md`, the "second half of the Assets-page tenant-scope leak" entry) after several sessions bypassed the governed migration pipeline via Supabase MCP `apply_migration` on 2026-08-31, jamming a real dispatch mid-run. Read `scripts/migrate-service.mjs` and `.github/workflows/apply-service-migrations.yml` in full before proposing anything, per the incoming brief's own instruction.
- Verified the incident report against live state rather than trusting it as given: confirmed the pipeline was NOT still jammed (a 09:39 re-dispatch had already succeeded after the earlier session's manual ledger reconcile); confirmed `get_assets_for_grouping`/`get_distinct_asset_types` are live and correctly tenant-scoped (one overload each, matching signatures); confirmed zero new ERROR-level Supabase advisor findings from the day's migrations.
- Corrected one detail in the incident report along the way: migrations `0231`-`0233` didn't actually need hand reconciliation — their GRANT/REVOKE DDL is idempotent, so the pipeline's own 02:21 dispatch re-ran and ledgered them cleanly before dying on `0234`. Only `0234`-`0237` needed the manual backfill the report described.
- Audited the full `0169`-`0238` ledger range against the repo's migration files (the incident's own open question: "is this contained to one event?"). Found one more undocumented bypass-then-reconcile instance — `0223`/`0224` from 2026-08-20 — not previously logged anywhere. No other gaps. Running total: 5 bypass-then-reconcile incidents through 2026-08-31, not one.
- Presented 3 governance decisions to Royce via `AskUserQuestion` rather than picking unilaterally, given the incoming brief explicitly framed these as his call: (1) how to close the pre-merge sequencing gap driving the MCP bypasses — DB-first PR split / scope the branch policy to the PR branch / open it to any branch / leave as-is; (2) whether to formalize the by-hand ledger reconcile into a script mode; (3) whether to add a steering note to the repo's own docs. Royce chose the recommended option on all three.
- Before building any of it: discovered a concurrent session had already built and pushed the exact same change to the same branch (`chore/migration-governance-reconcile-tooling`) as eq-service [PR #820](https://github.com/eq-solutions/eq-service/pull/820) — matching design, already tested (checksum verified against the live ledger, credless print-only fallback confirmed, missing-file/missing-reason error cases, multi-file invocation). Verified the diff and its test plan rather than duplicating the work.
- Merged PR #820 on Royce's "merge #820 once CI is green." `mergeStateStatus` was `UNSTABLE` — confirmed the one failing check (`Integration tests (Supabase local)`) is this repo's own documented pre-existing non-blocking failure by checking it also failed on the 3 immediately-prior merged PRs (#816/#817/#818); the real gates (`tsc + next build`, `Typecheck + audit`, migration `Plan`) were all green. Read the merged code afterward to confirm it matched the sound design before treating it as done, rather than trusting the merge alone.
- Synced local `eq-solves-service` checkout to the merged `main` (clean fast-forward, no local work to lose).

## Decided
- Royce, via `AskUserQuestion`: adopt a "DB-first PR split" discipline (no infra change — split a contract-changing migration into its own PR, merge + dispatch it before the dependent app-code PR) over loosening the `production` environment's branch policy.
- Royce, via `AskUserQuestion`: build the `--reconcile <file...> --reason <text>` mode on `migrate-service.mjs`.
- Royce, via `AskUserQuestion`: add a docs-only steering note to `AGENTS.md` over technically restricting MCP write access.
- Royce: "merge #820 once CI is green."

## Deferred
- None new from this session — `task_38071324` is fully closed. The unrelated older deferred items still open in `eq/pending/eq-solves-service.md`'s top section (P2 backlog, shell-sso retirement, UX click-through, 2 advisor findings to relay, a CLAUDE.md doc-typo) all predate this session and weren't touched.

## Notes
- The durable rule now lives in `AGENTS.md` § "Migrations to live ehow — governed pipeline only" — readable by any tool or session that opens the repo directly, not only ones that happen to load the right Claude memory file. Two of this session's own Claude-memory files (`project_service_migration_ledger_governed_apply`, `project_asset_grouping_tenant_scope_fix`) were also corrected/closed out in the same pass — the latter had gone stale mid-incident (written before PRs #816/#818 actually merged) and was still claiming the leak was open when it had already shipped.
- `eq-context`'s shared checkout was 1-18 commits behind `origin/main` at various points this session (all nightly-automation churn on `digest.md`/`suite-state.md`, confirmed via `git log --stat` before writing). `git pull` in the shared checkout was correctly hard-blocked by the F9 guard hook when attempted directly; confirmed via `fetch` first that the commits behind didn't touch any file this close needed, then wrote and committed against current HEAD per the skill's own rebase-not-pull fallback for the shared checkout.

---

# Session 2026-09-01 — the other side of task_38071324's close (this session spawned it)

*This session spawned `task_38071324` at the end of the 2026-08-31 PR #818 close and is the one Royce was talking to when he asked "what should we do now" after that spawned session reported back. Not duplicating its own close write-up above — it already covers the actual governance work in full.*

## Built
- Nothing new. Read PR #820's actual diff and merged state directly (`gh pr view`) before telling Royce it was the right call, rather than relaying the peer session's own summary uncritically.

## Decided
- None this session — the substantive governance decisions (DB-first PR split over a branch-policy change, `--reconcile` tooling, the `AGENTS.md` steering note) were Royce's, made directly with the `task_38071324` session.

## Deferred
- None.

## Notes
- The one earlier pending.md bullet this session had written for the pipeline jam (in `eq-solves-service.md`'s "4-axis service review" section, added at the 2026-08-31 close) is gone from the live file as of this session — not archived anywhere either, confirmed by grepping `pending-archive.md`. Most likely dropped in one of that night's several merge races on the same file, not a deliberate edit. No real loss: the item it described is now actually resolved (PR #820), so nothing open needed restoring — recorded here rather than silently re-added as a stale open item.

---

# Session 2026-09-01 — eq-cards: 9 more white-on-sky buttons swept to skyAA, folded into a larger button-contrast arc that shipped live

## Built
- Verified all 9 candidate sites from the incoming brief against live code, not the recon summary alone: read every file in full, confirmed exact button styling, and — for sites with no explicit `foregroundColor` override — traced the actual resolved colour through `eq_theme.dart`'s `ColorScheme(onPrimary: EqColours.white)` with no `filledButtonTheme` override, confirming Flutter's M3 default still renders white text there too. All 9 confirmed genuine ~2.70:1 failures at every text size involved (none qualify for the 3:1 large-text exemption).
- Applied `EqColours.sky` → `EqColours.skyAA` across 8 files / 9 call sites (`consent_gate.dart`, `otp_screen.dart` ×2, `email_entry_screen.dart`, `not_provisioned_screen.dart`, `provision_tenant_screen.dart`, `admin_members_screen.dart`, `licence_crop_screen.dart`, `sensitive_consent_sheet.dart`). Left untouched, on purpose, everything the brief flagged as a different WCAG question: 3 `EqSpinner` + 1 `RefreshIndicator` on sky, 2 decorative icons, a `Cancel` button on a black overlay (not white), and crop-tool paint brackets drawn over a live camera image. Confirmed via a full repo grep after editing — zero strays, every excluded item still on `sky`.
- Independently computed the WCAG relative-luminance contrast for `sky` (2.70:1) and `skyAA` (4.60:1) against white rather than trusting the existing code comments — both checked out.
- Published a before/after swatch-comparison artifact (real button labels, real font sizes/weights, computed ratios, grouped by flow) as a visual check ahead of committing, matching the precedent set for the original `EqButton` fix.
- Ran eq-cards' `/brief` Session Gate mid-session — its flag-file hook fired on the first `Edit` call in the fresh worktree, exactly as a standing memory predicted — before making any edits.
- Committed (`0056be7`) and pushed to the branch backing the already-open PR #336, fast-forward.

## Decided
- No live back-and-forth with Royce this session — the incoming brief was already a fully-specified task (exact files/lines/pattern/exclusions), so proceeded directly rather than re-confirming a brief that duplicated what Royce had already given. Own judgment call, not a Royce confirmation.
- Found PR #336 already `mergeable: CONFLICTING` against a same-day `main` commit (#334) that had independently moved the *same* `connect_to_company_screen.dart` Apply button to `deep` instead of `skyAA` — computed that `deep`-on-white is itself only ~4.07:1 (still short of the 4.5:1 bar), reinforcing `skyAA` as the correct resolution, but deliberately did **not** rebase/force-push to resolve it: that rewrites an open PR's history, which crosses into "check with Royce first," not a same-commit-reversible edit. Surfaced it as an open question instead of acting on it.

## Deferred
- Nothing new — the one thing this session meant to hand back open (the PR #336/#334 conflict above) was already resolved by the time this `/close` ran: `eq/pending/eq-cards.md` shows it settled in favour of `skyAA` (matching this session's own reasoning), PR #336 merged and deployed live, plus a second broader sweep (PR #339, 14 more instances), test coverage (PR #338), a fill-colour fix (PR #335) and a golden-drift fix (PR #337) — all documented there in full. Not duplicating that write-up here.

## Notes
- `eq-cards.md`'s own pending/changelog entries for this arc are more complete than this session's visible turns account for on their own (they cite this session's own commit message as evidence for a separate Flutter-SDK-PATH finding, and describe the conflict resolution plus PRs #339/#338/#335/#337 in detail) — consistent with a background continuation of this same session lineage finishing the arc after this transcript's last visible action. Treated the freshly-cloned substrate as authoritative for that outcome rather than re-deriving or re-narrating it.
- No Flutter SDK reachable in this environment this session (`PATH` pointed at `C:\Projects\flutter\bin`, which doesn't exist on disk) — couldn't run `flutter analyze`/`flutter test` locally, said so plainly in the commit message instead of claiming untested success. `eq-cards.md`'s "product-polish audit" section already tracks this as a recurring, multi-session-confirmed gap (real SDK is at `C:\src\flutter`) with a fix that needs Royce (a PATH edit outside what Claude can do itself) — not re-added here.
- A same-day clock discrepancy: PowerShell's `Get-Date` read 2026-08-31 mid-session while Bash's `date` and this file's own git history both said 2026-09-01. Resolved by checking which date concurrent sessions were actually committing under (2026-09-01, confirmed via `git log`) rather than trusting either shell in isolation — worth knowing if a future session hits the same split.
- `git pull` on the shared `eq-context` checkout was correctly hard-blocked by the F9 guard hook on the first attempt; recovered via a fresh isolated `git clone` per the hook's own suggested fix — the same pattern another session closing today independently also hit and used.

---

# Session 2026-09-01 — eq-shell: Records direct-nav permission guard shipped; tenant_role_overrides deny-arc audited end to end

## Built
- **eq-shell [PR #1688](https://github.com/eq-solutions/eq-shell/pull/1688)** — deferred item from PR #1686: a caller denied `entity.view`/`field.view` who navigated straight to `/{tenant}/data/{entity}` or `/{tenant}/staff` (bypassing the now-hidden nav links) saw real page chrome with a failing/empty fetch instead of a clear message. `EntityBrowserPage.tsx`'s default export now picks the right permission per entity type before rendering (mirrors `entity-rows.ts`'s `CRM_ENTITIES`/`FIELD_ENTITIES`/`HOURS_GATED_ENTITIES` split); `StaffPage.tsx` gained a `field.view` `<Gate>` wrapper around the renamed `StaffPageInner`, same pattern already used by 4 other pages. `tsc`/`check:perms`/full suite (468/470)/build all clean. Squash-merged `85ae80b4`, confirmed live via `published_at` (`10:03:34Z`), not just the deploy record existing.
- Corrected a wrong claim in Royce's global `CLAUDE.md`: the documented "2-4s merge-to-live" figure for eq-shell is trigger latency only (merge → Netlify creates the deploy record). Actual publish takes several minutes — 3 same-day data points (4m59s / 5m23s / 10m21s) against a real `pnpm run build:packages && build:tokens && tsc -b && vite build` pipeline plus 180+ function bundling. Edited the note in place with the evidence; kept the correct part (build starts unattended, no manual step) and fixed the part that was wrong (conflating trigger with publish).

## Decided
- Royce: "Confirmed, go ahead" on the PR #1688 brief.
- Royce: "merge" / "Try again" — explicit instruction to merge #1688 (production-deploying repo, gated per CLAUDE.md).
- Royce, via `/decide` + `AskUserQuestion`: picked "continue the eq-field deny-consumption arc" as the next task over 3 alternatives. Investigation found the entire arc was already closed (PRs #1690/#1691/#1693 here + eq-field PR #851 + its migration, all merged within the ~2 hours before this session checked) — reported that back rather than building something that already existed.
- Royce (implicit, via `/close` after a second `/decide`): held rather than self-selecting further into the NEEDS-YOU backlog. Both headline items (SEC-1, SEC-57) turned out to be gated on Royce's own hands, not buildable by a session — see Notes.

## Deferred
- **#1688 not click-tested live** — no Shell session/credentials in this environment. Worth a real pass: sign in as a denied SKS apprentice, hit `/sks/staff` and `/sks/data/customer` directly, confirm "Not allowed" renders; confirm an allowed caller sees the page unchanged.
- **SEC-1 (P0, security register)** — not a fixable-now finding. It's the SKS NSW Labour decommission, already scoped with a checklist, explicit 2026-07-20 decision ("no interim hardening either — stays untouched until Field replaces it"), and gated on a parallel-run proving period (0/3-4 clean weeks as of last check). Also inside the standing SKS-hands-off rule. Not picked up.
- **SEC-57 (P1)** — org-wide `grok-by-xai` GitHub App install. Confirmed (again) the API can't uninstall it (`DELETE /orgs/.../installations/{id}` 404s, `DELETE /app/installations/{id}` needs the app's own JWT). Needs Royce in GitHub's web UI. Not picked up.

## Addendum (same session, after this close ran) — the "13 RPCs" deferred item above is now closed
- Asked to draft the migration for the 13 remaining Quotes/CRM/pricing RPCs (previous section, `/brief eq-shell` re-run first since Step 6 had already cleared the flag). Step 3's git-state check surfaced `PR #1694` — already merged, ~10 minutes before the ask — doing exactly that fix, migration `0300_quotes_crm_rpcs_denied_perms.sql`, applied to **both** ehow and zaap. Verified all 13 function bodies directly on both planes rather than trusting the PR title: all now carry the deny-check. #1694 also fixed two JWT-minting paths (`mint-sks-jwt.ts`, `tenant-data-proxy.ts`) that weren't sending `extra_perms`/`denied_perms` at all — a bigger fix than what was scoped in the audit that led here.
- Reported back rather than drafting a duplicate. Corrected `eq/pending/eq-shell.md` (archived the now-fully-resolved PR #1690 section to `eq/pending-archive.md`), `eq/changelog/eq-shell.md` (added a #1694 entry, fixed the stale "still ungated" line under #1690), and the `tenant-role-overrides-denials-were-inert` memory file (third and final correction this session — added a pacing note: re-check live state immediately before writing code on a fast-moving arc, even minutes after the last check, given how much concurrent-session traffic this exact security cluster has seen today).
- Also caught: the "ehow-only" scope this session had assumed for the 13 RPCs (carried over from the unrelated eq-field migration's correct-but-different ehow-only scoping) was wrong — #1694 confirms they exist on both planes and needed the fix on both.

## Notes
- Session worktree: `C:\Projects\eq-shell-apprentice-perms`, originally checked out for `claude/apprentice-records-perm-enforcement` (PR #1686) — that branch had already squash-merged (`23f7516b`) before this session started, confirmed via `gh pr view` + byte-identical tree diff against main (see [[squash-merge-branch-contains-trap]]). Repurposed the same worktree onto a fresh branch (`claude/records-direct-nav-perm-guard`) off current `main` rather than continuing on the closed branch.
- `gh pr merge --squash --delete-branch` exited 1 locally — `main` was already checked out in a sibling eq-shell worktree (`eq-shell-quotes-customers-site-contact-wt`), so gh's post-merge local checkout step failed. The remote squash-merge had already succeeded before that step ran; confirmed via `gh pr view --json state,mergedAt` rather than trusting the command's exit code. Finished the branch cleanup by hand (`git push origin --delete`).
- The eq-context shared checkout correctly hard-blocked a direct `git merge`/`git pull` during this close (F9 guard) while 9-11 commits behind origin (all-day nightly/concurrent-session churn, none touching files this close needed). Used an isolated `git clone` per the guard's own suggested fix and this repo's existing memory on the same failure mode, rather than working around the block.
- `tenant_role_overrides` deny-arc memory (`tenant-role-overrides-denials-were-inert.md`) was updated three times across this session as new evidence changed the picture — twice narrowing an over-broad "fully closed" claim back down to what was actually verified. Left a durable note in the memory file itself: don't re-claim "full arc closed" without re-checking the 13-RPC list live first.

---

# Session 2026-09-01 — Flutter SDK PATH fix verified end-to-end; explained flutter doctor's remaining gaps

*Continuation of the 2026-08-31 licence_edit_screen close (`sessions/2026-08-31.md`) — the Flutter-SDK-PATH item flagged there as needing Royce's own action.*

## Built
- Nothing code-side — this segment was verification/support only, closing out the Flutter PATH item from the prior close.
- Gave Royce the exact, scope-correct fix after checking the registry directly (User PATH, not Machine — lower blast radius, no admin needed): a targeted `[Environment]::SetEnvironmentVariable` replace of just the dead `C:\Projects\flutter\bin` segment, leaving the rest of PATH untouched.
- Royce ran it. A first re-check (a genuinely fresh PowerShell window, startup banner visible) still failed — diagnosed as a real, separate gotcha: a "new" shell opened inside an already-running Windows Terminal instance inherits that instance's own stale environment snapshot, not a live registry read. The registry itself was independently confirmed correct at that same moment.
- Royce fully quit and relaunched Windows Terminal; `flutter --version` then resolved cleanly (3.44.8, stable). Fix confirmed complete end to end.
- Separately explained a `flutter doctor` screenshot Royce shared: the 2 flagged categories (missing Android SDK, incomplete Visual Studio install) are for Android/Windows-desktop targets neither needed for eq-cards' actual web deploy — Flutter, Chrome/web, and device/network checks are all green, which is everything `flutter analyze`/`flutter test`/`flutter build web` need.

## Decided
- None new — Royce acted directly (ran the PATH fix, restarted his terminal); no build/ship decisions this segment.

## Deferred
- None — this closes the one open item from the prior close.

## Notes
- Updated the eq-cards project memory (`flutter_sdk_path_stale.md`) twice this segment as new evidence came in — first correcting an over-optimistic "FIXED, should just work" edit another session had made (real screenshot evidence contradicted it), then confirming the actual resolution once Royce's terminal restart fixed it. Left the Windows-Terminal-staleness gotcha documented as a durable caveat: a live registry check and a "fresh-looking" shell banner are each necessary but not sufficient on their own to prove PATH actually resolved.
- Hit, independently, the same PowerShell-vs-Bash clock split the eq-cards session above already flagged — confirmed via Bash's own `date` (2026-09-01 04:26 AEST) before writing this entry, rather than assuming from ambient context.
- `eq-context`'s shared checkout was 11-18 commits behind `origin/main` at various points this segment (heavy concurrent activity, matching every other entry in this file today) — this close's writes were made from the same isolated `git clone` in scratch used for the prior close, fast-forwarded first, per the F9 guard hook's standing remediation.

---

# Session 2026-09-01 — eq-cards: PR #339's live deploy independently verified by pixel-sampling the production render

## Built
- Nothing new built — pure verification pass on request ("check that PR #339 actually deployed live and the flutter is working"), no code changed.
- Confirmed PR #339 merged (`e88a2ad`) and that a `Build & Deploy` `workflow_dispatch` run fired for exactly that commit and succeeded, but didn't stop there — this repo's manual/API Netlify deploys carry `commit_ref: null`, so a green Action and even `state: ready` don't by themselves prove *which* commit is actually serving. Found PR #339's own diff changed the "SIGN IN" label colour on the public sign-in screen (`sky` → `skyAA`), navigated to `cards.eq.solutions` live, located the actual Flutter/CanvasKit rendering canvas (nested inside `flt-glass-pane`'s shadow root — the top-level DOM has no visible text or `<canvas>` at all), and pixel-sampled it directly: the label reads `#267da6`, the exact `skyAA` hex. Direct proof of the specific commit's content, not just deploy-record trust.

## Decided
- None — verification only.

## Deferred
- None new.

## Notes
- Also re-checked the Flutter PATH fix from this session's own prior close (registry read: dead `C:\Projects\flutter\bin` entry gone, correct `C:\src\flutter\bin` present, `flutter.bat` genuinely exists) — this session's own Bash/PowerShell calls still can't find it. Not a new problem: the "Flutter SDK PATH fix verified end-to-end" entry above already root-caused this exact class of staleness (a shell inside an already-running terminal instance, or here an already-running tool session, inherits a stale environment snapshot rather than a live registry read) and confirmed the fix is complete once the terminal/process is genuinely restarted. This session's own tool processes are just another instance of that same pre-fix snapshot — expected, not a regression.
- Pixel-sampling a live element's actual rendered colour (piercing shadow DOM to the CanvasKit canvas, `drawImage`-ing it into a fresh same-origin canvas, reading `getImageData`) is a stronger live-verification method than this repo's established "Netlify `state:ready` + a screenshot" convention when a manual deploy's `commit_ref` is null and a specific commit's presence needs confirming — worth reaching for again in the same situation.

---

# Session 2026-09-01 — eq-shell: closed the tenant_role_overrides deny-check gap on the remaining 13 Quotes/CRM RPCs + 3 policies, PR #1694 merged, migration dispatched live

*Picks up the item the "Records direct-nav permission guard" session (earlier today, same file, above) left deferred: 13 of 14 `public.eq_*` RPCs + 3 RLS policies still had no `denied_perms` check after #1693 closed the first one.*

## Built
- Re-verified the incoming task brief against live state before building anything — it assumed PR #1690 was still open; it had already merged, and one of its target functions (migration 0298's entity.view gate) had already landed twice over via a separately-numbered PR (#1691) from a different concurrent session. Caught before any duplicate work, not after.
- Built in a fresh worktree (`eq-shell-quotes-denied-perms`), not the shared checkout, per the task's own instruction and this repo's file-path worktree trap (a bare path silently resolves to the root checkout).
- Migration `0300_quotes_crm_rpcs_denied_perms.sql`: added a `denied_perms` deny-check to the 13 remaining functions (2 shared pricing helpers, 9 inline functions, 2 with their own second inline PII check) + 3 RLS policies. Every boolean restructured as `is_platform_admin OR (grant AND NOT denied)`, not the flatter wrap the incoming task brief's own example text suggested — checked the actual shipped precedent (`_shared/permissions.ts`'s `can()`, PR #1686) and found it short-circuits `is_platform_admin` before ever consulting `denied_perms`; the flat wrap would let a tenant-role deny collide with and lock out a platform admin whose session role matches the denied role. PR #1693's own migration, merged independently mid-session, uses the identical ordering — external confirmation, not just self-consistency.
- Also fixed 2 of the 3 JWT minters behind `connectTenantClient()` (`tenant-data-proxy.ts`, `mint-sks-jwt.ts`) that didn't emit `denied_perms` — `tenant-data-proxy.ts` (tried first in the fallback chain) didn't emit `extra_perms` either. The SQL fix alone would have been inert without this half.
- `gh pr merge` failed on the first real attempt — not a stale-branch nuisance, a genuine conflict: PR #1693 (merged after this branch was cut) had independently added an equivalent deny-check to `eq__assert_entity_role` and to `mint-tenant-jwt.ts`; PR #1692 (also merged after) had added `created_by` ownership scoping to 2 of the same 13 functions this migration touches. The dangerous part: naively resolving the conflict would have silently reverted #1692's ownership fix — git gave no warning, since the SQL migrations are separate files with no textual overlap, purely a semantic collision on the same live function bodies. Reconciled by resetting the branch to current `main`, dropping the now-duplicate `eq__assert_entity_role`/`mint-tenant-jwt.ts` work entirely, and rebuilding `eq_update_quote_status`/`eq_add_quote_note` on top of #1692's live post-merge bodies (verified via a fresh Supabase query both before and after the rebuild) rather than the pre-#1692 versions — also extended deny-wins to `v_can_view_all` (`quotes.view_all`) in those two, since #1692 introduced that check there and it's the same key already deny-gated elsewhere in this migration. Migration renumbered 0299 → 0300 (0299 was independently claimed by both #1692 and #1693 by the time this pushed — a 3rd same-day collision on one number).
- eq-shell [PR #1694](https://github.com/eq-solutions/eq-shell/pull/1694): CI green (build, 472 tests, lint, schema-drift all pass), squash-merged on Royce's explicit "merge it" — given twice, once before the conflict was found and once again after reconciliation with a fresh confirm, since the actually-merged diff had materially changed from what the first approval covered. Merge commit `ae70b0d3`; confirmed genuinely published (not just merged) via `published_at` on the Netlify deploy record, ~6 minutes after merge — matched the deploy record to the exact commit rather than inferring liveness from the merge alone.
- Dispatched `tenant-migrate.yml` (`workflow_dispatch`, blank slug = whole fleet) on Royce's explicit "dispatch the migration" — read the live workflow file first rather than assuming its inputs from memory, per this repo's own standing note that dispatching from pattern-memory alone (without re-reading the workflow) is exactly how a near-miss happened before. Applied cleanly to both `eq`/zaap (3.1s) and `sks`/ehow (3.3s); confirmed live via a direct `pg_proc`/`pg_policy` query on both planes — all 13 functions + 3 policies now carry the check.

## Decided
- Royce: proceeded with the `is_platform_admin`-precedence design (flagged as a specific choice before building, since it deviated from the task brief's own literal example text); "what should we do" read as delegating the call rather than picking between two offered options.
- Royce: "merge it" (twice — see the conflict/reconciliation note above for why a second confirm was sought rather than just proceeding on the first one).
- Royce: "dispatch the migration."
- Royce: "yes, confirm it published" — asked for explicit live confirmation rather than accepting the merge record alone.

## Deferred
- Nothing new. This closes the item the earlier-today "Records direct-nav permission guard" session left open (`eq/pending/eq-shell.md`'s now-archived "threads tenant_role_overrides denials into the Field JWT" section, moved to `eq/pending-archive.md`).
- **Not click-tested live by a person** — no Shell session/credentials in this environment. Worth a real pass: deny a Quotes permission (e.g. `quotes.create`) for a role via Access Control, confirm the matching RPC now 403s for that role from the browser, confirm a platform admin is unaffected. _(added 2026-09-01)_
- **eq-field-side tracking task `task_9f3eb7a8`** (spawned by the earlier-today session) can be closed — this was the fix it was waiting on. Not closed directly this session (no tool access to another session's task chip from here); noted in the `eq/pending-archive.md` write-up instead. _(added 2026-09-01)_

## Notes
- A `gh pr merge` failure that looks like routine staleness can be a real, security-relevant conflict, not just a rebase nuisance. The tell here was `mergeStateStatus: UNKNOWN` followed by a merge-attempt error ("the merge commit cannot be cleanly created"), not a plain fast-forward-behind message — worth treating that specific error as "stop and diff against origin/main for real content collisions," not "just rebase and push through it."
- Migration-number collisions (three different sessions independently claiming `0299` in one calendar day) look like a recurring hazard during concurrent security sweeps on this repo right now — matches a note already in `eq/pending/eq-shell.md`'s Documents-to-Sign section from 2026-08-30 flagging the identical pattern on `0291`. No CI guard prevents it yet (harmless in practice since `app_data._eq_migrations` keys by full filename, not the numeric prefix, per that same note) — always re-check the target number live immediately before pushing a new migration file, don't trust whatever was free when the branch was originally cut.
- `eq-context`'s shared checkout was 7-14+ commits behind `origin/main` at various points this session (heavy concurrent activity, matching every other entry in today's log) — `git pull` correctly hard-blocked by the F9 guard hook on the first attempt; recovered via a fresh isolated `git clone` in scratch, the same pattern every other session closing today also hit.

---

# Session 2026-09-01 — eq-field Teams: live "Selected" chip panel + click-anywhere-to-edit (v3.5.623/v3.5.624, PRs #854/#856)

*Same-day follow-on to the Teams removal-bug fix (v3.5.621, PR #853) — both requests came from Royce reacting to live screenshots of that shipped fix.*

## Built
- **v3.5.623 (PR #854)** — a live "Selected" chip panel next to the Manage Teams edit form on the standalone Teams page, so a growing member list stays scannable without scrolling the checkbox list. Royce: *"I find this a difficult to see whos a member and will only get worse as our team grows... with all that space on the rigth ahnd side can you sho a summary of whos selected?"* Ran a `/decide` pass before building — upgraded the first-draft design (a read-only mirror list) to removable chips, since a passive mirror still means hunting the checkbox list to actually remove someone; a chip is both the summary and the control. Page-only (the modal opened from Roster/Contacts/Schedule/Timesheets is untouched, by design — narrow quick-edit surface, not the page this was about); page widens 640→960px only while editing. The summary panel deliberately does NOT reuse the `_teamsEditorRoot()` modal/page-detection helper the save path uses — it's pinned unconditionally to the page's own checkbox list, since it only ever renders there; covered by a dedicated test simulating the modal being left open from an earlier navigation.
- **v3.5.624 (PR #856)** — the whole team-list row now opens its edit panel, not just a small button. Royce: *"Can you make it so clicking anywhere within the teams row brings up the edit section."* The old "✎ Edit" button is removed as redundant now the row does the same thing, replaced by a small "Editing" label on the open row. Delete stays its own control, `event.stopPropagation()`'d so declining a delete never also pops the edit panel open underneath the confirm dialog. View-only users (no `field.manage_teams`) get no click affordance at all, mirroring the audit log's existing `.audit-row-jumpable` pattern (new `.team-row-clickable` in `base.css`).
- Both verified via a 35-scenario local harness running the real, unmodified `teams.js` through a local static server (discovered this morning: raw `file://` silently breaks the app's relative `<script src>` resolution), plus real mouse-click verification in the browser pane with the app's actual `tokens.css`/`base.css`/`field-v8.css` loaded — not just assertions. Neither click-tested live on SKS: no Core/SKS credentials in this environment, same disclosed gap as #853.
- Both PRs hit eq-field's cache-buster drift CI check before merge: #854 missed `app-state.js`'s own `?v=` tag (same shape as 4 prior recurrences already in memory); #856 missed `styles/base.css`'s tag — the first time this class of miss has hit a CSS file rather than a `.js` file. Both fixed in a follow-up commit before merge.

## Decided
- Royce: "merge it" — twice, once per PR, each after CI was green and the deploy preview was smoked.

## Deferred
- Live SKS click-through for both — same credential gap as #853, not new.
- **The cache-buster drift recurrence itself.** Hit twice more this session despite the exact fix already being in Claude's own memory (`feedback_cache_buster_tag_bump.md`) and applied correctly once earlier the same day on #853 — memory recall clearly isn't enough once the version-bump ritual feels routine a few hours later. Spawned `task_7499b5e4`: wire `check-cache-busters.mjs` into a local pre-push hook, mirroring this repo's existing brief-gate/reflection-gate enforcement, so it's mechanical rather than remembered.

## Notes
- **This whole session was date-stamped wrong until caught mid-session.** Every substrate write before this point in the day — the earlier close for #853, both `/brief` runs — used "2026-08-31" throughout, copied from the substrate files' own stamps (digest.md's `last_updated`, the pre-existing `sessions/2026-08-31.md`) rather than checked against the real clock. The actual date the entire session ran on was **2026-09-01** (confirmed via `date` after the eq-field brief-gate hook rejected a wrongly-dated flag file, which is what surfaced the mistake — the gate was checking today's real date and a flag named for yesterday didn't match). Corrected going forward from that point; the two pending.md items originally logged "(added 2026-08-31)" for #853's follow-ups were corrected to 2026-09-01 in this close. The prior session log entry and changelog entries for #853 were left dated 2026-08-31 rather than surgically relocated — low value to move, real risk of corrupting an actively-growing shared file for a one-day cosmetic fix; this note is the correction of record instead. Worth being explicit about for any future session reading this file's own chronology: **when copying a date from substrate content, verify it against the real system clock first** — this is exactly the class of drift the substrate's own F1/F14 failures already warn about, just self-inflicted this time instead of found in someone else's file.
- **A real near-miss, caught immediately: an Edit call meant for this session's worktree landed in the shared root instead** (`C:\Projects\eq-field\styles\base.css`, editing in the wrong of two near-identical absolute paths). The shared root was mid-flight on a different, unrelated branch (`claude/csv-import-preserve-existing-fields`) with its own untracked file present. Caught via the very next tool result, verified the damage was exactly one file's diff and nothing else via `git status`, reverted with a single-file `git checkout -- styles/base.css` (not a broad reset), leaving the other session's untracked file completely untouched, then re-applied the same edit correctly to the actual worktree path. No harm done, but a live instance of precisely the collision this repo's CLAUDE.md "Isolate first — never work in the shared root" rule exists to prevent — worth remembering that the failure mode is this mundane (a copy-paste path slip), not something exotic.
- `eq-context`'s shared checkout was correctly hard-blocked again (F9 guard) — this close was written from the same isolated clone opened for #853's close earlier the same day, fast-forwarded rather than re-cloned from scratch.
- The `tenant-role-overrides-denials-were-inert` Claude memory file (updated 3 times by the earlier-today session as the picture narrowed, per that session's own note above) needs one more update to reflect this session's actual closure — done as a follow-up memory-hygiene pass alongside this close, not part of the substrate write itself.

---

# Session 2026-09-01 — Shared DB object drift check: closed the views gap in IDENTITY-MODEL.md §3.3.3

*Run from an eq-field session investigating why neither existing cross-repo guard caught that session's own field_people/field_people_removed regression.*

## Built

- Investigated why neither of the two existing eq-field↔eq-shell guards caught eq-shell's `0249_field_people_view_parity.sql` (2026-08-19) silently changing `field_people`'s/`field_people_removed`'s column shape — root cause, confirmed by reading both guards' actual source: the `security_invoker` guard checks exactly one clause (which this migration correctly included), and the shared-fn guard is functions-only — neither view was even on the §3.3.3 registry table. The migration's own header shows its author *was* careful (explicitly reasserted `security_invoker`, checked `pg_depend`) but had no way to know eq-field's client code depended on the `employment_type` column's values, since that dependency lives in a different repo's JavaScript, not Postgres's catalog.
- Also found, verifying live: eq-shell's own `SHARED_REGISTRY_FUNCTIONS` array (`migrate-tenants.mjs`) was missing `field_job_numbers_src` — already in IDENTITY-MODEL.md's table and in eq-field's own copy of the array (both added 2026-08-26) — a real 3-way registry disagreement, not hypothetical.
- Published a findings + 3-option artifact for Royce (live-verified against ehow, both repos' current code — not from memory or stale docs). He picked, via `AskUserQuestion`: build the generalized live-definition-diff check now; hold the eq-shell-side cross-repo notification extension and the in-flight eq-field/eq-shell ledger-unification cutover (eq-shell PR #1684 + eq-field PR #846, merged 2026-08-30, plumbing only — 14/65 eq-field migrations still need reconciling before a real cutover) for later.
- Built here: `eq/identity/shared-db-objects.json` (machine-readable registry — the 7 existing functions + `field_people`/`field_people_removed` added, `field_job_numbers_src` included; `field_people_worker_id_iu` flagged as a candidate, not promoted), `shared-db-objects.snapshot.json` (baseline hashes seeded from live `pg_get_viewdef`/`pg_get_functiondef` queries against ehow via the Supabase MCP, not guessed), `scripts/check_shared_object_drift.py` + `scripts/test_shared_object_drift.py` (16/16 unit tests pass locally; registry↔snapshot↔diagnose wiring verified end-to-end against the live-fetched definitions), `.github/workflows/shared-object-drift.yml` (nightly + on-push, reuses the `SUPABASE_ACCESS_TOKEN` secret `security-audit.yml` already has — no new secret needed). Confirmed by reading `refresh_digest.py` directly that `scheduled_workflow_health()` auto-discovers any workflow with a `schedule:` trigger by scanning `.github/workflows/*.yml` at read time — a failure here needs zero extra digest wiring, no hardcoded list to update.
- Updated `IDENTITY-MODEL.md` §3.3.3: added `field_people`/`field_people_removed` to the registry table, added the third-incident narrative (dated separately from the two already there), documented the new automated check alongside the two existing narrower mitigations (neither replaced — they're still faster PR-time signals for the two specific patterns they already catch).
- One-line fix in eq-shell: added `field_job_numbers_src` to `SHARED_REGISTRY_FUNCTIONS` in `migrate-tenants.mjs`, closing the confirmed doc/code asymmetry. `node --check` clean.

## Decided

- Royce, via `AskUserQuestion`: "Generalized drift check only (Recommended)" — build the eq-context-hosted check + the eq-shell one-line fix; explicitly deferred the eq-shell notification hook and the ledger-unification cutover rather than bundling everything into one pass.

## Deferred

- **`field_people_worker_id_iu`** — surfaced live during this session's audit (writes `cards_worker_id` back onto `app_data.staff`, same object family as `field_people_iud`). Ownership/shared-edit status not confirmed either way. Tracked in `shared-db-objects.json`'s `candidates_not_yet_decided`, not enforced by the check. Royce's call.
- **eq-shell-side notification hook** — extending `migrate-tenants.mjs`'s existing live-fingerprint warning to also fire the `suite-state-changed` `repository_dispatch` eq-context already listens for, so a touch to a registry object reaches eq-field's side automatically instead of only eq-shell's own CI log. Scoped in this session's artifact, not built — explicitly deferred.
- **Ledger-unification cutover** — eq-shell PR #1684 + eq-field PR #846 (merged 2026-08-30) built the `MIGRATIONS_DIR`/`--bootstrap` plumbing for eq-field to eventually apply its own migrations through this same governed pipeline and ledger. Not cut over: 14 of eq-field's 65 on-disk migrations still need individual reconciliation first. Unrelated to this session's specific fix, but the same root problem's other half — worth revisiting once that lands, since it removes the "two ledgers" half of the gap on its own.
- **Not yet observed running for real in CI** — no `SUPABASE_ACCESS_TOKEN` available in this local environment, so the actual Management API fetch path (`fetch_live_def`) is unexercised beyond what `security_audit.py`'s identical auth pattern already proves works. Everything upstream and downstream of that one call (registry loading, snapshot loading/writing, the normalize/fingerprint/diagnose logic, the CLI wiring) is unit-tested and separately verified end-to-end against the real live-fetched definitions. Will self-confirm on the first scheduled or push-triggered run once merged — if it's silently broken, `scheduled_workflow_health()` surfaces that the same way any other cron failure does.

## Notes

- Full investigation write-up (incident timeline, both existing guards' exact scope, the three options with tradeoffs) published as an artifact in the originating eq-field session, 2026-09-01 — not duplicated here.
- Worked from fresh worktrees off `origin/main` for both repos (`eq-context-shared-object-drift-wt`, `eq-shell-registry-fix-wt`) rather than either shared root checkout: eq-context's root has documented concurrent-session git-race risk (F9/F12), and eq-shell's root was mid-task on an unrelated branch (`claude/entity-view-gate-crm-read-rpcs`) with uncommitted changes. Given today's exceptionally heavy concurrent-session traffic on this exact file (9 prior entries logged today before this one), fetched immediately before pushing rather than trusting the worktree's base.

## Addendum (same session, after this close's first pass) — both PRs merged, one unrelated CI gap found and delegated

- Before opening the PRs, `index-drift.yml` (`INDEX_DRIFT_STRICT=1`) correctly failed on the first push — the 2 new scripts and 1 new workflow weren't in `system/machinery.md`. Fixed (also indexed one pre-existing, unrelated orphan the strict repo-wide scan surfaced in the same run: `eq/sprints/2026-08-30-field-pipeline-and-rls-sprint.md`, missing from `eq/README.md` — read it to write an accurate one-line description rather than guessing; it's the Field tenant-migration pipeline cutover's own execution plan, 2 Wave-2 decisions still waiting on Royce, unrelated to today's fix). Verified `index_drift.py` clean locally before pushing.
- eq-context PR #196's CI came back green on everything in its own diff (index-drift, frontmatter, line-endings, claim-expiry, honesty). One unrelated check ("health" / `link_check.py`) failed — confirmed via `git show origin/main:<file>` that both broken links already exist on `main`, untouched by this PR. Spawned as background task `task_7db34849` rather than bundled in or silently left. eq-shell PR #1701 came back fully clean (`mergeStateStatus: CLEAN`).
- Royce: **"merge"** (both). Merged: [eq-context #196](https://github.com/eq-solutions/eq-context/pull/196) → `5c40dc1b` (merged despite the pre-existing "health" failure — confirms it isn't a hard-required branch-protection check here); [eq-shell #1701](https://github.com/eq-solutions/eq-shell/pull/1701) → `61ec190a`. Both worktrees removed, both local branches deleted post-merge.
- This `/close` pass hit the F9 guard on a plain `git merge --ff-only origin/main` in the shared eq-context root (34 commits behind by this point, all unrelated nightly/concurrent-session churn) — correctly blocked, recovered via the hook's own suggested fresh isolated clone rather than working around it.

---

# Session 2026-09-01 — eq-shell/eq-field: tenant_role_overrides deny-arc re-verified end to end; one real bug found (role-default bypass), fix spun off as its own task

*Continuation of the original apprentice-records-perm-enforcement work (PR #1686) in this same session/worktree lineage. By the time this segment ran, the broader arc (#1688/#1690/#1691/#1693/#1694 here, eq-field #851) was already merged and live — confirmed each directly via `gh pr view`/commit-ancestry/live Netlify `published_at` rather than trusting the pending.md state, since this repo's concurrent-session traffic makes "checked minutes ago" an insufficient bar today specifically.*

## Built
- Nothing code-side this segment — verification + one new finding, spun off rather than built here.
- Merged eq-shell PR #1693 on explicit "merge it" (Field-JWT deny-claim half), confirmed live via Netlify `published_at` matching the merge commit exactly (`22536459`, published `17:49:30Z`, ~6 min after merge — consistent with this repo's real build time, not the 2-4s trigger-latency figure).
- Dispatched migration `0299_entity_role_deny_gate.sql` (`tenant-migrate.yml`, blank slug = whole fleet) on explicit "dispatch 0299" — applied cleanly to both `eq`/zaap (3.5s) and `sks`/ehow (2.5s), confirmed via job logs, not just a green run.
- Reviewed eq-shell #1690 and eq-field #851 diffs directly before either merged (not just their titles/summaries) — both clean; #851 explicitly deferred its own companion ehow RLS/trigger migration as "NOT applied, pending Royce's dispatch" per its own reflection-log entry.
- Re-verified that deferred claim live rather than trusting it: found the companion migration (`20260831_field_permission_denials_enforce`) had **already been applied** to ehow by the time this was checked — 10/10 functions + 6/6 policies confirmed via direct `pg_get_functiondef`/`pg_policies` query, contradicting both the reflection-log entry and what this session had just told Royce. Corrected that immediately rather than letting the stale claim stand.
- Reading `field_people_iud`'s actual live body while confirming the above surfaced the real bug: `role_default OR (extra_perms grant AND NOT denied_perms)` — the deny-check only gates the extra_perms branch, so a manager/supervisor who already qualifies via the bare role check never hits the deny at all. Spot-checked one policy with no role-branch (`staff_conversations_creator_and_perm`) to confirm the gap is specific to objects mixing a role-default with an override, not universal.
- Spawned `task_d9611f7d` for the fix rather than building it here (different repo, no established worktree in this session, narrow/no live urgency — zero `tenant_role_overrides` rows target any affected key today). The spawned session built and pushed eq-field PR #859 (`20260901_field_permission_denials_role_bypass_fix.sql`) — its own build process wasn't observed directly from here; verified its actual state via `gh pr view` before writing anything about it (currently `mergeable: CONFLICTING`, not yet applied to live ehow, correctly held pending Royce's go).
- Also confirmed, in passing: eq-field PR #852 (an urgent live bug — an already-applied RLS migration silently blocking employee/apprentice/labour_hire from submitting prestarts/toolbox talks, found by a different concurrent session's reflection-log entry) was already merged and live — not something this segment needed to act on.

## Decided
- Royce: "merge it" (#1693).
- Royce: "dispatch 0299."
- Royce, via AskUserQuestion: reviewed #1690/#851 before merging either (not skipped).
- Royce, via AskUserQuestion: build the role-default-bypass fix as its own spun-off task rather than in this session.

## Deferred
- **eq-field PR #859 not merged (`CONFLICTING`) and not applied to live ehow** — needs a rebase, then Royce's explicit go to dispatch. See `eq/pending/eq-field.md` (new section, 2026-09-01).
- **Origin of 2 already-correct RLS policies (`labour_hire_companies_select`/`labour_hire_rates_select`) is unexplained** — they had the right shape before PR #859 existed, no PR/migration accounts for it. Low priority.
- **Live click-through still not done by any session in this arc** — every verification across the whole multi-session arc (this one included) has been via direct DB/API checks, never a real signed-in SKS apprentice clicking through Shell. Worth a real pass whenever convenient.

## Notes
- This segment corrected its own prior claim to Royce (the ehow migration being "not yet applied") within the same conversation, rather than letting a stale statement stand once live evidence contradicted it — worth naming since the instinct to just move on after answering is real.
- `tenant-role-overrides-denials-were-inert.md` (Claude memory) already carries the full, current, corrected picture as of this write — not duplicated here in full; see that file for the exact mechanism, object list, and the "genuinely fully closed" claim's own correction history (it was wrongly declared closed twice before this bug was found).
- Same F9 guard / isolated-clone pattern as every other session closing today — eq-context was 36 behind `origin/main` at the start of this close.

---

# Session 2026-09-01 — eq-solves-service: /defects revalidation gap fixed and shipped live (PR #822); migration 0197 CI bug root-caused

## Built
- Investigated an incoming, already-well-scoped task: confirmed via grep + direct reads that `updateCheckItemAction`/`updateCheckItemResultAction` in `app/(app)/maintenance/actions.ts` both write `maintenance_check_items.result` and neither calls `revalidatePath('/defects')`, while the `fn_check_item_to_defect` DB trigger (migration 0061) auto-creates/resolves a `defects` row on every fail/un-fail transition.
- Verified the trigger chain live on ehow before trusting the migration file alone: `service.maintenance_check_items`/`service.defects` are views over `app_data.*` (migration 0147's June canonicalization), and the trigger is still correctly attached to the real `app_data.maintenance_check_items` base table — confirms the bug is real, not theoretical.
- Discovered mid-task: open PR #821 ("P2 polish"), unmerged at the time, had independently found this exact gap while narrowing `revalidateMaintenanceSurfaces()`, documented it in a code comment, and filed it as `task_3b858742` instead of fixing it inline — this session's task is almost certainly that follow-up.
- Fixed both actions with `revalidatePath('/defects')`, matching the pattern already used by 3 other defect-mutating actions in the same file. `npx tsc --noEmit` and `npm run check` (full Turbopack build) both came back clean after clearing a stale `.next` build cache that was corrupting the first attempt.
- Committed on a fresh branch off `origin/main` (not stacked on #821), pushed, opened eq-service [PR #822](https://github.com/eq-solutions/eq-service/pull/822).
- Watched PR #822's CI to completion (`gh pr checks --watch`, backgrounded): `tsc + next build` and `Typecheck + audit` both passed; `Integration tests (Supabase local)` failed. Root-caused rather than waving it off as "known pre-existing": confirmed it fails identically on `main`'s own latest commit (not caused by this PR), then traced the SQL — migration `0197_report_settings_per_tier.sql` assumes `service.tenant_settings` already has a legacy column (`report_show_cover_page`) that was only ever added to `public.tenant_settings` (migration `0015`, a different schema). Verified live that production itself is unaffected (`service.tenant_settings` on ehow already has both columns). Filed as its own follow-up (`task_8369a579`) rather than scope-creeping into fixing an unrelated, month-old migration bug.
- On Royce's "merge": re-verified mergeable/CI state, squash-merged PR #822 (fast-forwarded past PR #821, which had merged in the meantime — both landed cleanly, no conflict). Confirmed the resulting Netlify production deploy actually published (not just merged, not just triggered) by polling the specific deploy record for the exact merge commit's SHA until `state: ready` — took ~2 minutes.
- Updated `eq/pending/eq-solves-service.md`: closed the "P2 items from the review" bullet (4/5 shipped via #821, the 5th — `ExportButton`/`CsvExportButton` consolidation — investigated and deliberately left alone per #821's own PR body); enriched the existing 2026-08-23 entry's CI bullet with today's full root-cause instead of filing a duplicate finding; added a new dated section for this session's fix.

## Decided
- Royce, via `AskUserQuestion`: "Commit + open PR now" over leaving the diff uncommitted in the shared root checkout — given this repo's documented history of concurrent sessions silently discarding uncommitted work in this exact shared checkout.
- Royce: "check back in a few minutes" on CI, then "merge" — explicit merge authorization, understood (per this repo's own Netlify-auto-deploy-on-push convention, same mechanism as eq-shell) as also authorizing the resulting production deploy.

## Deferred
- **`task_8369a579`** (migration 0197 schema-mismatch breaking CI's fresh-DB bootstrap) — Royce started it running independently in a separate local session before this session closed; not yet reported back. Root cause and both candidate fixes are written up in the task prompt and in `eq/pending/eq-solves-service.md`'s updated 2026-08-23 entry.
- **Live click-through of the /defects fix** — no Shell session/credentials in this environment to fail a real check item and watch `/defects` update without a hard reload.

## Notes
- `task_3b858742` (PR #821's own filed follow-up, almost certainly this session's incoming task) wasn't dismissable from here — `dismiss_task` only sees chips spawned in the same session/conversation. Left it for whoever sees it to close trivially; PR #822's description already explains it supersedes that chip.
- This session's implementation work (editing `eq-solves-service`'s own `actions.ts`) needed `/brief` run first — the guard blocked the first `Edit` attempt until the skill actually wrote its per-session flag file, standing documented behavior, not a surprise.
- `eq-context`'s shared checkout was 34-36 commits behind `origin/main` by the time this close ran (heavy same-day concurrent activity, matching every other entry in today's log). A plain `git merge --ff-only origin/main` was correctly hard-blocked by the F9 guard hook on the first attempt; recovered via a fresh isolated `git clone` in scratch, per the hook's own suggested remediation — the same pattern nearly every other session closing today also hit.

---

# Session 2026-09-01 — Mirror-image group-column bug: premise was stale, real bug was different and bigger

*Asked to investigate a bug PR #855 flagged but explicitly didn't fix: 3 call sites (`apprentices.js:561`, bundled `auth-gate-picker.js`/`auth-staff-ts-gate.js`) selecting `group` literally against the `people` table, assumed to 400 for managers on `field_people` (assumed real `employment_type` column, no `group` column).*

## Built

- Before touching any code, queried ehow live per this repo's own Rule 0.5 discipline — the premise was stale. `field_people`/`field_people_removed` both now carry a real `group` column (aliased from `staff.employment_type`) **and** a hardcoded `NULL::text AS employment_type` stub, confirmed via `pg_get_viewdef` and a live row sample. Source: eq-shell's own migration `0249_field_people_view_parity.sql`, applied to ehow 2026-08-19 — completely untracked in eq-field's own migration ledger (neither `supabase_migrations.schema_migrations` nor a local `.sql` file), same shared-object coordination gap this repo's CLAUDE.md already documents for `field_people_iud()`.
- Net effect: the 3 originally-flagged call sites need no fix at all — `group` already resolves correctly everywhere today. But PR #855's own merged fix (v3.5.625, landed mid-session) only translates `employment_type`→`group` on the non-manager/directory-routed branch, leaving a bigger, different bug live: a manager's own GET of `people`, and any GET of `people_removed` (which never routes to the directory), still asked for `employment_type` — now a hardcoded NULL on both views. The central roster/timesheets staff-name-map fetch does exactly this, so every manager has silently gotten blank group classifications (Direct/Apprentice/Labour Hire) since 2026-08-19.
- Fixed: widened `_jwtPhysical()`'s translation to the whole `people`/`people_own`/`people_removed` family regardless of routing branch, superseding PR #855's narrower version in the same function. No call-site changes needed anywhere. eq-field [PR #858](https://github.com/eq-solutions/eq-field/pull/858) (v3.5.626) — 7 new/changed `_jwtPhysical` test cases (45/45 total passing), `roster-adapter`/`timesheets-adapter` suites unaffected (131/131, 96/96). Caught and fixed two secondary gaps while verifying: a cache-buster tag miss on `app-state.js`'s own `?v=` tag (the exact recurring drift class this repo's history already warns about), and a `max-lines` breach (1551 vs the 1550 grandfathered ceiling on `scripts/supabase.js`) — trimmed the new comment rather than raising the ratchet.
- Rebased once over PR #857 (concurrent, unrelated tooling commit) before merge — re-verified the version number was still free, re-ran tests/lint/cache-busters post-rebase, all clean.

## Decided

- Royce: "Confirmed — go ahead and build it" (on the session brief, after the live-DB finding reframed the task). Then explicit "merge #858" — verified live via the actual Netlify deploy record (`commit_ref` match, `context: production`, `state: ready`, `published_at` set), not just the merge event or a fixed timer.

## Deferred

- Live click-through as a real SKS manager confirming roster/timesheets group labels are populated again — no SKS/Core credentials in this environment, disclosed in the PR.
- The recurring root cause (3rd incident of this shared-object-drift shape) was spawned as its own background task — see the immediately-preceding session entry above for its outcome (generalized drift-check, eq-context #196 + eq-shell #1701, both merged). Not re-summarized here to avoid duplicating that entry.

## Notes

- **Correction, made during this `/close`:** this entry originally speculated a connection between PR #859 (above) and migration 0249, based on a partial read of that session's mid-investigation transcript via `search_session_transcripts`. Reconciling this file against that session's own finished write-up (immediately above, once the concurrent edits merged) showed the speculation was wrong — PR #859's actual root cause is a different migration (`20260831_field_permission_denials_enforce`) with an unrelated OR-branch structuring bug. No connection between the two; corrected here and in `eq/pending/eq-field.md` rather than left standing.
- This session's own `/close` pass also hit the F9 guard on `git merge --ff-only` in the shared eq-context root (36 commits behind at that point) — recovered the same way the preceding session's addendum describes (fresh isolated clone), independently arriving at the same recovery pattern.

---

# Session 2026-09-01 — eq-field: Windows CRLF false-positive in check-cache-busters.mjs fixed and shipped (PR #861)

*Royce reported the bug directly in chat, with root cause, live evidence, and two candidate fixes already diagnosed — this session verified and implemented rather than investigated from scratch.*

## Built
- Reproduced live before changing anything: `core.autocrlf=true` confirmed on this clone; running `check-cache-busters.mjs --base origin/main` false-positived on `styles/tokens.css` even though `git diff --stat origin/main` showed zero real difference. Isolated the exact cause: `readAtRef()` reads `git show`'s always-LF blob, `headContent` reads raw `readFileSync()` working-tree bytes (CRLF on this checkout) — `headContent.replace(/\r\n/g,'\n') === baseContent` confirmed CRLF was the only difference (10904 vs 10690 bytes).
- Fixed with the reported option (a) — one `normalizeEol()` helper applied at the single equality check — over option (b) (`.gitattributes`), since in-script normalization doesn't depend on every contributor's git config.
- Verified both directions before opening the PR: false positive cleared (exit 0, 29 files checked); a genuine content change with no tag bump still correctly failed (exit 1) — tested by temporarily editing `app-state.js`, confirming the fail, then reverting before staging anything.
- Built in an isolated worktree (`claude/cache-buster-crlf-fix` off `origin/main`) — the shared root was mid-flight on an unrelated branch (`claude/field-permission-denials-role-bypass-fix`, PR #859's own branch — see below — left completely untouched throughout).
- This repo's `/brief` and `/reflect` guard hooks fired and were followed as designed: the brief-gate blocked the first `Edit` until `/brief eq-field` ran, the reflection entry was logged and staged in the same commit as the fix.
- Hit a real merge conflict on rebase: a concurrent session's Roster PR (#860) had independently appended its own `/reflect` entry to the same `docs/reflection-log.md`. Resolved by keeping both dated sections (no content overlap, just the same anchor line), re-verified the checker post-rebase, force-pushed.
- eq-field [PR #861](https://github.com/eq-solutions/eq-field/pull/861) — CI green (`Tests + lint`, `drift`, Netlify deploy preview), squash-merged on Royce's "wait for CI, then merge."
- `gh pr merge --delete-branch`'s local cleanup step failed — a different, unrelated worktree (`teams-member-removal-bug-48d4a4`) already had `main` checked out, so git refused to switch this one over. Confirmed via `gh pr view` that the GitHub-side squash-merge had already succeeded (`state: MERGED`) before finishing branch cleanup by hand. The remote-branch delete then hit the *same* stale-script false positive this PR fixes — run from the shared root, still checked out on the pre-merge branch with the old script — used `EQ_SKIP_CACHE_BUSTER_CHECK=1`, appropriate since a pure ref deletion carries no file content for the check to apply to.

## Decided
- Royce, via `AskUserQuestion`: fix option (a) (in-script normalization) over (b) (`.gitattributes`) or both.
- Royce, via `AskUserQuestion`: "Wait for CI, then merge" — explicit, conditional merge authorization (this repo auto-deploys on merge to `main`).

## Deferred
- None. No version bump needed (pure dev-tooling, never ships to the browser — same precedent as PR #857's hook-wiring commit); no live click-through gap (invisible to end users); no open thread left.

## Notes
- No connection to eq-field PR #859 (the role-default-bypass fix, same shared root, still `CONFLICTING`/unmerged/pending Royce's go per the earlier session entries above) beyond causation: pushing #859's branch is what originally hit this false positive, forcing the `EQ_SKIP_CACHE_BUSTER_CHECK=1` bypass to get that push through in the first place. #859 itself was correctly left untouched this session.
- `eq-context`'s shared checkout was 41 commits behind `origin/main` at the start of this write (heavy same-day concurrent activity, matching every other entry in today's log) — a plain `git pull` was correctly hard-blocked by the F9 guard hook on the first attempt; recovered via a fresh isolated `git clone` in scratch, per the hook's own suggested remediation, the same pattern nearly every other session today also hit.

---

# Session 2026-09-01 — eq-context: fixed 2 broken internal links flagged by CI health check (PR #197) — closes background task_7db34849

## Built
- Fixed 2 broken internal markdown links flagged by CI's health job (`scripts/link_check.py`), both pre-existing on `main` and confirmed unrelated to PR #196 before touching anything: `eq/pending-archive.md:6254` had one `../` too many (`[worktree-registry.md](../../system/worktree-registry.md)` → `../system/worktree-registry.md`) — grepped all 3 mentions of "worktree-registry" in the file first rather than trusting the incoming task's guess of "at least 3" broken occurrences: line 4759's identical link text already had the correct depth, and line 3990's mention is inline code, not a markdown link, so only the 1 at line 6254 was actually broken.
- `eq/verify-queue.md:2403` linked a bare `eq-solves-service.md` (resolves to a nonexistent `eq/eq-solves-service.md`) instead of `pending/eq-solves-service.md` — pending files were split into `eq/pending/<repo>.md` on 2026-08-17 per `eq/README.md`; confirmed the target file exists before fixing, not just assumed.
- Worked in an isolated worktree (`eq-context-linkfix-wt`, branched fresh off a just-fetched `origin/main`), never the shared root, per this repo's own F9 guidance.
- Verified with `python scripts/link_check.py`: 335 internal links checked, 0 broken.
- eq-context [PR #197](https://github.com/eq-solutions/eq-context/pull/197) opened (open, not yet merged as of this entry).

## Decided
- Royce confirmed the session brief ("Proceed") after the brief-gate hook blocked the second file edit mid-task — ran `/brief eq-context` to clear it rather than setting `EQ_SKIP_BRIEF=1`.

## Deferred
- None — task is fully self-contained. Only remaining step is merging PR #197.

## Notes
- **This closes background task `task_7db34849`.** An earlier session today (this file's own prior entry, "Shared DB object drift check," addendum) hit the identical failure merging PR #196 — same "health" check, same 2 pre-existing broken links, same "unrelated to #196" finding — and spawned that task rather than fixing it inline. The task description this session received at start matched it exactly (link paths, line numbers, PR #196 framing). If `task_7db34849`'s chip is still showing anywhere, it can be dismissed once #197 merges.
- Shared eq-context checkout was correctly hard-blocked by the F9 guard on a plain `git merge --ff-only origin/main` attempted for this close's own writes (36+ commits behind, all unrelated nightly churn) — recovered via an isolated worktree (`eq-context-close-wt`) rather than working around the block, same pattern as every other entry in this file today.

---

# Session 2026-09-01 — SKS NSW Labour safety records backfilled into EQ Field's SKS tenant

*Royce, direct request via the standing sks-nsw-labour reconciliation carve-out (read/reconcile its live data on request is fine; never proactive code work there) — copy prestart/toolbox-talk history into EQ Field.*

## Built
- Live-compared sks-nsw-labour's own live Supabase project's prestarts/toolbox_talks against EQ Field's SKS-tenant `ehow` before copying anything: schema shape is near-identical (this repo's own Safety module was evidently forked from sks-nsw-labour's), neither app enforces `site_abbr`/`submitted_by` as real foreign keys. Both systems had been recording the same May27-Aug31 window in parallel, so a raw copy risked duplicate compliance records for the same site-day — Royce chose dedup-first over a raw append.
- Deduped on normalized (site_abbr + date + subcontractor): 178 of 207 source prestarts and all 5 toolbox talks were new; 29 already existed in both systems. Copied in 5 batches (178 prestarts) + 1 batch (5 toolbox talks) into `ehow`'s `public.prestarts`/`toolbox_talks`, preserving original source row IDs, `ON CONFLICT DO NOTHING`. Stripped embedded `signature_image`/photo base64 (found only after two size-blowup retries mid-session — one query returned 21M characters before the `crew` column's embedded signatures were identified as the cause) — kept every other field, including crew/attendee names and sign-off timestamps.
- Verified counts live post-copy: 224 prestarts (was 46), 6 toolbox talks (was 1).
- Asked "is data usage an issue" — investigated live rather than assuming: confirmed no privacy/entity issue (same company's own data moving between two of its own systems; signatures/photos already excluded), but found a real functional gap — 79 of the 178 new rows (44%) carried a `site_abbr` that didn't resolve against EQ Field's canonical site list, verified against the actual rendering code (`safety.js`'s `.docx`-export site lookup), not assumed.
- Fixed 59 of the 79: 13 "Arncliffe" variants -> `ARN` (already existed, just under the full word not the site code); 8 casing/whitespace variants -> correct case. The remaining "DR" (38 rows) had zero site match under any name/abbr in `app_data.field_sites`; declined to create a new site record from here (site creation looks like Shell-owned canonical territory, same pattern as the Arncliffe/SY3 triple-dup issue already tracked in `project_site_dedup_resolver_and_sldc_gap`) and asked Royce instead. Royce corrected directly: "Should be SYD11" -> `field_sites.customer_name` for `SYD11` is literally "Digital Realty", missed by the initial name/abbr-only search. Remapped.
- 20 rows remain unresolved (15 blank, 5 one-off site names) -- left as free text, not guessed at.

## Decided
- Royce, via `AskUserQuestion`: dedup against existing EQ Field records before copying, rather than a raw append.
- Royce, via `AskUserQuestion`: proceed with the copy once the exact row counts (178+5) and transform plan were shown.
- Royce (free text): "Should be SYD11" -- corrected the "DR" site-matching gap directly rather than picking from the offered options (create/leave-as-text/not-worth-it).

## Deferred
- 20 prestarts with no resolvable `site_abbr` (see `eq/pending/eq-field.md`).
- Simon Bramall (via Royce) has asked to reconcile all *future*-dated sks-nsw-labour entries into EQ Field too -- not started this session; the immediate ask is a list of what's already entered on each side.

## Notes
- `eq-context`'s shared checkout was 43 commits behind `origin/main` at close -- `git pull` correctly hard-blocked by the F9 guard hook; recovered via a fresh isolated `git clone` in scratch, per the hook's own suggested remediation (same pattern nearly every other session closing today also hit, per the rest of this file).
- This was a live-data operation via the Supabase MCP directly against `ehow` -- no eq-field code file changed, no PR, no deploy.

---

# Session 2026-09-01 — eq-field: Weekly Roster "By Crew" gap chips + Edit Roster name/site filter (v3.5.627, PR #860)

## Built
- Royce called the SKS Weekly Roster "By Crew" card useless from a live screenshot ("what are our options?"). Read `_computeRosterOverview()`/`_renderRosterOverviewCard()` before proposing anything — the card already computed the coverage gap (`totalActive` vs `onCount`) but never surfaced who made it up. Presented 3 concrete directions via `AskUserQuestion` (lead with gaps / default to Grid / trim to search); Royce picked "lead with gaps."
- Added a "Not rostered today (N)" red chip row above the site list, listing active/non-leave people with no site code for today. Verified `personInActiveTeam()` reads live filter-chip state (`STATE.teamFilters`), not team membership, before deciding NOT to fold the "Unassigned(27)" Team chip into this card — different concept (a person with no Team can still be legitimately rostered today), would have been wrong. Left the existing "don't list empty sites" rule (Royce, 2026-07-31) untouched — that's about missing sites, this is about missing people.
- Mid-turn, Royce asked (from a second screenshot) whether Edit Roster could filter by name/site. Read `renderEditor()` + `#page-editor`'s markup directly — confirmed it had neither, unlike the read-only Weekly Roster page. Added `#editor-search`/`#editor-site`, computed independently inside `renderEditor()` rather than reused from the read-only page's `getRosterPeopleForOther()`/`_rosterApplySearchSiteSort()` — reusing that would have silently filtered Edit Roster's "Other" group bucket by the OTHER page's inputs while every other group filtered correctly, a real bug that would have shipped invisibly.
- The two changes pushed `roster.js` 4 lines over its CI-enforced 1900-line ceiling. Extracted the whole Roster Overview card block (`_rosterOverviewDayKey`/`_computeRosterOverview`/`_rvOverviewToggleSite`/`_renderRosterOverviewCard`) into new `scripts/roster-overview.js`, matching the existing `roster-overview-map.js`/`week-picker.js` extraction precedent rather than raising the ratchet — required hand-syncing `scripts/lazy-loader.js` AND `scripts/core-bundle-b1.js` (a "hand-merged" bundle that turned out to be the actual live-served copy of lazy-loader.js's TAB_SCRIPTS registry — `build-bundles.mjs --check` confirmed it as a real 3-source managed bundle despite its own header comment calling itself unmanaged) plus `sw.js`'s precache list.
- Full local boot isn't reachable in this sandbox — `loadTenantConfig()` now requires the `tenant-config` Netlify Function even for `?tenant=demo`, which a bare `npx serve` static server 404s on. Verified instead by fetch+eval'ing the real shipped source into a live browser tab against constructed fixture data, driving both new features through real DOM clicks/typing, screenshotted before and after the file extraction.
- `npx eslint` (0 errors), `node tests/roster-adapter.test.js`/`roster-cell-picker.test.js`/`roster-rules.test.js` (131/18/51, all green), `check-cache-busters.mjs`/`build-bundles.mjs --check` (clean) all run before opening the PR.
- eq-field [PR #860](https://github.com/eq-solutions/eq-field/pull/860) (v3.5.627) — CI green, deploy preview smoked, squash-merged on Royce's explicit "merge." Confirmed live via `field.eq.solutions/sw.js` showing v3.5.627 post-merge, not just the merge event.

## Decided
- Royce, via `AskUserQuestion`: "Lead with gaps" over "default to Grid" or "trim to search" for the By Crew card redesign.
- Royce: "merge" — explicit authorization (this repo auto-deploys on merge to `main`).

## Deferred
- **Red "Not rostered today" chips flag anyone blank-today, not just real gaps** — someone between jobs, on an unlogged admin day, or labour-hire not needed this week reads identically to an actual problem. No dismiss/reason-code escape hatch built; deliberately held until Royce has seen it in real use rather than guessed at pre-emptively. _(added 2026-09-01)_
- **Not click-tested live by a person** — no SKS/Core credentials in this environment. Worth a real pass: SKS supervisor opens Weekly Roster, confirms the gap chips read sensibly against real data; opens Edit Roster, confirms the new filters narrow correctly. _(added 2026-09-01)_

## Notes
- A later session today (this file's own "Windows CRLF false-positive" entry) independently hit and resolved a rebase conflict against this session's `docs/reflection-log.md` entry while rebasing an unrelated fix — no content overlap, no action needed from here; noted for continuity since it references this PR by number.
- `eq-context`'s shared checkout was 47 commits behind `origin/main` at the start of this close (heavy same-day concurrent activity, matching every other entry in this file today) — a plain `git merge --ff-only origin/main` was correctly hard-blocked by the F9 guard hook on the first attempt; recovered via a fresh isolated `git clone` in scratch, the same pattern nearly every other session closing today also hit.

---

# Session 2026-09-01 — Link an existing site from EQ Ops, warn on likely-duplicate new sites

## Built
- EQ Ops (Quotes module) Quote Details Site field gained a "Link existing site" button, next to Edit site/New site, gated on the same `entity.edit` permission as editing a site. Opens the existing `LinkSiteModal` (searches every site in the tenant by name/code/suburb, links via `site_customer_links`) — previously wired only into the Customers page (PR #1582). Linking now auto-selects the site onto the quote via the same path "New site" already used.
- `AddSiteModal` (shared by EQ Ops and the Customers page) now takes an optional `existingSites` list and shows a non-blocking warning when the typed name loosely matches (case/whitespace-normalized, substring either direction) a site already on file for that customer — "X already exists — use Link existing site instead" — and relabels Save to "Add anyway" while the warning is showing. Wired into both call sites.
- No schema/migration changes — reused already-shipped, already-granted RPCs and actions (`eq_list_sites`, `link_site_customer` via `crm-write.ts`, `entity-rows.ts`).
- Shipped as eq-shell PR #1703, merged (squash `537d0902`), confirmed live on core.eq.solutions via Netlify deploy record (`published_at` set, `commit_ref` matches, `context: production`, ~6 min build).

## Decided
- Royce spotted the gap himself from a live EQ Ops screenshot ("No sites — type to set" / "New site") and asked whether an existing site could be linked from there, flagging the duplicate-creation risk.
- Given the choice (after `LinkSiteModal` was found already built but unwired into EQ Ops) between just the duplicate-name warning, just wiring in the existing link modal, or both — Royce chose both.
- Royce explicitly said "merge" — treated as the production-deploy approval per this repo's own CLAUDE.md (merge to `main` auto-deploys, unattended).

## Deferred
- Not click-tested live by a person — no Shell session/credentials in this environment, verified via `tsc -b --force` + `eslint` + a merge-readiness audit reproducing CI. See `eq/pending/eq-shell.md`.
- GitHub MCP connector (`mcp__d2708d72…`) 404s on every `eq-solutions/eq-shell` call despite a valid authenticated identity (`get_me` succeeds) — used `gh` CLI instead, which has real access. Not investigated further. See `eq/pending/eq-shell.md`.

## Notes
- Before building anything, checked for prior/duplicate work per Rule 0.5: a stale local branch `claude/add-existing-site-customer-b89b1d` looked like the same task at first glance. Its one commit had already shipped to `main` via squash `53289f48` (PR #1582) — confirmed via `git log --all --grep`. That PR's diffstat touched `SiteModals.tsx` + `CustomersPage.tsx` only, confirming `QuotesModule.tsx` (EQ Ops) genuinely never got the wiring — the gap Royce found was real, not imagined.
- `eq-context` substrate was 51 commits behind `origin/main` at session start/close; per the F9 guard, all substrate writes this close were done from an isolated clone (scratchpad), not the shared `C:\Projects\eq-context` checkout — the shared checkout's `git merge` is now hook-blocked for exactly this reason.

---

# Session 2026-09-01 — eq-field: Edit Roster site dropdown showed no sites -> folded into search (v3.5.629, PR #863)

*Same-day follow-on to the "By Crew gap chips + Edit Roster name/site filter" session above (PR #860) — Royce screenshotted the just-shipped Edit Roster page hours later and reported the new site dropdown empty.*

## Built
- Royce: "Sites dropdown doesnt show any sites - can the search field just be for the whole roster (SY3 shows everyone at SY3)" — a bug report plus his own proposed fix, in one message. Read `getAllSiteCodes()` (defined in the lazy-loaded `roster.js`) and grepped every call site of `refreshPersonSelects()` (only `refreshData()` plus a few post-mutation call sites in `people.js`/`batch.js`/`import-export.js` — nothing tied to `showPage('editor')`) before concluding root cause: a select populated by a `refreshData()` cycle that ran before `roster.js` finished lazy-loading stays stuck on "All Sites," with nothing re-triggering population when the Editor tab is first opened. Checked `styles/*.css` for a simpler CSS explanation first — none found.
- Built exactly the fix Royce proposed rather than patching the race: dropped `#editor-site` entirely; `#editor-search`'s filter now matches a person's name OR any site code on their visible-week schedule in one field. Removes the lazy-load dependency rather than working around its timing.
- Verified via real `form_input` into the actual rendered control (not a simulated call): "syd27" correctly narrowed to exactly the 2 fixture people scheduled there that week, "casey" correctly matched by name alone, confirmed `#editor-site` no longer exists anywhere in the DOM. Same standing local-boot limitation as every other entry today — `tenant-config` Netlify Function 404s on a bare static server even for `?tenant=demo`.
- `eslint` (0 errors), `tests/roster-adapter.test.js`/`roster-cell-picker.test.js`/`roster-rules.test.js` (131/18/51, all green), `check-cache-busters.mjs` (clean) run before opening the PR.
- eq-field [PR #863](https://github.com/eq-solutions/eq-field/pull/863) (v3.5.629) — squash-merged on explicit "merge," confirmed live via `field.eq.solutions/sw.js` showing v3.5.629 within seconds of merge.
- Flagged, not fixed: the read-only Weekly Roster page's `#roster-site` shares the identical population code and is a plausible instance of the same bug — not reported broken, not reproduced live, so not touched blind. Spawned as `task_028d9925`; Royce started it running in a separate local session before this close, in progress, not yet reported back.

## Decided
- Royce specified the fix shape directly (fold site-matching into the search field) rather than being offered options — implemented as given.
- Royce: "merge" — explicit authorization, confirmed live immediately after.

## Deferred
- **`task_028d9925`** (the `#roster-site` follow-up above) — running independently in a separate session, outcome not yet known.
- One field doing double duty (name vs. site, no visual indication of which reason a person matched) is a real precision loss on a fleet with many `SY`-prefixed codes (SYD53/SYD27/SY3/SY6/SY7/SY9 all live) — flagged to Royce in the PR body as the likely-intended tradeoff, not something to silently harden further without him seeing it in use.

## Notes
- The root-cause diagnosis (lazy-load timing race) is inferred from reading the code, not directly reproduced live — this sandbox can't boot the app far enough to watch the dropdown actually fail and self-heal. Said so plainly in the PR body and reflection log rather than stating it as directly observed fact; the fix itself doesn't depend on the diagnosis being exactly right, since it removes the timing dependency either way.
- Rebased onto 2 releases (v3.5.628, and PR #861's CRLF-checker fix) that landed on `main` between the prior close and this session starting — picked up cleanly, no conflicts with either.
- Local `npx serve` dev-server port 4415 was already in use by a concurrent chat's own instance of this same repo — moved this session's `.claude/launch.json` (gitignored, local-only) to port 4419 rather than contend for the same port.

---

# Session 2026-09-01 — Local pre-push guard for cache-buster drift (eq-field PR #857, merged)

## Built
- `.githooks/pre-push` added to eq-field — mirrors CI's `cache-buster-drift.yml` locally: fetches `origin/main` then runs `check-cache-busters.mjs --base origin/main` before every push, blocking on drift instead of costing a push → CI-fail → fix → push round trip. Escape hatch `EQ_SKIP_CACHE_BUSTER_CHECK=1 git push`, matching the existing `EQ_SKIP_BRIEF`/`EQ_SKIP_REFLECT` convention.
- Two short CLAUDE.md notes added (Versioning section, Deploy-flow step 0) pointing at the hook and its one-time activation command.
- Activated `git config core.hooksPath .githooks` for the whole local eq-field clone — confirmed live it's stored in the shared common `.git/config`, so one command from any single worktree activated it for the shared root and every other worktree of the same clone immediately, no per-worktree repeat needed.
- Verified three ways before relying on it (clean-state pass, forced-drift block via a temp uncommitted edit, the skip-env-var bypass), then confirmed it fires through real git plumbing on the actual `git push` of this branch, not just direct shell invocation.
- eq-field [PR #857](https://github.com/eq-solutions/eq-field/pull/857) opened, CI green (tests+lint, deploy-preview — no user-visible surface to click through), squash-merged on Royce's explicit "merge it" — commit `084291ab`. Worktree removed post-merge.
- Closed `task_7499b5e4` (`eq/pending/eq-field.md`, was line 1055) — the tracked item that spawned this session, after twice hitting the same drift mid-session on 2026-09-01 (PRs #854, #856).

## Decided
- Royce: "merge it" — explicit merge authorization for PR #857.

## Deferred
- None new from this session's own work — shipped end to end, nothing left open.

## Notes
- The task's framing assumed eq-field already had `.githooks`/`guard.js` to "mirror" — false. Verified `git config core.hooksPath` blank at every scope (system/global/local/worktree) and zero tracked `guard.js`/`hooksPath` hits repo-wide before building anything. eq-field's actual brief-gate/reflection-gate are global, per-machine Claude Code `PreToolUse`/`Stop` hooks (`~/.claude/settings.json` → `~/.claude/hooks/guard.js`), not git-native and not something a fresh clone inherits. Worth checking before assuming this pattern for another repo.
- Empirically confirmed `core.hooksPath` lives in a repo's shared common `.git/config`, not per-worktree — setting it once from a new worktree was immediately visible from the shared root and an unrelated sibling worktree with no extra steps. Noting this here since this session's own gate output flags a `--worktree`/`--local` `core.hooksPath` disagreement on **eq-context itself** (F10) as an active latent-shadow risk — this finding is about eq-field specifically, not a fix for F10, but the underlying git mechanics may help whoever picks that up.
- eq-context was 57 commits behind `origin/main` at close time (up from 18 at session start) and `git merge` in the shared `C:\Projects\eq-context` checkout is hard-blocked by `hooks/pre_tool_use.py` (F9 guard, fired live this session). Did this close from an isolated `gh repo clone` per the guard's own printed instructions, not the shared checkout — flagging in case a future session hits the same block and wants a working example rather than re-deriving it.
- Same F9-guard / isolated-clone pattern as every other session closing today.

---

# Session 2026-09-01 — Addendum: future-dated sks-nsw-labour reconciliation, leave done, schedule scoped not executed

*Continuation of the SKS NSW Labour safety-records session above, same conversation. Simon Bramall (relayed via Royce) asked to reconcile future-dated entries too, after the historical prestart/toolbox-talk backfill closed.*

## Built
- Live-checked sks-nsw-labour's own project for tables carrying future dates before assuming scope — prestarts/toolbox talks are same-day logs, so the real candidates were `schedule` (roster) and `leave_requests`.
- Compared both against EQ Field's SKS tenant (`ehow`) for the same window: leave requests 21 vs 19, schedule ~121 person-week rows (11 weeks, Aug31-Nov23) vs 450 already-present `schedule_entries` (through Nov14). Asked to "scope properly" before writing anything, given the volumes were close enough to suggest real complexity, not a blank gap.
- Investigation found a prior one-time sync tagged `imported_from='nspb-phase3-2026-07-05'`: 29 `leave_requests` + 983 `schedule_entries` rows, covering out to late October. This explained why most of the "future" comparison already matched — it's literally the same imported rows, not independent double-entry in both systems. Reframed the real task from "diff two systems" to "find what nspb has added or changed since that snapshot" — a materially different and more precise question.
- Phase 1 (identity + site resolution) for the future roster window: 62 of 63 people resolved (every apparent gap was a legal-name-vs-preferred-name difference — Bruno Pedrosa/"Bruno Vita Pedrosa", Nabeel Hussain/"Mohammed Hussain", Jose Quintanilla/"Jose Luis Quintanilla Rodriguez", etc. — only Jhon Jairo Velasquez Meneses has no EQ Field account at all). Site codes: most resolved directly or via the DR->SYD11 alias from the earlier prestart work; 3 flagged as unmapped (AIRTRUNK-SYD1, DRT, ARNDELL) rather than guessed.
- Royce resolved the 3 flagged sites directly: AIRTRUNK-SYD1 -> confirmed live as `SYD1 - Airtrunnk` (Airtrunk SYD1, Schneider Electric) — a typo baked into the live abbr itself, left as-is, not mine to silently correct. ARNDELL turned out to be *today* (Cameron Tregoning, Sep 2), not a future date at all, so out of scope. DRT surfaced a real conflict: Royce said it should be SYD11, but a `field_site_projects` row (created literally the day before, 2026-08-31) already has DRT parented under SYD10 — both are genuine, separate Digital Realty sites. Flagged rather than picked; Royce confirmed SYD10 (yesterday's setup) was correct, no change made.
- Leave reconciliation: re-verified the "4 missing" and "3 mismatched" findings against each person's *full, unfiltered* leave history (not just the future-date-filtered view) before writing anything, per Royce's "should already be done but keen to make sure the info is there." Confirmed genuine: none of the 4 gaps existed under any status/archived flag; the 3 mismatches were independently, natively re-entered into EQ Field weeks after nspb's original (dated evidence: nspb's record predates the conflicting EQ Field entry in all 3 cases). Inserted the 4 missing rows (Wayne Rowe RDO Pending Aug3-Sep6/200hrs, Blake Reynolds RDO Approved Sep4, Jack Trusler annual Approved Sep4-7, Richard Brown RDO Pending Oct12) matching a real existing row's column conventions (approver_id/hours_requested/decided_at etc, not guessed) rather than inventing a shape. Corrected the 3 mismatches to nspb's earlier original (Brett Kilpatrick/Todd Wilson -> rdo, Maylin Ung -> unpaid). Verified all 7 live post-write. Future leave count: 19 -> 23.
- Wayne Rowe's new row has no `approver_id` — his nspb approver (Mark Brame) has no active EQ Field staff account, the same deliberate hold already on record from the 2026-08-26 account reconciliation. Not a new problem, surfaced again here rather than silently left unlinked.
- Roster/schedule reconciliation (the bigger remaining piece — ~500 future person-days, nspb's weekly-wide-cell format needs unpacking against EQ Field's per-day rows, plus the schedule table's own habit of recording leave a second way inside day-cells) was fully scoped but not executed. Royce: "I think that's everything that matters moving forward" once leave closed out — thread ends here unless picked up again later.

## Decided
- Royce, via `AskUserQuestion`: "scope properly" before any write on the roster/schedule piece — produced a written scope rather than proceeding straight to execution.
- Royce, via `AskUserQuestion`: start Phase 1 (identity + site resolution) now.
- Royce (free text, mid-Phase-1): resolved AIRTRUNK-SYD1 and DRT directly, asked when Arndell was scheduled rather than guessing.
- Royce, via `AskUserQuestion`: DRT stays under SYD10 (yesterday's setup was correct).
- Royce (free text): "keep going into leave reconciliation - it should already be done but keen to make sure the info is tehre" — read as "verify, don't just trust," which surfaced the prior phase3-sync context above.
- Royce, via `AskUserQuestion` (2 questions, one call): insert the 4 confirmed leave gaps; correct the 3 mismatches to nspb's earlier original.
- Royce (free text): "I think thats everything that matters moving forward" — closed the thread without going into roster/schedule.

## Deferred
- Roster/schedule reconciliation itself — fully scoped (see Built above and `eq/pending/eq-field.md`), not executed. Pick up only if asked again.
- Wayne Rowe's leave request has no approver_id (Mark Brame has no EQ Field account) and spans an unusually long 25 weekdays/200 hours, still Pending in both systems — flagged, not mine to resolve.
- Jhon Jairo Velasquez Meneses — the one person in the future roster window with no matching EQ Field staff account at all.

## Notes
- This addendum + the earlier close's substrate writes were both made from the same isolated `git clone` in scratch, fast-forwarded before each write (12-57 commits behind `origin/main` at various points, all unrelated same-day concurrent-session churn) — the shared checkout's `git pull` correctly hard-blocked by the F9 guard hook each time it was tried directly.
- The `nspb-phase3-2026-07-05` discovery is a good example of why "looks like a gap" needs a `created_at`/`imported_from` check before trusting a same-shape row count as evidence of independent double-entry versus a shared prior sync.

---

# Session 2026-09-01 — Supervisor's default quote visibility narrowed to own-only, merged, live

*Direct continuation of 2026-08-31's quote-visibility arc (quotes.view_all promotion, write-ownership scoping).*

## Built
- `/decide` pass run on "narrow Supervisor's default quotes.view_all now?" — full six-step output (frame, steelman, pre-mortem, value, feasibility, the call), given this touches a live production default even though the change itself is small and reversible. Recommendation: ship it. Royce confirmed.
- eq-roles [PR #33](https://github.com/eq-solutions/eq-roles/pull/33) (v2.7.7): `quotes.view_all` dropped from Supervisor's default grant, manager-only now. `npm test` 110/110 (incl. `higher-rank roles are strict subsets` — removing a grant from Supervisor can only help that invariant). Merged, tagged `v2.7.7`.
- eq-shell [PR #1704](https://github.com/eq-solutions/eq-shell/pull/1704): pure pin bump — `src/modules/quotes/permissions.ts` was already a pure re-export from the earlier promotion, zero source changes needed. `pnpm run build` + full test suite (470/472, 2 pre-existing skips) + `check-perm-sync.mjs` all clean. CI green (typecheck/test/lint, schema drift, migration hygiene, gitleaks, deploy preview). Merged (squash `5847e2a4`) on Royce's explicit "merge".

## Decided
- Royce: "yes" to the `/decide` recommendation — narrow Supervisor's default now, don't hold.
- Royce: "merge" — explicit approval for PR #1704 (auth-surface change + this repo's production-deploy-on-merge).

## Deferred
- **Not click-tested live** — no Shell session/credentials in this environment. Worth a real pass: sign in as a Supervisor without an individual grant, confirm EQ Ops now shows only their own quotes; confirm Access Control's Ops section no longer shows "See all quotes" checked by default for Supervisor.

## Notes
- **Deploy publish not independently reconfirmed at session close.** Merge confirmed (`5847e2a4`), and the Netlify deploy record for that exact commit was confirmed created and `state: "building"` within 1 second of the merge — matches this repo's documented 2-4s trigger pattern. A follow-up `listSiteDeploys` call to confirm `published_at` was made but hung (backgrounded, produced no output after several minutes, no clear cause — possibly an `npx` cold-start or a pipe-buffering artifact from an earlier `head -c` truncation on the same command). Not chased further per the standing rule against polling backgrounded calls. Accurate status as of this close: merged and building, not confirmed published. Next session (or Royce) should verify `published_at` is set for `5847e2a4` before treating it as live.
- Closes out the full same-day arc that started with "what guard rails do we have around who can see quotes in EQ Ops" (2026-08-31): module gate confirmed enforced, row-level view/edit scope built and click-tested live by Royce himself, write-side ownership scoping closed, and the one remaining access concern (Supervisor's over-broad default) scoped, decided, and shipped — all four pieces traced back to a single conversation, each with its own live verification rather than assumed correctness.

---

# Session 2026-09-01 — customer Field/Service status computed from owned sites, PR #1700 merged

## Built
- crm-customers.ts (list + detail) and get-data-activation-status.ts: customer field_enabled/service_enabled now computed as "any owned, active site has it on" instead of read from the independently-stored customers column.
- update-data-activation.ts: a table:'customers' write now cascades to every owned site instead of writing the now-unread stored column.
- CustomersPage.tsx / AdminDataActivationPage.tsx: customer-level toggle relabeled to reflect the cascade, disables (with tooltip) when the customer owns zero sites; "Apply to all sites" removed as redundant.
- eq-shell PR #1700 opened, then merged (squash bb9f501e) after Royce's go-ahead.

## Decided
- Royce: "make it computed" — customer-level Field/Service status should be computed from owned sites, not an independent flag.
- Royce (via AskUserQuestion): compute it everywhere (Customers page + App activation admin page), and repurpose the admin page's per-customer toggle into a cascade rather than leaving it as a dead write.
- Royce: "commit this and open a PR."
- Royce: "merge it once click-tested" → "go" — explicit merge authorization given without an actual live click-test having happened, in lieu of one.

## Deferred
- Not click-tested live — no Shell session/credentials in this environment; Vite/netlify dev unreliable under this machine's Node 24 (existing memory). Real pass needed: pill reflects sites, cascade toggle, zero-sites disabled state, same on the App activation page.
- PR #1700 merged but not yet confirmed published — at last check it was queued behind another concurrent deploy (commit 5847e2a4 building ahead of it). Needs a follow-up check that published_at/state:"ready" is actually set for bb9f501e before calling it live.
- Dropping the now-unused stored customers.field_enabled/service_enabled columns — explicitly out of scope, a separate bigger call.

## Notes
- Live-verified the bug's real impact via direct SQL on ehow + zaap before building: ehow had 8 customers that would gain a Field tick (4 Service, 1 would lose one); zaap had 30 that would gain Field and 20 that would lose Service. Confirms this wasn't a cosmetic nit — it was actively wrong for a real number of customers on both tenants.
- Cross-repo precedent found: eq-service already independently fixed the identical problem for service_enabled via its own 0178_service_customers_site_driven.sql (service.customers view, site-driven). Good corroboration that "compute from sites" is the right model, not a novel risk.
- Real recurrence of the eq-shell worktree Edit-tool/Bash filesystem desync (3rd distinct worktree now, this one field-tick-visibility-cf2bc5) — a first tsc/test run silently validated stale pre-edit files and reported false-positive clean. Caught by a direct grep -c for a distinctive added string coming back 0 across all 5 edited files. Fixed via the documented Bash-reconstruction (head/sed/tail splice) workaround. Found a genuine NEW failure sub-mode while doing it: a duplicated line at a splice seam broke a method-chain (sb.from('customers') typed twice) — caught immediately by tsc (TS1005) — plus a cosmetic double-blank-line variant at two different seams that tsc/eslint/tests did NOT catch, only found via a deliberate awk sweep for adjacent duplicate non-blank lines afterward. Both memory notes (schedulewakeup-misuse-as-generic-pause, worktree-tool-filesystem-desync) updated with today's specifics.
- Also mis-used ScheduleWakeup once this session as a generic "wait for pnpm install" mechanism (already a known standing mistake in memory) — logged as a cross-session recurrence rather than repeated silently.
- eq-context's shared checkout was 57 commits behind origin/main at /close time; git pull there was correctly blocked by the F9 guard hook (shared-checkout contention) — did this close via an isolated git clone into scratch instead, per the guard's own prescribed fix and the existing eq-context-shared-checkout-contention memory note.
- The commit queued ahead of this session's own deploy (`5847e2a4`, seen as "building" while checking PR #1700's live status) turns out to be the neighbouring session's own PR #1704 (Supervisor quote-visibility narrowing) — confirmed by cross-referencing its session-close entry directly above this one in this same file. Unrelated to this session's change; just a same-window queueing coincidence.

---

# Session 2026-09-01 — Weekly Roster site-filter lazy-load bug fixed and shipped (v3.5.630)

## Built
- Fixed the Weekly Roster page's `#roster-site` dropdown sticking on "All Sites" — `renderRoster()` (scripts/roster.js) now self-heals it once roster.js is actually loaded, instead of relying on a boot-time populate call that loses the lazy-load race. eq-field [PR #864](https://github.com/eq-solutions/eq-field/pull/864), v3.5.630, merged + confirmed live via `field.eq.solutions/sw.js`.
- Closes `task_028d9925`, spawned from v3.5.629's own reflection-log entry (which fixed the identical bug on Edit Roster's `#editor-site` but flagged, rather than fixed, this follow-up).

## Decided
- Chose a self-heal fix over v3.5.629's fold-into-search approach: `#roster-site` is the older, established pattern Editor's filter was modeled after, with 3 read consumers vs. Editor's 1 — dropping it would be a bigger UX change than removing something that had never worked since it shipped hours earlier. Engineering judgment call, confirmed by Royce's "merge it" on the finished PR.

## Deferred
- None — task was fully investigated, fixed, verified, and shipped within this session.

## Notes
- Real gotcha for future sessions: the Browser pane's `preview_start({name: ...})` only reads `.claude/launch.json` at the session's *primary* working directory (e.g. `C:\Projects\eq-field`), not a worktree's own copy — a worktree needing its own preview server has to add an entry to the shared root's launch.json (gitignored, low collision risk) rather than relying on a worktree-local file, which is silently ignored with no error.
- Reconfirmed the `eq` tenant's Core-only sign-in gate applies identically on Netlify deploy previews as on production — the deploy-preview PIN screen (which shows the demo access codes on screen) still redirects to "Sign in through Core" on submit. No standalone click-through path exists in this environment for either tenant; verification relied on a live-browser fetch+eval harness against the real shipped source instead (see PR body / eq-field's own reflection-log.md for the full trace).
- eq-context substrate was 51→62 commits behind `origin/main` for this entire session (grew as the session progressed, other sessions closing concurrently). Read as directed by the brief/close protocols but not reconciled in the shared checkout — a plain `git pull`/direct commit there is hook-blocked (stale-main-gate / F9); closed via an isolated worktree instead, matching the guard's own prescribed fix (and the same workaround the neighbouring session logged just above this entry).

---

# Session 2026-09-01 — tenant_role_overrides role-default bypass fixed, merged, confirmed live (13 app_data objects)

## Built
- Verified live (not assumed from the task brief) that 10 `app_data` functions on ehow shared one bug: `role_default OR (extra_perms ? key AND NOT denied_perms ? key)` — a caller who already qualifies via the bare role-default branch never reaches the deny-check, so denying a Field-domain key for a manager/supervisor via Shell's Access Control silently did nothing. Of the 6 named RLS policies, only 3 (`schedule_entries_insert`/`update`/`delete`) shared it — 2 (`labour_hire_companies_select`/`labour_hire_rates_select`) already had the correct shape from an unexplained source, 1 (`staff_conversations_creator_and_perm`) has no role-default branch to bypass.
- Wrote `20260901_field_permission_denials_role_bypass_fix.sql`, restructuring all 13 affected objects to `(role_default OR extra_perms ? key) AND NOT denied_perms ? key`. eq-field [PR #859](https://github.com/eq-solutions/eq-field/pull/859), merged (one CI fix along the way: the migration hadn't cited `IDENTITY-MODEL.md` §3.3.3 for the 7 shared-registry functions).
- Merged with the migration deliberately un-applied, held for a separate go. Re-checked later the same day and found all 13 objects already live with the fixed shape — applied by someone with no follow-up PR recording it. Corrected the migration header + reflection-log via eq-field [PR #865](https://github.com/eq-solutions/eq-field/pull/865), and functionally verified the live fix by evaluating `schedule_entries_insert`'s actual `with_check` clause against simulated JWT claims (a denied manager now correctly blocks; unaffected cases still allow).
- Flagged 2 out-of-scope findings as background tasks: a CRLF false-positive in eq-field's day-old pre-push cache-buster hook (fixed, eq-field [PR #861](https://github.com/eq-solutions/eq-field/pull/861), merged, confirmed working on a later push) and eq-shell's CLAUDE.md under-describing `eq_enforce_function_privacy`'s real scope/behaviour on ehow (still running as of this close).
- Updated Claude's own memory (`tenant-role-overrides-denials-were-inert.md`, corrected twice as the live-apply status kept changing within the same session) and this repo's substrate (`eq/pending/eq-field.md`, `eq/pending-archive.md`, `eq/changelog/eq-field.md`).

## Decided
- Royce: draft the fix + PR, hold live-apply for a separate explicit go (his standing instruction on this exact migration family) — overtaken by events when it turned out someone applied it live within hours anyway, with zero behavioural effect since no `tenant_role_overrides` row targets any of the affected keys.
- Royce: merge #865 (docs-only correction) — done.

## Deferred
- eq-shell's CLAUDE.md correction for `eq_enforce_function_privacy` — background task still running, not yet landed.
- Origin of the 2 already-correct `labour_hire_*` policies — unexplained, not blocking.
- "Hand-applied migration needs a PR" has now slipped 3 times in this one feature area (20260831's migration, #859, and its own paper-trail gap) with no process/CI enforcement — worth a real gate eventually, not built this session.

## Notes
- `shell_control.tenant_role_overrides` (jvkn) checked twice — before writing the fix and again at close — zero rows target any of the 7 affected keys either time. This whole arc has had no live behavioural effect on any real SKS user.
- Same F9 shared-checkout gotcha as the entry directly above: direct commit in `C:\Projects\eq-context` is hook-blocked, not just `pull` — used an isolated worktree (`git worktree add`, not a fresh clone) to close this session instead.

---

# Session 2026-09-01 — eq-solves-service: P2 backlog built and shipped (PR #821); ~14hr wait on 2 spawned follow-ups ended by closing rather than continuing to hold

*Direct continuation of the 2026-08-31 4-axis review + P0/P1 sprint (see that date's own close entry). Picked up here once both spawned follow-ups from that close (StatusBadge doc fix, `get_distinct_asset_types` tenant-scope) had reported back merged.*

## Built
- Ran `/decide` on "what's next" post-sprint; Royce chose to start the P2 tier of the review's own backlog (5 items, all P2 — no urgency, all reversible, none auth-touching) over stopping clean.
- Branched fresh off current `origin/main` (`service-p2-polish-20260901`) rather than continuing on the already-squash-merged P0/P1 branch — avoids the "branch still contains the old commits" trap this file's own history already documents elsewhere.
- Hit a brief-gate hook mid-task (`eq-guard blocked: brief-gate — brief not run this session`) on the first migration `Write` — ran `/brief eq-solves-service` properly rather than routing around it; Royce confirmed with "go."
- Discovered mid-session that `AGENTS.md` had gained a new `--reconcile` mode on `migrate-service.mjs` (PR #820, merged while this session was running) — read it before writing the search_path migration and shipped that one via the normal merge→governed-dispatch path instead of a manual MCP apply, per the new guidance.
- Built all 5 P2 items — 1 direct (search_path pin, migration `0239`, live definition pulled fresh via `pg_get_functiondef` before writing it) + 4 parallel background agents:
  - 5 dashboard gradients removed (brand no-gradient rule), collapsed to `bg-eq-ice`.
  - Shared `useFocusTrap` hook extracted from `Modal`/`SlidePanel`'s duplicated ~50-line focus/Escape/scroll-lock logic. `ExportButton`/`CsvExportButton` consolidation investigated and correctly declined — real behavioural difference found (`CsvExportButton` auto-disables at zero rows, `ExportButton` has no `disabled` prop; forcing the merge would silently break 2 call sites into downloading empty files).
  - `revalidateMaintenanceSurfaces()` narrowed from a `/sites`-layout-wide bust on every call (incl. the app's hottest write, `updateCheckItemAction`) to an options object defaulting to the narrowest useful scope — verified against all 19 real sites' actual page contents before narrowing any of them, not assumed.
  - Data Quality's aggregate-only counts replaced with real per-row, click-through worklists. Building it surfaced and fixed two live bugs in the underlying checks (verified against ehow, not assumed): the orphan check counted only dangling FK references, never NULLs (production showed 0/0 while 3 assets and 92 sites genuinely have no site/customer); the contacts-orphan check read a legacy column bypassed by migration 0167's canonical cutover (11 real unlinked contacts vs. a stale count of 1).
- 5 clean commits, pushed, eq-service [PR #821](https://github.com/eq-solutions/eq-service/pull/821) opened. Merged (18:34 UTC) — outside this session's own visible turns, most likely Royce directly via GitHub. **Correction, from the session directly below: not Royce — a separate Claude Code session merged it (squash, `938040f3`) after CI went green, following Royce's "merge it" in that session's own chat.**
- Two follow-up chips spawned by the P2 agents themselves during the build (`task_51c64ed0` — adopt `useFocusTrap` in `ConfirmDialog.tsx`'s own third near-duplicate; `task_3b858742` — the `/defects` revalidation gap). Royce started both independently. Waited across ~22 `ScheduleWakeup` cycles (~14 hours) for either to report back in this conversation — neither did. Told to close rather than keep waiting; checked both directly against live state for this close instead of leaving them as an open question: `task_3b858742` is done — shipped as eq-service [PR #822](https://github.com/eq-solutions/eq-service/pull/822) (see the dedicated entry earlier in this file), merged and live, its own close already fully updated `eq/pending/eq-solves-service.md`. `task_51c64ed0` shows no trace anywhere (no PR, no pending.md mention) — genuinely still open, not just unreported.

## Decided
- Royce, via `/decide` + `AskUserQuestion`: build the P2 tier now.
- Royce, via the `/brief` skill: confirmed with "go" before any P2 edits landed.
- Royce: "wait for the other two" — held for ~14 hours of fallback-only cycles rather than proceeding on anything else, until an explicit `/close` superseded it.

## Deferred
- **`task_51c64ed0`** (adopt `useFocusTrap` in `ConfirmDialog.tsx`) — still genuinely open as of this close, not just slow to report. Whoever picks it up next: `useFocusTrap` (`lib/hooks/useFocusTrap.ts`, from PR #821) doesn't yet support focusing a specific element on open (`ConfirmDialog` wants Cancel/Confirm, not "first focusable") — the hook needs a small API extension, not just a drop-in call. **CORRECTED — see the session directly below: this was in fact done, independently, in parallel with this session's own wait.** A separate Claude Code session (unaware of `task_51c64ed0`'s existence — it was given the same request directly by Royce in chat, not via the task's own notification path) added the exact `initialFocusRef` extension described here, wired `ConfirmDialog`, and pushed it onto this same `service-p2-polish-20260901` branch — it rode into PR #821 with everything else in this entry and is live in the same `938040f3` merge. Not still open. Left this bullet's original text in place rather than deleting it — it's an accurate account of what this session believed at close, and it's the reason two sessions did the same task without colliding on file content: they worked on it during genuinely non-overlapping windows.
- Unrelated older items in this file's top section (shell-sso retirement, 2 advisor findings to relay to Field/Shell, a CLAUDE.md doc-typo, the aging dependabot PRs) — all predate this continuation and weren't touched.

## Notes
- **Real gap in this session's own harness-notification coverage.** `task_51c64ed0`/`task_3b858742` were introduced with an explicit "You will be notified here when it ends" — for `task_3b858742` that notification never arrived in this conversation despite the task genuinely completing and shipping hours earlier (confirmed only by reading this file fresh at close time). Spawned background tasks running in separate local Cowork sessions don't carry the same delivery guarantee as in-process subagents (the same session's own P0/P1 and P2 agents all notified reliably) — worth treating "no notification yet" as weaker evidence of "not done" for this specific class of task than for an in-process one, and checking live/substrate state directly rather than continuing to wait on faith alone once a wait has run unusually long.
- `eq-context`'s shared checkout was correctly hard-blocked by the F9 guard on a direct `git pull`/`git rebase` attempt at both this session's original `/close` (2026-08-31) and this continuation's — recovered via a fresh isolated `git clone` both times, same pattern as every other session in this file today. Two separate push races on the 2026-08-31 close (divergent-append conflicts on `sessions/2026-08-31.md`) resolved by keeping both sides.
- Confirmed the local-vs-UTC date split independently (same discrepancy several other sessions in this file already flagged today): `date`/branch-naming resolved 2026-09-01 well before `digest.md`/hook messages caught up to it.

---

# Session 2026-09-01 — ConfirmDialog folded into useFocusTrap; PR #821 + #822 merged, deployed, and reconciled with each other

## Built
- `lib/hooks/useFocusTrap.ts`: added optional `initialFocusRef?: RefObject<HTMLElement | null>` param (backward-compatible — Modal/SlidePanel's 2-arg calls untouched).
- `components/ui/ConfirmDialog.tsx`: `ConfirmDialogModal` now uses the hook (`destructive ? cancelBtnRef : confirmBtnRef`) instead of its own ~60-line copy of focus-trap/Escape/scroll-lock. Public API unchanged.
- Both changes made inside `.claude/worktrees/service-review-sprint-e8ef31` (branch `service-p2-polish-20260901`) — the extraction existed there only as uncommitted WIP, not on main, mixed with ~9 unrelated files from a concurrent session. Committed `e4cf74e`, touching only the 2 files this task needed.
- Resolved a real merge conflict between PR #821 (this work, plus `revalidateMaintenanceSurfaces()` narrowing) and PR #822 (`revalidatePath('/defects')` fix) in `app/(app)/maintenance/actions.ts` — both touch `updateCheckItemAction`/`updateCheckItemResultAction`. Kept both changes, corrected #821's own comment that had called the `/defects` gap "accepted"/"self-heals". Merge commit `c083827`.
- PR #821 merged (squash, `938040f3`) after CI green; PR #822 merged by Royce directly via GitHub (`7bdcdb36`) before I could action his "merge" instruction — both confirmed live via Netlify deploy record (`published_at` set) within ~2 min of merge.
- Ran the `merge-readiness` subagent on #821 before merging — clean except the then-pending CI check; it also surfaced the #822 conflict risk ahead of time.

## Decided
- Royce: finish the ConfirmDialog work in the existing worktree rather than rebuild fresh on main (informed choice — worktree turned out to be actively in-use by another session, confirmed live moments later when it committed+pushed mid-conversation).
- Royce: merge #821, then (separately) #822, once each was confirmed CI-green and conflict-free.

## Deferred
- [ ] No live click-through of ConfirmDialog's focus/Escape/Tab behavior, the Data Quality worklists, or the dashboard changes — the worktree has no `.env.local`. See pending.md's PR #821 entry.

## Notes
- **New shared-checkout data point** (extends `feedback_shared_eq_shell_checkout_branch_off_main` in project memory): a *genuine* worktree (own `.git`, confirmed via distinct inode + independent branch/HEAD/log) can still be actively contended by a concurrent session — this isn't limited to the fake-no-`.git` worktree pattern already tracked in `worktree-registry.md`. Folder name (`service-review-sprint-e8ef31`) didn't match its branch (`service-p2-polish-20260901`) or the `-wt` convention either — don't infer isolation or activity from naming.
- **eq-context F9 guard hit directly this close**: `git merge` on the shared `C:\Projects\eq-context` checkout is hard-blocked (pre_tool_use.py, rung 4) — worked around per the guard's own suggested remedy, a fresh isolated clone (`git clone` to scratchpad), not `git worktree add` (which another session used earlier the same day for the same guard). Root checkout's own uncommitted WIP (`eq/pending-archive.md`, `eq/pending/eq-field.md`, another session's) was never touched.
- Local eq-context clone drifted from 14 to 69 commits behind `origin/main` over the course of this single session (multiple SessionStart:resume hooks). Never reconciled the root checkout itself, only worked around it via the isolated clone — whoever next works from the root checkout there should expect it to still be stale.
- PR #822's pending.md/changelog entries already existed (written by someone/something else) before this close ran — corrected their "landed in the same fast-forward" claim rather than leaving it inconsistent next to this session's own, more accurate #821 entry.

---

# Session 2026-09-01 — Roster staff-name map fixed (PR #855/#858/#867); a wider silent bug found along the way

## Built
- Root-caused Royce's "roster disappeared after refresh" report from his own attached browser console log: `_jwtPhysical()` (`scripts/supabase.js`) routes a non-manager's (supervisors included) `people` GET to `field_people_directory`, which renames `employment_type` to `"group"` — the staff-name-map fetch still asked for `employment_type`, 400ing the whole request and collapsing every person to "(unknown)" tenant-wide. Live-broken since v3.5.615 (2026-08-30). Fixed, tested (4 new cases), live-verified via JWT-simulated SQL against ehow. eq-field PR #855, v3.5.625 (renumbered twice — two live version collisions with concurrently-merging PRs #854/#856, full rebase+reverify each time).
- Spawned a follow-up task to check a "mirror-image" bug #855 flagged but didn't fix. It found something more important: eq-shell had separately given `field_people`/`field_people_removed` a real `group` column (2026-08-19, untracked in this repo), leaving `employment_type` a hardcoded `NULL` stub — so #855's fix, covering only the non-manager branch, left every **manager's** group classification silently blank for 13 days, unnoticed because it failed silently instead of 400ing. Fixed by widening the translation to the whole people family. eq-field PR #858, v3.5.626 — merged and live before I saw the result.
- Ran `/decide` on the newly-confirmed schema-drift gap; wrote a documentation-only migration (no DDL) recording eq-shell's untracked change, independently re-verified live before writing it. eq-field PR #867, merged.
- Confirmed the fix is genuinely live in production by fetching the deployed `scripts/supabase.js` from `field.eq.solutions` directly and matching it against the shipped `_jwtPhysical` logic — not just trusting the merge event.

## Decided
- Royce: "merge" (PR #855), "merge" (PR #867) — both explicit, both confirmed live before reporting done.
- Royce: "check the roster live" — attempted; hit the Core sign-in gate with no credentials to go further, same standing environment limitation as most PRs tonight. Pointed Royce at his own already-open Core tab as the fastest real check.

## Deferred
- [ ] Live click-through as a real SKS manager and a real SKS worker/supervisor, confirming names and group labels render correctly in the actual UI — no Core/SKS credentials in this environment, for either half of the fix.
- [ ] The structural gap PR #867 documents one instance of: eq-shell's and eq-field's migration pipelines still share no ledger, so eq-shell can silently reshape an `app_data` object eq-field depends on with nothing here recording it. Second confirmed instance after `field_people_iud()`.

## Notes
- **Own mistake, caught and assessed before it compounded**: an early `git rebase` targeted the shared `C:\Projects\eq-field` root instead of an isolated worktree (copy-paste error out of the `/brief` skill template's `C:\Projects$ARGUMENTS` pattern — lost the path separator, landed on the wrong directory). It silently rebased whatever branch happened to be checked out there. Checked before touching anything further: that branch (`claude/csv-import-preserve-existing-fields`) was already fully merged upstream weeks earlier, its remote already deleted, zero unique commits — nothing lost, but it was still the wrong directory to run a mutating command in. Left it as-is (pre-rebase SHA `0e644fc7` still recoverable from its own reflog) rather than risk a second mistake fixing the first.
- **eq-context F9 guard hit directly this close too**: `git pull` on the shared `C:\Projects\eq-context` checkout is hard-blocked (`pre_tool_use.py`, rung 4) — same guard, same isolated-clone workaround (`git clone` to scratchpad) another session independently used earlier the same day for the same reason (see the entry above this one). Also caught a live race directly: `git status` on the shared checkout showed staged-but-uncommitted changes to the exact two files this close would touch (`eq/pending-archive.md`, `eq/pending/eq-field.md`) from a concurrent session's own in-flight close; by the time of a follow-up `git diff --cached` moments later they were gone (that session had committed). Never touched those files in the shared checkout — did all of Steps 2-4 in the isolated clone instead.
- This session's own local `C:\Projects\eq-context` checkout drifted to 76 commits behind `origin/main` over the course of the session (multiple SessionStart:resume hooks) — never reconciled the root checkout itself, only worked around it via the isolated clone, matching the pattern the entry above this one already flagged. Third session tonight (at least) to hit this exact shape.

---

# Session 2026-09-01 — eq-field: TAFE Holidays staleness alert built + PR opened (v3.5.631, PR #868, NOT merged); underlying save mechanism confirmed live

*Continuation of the same session/worktree lineage as the "Roster: today's rostering gap + Edit Roster name/site filter" and "Edit Roster site dropdown" entries earlier in this file — same branch history, next task in sequence.*

## Built
- Royce, exploratory: "is there a clever way to ensure tafe holidays update?" Read `scripts/tafe.js` before answering — `tafeHolidays` is a hand-typed `{start,end,label}` array in `app_config`, nothing prompts anyone before the next NSW TAFE term break needs entering. Presented 3 directions via `AskUserQuestion`: a staleness warning reusing the existing licence-expiry/roster-gap Dashboard alert pattern; auto-import from TAFE NSW's own calendar (flagged as unverified-feasible, not promised); leave manual. Royce picked the staleness warning.
- Built `getTafeHolidaysStaleness()`/`renderTafeHolidaysStaleAlert()` in `scripts/dashboard.js`, wired into `renderDashboard()` next to the existing `renderRosterGapsCard()`, new `#dashboard-tafe-holidays-stale` container in `index.html`. Manager-only; warns when the furthest configured holiday end-date is under 45 days out or nothing's configured; "Update" jumps to Edit Roster.
- Caught and fixed a real gap before shipping, not after: first draft would have shown the card permanently, unresolvably, at any site with zero apprentices on a nominated TAFE day. Added a guard (`hasTafeApprentice`, same predicate `applyTafeDayForWeek()` already uses) so it only ever renders where there's someone it could affect. Verified all 3 population states (no apprentices / apprentice with no day / apprentice with a day) resolve correctly via real function calls in a live browser tab.
- Hit and recorded a real test-harness gotcha: `fetch('/scripts/dashboard.js')` against the local static server kept returning a browser-cached pre-fix response even after a full server restart and `{cache:'no-store'}` — only a cache-busting query string (`?cb=${Date.now()}`) actually forced a fresh fetch. Not a bug in the shipped code (confirmed via direct `grep` on disk throughout) — a blind spot in this fixture-testing method, now flagged in the reflection log for future sessions.
- Used `node scripts/build-bundles.mjs --write` correctly this time to regenerate `core-bundle-b1.js` from its 3 source files, rather than hand-editing it — last session's hand-edit had only passed `--check` because it happened to match the tool's own output byte-for-byte; this session's first hand-edit attempt correctly failed `--check` with real drift, caught before it shipped wrong.
- eq-field [PR #868](https://github.com/eq-solutions/eq-field/pull/868) (v3.5.631) opened — CI green (Tests+lint, both drift checks, deploy preview all passed). **NOT merged.** Royce never gave a merge instruction this session; instead asked a direct follow-up question (below). By the time of this close, `main` had moved again and the PR shows `mergeable: CONFLICTING` — needs a rebase before it can land. Left as-is rather than rebasing unprompted during a close.
- Royce asked directly: "does the tafe holiday dates save properly?" Checked live on ehow rather than just re-reading the code: `app_config.tafe_holidays` holds 4 real, correctly-shaped ranges spanning to Jul 2027 (not a localStorage-only fallback); `pg_policies` confirmed real INSERT/UPDATE/DELETE policies on `app_config` gated on `eq_role IN ('manager','supervisor')` plus a tenant-scoped ALL policy — not just grants with nothing enforcing them. Also surfaced a side effect of that real data: the new staleness card won't fire for SKS for a long while, since the actual furthest configured date is far past the 45-day window — correct behaviour given the real data, not a sign the card is broken.

## Decided
- Royce, via `AskUserQuestion`: the staleness-warning approach over auto-import-from-TAFE-NSW or leave-manual.
- No merge decision was made or asked for this session — PR #868 is open, CI-green, and now conflicting; needs a rebase and an explicit "merge" before it ships.

## Deferred
- **PR #868 needs a rebase before it can merge** (`mergeable: CONFLICTING` as of this close, main having moved twice since push). _(added 2026-09-01)_
- **The 45-day warning threshold is a judgment call**, not backed by any verified NSW TAFE term-length data or anything Royce specified — said so plainly in the CHANGES banner and PR body. Royce's call whether it needs tuning once seen in practice. _(added 2026-09-01)_
- **Not click-tested live by a person** — no SKS/Core credentials in this environment. Worth a real pass once merged. _(added 2026-09-01)_

## Notes
- This session's live-DB check (RLS policies + real saved data) is a stronger answer to "does X work" than re-reading the client code alone would have been — the client code's happy path (PATCH-then-POST-fallback, promise-serialized saves) was already read and trusted in the prior close's reflection entry, but a broken RLS write policy would have made every save silently fall through to the `catch` block's `localStorage` fallback, which the UI's own success toast wouldn't have distinguished from a real server save. Worth defaulting to a live check for this class of question rather than trusting the code's own error handling to have surfaced a policy gap.
- Same F9-guard / isolated-clone pattern as every other session closing today — `eq-context`'s shared checkout was 76 commits behind `origin/main` at the start of this close.
- eq-field's own version-bump collision problem (documented in memory as a recurring failure) is very much still live under tonight's concurrency — 2 collisions during PR #855's own merge window alone, plus more visible in other sessions' changelog entries for #854/#856/#857 the same night.

---

# Session 2026-09-01 — eq-shell CLAUDE.md fix landed (deferred item from the earlier tenant_role_overrides close); eq-field migration header finalized via PR #866

*Picked up the "eq-shell's CLAUDE.md correction for `eq_enforce_function_privacy` — background task still running, not yet landed" item deferred by the earlier "tenant_role_overrides role-default bypass" close entry above. That entry and the eq-field changelog already cover PR #859/#865/the migration story thoroughly — not re-narrated here, only what's new.*

## Built
- eq-shell [PR #1702](https://github.com/eq-solutions/eq-shell/pull/1702) — the deferred item, now landed. Verified `eq_enforce_function_privacy`'s live body on jvkn/zaap/ehow directly before writing anything; corrected CLAUDE.md's stale scope claim (zaap/ehow guard `service`+`app_data` too, not just `public`) and stale behavior claim (auto-restores since 2026-08-25/08-30, not a blind revoke) — kept the eq-cards `#0111` incident as history, time-boxed correctly. Also fixed the identical stale claim duplicated in `scripts/check-function-grants.mjs`'s comments (enforcement logic untouched). Merged, confirmed live via Netlify deploy record.
- Checked PR #859 on request: found it CI-red on the `IDENTITY-MODEL.md` citation guard, fixed and merged.
- Applied `20260901_field_permission_denials_role_bypass_fix.sql` to live ehow on explicit go — re-ran the migration's own pre-apply gate immediately before (all 13 objects still matched the assumed OLD shape, zero drift), applied, then live-reverified all 13 now show the NEW shape and all 10 functions kept their exact pre-existing grants (the auto-restore trigger from PR #1702's own finding worked as documented, confirmed on a real production apply).
- eq-field PR #866 — corrected the migration's now-stale "NOT YET APPLIED" header. Hit a real rebase conflict against eq-field PR #865 (a second, independent session had found and fixed the identical stale header in parallel, with a genuinely valuable addition — a simulated-JWT-claims functional test on `schedule_entries_insert` I hadn't done). Synthesized one accurate header from both accounts rather than stacking two competing narratives; that synthesis is what's actually live on the file today.
- Hit two separate concurrent-edit races on `docs/reflection-log.md` (append-only log, other sessions landing entries at the same point) while rebasing both eq-field PRs — resolved by keeping both sides' entries each time, no content lost.

## Decided
- Royce: merge PR #1702 ("go").
- Royce: apply the migration + merge PR #859 ("go" / "merge").
- Royce: retry the header-edit via an alternate tool after Claude Code's own auto-mode classifier blocked the direct Edit call twice ("retry it") — succeeded via Bash/Python instead; the classifier block was almost certainly generic (a `cd &&`-chained git command tripped it separately, unrelated to file content), not a considered objection to this specific edit.
- Royce: merge PR #866 and confirm the deploy ("merge and deploy when safe").

## Deferred
- Spawned `task_5edd53d8` — eq-field's own CLAUDE.md has the same stale `eq_enforce_function_privacy` description that eq-shell's had; flagged in both PR #859 and #866's bodies but never fixed. Not done this session (out of scope of what was asked); left as a chip rather than a silent gap.

## Notes
- **Found the same orphaned shared-checkout WIP this file's own entries above already flagged** (`eq/pending-archive.md` + `eq/pending/eq-field.md`, uncommitted, staged against a base ~74 commits behind current `origin/main`) — stashed rather than committed or discarded (`git stash push -u`, message names it explicitly) since its provenance and completeness are unclear; NOT applied, NOT deleted. On inspection it's a large (~318-line) eq-shell pending→archive rotation batch dated "rotated 2026-08-31," unrelated in substance to this session's own eq-field.md changes — genuinely orphaned, not something this close could safely finish on its author's behalf. Needs a human or a dedicated session to review; not resolved here.
- Same F9 guard hit on this session's own close: `git merge`/`merge --ff-only` on the shared `C:\Projects\eq-context` checkout blocked outright (rung 4) — same isolated-clone workaround as every other session in this file today.
- Confirmed both eq-field production deploys live by commit-ancestry, not by trusting the merge event: PR #859's merge commit (`8ed0e7e5`) and PR #866's (`fe43daba`) both checked against `listSiteDeploys`' newest `state:"ready"` production row for `commit_ref` match, not assumed from "merge succeeded."

---

# Session 2026-09-01 — Two trial accounts (Don Milmlow, Jordan A. Sample) permanently deleted; delete-user.ts's employment-record-link gap found

## Built
- Diagnosed why `delete-user.ts` refused to hard-delete "Don Milmlow": correctly archived, zero hits on the 5 `BLOCKING_TABLES`, but blocked by an active `app_data.staff` row on the SKS tenant plus its linked Cards `public.workers` row — the endpoint checks for this link but has no way to clear it.
- Swept the "sks" tenant's most-recently-created `shell_control.users` rows for other likely trial/placeholder accounts; flagged "Jordan A. Sample" (`contact@eq.solutions`) as a second candidate, confirmed by Royce.
- Mapped the full FK dependency graph for both blockers via `information_schema` (not guessed): `app_data.staff` on ehow has 20 possible referrer tables, `public.workers` on jvkn has 7.
- Manually purged both accounts end-to-end, in FK-safe order, replicating what `delete-user.ts`/`edit-user.ts` do internally since no endpoint exists for the staff/worker half:
  - **Don Milmlow**: deleted the (dependency-free) SKS staff row, its Cards worker row + 1 pending invite, wrote `user.hard_deleted` to `shell_control.audit_log`, deleted the `auth.users` identity (cascades), deleted the `shell_control.users` row.
  - **Jordan A. Sample**: still `active=true` — archived first (`active=false` + `user.deactivated` audit row + `eq_revoke_auth_sessions` RPC, which cleared 3 live sessions / 59 refresh tokens), then deleted 1 `leave_requests` + 2 `licences` rows hanging off the staff record, the staff row itself, the Cards worker row + 1 pending invite, then the same hard-delete sequence as above.
  - Verified zero remaining rows across `shell_control.users`, `auth.users`, `public.workers`, and `app_data.staff` for both ids before reporting done.
- Flagged the real gap — `delete-user.ts` reports the staff/worker block but can't clear it — as a follow-up task (`task_377c3404`, "Add employment-record purge to delete-user.ts"); Royce started it in a separate session, running independently.
- Wrote Claude memory note `delete-user-blocked-by-staff-worker-link` with the full FK graph and the manual-deletion recipe so the next trial-account request doesn't re-derive it from scratch.

## Decided
- Royce, via AskUserQuestion: purge the linked staff/worker records (not just unlink) to finish Don Milmlow's delete — the recommended, full-purge option.
- Royce: same cleanup for Jordan A. Sample, confirmed as a second trial/placeholder account.

## Deferred
- The actual product gap (no endpoint to clear a staff/worker link before hard-delete) is unfixed — only the two specific accounts were cleared by hand. Tracked as `task_377c3404`, running in a separate session as of this close, not yet reported back.
- Any other accounts Royce meant by "a few trial accounts" beyond these two were not identified — only a recency sweep of the "sks" tenant was done, not a full audit.

## Notes
- No product code files changed this session — entirely live-data diagnosis + cleanup via direct Supabase MCP access (service-role equivalent), plus a memory write and a spawned task. No changelog entry (close protocol Step 4 is conditional on a product file changing, and none did).
- `git pull`/`git merge --ff-only` blocked on the shared `C:\Projects\eq-context` checkout again (F9, rung 4) during this close — same isolated-clone workaround used by every other session in this file today.

---

# Session 2026-09-01 — eq_enforce_function_privacy CLAUDE.md task had a false premise; added a real gotcha entry instead (PR #869)

## Built
- Assigned task: find and correct an existing stale description of the `eq_enforce_function_privacy` event trigger in eq-field's CLAUDE.md, mirroring eq-shell's already-corrected text (eq-shell PR #1702).
- Verified before writing anything: grepped eq-field's CLAUDE.md on the checked-out branch and on `origin/main` — zero matches for `eq_enforce_function_privacy`/`function_privacy`/`ddl_command_end`. A repo-wide `git grep` on `origin/main` found the string only in `docs/reflection-log.md` and `supabase/migrations/20260901_field_permission_denials_role_bypass_fix.sql`, never in CLAUDE.md.
- Pulled PR #859's and #866's actual bodies via `gh pr view` rather than trusting the task's paraphrase — #859's own prose says only "eq-shell's CLAUDE.md ... A different repo's doc, not fixed here." Traced the false claim to an imprecise inline comment in #859's own migration file, which names both repos' CLAUDE.md when only eq-shell's needed fixing.
- Confirmed eq-shell PR #1702 is real, merged today, and does what the task claimed for eq-shell's file — but the local `C:\Projects\eq-shell` checkout was on a stale branch (`claude/entity-view-gate-crm-read-rpcs`, missing #1702), so had to pull eq-shell's actual `origin/main` CLAUDE.md to see the real corrected text rather than trust what was on disk.
- Independently verified the live trigger against ehow (`pg_get_functiondef`/`pg_event_trigger`, `ehowgjardagevnrluult`) rather than relying on either doc: guards `public`+`service`+`app_data`; since the 2026-08-30 hardening (migration `0295_enforce_function_privacy_smart_restore_app_data.sql`, eq-shell PR #1680) it diffs pre-DDL grants against fresh-function schema defaults and restores genuine divergence, not a blind revoke. Matches eq-shell's corrected description exactly.
- Confirmed eq-field has no CI equivalent to eq-shell's `check-function-grants.mjs` (`git ls-tree` over `scripts/`, `tests/`, `.github/workflows/` on `origin/main` — nothing grant/privacy-named).
- Added a new gotcha bullet to eq-field's CLAUDE.md (Schema gotchas section, right after the existing shared-fn-coordination entry) instead of "correcting" nonexistent text — scoped to ehow, matching this file's existing convention for the two closely-related gotchas already there. Paired `docs/reflection-log.md` entry (reflection-gate requirement). Built in an isolated worktree (`eq-field-fn-privacy-doc-wt`, branch `claude/fn-privacy-doc`) off `origin/main`, per this repo's isolation convention.
- eq-field [PR #869](https://github.com/eq-solutions/eq-field/pull/869) opened. CI/deploy-preview checks pending as of this close — not merged.
- Wrote Claude memory note `eq-enforce-function-privacy-doc` with the live trigger facts and the false-premise provenance, generalizing the existing `verify-live-before-shared-fn-rewrite` lesson to cross-repo doc claims, not just function bodies.

## Decided
- None — task ran under Auto Mode with no back-and-forth; PR left open rather than merged, since it diverges from the literal task ask and deserves a look.

## Deferred
- eq-field PR #869 not merged — no instruction to merge, and CI was still pending at close. _(added 2026-09-01)_
- zaap's copy of the trigger not verified — out of scope; eq-field's existing function/view gotchas are ehow-only by established convention and the task only asked for ehow. _(added 2026-09-01)_

## Notes
- `git pull` blocked on the shared `C:\Projects\eq-context` checkout during this close (F9, rung 4) — same isolated-clone workaround used elsewhere in this file today.

---

# Session 2026-09-01 — eq-shell: EQ Ops mobile "no way back to home" fixed, then a real state-desync bug found live-testing the fix and closed too

*Royce: "in EQ OPS there is no way to get back to homepage from archive jobs view."*

## Built
- Root-caused: EQ Ops' job-detail pane renders full-screen on mobile, above the persistent "Apps ←" home bar — opening any job (including from Archive) covered the only way back to home. Added a Home button into the detail pane's own header, mobile-only, reusing the existing `homeHref` prop/style already used elsewhere in the file. eq-shell PR #1705, squash-merged `cc3f7f64`, confirmed live via `published_at`.
- Live-tested the fix on the real production site via Claude in Chrome (Royce's own authenticated session; simulated a mobile viewport since window-resize doesn't reliably narrow this environment's actual page viewport) — found the button changed the URL but the panel never actually closed.
- Root-caused the second bug: `InternalLinkInterceptor` (an app-wide handler that routes same-origin `<a>` clicks through the client router so vendored-package links get SPA-speed nav) intercepts the Home link's click and calls `navigate()`, but `QuotesModule`'s `view`/`detailId` are local React state seeded once from `window.location.search` on mount — a same-route, query-only navigation updates the URL without the mounted component ever re-reading it. Confirmed the identical bug already existed on the pre-existing desktop Home button too (not something this session's new button introduced, just newly exercised by it).
- Fixed both Home buttons to reset `detailId`/`detail`/`view` directly in `onClick` instead of relying on the interceptor's navigation. eq-shell PR #1707 — hit a real merge conflict against `origin/main` (this branch's own prior commit vs. its squash-merged equivalent already on main, diverging just enough for a trivial adjacent-insertion conflict); resolved by keeping this branch's content, re-verified with a real `git merge --no-commit` trial rather than trusting `git merge-tree`'s simulation, which under-reported it as clean. Squash-merged `10614804`, confirmed live via `published_at`.
- Re-verified live on BOTH mobile and desktop after the second deploy — direct DOM inspection (title, URL, presence/absence of the detail pane and archived table, presence of the pipeline board), not just a screenshot: the panel genuinely closes and lands on the clean Ops board on both surfaces.

## Decided
- Royce: "commit this."
- Royce: "merge" (twice — once per PR), understood per eq-shell's own merge-is-deploy CLAUDE.md convention as also authorizing the resulting production deploy.
- Royce: "check archive jobs on mobile now" / "check archive jobs on desktop too" — explicit live-verification requests that surfaced the second bug and confirmed the final fix.

## Deferred
- None — this arc is fully closed: found, fixed, merged, deployed, and live-verified on both mobile and desktop within the same session.

## Notes
- Two Claude-in-Chrome tooling quirks worth remembering next time, neither an eq-shell product bug: `resize_window` doesn't reliably narrow this environment's actual page viewport (real mobile emulation needed `Object.defineProperty(window, 'innerWidth', ...)` plus a dispatched `resize` event instead); and the screenshot's pixel space doesn't match the real page's CSS pixel space here, so coordinate-based clicks can silently land on the wrong element even when the screenshot looks correct — a direct DOM `.click()` proved the more reliable verification method.
- Also caught and corrected a recurring own-mistake this session: called `ScheduleWakeup` (twice) to wait on a backgrounded Bash task instead of just letting the task-notification arrive — documented as a 7th+ recurrence in the `schedulewakeup-misuse-as-generic-pause` Claude memory note.
- This is a small, concrete instance of the failure class this substrate already tracks (F14-shaped): a hand-written claim about current state (here, a migration-file inline comment) was imprecise, got treated as fact by a downstream task, and would have propagated further if not verified against the actual file before acting.

---

# Session 2026-09-01 — eq-field: safety forms "Pull from roster" date-resolution bug fixed, merged, live (v3.5.632, PR #870)

## Built
- Royce reported directly in chat: "pulling from roster for the safety forms (prestarts, toolboxes etc) doesnt work - it says something about could not resolve from todays date." Traced the exact toast string ("Could not resolve roster day from date") to 4 call sites: `toolbox.js`'s `addToolboxAttendeesFromRoster`, `diary.js`'s `addDiaryAttendanceFromRoster`, and `site-reports.js`'s `addPrestartCrewFromRoster`/`addPrestartCrewFromApproved`.
- Root cause: all 4 gate on `getWeekForDate()`, defined only in `leave.js` (grep-confirmed single definition site). `lazy-loader.js`'s TAB_SCRIPTS buckets for `site-reports`/`prestart`/`toolbox`/`diary`/`safety` never load `leave.js` — a session opening a safety form without having visited Leave first (or before Dashboard's own background `_kickLeave` finishes) hits the `typeof getWeekForDate === 'function'` guard as false, and gets a toast blaming the date when the real gap is a missing script. Same latent-dependency class this file has hit and fixed repeatedly (tafe.js/jobnumbers.js/teams.js gaps, `openLeaveRequestSafe`, `_leaveAction`) — just never caught for this call site before.
- Fixed all 4 call sites: each now awaits `EQ_LAZY.loadTabScripts('leave')` before computing the week/day key if `getWeekForDate` isn't yet defined, self-healing instead of failing.
- Verified: `node --check` on all 4 files; full unit-test loop (`tests/*.test.js`) green; `npx eslint@9 scripts tests netlify` — 0 errors; `node scripts/build-bundles.mjs --check` clean (none of the 4 files are bundle sources); `node scripts/check-cache-busters.mjs` caught a real miss on first run (`app-state.js`'s own `?v=` tag hadn't moved), fixed, re-ran clean.
- Live browser verification (own worktree, then the real deploy preview): reproduced the exact bug precondition (`getWeekForDate` genuinely undefined after loading only the `toolbox`/`prestart` tab buckets) and confirmed the real `addToolboxAttendeesFromRoster()`/`addPrestartCrewFromRoster()` functions self-heal and correctly pull a fake roster row from that starting state. `diary.js`'s and `addPrestartCrewFromApproved`'s fixes verified by lint+syntax+identical-pattern review only, not independently browser-tested — disclosed as such rather than overclaimed.
- Hit a local test-harness gotcha: `.claude/launch.json`'s `eq-field` preview config pointed at the shared root checkout (`C:/Projects/eq-field`), not this worktree — another concurrent session had that root on an unrelated branch, so the preview server initially served stale content with zero error. Caught by checking the served `APP_VERSION` against what was just written; fixed locally (untracked, not part of the diff) by pointing the preview at the worktree on a separate port.
- Version bumped v3.5.631 → v3.5.632 (`app-state.js`, `sw.js`, `index.html` changelog banner + script-tag cache-buster). Committed with a paired `docs/reflection-log.md` entry (reflection-gate). eq-field [PR #870](https://github.com/eq-solutions/eq-field/pull/870) — CI green, deploy preview smoke-tested: fetched the real served `toolbox.js`/`diary.js`/`site-reports.js`/`app-state.js` from the deploy-preview CDN and confirmed they carry the exact fix, not just a passing build.
- Squash-merged (`8126888a`), branch deleted. Confirmed live: `curl https://field.eq.solutions/sw.js` shows `v3.5.632`.

## Decided
- Royce: "go ahead" (on the session brief).
- Royce: "merge" — explicit production-deploy authorization, per this repo's own Netlify-auto-deploy-on-push convention.

## Deferred
- **Not click-tested through the full authenticated UI by a person.** This sandbox has no real Core session/credentials — the deploy preview's own login redirects to "Sign in through Core" for any code path, a pre-existing gate unrelated to this fix. Worth a real pass: open Prestart or Toolbox fresh (no prior Leave tab visit that session) and confirm "Pull from today's roster" now works on the first try. _(added 2026-09-01)_
- **Minor UX tradeoff, not addressed:** the self-heal adds a real network round-trip (fetching `leave.js`) to the first "Pull from roster" tap each session — better than the prior silent failure, but a supervisor on slow site wifi/4G will feel a brief delay that wasn't there before. No loading-state UI was added for this specific await, matching the existing (also-silent) `_ensureWeeksLoaded` await two lines below it. Royce's call whether it's worth a spinner.

## Notes
- `getWeekForDate`/`_ensureWeeksLoaded` remain duplicated per-file (`_toolboxDayKey`/`_prestartDayKey`/`_diaryDayKey` are 3 separate copies) — deliberately not consolidated, matching this file's existing per-module helper convention rather than introducing a new shared abstraction for a bug fix.
- This is the same latent-lazy-dependency failure shape this repo's own `lazy-loader.js` comments already document extensively (tafe.js, jobnumbers.js, teams.js, `openLeaveRequestSafe`, `_leaveAction`, the Dashboard birthday-widget gap). Spawned as a background task (`task_9a1a710d`) rather than left as a dangling note: a systematic sweep of every `typeof X === 'function'`-guarded (or unguarded) cross-module call against TAB_SCRIPTS, to find any remaining instances before the next one surfaces as a user-reported bug.

---

# Session 2026-09-01 — eq_enforce_function_privacy: CI gate built, a real GitHub Actions anomaly diagnosed and fixed, both PRs merged

## Built
- Checked PR #869's CI (from the prior entry this same session) — all green (Tests+lint, both drift checks, deploy preview).
- Asked to "work on the rule you've identified": built `tests/migration-function-grant-guard.test.js`, eq-field's first CI gate for `eq_enforce_function_privacy` grant loss. Ported eq-shell's `check-function-grants.mjs` logic (walk migration history, require a same-file GRANT/REVOKE when a `CREATE OR REPLACE FUNCTION` replaces a function that had a broader `authenticated`/`anon` grant) rather than its file verbatim — eq-shell diffs "newly-added files" against a git base ref, which needs `fetch-depth`/`BASE` plumbing this repo's `ci.yml` doesn't have. Followed this repo's own established convention instead (`migration-security-invoker-guard.test.js`, `migration-shared-fn-guard.test.js`): scan every file unconditionally, grandfather what predates the check. Picked up automatically by `ci.yml`'s existing `tests/*.test.js` loop.
- Ran the check ungrandfathered against all 69 real migration files before writing any exemption — found 2 genuine pre-existing violations: `20260831_field_permission_denials_enforce.sql` and PR #859's own `20260901_field_permission_denials_role_bypass_fix.sql`, both dropping the `authenticated` grant on 3 functions with no trailing GRANT. Not live incidents — PR #859's and #866's own bodies already confirmed live via `has_function_privilege` that the auto-restore trigger preserved the grants correctly both times. Grandfathered both rather than retroactively editing an already-applied migration file, which would make the file claim a statement that never actually ran against ehow.
- Opened eq-field PR #871, stacked on PR #869's branch (`claude/fn-privacy-doc`) since it edits a CLAUDE.md line #869 itself introduces.
- Diagnosed a real GitHub Actions anomaly on PR #871: the "Tests + lint" check never triggered — confirmed at the Checks API level (only a `netlify` check-suite existed for the commit, no `github-actions` one), not just UI lag. A fresh empty commit (forcing a `synchronize` event) didn't fix it. Asked Royce before trying the next remedy (closing/reopening the PR is a state-changing action); confirmed via `AskUserQuestion`. Close+reopen (GitHub's own event log confirmed both registered) *also* didn't fix it — ruled out both standard remedies before escalating further.
- Royce: "merge #869 first." Rebased `claude/fn-privacy-doc` onto the current `main` (v3.5.631/632 had merged since branching) — real append-only conflict in `docs/reflection-log.md` against those two sessions' own entries, resolved by keeping all three (their two plus this session's), in order. Re-ran the full 33-file test suite + eslint + cache-buster check clean, force-pushed, waited for fresh CI (via `Monitor`, not polling) — green — then squash-merged on the explicit instruction.
- Rebased PR #871 onto the newly-updated `main` — this both kept it current and tested the stacked-branch hypothesis for the CI anomaly. A second, simpler reflection-log.md conflict (the incoming side was fully redundant, already absorbed into `main` via #869's squash) resolved by keeping `main`'s side entirely. Force-pushed — a real `github-actions` check-suite appeared and went green within a minute, confirming the stacked-branch structure was the actual cause.
- Reported back and asked before merging #871 too (a separate PR from what was explicitly authorized). Royce: "merge" — squash-merged.
- Cleaned up both worktrees (`eq-field-fn-privacy-doc-wt`, `eq-field-fn-grant-guard-wt`) and their local branches after confirming both were clean (no uncommitted changes) and both PRs were merged.

## Decided
- Royce, via `AskUserQuestion`: close and reopen PR #871 to try to force GitHub to re-request a check-suite (didn't end up fixing it — de-stacking did — but was the right next step to try before that).
- Royce: "merge #869 first" — explicit merge authorization for #869, specifically ordered ahead of #871.
- Royce: "merge" — explicit merge authorization for #871, asked for separately since it wasn't covered by the first instruction.
- Royce: "clean up the worktrees" — explicit instruction, scoped (by context) to this session's own two worktrees, not the several other concurrent sessions' worktrees also present in this repo.

## Deferred
- None new this session — the one item carried from the prior entry (PR #869 not merged) is now resolved; both PRs are merged.

## Notes
- **Root cause of the GitHub Actions anomaly, as far as it could be diagnosed from the CLI**: a PR whose branch is stacked on another open PR's branch (rather than branched directly off `main`) can silently fail to get a `github-actions` check-suite created at all — not delayed, not queued, genuinely never requested, confirmed via the Checks API (`GET /commits/{sha}/check-suites`) showing only the `netlify` app's suite. Neither a fresh `synchronize` commit nor a close/reopen fixed it; rebasing directly onto `main` (removing the stacked-branch relationship) fixed it immediately. Worth checking for this specifically before attempting another stacked PR in this repo — the workaround (wait for the base PR to merge, then rebase) is straightforward once recognized, but the symptom looks identical to "CI is just slow" until you check the Checks API directly.
- `main`'s branch protection is disabled on this repo (`gh api repos/.../branches/main/protection` → 404) — nothing technically blocks merging a PR whose checks never ran. Didn't rely on that to merge #871 without a green check; waited for the fix and a real pass instead.
- `git pull` blocked on the shared `C:\Projects\eq-context` checkout again (F9, rung 4) during this close — same isolated-clone workaround used throughout this file today. The clone was still present from the prior close in this same session; `git pull --ff-only` inside it (not the shared checkout) was sufficient to catch it up.

---

# Session 2026-09-01 — eq-cards polish sprint close-out (continues 2026-08-31's session): a coverage overclaim caught and corrected, plus a scheduling-tool gap found

*Direct continuation of this same session's earlier "rounds 2 & 3" entry above (this is the same session, closing out). Nothing new built here — this pass is: confirming the two spawned background chips' outcomes, catching a factual error in what was reported as fixed, and answering Royce's own "what should I do" and "schedule these" questions.*

## Built
- Nothing new — this is a verification and correction pass, not new code.

## Decided
- Ran `/decide` at Royce's request ("im a bit lost here - what should i do") on the implicit decision "what's the next right-sized action right now." Conclusion: nothing further needed from him — check the two spawned background chips (zero effort on his part) and otherwise stop, since nothing in the session is actually time-sensitive and `TODAY.md` GOALS is unset (stated plainly rather than inventing urgency).

## Deferred
- **Correction, caught at close: `worker_house` was never given test coverage, despite round 3's own scorecard claiming "every feature folder now has real coverage, zero exceptions."** Verified directly: `test/features/worker_house/` doesn't exist; it's the only `lib/features/*` folder without a matching `test/features/*` one. Root cause: this session's own round-3 dispatch only assigned agents to 4 of the 5 remaining untested folders (alerts, platform, consent, legal) — worker_house was miscounted and simply never assigned. Corrected in `eq/pending/eq-cards.md` and in the scorecard artifact itself (dimension score for test coverage dropped 9→8 to reflect this honestly) rather than left standing as a false "done."
- The 2 human-only items (real usability testing with tradies, live click-throughs of the admin error-retry state + PWA update banner) — Royce asked to have these "scheduled." No tool available to this session can schedule a future personal reminder to a human: the `schedule` skill only creates cloud *coding* agent routines (useless for "watch a tradie" or "click through my own phone"), and `PushNotification` only fires immediately, not on a delay. Gave the full instructions directly in chat instead, and told Royce plainly that his own phone's reminder app is the right tool for an actual timed nudge. Worth knowing for next time this pattern comes up — there is currently no way to schedule a future notification to the user from within a session.

## Notes
- Both spawned chips from earlier in this session (`task_22f29e79` EqButton contrast, `task_824a07b7` licence_edit dropdown tokens) are fully resolved — each already ran its own proper `/close` (session log + pending.md entries already present above/nearby in this same file) and each spun off a further chip of its own (`task_236abfe6` licence_edit_screen test coverage, now also closed via PR #338). Net result across both threads: 5 merged PRs (#335-339), all confirmed deployed live by their own sessions via the correct Netlify `state:ready`/`published_at` method, not just green CI.
- Repeated the `ScheduleWakeup`-for-background-wait mistake a second time this session (a no-op `{stop:true}` call with nothing actually pending) despite the existing memory on this exact failure mode — memory note strengthened same-session to name the recurrence explicitly and reframe the fix as behavioral ("call no tool at all while waiting," not "remember not to call this one tool").
- Hit the squash-merge-branch-contains-trap three separate times this session (after PR #332, #333, and #334 each merged-and-deleted-branch) — recovered the same way each time (fresh branch off `origin/main`, `git stash`/cherry-pick the real diff across). Worth a standing habit rather than a per-incident recovery: always re-branch fresh immediately after any squash-merge, before starting the next round's edits, not after hitting the trap.

---

# Session 2026-09-01 — eq-shell: purge_employment_records flag ships — delete-user.ts's staff/worker link block now has a real resolution path (PR #1708, merged + live)

*Picks up `task_377c3404`, spawned by the earlier-today trial-account-deletion session as its own follow-up (this file's first entry today, "two trial accounts hard-deleted... purge-endpoint gap flagged") — the item that session's `eq/pending/eq-shell.md` write-up left open.*

## Built
- Ran `/brief eq-shell` per the Session Gate before writing anything. Found the current branch (`claude/entity-view-gate-crm-read-rpcs`, PR #1691 already merged) had unrelated uncommitted CRM/data-activation WIP sitting on top from a different task — left it completely untouched and built in a fresh isolated worktree off `origin/main` instead, to avoid any risk of disturbing it.
- Re-verified the FK graph live (`pg_constraint`) against BOTH tenant planes rather than trusting yesterday's memory note's ehow-only list: found 3 more real referrers on ehow the memory missed (`feedback_entries`/`quarterly_reviews`/`skills_ratings`, all `.person_id`) and confirmed zaap genuinely diverges (no `team_members`/`team_supervisors`/`quarterly_reviews`; has a `tender_nominations.staff_id` ehow doesn't) — same recurring "the two planes genuinely diverge, don't reuse one list for both" shape as other sessions in this file today.
- Ran `/decide` on the one real design fork: should the new purge flag delete every FK-referencing row (matching the prior session's manual process exactly), or only rows unambiguously owned by the target, leaving attribution references (who raised an asset defect, who supervised someone else's shift) as a reported block? Recommended the conservative split — the irreversible-risk asymmetry is one-sided (aggressive's failure mode is silent permanent loss of a customer's compliance history; conservative's failure mode is falling back to today's already-accepted manual path). Royce: "Go ahead with the split."
- Built `netlify/functions/_shared/staff-employment-purge.ts` (new) — deliberately no hardcoded "attribution" table list: the final `staff` row delete either succeeds or surfaces Postgres's own FK-violation message naming what's left, self-maintaining against schema drift instead of a second list that goes stale. Plus 5 unit tests against a fake Supabase client. Threaded `purge_employment_records: true` through `delete-user.ts`'s existing per-membership staff-link loop (additive only — default/omitted-flag behaviour is byte-identical to before); added a `staff.employment_record_purged` audit_log event per attempt. `AdminEditUser.tsx` surfaces the option only once a plain delete has actually 409'd on the staff link.
- Verified via `pnpm run build` (tsc -b + vite, twice), `eslint` (clean on all 4 authored files — 2 pre-existing lint errors elsewhere in `AdminEditUser.tsx` confirmed via an `origin/main` diff to be unrelated pre-existing debt, and this repo's CI treats lint as advisory), full test suite (477 tests, 0 failures). Not click-tested live — no Shell session in this environment.
- eq-shell [PR #1708](https://github.com/eq-solutions/eq-shell/pull/1708), CI fully green per a `merge-readiness` audit — which also flagged, correctly, one worth-surfacing design nuance: if the linked Cards `workers` row fails to delete after the `staff` row is already gone, the function still reports `purged: true`. Not a regression — `delete-user.ts` never checked `public.workers` as a blocker even before this change, and `check-dangling-staff-pointers.ts` already independently monitors for exactly that residual shape — but worth a human seeing before merge, not just a code comment. Royce: "merge" — squash-merged `d5503203`, confirmed genuinely published (not just merged) via the Netlify deploy record for that exact commit (`published_at` 05:02:23Z, `deploy_time: 357s`), not the merge event alone.

## Decided
- Royce, via `/decide`: the conservative owned/attribution split over full-cascade for `purge_employment_records`.
- Royce: "Commit and open a PR."
- Royce: "merge" (after an explicit CI-status check via the `merge-readiness` agent).

## Deferred
- **Not click-tested live** — no Shell session/credentials in this environment. Worth a real pass: archive a test account with a staff link, hit delete, confirm the 409 names the link, tick the new checkbox, confirm it clears and the account deletes.
- **The `public.workers`-survives-after-`staff`-purge edge case** (see Built, above) is a considered, documented tradeoff, not a bug — flagged here in case Royce wants it tightened later (e.g. re-blocking the whole delete if the worker-side cleanup fails, rather than just logging it).
- **Any other trial accounts Royce meant by "a few"** — carried over unchanged from the originating session's own deferred item; not investigated further this session.

## Notes
- This session made, then immediately caught and corrected, a recurring self-mistake: called `ScheduleWakeup` to wait on a backgrounded Netlify-deploy-status check — that tool is `/loop`-only, never a wait-for-background-task mechanism. Consolidated the `schedulewakeup-misuse-as-generic-pause` Claude memory file (8th documented occurrence across 8+ sessions) from a growing wall of near-duplicate per-incident paragraphs into one compact rule + occurrence log, since repeating the same explanation clearly wasn't fixing the reflex.
- `eq-context`'s shared checkout correctly hard-blocked a plain `git pull` (F9 guard) at both the mid-session `/brief` step and this close — recovered via a fresh isolated `git clone` in scratch each time, per the guard's own suggested remediation and the same pattern nearly every other session in this file hit today.
- Left the root eq-shell checkout's unrelated uncommitted CRM/data-activation WIP (branch `claude/entity-view-gate-crm-read-rpcs`) exactly as found — not this session's to touch, still sitting there as of this close.

---

# Session 2026-09-01 — Chat-gateway "can't write" claim re-verified across all three tiers; root cause pinned to GitHub's own connector, not a fixable org grant

*Triggered by new same-day evidence: a Claude Code session's GitHub MCP tool calls 403'd identically to what Chat's connector has always seen, while `git push`/`gh pr create` succeeded seconds later via a separate credential — raising the question of whether `eq/`, `ops/`, `sks/chat-gateway.md`'s identical Hard Rule #3 ("known, open Anthropic bug, not a settings problem") was actually just a missing org-level permission grant, sitting stale since 2026-08-24 in three `read_priority: critical` files.*

## Built
- Checked `eq-solutions`'s installed GitHub Apps directly (`gh api orgs/eq-solutions/installations`, this session's `gh` auth carries `admin:org`): 5 installations — `netlify`, `claude-design-import`, `figma`, `cloudflare-workers-and-pages`, `grok-by-xai`. None is a general-purpose Anthropic/Claude connector; `claude-design-import` is read-only and serves a different feature (Claude Design's codebase-import, not the chat file-read/write connector). `gh api repos/eq-solutions/eq-context/installation` 401'd ("JWT could not be decoded") — that endpoint needs GitHub App JWT auth, not a user token, so it could never have answered this on its own.
- Read `anthropics/claude-ai-mcp#822` in full (open since 2026-08-10, labeled bug, never closed). The connector in question is `https://api.githubcopilot.com/mcp/` — GitHub's own hosted MCP server, not a per-org installable Anthropic App. The reporter reproduced the identical 403 on a **personal** repo they own outright with `can_push: true`, which rules out any org/account permission explanation; they also tried disconnect/reconnect and a full revoke-and-reauthorize on GitHub's own side, with no change, and the connector setup has no field to supply a workaround PAT.
- Found and read the issue #822 itself cross-references: `anthropics/claude-code#80874` (filed 2026-07-24 from a **Claude Code** session on macOS, closed 2026-08-10 by its own reporter, no fix comment) — nearly identical shape, survives a full disconnect/revoke/reauthorize cycle. One real difference from today's new evidence: there, `git push` itself also failed ("through the session's local git proxy"), whereas today `git push`/`gh` CLI succeeded because they ran on a separately-authenticated OAuth credential (this session's `gh auth status`: user Milmlow, scopes `admin:org, gist, repo, workflow`) that never routes through the connector. Reconciles cleanly — whether git push 403s depends on which credential path it's wired through, not on anything specific to this org or repo.
- Conclusion: the docs' claim holds, and is better-evidenced now than on 2026-08-24, not weaker. Updated `last_updated` to 2026-09-01 and rewrote Hard Rule #3 in `eq/`, `ops/`, `sks/chat-gateway.md` (identical core text added to all three: no Anthropic connector is installed org-wide so there's no grant to fix, the mechanism is GitHub's own MCP endpoint, and today's not-Chat-specific MCP-tool evidence). `ops/chat-gateway.md` also picked up the "don't tell Royce it's saved" trailer sentence `eq/` and `sks/` already had on this same rule — a plain inconsistency across the three siblings, fixed while already in the paragraph.
- Checked `system/failures.md`'s schema before deciding **not** to add an entry: it's a machine-read ratchet (`guard-ratchet.yml`) for failures that got past a safeguard and caused damage, keyed by a recurrence count that drives rung promotion. This claim didn't fail a safeguard — it was true on 2026-08-24 and is still true today — so an entry would misrepresent a re-confirmed-correct claim as a guard failure.
- Checked `ops/pending.md` for a duplicate or related open item: none for this specific bug. Confirmed SEC-57 (already tracked there, "GitHub App permissions, your call") is a different, unrelated finding from the same API response — `grok-by-xai`'s org-wide installation has too *much* access (a security risk needing Royce's tighten-or-accept call), the opposite shape of this issue (a connector with no write access at all, not fixable by any settings change). Kept the two separate in the doc rewrite rather than conflating them.

## Decided
- None requiring Royce's input this session — the task's own "claim re-confirmed" branch applied (not the "correct a wrong claim" branch), and strengthening an already-true Hard Rule plus bumping a `last_updated` stamp sits squarely inside "resolve silently in the diff" per the completion standard.

## Deferred
- None new from this task itself. In passing, noticed `ops/pending.md` still carries an older, undated-in-this-session "OAuth GitHub MCP connector" item describing a *consent-screen auto-login loop that blocks the org-picker* — a different symptom (can't even complete authorization) than the one re-verified today (authorizes and reads fine, only writes 403). Since both 2026-08-24's and today's evidence confirm the connector authorizes and reads cleanly, that older item likely describes an already-resolved predecessor bug, not the live one. Didn't edit someone else's tracked item on this level of confidence — flagged via `spawn_task` instead.

## Notes
- `eq-context`'s shared checkout was 2 commits behind `origin/main` (both routine nightly `[skip ci]` refreshes) at session start; `git pull` was correctly hard-blocked by the F9 guard hook on the first attempt — recovered via a fresh isolated `git clone` in scratch, same pattern as every other session in this file today.
- This task's brief explicitly suspected an F14-shaped failure (a hand-written claim quietly aging into a lie across three critical-priority files). Investigation found the opposite: the claim was right on 2026-08-24 and is still right on 2026-09-01. Worth stating plainly rather than forcing today's evidence into the shape it was expected to fit.

---

# Session 2026-09-01 — SKS NSW Labour: login-screen "moved to core.eq.solutions" notice shipped (v3.10.112, PR #79); live proof the 2026-08-14 EQ Field divergence is still active and now includes timesheets

## Built
- Investigated Royce's question on what's involved in redirecting the SKS website to core.eq.solutions. Read the live SKS↔EQ Field merge runbooks (`docs/merge/*.md` in this repo) rather than trusting them at face value — reality had moved past them (Part 1's Phase-1 blockers were logged 2026-06-04/06, ~87 days stale by the time this session read them).
- Live-verified via direct SQL on both `nspbmirochztcjijmcrx` (sks-labour) and `ehowgjardagevnrluult` (sks-canonical/ehow) that the divergence flagged in `sks/pending.md`'s 2026-08-14 "SKS → EQ Field roster CSV sync" entry (317 rows entered natively, one-way, as of 2026-08-05) is still live and has grown. Joined 12 real SKS workers across both DBs via `people.canonical_id = workers.id = field_people.worker_id` (100% match rate — the identity bridge itself is sound). Found concrete, named, dated proof of ongoing one-way drift: Aiden Crowley's entire week was rewritten in EQ Field on 2026-09-01 03:39 (marked sick Monday, moved Wed–Fri to a different site) while sks-labour still showed the prior day's version; Richard Brooks' Wednesday assignment was changed in EQ Field on 2026-08-31 21:48, never synced back. Also checked timesheets (out of scope of the 2026-08-14 investigation): EQ Field's timesheet capture for the same real people is thin (5–11 rows/person over ~2 months) against sks-labour's dense daily capture (779 rows total, 46 in the last 7 days) — EQ Field is not yet a working timesheet replacement.
- Built and shipped, on Royce's explicit spec + "commit and deploy it": a tenant-scoped (SKS only) login-gate notice on sks-nsw-labour.netlify.app stating plainly the app is no longer being updated, linking to `core.eq.solutions/sks/field`, with click-to-call (0432 944 014) / click-to-email (royce.milmlow@sks.com.au) fallback. Same mechanism as the existing `gateDisclaimer` (`TENANT_BRANDING.sks` in `scripts/app-state.js`), new `gateMoveNoticeHtml` field rendered via `innerHTML` (the existing one is text-only). Verified live in the browser preview on both `?tenant=sks` (notice renders, all 3 links' actual `href`s confirmed correct) and `?tenant=eq` (no notice, correctly scoped), no console errors.
- Version bumped 3.10.111 → 3.10.112 across all 4 required files (`scripts/app-state.js`, `sw.js`, `index.html`, `CHANGELOG.md`) per the standing SKS version-bump convention.
- sks-nsw-labour [PR #79](https://github.com/eq-solutions/sks-nsw-labour/pull/79), squash-merged `4c7dc70`. Confirmed genuinely published, not just merged or triggered: matched Netlify deploy `6a96858f`'s `commit_ref` to the exact merge commit, `state: ready`, `published_at` set (`07:58:17Z`, 10s after merge — this repo has no build step, unlike eq-shell's multi-minute pipeline), `context: production`, clean secret scan. Re-loaded the live production URL directly and screenshotted it as final proof.

## Decided
- Royce, via `AskUserQuestion`: "Data divergence first" — investigate whether EQ Field's SKS entries duplicate or diverge from sks-labour's before anything else, over checking the auth-bridge state or just drafting the redirect unmerged. This is what produced the live proof above.
- Royce: keep the existing PIN/name login fully working — this is a nudge, not a cutover. Confirmed the link target `core.eq.solutions/sks/field` (not the Shell root) and supplied the real contact (phone + email) himself rather than a placeholder.
- Royce: "as long as its very obvious this website is no longer being updated" — drove the amber/warning treatment and the explicit "no longer being updated" wording, over the softer sky-blue "also try this" framing originally drafted.
- Royce: "Commit and deploy it" — explicit deploy authorization for a Netlify site with no separate approval gate documented for this repo (unlike eq-shell's auth-change gate); treated as covering both the PR merge and the resulting production deploy.

## Deferred
- **The EQ Field roster/timesheet divergence itself — not reconciled, not even fully scoped.** Added as a new dated bullet to `sks/pending.md`'s existing 2026-08-14 section rather than a new section (same topic, more current evidence). Flagged as now higher-stakes than when first logged: this session's own shipped notice actively sends people toward the diverging, thinner copy. Needs Royce's call on which system is authoritative before real traffic follows that link.
- **Phase-1 SSO/identity-bridge status** (`docs/merge/cards-field-sso-runbook.md` — Gate 0 salt parity, eq-shell A1 token mint, staff provisioning) — surfaced as relevant context but not re-verified live; that runbook is ~87 days stale by its own last-verified date. Not duplicated into pending.md since it's already tracked in-repo with its own Build-status table; flagged only that it needs a fresh live check, not a re-read of the doc, before anyone treats it as current.

## Notes
- `sks/pending.md`'s "SKS → EQ Field roster CSV sync" section still has open items and mixes checklist bullets with free-standing "Decided (Royce)" prose (site-record canonicalization rules) that reads as a standing rule, not narrative — left it untrimmed per the close skill's own escape hatch for exactly this shape, rather than guessing at how to cut it. It's also from a prior (2026-08-14), not this, session, and that day shipped no code (pure investigation), so there's no changelog entry underwriting a safe trim the way the skill's rule assumes.
- PR #79's own Netlify checks (`Header rules`/`Redirect rules`/`Pages changed`/deploy-preview) were still `pending` at merge time (`mergeStateStatus: UNSTABLE`, not `CLEAN`) — merged anyway since `mergeable: MERGEABLE` and none of those are required/blocking checks, and this is a plain static-file change with no build step to actually fail.
- `eq-context`'s shared checkout was 7+ commits behind `origin/main` by this close (matching the heavy same-day concurrent-session traffic every other entry in this file reports) and correctly hard-blocked a `git pull` (F9 guard) on the first attempt — recovered via a fresh isolated `git clone` in scratch, the same pattern essentially every session closing today has hit.

---

# Session 2026-09-01 — eq-shell: create-worker-invite.ts duplicate-invite bug fixed and shipped live (PR #1709)

*Picked up the finding an earlier same-day session (`originSessionId: d9b21aa1…`) root-caused but didn't build — memory `create-worker-invite-no-dedup-check.md`, `task_533933eb`.*

## Built
- Re-verified the incoming memory's claim against live code before building anything (this repo's own recurring "verify pasted reports against live code" lesson) — line numbers and behaviour in both `invite-user.ts` and `create-worker-invite.ts` checked out exactly as described: `invite-user.ts` looks up an existing unaccepted, unexpired `shell_control.user_invites` row by tenant+email before inserting; `create-worker-invite.ts` never did, so the two admin-invite paths (`+ Invite user` vs `Add workers`) could each create their own independent row for the same person (confirmed live 2026-09-01, Ian Marston/SKS — two rows, 70s apart, neither function aware of the other).
- The root checkout (`C:\Projects\eq-shell`) had unrelated uncommitted work already sitting in it (5 files, `claude/entity-view-gate-crm-read-rpcs` branch, a different in-flight task) plus ~30 other agents' active worktrees under `.worktrees/`/`.claude/worktrees/`. Isolated this fix from both: stashed only the 5 tracked files (named explicitly, not `-u`, to avoid sweeping the worktree directories into the stash), branched fresh off `origin/main` for the fix, then popped the stash back onto the original branch byte-identical once the fix was committed elsewhere — left every other agent's worktree untouched throughout.
- Fixed `create-worker-invite.ts`: added the same existing-invite lookup `invite-user.ts` has (tenant+email, `accepted_at IS NULL`, unexpired), refreshing in place instead of inserting a second row. Deliberately did NOT mirror `invite-user.ts`'s own refresh exactly (token/expiry only) — read `accept-invite.ts` directly and confirmed its identity-link + credential-promotion step (~line 477) only fires when `invite.worker_id` is set, so also overwrites role/entitlements/phone/has_field_access/worker_id on refresh; a narrower mirror would have silently dropped the reason this endpoint exists.
- Verified live DB schema before trusting the fix compiles against real columns: `shell_control.user_invites` on jvkn (`eq-canonical`) confirmed via Supabase MCP — every field used, correct types, `worker_id` FK to `public.workers`.
- Ran the repo's own Session Gate (`/brief`) mid-session — its flag-file hook fired on the first `Edit` call, as documented; produced the structured brief before proceeding rather than treating the incoming task's own detail as a substitute.
- `tsc -b --force` and `eslint` both clean. Not click-tested live — no Shell session/credentials in this environment.
- eq-shell [PR #1709](https://github.com/eq-solutions/eq-shell/pull/1709) opened.

## Decided
- Royce: "merge when safe" — treated as explicit sign-off for this auth-adjacent invite function (per this repo's CLAUDE.md, merging to `main` is itself the production-deploy trigger, so approval has to land before the merge click). Waited for CI to actually go green (5 required checks + Netlify deploy-preview) and `mergeStateStatus: CLEAN` before merging — branch protection here has no required PR review (`required_reviews: null`), so this was a plain merge, not a forced/admin override.
- Royce: explicit follow-up ask to confirm it's live on core.eq.solutions specifically, rather than accepting "merged" or "a build was triggered" as good enough — matches this repo's own documented trigger-vs-publish distinction.

## Deferred
- None new — fix is complete, merged, and confirmed live. `task_533933eb` can be closed.

## Notes
- Squash-merged `a3f8e0e5`; confirmed genuinely published (not inferred from the merge event or the deploy record merely existing) via the exact Netlify deploy for that commit: `state: ready`, `context: production`, `published_at` set (`08:02:09Z`, 285s build — 2s trigger latency matched the documented pattern exactly), `create-worker-invite` present in that deploy's function bundle, `url: https://core.eq.solutions`.
- Updated the originating memory file (`create-worker-invite-no-dedup-check.md`) and its `MEMORY.md` index line to reflect merged+live status.
- `eq-context`'s shared checkout had a *different* concurrent session's uncommitted edits sitting in exactly the 3 files this close needs to touch (`eq/pending/eq-shell.md`, `eq/changelog/eq-shell.md`, today's session log) at close time — did not touch that working tree at all; wrote this close from a fresh isolated `git clone` in scratch instead, same pattern as every other session in this file today. No change needed to `eq/pending/eq-shell.md` itself — this fix was never logged there as a standing open item (it was root-caused and fixed same-day, tracked only via memory + the spawn_task chip), so nothing to tick or archive.

---

# Session 2026-09-01 — ops/pending.md OAuth connector consent-loop item closed: superseded by write-403 tracking, confirmed live with Royce

*Picks up the loose end the earlier "Chat-gateway 'can't write' claim re-verified" session in this file explicitly left open: it noticed `ops/pending.md`'s 2026-07-03 "OAuth GitHub MCP connector" item describes a consent-screen loop blocking the org-picker — a different, more severe symptom (can't complete authorization at all) than the write-403 gap it had just re-confirmed — but declined to edit someone else's tracked item on doc-evidence alone and flagged it via `spawn_task` instead.*

## Built
- Read the full item plus both `chat-gateway.md` re-verifications (2026-08-24 original, 2026-09-01 re-check) it was checked against. The old item's own suggested fix — revoke the OAuth grant, sign out, reconnect — is documented elsewhere (`eq/pending-archive.md`'s 2026-08-24 entry) as already tried and confirmed NOT to fix the current write-403 issue, which is inconsistent with the old item still presenting that fix as live and actionable — one more sign it's describing an earlier, different bug.
- Two independent Claude-side technical checks already existed (both confirming the connector authorizes and reads correctly), but neither is literally "connects via the claude.ai UI with no loop" — asked Royce directly rather than closing on doc-evidence alone, per this task's own instruction. Royce confirmed: connects fine, no loop.
- Closed both bullets in `ops/pending.md`'s "Infrastructure — Live Blockers" section — the loop item, and its dependent "restart the held Chat session on the new bootstrap" follow-up (also moot: the connector-based Chat bootstrap it describes verifying is now in routine daily use per the actively-maintained `chat-gateway.md` files, `read_priority: critical`, last touched today) — then rotated the fully-closed section into `ops/pending-archive.md`.
- Used `scripts/rotate_pending.py`'s own `rotate_file()` directly, scoped to only the OPS trio (`ops/pending.md` + `ops/pending-archive.md` + `ops/verify-queue.md`), rather than running the unscoped script: a first dry-run showed the full script would also rotate unrelated, pre-existing done items out of `eq/pending/*.md` and `sks/pending.md` — outside this task's scope and risky against the same-day concurrent traffic every session in this file is hitting.
- Worked from an isolated fresh clone in scratch (the shared checkout's `git pull` is hard-blocked by the F9 guard hook, same as every other session today). Commit `7cca3c7d`, pushed after one fetch+rebase retry (non-fast-forward against a same-day automated `chore: nightly suite state refresh` commit — no real conflict). This session log entry itself needed a second reconciliation — a fast-forward, not a rebase — after `eq-shell: create-worker-invite.ts` (immediately above) landed its own append to this same file first.

## Decided
- Royce, via `AskUserQuestion`: the connector "connects fine, no loop" — settled the classification as fully superseded (not a narrower residual symptom, not a distinct bug coexisting with the write-403 gap).

## Deferred
- None. Both bullets fully closed; the still-live write-403 gap remains correctly tracked in `eq/`/`ops/`/`sks/chat-gateway.md` and `eq/pending/eq-context.md` — deliberately not duplicated into `ops/pending.md`.

## Notes
- This closes the loop the earlier chat-gateway session in this file left open on purpose rather than guess — the live confirmation it was missing is exactly what this session added before editing.

---

# Session 2026-09-01 — SKS Uniform Order Template applied from a Google Drive chat-gateway patch; the relay mechanism itself critiqued, a real GitHub-connector-scope gap found

## Built
- Found and applied a chat-gateway "substrate patch" Doc from Google Drive (`patch_sks_templates.md`, created 2026-09-01) — appended a new "SKS Uniform Order Template" section to `sks/templates.md`, sourced from Sharon Maroni's "Uniforms" email + the Stay Safe Wholesale catalogue. Verified live content against `origin/main` before writing (target section didn't exist yet, insertion point checked out). eq-context [PR #198](https://github.com/eq-solutions/eq-context/pull/198), squash-merged `e32f207`; re-verified live post-merge by re-fetching the file straight off `main`.
- Ran the repo's `/brief` gate before writing (blocked the first `Edit` attempt) — surfaced digest.md's Needs You list plus git state on both the shared checkout and this session's worktree, and flagged F9/F12 (concurrent-checkout git races) as directly relevant to the write about to happen. Royce confirmed after seeing that brief.
- Direct writes via the GitHub MCP connector (`create_or_update_file`, `create_branch`, `create_pull_request`) all 403'd (`Resource not accessible by integration`) against this repo. Local `git push` + `gh pr create`/`gh pr merge` succeeded immediately after, same repo, same session — used that path instead.
- Delivered a structured critique of the chat-gateway Drive-relay pattern on request, grounded in the actual governing docs (`eq/`, `ops/`, `sks/chat-gateway.md`, `rules/reflection-protocol.md`) and `system/failures.md`'s F1/F9/F10/F12/F14 — not opinion alone.

## Decided
- Royce, via `AskUserQuestion`: apply the patch, push to main (direct push blocked by the connector; landed via a PR + squash-merge instead, using local git/gh).
- Royce, via `/decide`: hold the "harden vs. retire the chat-gateway Drive-relay pattern" call until `task_d45603b4` (spawned this session) reports back on whether the connector's write-403 is a fixable permission scope or a genuine bug.
- Royce: "wait for task_d45603b4 to finish before deciding" — confirmed the hold.

## Deferred
- **`task_d45603b4`** (background, separate session, still running) — checking whether the GitHub MCP connector's write-403 on this repo is a fixable App permission scope. Spawned before `eq/pending/eq-context.md`'s existing 2026-08-24 entry on this exact symptom (Chat's connector) was found — that task doesn't yet know a ~2-hour prior investigation and an Anthropic-tracker citation already exist for at least the Chat-side version of this failure. Not corrected mid-flight: couldn't reliably identify which peer session (checked via `ListAgents`) is actually that task, so the correction went into `eq/pending/eq-context.md` instead of a possibly-misdirected message to an unrelated session.
- **Division→kit mapping in the new Uniform Order Template** — Title-only, not confirmed with Sharon/Royce. New entry, `sks/pending.md`.
- **The chat-gateway pattern's own hardening/retirement** — blocked on `task_d45603b4`, see Decided above. Appended to the existing 2026-08-24 section in `eq/pending/eq-context.md`.

## Notes
- The three `chat-gateway.md` files (eq/ops/sks) all assert the connector write-403 is "a known, open Anthropic bug... not a settings problem," dated 2026-08-24. Today's Code-side evidence (connector 403s on 3 separate calls; local git/gh works fine) doesn't confirm or refute that specific claim — unclear whether Code's MCP connector is the same GitHub App installation as Chat's hosted connector or a different one. Worth being explicit that this session's critique initially asserted "nobody's re-tested this since 2026-08-24" without having checked `eq/pending/eq-context.md` first — that file actually has a materially richer prior investigation (~2 hours, cross-referenced against Anthropic's own tracker) than the critique gave credit for. Caught and corrected before writing anything durable, not after — logged in `eq/pending/eq-context.md` directly rather than left as a spoken-only correction.
- `eq-context`'s shared checkout was well behind `origin/main` throughout this session (heavy same-day concurrent traffic — over a dozen other sessions closed against this same file today, all logged above). This close's writes went through a fresh isolated clone in scratch after confirming (via every other entry in this file today) that `git pull`/`merge --ff-only` on the shared root gets hard-blocked by the F9 guard — same remediation essentially every other session closing today already used.
- Local `git`/`gh` clearly have real write access to this repo from Claude Code, independent of the GitHub MCP connector's write scope — worth remembering as the working path regardless of how the connector-scope question above resolves.
