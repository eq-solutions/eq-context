# Session 2026-09-08 — Customers/Staff/Equipment gained their own URL; 3 PRs merged + live (work done 2026-09-07, closed after midnight)

## Built
- Diagnosed and fixed the "detail view has no URL" gap across all three eq-shell RECORDS pages: Customers, Staff, and Plant & equipment. Each gained a `<page>/:id` route; the open-record state moved from local `useState` to `useParams`/`useNavigate`.
- Staff's existing `?open=<id>&focus=conversations` one-shot deep link (from the "Ask anything" bar and the Resourcing dashboard) now redirects into the new canonical URL instead of just seeding transient state — refresh-safe as a side effect, no external caller changes needed.
- Fixed a genuine pre-existing `react-hooks/rules-of-hooks` violation found while editing the equipment module (a `useMemo` declared after an early permission-gated return) — confirmed pre-existing via `git stash`, unrelated to the routing work, fixed in the same PR since it would otherwise fail that PR's own CI.
- Shipped and merged eq-shell [PR #1807](https://github.com/eq-solutions/eq-shell/pull/1807) (Customers), [PR #1808](https://github.com/eq-solutions/eq-shell/pull/1808) (Staff), [PR #1810](https://github.com/eq-solutions/eq-shell/pull/1810) (Plant & equipment). Confirmed all three live via deploy-ancestry (`git merge-base --is-ancestor` against the actual serving commit, not just a green deploy).

## Decided
- Royce: full nested routes (matching the existing `AdminEditUser` precedent), rolled out to all 3 RECORDS pages at once — not a smaller per-page patch, and not a lighter searchParams-only approach.
- Royce: "merge them all" — explicit deploy approval for all three, given directly after being told merging this repo's `main` is the deploy.

## Deferred
- Not click-tested live by a person, all three PRs — no Shell session/credentials in this environment. Logged in `eq/pending/eq-shell.md`.

## Notes
- **Self-caught process mistake**: built the Staff fix in the same worktree/branch as the already-opened Customers PR instead of creating a fresh one, which silently added its commit onto the wrong PR. Caught before either PR was touched by anyone; fixed via cherry-pick onto a new branch (Staff) + reset and force-push (Customers) to restore the original scope. Worth remembering: create the worktree *before* starting each PR's work, not partway through a multi-PR plan.
- **Real merge conflict, not just a race**: merging all 3 in sequence, the third (Equipment) stopped applying cleanly to `main` once the other two had landed — same file, non-overlapping route blocks, but close enough that the surrounding context shifted. Needed a rebase, manual conflict resolution (both sides' changes were compatible, just needed interleaving), a `pnpm install` refresh (the lockfile had also moved from unrelated concurrent activity), and a full re-verify before it would merge cleanly.
- **Deploy verification caught a real ordering trap again**: after merging, `currentDeploy` briefly pointed at a deploy that predated all three merges, and later at another session's unrelated merge that landed in between — checking the *newest* commit's own deploy id specifically (not just "is something ready") was what actually confirmed liveness, same discipline as the toggle-bug fix earlier this session.
- `jq` is still not available in this Bash/Git-Bash environment (re-confirmed) — all CI/deploy polling this session used plain `gh`/grep-based parsing instead, which worked reliably both times.
- Session tier: EQ (eq-shell). Session log dated 2026-09-08 per the close protocol's own convention (today's date at close time), though the work itself happened on 2026-09-07 before midnight.

---

# Session 2026-09-08 (cont.) — Verified Documents-to-Sign survived a same-day eq-shell change; declined a stale fix-task spawn

## Built
- (none — verification-only session, no eq-field code changed)

## Decided
- Royce: don't spawn a task to fix the "Sign failure-reason gap" — after evidence showed it was already resolved (v3.5.537/v3.5.539, confirmed live by Royce on 2026-08-20/21, ~140 releases ago with no recurrence since).

## Deferred
- (none)

## Notes
- **eq-field**: confirmed the "Documents to Sign" signer flow ([scripts/sign-documents.js](https://github.com/eq-solutions/eq-field/blob/main/scripts/sign-documents.js), [netlify/functions/document-signoffs.js](https://github.com/eq-solutions/eq-field/blob/main/netlify/functions/document-signoffs.js)) still works after eq-shell PR [#1801](https://github.com/eq-solutions/eq-shell/pull/1801)/[#1811](https://github.com/eq-solutions/eq-shell/pull/1811) (same-day, additive-only, zero tenant-migration changes). Corrected a stale premise along the way: the client hasn't talked to Supabase/PostgREST directly for this flow since v3.5.467 (~200 releases ago) — it routes through eq-field's own service-role proxy. Live-checked ehow directly (not inferred): grants on `app_data.document_signoffs`/`document_versions` are `service_role`-only (zero `anon`/`authenticated`), and every column the code reads or writes matches the live schema exactly — zero drift. `tests/document-signoffs.test.js`: 21/21 pass. Production smoke test (credential-free POST, no token) returned the expected 401. Not verified: a full authenticated click-through (view → sign) — both tenants are Core-only, a valid session token needs a `shell_user_id` claim only a real eq-shell login produces, and this session had no eq-shell access. Deliberately did not work around that by pulling `EQ_SECRET_SALT` to forge a session token.
- **Substrate correction, for future sessions**: `eq-field/docs/reflection-log.md`'s Aug 20 entry ("sign(): six failure reasons... Root cause: not yet found") reads as an open bug if read in isolation — it isn't. The very next entries in the same thread (v3.5.539 root-caused a header-merge bug in `ehowFetch()`; v3.5.540 recorded Royce's live signed row) closed it the same week. This session nearly re-flagged it as open from a truncated grep read before catching the mistake via a full read. Read the whole thread, not one entry, before treating anything in that log as a live gap.
- **eq-context itself is in a bad state — unrelated to the above, found while closing**: the shared root checkout (`C:/Projects/eq-context`) was 96 commits behind `origin/main` with ~33 files of mixed staged/unstaged changes this session did not make (new `rules/*.md` protocol files, a 571-line `worktree-registry.md` trim, a large rewrite of yesterday's `sessions/2026-09-07.md`, new `scripts/check_budgets.py` + `safe_commit.py`) — most recently touched ~8h before this close, i.e. left mid-work, not actively being edited at close time. This is the F9 "concurrent-session git races corrupt the shared eq-context checkout" failure the gate already tracks, caught live rather than inferred. Also found two already-orphaned close-worktrees (`eq-context-wt-close`, `eq-context-wt-close-b6eaba92`) and one tidy worktree (`eq-context-wt-pending-trim`) sitting alongside the root — this is not the first session to hit this and route around it rather than resolve it. Did not touch, stash, or commit over any of it. Closed this session via a brand-new isolated worktree instead (`eq-context-wt-close-3aa1da84`, removed after pushing), mirroring the isolation convention `CLAUDE.md` already mandates for eq-field. Recommend a dedicated session (or Royce directly) reconcile the root checkout deliberately — review and either commit or discard what's staged/unstaged, and decide what to do with the orphaned worktrees — before more sessions pile further uncommitted state on top.
- pending.md: nothing to tick or add for eq-field this session — no pre-existing pending item was cleared, and nothing genuinely deferred came out of this session (the one candidate deferred item, the fix-task spawn, was assessed and correctly declined, not left open).

---

# Session 2026-09-08 (cont.) — EQ Ops board's dead "Closed" column removed, merged live

## Built
- **[PR #1809](https://github.com/eq-solutions/eq-shell/pull/1809)** — removed the Ops quotes board's "Closed" column (`QuotesModule.tsx`'s `CLOSED_STAGE`): a 6th board-only lane grouping lost/cancelled/expired/superseded quotes, with no `internalStatus` and no drop-target support, that existed only so those quotes had somewhere to render. Table view's `status_stage` filter already surfaces them individually — nothing lost. Drag handlers simplified now that every remaining column has a real `internalStatus`.
- Squash-merged `5e9b00d8`, confirmed live via Netlify's `currentDeploy` record for `core.eq.solutions` directly (deploy state `ready`, branch `main`, context `production`) — not inferred from merge success alone.

## Decided
- Removed the Closed column outright rather than making it filterable — Royce's call after a steelman of both ("simple is best"); the underlying data stays reachable via Table + the existing status filter either way.
- Recovery path after the checkout collision (below): isolate into a dedicated worktree rather than restore-to-root or leave it stashed — Royce's explicit choice among three options.
- "Merge it once approved" clarified via `/decide`: `main` has no required-review branch protection, so literal "approval-gated" merge isn't enforceable here. Royce chose CI-green auto-merge deliberately over a manual-merge fallback, and separately enabled the repo's "Allow auto-merge" GitHub setting (was off).

## Deferred
- **Not click-tested live** — `/sks/ops` is behind `RequireSession`; no way to authenticate from this environment. Needs Royce to confirm in a real signed-in session: board shows only the 5 pipeline stages, and the 4 closed-out statuses are still reachable via Table + filter.

## Notes
- **Concurrent-session checkout collision — a third independent instance tonight, per the two sessions logged above this one.** Mid-task, 9+ other sessions' branch-hops/commits landed in the shared `C:/Projects/eq-shell` root checkout between this session's edit and its commit, silently stashing both this change and Royce's own pre-existing uncommitted work (`invite-user.ts`, `package.json`, `pnpm-lock.yaml`, `vite.config.ts`) via an auto-generated stash. Nothing lost — recovered via a dedicated worktree, same pattern both sessions above independently reached for. **Three independent sessions hitting the identical failure mode on eq-shell in one evening is a strong signal, not a one-off** — worth Royce deciding whether sessions should default to a worktree from the start on this repo rather than the shared root. Royce's own 4 files are still sitting in that root-checkout stash, untouched, waiting on him.
- **eq-context itself, same finding as both sessions above, independently reached a third time**: fetched origin/main directly rather than trusting the local root checkout, which was still 101 commits behind with ~30 unrelated dirty files at the time this session tried to close. Hit the repo's own `stale-main-gate` hook on the first commit attempt — correctly blocked it, and its own message pointed at the fix used here: a fresh worktree off `origin/main` (`eq-context-wt-close-2026-09-08`, removed after pushing) rather than fetch+rebase on top of ~30 files of other sessions' uncommitted work. Also discovered mid-close: the version of `pending/eq-shell.md` sitting in the dirty root checkout already had a *fourth* same-night eq-shell session's edits layered onto it (a "Documents feature rebuilt" close, PRs #1801/#1811/#1813) that **hadn't been pushed to origin/main yet** — this worktree's clean fetch of `origin/main` did not include it. That session's own close still needs to land; this session's edits here don't depend on it and shouldn't conflict with it landing separately.
- **Windows long-path gotcha**: removing an eq-shell worktree that had a full `pnpm install` + `build:packages` done in it (needed for a real typecheck after the collision, since the recovered worktree started with no `node_modules`) failed `git worktree remove` with "Filename too long" — the `.pnpm` tree exceeds Windows' 260-char path limit. `--force` didn't help (OS-level failure, not a git safety gate); needed `Remove-Item -LiteralPath "\\?\<path>" -Recurse -Force` (the extended-length-path prefix). The directory was already correctly unregistered from `git worktree list` by the first attempt, so only the on-disk files needed the long-path-safe delete.

---

# Session 2026-09-08 — Documents feature rebuilt into 3 tier-scoped pages, retired page deleted, mobile fix shipped

## Built
- **[PR #1801](https://github.com/eq-solutions/eq-shell/pull/1801)** — split the old single-page `admin/documents` (3 tabs, 5,342-line `AdminDocumentUpload.tsx`) into three permission-tier-scoped pages: `admin/documents` (Assigner — list view + a new person/site compliance matrix with bulk push/remind), `admin/documents/library` (all three tiers — upload, categories, reference library), `admin/documents/mine` (Viewer — own sign-offs only, new). Two new additive Netlify routes (`resource=my-signoffs`, `resource=remind`), zero schema/migration changes. Fixed a real reachability gap: the old page had no sidebar entry at all, only reachable through the `admin.list_users`-gated Hub tile — a key ordinary tenant roles don't hold even though they hold `documents.view`. Added a real sidebar section outside that gate.
- **[PR #1811](https://github.com/eq-solutions/eq-shell/pull/1811)** — deleted the now-unrouted `AdminDocumentUpload.tsx` (5,342 lines), after confirming zero remaining imports.
- **[PR #1813](https://github.com/eq-solutions/eq-shell/pull/1813)** — fixed a real mobile layout bug on `admin/documents/mine`: a long title wrapping to 3-4 lines left the status pill + View button vertically centered against the whole wrapped block, floating disconnected from it. Found via an isolated harness (real JSX + real CSS tokens), screenshotted at 375×812 before/after.
- Wrote a self-contained handoff prompt for verifying EQ Field's own "Documents to Sign" flow still works, since this session had no GitHub access to the private `eq-field` repo. **A same-day sibling session ("Verified Documents-to-Sign survived a same-day eq-shell change" above) already ran this directly against eq-field's own source — confirmed unaffected, `service_role`-only grants, zero column drift, 21/21 tests pass.**
- Published an HTML wiring-audit artifact for the whole feature (18/18 automated checks, 3 items explicitly called out as needing Royce's own login rather than folded into a score).

## Decided
- Deliverable for the redesign: hand a build spec to Claude Design, not an in-chat mockup (Royce's explicit choice via AskUserQuestion).
- Scope: the whole Documents feature, not just the Register tab.
- Build the two new backend routes (`my-signoffs`, `remind`) as part of this work rather than shipping them disabled.
- Delete the old page only as an explicit, separate step once the split was verified — not bundled into the same PR.
- Merge approved individually at every commit/push/PR/merge step across all three PRs — no standing blanket approval.

## Deferred
- **Real click-through by a person** — no Shell credentials anywhere in this session's environment; confirmed again at close by hitting the actual login wall at `/sks/admin/documents/mine`. Most important specific check: as a Viewer-tier account, confirm the `my-signoffs` Network response never contains another person's name or email.
- **Manager-tier testing** — `documents.manage` without `documents.assign` isn't a stock role; needs a custom Access Control group grant before anyone can even test that persona.
- **`PdfBackfillButton` placement** — relocated from the Reference-library tab (where it had landed by accident) back onto the List page per Royce's original 2026-09-02 call, not re-verified live.

## Notes
- **Substrate correction, not a build item**: `ops/security-register.md`'s SEC-1 (listed elsewhere as an open P0 live-PII leak) was independently re-verified live this session (Supabase advisor output directly, then cross-checked against the required CI security-invariant check's own log): the two `SECURITY DEFINER` views involved (`field_people_directory`, `field_managers`, both zaap and ehow) are a formally reviewed, content-verified, column-allow-listed exception, not an open leak. Whoever owns `ops/security-register.md` should reconcile SEC-1's status there — not touched this session.
- **This session hit the same `eq-shell` checkout collision as the two sessions logged above** — same evening, same mechanism (another session's branch-hops flipping the shared checkout out from under this session's own subagents mid-build). No work lost — isolated into a dedicated worktree (`C:\Projects\eq-shell-worktrees\documents-navigation-redesign`) rather than continuing in the shared checkout, same recovery pattern independently reached for by the other sessions in this file. That makes at least four separate sessions hitting this exact failure mode on eq-shell in one evening.
- **eq-context itself, same finding as this file's other sessions, independently reached again**: local clone 101 commits behind `origin/main`, ~30 files dirty across the whole substrate at close. This session's own writes were scoped to exactly its three files and nothing else. Hit the repo's own `stale-main-gate` hook on the first commit attempt (correctly blocked); recovered via a fresh isolated worktree off current `origin/main`, with this file's own content re-applied as a clean targeted insertion each time the base moved — twice, as two further sibling sessions' own close commits landed on `origin/main` in the middle of this session's own close.
- **Deploy verification note, reconfirmed twice more today**: both #1801 and #1811's own merge commits came back `error: Skipped` from Netlify (superseded by a later merge's build under heavy concurrent-merge volume) — confirmed live both times via `git merge-base --is-ancestor` against whichever deploy actually reached `ready`, not from a green status on the commit's own deploy record or from deploy titles. #1813 went straight to `ready` with no race.
