# Session 2026-08-05 (j) — entity-patch origin-guard shipped and verified live; two live shared-checkout races caught and isolated, in two different repos

*This session was the actual build-and-ship side of the entity-patch.ts audit `sessions/2026-08-05-f.md` describes from the spawning session's vantage point — that log credits it as "spawned as a background task, built and merged independently"; this is that independent execution. This log adds what isn't in that one: the PR #1231 merge, the direct live-deploy verification steps, and two live shared-checkout races caught mid-session in two different repos.*

## Built
- **`entity-patch.ts` origin-guard.** Added `checkShellOrigin(req, 'entity-patch')` as the first check in the handler, matching the exact call shape used by the other 15 cookie-authenticated Shell functions, report-only by default (`ENFORCE_IFRAME_ORIGIN`). Not previously exploitable (no CORS headers + JSON-only body blocked real cross-origin CSRF by accident, not by design) but a genuine inconsistency against this codebase's own established pattern. Built on a fresh branch (`claude/entity-patch-origin-guard`) cut from `origin/main`, not the shared root's checkout — that checkout was sitting on a stale, unrelated branch (`claude/staff-company-field-visibility`, 8+ commits behind `main`) with two pre-existing uncommitted files already dirty in it. Saved the diff, reverted the working copy, stashed the two unrelated files, branched clean, reapplied, verified `tsc -b --force` clean, committed, pushed, restored the original branch's stash exactly as found. eq-shell [PR #1244](https://github.com/eq-solutions/eq-shell/pull/1244) — all 8 CI checks green, merged (squash `3a3613d6`) on explicit "merge PR 1244."
- **PR #1231** (`chore(intake): auto re-vendor eq-intake/eq-platform`) — routine automated vendored-package bump (package.json + vitest/vite configs, 6 files, +59/-7, no functional change). Merged (squash `c670d6f4`) after disambiguating an ambiguous later "merge" request via AskUserQuestion — it was the only other open PR authored by this session's account; nothing else matched.
- **Live-deploy verification for both**, done directly rather than assumed from the merge: queried Netlify's project/deploy API (`get-project` → `currentDeploy`, `get-deploy-for-site`) for the live production deploy on `core.eq.solutions`, then confirmed both `3a3613d6` and `c670d6f4` are ancestors of the currently-published commit (`b72f0ca5`, `state: ready`, published 2026-08-05T00:56:52Z).

## Decided
- Royce, via AskUserQuestion: build the origin-guard fix on a fresh branch off `main`, not the shared root's stale/unrelated branch.
- Royce: "merge PR 1244" (explicit).
- Royce, via AskUserQuestion: the later ambiguous "merge" resolved to PR #1231 specifically, not guessed.
- Royce: "open a worktree for that session" (explicit) — acted on, not just flagged.
- Royce, via AskUserQuestion: recover this session's dropped substrate writes (below) via an isolated clone rather than dropping them or touching the shared checkout again.

## Deferred
- Nothing new deferred here specifically. See `pending.md`'s existing open item on the eq-shell/eq-field root-sharing risk — this session added a third eq-shell occurrence and a first `eq-context` occurrence to that same open question.

## Notes
- **First race: caught in `eq-shell`'s root checkout in real time, not after a collision.** Mid-session, `git reflog` on `C:\Projects\eq-shell` (root, not a worktree) showed a separate concurrent session had checked out `fix/staff-compliance-pack-name-overlay` (commit `78fa589e`) and `feat/staff-address-field` (commit `5e524be6`) in that exact same directory while this session was also checked out there on `claude/staff-company-field-visibility`. Verified both commits were already pushed to `origin` before doing anything else — nothing was actually at risk. Mitigated going forward by creating an isolated worktree — `C:\Projects\eq-shell\.claude\worktrees\staff-company-field-visibility-concurrent`, forked from the shared branch's own tip so nothing was lost — rather than leaving both sessions sharing root.
- **Second race: this session's own `/close` landed inside a different concurrent session's paused interactive rebase of `eq-context`'s `main`.** A pathspec-scoped `git commit -- eq/pending.md eq/changelog/eq-shell.md sessions/2026-08-05-h.md` (deliberately narrow, to avoid sweeping another session's already-staged files — which it correctly didn't) turned out to run mid-rebase, so the commit landed as a step inside someone else's paused `edit`, not on `main`. Protected it immediately with a plain branch pointer (`eq-context-close-h-backup`) before that rebase could resolve underneath it. When it did resolve — via that other session redoing its own work rather than continuing — my commit was left unreachable from `main`. Nothing was lost (the backup branch held it), but reapplying it directly in the shared checkout would have meant either reconciling `main`'s 6-ahead/21-behind divergence from `origin/main` there (a `pull`/`rebase`, which this repo's own F9 guard explicitly redirects away from the shared checkout) or risking a third collision. Recovered instead via a fresh isolated clone of `origin/main` — content pulled back out of the backup commit by hand (not cherry-picked, to avoid depending on the shared checkout's local-only branch at all), reapplied here, and pushed straight to `origin/main` from the clone.
- **The session-log filename itself collided three times over across two different fixes for the same underlying problem.** First tried `-g` (already claimed by the F9/hooks session), then `-h` (claimed locally in the shared checkout, but *also* independently claimed on `origin/main` itself by a completely unrelated EQ Ops session — two different sessions picked the same next letter without ever seeing each other's work), landing finally on `-j` here in the isolated clone after checking `origin/main` directly rather than the shared checkout's local view of available letters (which was already stale by the time it mattered).
- **Two pre-existing, unrelated uncommitted files** (`src/pages/AdminWorkerInviteForm.tsx`, `src/pages/AdminWorkerInvites.tsx`) were already dirty in `eq-shell`'s shared root at the very start of this session, unrelated to anything asked of it. Preserved through every stash/checkout used to isolate the entity-patch build — never staged, never touched, still uncommitted as of this close. Ownership/intent unknown from here; worth whoever's actually continuing `claude/staff-company-field-visibility` checking what they are before an unrelated `git clean`/`checkout` on that branch loses them.
