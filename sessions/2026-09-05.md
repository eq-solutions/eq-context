# Session 2026-09-05 — SEC-53 verified live, closed in the register, and merged

## Built
- Verified eq-field [PR #916](https://github.com/eq-solutions/eq-field/pull/916) (CSP drops the deleted ktmj Supabase host) was merged and actually live — checked commit ancestry against the Netlify production deploy for field.eq.solutions, not just the version string in `sw.js`.
- Independently re-checked core.eq.solutions' CSP: zero `ktmj` references, and `frame-src` no longer lists `quotes.eq.solutions`.
- Closed SEC-53 in `ops/security-register.md` (both the eq-shell and eq-field halves) and flipped the matching `eq/pending/eq-field.md` and `eq/changelog/eq-field.md` entries from OPEN/NOT-merged to MERGED — eq-context [PR #206](https://github.com/eq-solutions/eq-context/pull/206), done from an isolated worktree, never the shared checkout.
- Merged PR #206 on Royce's explicit "merge" instruction. Re-verified mergeability first: no branch protection on eq-context's `main`, and no real conflicts — the only overlap with `origin/main`'s movement was unrelated nightly pending-rotation edits to other sections of `eq/pending/eq-field.md`.
- Applied the pending.md archive rule to the SEC-53 section post-merge: one open item remained (the supervisor-digest README fix), so the section was trimmed to header + that one line, dropping the intro paragraph and the three completed checklist lines (narrative already lives in the changelog entry and this log).

## Decided
- Royce: merge eq-context PR #206.

## Deferred
- `supabase/functions/supervisor-digest/README.md` still points its deploy/curl instructions at the deleted ktmj Supabase project — docs-only, not touched this session. Tracked in `eq/pending/eq-field.md`.
- eq-context [PR #205](https://github.com/eq-solutions/eq-context/pull/205) (review-clock ratchet fix, 8 overdue state files → 0) — already merged by the time this session closed; not touched by this session, noted only because it's what unblocked PR #206's `health` check.

## Notes
- eq-context's `health` CI check failed on PR #206 for a reason unrelated to the change: the repo-wide "Review clock" ratchet (8 overdue state files against a ceiling of 7). Confirmed via `gh run view` that the identical failure was already on `main`'s own latest push run before PR #206 even existed — not a defect in this PR. Saved as a standing memory (`reference_eq_context_review_clock_ratchet_fails_unrelated_prs.md`) so a future session doesn't mistake this ratchet failure for something caused by its own diff.
- All edit/commit/push work for both the SEC-53 close and this session-close pass ran in git worktrees off `origin/main` (`claude/sec-53-close`, then `claude/session-close-2026-09-05`), never in the shared `C:\Projects\eq-context` checkout — that checkout's own hook hard-blocks a bare `git commit` and redirects `rebase`/`merge`/`pull` there (failure F9), and the checkout was confirmed behind `origin/main` (7, then 3 commits) for most of the session.
- Worktree and remote branch removed after PR #206 merged.

---

# Session 2026-09-05 — EQ Field white-pane stall fixed, merged, confirmed live; EQ-SHELL-T/V checked and still open

*Started 2026-09-04 (build + PR + merge), continued into 2026-09-05 (live re-verification on "is it live yet"). Logged as one entry since it's one continuous session.*

## Built
- Root-caused a reported white `/sks/field` pane (iframe present, no spinner, no error, tab responsive) to EQ Field posting its 'accepted' sign-in message before it renders anything, combined with Shell's only stall watchdog never arming for a background pre-warmed frame (the normal case since 2026-09-02's staged-prewarm work).
- Built and shipped [eq-shell PR #1758](https://github.com/eq-solutions/eq-shell/pull/1758) in an isolated worktree off `main`: a 10s "still loading" card with Retry, a non-blocking reload strip for the accepted-but-not-drawn case, a 20s cap on the previously-unbounded sign-in request, immediate retry for a stale pre-warmed token, and per-handoff-step Sentry/PostHog telemetry (`src/lib/fieldHandoffTelemetry.ts`) plus a `Server-Timing` breakdown on `token-exchange.ts`. No auth/gating logic touched.
- All local gates green before push: `tsc -b --force`, eslint (0 errors/warnings on changed files — confirmed the one exhaustive-deps warning was pre-existing on `main`, not introduced), full test suite 491/491 (11 new), CSS coverage gate.
- Pulled EQ-SHELL-T (`verify-timeout`) and EQ-SHELL-V (`session-spinner-timeout`) from Sentry to check whether they were the same bug. They aren't — separate stall in `verify-shell-session.ts` — and confirmed #1736 (merged 2026-09-02) did NOT close them: both kept firing on `/sks/field`, most recently 2026-09-04T02:17:5[5-7]Z (same user session, Chrome/Mac, Sydney), after #1736 was live.
- Royce merged #1758 (squash `b3c687f1`) — my own merge attempt was blocked by the auto-mode classifier, a known gate on eq-shell merges; handed the exact `gh api` command back to him.
- Verified live twice: immediately after merge (Netlify `published_at` 2026-09-04T09:36:04Z on that commit, ancestry-checked, plus a direct probe of `core.eq.solutions` confirming the new function and client bundle were actually being served) and again the next day on "is it live yet" (main had moved 3 PRs further to `7fd42e70`; confirmed `b3c687f1` is still an ancestor of the live commit).

## Decided
- Royce: "merge #1758" — after being shown CI green, the PR body, and the not-click-tested caveat; ran the merge himself once my attempt hit the classifier gate.
- Royce: "verify the deploy is live" — done both immediately post-merge and again the next day.

## Deferred
- **Not click-tested live** — no Shell session/credentials in this environment. Manual test plan is in the PR body and copied into `eq/pending/eq-shell.md`.
- **The durable fix belongs on EQ Field's side**: it needs to tell Shell once it has actually finished rendering, not just that sign-in succeeded. Spawned as `task_043e28eb`; Royce started it in a separate local session on 2026-09-04 — not tracked further here.
- **EQ-SHELL-T/V remain open**, unaffected by #1758. 7 of `verify-shell-session.ts`'s 11 background reads are still unbounded (4 were fixed by #1736). No fix attempted this session — flagged only.

## Notes
- **Stale claim found, not corrected in place**: `eq/pending/eq-shell.md`'s "Sentry sweep — 2 small data items left (2026-09-02)" section has a trailing paragraph describing PR #1736 as having fixed "the auth-stall" — true that it merged and went live, but it did not actually close EQ-SHELL-T/V (see Built, above). Left that section untouched per the close protocol (it mixes two unrelated open checklist items with free-standing prose not part of any bullet) rather than guess at how to edit it; flagging here instead. A future pass correcting it should link to this session's entry.
- The shared `eq-context` checkout was clean and only 2 commits behind, but writes for this close were still done from a fresh clone rather than in place, consistent with the recurring shared-checkout-contention pattern logged elsewhere in this file's history — a local `git clone` from the shared checkout's own working copy hung for 60s+ (likely a filesystem/AV contention issue on this machine, not a network one; a plain clone straight from GitHub completed normally), so the isolated copy came from the remote directly rather than from `C:\Projects\eq-context`.
- Confirmed nothing else had already logged PR #1758, EQ-SHELL-T/V's post-#1736 status, or the white-pane symptom anywhere in `eq/pending/*.md`, `eq/changelog/eq-shell.md`, or `sessions/2026-09-0[1-4].md` before writing — this is new content, not a duplicate of concurrent work.

---

# Session 2026-09-05 — eq-shell: Collin Toohey's duplicate identity fully deleted, closing a live symptom the 2026-08-02 fix left open

## Built
- Investigated a review-clock finding on `eq/active.md`: jvkn `public.workers` still held two rows for Collin Toohey — the real one (`7514e57d`, correctly linked to his real account `1becb454` and ehow staff record `3c9714bd`) and an orphan (`bf26e8c4`, all fields null, `user_id` pointing at stub account `38859cae`) dating to a 2026-07-26 failed-signup collision. The stub account had already been deactivated 2026-08-02 (Sentry EQ-SHELL-Z, `shell_control.audit_log` 4186) but the duplicate worker row was deliberately left in place at the time, noted "harmless, no staff_id link."
- Re-verified live rather than trusting the historical note: swept every table with a `worker_id`/`user_id`/`cards_worker_id`/`new_user_id` column across jvkn, ehow, and zaap (~30 tables) — confirmed zero references to either id anywhere. ehow's and zaap's `app_data.staff.cards_worker_id` both correctly point only at the real worker id.
- Found something the original fix couldn't have caught: `auth.sessions` showed a new session minted on the stub account 2026-09-04T02:18:07 — 20 seconds after a normal session on Collin's real account, same iPhone, same IP. The 2026-08-07 `handle_phone_dedup`/`link_pending_invites` trigger fix (PR #1270/#1272/#1273) — which explicitly checked Collin's case and confirmed the fix pattern covered it — stops a *new* duplicate row from being created on a future collision, but doesn't stop this *already-existing* dormant account from still authenticating in parallel with the real one. No new data resulted from that session (the duplicate worker row's `updated_at` was unchanged since creation, the tenant membership stayed inactive), but it meant Collin was intermittently still landing on a dead-end identity.
- Checked the cascade blast radius before recommending action: `public.workers.user_id`, `public.profiles.id`, and `public.identity_collision_flags.new_user_id` all carry `ON DELETE CASCADE` to `auth.users`; `shell_control.user_tenant_memberships` does not.
- Presented Royce the three real end-states (delete the stub account / ban it and keep the records / leave as-is) with the full evidence above. Royce: delete.
- Deleted `auth.users` `38859cae` via Supabase MCP (matches the established "manual DB correction, not a migration" precedent from 2026-08-02/08-07) — cascade automatically removed `public.workers.bf26e8c4`, `public.profiles.38859cae` (`full_name: "Collin Toohey"`, `email: null`), and `identity_collision_flags.ef5d8ae0`; confirmed 0 rows remaining across all four plus `auth.identities`/`auth.sessions`. Manually deleted the orphaned (already-inactive) `user_tenant_memberships` row, which had no FK so didn't cascade. Re-confirmed Collin's real account/worker/staff records untouched throughout.
- Logged as `shell_control.audit_log` id 10300, matching the `admin.manual_account_correction` convention from ids 4183/4186 (2026-08-02) exactly, with the full incident history in `detail.reason` since the cascade removed the `identity_collision_flags` row that used to carry it.
- Corrected the stale `eq/active.md` "Collin Toohey fresh invite" Gated item (last touched 2026-06-13/07-21) — his real account has 15+ genuine sessions June→September, so the original "needs a fresh invite" concern is moot.

## Decided
- Royce, via AskUserQuestion: delete the stub account outright rather than ban-only or leave-as-is, once shown it was still intermittently authenticating alongside his real account.

## Deferred
- None — fully closed: verified, decided, executed, verified again, logged, substrate updated.

## Notes
- The 2026-08-02 "harmless, no staff_id link" call was correct at the time and remained correct on the data-integrity side today (zero references, confirmed twice) — what changed the calculus wasn't new data risk, it was discovering the underlying auth identity was still live enough to mint sessions, which a pure data-reference sweep alone wouldn't surface. Worth remembering for any future "is this dormant duplicate account really inert" question: check `auth.sessions`/`last_sign_in_at`, not just downstream table references — an account can be fully unreferenced by application data and still be authenticatable.
- eq-context clone was 3 commits behind `origin/main` at session start (SYNC STOP gate). The shared checkout's `git pull` is guarded (F9) against concurrent-session races, so did the substrate reads and this session's own writes from a fresh isolated clone instead, per the guard's own suggested remediation.
- **F9 materialized for real even in the isolated clone, twice, not just as a theoretical risk.** First wave: between the first fetch (0 commits behind) and the actual commit attempt minutes later, a concurrent session closed out SEC-53 and pushed a full line-by-line rewrite of `eq/active.md` plus its own entry in this file — 11 new commits landed mid-session, producing a real content conflict in `eq/active.md` (resolved by keeping the concurrent rewrite and re-applying just the Collin Toohey resolution as an edit to its already-updated bullet) and a filename collision on this session log (resolved by appending below theirs). Second wave: re-fetching immediately before the retry commit found 4 more commits, including a third session's own EQ Field white-pane-stall entry appended into `eq/changelog/eq-shell.md`, `eq/pending/eq-shell.md`, this file, and a same-line edit to `sessions/INDEX.md`'s 2026-09-05 row title — all resolved the same way, keep both sides' content, order by date. Worth remembering: even a private clone doesn't remove the need to re-fetch immediately before committing when a session runs long, especially on a repo this many concurrent sessions write to on the same day — the race is with `origin/main`, not with the shared local checkout specifically, and it can recur more than once in a single close.

---

# Session 2026-09-05 — EQ brand brief reconciled to Direction D tokens: neutral ramp fixed, accent tokens documented, both merged

## Built
- Reconciled `rules/brand-eq.md`'s neutral colour scale and every border/component-rule hex derived from it — they still specified the pre-Direction-D cool-grey ramp (`#F9FAFB`/`#F3F4F6`/`#E5E7EB`/`#D1D5DB`) more than three months after `eq-design-tokens` shipped the warm-sand ramp + clay accent ("Direction D", `7af9b69`, 2026-05-31). Verified live against the tokens repo's current tag (v1.3.3) and confirmed the drift never reached shipped product — eq-field's generated `styles/tokens.css` and eq-solves-service's CSS import both already had Direction D live; only this doc had drifted. Also fixed a stale SKS-palette exclusion hex two lines away (`#1F335C` → `#203060`, cross-checked against `rules/brand-sks.md`'s own recorded correction). [eq-context PR #207](https://github.com/eq-solutions/eq-context/pull/207).
- `rules/brand-check.md` (the SKS-side preflight, not an EQ one) was cross-checked per the original task's instruction — confirmed it carries zero EQ neutral-ramp hexes and already correctly excludes EQ tokens. No change needed.
- Found the identical drift in `eq/design/claude-design-context.md` — but a concurrent session (`7db06c0`) had already independently re-verified that file end-to-end while this one was mid-flight, including a note flagging brand-eq.md as needing this exact fix. Rebased on top of it rather than duplicating or conflicting with a more complete fix.
- Spawned, then executed directly, the follow-up gap that PR #207 deliberately left alone: `accent.clay`/`clayDeep`/`clayBg` and `brand.amber`/`amberDeep`/`slate`/`live` have existed in `@eq-solutions/tokens` since Direction D with zero documentation footprint in `brand-eq.md` at all. Added them (4 new Brand-tokens-table rows, a new "Accent tokens (Direction D)" subsection, one Hard Don'ts line), porting the already-reviewed usage language from `claude-design-context.md` rather than drafting new guidance from scratch. [eq-context PR #210](https://github.com/eq-solutions/eq-context/pull/210), stacked on #207.
- Merged both into `main` on Royce's instruction: #207 first (squash `48cabc3`), then #210 retargeted from #207's branch to `main` and merged (squash `0bfbd78`). Both branches deleted and their worktrees removed afterward.

## Decided
- Royce: merge eq-context PR #207.
- Royce: retarget PR #210 to `main` and merge it too.
- Royce: spawn a background task for the clay/amber/slate/live docs gap — then, on starting that task, had it executed directly in this session rather than a separate one.

## Deferred
- None — both PRs merged, branches and worktrees cleaned up, nothing left open from this thread.

## Notes
- **Retargeting a stacked PR after its base PR squash-merges produces a real `CONFLICTING`/`DIRTY` GitHub mergeable state, even when the file content is fully compatible** — worth remembering for the next stacked-PR close on this repo. GitHub's merge check is history-based: the squash commit on `main` shares no ancestry with the stacked branch's own pre-squash commit of the same change, so the three-way merge sees a conflict that a content diff wouldn't. Fix: `git rebase origin/main` on the stacked branch — git recognises the now-redundant commit as empty via patch-id and drops it automatically (`warning: skipped previously applied commit <sha>`), leaving just the branch's own new commit(s) to reapply cleanly on top. Force-push, re-verify CI green, then merge normally.
- All edit/commit/push work this session ran in isolated worktrees off `origin/main` (`eq-context-brand-tokens-wt`, `eq-context-accent-tokens-wt`, `eq-context-close-brand-wt`), never the shared `C:\Projects\eq-context` checkout — confirmed necessary again: a plain `git merge --ff-only` attempted directly in the shared checkout, just to sync before writing this close, was hard-blocked by the F9 guard (rung 4).
- The `/brief` session-gate flag, written once at this session's start for eq-context, correctly covered every later edit in the session — including the second, unrelated accent-tokens task that came in in a later turn — confirming the flag is genuinely per-session, not per-task.

---

# Session 2026-09-05 (b) — EQ Field white-pane fix click-tested live; both follow-on fixes confirmed live

*Continuation of the same day's earlier close (PR #1758 built/merged/verified live). This entry covers the live click-test Royce asked for afterward, plus two discoveries made during it.*

## Built
- Nothing new built — this stretch was verification only, on Royce's real Chrome via Claude in Chrome (per his standing preference: real browser only, never the in-app pane).

## Decided
- Royce: "click-test the new stall card live" — done on his own authenticated session (SKS Technologies workspace).
- Royce: "check when #1764 goes live" — checked and confirmed.

## Deferred
- **EQ-SHELL-T/V not yet confirmed fixed.** Both #1758 (this session's fix) and #1764 (the other session's fix for the remaining 7 unbounded `verify-shell-session` reads) are live as of 2026-09-04 evening. Whether they've actually stopped the recurrence needs a few real days against Sentry — added as a pending item to check back around 2026-09-08.

## Notes
- **Click-test method**: navigated to `/sks/field` with `window.fetch` monkey-patched (in-page, via `javascript_tool`) to intercept and hang any request to `/token-exchange`, forcing the exact stalled-handoff state the fix targets, without touching system network settings or leaving anything broken — the override lives only in that page's JS runtime and a plain reload clears it. Confirmed the "Still loading EQ Field…" card renders with the right copy, fires at exactly 10 seconds (own telemetry timestamps), and Retry issues a real second request (counted via an intercept counter, not just a UI-state check). Restored real `fetch` and reloaded to confirm the unblocked baseline still works end to end.
- **Real discovery made only because of the live test, not from reading code**: the console showed a `rendered` handoff step that doesn't exist in what this session built. Git history traced it to eq-field/eq-shell PR #1765, merged and live since earlier the same day — the exact follow-up task (`task_043e28eb`) spawned in the morning's close, already done by the time this test ran in the evening. This is a case where testing against the real, currently-live bundle caught a fact (another session's shipped fix) that reading `origin/main` from a stale local checkout would also have caught, but the live click-test caught it as a *side effect of proving the primary fix works*, which is worth remembering as a reason to click-test even when the code review already looks complete.
- The automated browser session's tab reported `document.visibilityState === 'hidden'` throughout, even though it was the active/only tab — worth knowing for any future live-testing of the eager pre-warm mechanism specifically (which gates on `visibilityState === 'visible'`): a Claude-in-Chrome-driven tab may never trigger it. Not a product bug; an artifact of how the automation surfaces to the Page Visibility API. Didn't block this test since navigating directly into `/sks/field` mounts `FieldIframe` regardless of pre-warm state.
- This close's writes went through a fresh isolated clone of `eq-context` (`git clone` straight from GitHub) rather than the shared checkout, consistent with the recurring shared-checkout-contention pattern already logged elsewhere in this file's history. The shared checkout was only 1 commit behind this time, but a concurrent session's own close landed on `eq/pending/eq-shell.md` (an unrelated new section, "Collin Toohey's duplicate identity") and another closed SEC-72 in the same file, in the gap between this session's two close passes — confirmed no line-level overlap with this session's own section before writing.

---

# Session 2026-09-05 — eq-field: dead Shell staff auto-mode block removed, merged, confirmed live

## Built
- Investigated a pre-written finding: index.html's "Shell staff auto-mode" block (v3.5.138) read `sessionStorage.getItem('eq_role')`, a key that looked like it might never have been set. Checked for duplicate dispatch first (worktree list, full branch list, peer session transcripts) — clean, no live collision.
- Root-caused via `git log -S` across every branch, all history: the bare `eq_role` key was never once written anywhere in this repo. The same commit (`1ff0309d`, v3.5.138) both introduced the block and is the only commit that ever touched it — dead the day it shipped, not broken by a later rename. Confirmed the real key (`eq_shell_role`) and real role source (`permissions.js`'s `getRole()`/`EQ_PERMS.role()`) both exist and are correctly used elsewhere.
- Found a sibling dead path to the same feature already documented as intentionally left dead by a prior session (`auth-staff-ts-gate.js`'s `checkStaffTsLogin()`, behind the Core-only gate).
- Ran the eq-field Session Gate (`/brief eq-field`) after a brief-gate hook blocked the first edit attempt — the gate had not been run yet this session.
- Built the fix in an isolated worktree: removed the dead block, bumped versions, added the changelog banner entry and a reflection-log entry. Verified `check-cache-busters.mjs`, a direct `node --check` on the edited inline script, and a local boot test via `npx serve` before opening eq-field [PR #919](https://github.com/eq-solutions/eq-field/pull/919).
- On "merge," found `main` had moved: [PR #917](https://github.com/eq-solutions/eq-field/pull/917) (Royce co-authored) had independently found the identical dead-key bug while fixing this exact block's fire-and-forget call, and had explicitly deferred the keep-or-delete decision as its own task — this closed that task. Real conflict on `initApp()`; rebased, took the delete side, renumbered past #917's version claim, re-verified, force-pushed, squash-merged (`fb8c98bd`). Confirmed live via direct curl: `field.eq.solutions/sw.js` shows v3.5.676.

## Decided
- Royce, via AskUserQuestion: delete the dead block rather than revive it — WorkerHome in Shell looks like the intended landing spot for staff now.
- Royce: "merge" — PR #919 squash-merged after resolving the real conflict with #917.

## Deferred
- None from this thread — investigated, decided, built, merged, verified live, substrate updated.

## Notes
- The sibling dead path (`auth-staff-ts-gate.js`'s `checkStaffTsLogin()`) was not touched — the same product reasoning (WorkerHome supersession) plausibly applies to it too, but deleting it was out of this session's scope and nobody asked.
- All eq-field edit/commit/push work ran in an isolated worktree (`eq-field-shell-staff-automode-wt`) off `origin/main`, never the shared `C:\Projects\eq-field` checkout, which was mid-task on an unrelated branch (`claude/field-permission-denials-confirm-live`) for another concurrent session throughout.
- This close's own eq-context writes ran from a fresh `git clone` of eq-context (not the shared checkout, not a worktree) — this file alone gained a 5th same-day session entry between this session's first read and its write, confirming the F9 concurrent-session race this file's own history already documents is still live and recurring, not historical. Hit the branch-level `stale-main-gate` guard twice trying to commit directly on `main` before switching to a dedicated branch, which sidestepped it entirely — worth remembering as the faster path next time this guard fires.

---

# Session 2026-09-05 (c) — EQ Field's durable render signal (#917) shipped and merged; close-time collision with two sibling sessions on the same thread resolved

*Independent session, working from the earlier white-pane thread via the task-chip breadcrumb in `eq/pending/eq-shell.md` (`task_043e28eb`) rather than as a direct continuation of session (b) above — the two ran concurrently without either being aware of the other until this close. A third session (its own close is the very next entry above, on eq-field's dead auto-mode branch) also ran concurrently.*

## Built
- [eq-field PR #917](https://github.com/eq-solutions/eq-field/pull/917), v3.5.675: `index.html`'s boot caller now does `initApp().then(rendered, failed)` — posts `{ kind: 'rendered', elapsed_ms, landing }` once `initApp()` settles, or `{ kind: 'render-failed', detail }` if it throws, exactly once per boot across every landing branch. Every handoff message now carries `caps: ['rendered']` so Shell can tell a build that sends the new signal from an older one still cached by the service worker. Full mechanics in `eq/changelog/eq-field.md`.
- [eq-shell PR #1765](https://github.com/eq-solutions/eq-shell/pull/1765): `FieldIframe.tsx`'s 10s draw notice now gates on the `rendered` signal instead of the iframe `load` event.
- Royce: "merge 917 then 1675" (1765) — explicit go-ahead for both production deploys. eq-field's `main` needed two rebases before #917 would merge clean (PR #916 claimed the version number, PR #918 collided on the shared changelog append point) — resolved both times, full suite re-run after each. Both PRs merged via `gh api -X PUT .../pulls/<n>/merge`, not `gh pr merge`, per the standing local-checkout-collision workaround.
- Verified live via commit-ancestry/deploy-record checks, not the merge alone — eq-field via `sw.js`, eq-shell via `listSiteDeploys` for the exact `commit_ref` (`state: "ready"`, `published_at` 2026-09-04T20:50:44Z).
- Click-tested live, signed in as Royce (manager, SKS) at `core.eq.solutions/sks/field` — full chain in console: `mint-start → mint-ok → src-set → boot → accepted → iframe-load → rendered`.
- Along the way, found (didn't fix) a dead "Shell staff auto-mode" branch reading a `sessionStorage` key nothing ever wrote, and spawned a task chip for it (`task_ae6b6f1f`) rather than fold the product decision into this PR's scope. Royce started that chip in a separate session — see the entry immediately above this one for its own full build/decide/merge account (eq-field PR #919, v3.5.676). That session's own PR body confirms the real overlap: its rebase hit an actual conflict against this session's #917 on the same `initApp()` block, resolved by taking the delete side entirely.

## Decided
- Royce: "merge 917 then 1675" — explicit production-deploy approval, both PRs, stated order.

## Deferred
- **EQ-SHELL-T/V**: found at close time, not by this session — [eq-shell PR #1764](https://github.com/eq-solutions/eq-shell/pull/1764) (`d434a407`, merged 2026-09-04T20:55:28Z, rebased onto this session's own #1765) bounds the remaining 7 of `verify-shell-session.ts`'s 11 unbounded reads, the root cause this session's own PR explicitly did not touch. Confirmed real and merged (`gh pr view`), but not yet confirmed to have stopped the actual Sentry recurrence — already tracked as a watch-until-~2026-09-08 item in `eq/pending/eq-shell.md`, not duplicated here.

## Notes
- **Two separate close-time collisions on the same thread, resolved by reconciling twice, not overwriting.** First: this session's `eq-context` clone was made before session (b)'s close pushed — `origin/main` had moved with session (b) covering the same pending.md section and eq-shell changelog, plus one genuinely new fact this session didn't have (PR #1764). Reset to the new tip, kept session (b)'s content, added only what was non-overlapping (eq-field's actual PR numbers/mechanics). Second, minutes later: a third session's own close for eq-field PR #919 landed — a much more detailed, authoritative account of #919 than this session's own secondhand summary of it (this session never built #919, only flagged the need for it). Reset again, trimmed this session's eq-field.md entry down to just its own work (#917) instead of restating #919, and pointed at the sibling entry instead.
- This session's own Deferred claim ("EQ-SHELL-T/V remains open, unaffected") was corrected before this close using session (b)'s discovery (#1764) rather than left stale — worth remembering generally: a session's understanding of "what's still open" can go stale between when it forms that view and when it actually closes, on a repo this actively worked concurrently.
- Three independent sessions closing out threads of the same underlying incident within roughly an hour, each hitting real (not just textual) overlap with at least one other, is worth treating as a normal load pattern for this repo right now, not a sign anything went wrong — the fix in every case was re-fetch immediately before committing and reconcile by content, never force-push over a sibling's already-pushed close.
- Same reconciliation methodology as sessions (b)/(third): fresh isolated `git clone`, never the shared `C:\Projects\eq-context` checkout (blocked outright by the F9 guard for even a trivial fast-forward).

---

# Session 2026-09-05 (d) — SEC-72 role-override fail-open: reviewed #1762 through 5 rounds, verified #1762/#1767/#1768 live, closed the register

## Built
- Independently verified the SEC-72 finding against `origin/main` a3508c60 before any work started: `withDeadline`'s never-rejects semantics and both empty-denial fallback sites in `verify-shell-session.ts`/`token-exchange.ts`.
- Found the task had already been dispatched to a sibling session (an unpushed worktree with fresh file mtimes) before starting any code — stood down, verified independently instead, and reviewed rather than duplicated. Logged as a new memory rule (`duplicate-task-dispatch-check-peers-first`).
- Reviewed eq-shell [PR #1762](https://github.com/eq-solutions/eq-shell/pull/1762) across all 5 pushed commits (`431ad43b` -> `1ecb7a72`), posting 5 comments on the PR itself: confirmed the fail-open paths were closed, caught a real merge conflict with a concurrently-merged PR (#1758) before it could land wrong, and pushed for (accepted) design changes -- token-exchange refusing the mint with a 503 instead of minting a crippled 7-day Field session, Sentry throttling, and a floor on the anchored deadline -- verifying each fix against the actual diff, not the author's description, before signing off.
- After Royce merged #1762, confirmed it live the hard way: its own deploy record came back `state: error`/`"Skipped"` (a concurrent merge built ahead of it), so verified via `git merge-base --is-ancestor` against the production deploy that actually published, per this repo's documented deploy-verification method -- not the deploy record alone.
- Opened eq-context [PR #211](https://github.com/eq-solutions/eq-context/pull/211) (isolated clone) closing the SEC-72 row in `ops/security-register.md` and ticking `eq/pending/eq-shell.md`, citing the squash SHA and the ancestry check. Royce merged it directly.
- Reviewed and independently confirmed live eq-shell PR #1768 (a sixth call site, `security-groups.ts`, found by the sibling session while closing the three-minters follow-up) before finding Royce had already merged it.
- Confirmed PR #1767 (the three-minters follow-up, `task_cff561fd`) was already merged and live when asked to "pick it up" -- reported that back rather than starting duplicate work.
- This close: added the still-missing changelog entry for #1762/#1767/#1768 (`eq/changelog/eq-shell.md`) and applied the pending.md archive-trim rule to the now-mostly-closed "security register reconciled" section (dropped the SEC-72/SEC-73 `[x]` lines, kept the header and the still-open SEC-71 line) -- the full narrative for both now lives in the register and this changelog entry.

## Decided
- Royce: merge eq-shell PR #1762 (auth change, explicit approval given).
- Royce: "merge #211 when ready" / "merge #1768 when ready" -- both already merged by him directly before this session acted on either instruction.
- Royce: "pick up task_cff561fd's remaining minters when Royce's ready" -- already done (PR #1767, merged by him).

## Deferred
- `_shared/permissions.ts`'s `resolvePrincipal()` stays fail-open by design -- `requirePerm()` calls it from roughly 124 independently-bundled functions, most far lower-traffic than the six sites now fixed, so adopting the new resolver there would trade today's narrow, accepted gap for tenant-wide 403s on any blip once adopted everywhere at once. A real trade-off, needs Royce's explicit decision before anyone touches it -- not attempted this session, and not added as a fresh `eq/pending/eq-shell.md` item either, since eq-context PR #213 already documents it fully inside the SEC-72 register row.
- The 10-minute per-container cache also over-restricts in the reverse direction: a denial an admin *removes* stays enforced for up to two polls in a warm container. Accepted trade-off, documented in the register, not actionable.

## Notes
- **The actual code for #1762/#1767/#1768 was built by a sibling Claude session** ("Fail closed on slow role-override reads", rooted at the same `C:\Projects\eq-shell` path) -- this session's role was independent verification, code review, live-deploy confirmation, and the eq-context register closure, not authorship. Recorded plainly here so the changelog entry above isn't misread as this session's own build.
- **Discovered mid-session: sessions rooted at the same project path share the same Claude memory directory**, not just the same eq-context substrate -- both this session and the sibling appended near-duplicate "merged + live" notes to the identical memory file within minutes of each other. Trimmed the duplicate in place rather than leaving both stand. Worth remembering for any future multi-session work on the same repo: check memory files for concurrent edits the same way eq-context's own F9 guard already trains you to check substrate files.
- **eq-context PR #213** (from the sibling session, landed mid-close) had already updated the SEC-72 register row and pending line for #1767/#1768 and the sixth call site by the time this close pass re-fetched. Re-read fresh state before touching either file rather than trusting this session's earlier-cached reads, then built the changelog entry and the pending-trim on top of the post-#213 content.
- eq-context was re-fetched immediately before this close's own commit, per the recurring F9 same-day-collision pattern already logged elsewhere in this file -- caught PR #213 landing in exactly that window, before it could be overwritten.

---

# Session 2026-09-05 (e) — eq-solves-service attachment uploads fixed live; follow-up tenant-guard hardening merged

*Started 2026-09-04 (bug report, root cause, fix, PR #827, live dispatch), continued into 2026-09-05 (broader feature security review, PR #828, merge). Logged as one entry since it's one continuous session.*

## Built
- Royce reported every attachment upload failing with "new row violates row-level security policy" on a maintenance check page. Root-caused via a live `pg_policies` query on ehow (not migration-file reading): the `attachments` Supabase Storage bucket's RLS policies had drifted out-of-band at some point — flipped from `public=true` + 3 tenant-scoped policies (migration 0005's original shape) to `public=false` with a single SELECT-only policy, INSERT/DELETE dropped entirely, zero trace in any tracked migration. Ruled out two plausible alternatives first (the 2026-08-16 canonical role rename; a JWT-claim/DB-role mismatch under Shell iframe auth) by reading the actual code and live role grants before committing to the storage-RLS diagnosis.
- Fixed via migration `0240_attachments_bucket_write_policies.sql` — restores INSERT/DELETE policies against the bucket's current (private, signed-URL) shape rather than trying to restore the old public shape. Dry-run verified via a rolled-back live transaction against ehow before committing (technique precedented in migration 0208's own header). [eq-service PR #827](https://github.com/eq-solutions/eq-service/pull/827) — merged past two pre-existing, unrelated CI failures (npm-audit high-severity findings already red on `main`; the documented flaky integration-test gap), confirmed both were failing on `main`'s own latest run at the same commit before overriding.
- Dispatched `apply-service-migrations` on Royce's explicit "yes, dispatch now" — completed successfully; verified live via both the migration ledger (`service._eq_migrations`) and a direct `pg_policies` re-query showing `attachments_tenant_insert`/`attachments_tenant_delete` now exist.
- Broader review of the same feature on Royce's ask ("anything obvious we can improve? are the files secure?"): read the full upload/delete/signed-URL action, the component that renders attachments, the `/api/portal/download` route, live bucket config, and the full `app_data.attachments` RLS policy set — including chasing down and *ruling out* a false-positive: a `staff_conversation`-only SELECT policy looked at first like it could be silently bypassed by a broader co-existing permissive policy, until checking the `permissive` column directly showed it's RESTRICTIVE (AND-combined), so it genuinely narrows access as intended. Not reported as a finding since it isn't one.
- Found and fixed a real gap: `getAttachmentUrlAction` took a caller-supplied storage path with no app-layer tenant check, relying entirely on the storage RLS SELECT policy — the exact policy class just shown able to drift silently. Added a one-line tenant-prefix guard, pure defense-in-depth, no behavior change for real callers (confirmed by diffing against `origin/main` before committing). [eq-service PR #828](https://github.com/eq-solutions/eq-service/pull/828) — merged past the same two pre-existing CI failures, independently re-confirmed failing on `main` at this PR's own base commit via `gh run view`/`gh run list`.
- Spotted a genuinely separate issue while pulling security advisors for this review: `public.eq_list_quote_attachment_counts()` (a Quotes-feature function, unrelated table) flagged SECURITY DEFINER + executable by any authenticated user. Spawned as its own task chip rather than chased here; Royce started it running in a separate session (`task_d02e3485`).

## Decided
- Royce: "Confirmed, go ahead" — root-cause fix (migration 0240).
- Royce: "Merge the PR" — PR #827.
- Royce, via AskUserQuestion: "Yes, dispatch now" — apply migration 0240 to live ehow immediately (production, no approval checkpoint).
- Royce: "Reapply fix 1, leave 2 as-is for now" — take the tenant-guard hardening, explicitly defer the client-supplied-MIME-type hardening (bounded risk, real risk of breaking real uploads that can't be click-tested in this environment).
- Royce: "Merge the PR" — PR #828.

## Deferred
- Client-supplied MIME type on upload is trusted with no server-side content verification — deliberately left as-is, Royce's call.
- Evidence attachment type's UI copy advertises video support the upload allowlist doesn't include.
- Bucket-level file size limit (50MB) / no MIME allowlist is looser than the app's own (10MB + allowlist) — low priority, only matters if storage is ever written to directly.
- No real click-through test of upload/delete by an actual technician — verified end-to-end at the database/RLS level only.
- Whether other storage buckets have the same silent-drift problem — not checked, only the one bucket Royce reported on.
- `eq_list_quote_attachment_counts()` SECURITY DEFINER review — handed to a separate session (`task_d02e3485`), in progress as of this close.

Full writeup in `eq/pending/eq-solves-service.md`.

## Notes
- **Second confirmed instance of the storage/DB drift failure class this repo already tracks** (Claude memory `project_service_migration_ledger_governed_apply`; AGENTS.md's "Migrations to live ehow" section) — but a new variant. Previous incidents were DDL applied out-of-band that never reached the governed ledger. This one silently *removed* working policies with zero trace anywhere, on a Storage bucket rather than a table RLS policy. Worth treating "storage.objects policies" as its own drift-risk category going forward, not folded into table-RLS drift.
- All work happened in the pre-existing worktree `service-review-sprint-e8ef31` — branch `claude/attachment-upload-error-5f23de` for the original fix, then a fresh branch `claude/attachment-url-tenant-guard` cut from updated `origin/main` for the follow-up. Reusing the first branch post-squash-merge would have dragged the already-merged migration back into the second PR's diff.
- This close's own eq-context writes went through a fresh isolated `git clone` (not the shared checkout, not a worktree) — the shared checkout's `git pull`/`git merge` were both hard-blocked by the F9 guard when tried directly, consistent with every other entry in this file today.
- **Rebase conflict on this exact file when pushing this close**: a sibling session's SEC-72 close (entry immediately above) independently used the same "(d)" suffix for its own unrelated entry, landing between this session's fetch and its push. Resolved by keeping both entries in full — content is completely non-overlapping — and renumbering this one to "(e)".

---

# Session 2026-09-05 — SEC-73 merged, confirmed live on both repos, and closed out (continues session (o) in sessions/2026-09-04.md)

## Built
- Merged eq-shell [PR #1763](https://github.com/eq-solutions/eq-shell/pull/1763) (squash `0b37676`) then eq-context [PR #203](https://github.com/eq-solutions/eq-context/pull/203) (squash `bedc7e9`), in that order, on Royce's explicit instruction ("merge #1763 then #203").
- Confirmed the weekly `security-audit.yml` fired automatically on #203's merge push and went green ([run 33915167442](https://github.com/eq-solutions/eq-context/actions/runs/33915167442)): all four SEC-73 findings show `ACCEPTED … [SEC-73 — review_by 2026-12-04]`, ending the red streak since 2026-08-30.
- Confirmed eq-shell's change is actually live, not just merged. Netlify coalesced `0b37676`'s build with PR #1761 (an unrelated ledger-hygiene CI fix) that merged ~2 minutes later, so no deploy record names `0b37676` directly — checked commit ancestry instead: `0b37676` is a direct parent of the production deploy that published at 2026-09-04T20:24:47Z, and still an ancestor of the newest published production deploy at close time.
- Pushed a merged-confirmation update to `ops/security-register.md` SEC-73 and the (then-current) `eq/pending/eq-shell.md` item, replacing "open, not merged" language with the merge SHAs and the confirming audit run.
- This close: added the `eq/changelog/eq-shell.md` entry for #1763 itself (the guard implementation), which had no changelog entry yet — only the earlier *decision* to accept SEC-73 was logged there, via PR #201. Corrected the "eq-shell: security register reconciled" pending-section header, left stale ("2 items still open + SEC-73 ticked") after a concurrent session's close (`812fe6bc`) had already trimmed both the SEC-72 and SEC-73 bullets out from under it without touching the header.

## Decided
- Royce: "merge #1763 then #203" — explicit merge instruction, treated as this repo's standing production-deploy approval for the eq-shell half per the documented merge-is-deploy rule.

## Deferred
- Nothing new from this pass. The two things already on record as deliberately not built stay that way: project-scoped `ACCEPTED_ERRORS` keys (four instead of two, keyed by project — proposed in #203's body) and a `rls_probe.py` `KNOWN_LEAKS` review-date guard (moot today, that dict is empty).

## Notes
- Full build detail (the SEC-73 gate check, both PRs' construction, the cross-session guard request and how the two guards were built and verified) lives in `sessions/2026-09-04.md` session (o) — this entry only covers what happened after that: the merge, the two live-confirmations, and this close's substrate tidy-up.
- Another same-day, same-thread concurrency hit: this session's first `git clone` read a version of `eq/pending/eq-shell.md` where the "security register reconciled" section still carried the SEC-72 and SEC-73 `[x]` bullets; a re-fetch immediately before editing showed a sibling session's close (`812fe6bc`) had already trimmed both out (SEC-72's own changelog entry alongside it) in the few minutes since. Rebuilt this session's edit against that fresher state instead of reintroducing content someone else had just correctly removed — same reconcile-by-content discipline the sibling SEC-72 session itself used minutes earlier for the same file.
- A Bash heredoc mangled this session's own em dashes again mid-close (a Python patch script built inline via `<<'EOF'` came out with `—` replaced by `�`, breaking an anchored string match) — same failure class already on record in memory (`bash-heredoc-breaks-use-write-tool.md`). Fixed by writing the patch script with the Write tool instead, per that note; no heredoc was used for non-ASCII content for the rest of this close.
- All substrate writes this session went through isolated `git clone`s in the scratchpad, never the shared `C:\Projects\eq-context` checkout (the F9 guard blocks even a `git merge` there) — consistent with the whole SEC-73 thread across both 2026-09-04 and 2026-09-05.

---

# Session 2026-09-04/05 — EQ Core go-live week review published; 4 follow-up chips spun off and merged live; nightly digest's dead-token blindness fixed

## Built
- Full review of the EQ Core go-live week (Mon 31 Aug – Fri 4 Sep): traffic (PostHog + jvkn/ehow), code (every eq-shell commit 29 Aug → 4 Sep), security (register cross-check + live `get_advisors` + response headers), UI/UX (live click-through in Royce's own Chrome). Scored 8/6/6/5, overall 6/10. Published as an artifact: https://claude.ai/code/artifact/35d31742-59e5-49c5-839a-1877fc1ed35e.
- Headline numbers, all verified live: Shell accounts signing in 9 → 46 of 85 that week (48 by close of this session); EQ Field via Core 58 → 106 people; SKS NSW Labour 50 → 24 with timesheet saves 618 → 153 while EQ Field went 10 → 669.
- Spawned 4 background chips; Royce started all four the same evening: Field-panel stall (`task_7434eb43`), fail-closed permission denials (`task_b006ae9d`), CRM/purge/rate-limit fixes (`task_87f2c896`), security-register refresh (`task_ed155ebf` → eq-context [PR #201](https://github.com/eq-solutions/eq-context/pull/201), merged).
- Ran independent merge-readiness audits (file:line evidence, CI/staleness/correctness/test/risk) on every eq-shell PR the chips produced before Royce merged them: [#1760](https://github.com/eq-solutions/eq-shell/pull/1760) (join-door IP-then-phone rate limit), [#1761](https://github.com/eq-solutions/eq-shell/pull/1761) (duplicate migration-prefix guard), [#1762](https://github.com/eq-solutions/eq-shell/pull/1762) (fail-closed `tenant_role_overrides` denials), [#1763](https://github.com/eq-solutions/eq-shell/pull/1763) (drift-guard column allow-list, SEC-73). Royce merged all four plus two more from the same thread ([#1765](https://github.com/eq-solutions/eq-shell/pull/1765) Field-panel render-signal gate, [#1766](https://github.com/eq-solutions/eq-shell/pull/1766) same lockout fix on the phone-OTP login door) — confirmed every one live on core.eq.solutions by commit-ancestry against the newest ready Netlify production deploy (last at 20:50 UTC), not by deploy-row titles alone.
- Fixed the nightly digest's own dead-`EQ_CONTEXT_PAT` blindness (found mid-review): `refresh_digest.py` now records 401/403s, retries once with the workflow's own `GITHUB_TOKEN`, and surfaces a 🔴 Needs-you item instead of silently reporting "unknown"/"no merges". Wired Product Pulse's "Active users" row to jvkn `shell_control.users` (was a hard-coded "blocked" pointing at the wrong table). eq-context [PR #202](https://github.com/eq-solutions/eq-context/pull/202) (`12a530f3`), merged and verified live via manual `workflow_dispatch` — see `eq/changelog/eq-context.md` for full detail.
- Re-verified `eq/active.md` against live systems during the same PR (was stale since 2026-07-21) — clears the repo's own review-clock overdue count.
- Saved 3 memory notes: Royce's own-Chrome preference for live click-tests, a pointer to this review, and a correction of the Tier-2 perf memory (shipped 2026-09-03, not "next"). Wrote and then deleted one wrong memory note (a CRLF false alarm — see Notes).

## Decided
- Royce: log the review to the substrate + fix the digest's blind spots (chosen over deciding the 2FA posture or doing the security clicks in this session).
- Royce: merge #1761 then #1760, confirm live after each; then (same evening) merged #1762/#1763/#1765/#1766 as they came available from the same review thread.
- Royce: run the merge-readiness audit on the remaining eq-shell PRs before merging any of them.

## Deferred
- **Royce's own hands, all still open:** regenerate `EQ_CONTEXT_PAT`; create `NETLIFY_TOKEN`; uninstall the `grok-by-xai` GitHub App (SEC-57, re-confirmed live tonight via `gh api orgs/eq-solutions/installations` — still installed with `actions/contents/administration/workflows:write` on every repo); delete the `dev`-context value of the account-scope JWT secret (SEC-63, confirmed still open in the register); hand-check `core.eq.solutions/sks/field` on a phone and a laptop now that the code-side fix is live; write down the 2FA posture (off for everyone since 1 Sep by a hard-coded constant, no expiry). Full detail and context in `eq/pending/cross-repo.md` and `eq/pending/eq-context.md`.
- **A new SECURITY DEFINER finding, not yet a register row when first surfaced** (closed same evening by a concurrent session as SEC-73, PR #1763/#203) — `app_data.field_people_directory`/`field_managers` bypass RLS but filter tenant_id inside the view definition; the drift guard now re-checks the column list every run, not just the filter.
- **Standing process question, Royce's call:** whether a second look on auth/security PRs before merge becomes routine — exercised once tonight (all 6 merges got an audit first), not yet a rule.

## Notes
- Substrate churn tonight was extreme (eq-context moved from 2 to 12+ commits behind `origin/main` at several points, driven by other sessions' own closes and nightly crons) — every push from this session hit at least one real rebase conflict on `sessions/2026-09-04.md` (concurrent sessions reusing the next free letter) and once on `eq/active.md` itself (superseded wholesale by a more thorough same-day rewrite from another session — resolved by taking theirs, not force-pushing mine over it).
- Self-correction on the record: briefly diagnosed a CRLF problem in `eq/active.md` using `grep -c $'\r'` in this harness's Bash tool (POSIX sh — `$'...'` doesn't expand, so it counts lines containing the letter *r*, not carriage returns). A python byte count and the CI "Line endings" check both confirmed the file was clean LF throughout. Wrote a wrong memory note on the false signal, caught it, deleted it, and replaced it with the correct lesson (`crlf-check-use-python-not-grep.md`).
- PostHog's governed metric catalog (`data_catalog:read` scope) was unavailable to this connector all session — every traffic figure in the published review is a direct HogQL/SQL derivation, labelled as such, not a governed metric.
- Every Netlify "is it live" check this session used commit-ancestry against the newest `ready` production deploy, never a deploy row's own state or title — Netlify supersedes concurrent production builds (`error: Skipped`) and the superseding build still carries the earlier commit as an ancestor, so a "skipped" row is not a failure and a "ready" row's title is not proof of what's actually being served.

---

# Session 2026-09-05 (e, continued) — swept the other storage buckets for the same drift as the attachments bucket; none found

*Follow-up to session (e) above, asked after that session's own close: "sweep the other buckets for the same drift."*

## Built
- Live-queried every bucket's RLS policy set on `storage.objects` and cross-referenced against what eq-service's own code actually does with each bucket, the same technique used to find and fix the original `attachments` bug.
- `logos` (the only other bucket eq-service uses — 20 real files) is correctly configured: public read, tenant-scoped INSERT-only, matching the app's actual usage (upload + soft-delete, never a physical remove call). No drift.
- `compliance-packs`, `licence-photos`, `safety-photos` all show zero objects in `storage.objects` right now (confirmed live, not from a stale doc) and have no code anywhere in eq-service referencing them. A relevant, previously-unread eq-context doc (`eq/sprints/2026-08-13-attachment-upload-closeout.md`) independently confirms these three were already empty on 2026-08-13 on both tenant planes — these are another app's buckets sharing the same database, not eq-service's, and hold nothing at risk either way.
- Noted in passing, not touched: `licence-photos`'s policies key off `auth.jwt() -> 'user_metadata'` instead of `app_metadata` (the convention everywhere else in this system) and scope to the `public` Postgres role instead of `authenticated`. Not fixed — it's empty, it isn't eq-service's feature, and `user_metadata` is user-editable so "fixing" the claim path without knowing the owning app's actual auth flow could be actively wrong. Left as an observation for whoever owns it.

## Decided
- None needed — pure read-only sweep, nothing found that required a change.

## Deferred
- None new.

## Notes
- Closes the deferred item from session (e)'s own close ("whether other buckets have the same problem — not checked") — updated `eq/pending/eq-solves-service.md` to `[x]` with the result.
- This is the second time this session found eq-context substrate slightly behind reality mid-task (the 2026-08-13 sprint doc's object-count table was 3 weeks stale) — re-verified live rather than trusting it, consistent with this project's own "verify against live" standard.

# Session 2026-09-05 (f) — security-groups.ts's Preview-permissions read closed the same way (PR #1768, built + confirmed live); Access Control click-test debt consolidated

## Built
- [eq-shell PR #1768](https://github.com/eq-solutions/eq-shell/pull/1768) (`364993e3`): security-groups.ts's `user_perms` preview action (feeds AccessControlPage.tsx's "Preview permissions" tab) was the one call site #1762/#1767 explicitly left fail-open, since it's a read-only preview and nothing is authorized off it. Swapped its direct `getRoleOverridePerms()` call for `resolveRoleOverridesFailClosed()`, same 6s ceiling the other 5 call sites use, `caller: 'security-groups'`. Unlike the JWT minters this doesn't refuse on tier 3 -- a hard deny just returns 200 with every overridable key marked denied, since a degraded preview beats no preview. Updated `getRoleOverridePerms()`'s doc comment (`_shared/supabase.ts`) to drop this call site from its remaining-fail-open list. New `netlify/functions/_shared/security-groups.test.ts` (4 cases: fast / slow-past-deadline / failed / cached-after-good-read), same shape as `mint-tenant-jwt.test.ts`; `requirePerm` mocked to always-allow, same convention `crm-write-check-before-write.test.ts` already uses, so the test isolates the role-overrides read rather than `resolvePrincipal()`'s own separate fail-open path. All gates green: `tsc -b --force`, eslint, `pnpm test` (561 tests), `check-role-literals.mjs`, `check:perms`.
- Started from a fresh branch off `origin/main` rather than the session's prior branch (`claude/quotes-search-ignores-active-tab`), which was missing #1762/#1764/#1765/#1766/#1767 -- checked worktree list and open PRs first, no collision found.
- Opened the PR without merging (auth-adjacent file). Royce merged it directly minutes later. Confirmed live rather than assumed: Netlify's production build for commit `364993e3` reached `state: ready`, `published_at` 2026-09-04T22:40:29Z (339s build, no error), and a live smoke check of `verify-shell-session` returned the expected 401 post-deploy.
- Asked to check for other outstanding AccessControlPage click-test debt: found 3 separate never-checked "not click-tested live" items from three earlier shipped PRs (2026-08-17 searchable drawer, 2026-08-18 ring visual + tab strip, 2026-08-25/26 "Preview a person" honesty pass). Confirmed live via the Browser pane (navigated to core.eq.solutions, got the sign-in page) that no Shell session exists in this environment either, so none of the four -- those three plus #1768 -- could be executed here.
- Consolidated all four into one new pending item at the top of `eq/pending/eq-shell.md` (commit `45ac3c1e`, +13/-3): removed the three original scattered bullets from their sections so the same gap stops being tracked four times, and applied the archive-trim rule to the two of those three sections that still carried an italic intro paragraph (2026-08-17, 2026-08-18) now that each is down to a single open item.
- Both pending.md writes went through the GitHub Contents API directly (read current blob SHA, PUT with that SHA) rather than any local checkout -- this file was under continuous concurrent write the entire session (a sibling session updated the same file's SEC-72 bullet mid-task, caught cleanly by a SHA mismatch on the first push attempt and safely retried against the newer content with nothing lost).

## Decided
- None needing Royce's input this session -- the fail-closed pattern and its policy (live -> cached denials -> hard deny) were already established and approved via #1762; this only extended it to one more, lower-stakes call site the original sweep had explicitly deferred.

## Deferred
- The consolidated click-test item itself: needs Royce, or anyone with a live Shell session, to spend roughly 10-15 minutes in Access Control confirming all four checks at once. No Shell credentials available anywhere in this environment.
- Noticed, not this session's work: mid-task, a concurrent sibling session (rooted at the same `C:\Projects\eq-shell` path, so sharing this session's own memory directory) updated this session's `role-overrides-fail-open-under-latency.md` memory file to report that Royce delegated the `resolvePrincipal()` decision outright, the call was ADOPT, and it's implemented in [PR #1770](https://github.com/eq-solutions/eq-shell/pull/1770). Confirmed only that #1770 genuinely exists and is open (`gh pr view`) -- not reviewed or verified further by this session; noted here so it isn't lost, not claimed as this session's finding.

## Notes
- This session and at least one concurrent sibling (its own close is logged as "(d)" immediately above) share both the eq-context substrate and, being rooted at the same project path, the same Claude memory directory. The sibling's close already covers the eq-shell changelog entry for #1762/#1767/#1768 in full, including PR #1768's mechanism -- not duplicated here or in `eq/changelog/eq-shell.md`.
- Consistent with every other session logged today: the shared `C:\Projects\eq-context` checkout was left untouched throughout -- read only via `git fetch` + `git show origin/main:<path>`, written only via an isolated clone or the GitHub Contents API, never a `pull`/`merge`/`commit` in place.
- **Same collision pattern as the entry two above ("(d)"->"(e)" rename), one letter later**: an eq-solves-service session's own close landed on this file between this session's clone and its push, independently claiming the same "(e)" suffix. Resolved the same way -- kept both entries in full (non-overlapping content), renumbered this one to "(f)". A SEC-73 close session then landed a third time before this push finally went through -- no letter collision that time (its entry used no suffix), but four separate drifts inside one `/close` pass on this exact file, three of them genuine content conflicts, is worth flagging as its own data point: this file is currently the single hottest concurrent-write surface in the repo.
- **A real data-loss mistake happened and was caught and fixed, not just risked** -- worth recording in full rather than glossing over. `EQ_SKIP_STALE_MAIN=1` inline on the local `git commit` never reached the guard (it's a PreToolUse hook pattern-matching the Bash command, not a real git hook reading the child process's environment), so the local-commit path was abandoned for two sequential GitHub Contents-API PUTs instead. The first PUT for this file sent a locally-merged copy that had gone stale between the last `git fetch` and the API call, silently overwriting the live "EQ Core go-live week review" entry -- SHA-matched, so the API accepted it (a Contents-API PUT replaces wholesale; matching the base SHA only proves no OTHER write raced in between the read and the PUT, it says nothing about whether the content being sent already reflects everything currently live). Caught by diffing the pre-push blob against the post-push content rather than trusting the green response. While rebuilding the fix, ANOTHER session ("(g)", the resolvePrincipal()/SEC-74 close) appended its own entry on top of the already-bad file before the correction could land -- caught that too, via a second pre-push SHA check, and did the final rebuild from the truly-current live content (SEC-53 through "(g)") with only the missing go-live-week entry re-inserted in its original position, rather than from an older local copy. Net effect once fully corrected: zero content lost, but only because every write was diffed against live before being trusted, not because the write pattern itself was safe by construction.
---

# Session 2026-09-05 (g) — SEC-72 thread: executed the merges, closed the last two residuals, got Royce's explicit call on `resolvePrincipal()` — see also the CONFLICT flagged in Notes below

## Built
- Executed the actual merge of eq-shell [PR #1762](https://github.com/eq-solutions/eq-shell/pull/1762) on Royce's explicit "merge #1762" (`gh pr merge` failed on the standard root-checkout worktree contention; used `gh api -X PUT .../pulls/1762/merge` instead, squash `6c6ec81b`). Its own deploy record came back `state: error`/`"Skipped"` (superseded by a concurrent merge, #1766); confirmed live via `git merge-base --is-ancestor 6c6ec81b ff756c0c` plus `published_at` on the deploy that actually published, and a live 401 from `core.eq.solutions/.netlify/functions/verify-shell-session`.
- Asked to "pick up `task_cff561fd`" (the chip for the 3 remaining minters): verified first rather than assuming — [PR #1767](https://github.com/eq-solutions/eq-shell/pull/1767) (squash `726ba3ef`) was already merged and live, built and merged directly by Royce in a separate session. Read the full diff myself before reporting it done (correct fail-closed handling per endpoint, and the `tenant-data-proxy.ts` refusal-before-cache-write ordering specifically).
- Reviewed and executed the merge of a 6th call site found during that work, [PR #1768](https://github.com/eq-solutions/eq-shell/pull/1768) (`security-groups.ts`'s permissions preview, squash `364993e3`) on Royce's "merge #1768" — read the diff, confirmed CI green, merged via the API, confirmed live directly (published without a supersede this time).
- Reviewed eq-context [PR #211](https://github.com/eq-solutions/eq-context/pull/211) (a sibling session's SEC-72 closure) against the actual diff before relaying it, then executed its merge on "merge #211" (squash `e9ac2d6b`).
- Authored and merged eq-context [PR #213](https://github.com/eq-solutions/eq-context/pull/213) (squash `5e328a5b`, "merge #213"): #211 had gone stale within about two hours of its own merge, since #1767/#1768 closed the exact residuals it had just recorded as still-open. Updated the SEC-72 row and the pending-doc line with the real commits, not a restatement of the earlier claim.
- Presented the `resolvePrincipal()` trade-off to Royce directly via a structured question (not left as a default follow-up either way): adopt the same fail-closed resolver everywhere (~124 `requirePerm()` call sites), fix only the highest-risk subset, or record it as accepted risk. Royce chose accepted risk.
- Authored and merged eq-context [PR #214](https://github.com/eq-solutions/eq-context/pull/214) (new SEC-74 row, review_by 2026-12-05, squash `db45e0fc`) recording that decision. Hit a genuine merge conflict along the way — a concurrent session's own close (`812fe6bc`) pruned the exact two pending-doc lines this PR's insertion was anchored to, in the few minutes between opening and merging. Resolved by force-resetting the branch to the new `main` tip and re-pushing both files against the current content (no local clone touched) — the reset briefly made the branch byte-identical to `main`, which GitHub read as an empty diff and auto-closed the PR before the corrected commits landed on top. Caught it, reopened, confirmed the branch's real content and the PR's clean/mergeable state, then merged.
- Consolidated `role-overrides-fail-open-under-latency.md` (Claude auto-memory, not this substrate) — a file this session and the sibling session had both been appending to in parallel all day, grown to 138 lines of repetitive blow-by-blow. Rewrote it to the load-bearing facts once the thread was fully closed. Wrote a new memory note (`concurrent-session-shared-memory-file-race.md`) recording the underlying cause: Claude Code's per-project memory files are shared by project path, not by session, so any two sessions rooted at the same directory read and write the identical files with no coordination.

## Decided
- Royce: "merge #1762", "merge #1768", "merge #211", "merge #213", "merge #214" — five separate explicit merge instructions across two repos, each given only after the corresponding PR was presented with its diff, test plan, and CI state.
- Royce, via a direct question rather than a default: `resolvePrincipal()` stays fail-open, recorded as an accepted risk with a 2026-12-05 review date (eq-context SEC-74) — not fixed, not silently deferred.

## Deferred
- Nothing new. SEC-74's review_by 2026-12-05 is the only future trigger this session created.

## Notes
- **The actual code for #1762/#1767/#1768 was authored and reviewed by a sibling Claude session** ("Fail closed on denials when tenant_role_overrides read times out", rooted at the same `C:\Projects\eq-shell` path, communicating throughout via cross-session messages) — this session's distinct contribution was independent verification of the code before every merge, executing the merges themselves once Royce approved each one, live-deploy confirmation via the ancestry method (twice — #1762's own deploy record was skipped, #1768's was not), and authoring the two eq-context PRs (#213, #214) that keep the register accurate as the finding evolved same-day.
- **Two tooling gotchas worth keeping**, both hit directly this session, neither yet on record anywhere: (1) `gh api` rejects a large base64-encoded file body passed inline as `-f content=...` with a plain shell-level "Argument list too long" on Windows — happens before `gh` itself runs, so it fails silently as far as the API is concerned (nothing reaches GitHub). Fix: write the full JSON request body to a file and call `gh api --input <file>` instead. (2) Force-updating a PR's head branch ref to exactly match the base branch — even as a deliberate, momentary step toward re-pushing corrected content on top — can make GitHub auto-close the PR as having an empty diff, before the follow-up commits land. If a conflict needs resolving via a ref reset rather than a real rebase, push the corrected content in the very next call and check the PR's `state` immediately after; `gh pr reopen` recovers it cleanly if it did close.
- Same discipline as the rest of this thread: every "done" claim from a sibling session or a shared memory file was re-verified against `gh pr view`, `git log`, or the live Netlify deploy record before being acted on or relayed to Royce — including one case (`task_cff561fd`) where the claim turned out to be accurate, and the concurrent-memory-file duplication, where a sibling's "I trimmed it down" claim turned out not to match the file's actual state.
- **Unresolved conflict surfaced by a rebase, not by this session's own investigation — flagging rather than silently picking a side.** The entry immediately above this one (also independently titled "(f)", renamed to "(g)" here to keep both) reports that a concurrent sibling session's own memory file update claims Royce gave a DIFFERENT instruction on `resolvePrincipal()` than the one this session received: not "leave as-is" but "adopt the fix everywhere," implemented in eq-shell [PR #1770](https://github.com/eq-solutions/eq-shell/pull/1770). That entry explicitly says it only confirmed #1770 exists and is open, not that the underlying claim is accurate. This session's own record of Royce's decision (accepted risk, SEC-74, review_by 2026-12-05) came directly from a structured question asked in this conversation, not relayed secondhand — but two PRs implementing opposite decisions about the same function cannot both be right, and whichever merges last will silently overwrite the other's premise in the register. Needs Royce to say which instruction stands before either #1770 merges or SEC-74's "accepted risk" framing is treated as settled. Not resolved by this session — surfaced here specifically so it isn't lost in a git conflict resolution.

---

# Session 2026-09-05 (h) — eq-solves-service: Supabase advisor triage on eq_list_quote_attachment_counts() widened to the whole pricing/quote-audit RPC family; found and fixed a real gap on the audit writer

*Started as a single-function check on the SECURITY DEFINER advisor finding `public.eq_list_quote_attachment_counts()` (task `task_d02e3485`, spawned by a sibling eq-solves-service session's own attachment-upload review — see that session's "(e)" entry earlier this file). Widened on Royce's follow-up ask to cover the rest of the `eq_*` pricing/quote-audit RPC family on ehow.*

## Built
- Nothing built directly by this session — pure investigation, cross-session coordination, and one spawned task. The actual code (migration + PR) was authored by the sibling session that picked up the spawned task, below.
- Confirmed `eq_list_quote_attachment_counts()` (eq-shell, migration `0216_quote_status_taxonomy_remaining_rpcs.sql`) is correctly tenant-scoped via the JWT `app_metadata.tenant_id` claim, takes no parameters, and has `search_path` pinned — SECURITY DEFINER + `authenticated` EXECUTE is intentional here, same pattern used suite-wide, not a leak.
- On Royce's follow-up ask, reviewed all ~13 `eq_*` pricing RPCs (`eq_get_pricing_config`, `eq_get_pricing_matrix`, `eq_list_pricing_bands/materials/products/products_full`, `eq_upsert_/eq_archive_pricing_*`, `eq_replace_pricing_bands`, `eq__assert_pricing_role`/`_view_role`) plus `eq_list_quote_audit` — all correctly tenant-scoped and role-gated. One asymmetry (`eq_list_pricing_products` skipping the view-role gate its siblings use) checked out as deliberate against migration `0246_ops_view_rates_setup_gate.sql`'s own explicit enumeration of the nine RPCs that needed the gate — not a gap.
- Found a real one: `public.eq__log_quote_audit()` had a live `authenticated` EXECUTE grant on ehow, directly contradicting its own migration's comment (`0090_quote_audit_and_edits.sql`: "intentionally NOT granted to authenticated: internal helper only"). No role gate, no check that `p_quote_id` belongs to the caller's tenant — any authenticated user could forge a `quote_audit` row against any quote_id they name, with arbitrary `action` text and `actor_initials`. Audit-trail spoofing, not a cross-tenant read leak (forged rows carry the attacker's own real tenant_id, so they don't surface in another tenant's own reads).
- Spawned `task_a77ca1b0` for the fix. Royce started it in a separate local session (`local_13f672f6-05ab-424e-9d96-42b8d3ff0211`) almost immediately — in the same turn, before seeing that, this session raced a duplicate general-purpose agent against it; caught via the `dismiss_task` response ("already started by the user") and killed the duplicate before it touched any files. Lesson: don't fire `dismiss_task` and a replacement `Agent` call in the same parallel batch — "start it now" can itself be the chip-click's own auto-message, meaning the real session is often already running by the time you ask.
- Checked the sibling session's progress twice (`search_session_transcripts` + `list_events`, since it's a separate local session, not a subagent). It ran its own session brief, independently re-verified the grant live on **both** ehow and zaap (extending the finding — the original check only covered ehow), found eq-shell's actual sanctioned migration path (`tenant-migrate.yml`, workflow_dispatch, production-gated, running `migrate-tenants.mjs` — hand-applying via Supabase MCP is the named anti-pattern there too), and wrote migration `0302_revoke_eq_log_quote_audit_authenticated.sql` off the `0245_revoke_eq_update_staff_authenticated.sql` precedent. Confirmed via `gh pr view` (not just the sibling's own memory-file claim) that [eq-shell PR #1771](https://github.com/eq-solutions/eq-shell/pull/1771) genuinely exists, OPEN, not yet merged.

## Decided
- Royce: "check the pricing and audit RPCs too" — widened the investigation's scope.
- Royce: "start the eq-shell fix task now" — started `task_a77ca1b0` (in its own session, not this one).

## Deferred
- **PR #1771 not yet merged.** Merging eq-shell `main` auto-deploys core.eq.solutions in minutes — a real production-deploy approval, not just a code review. Full detail in `eq/pending/eq-shell.md`.
- **Even after merge, the migration isn't live** — a separate `tenant-migrate.yml` dispatch (production-gated) is required to actually apply it to ehow + zaap, per eq-shell's own migration governance. Not done by either session as of this close.
- **Post-apply verification not done**: re-run advisors, re-check `pg_proc.proacl`, smoke-test a real caller.

## Notes
- **ehow's Supabase security-advisor list is not eq-solves-service-exclusive.** ehow is a shared physical Supabase project — eq-shell's own `eq_*` Quotes/CRM/pricing RPC layer (`tenant-migrations/*.sql`, numbered ~0200-0300+) lives on the same project and throws its own advisor findings. Before spending time hunting an ehow advisor finding inside eq-solves-service's own migrations, check the function's name against eq-shell's RPC namespace first (`eq_list_*`, `eq_quote_*`, `eq_match_*`, `eq_pricing_*`, `eq__*` are eq-shell's). Recorded for now only in eq-solves-service's own Claude memory (`project_advisor_security_baseline.md`) — worth promoting to a shared cross-repo note if it keeps causing wasted lookups.
- Sessions rooted at the same project path share the same Claude memory directory, not just this substrate — the sibling session (`task_a77ca1b0`, also rooted at `C:\Projects\eq-solves-service`) updated this session's own `project_eq_shell_quote_audit_grant_drift.md` memory file directly with its findings (PR number, zaap confirmation) mid-flight. Consistent with the same discovery other sessions logged earlier today for eq-shell.
- Checked `eq/pending/eq-solves-service.md` for the `task_d02e3485` handoff item session (e) said it wrote up there — not found under that task id or the function name. Not re-created here since the investigation is now fully concluded (safe, no fix needed for that specific function); the one genuinely open item from this thread (PR #1771 + dispatch) is tracked in `eq/pending/eq-shell.md` instead, where the actual fix lives.

---

# Session 2026-09-05 (s) — Field-panel P1 closed out: hand-verified live, one small new bug found and spun off

## Built
- Hand-verified `core.eq.solutions/sks/field` live (Royce's own Chrome), twice, on a completely fresh tab with a full page reload each time — the check the review's own P1 finding had been waiting on since 4 Sep. The handoff completes in ~4 seconds with the full telemetry chain firing clean, no errors: `mint-start → mint-ok → src-set → boot → iframe-load → accepted → rendered`. `?tab=dashboard` (headcount, leave, birthdays, licence-missing) and `?tab=schedule` (real roster data) both render correctly with live data. Closed the item in `eq/pending/cross-repo.md`.
- Found one separate, much smaller bug in the process: `/sks/field` with no `tab=` param defaults to `?tab=home`, which EQ Field's own tab router has no case for — the handoff still completes and `rendered` still fires, but the content pane stays blank until the person clicks a real nav item. Confirmed at full resolution (not a screenshot-scale artifact — an initial low-res screenshot briefly looked identically blank for the working `schedule` tab too, caught by re-checking at scale 1 before concluding anything). Only affects a first-time visit with no deep link; spun off as `task_5c33a025`.
- Attempted to locate and check the actual PR output of the originally-spawned `task_7434eb43` chip (the one this session's P1 verification was meant to close) after its own completion notification arrived — could not find a distinctly-matching PR or session transcript among tonight's very high volume of concurrent activity (`list_sessions`/`search_session_transcripts` came back empty for its task id, only cross-references from this session's own earlier pending-file writes). Most likely explanation, not confirmed: the underlying bug was already fixed by two unrelated organic sessions (eq-shell #1758/#1765, eq-field #917) before the chip could act, and it declined to duplicate — the live hand-check above answers the substantive question either way.
- Kicked off a post-merge audit of 4 more eq-shell PRs from the fail-closed thread that landed after my own #1762 review and that I hadn't independently checked (#1764, #1767, #1768, #1770 — closing the remaining gaps my own #1762 report had flagged as out of scope, including the higher-risk `resolvePrincipal()` conversion). Still running as of this entry.

## Decided
- Royce: "check the other two chip PRs" — read as covering both the just-finished Field-panel chip's outcome and a fresh look at auth-adjacent PRs from the fail-closed thread I hadn't yet reviewed, given the ambiguity in which "two" was meant amid tonight's volume.

## Deferred
- `task_5c33a025` (the `tab=home` default-routing bug) — not built this session, chip spun off with full repro steps.
- The post-merge audit of #1764/#1767/#1768/#1770 was still running when this entry was written — result to follow.
- Never definitively identified which session (if any) task_7434eb43 actually was, or what its own final report said. Not chased further — the live re-verification made the question moot for Royce's purposes.

## Notes
- **A real near-self-miss on this session's own part**: the very first screenshot of `/sks/field?tab=schedule`, taken at 0.6 scale, looked identically blank to the genuinely-broken `tab=home` case — real content (a populated weekly schedule grid) only became visible on a `zoom` into the region and again at a full-resolution scale-1 screenshot. Concluding "still broken" from the low-res screenshot alone would have been a false regression report. Worth remembering: a blank-looking downscaled screenshot of a pale/light-themed page is not proof of no content — check at native resolution (or zoom in) before reporting a UI as broken.
- `list_sessions`/`search_session_transcripts` (the `mcp__ccd_session_mgmt__*` tools) list Royce's own visible Claude Code sessions/worktrees, not a separate ledger of `spawn_task` chip ids — searching by a task id only surfaces sessions whose own transcript happens to quote that id (e.g. from reading a pending.md file that mentions it), not the chip's own session. No reliable way found this session to go from a `task_...` id to its originating session after the fact once it's aged out of the visible list.

---

# Session 2026-09-05 (t) — eq-shell: built, merged, and dispatched the eq__log_quote_audit grant-drift fix that task_a77ca1b0 was spawned for

*This session is `task_a77ca1b0`, spawned by the sibling eq-solves-service session's own investigation (see its "(h)" entry above). That entry's own Deferred section left three things open at its close: PR #1771 unmerged, the migration not yet dispatched, and post-apply verification not done. This session did all three — recorded here since (h) predates that work and can't cover it.*

## Built
- Independently re-verified the finding live before writing anything, rather than trusting the handed-off brief at face value: confirmed via direct `pg_proc.proacl` on both ehow and zaap that `public.eq__log_quote_audit()` held a live `authenticated` EXECUTE grant, contradicting migration `0090`'s own comment.
- Found the task's own worktree already reserved and empty (`eq-shell-quote-audit-grant-wt`, branch `fix/quote-audit-grant-drift`, zero commits ahead of `origin/main`, no PR yet) — worked there rather than opening a competing branch.
- Ran `/brief eq-shell` properly after a brief-gate hook blocked the first file write (a brief written only in chat doesn't satisfy the gate). Royce confirmed with "go".
- Wrote migration `0302_revoke_eq_log_quote_audit_authenticated.sql` off the `0245_revoke_eq_update_staff_authenticated.sql` precedent, opened [eq-shell PR #1771](https://github.com/eq-solutions/eq-shell/pull/1771).
- Royce: "merge it" — checked CI (all green: typecheck/test/lint, gitleaks, migration-ledger hygiene, function-grants-preserved, schema-drift/anon-grant/policy-lint) and mergeable state first, then squash-merged (`3ea4cbad`).
- Checked what merging actually triggered before assuming anything was fixed, rather than conflating merge with deploy: `tenant-migrate.yml`'s `push` handler only posts a "ready to dispatch" PR comment — the real `apply` job is `workflow_dispatch`-only, by design with no approval pause (the workflow's own header: "only Royce triggers this, deliberately" is the safeguard, not a second click). Asked via AskUserQuestion rather than assuming "merge it" implied "and apply it live"; Royce: dispatch now, whole fleet. Dispatched (run `33931143462`), succeeded in ~15s.
- Re-verified live via `pg_proc.proacl` on both planes post-dispatch: `authenticated` gone, only `postgres`/`service_role` remain, on both ehow and zaap.

## Decided
- Royce: "go" (confirmed the session brief), "merge it" (PR #1771), and "yes, dispatch now (whole fleet)" (`tenant-migrate.yml`) — three separate explicit approvals for the review, the production deploy, and the live DB apply respectively.

## Deferred
- Did not re-run `get_advisors` (security) post-fix, and did not click-test a real caller live (no Shell session in this environment) — relied on the `proacl` re-check plus the deterministic Postgres PERFORM/SECURITY DEFINER privilege mechanism instead. A concurrent closing pass (see Notes) separately did the equivalent live-function-body spot-check on `eq_set_expires_at`.

## Notes
- **This session's Claude memory directory is shared with the spawning sibling session** (`C:\Users\EQ\.claude\projects\C--Projects-eq-solves-service\memory\`, both rooted at `C:\Projects\eq-solves-service`) — the sibling's "(h)" entry confirms it read this session's progress mid-flight via the shared `project_eq_shell_quote_audit_grant_drift.md` file rather than any direct session link. Deleted that file once the fix was confirmed live, per its own "delete once confirmed" note, and added `feedback_dismiss_task_race_before_relaunch.md` — the same dismiss_task/relaunch-race lesson (h) narrates in prose, now persisted as a standing memory rather than left only in a session transcript.
- **Substrate had already been closed out from underneath this entry by the time this session reached its own `/close`.** Commit `da38f117` ("chore(eq-shell): close out eq__log_quote_audit grant-drift fix") had already updated `eq/changelog/eq-shell.md` and archived the `eq/pending/eq-shell.md` section into `eq/pending-archive.md` — accurately, independently re-derivable from the same live GitHub/DB facts this session also checked, and not redone here. But it cites this file's "(h)" entry for full detail, and (h) itself predates the merge/dispatch and explicitly lists them as still open — this entry is what actually covers who did that work and how, since (h) couldn't. Discarded this session's own now-superseded working-tree edit to `eq/pending/eq-shell.md` (made before discovering `da38f117`) rather than commit it on top.
- All eq-context writes for this close went through an isolated `git clone` in the scratchpad, never the shared `C:\Projects\eq-context` checkout — hit the same F9 `git merge` block documented everywhere else in this file when trying just to fast-forward the shared checkout for a clean read.

---

# Session 2026-09-05 (u) — Go-live review: 4 eq-shell findings fixed and merged live; duplicate re-vendor PR closed

## Built
- Fixed and shipped 4 findings from a 2026-09-04 code review of eq-shell `origin/main`, each verified against live code before touching anything, each in its own PR with a negative-proof unit test (fails on the pre-fix code, passes on the fix):
  - `netlify/functions/crm-write.ts` — `update_site`/`add_site` validated a chosen contact only *after* already writing site/role changes; now every contact is validated up front, before any write. [PR #1757](https://github.com/eq-solutions/eq-shell/pull/1757), merged `15a14f92`.
  - `netlify/functions/_shared/staff-employment-purge.ts` — dependent-table deletes carried no tenant/org predicate and swallowed delete errors; now every delete is explicitly tenant- or org-scoped, and a `workers` row owned by a different login is refused rather than deleted. [PR #1759](https://github.com/eq-solutions/eq-shell/pull/1759), merged `5db0d39e`.
  - `netlify/functions/shell-join-tenant.ts` — rate-limited by phone number only, checked *after* token validation, so an attacker could burn a real phone's window without ever presenting a valid token. Now an IP-keyed bucket gates before validation; the phone-keyed bucket stays after. [PR #1760](https://github.com/eq-solutions/eq-shell/pull/1760), merged `b8802ec4`. Live-verified end-to-end: 6 real HTTP POSTs against the deploy preview with distinct phone numbers, cross-checked against live jvkn `rate_limit_buckets`/`audit_log` — only an IP bucket existed, zero phone buckets, matching intended behaviour.
  - `scripts/check-migration-hygiene.mjs` — no CI guard against duplicate numeric migration prefixes; added a check comparing new files' prefixes against existing ones and against each other within the same PR. [PR #1761](https://github.com/eq-solutions/eq-shell/pull/1761), merged `7fd42e70`. Closes the "migration-number collision spotted, not fixed" item from 2026-08-30.
- All four confirmed live via commit-ancestry check against the current production deploy (`git merge-base --is-ancestor`), not inferred from "merged."
- On request, checked deploy status only (not code) for 6 unrelated PRs from other concurrent sessions this window — #1758, #1762, #1763, #1764, #1765, #1766 — all confirmed merged and live.
- Diffed the two open `chore(intake): auto re-vendor eq-intake/eq-platform` PRs (#1753, #1756) and found them byte-identical (same 5 vendored files, same +97/-73) — they differ only in which stale base commit each was cut from. Closed #1753 as a duplicate of #1756, with a comment.

## Decided
- Royce: "merge #1757 and #1759" — explicit go-ahead for the two non-auth fixes.
- Royce: "merge if safe" — read as covering #1761 (CI-only); #1760 was held back and put to Royce directly since it changes an auth door.
- Royce, via AskUserQuestion: "Yes, merge #1760 now" — explicit approval for the auth-door PR.
- Royce: "close #1753 as a duplicate" — executed directly.

## Deferred
- 3 of the 4 fixes (#1757, #1759, #1761) verified only via `tsc -b --force` + eslint + `pnpm test` + negative-proof tests, not a real click-through. Logged in `eq/pending/eq-shell.md`.
- PR #1756 (the surviving re-vendor PR) still open, not reviewed for content.
- The auto re-vendor workflow appears not to check for an already-open PR before opening a new one (two PRs, two consecutive days, identical change) — not investigated further.

## Notes
- A plain `git diff origin/main branch` is the wrong comparison once `main` has moved since the branch was cut — it renders every intervening commit's content as a false "deletion." The correct comparison (and what GitHub's own PR view uses) is the three-dot `git diff origin/main...branch`, from the merge-base. Both re-vendor branches were 13–15 commits behind `main`, which is what made the naive two-dot diff look like the PRs deleted thousands of lines of unrelated code.
- This eq-context clone showed "behind 14" → "behind 17" → "behind 21" in the session-gate/prompt hooks over the course of this session, but a direct `git fetch` + `git rev-list --left-right --count HEAD...origin/main` at close time showed 0/0 exactly. The hook's figure looks like a point-in-time snapshot rather than a live fetch — worth treating as "reconcile before trusting" rather than a literal live commit count.

---

# Session 2026-09-05 (v) — eq-field: timesheet approver initials fixed and merged live; leave requests checked for the same bug and found clean

## Built
- Royce asked whether the approver's initials could be shown on an approved timesheet in Core's SKS Timesheets view. Investigated before building — the plumbing already existed (an initials-chip pattern on the mobile view, a name in the desktop button's hover tooltip) but was silently broken.
- Root-caused live against ehow, not assumed: `app_data.timesheets.approved_by_user_id` is stamped by a DB trigger (`eq__guard_timesheet_status`) from the approving manager's Shell/Core login identity (`app_data.staff.user_id`). The client (`timesheets-adapter.js`) resolved it as if it were `staff_id` — a different id space that can never match. Confirmed: 100% of 103 currently-approved SKS timesheets had real, correct attribution the app couldn't read back (Royce Milmlow, Ian Marston, Simon Bramall, Jack Cluff — all real, current people, none matching any `staff_id`).
- Fixed entirely client-side, no migration: a new `user_id -> name` map in `leave-adapter.js` (`staffUserIdToName`), fed by adding `user_id` to the existing shared `people` fetch in `supabase.js` (already exposed on `app_data.field_people`). `timesheets-adapter.js`'s read side now uses it. Also removed the dead write-side "guess the approver's staff_id from their name" logic — the DB trigger unconditionally overwrote it anyway, and its failure path was firing a false "attribution not linked" toast at supervisors. Desktop Approved button now shows initials directly instead of only a blank hover tooltip.
- eq-field [PR #921](https://github.com/eq-solutions/eq-field/pull/921), shipped as v3.5.678, merged, confirmed live on `field.eq.solutions/sw.js`.
- Real merge-time collision: PR #920 (an unrelated Timesheets error-handling fix) landed on `main` first, also claiming v3.5.677 and also touching `timesheets.js`. Rebased, resolved both real conflicts (`docs/reflection-log.md`, `index.html`), caught two things a plain merge would have missed silently: the version-stamp files (`app-state.js`, `sw.js`) had auto-merged to the *other* PR's identical edit rather than this one's, and the two PRs' combined line count pushed `timesheets.js` 4 lines past its file-size ratchet (bumped 2550 → 2600 in `eslint.config.js`, documented). Re-verified full test suite + lint + cache-buster check clean before force-pushing and merging.
- Asked to "fix the same gap for leave requests" — investigated before assuming the premise was true. It wasn't: `app_data.leave_requests.approver_id` is already correctly resolved to `staff_id` by its own trigger (`eq__guard_leave_status`, via `eq__caller_actor_staff_id()` — a more careful design than the timesheets trigger, which stamps the raw actor id). Verified live: all 17 populated `approver_id` rows resolve cleanly to real names already.
- **Root cause of the 30 blank-approver rows got corrected mid-conversation, worth recording as-it-happened.** First pass claimed those 30 were live emailed magic-link approvals that structurally never capture identity — drawn from a migration file's comment, never checked against the actual rows. Royce pushed back with a screenshot showing several populated approvers, then supplied the real explanation directly ("we manually moved leave from sks-nsw-labour"). Re-checked properly via `imported_from`/`imported_at`: 29 of the 30 carry `imported_from = 'nspb-phase3-2026-07-05'`, all with the identical `created_at`/`decided_at` timestamp — one bulk import batch, exactly as Royce said. The last 1 predates the trigger that stamps `approver_id` even existing. Zero of the 30 are actually caused by a live magic-link approval — that theory was wrong for every single row it was meant to explain.

## Decided
- Royce: "Fix identity + add visible initials" (via AskUserQuestion, choosing the recommended option over a cosmetic-only or identity-only fix).
- Royce: "merge it" — explicit go-ahead, executed after rebase/re-verification.
- Royce, on the leave-requests gap (before the root-cause correction above): "Leave it as-is" — no code change. Still the right call after the correction, now for a cleaner reason: it's one-time historical import data, not a live product gap.

## Deferred
- Nothing outstanding on leave requests — the blank approvers are accounted for (historical import + one pre-infrastructure row), not a live gap needing a fix. Corrected finding logged in `eq/pending/eq-field.md`, `eq/changelog/eq-field.md`, and a project memory so the earlier wrong theory doesn't get re-cited.
- Not click-tested live by a person — standing Core-only sandbox limitation (no Shell/Core session available in this environment). Verified instead against real production data (the live query results above), which is a stronger check for this specific class of bug (a resolution-table mismatch) than a single manual click-through would have been.

## Notes (addendum)
- The self-correction above is itself worth flagging as a pattern: a plausible-sounding explanation copied from a migration file's comment was stated as fact without checking it against the rows it was meant to explain — the same discipline ("verify live, don't trust a committed doc") that caught the *timesheets* bug's root cause wasn't applied a second time to the *leave* investigation's own conclusion, in the same session. Caught only because Royce checked a screenshot against the claim, not by independent re-verification. Worth remembering: finishing one root-cause investigation carefully doesn't mean the next one in the same session gets the same rigor by default.

## Notes
- The committed copy of the 2026-07-21 migration that created the timesheets/leave approval triggers is stale versus what's actually live — it still reads `eq__caller_uid()`/the raw `sub` claim, while the live function reads `eq__caller_actor_uid()`/the JWT actor-metadata claim (a later, uncommitted-to-that-file evolution). Same "committed migration ≠ live" gotcha this repo's own CLAUDE.md already warns about for shared `app_data` objects — pulled `pg_get_functiondef` live rather than trusting the file, which is what caught it.
- `gh pr merge --squash --delete-branch` failed locally on `fatal: 'main' is already used by worktree at ...eq-field-lazy-tab-gaps-wt` — the squash-merge itself had already succeeded on GitHub by the time that error fired (confirmed via `gh pr view --json state,mergedAt`); only the local post-merge branch-switch/cleanup step failed. Deleted the now-merged remote branch manually afterward. Worth remembering this specific error string doesn't mean the merge failed.
- Full technical detail: `eq/changelog/eq-field.md` (2026-09-05 entry).

---

# Session 2026-09-05 (w) — Cross-repo Sentry sweep: eq-shell auth-stall fixed live, eq-field timesheet-403 message fixed live, 2 data cleanups closed

## Built
- **eq-shell PR #1764** (`d434a407`, merged, live): `verify-shell-session.ts` fires 11 Supabase reads concurrently; #1736 (2026-09-02) bounded 4 best-effort reads with `withDeadline()` but explicitly left the other 7 gating reads (user, revocation, memberships, tenant, entitlements, tenant_config, tenant_routing) unbounded — Sentry EQ-SHELL-T/EQ-SHELL-V kept firing after that merge. New `withDeadlineOrReject()` (rejects on timeout with a distinct error instead of a made-up fallback, so a timeout lands on each read's own already-correct rejection handling) wraps all 7. Real conflict with #1762 (a same-day, same-function fail-closed role-overrides rework that merged mid-review) resolved by hand — verified the two changes touch independent array slots before trusting the auto-merge; one broken test from #1762's own module mock fixed in a follow-up commit.
- **Full Sentry sweep across all 5 `eq-solutions` projects** (eq-shell, eq-field, eq-cards, eq-solves-service, eq-quotes) — the other two are clean. Registered eq-shell's own Sentry MCP mid-session (didn't exist yet) to do this.
- **eq-field PR #920** (v3.5.677, `16350ea8`, merged, live): Sentry EQ-FIELD-1H, found live and 2 minutes old mid-sweep. A real SKS user hit the DB's own "only a manager can reopen an approved timesheet" 403 and got a misleading "check connection" toast instead of the real reason, plus a genuinely unhandled promise rejection from the same root cause (`_tsFlushRow()`'s `.catch()` was attached to a derived promise, not the one its callers actually `await`). Fixed both: the thrown Error now carries the real `.pgMessage`, and the two call sites (`saveTsCell`/`saveTsCellsBulk`) no longer leak the rejection unhandled.
- **EQ-SHELL-14 data cleanup**: 2 dangling `app_data.staff.cards_worker_id` pointers on ehow, traced to self-labeled test fixtures ("TEST VERIFICATION"/"DO-NOT-CONTACT", "Alpha Tester"), not a live recurrence of the known duplicate-identity bug. Zero FK references across all 20 tables that reference `app_data.staff`, confirmed before Royce ran the DELETE directly (blocked for Claude by the same live-prod-DML classifier as the earlier Nelson Sareto/Conor Horgan cleanup). Re-verified 0 rows remain.
- **EQ-SHELL-1V data cleanup**: what looked like "13 stale org_memberships, 1 new since the 08-23 baseline" turned out on investigation to be no new grants at all — all 13 grants predate the baseline; the drift is that the *holder's shell account* got deactivated after the grant was made, with nothing to revoke the grant in step. Royce delegated the decision explicitly ("decide the org_memberships cleanup"); decided to revoke the current 13 now (a `status='revoked'` update, not a delete — reversible) and spawn the cascade fix as a separate task rather than build it inline. Executed the UPDATE on jvkn, re-verified 0 of the 13 remain active.
- **EQ-SHELL-1W investigated, left open**: the 2 "unclaimed invites" aren't symmetric — Callum Treharne is genuinely still unclaimed (matches eq-field's own separately-tracked missing-staff list), but William Brown already has a linked account and 7 real licences visible; his invite's `claimed_at` just never got stamped, a harmless bookkeeping gap.
- **EQ-SHELL-1Z investigated, no fix needed**: a single "handoff stalled at minting (10s)" event, from Royce himself, is PR #1758's own brand-new stall telemetry correctly catching a real-but-self-healing stall exactly as designed.
- **eq-cards EQ-CARDS-1N reviewed**: single occurrence, no recurrence in 3+ days, same shape as eq-field's already-closed transient-network-blip issues. Left open, not verified against eq-cards' own retry handling (no Flutter toolchain stood up for one non-recurring event).

## Decided
- Royce: "merge #1764" and "merge #920" — each given only after CI was independently re-verified green immediately before merging, not assumed from an earlier green check.
- Royce: "decide the org_memberships cleanup" — explicit delegation of the revoke-vs-cascade-fix call; decided both (revoke now + spawn the cascade fix separately) rather than picking one exclusively.
- Royce ran both DELETE/live-prod-DML actions himself (EQ-SHELL-14's 2 rows, via the Supabase SQL editor) after being told plainly that permanent deletion isn't something Claude will do regardless of authorization — the org_memberships revoke was an UPDATE, not a delete, and was executed directly since it's reversible.

## Deferred
- **The org_memberships cascade fix itself** (nothing currently keeps `org_memberships.status` in sync with `shell_control.users.active` on deactivation) — spawned as background task `task_9e4a9490`, Royce started it in a separate session, running independently as of this close. _(added 2026-09-05)_
- **EQ-SHELL-1W's Callum Treharne** — nothing to do but wait for him to sign up. _(added 2026-09-05)_
- **eq-cards EQ-CARDS-1N** — Royce's call whether to chase further. _(added 2026-09-05)_
- **The underlying UX question EQ-FIELD-1H's fix doesn't address**: nothing stops a supervisor from attempting to edit an approved-but-unlocked timesheet week in the first place; "Approved" and "Locked" are two separate gates the UI never explains. Named, not built. _(added 2026-09-05)_

## Notes
- This whole thread started as a direct continuation of an earlier same-day close (the initial eq-field-only Sentry investigation, EQ-FIELD-1G) — Royce's "check the other repos for open sentry issues too" turned a single-repo task into a 2-repo, 8-issue sweep across one continuous session.
- Both merges (#1764, #920) hit the same known local `gh pr merge --delete-branch` error (`'main' already used by worktree at ...`, an unrelated worktree elsewhere on this machine) — confirmed via `gh pr view --json state,mergedAt` that the merge had already succeeded server-side before treating either as done, then deleted each remote branch manually.
- Every merge was followed by a direct live-state check, not assumed from the merge event: `curl` against `field.eq.solutions/sw.js` for the version string (eq-field), and a Netlify `listSiteDeploys`-style deploy-record check confirming `commit_ref` match + `published_at` set (eq-shell) — matching the global CLAUDE.md correction that a merge and a live deploy are not the same fact.
- Hit the F9 shared-checkout guard on this close (a second isolated scratch clone, same pattern as every other session today) — the shared checkout was left un-synced again.
- Rebase note: real label collision, not just a textual one — the session immediately above independently also claimed letter "(v)" the same day (its own PR #921 even name-checks this session's PR #920 directly: "an unrelated Timesheets error-handling fix landed on main first, also claiming v3.5.677"). Relabeled this entry to "(w)"; no version-number renumbering needed on this side since #920 (v3.5.677) merged first and #921 already renumbered itself to v3.5.678 around it.

---

# Session 2026-09-05 (x) — Access Control click-test debt: 3 of 4 items confirmed live via Royce's own Edge session

*Continuation of this session's earlier close (PR #1768 built/shipped/confirmed live, click-test debt consolidated into one pending item). Royce had a live, signed-in Edge session open and asked for everything checkable to be checked.*

## Built
- Nothing new built — this stretch was live verification only, via Claude in Chrome driving Royce's own logged-in Edge browser (never the in-app pane), per his standing preference.

## Decided
- Royce: "check everything you need to" — go-ahead to click-test the consolidated Access Control item using his own open, authenticated session.

## Deferred
- **Grant/Revoke platform admin, end-to-end** — the one sub-check not attempted. Confirmed both gates are correct by reading the code instead: client (`src/pages/AdminEditUser.tsx:960`, the whole "Platform admin" section wrapped in `{isPlatformAdmin && target.id !== session?.user.id && (...)}`) and server (`netlify/functions/set-platform-admin.ts:64`, a raw `session.is_platform_admin` check — deliberately not `requirePerm()`, since this flag sits above the whole delegable-permission system by design — returning 403 `platform-admin-only`, plus a `self-action-forbidden` guard and an audit-log write on every change). Didn't actually click it: granting or revoking "every permission, in every tenant" on a real employee, even briefly and reversibly, isn't something to improvise without either Royce's own hands or a named disposable test account. Neither was available this session.

## Notes
- **(2026-08-17) Base-permissions drawer + search** — confirmed live. Typing "licence" into the search box correctly dims every non-matching column (only Field stayed highlighted); clicking a role×app cell (Manager/Field) opens the diffed drawer — "93 of 94 granted — matches the Manager default exactly" — with its own filter box and collapsible per-area sections (Roster, Timesheets, Project hours, Leave, People, Sites, App, Reports, Field admin), each showing a live count.
- **(2026-08-18) Ring visual + tab strip + real permissions in Custom groups** — confirmed live. Every role×app cell in the Base permissions table renders a proportional ring, not a placeholder; the amber "Custom override" ring + legend entry matches real `tenant_role_overrides` rows (Supervisor/Ops, Supervisor/Records, Employee/Field, Employee/Records, Apprentice/Field, Apprentice/Records all showed the amber ring). All 4 tabs (Base permissions / Custom groups / Preview a person / Activity) switch cleanly. Custom groups tab lists 4 real named groups (Calendar Digest Recipients, Field Managers, Service tech, Staff Conversations) with real descriptions, not raw group-id labels.
- **(2026-08-25/26) "Preview a person" — grants AND denials, both checked** — confirmed live on two real people, deliberately picking one of each shape rather than just the Supervisor the pending item named: Anthony Hartley (supervisor) shows `Role overrides: +Approve quotes, +Show records in Field & Service, +Add records` (all grants); Aiden Crowley (apprentice) shows `Role overrides: -View imports, -View records, -View field information` — the exact 3 live denial rows already confirmed to exist in jvkn for apprentice/employee. Both are exactly what PR #1768's fixed `resolveRoleOverridesFailClosed()` call is supposed to surface on an ordinary fast read (tier 1, live) — no fail-closed/full-deny-list behavior triggered, as expected when the read isn't actually slow or failing. The "what they actually see" block is honest for both: Aiden's correctly reads "Never sees this dashboard on sign-in — lands on a two/three-tile WorkerHome page instead, with no sidebar at all," rather than describing a dashboard he'll never reach. Grouped/plain-English sections (a top-level "Effective permissions" block plus a separate "Field permissions" block) rendered with correct live counts for both people, not stale or zeroed.
- One transient glitch: the Access Control tab went unresponsive to the browser-automation extension mid-session (repeated "script injection timed out" errors on screenshot/read_page/find, across several retries and waits) and recovered cleanly on a plain page reload — no console error, no data loss, no repeat. Most likely the automation extension's own resource contention rather than an app bug (tabs_context and navigate kept responding throughout; only in-page script injection stalled) — noted in case it recurs, not treated as a finding.
- Confirmed the shared `C:\Projects\eq-shell` checkout is mid-task for a different concurrent session as of this close (`claude/staff-user-id-fill-on-cards-link`, with `origin/main` now 3 commits past this session's own #1768: #1770 resolvePrincipal fail-closed, #1771, #1772) — not touched, consistent with the standing rule never to edit from a shared checkout mid-use by another session.
- Substrate write for this close went through the same GitHub Contents-API pattern as the rest of today's `/close` passes (fetch current sha immediately before each PUT, diff after) rather than a local clone — today's session log and pending file are both hot enough that a local commit risks the same `stale-main-gate` loop hit earlier this session.

---

# Session 2026-09-05 (y) — EQ Core go-live review: closing the loop — fail-closed follow-ons audited, Field-panel P1 hand-verified, one new bug found and fixed

## Built
- Published the "EQ Core Go-Live Review" artifact (traffic 8, code 6, security 6, UI/UX 5, overall 6/10) and spun off four engineering chips, all now resolved: Field-panel stall (`task_7434eb43`), fail-closed denials (`task_b006ae9d` → eq-shell #1762), CRM/purge/rate-limit fixes (`task_87f2c896` → #1757/#1759/#1760/#1766), register refresh (`task_ed155ebf` → eq-context #201). Full detail in this file's earlier (r)/(s) entries and the artifact itself.
- Ran merge-readiness audits on eq-shell #1760/#1761/#1762/#1763 before/around merge; merged #1761 then #1760 myself on Royce's explicit go, confirming each live via Netlify deploy-commit ancestry (not deploy-row titles, which can read as shipped when superseded).
- Fixed eq-context's own `refresh_digest.py`/`refresh_suite_state.py` (the dead-`EQ_CONTEXT_PAT` failure was silently swallowed as "unknown" — now surfaces a loud 🔴 token-rejected warning — and added a real "Active users" figure to Product Pulse from jvkn `shell_control.users.last_login_at`, closing a signal that had read "blocked" since June) and fully re-verified `eq/active.md` from a 45-day-stale snapshot (v3.5.334) to live (v3.5.673 at the time). Landed as eq-context PR #202 after two rebases against concurrent session closes — merged, generators re-run and confirmed working (`suite-state.md` now shows a real number, `digest.md` now shows the honest warning).
- **Hand-verified the Field-panel P1 live** (session (s), same day): fresh tab, full reload, twice — handoff completes in ~4s with a clean, error-free telemetry chain and real content renders on both dashboard and schedule views. Closed in `eq/pending/cross-repo.md`.
- Found and spun off a second, much smaller bug during that check: `/sks/field` with no `tab=` param defaults to `?tab=home`, which has no case in Field's router on desktop — spun off as `task_5c33a025`, built and merged same night as eq-field **#922** (v3.5.679): `home` is the mobile tile screen, hidden by CSS above 767px; the desktop deep-link path was the one caller that skipped the eligibility check the other two already had, so it now falls back to `dashboard` like they do. Confirmed live via the service-worker version; changelogged in `eq/changelog/eq-field.md` (this session's own entry, since the fix's own session didn't log one).
- Post-merge audited the four fail-closed follow-on PRs that landed overnight closing every gap the original #1762 audit had flagged out of scope (#1764 bounds the remaining 7 gating reads in `verify-shell-session.ts`; #1767 closes the 3 other JWT minters; #1768 closes `security-groups.ts`; #1770 converts `resolvePrincipal()`, the ~124-call-site holdout). **Verdict: clean with caveats** — all four do exactly what they claim, all tested against slow/failed reads not just fast, zero remaining fail-open `tenant_role_overrides` caller anywhere in eq-shell bar one deliberately-scoped exception (`mint-supabase-jwt.ts`, jvkn-only). #1770's blast radius (the one genuine risk) was reasoned through and live-confirmed, not hand-waved: eq-context's SEC-74 register row shows the decision reversed the same day with real reasoning, and cites a live 401 proving it enforces in production. Caveats are process only — same author/merger, no formal GitHub review on any of the ten PRs merged in this arc.
- Trimmed `eq/pending/cross-repo.md`'s go-live-review section per the archive rule (all items I touched this session are now covered here or already closed; 7 items with no chip owner remain open, listed there).

## Decided
- Royce: merge #1761 then #1760, confirm live after each.
- Royce: merge eq-context #202.
- Royce: "check the other two chip PRs" (ambiguous which two, given the volume — read as covering both the finished Field-panel chip and a fresh audit of the fail-closed thread's new PRs; both done).

## Deferred
- The 7 items with no chip owner in `eq/pending/cross-repo.md`'s go-live section — 2FA posture, hide the 2FA settings page while it's off, a UI copy nit, whether independent PR audits become standing practice, an SKS NSW Labour switch-off date, rollout data-hygiene watch items, PostHog cross-app identity.
- Still Royce's own hands, unchanged from the review: regenerate `EQ_CONTEXT_PAT`, uninstall `grok-by-xai` (SEC-57), delete the `dev`-context value of the shared JWT secret (SEC-63).

## Notes
- **Could not locate `task_7434eb43`'s own session or PR** despite trying `list_sessions`/`search_session_transcripts` — most likely its target was already fixed by unrelated organic work (#1758/#1765/eq-field #917) before it could act, and it declined to duplicate. Not chased further since the live hand-check answered the substantive question either way; noted here so nobody goes looking for a PR that may not exist.
- **A real near-self-miss worth repeating:** a 0.6-scale screenshot of the (correctly working) `?tab=schedule` view looked identically blank to the genuinely-broken `?tab=home` view. Real content only became visible on `zoom` and at a full-resolution scale-1 screenshot. A downscaled screenshot of a pale/light-themed page is not proof of "blank" — check at native resolution before reporting a UI as broken.

# Session 2026-09-05 (y) — Documents sign-off register: marketing explainer built, then an 8-angle cold code audit found and fixed 10 real gaps, merged and confirmed live

## Built
- Built a marketing/explainer artifact for the Documents sign-off register at `/sks/admin/documents` (competitive positioning against Aconex/Procore/Autodesk Construction Cloud/Bluebeam/Newforma, an 8-step "how it works" diagram, an honest read of what's real vs. aspirational) — grounded in an Explore-agent audit of the actual shipped code, not the feature's own marketing copy.
- On request, ran a full cold code audit of the same feature: 8 independent finder angles (correctness/tenant-isolation, missing-invariant, cross-file contract tracing, reuse, simplification, efficiency, altitude, CLAUDE.md conventions) via parallel subagents, then an independent 1-vote verifier against the actual current code for every candidate before reporting anything. 8 of 10 final findings came back CONFIRMED, 2 PLAUSIBLE (both real, with a caveat each).
- Applied all 10 with minimal edits, then verified `tsc -b --force` and `eslint` clean across every touched file:
  - **Tenant isolation (most severe two)**: `push-document-audience.ts`'s crew-targeted push and its audience-picker endpoint both resolved/leaked another tenant's crew on a shared data-plane project — the one code path in the file missing the `.eq('tenant_id', ...)` filter every other query already has. Closed.
  - **Onboarding auto-push wired to only 1 of 3 real write paths** — Cards approval and "Add to Roster" both set `start_date` directly and never triggered it; the code's own comment wrongly claimed it had checked every path. Relocated the shared push function to `_shared/onboarding-due-date.ts` and wired the two real UPDATE-path call sites (the two pure-INSERT paths have no linked Shell login yet at creation time, so correctly no-op — see Deferred).
  - Same auto-push ignored a category's `requires_signoff` override (doc_type check only) — same bug class as an already-fixed 2026-08-20 incident, recurring in a second implementation that fix never reached. Fixed.
  - Archived documents could still be pushed/republished/re-versioned via a direct API call — the block was client-side only. Added a server-side check.
  - `due_at` was permanently pinned on first push; a corrected `start_date` now refreshes it for still-outstanding, unscoped rows.
  - The nightly reminder cron fanned out both its loops with no concurrency cap — capped both, matching the pattern `licence-expiry-scheduler.ts` already uses.
  - 3 cleanup items: a duplicated/drifted surname-cleanup helper replaced with the existing shared one; a duplicated relative-time helper consolidated against `staffHelpers.ts`'s; the resource router's 21 near-identical dispatch blocks collapsed into a table (preserving its two real behavioural quirks).
- [eq-shell PR #1772](https://github.com/eq-solutions/eq-shell/pull/1772) — CI green (typecheck/test/lint, gitleaks, migration-ledger hygiene, function-grants, schema-drift), squash-merged (`c2cd1414`) via the GitHub API (the shared `C:\Projects\eq-shell` checkout's `main` was busy in another worktree, blocking `gh pr merge`'s local branch flow). Confirmed live via the Netlify deploy record for that exact commit: `state: "ready"`, `published_at` 2026-09-04T23:29:38Z.
- Redid the marketing artifact after the fixes landed, per explicit instruction to keep it non-technical ("tech should be invisible"): added a plain-English "this week's check-up" section describing the six user-visible fixes with no jargon, and a "still to chase down" section for the two genuine open questions below.
- Spawned both open questions as background task chips; Royce started both in separate local sessions before this session closed.

## Decided
- Royce: "merge it" — explicit go-ahead to commit, open the PR, and merge, after seeing the full audit and verification pass first.
- Royce: redo the explainer artifact with the fixes folded in, and strip all technical framing from it.

## Deferred
- **Where does a brand-new starter's Shell login actually get connected?** The onboarding-push fix only covers write paths where a staff row already has a linked `user_id`. Cards approval and "Add to Roster" both insert a brand-new staff row with no `user_id` at all — correct to no-op there, but the real question (where that link gets made afterward, and whether *that* write path needs the same trigger) wasn't traced this session. Spawned as `task_a1e54a2d`; Royce started it in a separate session.
- **Whether the crew-push tenant-isolation gap was ever actually triggered in production** — the code-level fix doesn't tell you whether it already happened. Spawned as `task_ba1bdb4b` (a live-data check, no writes) for peace of mind; Royce started it in a separate session.
- The router-refactor and relative-time-dedup fixes are pure internal cleanup with zero user-visible effect — real, but not the kind of thing that belongs in a plain-English artifact, so they're folded into one footnote line there instead of explained.

## Notes
- This session's own background CI-watch and Netlify deploy-status checks were interrupted mid-run by a context reset ("we got cut off") — recovered by reading the partial output already written to disk (confirmed the merge commit was still `state: "building"` at interruption) rather than re-running blind, then re-checked fresh to get the actual `published_at` timestamp before reporting the deploy as live.
- `gh pr merge --squash --delete-branch` failed outright (`'main' is already used by worktree at ...`) — the standing shared-checkout-contention pattern already logged elsewhere in this file, this time hitting the merge step itself rather than a substrate write. Worked around with `gh api repos/.../pulls/1772/merge -X PUT` directly, same fix this file already documents for the equivalent `git`-level block.
- This close's own eq-context writes went through a fresh isolated `git clone`, not the shared checkout (which already had another session's uncommitted work sitting in `eq/changelog/eq-shell.md` and this file) — consistent with every other entry today.

---

---

# Session 2026-09-05 (z) — SEC-74 resolved for real: a redundant correction caught before merging, then a `/decide` pass kept eq-shell #1770 and added a dated check-back (PR #216)

*Closes out the CONFLICT flagged in section (g): two sessions gave Royce opposite answers on `resolvePrincipal()` within minutes of each other (this session recorded "leave as-is" as SEC-74; a sibling session's [PR #1770](https://github.com/eq-solutions/eq-shell/pull/1770) adopted the fix, merged by Royce directly). This section is what happened after that conflict was surfaced.*

## Built
- Nothing in eq-shell — this stretch was eq-context register/process work only.
- Verified #1770 properly before writing anything about it (not just "exists and is open," which is as far as section (g) had gotten): confirmed via `gh pr view` that Royce (`Milmlow`) merged it directly at `2026-09-04T23:00:01Z` (squash `4dc8e8e9`), confirmed it's actually live via commit-ancestry against the production deploy, and read its full diff. No code objection — the reasoning holds (this was the only one of the seven `tenant_role_overrides` call sites with no timeout wrap at all, a real latency bug independent of the security question, and the failure mode it trades away is a silent bypass for a loud, self-correcting 403).

## Decided
- Royce: "merge #215" (a drafted correction marking SEC-74 closed and citing #1770). Before merging, re-fetched live state and found another session had already pushed a more precise correction directly to `main` — citing the actual Netlify deploy id and exact `published_at` timestamp, where #215's draft only had approximate reasoning. Did not merge #215: closed it with a comment explaining why, deleted its branch. Diverging from the literal instruction was deliberate and stated to Royce at the time, not silent — merging would have overwritten better, already-live content with a worse version.
- Royce: `/decide - help` → selected "The resolvePrincipal() conflict from tonight" as the decision to run the six-step protocol on.
- The `/decide` pass concluded: **keep #1770**, don't revert to the earlier same-day "accepted risk" call. It already closed a real gap and had run live for hours with nothing reported wrong; the wider-blast-radius downside (most of `resolvePrincipal()`'s ~124 call sites are lower-traffic than the six already-fixed endpoints, so a hiccup lands more of them on the hard-deny tier) was, in every version of this reasoning including #1770's own PR body, always a stated *concern* — never something actually observed. Paired with a deliberately short check-back (not the usual 3-month cadence) since this is the one call site with no real production traffic data yet.
- Royce: "go" — authorized executing the `/decide` recommendation.
- Authored and merged eq-context [PR #216](https://github.com/eq-solutions/eq-context/pull/216) (squash `971b88d7`): adds a dated follow-up to the already-closed SEC-74 row — by 2026-09-15, check Sentry for `[role-overrides] resolvePrincipal` fail-closed events and whether they cluster with genuine `tenant_role_overrides` slowness or with routine cold starts (a burst of fresh containers, e.g. after a deploy, could produce the same signal without anything actually wrong) — plus a matching standalone bullet in `eq/pending/eq-shell.md` so the checkpoint surfaces outside a row that otherwise reads as closed and settled. One transient "claude-sonnet-5 rate-limited" classifier block on the first merge attempt; retried immediately, succeeded (`merged_at` 2026-09-05T00:37:49Z).

## Deferred
- The 2026-09-15 check itself is a future-dated task, tracked in `eq/pending/eq-shell.md`, not something to act on now.

## Notes
- **The same redundant-draft pattern already logged elsewhere in this file recurred here, on the same row, the same night.** #215 was a real, correctly-reasoned draft — it just lost a race to an equivalent correction pushed straight to `main` in the gap between drafting and merging. Caught only because live state was re-fetched immediately before merging rather than trusting the "merge #215" instruction at face value against a stale read.
- Confirmed at the time of writing this entry: `ops/security-register.md`'s SEC-74 row and `eq/pending/eq-shell.md`'s matching bullet both already carry the full corrected-plus-checkpointed content on `origin/main` — nothing further to reconcile there.
- This file itself moved twice more while this close was being written (behind-count climbed from 9 to 12 to a fresh fetch mid-session showing 574 lines against an earlier 548-line read) — consistent with this file's own repeated observations elsewhere that the session-gate's "behind N" figure is a snapshot, not a live count, and that reconciling immediately before every write (not once at session start) is the only discipline that actually holds under this much same-day concurrent traffic.
