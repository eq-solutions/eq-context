---
title: SYSTEM — Failure Ledger (the ratchet's memory)
owner: Royce Milmlow
last_updated: 2026-09-10
scope: Every failure that escaped the safeguards, with the rung its guard currently sits at. Machine-read by guard-ratchet.yml. Append-only for entries; rung/count are mutable.
read_priority: high
status: live
---

# SYSTEM — Failure Ledger

**This is the memory of the ratchet.** Every failure that got past the safeguards is recorded here with the rung its guard sits at. When a failure **recurs**, its guard must climb.

The ratchet only tightens. It is driven by reality — a failure that actually happened — never by opinion.

**Budget:** ~400 lines (currently 257). Past that, evict entries whose guard
has held for 90+ days with zero recurrences to a new `system/failures-archive.md`
(same split pattern as `system/worktree-registry-archive.md`) — keep the F-number
and rung here as a one-line pointer, move the full incident narrative.
(`rules/tidy-protocol.md` Step 5, 2026-09-07.)

## The enforcement ladder

| Rung | Form | Catches |
|---|---|---|
| **0** | unknown | nothing |
| **1** | lesson in `lessons.md` (prose) | only if the agent reads *and* recalls it at the right instant |
| **2** | session-start checklist item | usually |
| **3** | CI check | **after** the damage |
| **4** | **hook** — enforced at the point of action | **before** the damage. Prevention. |

**The rule:** `recurrences >= 2 AND rung < 4` ⇒ `guard-ratchet.yml` opens an issue proposing promotion.
**Propose-only** (Royce, 2026-07-11) — the ratchet never merges its own changes. It argues; you decide.

> **Why prose is rung 1, not rung 3.** On 2026-07-11 the truncation lesson (F2) *existed* in `lessons.md`. The assistant *read* `lessons.md` that session. It **still truncated `CLAUDE.md`.** Knowledge that requires an agent to have retained 455 lines and recall the right one at the right moment is not a safeguard. It is hope with a timestamp.

---

## Ledger

```yaml
failures:
  - id: F1
    title: Substrate read path served 8-12 day stale content, 200 OK, no error
    first_seen: 2026-07-11
    last_seen: 2026-07-19
    recurrences: 2
    rung: 4
    target_rung: 4
    guard: "hooks/session_start.py — SessionStart gate (rung 4, built 2026-07-11)"
    detected_by: "human — git reflog contradicted the fetched file"
    cost: "near-revert of the 2026-07-03 contract rewrite; two false headline findings"
    note: "The §1 fallback cannot catch this — it triggers on errors, and a stale cache hit is not an error. CHECKED 2026-08-30: digest.md flagged a 'possibly recurred' hit in sessions/2026-08-23.md. Investigated directly — FALSE POSITIVE, not added to confirmed_in (would misrepresent it as a real incident). The matching line ('raw.githubusercontent.com served stale content after pushes; the GitHub Contents API reflected them immediately. Use the API to verify a push landed, not raw.') describes verifying a PUSH TO A DIFFERENT REPO landed (eq-field/eq-shell/eq-cards that session), not eq-context's own substrate read at session start — the specific surface this guard covers — and it demonstrates the F1 lesson being correctly applied (checked the Contents API instead of trusting raw), not a fresh instance of being fooled by stale content. Regex left as-is: tightening it to exclude this shape (e.g. requiring proximity to a substrate-specific term or a trust/cost consequence) is a real, identified follow-up, not attempted this pass — the F6/F7 precedent for safe regex-tightening involved building a dedicated adversarial test batch against a full day of session logs before shipping, which this pass didn't have scope for. Expect this same file to re-flag on every future digest run until either the regex is tightened or the file ages past relevance. RE-FLAGGED AGAIN 2026-09-07, same file (sessions/2026-08-23.md), same predicted shape — deferred via a `/triage` pass rather than re-investigated from scratch; regex tightening still not attempted this pass. RE-FLAGGED AGAIN 2026-09-09, same file, same predicted shape — deferred via a `/triage` pass, same as 2026-09-07; regex tightening still not attempted. REGEX TIGHTENED 2026-09-09: every alternative now requires a substrate-specific anchor (eq-context, CLAUDE.md, digest.md, suite-state, substrate, lessons.md, the literal id `F1`, or session-start/session-gate wording) on the SAME line as the stale-shape match — `(?:^|\\n)` plus two lookaheads detects line boundaries without needing re.M, since the real compile call (failure_recurrence_signals()) is `re.I`-only. Verified against the full sessions/*.md corpus (152 files, not a sample): the 3 genuine incidents (2026-07-11 original discovery, `system/lessons.md`'s own \"The Substrate Read Path Lied\"; the 2026-07-19 recurrence via COWORK-PROMPT.md, caught because it named CLAUDE.md; and the 2026-07-21 confirmed_in mention, which cites F1 by id) all still match. The 2026-08-23 false positive — and both its 2026-09-07/2026-09-09 re-flags, the second of which was still showing live in digest.md as a rung-4 'Guard bypass?' Needs-You item at the moment this fix was written — no longer matches, because that line never names eq-context, CLAUDE.md, digest.md, substrate, F1, or session-start anywhere; it is purely about verifying a push to a DIFFERENT repo landed. New matches are structurally impossible from this change (the anchor requirement can only narrow the existing three alternatives, never widen them) — confirmed empirically too: zero new hits anywhere in the corpus. 10 unit tests (5 must-catch: the 3 real incidents plus 2 synthetic cases for the CDN-cache/serving-stale alternatives, which had no real historical example to draw a fixture from; 5 must-not-catch: the real false positive plus 4 adversarial synthetic near-misses, including the same shape deliberately split across two lines to prove the no-DOTALL line boundary holds) now pin this in `.github/scripts/test_failure_recurrence_signals.py` — it reads the live `signal:` string out of this file the same way failure_recurrence_signals() does, rather than a hardcoded copy, so a future edit here that weakens precision fails CI instead of silently drifting. Wired into digest-refresh.yml's pre-flight test step alongside F11's test_scheduled_workflow_health.py. Same precision-tightening shape F6/F7/F9/F10/F12 already used for their own signal regexes — this is F1's first. SELF-REFERENCE FOUND 2026-09-09, same day, immediately after landing: this fix's own commit message and session-log write-up (sessions/2026-09-09.md) itself combines the stale-shape phrase with substrate anchors (eq-context, CLAUDE.md, digest.md, F1 by id) while describing the fix — exactly the shape the anchor requirement is designed to catch, and exactly the self-reference class already named once above ('found live 2026-07-26, F1') and hit for real by F12 on 2026-09-07 ('the ledger's own historical record of checking F12 is what re-triggered F12's signal'). Added to confirmed_in rather than reopening the regex — this is what that field exists for, not a precision gap in the new anchor logic."
    signal: "(?:^|\\n)(?=[^\\n]*(?:raw\\.githubusercontent[^\\n]*stale|serving stale[^\\n]*(?:no error|200 ok)|CDN.?cache[^\\n]*stale))(?=[^\\n]*(?:eq-context|CLAUDE\\.md|digest\\.md|suite-state|substrate|lessons\\.md|\\bF1\\b|session.?start|session gate))"
    confirmed_in: ["sessions/2026-07-21.md", "sessions/2026-09-09.md"]

  - id: F2
    title: Edit/Write silently truncates long files on the C:\Projects virtiofs mount
    first_seen: 2026-05-24
    last_seen: 2026-07-11
    recurrences: 2
    rung: 4
    target_rung: 4
    guard: "hooks/pre_tool_use.py — PreToolUse block, fail-closed (rung 4, built 2026-07-11)"
    detected_by: "wc -l after the write"
    cost: "CLAUDE.md truncated 308->277 lines, §12/§13/End destroyed. Tool reported success."
    note: "PROMOTED 2026-07-11 — the ratchet's first closed loop. It demanded promotion; the hook was built; it went quiet. Prose failed twice; the hook cannot be forgotten."
    signal: "truncat(e|ed|ion|es).{0,40}(virtiofs|mount)|virtiofs.{0,40}truncat"

  - id: F3
    title: A goal nobody owned governed every session for two weeks from a read_priority critical file
    first_seen: 2026-07-11
    last_seen: 2026-07-11
    recurrences: 1
    rung: 3
    target_rung: 3
    guard: "TODAY.md GOALS UNSET + claim-expiry.yml (rung 3, built 2026-07-12) — fails CI on an unowned/undated/expired goal"
    detected_by: "human — Royce asked 'what's that deadline? why are you mentioning it?'"
    cost: "an assistant repeatedly told Royce to defer work against a phantom deadline"
    note: "Every check passed green. auto-bump-frontmatter was faithfully keeping the phantom looking fresh. Freshness != truth. claim-expiry.yml built 2026-07-12 — F3's guard climbed 1->3; a goal now cannot sit in TODAY.md undated, unowned, or past expiry without failing CI."
    signal: "phantom deadline|unowned goal|goal.{0,20}nobody owned"

  - id: F4
    title: Nothing watches the product — and the metric raised to prove it was itself over-read
    first_seen: 2026-07-11
    last_seen: 2026-07-11
    recurrences: 1
    rung: 0
    target_rung: 3
    guard: "PLANNED, NOT YET BUILT -> product signals in digest.md (rung 3), framed as TRANSITIONS not thresholds"
    detected_by: "human — an ad-hoc live SQL query; corrected 2026-07-12 by a second, due-date-aware query"
    cost: "REAL GAP: zero monitoring of any product signal. But the alarm that surfaced it — '14/16 created, 0 ever completed, the core workflow has never worked' — was OVER-READ. Verified live 2026-07-12: of 16 checks, 10 are live and ALL future-dated (earliest due 2026-08-06; 8 are RCD compliance seeds due 2027); the only past-due rows are soft-deleted; nothing has even been started. 0 completions is a young, forward-scheduled system, not a broken completion path."
    note: "The lesson doubled. (1) Nothing watched the product — still true, still rung 0. (2) The very first metric used to raise the alarm was un-verified against due-dates — the exact 'verified falsehood' the plan (residual risk #1) calls its floor. Mitigation: the pulse must watch TRANSITIONS (did the 2026-08-06 check complete WHEN DUE?), never a pre-due backlog. Real soft signals to watch instead of the completion count: prestarts stalled (30, last 07-04), safety modules at 0, 31 non-Royce writes/14d, last_login_at never written."
    signal: "over-?read.{0,20}(metric|alarm|signal)|nothing (watches|monitors) the product"

  - id: F5
    title: An ungoverned shadow memory overrode the canonical contract
    first_seen: 2026-07-11
    last_seen: 2026-07-11
    recurrences: 1
    rung: 0
    target_rung: 3
    guard: "none -> memory-coverage.yml (rung 3) + collapse shadow memories to thin pointers"
    detected_by: "human — traced backwards from F1"
    cost: "the Cowork preferences patch routed an agent to a stale URL instead of the authoritative local clone"
    note: "Seven memory layers. One governed. The CI audits the memory that was CORRECT. The patch written to prevent drift is what caused it."
    signal: "shadow memory|ungoverned memory"

  - id: F6
    title: Append (>>) NUL-fills files on the C:\Projects virtiofs mount
    first_seen: 2026-07-11
    last_seen: 2026-07-11
    recurrences: 1
    rung: 4
    target_rung: 4
    guard: "hooks/pre_tool_use.py — blocks >> to any mount path (rung 4, built 2026-07-11)"
    detected_by: "NUL-byte scan after the write — wc -l reported a SANE line count"
    cost: "3,955 NUL bytes written into system/lessons.md. Two lessons destroyed. File became binary."
    note: "Found while fixing F2 — and it INVALIDATED the F2 fix. The old lesson said 'prefer cat >> over Edit for appends'. That advice was WRONG and it corrupted the file. Only FULL REWRITE (cat >) is safe. wc -l alone will not catch this: the NUL-fill made the file LARGER. Signal regex tightened 2026-08-05 after a same-day false-positive digest hit — the bare phrase alone matched a session log merely citing 'the open F7 NUL-fill risk' as workflow rationale, not a new incident. Now requires an incident verb (corrupted/destroyed/wrote/written/found N/became binary/turned binary) near the phrase. Tested against all of that day's ~22 session logs (zero false positives before or after, except the one known case, which the new regex correctly drops) and against this entry's own cost text verbatim (still matches). Known tradeoff: no regex distinguishes 'reference' from 'incident' with perfect recall — this trades a little sensitivity for materially better precision, not a perfect fix."
    signal: "(?:corrupt(?:ed|ion)?|destroy(?:ed)?|wrote|written|found\\s+\\d|became\\s+binary|turned?\\s+(?:into\\s+)?binary).{0,60}(NUL.?(fill|byte)s?|nul-fill)|(NUL.?(fill|byte)s?|nul-fill).{0,60}(?:corrupt(?:ed|ion)?|destroy(?:ed)?|wrote|written|found\\s+\\d|became\\s+binary|turned?\\s+(?:into\\s+)?binary)"
    confirmed_in: ["sessions/2026-07-28.md", "sessions/2026-07-31.md", "sessions/2026-08-04-b.md", "sessions/2026-08-04-c.md"]

  - id: F7
    title: git merge/stash-pop round-trip NUL-fills files on the C:\Projects virtiofs mount
    first_seen: 2026-07-28
    last_seen: 2026-07-28
    recurrences: 1
    rung: 4
    target_rung: 4
    guard: "hooks/pre_tool_use.py — (1) a blanket block on git write verbs (add/commit/push/merge/stash/rebase/etc.) in-sandbox, which turned out to ALREADY exist since 2026-07-12 and already covered merge/stash — this entry's original 'guard: none' claim was wrong, corrected below; (2) tool matching widened to Bash + PowerShell (was Bash-only); (3) NEW, NOT sandbox-gated: a pre-git-verb NUL-byte scan of the working tree (git status --porcelain output) that blocks fail-closed if any modified/untracked file already has NUL bytes, on any platform, regardless of in_sandbox(). Built + tested 2026-07-31, adversarial suite 51/51 (6 new cases: 2 proving the widened PowerShell match, 4 proving the scan fires independent of the sandbox flag)."
    detected_by: "human — `file` + a byte-level scan on scripts/sites.js after a git stash pop/merge round-trip; node --check did not catch it"
    cost: "scripts/sites.js corrupted with NUL bytes mid-session; caught and fixed before commit, so no damage landed — but the corruption mechanism is real and currently invisible to every existing guard"
    note: "CORRECTED 2026-07-31: this entry originally claimed 'F6's guard has no visibility into git operations' — that was wrong. hooks/pre_tool_use.py already had a blanket block on git write verbs including merge/stash, live since 2026-07-12, which SHOULD have stopped the 2026-07-28 incident outright. The real open question — never resolved, not fixed by this build — is WHY that didn't fire: either the hook wasn't wired into whatever sandbox ran that session, or the git command reached it through a path this hook's tool-name matching didn't cover. Widening to Bash+PowerShell closes one candidate cause but doesn't prove it was THE cause — nobody can currently inspect that sandbox's actual settings.json from here. What this build DOES provide unconditionally: an independent NUL-byte integrity scan ahead of any future git verb, on any platform, that blocks before corruption (from ANY path, wired-hook-covered or not) can be committed/pushed further. Tracked as a separate open item in ops/pending.md, not closed by this entry. CORRECTED AGAIN 2026-08-07: the signal regex's bare `NUL` (no suffix requirement, unlike F6's own `NUL.?(fill|byte)s?`) was matching the ordinary English word 'null' case-insensitively — confirmed live via direct `re.search` against `sessions/2026-08-05.md`: `error_message: null` sitting within 80 chars of an unrelated 'not just \"merged\"' produced a false 'possibly recurred' hit that survived the 2026-08-06 confirmed_in fix untouched, since that fix only addressed a different (also-real, but not the live-triggering) file. Tightened to require the same `NUL.?(fill|byte)s?` suffix F6 already uses — verified against all 9 files that mention 'F7' in prose plus both genuine historical incidents (2026-07-28.md, 2026-07-31.md, still match) before shipping, not assumed."
    signal: "git (stash pop|merge).{0,80}NUL.?(fill|byte)s?|NUL.?(fill|byte)s?.{0,80}(stash pop|merge|round-trip)"
    confirmed_in: ["sessions/2026-07-28.md", "sessions/2026-07-31.md", "sessions/2026-08-05-x.md"]

  - id: F8
    title: Prose written into the frontmatter status field turned CI red on main, twice in four days
    first_seen: 2026-08-03
    last_seen: 2026-08-04
    recurrences: 2
    rung: 4
    target_rung: 4
    guard: ".githooks/pre-commit check 17.5 — blocks any governed .md whose status: is outside live|draft|archived|deprecated, or is missing the key entirely (rung 4, built 2026-08-04). Exemption list held identical to .github/workflows/frontmatter-check.yml so hook and CI cannot disagree. Adversarial battery 7/7: real defect blocked, all four valid values pass, exempt paths skipped, missing key blocked, secret guard still fires through the new delegation, fail-closed when the guard script is absent, clean file commits."
    detected_by: "CI — Frontmatter validation, i.e. only AFTER the push. Never caught before the damage."
    cost: "Frontmatter validation red on main from 092466d until 2026-08-04, so every branch cut from main inherited a failing required check. The 2026-08-03 session fixed this exact defect on two other files; the very next commit to touch a third file reintroduced it."
    note: "PROMOTED 2026-08-04 on the same day the recurrence was found. The guard existed at rung 3 the whole time and worked exactly as designed — it just cannot fire until the damage is already pushed. This is the ladder's own argument: CI catches AFTER, a hook catches BEFORE. Uncovered while building it: core.hooksPath on the Beelink clone pointed at .git/hooks (an untracked copy of scripts/pre-commit-secrets.sh), so the governed .githooks/pre-commit had never run on this machine at all — a recurrence of the 2026-05-24 core.hooksPath lesson, and the reason a style hook could not have caught this even if the check had existed. Fixed by delegating the secret guard from inside the governed hook, so pointing core.hooksPath at .githooks no longer trades a credential guard for a style guard."
    signal: "status:.{0,40}(must be one of|carried prose)|prose in the status field"
    confirmed_in: ["sessions/2026-08-04-c.md"]

  - id: F9
    title: Concurrent-session git races corrupt the shared eq-context checkout
    first_seen: 2026-07-14
    last_seen: 2026-08-21
    recurrences: 5
    rung: 4
    target_rung: 4
    guard: "hooks/pre_tool_use.py — two checks, both scoped to the ONE shared checkout by exact path (never a private/fresh clone): (1) blocks bare `git commit` with no `--` pathspec — a bare commit records the WHOLE index, not just what was just `git add`ed; (2) redirects `git rebase`/`merge`/`pull` (minus --abort/--continue/--skip, which recover an already-stuck state and must stay allowed) to an isolated clone instead. Not sandbox-gated — every occurrence happened natively on the Beelink. Rung 4, built 2026-08-04, adversarial suite 65/65 (15 new F9 cases: bare-commit block, pathspec/--amend allow, a commit message containing a literal ' -- ' proven not to false-negative, rebase/merge/pull block, --abort/--continue/--skip allow, PowerShell tool-matching, and 3 controls proving none of it fires outside the shared checkout)."
    detected_by: "human — a targeted `git add system/worktree-registry.md && git commit` visibly swept up three files staged by a concurrent session; caught via `git show --stat HEAD` before push, same session also saw a stale mid-rebase read self-resolve"
    cost: "2026-07-14: a session-log append was reset by a concurrent rebase mid-commit, had to be re-added in a follow-up commit. 2026-08-03: three distinct races in ~10 minutes — a stuck rebase, a live conflict-marker (`<<<<<<< HEAD`) committed straight to main, two non-fast-forward push rejections — plus, even after reconciling in an isolated clone, applying the fix back on the shared checkout collided with a SECOND concurrent rebase (HEAD detached, main ref left pointing at a stale commit). 2026-08-04: the sweep described above, then push rejected non-fast-forward, `git rebase` refusing to start (unrelated uncommitted files from another process already in the index), and `git stash` blocked by an unrelated environment permission classifier — resolved via a fresh isolated clone + cherry-pick. No data lost on any occurrence; real time burned untangling each one, and the pattern was accelerating (3 distinct days, escalating to twice in one session)."
    note: "The gap this ledger exists to close had itself gone uncaught here: this exact failure class recurred 3+ times across 3 weeks with NO ledger entry at all — so failure_recurrence_signals() had nothing to scan for and guard-ratchet.yml never had a `recurrences` counter to bump past 2. Two independent mechanisms got two independent, narrowly-scoped checks rather than one blanket rule: (a) index-level — bare `git commit` sweeping up a concurrent session's staged files, fixed unconditionally regardless of whether anyone remembered to check `git status` first; (b) ref-level — rebase/merge/pull mutating HEAD/index/refs across several non-atomic steps while another session touches the same working directory. Considered and rejected: a lock/coordination file (a hook-enforced lock needs reliable cleanup on abnormal session termination or it becomes a new stuck-forever failure class — this ledger already documents that exact shape of damage from imperfect guards, see the Loop of Despair) and blocking ALL git writes to the shared checkout (would break routine, currently-reliable automation — nightly cron commits, single-file pending.md ticks, session close — none of which have ever been the source of the actual damage; only rebase/merge/pull and bare commits have). eq/pending.md's 2026-08-03 entry is closed by this fix, not re-deferred a third time. KNOWN SCOPE BOUNDARY found 2026-08-05, not counted as a recurrence (the guard was never in a position to see this, same as F2/F6's documented Windows/sandbox boundary): the exact sweep mechanism (a) fixes recurred within 24 hours of shipping, this time via a `git commit` authored 'via Cowork' against this same shared checkout — outside Claude Code's own tool-call hook entirely, since Cowork emits scripts for a human to run rather than executing git itself. `pre_tool_use.py` cannot see or block that path by construction; a git-level hook can't reliably substitute — checked empirically 2026-08-05, not just reasoned (eq/pending.md has the full writeup). The literal claim is false as stated: a hook CAN tell whether pathspec/scoping syntax was used (`git commit -- <path>` builds a temp index at `.git/next-index-<pid>.lock`; a bare commit stays on the default `.git/index` — confirmed across 5 scenarios in a throwaway sandbox). But the conclusion holds anyway, for the real reason: that signal detects whether scoping was used, not whether the commit is safe — a safe bare commit and an unsafe one that sweeps a stray are indistinguishable on every hook-visible signal, because git's index has no field for which session staged which file. A git-level reject-bare-commits hook would only relocate F9(a)'s existing rule and its existing blind spot (a lazy `-- .` defeats both equally), while adding a gap F9(a) doesn't have: `core.hooksPath` activation is per-clone, manual, and was found silently drifted on THIS machine mid-investigation — a worktree-scoped override was shadowing the correct value, so F8's own hook had not actually been running here despite F8 being marked closed (fixed as a side effect). `/proc/$PPID/cmdline` and `ps -o args=` are also unavailable on this Windows/Git-Bash target. A weaker commit-msg-stage (not pre-commit — confirmed pre-commit cannot see the message even with `-m`; `COMMIT_EDITMSG` holds the previous commit's stale content at that point) WARN heuristic — flag a staged file sharing no keyword with the commit message — is real and would have caught this incident's actual shape, but isn't built: standing tunable infrastructure is a product call for Royce, proposed in eq/pending.md rather than shipped silently. No git-level hook closes this gap; the recommended fix is process, not detection — extend the existing Cowork-emits-scripts convention to also require pathspec-scoping, the same bar F9(a) already holds Claude Code to. CORRECTED 2026-08-05, same day, same investigation thread (task_94836df0): the premise both notes above share — that commit 2104668 ran outside Claude Code's own tool-call hook entirely — is wrong for THIS incident, and it IS counted as this entry's 4th recurrence, not exempted. guard.log has a matching entry down to the second (gate-outbound fired, warn mode, identical command, identical session): this was an ordinary Claude Code Bash call. The real mechanism was three compounding, fixable bugs, all fixed same day: (1) pre_tool_use.py was wired into PreToolUse only at the umbrella-root settings.json (C:/Projects/.claude/settings.json) — the identical 'guard that isn't wired' shape session_start.py hit and fixed 2026-07-12 by moving to user scope, a fix this hook never got, so a session launched inside a repo or worktree (the common case, and what 2104668's session actually was) never invoked it at all; (2) even when invoked, F7/F9 read data.cwd directly, never an in-command cd or -C — the identical blind spot guard.js's own reflection-gate rule already fixed for itself 2026-07-26; (3) found live while fixing (2): COMMIT_RE/REBASE_MERGE_PULL_RE required git and the verb separated by whitespace only, so git -C <path> commit never matched at all, independent of cwd. Fixed: user-scope wiring (matcher widened to include PowerShell too), effective_cwd(), both regexes widened to tolerate an optional -C <path> prefix. Regression cases added to both suites (70/70 python, 32/32 bash, from a clean, correctly-mount-pathed clone). This does NOT undercut the git-level-hook analysis directly above — that investigation answers a genuinely separate, still-open question (can anything backstop git that truly runs outside Claude Code's tool calls: a human terminal command, or a Cowork-emitted script run by a human) that remains real even though it wasn't what caused this specific commit. ops/pending.md's parallel F7 'still open — why didn't the guard fire' question closed by this same finding. RECURRED 2026-08-21, 5th occurrence, found retroactively 2026-08-30 (not flagged live by digest at the time — dated within the pending-rotate-style blind spot, surfaced only while investigating an adjacent, unrelated 'possibly recurred' flag against this same file for a different failure ID): sessions/2026-08-21.md's own entry (its 'Notes' section, second bullet) describes a session's OWN staged content (a `git add`, blocked from committing by stale-main-gate) vanishing entirely between two read-only status checks — 'git diff/git diff --cached both empty, git status no longer listing the file... No error, no conflict marker, nothing to catch' — caused by a DIFFERENT concurrent session's `reset: moving to origin/main` on the same shared checkout, described one entry earlier in the same file. This is a genuinely distinct sub-mechanism from the two this guard already covers (bare-commit index-sweep; rebase/merge/pull redirected to an isolated clone): here, the session doing the vanishing issued no risky git verb of its own at all (git add, then a commit correctly BLOCKED by a different guard) — the damage came entirely from a THIRD PARTY session's git operation on the same checkout, invisible to this session's own tool calls and therefore outside anything pre_tool_use.py could intercept on this session's side. Not yet built: no guard currently protects a session's own staged-but-uncommitted work FROM a concurrent session's git operation; the existing two mechanisms only stop THIS session from being the one that causes damage. Flagged, not fixed this pass. Separately, this same 2026-08-30 investigation checked whether F9's own 'possibly recurred' digest flag against sessions/2026-08-30.md (today) was a 6th occurrence — it is not: the one regex hit there ('git merge... blocked outright by a hard guard citing failure F9') describes the rung-4 guard firing correctly and blocking a risky merge attempt, recovered via isolated clone, the intended-working case, not a bypass. Two other same-day entries in that file independently report the identical guard firing with the identical recovery — consistent with the guard holding all day, not a fresh incident. Also checked, since a live re-run of failure_recurrence_signals() against the updated ledger surfaced them fresh (sessions/2026-08-25.md and sessions/2026-08-27.md, both newer than the original last_seen and not yet in confirmed_in): 2026-08-27.md's three matches are all the same guard-fired-correctly narration pattern (a git pull/pending operation correctly blocked, isolated clone used) — false positives, not added. 2026-08-25.md has one real incident matching the sweep mechanism ('swept up this session's already-staged doc changes' via a bare commit) — but it is explicitly about **eq-cards'** shared root checkout, not eq-context's; this guard's own is_shared_eq_context() scoping means it was never positioned to fire there regardless, since that incident belongs to F15 (eq-cards' own shared-checkout entry), which has a stated, deliberate scope boundary excluding exactly this git-verb shape ('a commit landing on whichever branch the shared root happens to be on... deliberately left OPEN'). Not folded into this entry to avoid crediting eq-context's guard for a gap in a different repo's guard; flagged for whoever next touches F15, not fixed here — this pass did not carry the same rigor into F15's current guard mechanics that it applied to F1/F9/F10/F12/F14. CHECKED 2026-09-07 (via `/triage`): digest.md flagged a 'possibly recurred' hit in sessions/2026-09-04.md. Investigated directly — FALSE POSITIVE, not added to confirmed_in. Matched text ('A plain `git merge --ff-only origin/main` inside the shared checkout was hard-blocked by hooks/pre_tool_use.py, followed the prescribed remediation') is the rung-4 guard firing correctly and being recovered from as designed — the same 'guard held, not a fresh incident' shape already documented above for 2026-08-27. CHECKED 2026-09-09 (via `/triage`): digest.md flagged a 'possibly recurred' hit in sessions/2026-09-08.md. Investigated directly — FALSE POSITIVE for F9 specifically, not added to confirmed_in. The matched text (multiple 'Concurrent-session checkout collision' entries, at least four separate sessions hitting the identical failure mode in one evening) is real and well-documented, but every instance names the shared checkout at `C:/Projects/eq-shell` — a different repo entirely, out of this guard's `is_shared_eq_context()` scope by construction, same boundary reasoning as the existing F15 note above (eq-cards). eq-shell's own shared-checkout collision problem reads as a real, high-frequency, already-self-documented pattern in its own session logs, but has no eq-context ledger entry of its own (this file only tracks eq-context's shared checkout) — flagged here for visibility, not filed as a new id or folded into F9/F15, since that call belongs to whoever owns eq-shell's own tracking, not this pass. REGEX TIGHTENED 2026-09-10 (same pass as F1/F10/F14): the two false-positive shapes this note already found by hand on 2026-08-30/09-07/09-09 — (a) the guard firing correctly and being recovered from via an isolated clone (2026-08-27, sessions/2026-09-04.md), and (b) the identical language describing a DIFFERENT repo's own shared checkout (eq-cards on 2026-08-25, eq-shell on 2026-08-27/2026-09-08) — were never actually excluded by the regex; each prior check was investigated and correctly judged false by hand, every time, without ever closing the gap. Added two same-line exclusions, applied across all five alternatives (2026-08-25's false positive matches via alternative 1's 'swept up... staged', not just alternative 5, so both exclusions had to be global, not scoped to one alternative): (1) guard-fired-correctly framing (`correctly BLOCKED`, `hard-blocked`, `hit... the... block`, `isolated (git )?clone` — the last one a reliable signal on its own, since a genuinely live, unresolved incident cannot yet describe its own clean recovery); (2) an explicit reference to another product's own checkout (`eq-shell`/`eq-cards`/`eq-field`/`eq-solves-service` near `checkout`, or the literal `C:/Projects/<repo>` path) — deliberately requires proximity to `checkout`, not a bare product-name mention, since eq-context's own tracking files are routinely named `eq/pending/eq-shell.md` etc. and must not trip this. Verified against the full sessions/*.md corpus (153 files): total matches dropped 13 to 4. The one file still matching after last_seen (sessions/2026-09-09.md) is a genuine, live, first-hand incident found the same day this fix was written — a safe_commit.py push landed cleanly but its specific line edits to eq/pending/eq-shell.md were silently reverted by a concurrent session's own commit to the same file — correctly left matching for human /triage judgement, not suppressed; not a false positive, so not excluded and not added to confirmed_in by this pass. 8 unit tests (2 must-catch: the live 2026-09-09 incident verbatim, plus a synthetic genuine-collision case; 6 must-not-catch: both 2026-08-27 false-positive shapes and the 2026-09-08 cross-repo shape verbatim, plus 2 synthetic near-misses) extend `.github/scripts/test_failure_recurrence_signals.py` (now `test_f1()`/`test_f9()`/`test_f10()`/`test_f14()`). NEAR-MISS caught before shipping, worth recording: the first draft of this regex dropped the `(?:^|\\n)` line-start anchor F1/F10/F14 all use, hand-transcribing the pattern into this file from a validated script rather than pasting its exact output. Without that anchor, `re.search` tries the two negative lookaheads from every character position in the file rather than only from line starts, and against at least one real session log this produced catastrophic backtracking — a corpus scan that should take under a second instead exceeded 120 seconds before being killed. Had this shipped, the same blowup would hit `failure_recurrence_signals()` inside `digest-refresh.yml` on every run, risking a workflow timeout for the whole digest, not just F9's own signal. Caught by timing the test run, not by reading the pattern — re-verified the loaded-from-file signal against the full corpus at 0.79s before shipping. Lesson for the next signal-regex edit in this file: always paste a validated script's exact printed/written output, never hand-retype a lookahead-heavy pattern from memory."
    signal: "(?:^|\\n)(?!.*(?:correctly BLOCKED|blocked outright|hard-blocked|hard.?block|guard.{0,20}(held|blocked|fired correctly)|blocked by (the|this) (repo|guard)|hit.{0,30}(the |this )?(identical|same )?.{0,10}block\\b|shared-checkout block|isolated (git )?clone))(?!.*(?:(?:eq-shell|eq-cards|eq-field|eq-solves-service).{0,40}(root )?checkout|checkout.{0,40}(?:eq-shell|eq-cards|eq-field|eq-solves-service)|C:[/\\\\]Projects[/\\\\](?:eq-shell|eq-cards|eq-field|eq-solves-service)\\b))(?=[^\\n]*(?:(swept up|sweeping up)\\b.{0,40}(staged|concurrent session)|stuck rebase.{0,60}(shared|eq-context)|HEAD detached.{0,60}(main ref|stale commit)|conflict marker.{0,40}(committed|landed).{0,40}main|concurrent.session.{0,30}(git race|checkout collision|checkout race)))"
    confirmed_in: ["sessions/2026-08-04.md", "sessions/2026-08-05.md", "sessions/2026-08-20.md", "sessions/2026-08-21.md", "sessions/2026-08-26.md"]

  - id: F10
    title: core.hooksPath silently resolves to the wrong location — four distinct mechanisms, one symptom
    first_seen: 2026-05-24
    last_seen: 2026-09-07
    recurrences: 5
    rung: 4
    target_rung: 4
    guard: "system/lessons.md's 2026-05-24 entry (rung 1, prose only) plus hooks/session_start.py's HOOKS check (2026-08-05: prints local/--worktree/effective scope, warns loudly on any mismatch, every session, unprompted, same mechanism F1's FRESHNESS check uses) — real signal, but print-only, didn't itself close the gap. PROMOTED to rung 4 same day, same investigation thread: hooks/pre_tool_use.py now BLOCKS an explicit `git config` SET of core.hooksPath to anything but .githooks (no-scope/--local/--worktree/--global all covered; --unset deliberately left alone — see HOOKSPATH_SET_RE's own comment, a --worktree --unset specifically can be the legitimate fix for mechanism (3) below, not a new instance of it), scoped to the ONE shared eq-context checkout via the identical is_shared_eq_context() exact-path check F9 already built and proved out. Adversarial suite: 12 new cases — all 3 real recurrence shapes reproduced and blocked (wrong directory, --worktree override, and the --global case defensively), the correct value plus a differently-spelled normalized-equivalent both proven to NEVER block, a private-clone control (F10's own escape valve, mirroring F9's), a -C-prefix and a backslash-path-separator case. 91/91 python suite, 36/36 bash, 0 regressions. Extended 2026-09-06 for mechanism (4): hooks/session_start.py's HOOKS check now self-heals the completely-unset case directly — runs `git config --local core.hooksPath .githooks` itself and re-verifies via the same read used for the steady-state 'ok' case — instead of only printing; a wrong-but-SET value (mechanisms 1-3's shape) still only warns, unchanged, since overwriting a value someone may have set deliberately is a different risk than filling an empty one. 4 new adversarial cases (3 covering the self-heal path, 1 control proving a wrong-but-set value is left untouched). Full python suite 150/150, 0 regressions. The bash suite (`hooks/adversarial_test.sh`) is not CI-authoritative (`.github/workflows/adversarial-suite.yml` only runs the python suite) and failed uniformly here on an unrelated, pre-existing issue: this machine's python3 resolves to the Windows Store app-execution-alias stub, not a real interpreter, while python correctly resolves to 3.14.7 — every bash-suite case failed identically (exit 49) regardless of what it tests, confirming it's environmental, not a regression."
    detected_by: "none automated, all three incidental — (1) 2026-05-24: a human noticed the auto-bump-frontmatter bot fighting every push and traced it back; (2) 2026-08-04: found while building F8's own status-enum guard, not by the guard itself; (3) 2026-08-05: found by hand mid-investigation into a different question (F9's git-level-hook analysis, throwaway sandbox testing of pre-commit/commit-msg behaviour), not by any standing check."
    cost: "(1) 2026-05-24: the pre-commit date-bump hook silently skipped; the since-retired auto-bump-frontmatter.yml bot kept pushing its own correcting commits, every push needed a pull --rebase first. (2) 2026-08-04: the governed .githooks/pre-commit (secret scanning + F8's new status-enum check) had never run on the Beelink at all — a real exposure window on the secret-scanning guard specifically. (3) 2026-08-05: F8's own guard silently stopped running AGAIN, this time on the shared checkout's own main worktree, despite F8 being marked closed at rung 4 — a rung-4 guard was actually rung 0 in practice, undetected until found by hand mid-investigation into something else entirely."
    note: "Four distinct mechanisms, same symptom, same root shape (core.hooksPath resolves somewhere other than the governed .githooks): (1) 2026-05-24 — hooksPath was set to hooks/ (containing only post-commit), while the real pre-commit hook lived in .githooks/pre-commit, so .githooks was never read at all. Fixed by consolidating both hooks into .githooks and repointing. Captured only as prose in system/lessons.md — no guard, rung 1. (2) 2026-08-04 (F8) — hooksPath pointed at .git/hooks, which held an untracked but load-bearing shadow copy of scripts/pre-commit-secrets.sh, so the governed .githooks/pre-commit had never run on the Beelink at all. Found incidentally while building F8's fix; folded into F8's own note field rather than given its own id — F8's note explicitly calls it 'a recurrence of the 2026-05-24 core.hooksPath lesson.' (3) 2026-08-05 — during the F9 git-level-hook investigation, a --worktree-scope core.hooksPath override of .git/hooks was found live on the shared checkout's own main worktree, silently shadowing a correct --local value of .githooks (--worktree scope wins over --local when extensions.worktreeConfig is true). Root-caused to the F8-fix session's own worktree-repointing loop (sessions/2026-08-04.md 'Notes': repointed all 5 open worktrees' hooksPath to .githooks, checked each for live secret-scanning content, then reverted 4 of 5 back to .git/hooks since their branches predated F8's secret-guard delegation) most likely leaking a --worktree-scope command onto main by accident — main was never named as a target anywhere in that session's plan. Fixed by hand: `git config --worktree core.hooksPath .githooks`. Documented in eq/pending.md's 'Correction, 2026-08-05' note under 'eq-context: shared-checkout git races' (NOT ops/pending.md, which has no core.hooksPath mentions at all) and explicitly flagged there as 'not filed as its own ledger item here' — this entry closes that gap. Partial mitigation shipped same day as this entry: hooks/session_start.py's new HOOKS check (see guard: above) — real signal, but print-only, so target_rung 4 stayed open until something actually prevented a wrong value from taking effect. CLOSED same day, same investigation thread: hooks/pre_tool_use.py now blocks the SET itself (see guard: above for the exact mechanism and test coverage) — exactly the fix this note named as the open gap. The 4 worktrees eq/pending.md documents as deliberately still on .git/hooks (their own .githooks/pre-commit predates the secret-guard delegation, so repointing them would have removed their only secret guard until their branches catch up) are unaffected by construction, not by a special case written for them: the new guard is scoped to the ONE shared checkout by exact path, identical to F9's own is_shared_eq_context(), so a linked worktree — any of them, not just these 4 — was never in scope regardless of what its core.hooksPath happens to be set to. CHECKED 2026-08-30: digest.md flagged a 'possibly recurred' hit in sessions/2026-08-26.md. Investigated directly — FALSE POSITIVE, not added to confirmed_in. The matching text is hooks/session_start.py's own '*** LATENT SHADOW ***' diagnostic firing exactly as designed: effective core.hooksPath correctly resolves to .githooks ('ok for now'), the print is warning only that the dormant --worktree/--local values disagree (a pre-existing condition since the 2026-08-05 fix, not a new event) — the session log itself calls it out explicitly: 'this is F10's own guard working as intended,' no action taken, that session never wrote through the shared checkout at all. Distinct from a genuine recurrence, which would need the EFFECTIVE value itself to resolve wrong, or the rung-4 SET-blocking guard in pre_tool_use.py to fail an actual wrong SET — neither happened here. This same LATENT SHADOW condition was still present and printing at the start of the 2026-08-30 session that ran this check, unchanged — a real, known, informational-only standing risk (documented already above), not a new incident each time it prints. (4) 2026-09-06 — a brand-new Windows PC's first eq-context session found core.hooksPath completely unset at every scope (local, --worktree, --global, --system all confirmed empty via explicit `git config --get`), with extensions.worktreeConfig itself also unset. A different shape from (1)-(3): never configured, not misdirected — core.hooksPath is local git config and a fresh `git clone` cannot set it for itself. Mechanisms (1)-(3) were checked and ruled out first: no shadow file at .git/hooks/pre-commit, and `git rev-parse --git-dir`/`--git-common-dir` both resolved to `.git`, confirming a normal checkout, not a linked worktree. hooks/session_start.py's HOOKS check caught it correctly and unprompted, same as designed ('resolves to None, not .githooks'). hooks/pre_tool_use.py's rung-4 guard was confirmed live mid-fix: it blocked a `git config --local --get core.hooksPath || echo ...` diagnostic read as an apparent SET to the literal value `||` (the guard's command-text matching doesn't distinguish a shell `||` fallback from a real value) — a false positive on a read, not a failure to catch a real bad write; resolved by rerunning the read without `||`. Fixed with the standard `git config --local core.hooksPath .githooks`, confirmed via `--get`. No target_rung change: the existing detect-then-fix flow worked exactly as designed against a genuinely new scenario (first-time setup on a fresh clone) — it didn't surface a gap in that flow. Real open gap, filed separately rather than folded into this fix: nothing sets core.hooksPath automatically on `git clone`, so every new machine or throwaway isolated clone starts here by construction (this same session's own isolated clone for an unrelated commit needed the identical manual fix minutes earlier). CLOSED same day: hooks/session_start.py's HOOKS check now self-heals exactly this case (see guard: above) instead of only printing, and system/onboarding.md gained a 'First-time setup' step covering the pure-human/non-Claude-Code path that hook cannot reach. No target_rung change needed — rung was already 4. (5) 2026-09-07 — CONFIRMED as a genuine recurrence via Royce's explicit decision on 2026-09-09 (`/triage`, NY4): a session's own resume gate reported core.hooksPath resolving to the wrong-but-set shape (the absolute path `C:\\Projects\\eq-context\\.githooks`, matching mechanisms 1-3, not the fully-unset shape the 2026-09-06 self-heal covers) at start. A second, unrelated session working the same day (via `EnterWorktree`) hit the identical symptom independently, minutes apart — the effective value was observed wrong twice, by two different sessions, which is this file's own documented bar for a genuine recurrence rather than a stale-print LATENT SHADOW false positive (see the 2026-08-30/sessions-08-26 check above for that distinction in practice). By the time either session checked again, the value was already back to the correct `.githooks` (single clean `.git/config` entry, `extensions.worktreeConfig` confirmed off so mechanism 3's worktree-shadow is structurally ruled out). Cause unconfirmed: neither session fixed it, and whether Royce corrected it by hand in between or something else did is still unknown. Proposed to `ops/pending.md` on 2026-09-07 per this file's own governance (a recurrence-count bump is a proposal, Royce decides — see F12's 2026-08-18 promotion for the same process) rather than self-filed at the time; confirmed here 2026-09-09 once Royce made the call. Root cause and whether `EnterWorktree` is actually the trigger remain untested — open follow-up, not this entry's fix. REGEX TIGHTENED 2026-09-09 (same pass as F1's own tightening, same shape of fix): the confirmed 2026-08-26 false positive is the HOOKS \"LATENT SHADOW\" diagnostic firing exactly as designed — the EFFECTIVE core.hooksPath value resolves correctly, and the guard is only warning about a dormant --worktree/--local disagreement, never a live wrong resolution. Added a same-line exclusion for that diagnostic's own self-description (`LATENT SHADOW`, `resolves? correctly`, `working as (intended|designed)`, `print-only by design`, `informational...only`) using the same `(?:^|\\n)` line-boundary + lookahead technique as F1, since the real compile call is `re.I`-only. Verified against the full sessions/*.md corpus (152 files): all 3 genuine incidents (2026-05-24 mechanism 1, 2026-08-05-g mechanism 3 — both its title and detail line, 2026-09-07 mechanism 5) still match; the 2026-08-26 false positive no longer matches, and neither does an earlier, previously-unflagged instance of the identical LATENT SHADOW shape found in sessions/2026-08-05-z.md while building this fix (never live-flagged itself, since 2026-08-05 predates last_seen and was never rescanned, but the same underlying diagnostic text). New matches are structurally impossible (the exclusion can only narrow the three existing alternatives, never widen them) — confirmed empirically too: zero new hits anywhere in the corpus. A handful of other 2026-08-05 mentions (the day the guard itself was built) still match — describing the guard's capability, its build, or its ratchet-promotion process rather than reporting a fresh incident — left as-is: they predate `last_seen` by 34+ days and are never live-rescanned regardless, so tightening further against them would harden a case with no operational effect, unlike the LATENT SHADOW shape which is a standing, still-printing condition (see (3) above) that can recur in a future session log at any time. Same 10-test file as F1 (`.github/scripts/test_failure_recurrence_signals.py`) extended with F10's own must-catch/must-not-catch cases."
    signal: "(?:^|\\n)(?!.*(?:LATENT SHADOW|resolves? correctly|resolved correctly|working as (?:intended|designed)|print-only by design|informational.{0,10}only))(?=[^\\n]*(?:core\\.hooksPath.{0,40}(wrong|shadow|silently|drift)|hooksPath.{0,30}(worktree|shadow)|pre-commit.{0,30}(silently skip|never ran|didn't run|isn't running)))"
    confirmed_in: ["sessions/2026-09-07.md"]

  - id: F11
    title: A scheduled bot workflow can fail silently for days — nothing watches eq-context's own crons
    first_seen: 2026-08-01
    last_seen: 2026-08-05
    recurrences: 1
    rung: 4
    target_rung: 4
    guard: "refresh_digest.py's new scheduled_workflow_health() (discovers every .github/workflows/*.yml with a schedule: trigger at read time — never a hardcoded list, the exact drift class that caused this bug — and checks each against its own event=schedule GitHub Actions run history) feeds into digest.md's Needs You section, read unconditionally every session by hooks/session_start.py's existing NEEDS YOU parsing — the identical always-on, unprompted-every-session mechanism F1/F3/security-findings/Sentry already rely on for rung 4, not a new pipeline. Fires at >=1 consecutive scheduled-run failure (🟠), escalates to 🔴 at >=2 — deliberately lower bar than the ratchet's own recurrences>=2 promotion threshold, since 'silent for days' was the actual cost here and same-night visibility is the whole point. Pure counting logic (_scan_run_conclusions) unit tested, 8/8, wired as a pre-flight step in digest-refresh.yml (test_scheduled_workflow_health.py) — same 'test before trusting a script that writes the substrate's most-read file' posture pending-rotate.yml already holds itself to."
    detected_by: "human — Royce asked 'is all this extra eq context sessions due to the improvements we recently made?' about the fast-growing Needs You list; traced via `gh run list --workflow=pending-rotate.yml` to 4 consecutive failed nightly runs, not found by any standing check"
    cost: "eq/pending.md grew 368KB -> 500KB over 4 days of zero rotation during the highest-volume session week yet (~100 commits/day in eq-context alone) — the SAME bloat the 2026-08-01 /decide session had just fixed, un-fixed by an adjacent commit 4 minutes later in that same session (see F11's note). More broadly: 18 separate workflows currently carry a schedule: trigger (backups, restore drills, security audits, drift checks) and every one of them was equally invisible to failure — this specific incident is pending-rotate.yml, but the exposure was suite-wide."
    note: "This is not a new bug class in isolation — it's the same 2026-08-01 backlog-bloat incident recurring, caused by the very fix that closed it. Commit c5fbe4f (09:31, /decide with Royce) shipped the verify-queue split that fixed eq/pending.md's 368KB bloat. Commit b8a7bc4 (09:35, four minutes later, same session) wired the new verify-queue.md files into pending-rotate.yml's git add list — unconditionally, including ops/verify-queue.md, which is only ever created once OPS has a verify item to move and had none. `git add` on a nonexistent pathspec is fatal under bash `set -e`, so the workflow's 'Commit if changed' step died before reaching `git diff --staged --quiet`, every night, starting that same day. All 43 rotate_pending.py unit tests kept passing throughout (they test the Python engine, not the YAML step around it) and every commit carries [skip ci], so nothing in CI, digest, or a human scanning `git log` would ever see it fail. Fixed same session as this entry (see eq/pending.md and the 2026-08-05 commit fixing pending-rotate.yml's git-add loop) by only adding files that exist on disk. The guard here is deliberately broader than that one-line fix: it doesn't re-check pending-rotate.yml specifically, it watches every scheduled workflow eq-context has, because the actual lesson is 'an unattended [skip ci] cron has zero human witnesses by construction' — the next silent failure will not be this same line of YAML. KNOWN SCOPE BOUNDARY, stated plainly rather than hidden: this guard runs inside digest-refresh.yml itself; if digest-refresh.yml's own cron ever stops running entirely (as opposed to running and finding nothing), nothing here notices — digest-refresh.yml's repository_dispatch trigger on every EQ-repo push to main is the only mitigation, not a closed guarantee. Not treated as a second recurrence of this same failure — a different, harder 'who watches the watchmen' question, out of scope for this entry."
    signal: "cron.{0,40}(silently|fail(ed|ing)).{0,40}(day|days|undetected)|scheduled workflow.{0,40}fail(ed|ing).{0,40}(silent|undetected|days)|nightly (cron|workflow).{0,60}(broken|failing).{0,40}(day|days|noticed)"
    confirmed_in: ["sessions/2026-08-05-v.md"]

  - id: F12
    title: Side-clone reconciliation blind-overwrote a concurrent session's already-pushed shared-file edits
    first_seen: 2026-08-05
    last_seen: 2026-08-17
    recurrences: 2
    rung: 4
    target_rung: 4
    guard: "BUILT 2026-08-18, on top of a 2026-08-17 partial fix. The 2026-08-17 piece: hooks/substrate_sync.py's UserPromptSubmit sync (runs on every prompt, fetches + `pull --ff-only` against the SHARED root checkout whenever the last fetch is >10min old) never checked whether that checkout had uncommitted/staged changes before pulling. Added working_tree_dirty() (git status --porcelain) as a precondition -- dirty main now gets fetch-only, never a pull. Also fixed that day: the LIVE UserPromptSubmit wiring in C:/Users/EQ/.claude/settings.json (user scope, ungoverned, no tests) still ran an old inline PowerShell git-pull blob instead of calling this governed script, so the 2026-08-15 rewrite documented in substrate_sync.py's own docstring had never actually taken effect live; rewired to match hooks/settings.template.json. That closed ONE mechanism, not the general class. The 2026-08-18 piece closes the general class: hooks/pre_tool_use.py now blocks any cp/mv/copy/xcopy/robocopy/Copy-Item/Move-Item whose DESTINATION path resolves inside the SHARED eq-context checkout while its SOURCE does not -- the general 'diff/refuse before copy-back' guard this entry named as still open. Scoped on the destination path STRING itself (_copy_targets_shared / _is_under_shared), not on cwd or repo-root the way F9/F10 are, since the incident shape is an absolute-path copy issued from anywhere. Same-checkout copies and copies OUT of the shared checkout (the legitimate escape-valve direction -- push FROM the side-clone) are correctly left alone; the message directs the correct alternative (`git push origin main` from the other checkout, which fails loudly on divergence instead of silently clobbering). Not sandbox-gated, same reasoning as F9/F10/F13: both real recurrences happened natively on Windows. 18 new adversarial cases (hooks/adversarial_test.py): cp/mv/xcopy/robocopy/Copy-Item/Move-Item in bash and PowerShell, Windows-backslash and Git-Bash /c/... destination spellings, same-checkout and out-of-checkout controls, an EQ_CONTEXT-elsewhere dormancy control, and `scp`/a script literally named 'backup-copy.sh' proven not to false-positive. One real bug caught by the suite itself before shipping: the first version's flag-detection (skip any token starting with '-' or '/', to tolerate PowerShell's `-Path`/`-Destination` and robocopy's `/E`/`/MIR`) also silently swallowed the '/c/...' Git-Bash destination form as if it were a bare flag, defeating the exact msys-path case this guard exists to catch; fixed via _looks_like_flag()'s further-slash check (a real path always has one after the drive segment, a Windows flag never does) before merge, not found live. 124/124 adversarial suite, 0 regressions. Known, stated narrowing: does not parse robocopy's trailing multi-file list or wildcard globs -- catches the incident shape that actually happened (a single-file cp/Copy-Item), not every copy grammar."
    detected_by: "2026-08-05: human/session self-catch, diffed the push before trusting it. 2026-08-17: session self-catch -- diffed a push before trusting it a second time, same discipline, different mechanism (staged content vanished from the shared root checkout between two read-only commands, not from a side-clone copy-back)."
    cost: "2026-08-05: eq/pending.md briefly lost a core.hooksPath/F10 section and a PR #1254 write-up (4-item deferred list) — both already pushed by a different concurrent session — for one push cycle on main, before being caught and restored in a follow-up commit (e20df0f). 2026-08-17: a session's own staged edits to eq/pending-archive.md, two product changelogs, and sessions/2026-08-17.md vanished cleanly (HEAD moved, working tree fully clean, zero error, zero stash entry) between two read-only diagnostic Bash calls with no git command of the session's own in between -- redone from scratch in an isolated worktree rather than the shared root, per F9's own recommended recovery path (itself blocked on the first attempt by an unrelated guard.js bug, fixed same session -- see detect-fake-worktree note below). No commit was ever lost (nothing had been pushed yet), so the true cost was session time re-doing the edits, not permanent data loss."
    note: "CLOSED 2026-08-18 -- see guard: above for the general destination-path guard that closes the gap this entry named as open ('a manual side-clone copy-back can still blind-overwrite without triggering this check'). Approved for promotion by Royce via /decide (F12 had recurred twice; propose-only ratchet, per 2026-07-11 -- it argues, he decides). 2026-08-05 note unchanged below. 2026-08-17 addition: root-caused via direct inspection of hooks/substrate_sync.py plus the LIVE (not template) C:/Users/EQ/.claude/settings.json -- the settings.json drift (still running the pre-2026-08-15 inline PowerShell pull instead of the governed script) is itself a THIRD instance of the exact 'template updated, user-scope copy never done' pattern this repo has hit before for SessionStart (fixed 2026-07-12) and pre_tool_use.py/F9 (fixed 2026-08-05) -- worth a standing check that user-scope settings.json actually matches settings.template.json, not just a one-off fix, but not built today (out of scope for this pass). Separately, while recovering from this incident, the F9-recommended escape valve (`git worktree add <path> origin/main`) itself failed on the first attempt against an external path: guard.js's detect-fake-worktree rule normalizes a Git-Bash `/c/...` drive-letter path before checking for `.git` (fixed 2026-08-05) but had no equivalent handling for the `/tmp` MSYS mount, so a real, valid linked worktree created under the OS temp dir was misreported as fake (fs.existsSync checked the wrong OS-level path). Fixed same day by extending the existing normalization helper to also resolve `/tmp` via os.tmpdir() -- same fix shape as the original /c/ case, one mount style closed, not a general MSYS-path solution (a future unhandled mount alias would still misfire, failing open per this file's existing design). Original 2026-08-05 finding unchanged: happened while reconciling eq/pending.md through an isolated side clone — the F9-mandated escape valve for rebase/merge/pull on the shared checkout, since a direct pull/rebase there is itself blocked by F9's own guard. The reconciliation step needed to copy a local edit into the clone to merge it back, but the first attempt copied the checkout's older local copy OVER the clone's freshly-pulled content instead of merging the two — silently reverting the other session's already-pushed edits for one push cycle. Caught immediately by diffing before trusting the push, not by any guard. Surfaced into this ledger via a /decide pass run on a different question entirely (whether F6/F7's 'possibly recurred' gate flag was real) — that investigation found F6/F7 were both false alarms (single historical incidents, both already at target_rung, zero NUL bytes found on a direct byte-scan of every file touched that session) but turned up this adjacent, genuinely new, not-yet-ledgered gap along the way. CHECKED 2026-08-30: digest.md flagged a 'possibly recurred' hit in sessions/2026-08-21.md. Investigated directly — FALSE POSITIVE for F12 specifically, not added to confirmed_in here. The matching text ('by the very next status check both the staged content AND the working-tree edit were simply gone') is real data loss, but no cp/mv/copy/xcopy/robocopy/Copy-Item/Move-Item command appears anywhere near it — this guard's rung-4 mechanism (blocking a copy-back whose destination resolves inside the shared checkout) was never in a position to fire, because no copy-back happened. The actual mechanism is a concurrent session's own git operation (a `reset: moving to origin/main`) silently wiping this session's staged-but-uncommitted content with zero error — F9's failure shape, not F12's. Logged instead under F9 (see that entry's 2026-08-30 addition, sessions/2026-08-21.md now in F9's confirmed_in, recurrences bumped 4→5) rather than here, to avoid double-counting one incident under two IDs. CHECKED 2026-09-07 (via `/triage`): digest.md flagged a 'possibly recurred' hit in sessions/2026-08-30.md. Investigated directly — FALSE POSITIVE, not added to confirmed_in, and a self-referential one: the matched text is this same investigation session's own write-up of the 2026-08-30 five-flag check (the paragraph beginning 'All 5 flagged possibly recurred items investigated...' two entries above), not a new incident — the ledger's own historical record of checking F12 is what re-triggered F12's signal regex against the file that recorded the check. RE-FLAGGED AGAIN 2026-09-09 (via `/triage`), same file (sessions/2026-08-30.md), same self-referential match — not re-investigated (nothing left to find, this is a permanent loop until the regex changes) and not spawned as a task either, since a fresh investigation would just re-arrive at this same paragraph. Regex tightening remains the real fix, still not attempted; noted directly here instead."
    signal: "(cp|copy)(-| )overwr(o|i)te.{0,60}(clone|checkout)|silently revert(ed)?\\b.{0,80}(already.pushed|pushed edits|concurrent session|another session)|blind.?overwr(o|i)te.{0,40}(pending|shared|clone)|staged (edits|changes|content).{0,80}(vanish|gone|disappear)|(working tree|checkout).{0,40}(fully clean|fast.?forward).{0,80}(staged|uncommitted).{0,40}(gone|vanish|lost)"
    confirmed_in: ["sessions/2026-08-05-o.md", "sessions/2026-08-17.md"]

  - id: F13
    title: "\"eq-shell doesn't auto-deploy on merge\" — a false deploy-posture claim that survived two re-verifications because its evidence was still true"
    first_seen: 2026-08-14
    last_seen: 2026-08-15
    recurrences: 2
    rung: 4
    target_rung: 4
    guard: "BUILT 2026-08-15 as proposed — scan_deploy_posture() + classify_deploy_posture() in scripts/substrate_honesty.py, running in substrate-honesty.yml on every PR to main and nightly at 11:00 UTC. Flags 'manual-deploy-only'/'explicit-only'/'merged but not live'/'not deployed'/'doesn't auto-deploy'/'Royce to trigger' on any line naming eq-shell or core.eq.solutions. Allow-lists the genuinely manual repos by name (eq-cards, eq-receipts, eq-website, sks-nsw-labour, cloudflare, eq-intake), exempts historical record via the existing is_historical(), exempts failures.md/the script/its tests, and suppresses a line that quotes the claim in order to correct it. 13 unit tests pin the precision rules (7 must-catch from real wordings that actually shipped, 6 must-not-catch from real false positives) plus an adversarial injection test run at build time. STILL REPORT-ONLY, so this is not yet rung 4: substrate-honesty.yml deliberately does not set SUBSTRATE_HONESTY_STRICT=1, and flipping it would make every check in that script blocking (including network-dependent liveness probes), which is a separate call for Royce. Two precision bugs were caught by the tests during the build and are worth knowing: a bare 'auto.?deploys?' alternative in the correction pattern swallowed 'doesn't auto-deploy' — the most common wording of the false claim — and would have shipped a guard that caught nothing; and git grep decoded with text=True raises UnicodeDecodeError on this substrate's em dashes under Windows cp1252, leaving stdout None (latent in the pre-existing stale-ref scan, whose sole ASCII-only token never triggered it; encoding is now pinned in both scans). RUNG 4 COMPLETED 2026-08-15 by a second session, on top of the above rather than beside it: hooks/pre_tool_use.py blocks a Write/Edit that puts the claim into any non-historical .md, so it never reaches disk. That closes the gap the scanner note names — prevention without flipping SUBSTRATE_HONESTY_STRICT=1, which stays Royce's call because it would make the network-dependent liveness probes blocking too. The hook IMPORTS classify_deploy_posture() rather than restating its patterns: two copies of this definition drifting apart is the exact shape of F13, so a guard built by copy-paste would reproduce the failure it exists to prevent. It adds only SCOPING — the scanner classifies a line at a time, which suits a file it can re-read around, while a Write payload arrives as one blob, so the hook splits it into bullets first. That is a real difference, not cosmetic: a claim wrapped across two lines of one bullet ('...production deploys on\\n  core.eq.solutions are a separate explicit step') reads False on BOTH lines and True on the bullet. A tree sweep found 0 such wrapped claims already on disk, so the line-scoped scan is hiding nothing today; the gap is prospective and the hook closes it at write time. Placed ABOVE pre_tool_use's in_sandbox() gate, like F7/F9/F10 — both recurrences were written from a normal Windows session, so a sandbox-scoped guard would have been dormant for both. Fails open on import error: a broken classifier must not become an outage on the Write tool. 11 adversarial cases (hooks/adversarial_test.py), EQ_FORCE_GUARD pinned per case so F2's mount rule cannot green them for the wrong reason."
    detected_by: "human — Royce, a day after the same claim was corrected in global ~/.claude/CLAUDE.md; the substrate copy outlived the correction and kept propagating"
    cost: "a phantom 'Royce to trigger when ready' action item sat in eq/pending.md and was surfaced verbatim in digest.md — auto-loaded into every session for ~1 day — for work (PR #1346) that had been live since the second it merged. At least four PRs (#1343, #1344, #1355, #1361) were reported to Royce as merged-but-not-live when all four were already serving production. Worst case not realised: auth-change PR #1352 sat in system/worktree-registry.md annotated 'core.eq.solutions is manual-deploy-only', which would have made merging it read as a safe intermediate step rather than as deploying an auth change."
    note: "The trap is that the claim's two supporting observations are STILL literally true — cdp_enabled_contexts really is [deploy-preview], and production deploys really do report deploy_source: api. Only the inference was false: 'api' does not mean 'a human triggered it'. Anyone re-checking the config finds it exactly as described and marks the note verified, which is how it passed two re-verifications while staying wrong. Same shape as the retired supabase/MIGRATION-LEDGER.md (eq-shell PR #1363): a stale doc that still reads as authoritative and inverts a verified fact. Ground truth: 13 consecutive merges to eq-shell main produced 13 production deploys, live 2-4s later, via Netlify's own GitHub App (installation 121276861). NEVER re-derive this from deploy_source/cdp_enabled_contexts — correlate merge timestamps against deploy created_at, and check liveness by commit ancestry against the newest ready production deploy, not by your own deploy's state (concurrent merges produce state:error/Skipped deploys whose commits still shipped inside a later build)."
    signal: "core\\.eq\\.solutions.{0,80}(doesn'?t|does not|never) auto.?deploy|core\\.eq\\.solutions.{0,80}(manual-deploy-only|explicit-only|manual.{0,20}trigger|is a separate explicit step)|(merged but not|not deployed|manual.{0,20}trigger|manual-deploy-only|explicit-only).{0,80}core\\.eq\\.solutions|eq-shell.{0,60}(manual-deploy-only|deploy.{0,20}separate explicit step)"
    confirmed_in: ["sessions/2026-08-14.md", "sessions/2026-08-15.md"]

  - id: F14
    title: A hand-written claim about current state ages into a lie, and nothing anywhere notices
    first_seen: 2026-06-22
    last_seen: 2026-09-04
    recurrences: 5
    rung: 4
    target_rung: 4
    guard: "BUILT 2026-08-15. scripts/review_clock.py classifies every tracked .md as generated/record/state from its path and clocks only the state — 30/60/90 days by read_priority, derived from last_updated, never a hand-typed date. Runs in md-health.yml on every PR and push (rung 3), and hooks/session_start.py imports the same pure functions to name any MANDATED file past its clock at the moment the gate tells a session to read it (rung 4 — the action worth intercepting is not a file aging, there is no write to hook, it is a session TRUSTING a stale file). Three things gate immediately with zero backlog to grandfather: an invalid kind value, a record carrying a review_by, and a generated file past its 3-day refresh window — that last one turns the generated class from an exemption into a cron-liveness check, since a dead nightly action leaves digest.md and suite-state.md frozen while still reading as current. The overdue count itself is a lower-only ratchet at the measured 15, not a cliff: a blanket clock over all 174 status:live files would have gone overdue on 82 of them on day one, been grandfathered on day one, and become F10. 44 unit tests on the pure logic, 5 REVIEW cases in hooks/adversarial_test.py, and 4 mutation tests run at build time (lowered ceiling, frozen cron, record given a clock, typo'd kind — 4/4 caught)."
    detected_by: "human, every time — Royce or a session noticing by hand that a file disagreed with the live system. Never by a guard."
    cost: "urjh (2026-06-22): the substrate pointed the whole fleet at a DELETED Supabase project for weeks after deletion, laundering a wrong premise into every session that read it. EQ Quotes (until 2026-07-12): a status table dated 2026-05-21 in global CLAUDE.md still called Quotes 'Built' long after retirement and Cards/Shell/Service 'in build' after they went live, contradicting canonical for ~7 weeks. SKS brand hex (2026-07-30): rules/brand-sks.md carried #1F335C after the real value moved to #203060 — found only by a hand-check against organisations.branding.palette.primary, by which point the stale value had been hardcoded as the default in every EQ Field transactional email."
    note: "Distinct from F1, which is the TRANSPORT going stale (a CDN alias serving 8-12 day old bytes). This is the CONTENT going stale while the transport is perfect: a fully-synced clone can serve a 62-day-old claim, and until this guard nothing looked. Also distinct from F13, which is one specific false claim with a bespoke scanner; this is the general class. substrate_honesty.py already covers 9 hand-listed facts, which is a whitelist — it catches only what someone thought to enumerate, and all three occurrences above were outside it. The measurement that opened this entry: 174 files declared status: live and 82 had not been touched in 30+ days, but most were MISLABELLED rather than stale — a session log from May is not out of date, it is a record of May. Split properly: 3 generated, 234 records, 91 state, of which 15 overdue. That split is the whole fix; the 82 number is what makes the problem look unfixable and therefore go unfixed. Opened at rung 4 rather than climbing the ladder because the guard shipped in the same change — there was no interval during which this sat as prose. RECURRED 2026-08-30 — a NEW instance, distinct in shape from the first three: not a whole file/table going stale, but one hand-written narrative bullet inside suite-state.md's Key Decisions log (the 2026-08-12 mobile-centering entry) going false while the FILE's own regeneration cadence looked perfectly healthy. suite-state.md is classified `kind: generated` (3-day clock, checked in md-health.yml) because most of it is nightly-rebuilt tables — but Key Decisions is hand-appended prose that the nightly regen never re-verifies against live state, so the file passing its freshness check says nothing about whether an individual narrative claim inside it is still true. The claim ('Prestart/Toolbox list pages are supervisor-only by permission grant') was accurate when written 2026-08-12, silently falsified by an unrelated 2026-08-24 permission-matrix change, and caused a real live bug — a supervisor-permitted worker couldn't see the Prestart button for 6 days (Cameron Tregoning, eq-field PR #839) — before a 2026-08-30 session traced the bug back to this exact stale claim. Corrected in suite-state.md same day. This is a real gap in the review_clock.py guard's coverage, not a bypass of it: the guard was never positioned to check this content in the first place, since 'generated' files are exempted from the per-content staleness clock by design. Flagged, not fixed this pass — closing it would mean either reclassifying hand-maintained sub-sections of generated files (a real design question, not obviously right either way: Key Decisions gets evicted/rewritten by the same nightly process, just not content-checked) or adding a narrower guard scoped to that one section. Left for Royce to decide whether it's worth building. RECURRED 2026-09-04 — a 5th instance, same sub-mechanism as the 2026-08-30 recurrence (hand-maintained prose inside a nominally `generated`-classified file, outside review_clock.py's per-content clock by design): suite-state.md's hand-maintained 'Import/write-time tooling' table listed Shell's Contacts dedup as reimplementing Intake's fuzzy matcher — true before eq-shell #1287, silently false after — caught by a human, not any guard, and fixed same-day via eq-context PR #200 (squash 9a3df2f3). Found and closed within one session (sessions/2026-09-04.md, session 'h'), no live-bug cost this time, unlike the 08-30 recurrence's 6-day Prestart-button bug. Same open structural gap the 08-30 note already names applies here too — not re-argued, not fixed this pass. CHECKED 2026-09-07 (via `/triage`): digest.md flagged a 'possibly recurred' hit against sessions/2026-09-04.md — investigated directly, this IS a real recurrence (not a false positive like the same day's F1/F9/F12 checks), added above. CHECKED 2026-09-09 (via `/triage`): digest.md flagged a 'possibly recurred' hit in sessions/2026-09-09.md — the line 'The real gap: pending.md's own staleness was never checked before it was used to build a `/decide` question,' describing an `eq/pending/eq-shell.md` entry that said 'not built, Royce's call' when another concurrent session had already shipped the fix. Real, but a different flavor than the four instances above: those were `status: live` reference/state files outside review_clock.py's per-content clock; this is an ordinary pending.md checklist item lagging a concurrent session's own work by less than a day, in a file whose whole design assumes exactly this kind of latency (that's what close/triage/rotate_pending.py exist to catch). The originating session itself weighed this and declined to escalate ('not proposing a protocol change unprompted... just naming the gap') — that restraint isn't re-litigated here: not added to confirmed_in, no recurrence bump, since promoting this ledger's counts is Royce's call per its own established process (see F10's 2026-09-07 entry in ops/pending.md for the same standing rule), not something a fast `/triage` pass should self-file on a question the originating session already considered and closed. REGEX RESTRUCTURED 2026-09-10 (same pass as F1/F10's own tightenings, but a materially harder problem — not one clean false-positive shape): full-corpus audit found alternative 1 had a latent directional bug (it only ever checked stale-word-THEN-filename, never the reverse), so the two real, text-verified incidents this file cites (2026-08-30, 2026-09-04) were actually matching by ACCIDENT, via a coincidental later mention of the generic word `substrate` elsewhere in the same long bullet — not via the real `suite-state.md...stale` connection, which is in the wrong order for a one-directional regex. Made bidirectional. Separately, `pending.md` as an anchor term was found to drive most of this signal's noise (80+ lines across the corpus): dropped, on the same reasoning the 2026-09-09 check above already gave — pending.md staleness is routine, expected latency (that's what close/triage/rotate_pending.py exist to catch), not this failure's class. Bare `substrate` also dropped as an anchor: too generic (it is not how the two real incidents actually connect once the directional bug is fixed), and dropping it independently resolves two currently-live false triggers — this session's own F1/F10 write-up (sessions/2026-09-09.md, which mentions `substrate` extensively while describing an unrelated fix) and the 2026-09-09 pending.md flag analyzed above. Remaining anchors (CLAUDE\\.md, suite-state, brand-) gated to a tuned 35-character same-line window: wide enough for both real incidents' verbatim text, tight enough to exclude several instances of unrelated clauses coincidentally bridging within this substrate's very long single-line bullets (found live in sessions/2026-09-05.md and 2026-09-06.md while tuning). Verified against the full sessions/*.md corpus (152 files): file-level matches dropped 35 to 23; only one file dated after last_seen still matches (sessions/2026-09-07.md, a real but minor, already-unescalated suite-state.md drift — correctly left matching for human /triage judgement, not suppressed, since it is not a false positive). HONEST LIMIT, not solved and not claimed to be: unlike F1/F10, there is no textual marker that reliably separates a genuine incident from a session merely finding-and-fixing an ordinary stale CLAUDE.md/suite-state.md/brand- line in passing — both are written as the identical short \"X is stale\" phrase (09-04's own real incident text is exactly this shape). A regex over session-log text cannot recover the actual qualifying criterion (a specific structural property: hand-maintained prose inside a nominally `generated`-classified file, outside review_clock.py's per-content clock) that distinguishes them. This fix removes the specific, provable noise sources found by full-corpus audit; it does not achieve F1/F10-level precision, and claiming otherwise here would be dishonest. 9 unit tests (5 must-catch: both real incidents' verbatim text — one in each direction, proving the bug fix — plus 3 synthetic for CLAUDE.md/brand- shapes with no surviving verbatim text from that era; 4 must-not-catch: this session's own self-reference, the 2026-09-09 pending.md case, and 2 adversarial near-misses) extend `.github/scripts/test_failure_recurrence_signals.py` (now `test_f1()`/`test_f10()`/`test_f14()`). SELF-REFERENCE FOUND 2026-09-10, same day, immediately after landing (same shape F1 hit the day before): this fix's own session-log write-up (sessions/2026-09-10.md) names `CLAUDE.md`/`suite-state`/`brand-` explicitly while describing the fix, on lines close enough together to trip the new anchor logic. Added to confirmed_in rather than reopening the regex — same mechanism, same reasoning as F1's 2026-09-09 addition."
    signal: "(?:stale|out.?of.?date|no longer true|had drifted|still (?:said|claimed|listed))[^\\n]{0,35}(?:CLAUDE\\.md|suite-state|brand-)|(?:CLAUDE\\.md|suite-state|brand-)[^\\n]{0,35}(?:stale|out.?of.?date|no longer true|had drifted|still (?:said|claimed|listed))|substrate .{0,40}(said|claimed|pointed).{0,60}(deleted|retired|wrong)|last_updated.{0,40}(months|weeks) (old|stale)"
    confirmed_in: ["sessions/2026-08-15.md", "sessions/2026-08-18.md", "sessions/2026-08-20.md", "sessions/2026-08-30.md", "sessions/2026-09-04.md", "sessions/2026-09-10.md"]

  - id: F15
    title: The shared eq-cards checkout takes concurrent Edit/Write from multiple Claude Code sessions with no compound git op required to get hurt
    first_seen: 2026-08-16
    last_seen: 2026-08-25
    recurrences: 4
    rung: 4
    target_rung: 4
    guard: "hooks/pre_tool_use.py — eq_cards_bare_root_path() blocks Edit/Write/NotebookEdit/MultiEdit whenever the target file_path resolves inside the eq-cards BARE root (C:\\Projects\\eq-cards), excluding anything under its .claude/worktrees/* (EnterWorktree's own documented, exclusive worktree location — the sanctioned escape valve the block message points at). A plain string check, not repo_root_for()'s git-shellout: EnterWorktree's worktree location is a fixed, known convention, not something only git can resolve, so there is nothing here that needs a real subprocess call. Not sandbox-gated — same reasoning as F9/F13: every occurrence happened natively on the Beelink. EQ_CARDS env override matches the EQ_CONTEXT convention; EQ_CARDS_ROOT_OK=1 is the deliberate-root-task escape hatch. Built 2026-08-25 directly from Royce's instruction in-session ('make eq-cards sessions default to their own worktree') after this class had already independently cleared the ratchet's own recurrences>=2 threshold — not a unilateral rung-1-to-4 jump; see note. STATED SCOPE BOUNDARY: git verbs (a commit landing on whichever branch the shared root happens to be on, not F9(a)'s index-sweep mechanism) are a known, real, smaller risk deliberately left OPEN by this pass — closing it would need the same effective_cwd()-grade shell-command parsing F9 required, which is a materially larger lift than the dominant, best-evidenced damage vector (content landing in the wrong tree via Edit/Write, 3 of 4 recurrences) justified in one pass."
    detected_by: "human — Royce, reviewing this session's close-out card, which surfaced the pattern as a deferred item ('sessions keep working eq-cards from the bare root instead of a worktree... a habit/tooling question for Royce') after a live near-miss earlier the same session (a delete instruction that had gone stale between turns, aimed at a file that turned out to be the just-merged fix for the collision the root-switch had caused)"
    cost: "2026-08-16: a code fix committed via a bare root path landed on a different session's active feature branch (chore/close-sign-in-audit-followups), several commits deep, mixed with unrelated work — caught before push. 2026-08-17 (x2, same session): 8 files (5 edits + 3 new) written via absolute bare-root paths landed in the root instead of the session's own assigned worktree, invisible for ~15 tool calls because `flutter analyze` in the (genuinely unmodified) real worktree reported clean; recurred an hour later, same session, same mistake, this time landing on the shared main checkout directly. 2026-08-19: the shared root's branch changed underneath a session with zero action of its own — a different session's active feature work, carrying the first session's uncommitted edit along with the switch, then two more files changed on that branch while the first session was still investigating. 2026-08-25: the root switched branch TWICE mid-task under a third concurrent session (one of which had itself created the collision being fixed); an untracked file that a Royce instruction was about to target for deletion vanished from under the instruction between the observation and the act — the deletion, if it had glob-matched instead of re-checking state, would have destroyed a different, legitimate, just-merged migration file sharing the same numeric prefix. No data was actually lost in 3 of 4 recurrences (patch/restore recovery each time); 2026-08-25's vanished file is the sole exception — never committed on any branch, in no stash, unrecoverable, though of near-zero practical cost since a same-purpose migration was already live under a different number."
    note: "Distinct from F9 in mechanism, not just repo: F9's two damage paths both require a compound git operation (a bare commit sweeping the index, or rebase/merge/pull mutating HEAD/index/refs non-atomically) — closing them meant parsing arbitrary shell-command text for those specific verbs. F15's damage needs no git verb at all: a plain Edit/Write against a shared root that ANOTHER session has already switched to a different branch is sufficient on its own, since the file lands in whatever tree is currently checked out, no matter what tool wrote it. This is also why F15 could ship as a pure Edit/Write-tool-input check (file_path is already structured JSON) rather than needing F9's effective_cwd()-grade shell-command cd/-C parsing. Considered and deliberately deferred, not silently dropped: a matching git-verb guard for this checkout (mirroring F9(a)'s shape) would additionally close the branch-identity risk (a correctly pathspec-scoped commit can still land on the wrong branch, which F9(a)'s own fix does not address, since that's an index-sweep fix, not a branch-identity one) — left open in this pass because it requires materially more shell-command-parsing machinery than the Edit/Write check for a smaller share of the documented damage (1 of 4 recurrences was primarily a git-commit-on-wrong-branch shape; 3 of 4 were Edit/Write landing in the wrong tree). A SessionStart banner nudge (mirroring F1/F10's always-on print) was also considered and deferred: session_start.py currently reads zero stdin/JSON and has no mechanism to know the current session's own starting cwd at all — every existing check in that file operates on eq-context's own fixed substrate, never on the invoking session's location — so adding this would mean teaching that file an entirely new capability, for a benefit (saving one blocked-then-redirected round trip at session start) smaller than the PreToolUse block alone already delivers unprompted. Both deferrals are typed here so a future pass has the reasoning, not just the gap. Ratchet note: this failure class had recurred 4 times across 4 distinct dates in eq-context's OWN session logs (sessions/2026-08-16.md, 2026-08-17.md, 2026-08-19.md, 2026-08-25.md) before this entry existed — the exact 'recurred with no ledger entry at all' gap F9's own note already flagged as a known failure mode of this ledger, repeating here for a different repo. Opened directly at rung 4 in the same change that filed the entry, same as F14 — not the ladder's normal propose-then-Royce-approves path (guard-ratchet.yml proposes only once `recurrences >= 2` AND the entry already exists), because Royce's own in-session instruction is the decision the ratchet would otherwise be proposing, stated here rather than smuggled in as if rung-4-on-first-entry were the default."
    signal: "(bare root|shared root).{0,60}(eq-cards|switched branch|moved (branch|underneath))|(landed|committed).{0,40}(wrong|another session'?s).{0,20}branch.{0,40}eq-cards|eq-cards.{0,80}(concurrent session|worktree).{0,60}(collision|race|vanish(ed)?)|worktree.{0,40}eq-cards.{0,60}(default|EnterWorktree)"
    confirmed_in: ["sessions/2026-08-25.md"]

  - id: F16
    title: eq-context itself never got the isolate-first treatment its own concurrency gave every sibling repo
    first_seen: 2026-09-08
    last_seen: 2026-09-08
    recurrences: 1
    rung: 4
    target_rung: 4
    guard: "TWO-STAGE FIX, both stages built and shipped 2026-09-08 in one session. (1) hooks/session_start.py gained an ISOLATE line — prints unconditionally, every session, on any repo (same reasoning as its own NEEDS YOU/GOALS/RATCHET sections), pointing at EnterWorktree and scripts/safe_commit.py. (2) hooks/pre_tool_use.py gained eq_context_bare_root_path() + a main() block mirroring F15's eq-cards guard exactly (blocks Edit/Write/MultiEdit/NotebookEdit in the bare root, excluding .claude/worktrees/* and any sibling eq-context-wt-* — the latter needs no special case, since a sibling path is never a prefix of SHARED_EQ_CONTEXT). Shipped inactive FIRST (gated on EQ_CONTEXT_ROOT_GUARD=1, opt-in, deliberately inverted from EQ_CARDS_ROOT_OK's opt-out shape) because tools/commands/close.md's own Steps 2-4 still wrote the bare root directly at that point — activating immediately would have broken every session's close mid-flight, including ones already running when this shipped. Presented to Royce via AskUserQuestion with three options (leave proposed / restructure close.md then activate / activate as-is accepting the breakage); he chose the middle one. tools/commands/close.md (and its live source ~/.claude/commands/close.md) was then restructured with a new Step 0 — isolate into a worktree (EnterWorktree, or manual `git worktree add` for a session already isolated elsewhere) before Steps 1-4 touch any file, all four now writing `<WORKTREE>\\...` paths instead of the bare root — after which the EQ_CONTEXT_ROOT_GUARD gate was removed entirely and the block made unconditional, identical in shape to F15. 14 adversarial cases in hooks/adversarial_test.py (5 must-block shapes, 6 escape-valve/control cases, matching F15's exact test structure) — 161/161 full suite green, 0 regressions, after removing 3 gate-specific dormancy cases the unconditional version no longer needs. Live-verified against the real default path post-activation, not just the fixture. Separately, hooks/session_end.py (the Stop-hook bookend) gained a WORKTREES check — enumerates every registered eq-context worktree via `git worktree list --porcelain` and flags any carrying uncommitted or unpushed work at session end, the same shape as its existing DIRTY/UNPUSHED checks — plus a new Step 5.5 in close.md for tearing the Step-0 worktree down after a successful push — and session_end.py's own UNPUSHED message, which previously suggested a bare `push origin main` with no divergence check, now suggests fetch+rebase first. Also fixed: tools/commands/brief.md (both copies) gained a one-line pointer for eq-context-targeted sessions; system/worktree-registry.md gained an eq-context section documenting the sibling-folder <repo>-wt-<topic> convention (the dominant live pattern — 7 folders across eq-shell/eq-solves-intake, checked 2026-09-08 — over eq-field's documented-but-unused <repo>-<topic>-wt variant) plus the three orphans found live (below)."
    detected_by: "human instruction — Royce's task explicitly named F9 as the tracked-but-unguarded root cause and asked for the isolation enforcement itself, not a specific recurrence report; the session then independently verified live evidence before designing anything (per the task's own instruction to verify rather than trust the brief's cited numbers)"
    cost: "Verified live 2026-09-08, all independently, in one evening: the shared root ~100+ commits behind origin/main carrying a ~33-file staged/unstaged/untracked pile with no single owner (grew to 38 files during THIS session, confirming the pile is actively live, not a stale snapshot); three separate orphaned worktrees in three different ad-hoc naming conventions (nested .claude/worktrees/land-documents-feature-20260908, sibling eq-context-close-2026-09-08-wt, sibling eq-context-wt-session-close-20260908) — two of the three carrying real, never-pushed commits (a full eq-shell Documents-feature close write-up, PR #1801/#1811/#1813, staged in root and never pushed before the session that wrote it ended; a 2026-09-08 session-close commit); and close.md's own documented landing command is a bare `git commit` that F9(a)'s existing guard would refuse the moment a session actually ran it verbatim, forcing an improvised fix at the worst possible moment (the last step of a session, under context pressure) — plausibly why no two of the three orphaned worktrees share a naming convention: each session invented its own workaround on the spot rather than following a sanctioned default, because none existed. Independently corroborated same day, before this entry was written: two unrelated eq-shell sessions logged in `sessions/2026-09-08.md` each separately hit and reported the identical root-drift finding (101 commits behind, ~30 dirty files) at their own close, with no coordination between them — one explicitly recommending 'sessions should default to a worktree from the start on this repo,' the exact fix this entry ships. The second of those two sessions also hit a LIVE near-miss of F9/F12's own failure shape in this shared checkout that same evening: its target file (`eq/pending/eq-shell.md`) was modified out from under it between a read and a write by a third, concurrent session's own close landing in real time — caught only because it happened to re-read before writing, not by any guard."
    note: "Distinct from F9 in shape, not just severity: F9(a)/(b)'s two guards stop specific COMPOUND git operations (a bare commit sweeping the index, rebase/merge/pull racing across non-atomic steps) from CORRUPTING the shared checkout — both already rung 4, both proven. F16 is the failure F9's own note explicitly named as still open and deliberately NOT solved by those two guards: 'Considered and rejected: ... blocking ALL git writes to the shared checkout (would break routine, currently-reliable automation — nightly cron commits, single-file pending.md ticks, session close)'. That rejection was correct for F9(a)/(b)'s narrow git-verb shape, but left the broader question — should ordinary Edit/Write ever land directly in this bare root at all — entirely unaddressed, which is exactly the gap F15 (eq-cards) already closed for a sibling repo and this repo never got, until now. CLOSED IN FULL, same session: the scope boundary an earlier version of this note left open (close.md's Steps 2-4 still writing the bare root directly) is fixed — Step 0 now isolates into a worktree before any of Steps 1-4 touch a file, so the F16 block activating unconditionally has nothing left in the documented close protocol to conflict with. Practiced what it built: this entire fix was designed, coded, and tested from an isolated worktree (C:\\Projects\\eq-context-wt-f9-worktree-isolation, branch claude/f9-worktree-isolation) off a freshly fetched origin/main, not the shared root — which was itself ~111 commits behind at the session's start."
    signal: "(orphan(ed)?|abandoned|stranded) worktree.{0,60}(eq-context|naming convention|no single owner)|eq-context.{0,40}(root|checkout).{0,40}(100\\+|dozens? of|hours).{0,40}(behind|dirty|uncommitted|unowned)|close\\.md.{0,60}(bare .git commit|no.{0,10}pathspec|F9\\(a\\))"
    confirmed_in: ["sessions/2026-09-08.md"]

  - id: F17
    title: safe_commit.py could silently overwrite a concurrent session's already-pushed edit -- a mechanism F12's own guard can't see
    first_seen: 2026-09-09
    last_seen: 2026-09-09
    recurrences: 3
    rung: 4
    target_rung: 4
    guard: "BUILT 2026-09-09, same session as this entry, shipped via safe_commit.py itself (commit ac46bae8). New check_upstream_divergence() in scripts/safe_commit.py, called immediately after the script's existing `git fetch origin main` and before the scratch worktree is created. For each requested file it compares origin/main's current content (`git show origin/main:<path>`) against what the caller's own HEAD had for that path (`git show HEAD:<path>`) -- HEAD being the base the caller's worktree was branched or last synced from, the best available proxy for what they read before editing. If the two differ, origin/main has moved past the caller's own base; copying the caller's bytes into the freshly-branched scratch worktree would silently discard that change, so the script prints a unified diff of what changed upstream and aborts (exit 1) unless --force is passed, in which case it proceeds and still prints the diff for visibility rather than overwriting quietly. One deliberate carve-out to avoid false positives: if the caller's new bytes already equal origin/main's current content, it proceeds without complaint -- nothing would be lost regardless of how stale the caller's local HEAD is. Verified against four live scenarios in a throwaway origin+two-clone git setup (not just reasoned): (1) genuine divergence -- aborts, prints the correct diff, origin/main left untouched; (2) --force -- overrides, still shows the diff first, pushes; (3) caller's bytes coincidentally already match upstream despite a stale local HEAD -- proceeds via the script's pre-existing 'nothing to stage' path, no false alarm; (4) an ordinary in-sync commit of an existing file plus a brand-new file -- unaffected, matches pre-fix behavior exactly. No adversarial_test.py coverage: that harness is hooks/-scoped, and scripts/safe_commit.py has no pre-existing test file of its own (checked before building, not assumed). EXTENDED 2026-09-09 (commit 0c472ac1, same day as the recurrence below): new check_script_currency(), called before check_upstream_divergence, compares THIS SCRIPT's own on-disk bytes against origin/main's current copy of itself and refuses (same --force override) if they differ. Directly targets the mechanism the recurrence actually used -- a caller running an outdated copy of the script gets none of the protection check_upstream_divergence added, no matter how current origin/main is, because git fetch refreshes the origin/main *ref*, not the worktree's own checked-out files. Explicitly documented in its own docstring as a partial fix, not a full one: it can only warn a caller whose own copy is already new enough to contain this exact check; it cannot retroactively protect a call already running code that predates it. DURABLE FIX LANDED SAME DAY, on request (commits d83f007e, f346837): scripts/safe_commit.py restructured into a ~90-line bootstrap -- fetch origin/main, `git show` the current scripts/_safe_commit_impl.py, write it to a temp file, exec it with the caller's own argv, forward its exit code -- with all actual behavior, including both checks above, moved to scripts/_safe_commit_impl.py and re-fetched fresh on every single invocation. Closes the residual gap structurally rather than just detecting it: a worktree that syncs the bootstrap even once never has to sync again for its logic to stay current, because it no longer executes its own on-disk copy of the logic at all. See note for the one narrow case this still can't cover, and for a second, unrelated bug (a scratch-worktree leak) found and fixed while smoke-testing it."
    detected_by: "human -- Royce, confirmed live 2026-09-09 against eq/pending/eq-shell.md: a Sentry-sweep entry was fully clobbered by another session's push via this exact mechanism, then re-landed by hand (see commit 63fd8a1f, 'docs(eq-shell pending): re-land Sentry sweep entry, continue T/V investigation'). RECURRENCE also human-detected: Royce's own task instructions on 2026-09-09 named a second, later-vanished section in the same file and asked for the mechanism investigated directly, rather than reporting a bare 'it's missing again.'"
    cost: "One confirmed casualty: a Sentry-sweep entry in eq/pending/eq-shell.md, silently discarded when safe_commit.py copied a caller's stale on-disk bytes over a scratch worktree branched fresh off origin/main -- no error, no conflict marker, no diff shown, recovered only because a human noticed the entry was missing and re-added it by hand. safe_commit.py itself only shipped 2026-09-07 (commit cccdb241, as part of F9's fix) -- so the exposure window before this fix was two days, not weeks, but it is the SOLE sanctioned landing path for substrate changes per F16, meaning every safe_commit.py call in that window carried this exposure, not just the one confirmed hit. RECURRED SAME DAY, hours after the fix landed: between 16:02:42 and 16:03:55, three separate eq-context commits (9b04b132, b83f9524, 6947c4c6) each wholesale-replaced the immediately-prior one's brand-new section in eq/pending/eq-shell.md -- 76d7e4d8's 'provisioning-completeness audit script' section (3 open deferred items, from PR #1832) clobbered by 9b04b132's 'EQ-SHELL-23/Madagins' section, itself clobbered by b83f9524's 'labour-hire batch-intake' section 28 seconds later, itself clobbered by 6947c4c6's 'Sentry EQ-SHELL-24/25' section 45 seconds after that -- a cascading chain, not one isolated repeat. All three original sections (and their deferred items) are gone from the live file with no trace, recoverable only via `git show <sha> -- eq/pending/eq-shell.md`; not restored as part of this entry -- see note."
    note: "Same user-visible shape as F12 ('blind-overwrote a concurrent session's already-pushed shared-file edits') but a genuinely distinct mechanism, kept as its own id rather than folded into F12 as a recurrence -- same reasoning F9's own 2026-08-30 addition already used to keep an adjacent-looking incident separate from F12 ('to avoid double-counting one incident under two IDs'), applied here in the opposite direction (why NOT to merge). F12's 2026-08-18 guard watches shell command text -- cp/mv/xcopy/robocopy/Copy-Item/Move-Item -- for a destination resolving inside the shared checkout; it was never positioned to see this incident because safe_commit.py's copy step is an internal Python `write_bytes()` call into a scratch worktree under .claude/worktrees/, which is F12's own sanctioned escape-valve location, explicitly excluded from its destination check by design. Nothing in F12's guard was bypassed or failed here -- it simply doesn't instrument this code path, the same category of gap F16's note describes for F9(a)/(b) ('left the broader question ... entirely unaddressed'). Surfaced by Royce's own task instructions, which already named the mechanism and cited live confirmation before any investigation started; this session verified the docstring's pre-existing disclosure of the gap (scripts/safe_commit.py's 'This does NOT resolve content conflicts' paragraph, present since the 2026-09-07 add) rather than treating the report as new information, then built and live-tested the fix from an isolated worktree (C:\\Projects\\eq-context-wt-safe-commit-guard, branch claude/safe-commit-staleness-guard, off a freshly fetched origin/main) before landing it through safe_commit.py itself -- same practiced-what-it-built posture as F16's own close. ROOT CAUSE OF THE RECURRENCE (investigated 2026-09-09, same day): check_upstream_divergence() is correct code and, if it had executed, would have caught all three clobbers above -- for any of them, the calling worktree's own git HEAD content for eq/pending/eq-shell.md necessarily predates whatever the immediately-prior session had just pushed, which is exactly the divergence the function exists to detect. It did not execute with its protection intact because nothing verified the CALLING script's own on-disk copy was the fixed one -- git fetch (run just before the check) refreshes only the origin/main *ref*, never the worktree's own checked-out files, so a caller running from a worktree/checkout whose local files were last synced before ac46bae8 (06:46:09) silently ran the pre-fix, no-guard version, regardless of how current origin/main was. Live-confirmed, not just inferred: the shared C:\\Projects\\eq-context root checkout's own local HEAD was found sitting at bd9fa5412c (04:42:05) -- two hours before ac46bae8 -- still unmoved 11+ hours later while this was being investigated. F16's own cost narrative (2026-09-08, one day earlier) independently corroborates this exact class of drift as a recurring pattern in this repo specifically, not a one-off: a 100+-commit-stale root, orphaned worktrees on inconsistent naming/freshness, and a same-day near-miss where a session's own target file was 'modified out from under it between a read and a write by a third, concurrent session's own close landing in real time -- caught only because it happened to re-read before writing, not by any guard.' Cannot forensically prove after the fact which exact worktree/checkout each of the three culprit commits ran from -- they were torn down as part of each session's own close, and nothing logs which script version executed -- but a raw-git bypass or three independent, uncommented --force overrides are far less parsimonious than three long-running concurrent sessions each carrying a stale pre-06:46:09 copy of the script from whenever their own worktree was created. SEPARATE, ALSO NEW FINDING: guard-ratchet.yml's own promotion rule ('recurrences >= 2 AND rung < 4' opens an issue) structurally cannot fire for this recurrence, because F17 was already sitting at rung 4 before it recurred -- the ladder has no rung above 4 to promote to, so a rung-4 guard that fails again generates no automatic signal at all, only whatever a human or session happens to notice. Worth naming plainly: rung 4 ('hook -- enforced at the point of action... before the damage. Prevention.') implies enforcement independent of the caller's own state, which an in-script check that must itself be freshly synced to apply is not -- closer in practice to rung 2/3 wearing rung-4 packaging. Left rung/target_rung at 4 rather than silently reclassifying a tracked metric -- flagging the definitional gap here for Royce's call, not deciding it unilaterally. check_script_currency (commit 0c472ac1, this entry) narrowed but did not close this: by the same logic, it only protected a caller whose copy was already new enough to contain IT. DURABLE FIX LANDED SAME DAY, on request (commits d83f007e, f346837): scripts/safe_commit.py split into a stable bootstrap plus scripts/_safe_commit_impl.py holding all actual behavior, re-fetched fresh from origin/main on every call -- full mechanism in guard, above. Turned out not to need the file/convention sprawl this note originally expected: every existing reference to 'scripts/safe_commit.py' in this repo (hooks/pre_tool_use.py, hooks/session_end.py, close.md, worktree-registry.md, machinery.md -- checked exhaustively; no CI usage, no module imports anywhere) already just says 'run scripts/safe_commit.py', which stays true and unchanged under the split -- so tools/commands/close.md, the SessionStart ISOLATE message, and EnterWorktree needed zero edits. One narrow gap this still can't close, inherent to any fix living in a file that must itself be synced at least once: a worktree that forks and never once syncs past commit d83f007e never receives the bootstrap at all, and would still silently run whatever ancient single-file logic was already on its disk -- but that is now a one-time, ever-shrinking bootstrapping window (every worktree created from now on gets the bootstrap immediately), not the ongoing, indefinitely-recurring exposure the single-file design had, where every future fix to it carried the identical risk this entry describes. Also found and fixed in passing while smoke-testing the split, unrelated to the recurrence itself: scripts/_safe_commit_impl.py's 'nothing to commit -- every requested file already matches origin/main' fast path skipped the trailing cleanup() call, leaking the scratch worktree and its throwaway branch on every already-current-file invocation -- live-reproduced (orphan appeared), fixed, re-verified (orphan gone) before landing. The other two early-return paths in the same function are unaffected by design: both explicitly say in their own printed message that the worktree is preserved on purpose (for inspection, or for a manual push after exhausting retries). Also self-caught during THIS fix, not left in: a bare `git checkout -- scripts/safe_commit.py`, intended to discard a now-redundant working-tree diff, silently restored the file from this worktree's own stale local HEAD (pre-dating every commit in this entry) rather than from origin/main -- reverting the bootstrap back to the old single-file content, entirely locally, never pushed. Caught by re-diffing against origin/main before the next step rather than trusting the checkout had done what was intended; fixed with an explicit `git checkout origin/main -- <path>` instead. Zero live impact -- flagged here only because it is a first-hand, same-session illustration of exactly how easy this entry's whole failure class is to trigger by accident, even while actively fixing it. Lost content NOT restored: the three clobbered sections (provisioning-completeness audit findings, EQ-SHELL-23/Madagins cleanup, labour-hire batch-intake audit) are recoverable verbatim via git show but weren't re-added here -- 0e16b1f4 already re-derived 2 of the original 3 deferred items independently (from PR #1832 directly, not from the lost section), so a blind verbatim restore would duplicate that; reconciling what's still current against what's already been re-derived or superseded is a separate, judgment-call task, spawned as its own follow-up (task_b54b5e3a). THIRD RECURRENCE, same day, later that evening: commit `d8c83d3e` (20:36:29, 'retire not click-tested live as a waiting-on-you class') wholesale-replaced `eq/pending-archive.md` again from a base that predated `24b51c2a` (20:34:43, landed ~106s earlier), silently deleting both the freshly-landed EQ-SHELL-23 write-up and the already-twice-recovered labour-hire/madagins section in the same diff. Mechanism not confirmed as safe_commit.py's copy step specifically -- unlike the earlier recurrence, no session log or commit trail available to this session establishes whether this went through safe_commit.py (with or without --force) or bypassed it via direct git; d8c83d3e's own diff spans 6 files as one 'blanket policy application' (73 items mass-dismissed across 5 pending/*.md files plus this one), a shape that doesn't obviously fit safe_commit.py's single-purpose interface, but that's an inference, not a confirmed trace. Both clobbered sections recovered by hand this time, each claim independently re-verified live rather than trusted from the old text (commits 5ce69cdac3e41a5df25345330d64239cb9c274af and the labour-hire/madagins restoration alongside this entry) -- see eq/pending-archive.md itself for both."
    signal: "safe_commit\\.py.{0,80}(silently )?(overwr(o|i)te|clobber(ed)?|discard(ed)?).{0,60}(concurrent|upstream|already.?pushed|origin/main)|check_upstream_divergence.{0,60}(bypass|fail|miss|didn't (catch|fire)|false negative)|scratch worktree.{0,60}(overwr(o|i)te|clobber).{0,60}(origin/main|concurrent|upstream)|check_script_currency.{0,60}(bypass|fail|miss|stale)|stale (copy|checkout|worktree).{0,60}safe_commit"
```

---

## How to add a failure

When something escapes the safeguards — **not** when it is merely annoying, but when a guard that should have caught it did not:

1. Append an entry. `recurrences: 1`, `rung:` = whatever the guard sits at **today** (be honest; prose is rung 1). `last_seen:` = the date of this occurrence (same as `first_seen` on a new entry).
2. If an `id` already exists for this failure class, **increment `recurrences`** and bump `last_seen` to today — do not add a new entry. Recurrence is the whole signal.
3. Add a `signal:` regex — a short, specific phrase pattern that would appear in a session log describing this exact failure recurring (not the general topic area; specific enough that it wouldn't match an unrelated mention). This is what `failure_recurrence_signals()` in `refresh_digest.py` scans `sessions/*.md` for.
4. Add the session file you just wrote this entry into (or bumped it from) to `confirmed_in:` (a list). The date filter alone (`file_date > last_seen`) cannot tell "a session narrating an already-confirmed recurrence" apart from "the failure happening again" — without this, the very session log that reports and closes a recurrence permanently re-triggers itself on every future digest run, since its own file date always stays after `last_seen`. Found live 2026-07-26: F1's confirmation write-up in `sessions/2026-07-21.md` was re-flagging as a "possible guard bypass" in every digest since, purely because it quotes its own signal phrase while being dated after the `last_seen` it set.
5. `guard-ratchet.yml` does the rest. It proposes; Royce disposes.

**Do not close a failure by writing a lesson.** A lesson is rung 1. If it already had a lesson and recurred, the lesson is the thing that failed.

**The recurrence-detection loop (2026-07-21).** `guard-ratchet.yml` has always proposed a rung promotion once `recurrences >= 2` — but nothing ever noticed *when* to bump that counter; a human had to happen to recognise their own past failure in a new session. `failure_recurrence_signals()` closes that gap: it scans every session log dated after a failure's `last_seen` for its `signal` regex and surfaces a candidate in digest.md — a rung-4 hit lands in **Needs you** (the guard was supposed to make this impossible; a hit anyway is a bypass), anything below rung 4 lands in the quieter **Possible recurring failures** section. It never writes to this file — confirming a real recurrence and bumping `recurrences`/`last_seen` stays a human call, same posture as the ratchet itself.
