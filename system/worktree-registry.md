---
title: Worktree Registry
owner: Royce Milmlow
last_updated: 2026-07-20
scope: Active and stale git worktrees — check before creating a new one
read_priority: critical
status: live
---

# Worktree Registry

Agents MUST add a row to Active before creating a worktree.
Check this file first — if your target repo/area is already claimed, coordinate before starting.

**Protocol:**
1. Read this file before any `git worktree add`
2. Add your row to Active (folder, branch, session ID or agent name, date)
3. When done: move row to Stale or delete it
4. Never edit a worktree that isn't yours (see Rule in `C:\Projects\CLAUDE.md`)

**Root cause of the eq-solves-service shared-checkout collisions (found + fixed 2026-07-23):**
A real linked git worktree has its own `.git` FILE at its root (a ~70-byte gitdir
pointer — e.g. `.claude/worktrees/eqsvc-loadtime-ux/.git`). Two paths under
eq-solves-service's `.claude/worktrees/` — `asset-import-export-1fe110` and
`unruffled-noyce-657c65` — turned out to have **no `.git` of their own at all**.
They were never created via `git worktree add`; they're plain nested folders.
Any git command run from inside one silently walks up to the **parent repo's**
`.git` and mutates its shared branch/working tree — which is exactly how two
concurrent sessions each believing they're isolated collided (one session's
uncommitted edit appearing in another's `git status`, the checked-out branch
changing mid-task). `unruffled-noyce-657c65` holds nothing (empty `.claude`
dir only) and is safe to delete whenever someone has `rm -rf` permission —
this session's classifier blocked the delete. `asset-import-export-1fe110`
similarly holds no files of its own (just `.claude`/`.next`) — the actual
uncommitted work sessions have found "in" it lives in the **root checkout**
(`C:\Projects\eq-solves-service`), not the subfolder. **Fix shipped:** `~/.claude/hooks/guard.js`
now has a `detect-fake-worktree` rule (rule 1b) that force-blocks any Edit/Write
whose path matches `/worktrees/<name>/` or `<name>-wt/` but whose root has no
`.git` — tested against both known-fake dirs (blocks) and two real worktrees,
one of each path shape (passes clean). Does not block `Bash`/`git worktree add`,
so fixing a path is never gated. **This does not retroactively fix the two
existing fake folders** — it only stops future silent edits into them (or any
new ones like them) from mutating the wrong checkout. If a session gets
assigned one of these two paths, treat it as: work is actually happening in
the root `eq-solves-service` checkout, verify `git status`/`git branch` there
before editing, or spin up a real worktree elsewhere instead.

---

## Recently pruned (2026-07-27 -- 39 folders, registry found unreliable in both directions)

Royce asked `are all these folders required?` -- a `C:\Projects` audit against this registry's own struck-through rows found the file **out of sync with live git state in both directions**: some rows already marked `~~REMOVED~~` (e.g. `eq-field-approvals-wt`, `eq-intake-dupes-merge-wt`, `eq-shell-phone-otp-selfheal-wt`) were still live, git-tracked worktrees on unmerged-looking branches when checked -- and `eq-field-guard` had **no row at all**, ever, despite being a real worktree. Ground truth used throughout was `git -C <parent-repo> worktree list` (not this file), plus a per-folder clean-working-tree check, plus GitHub PR state via `gh pr list --head <branch>` for the 5 folders git still had registered.

**Batch 1 -- 34 folders, already unregistered by git (`git worktree list` showed no entry for any of them), holding nothing but leftover build cruft (`node_modules`, `.next`, or empty) and no `.git` of their own.** Deleted with a plain recursive filesystem delete, not a git worktree operation (git had already forgotten them -- likely from an earlier worktree teardown that didn't clear its build-tool output directories): `eq-shell-accessmodel-c1-wt`, `eq-shell-site-merge-wt`, `eq-shell-cal-phase3-wt`, `eq-shell-canonical-phone-wt`, `eq-shell-staffptr-wt`, `eq-shell-spin-keyframe-wt`, `eq-shell-preset-saveall-wt`, `eq-shell-pricing-guard-wt`, `eq-shell-cert-migrate-wt`, `eq-shell-collapse-matrix-wt`, `eq-shell-cards-perm-cleanup-wt`, `eq-solves-service-roles-bump-wt`, `eq-shell-bump-ui-1120-wt`, `eqsvc-bump-ui-1120-wt`, `eq-shell-roles-bump-wt`, `eq-shell-market-vertical-backfill-wt`, `eq-shell-invite-role-fix-wt`, `eq-shell-onelogin-wt`, `eq-shell-quote-draft-wt`, `eq-shell-tenant-cache-wt`, `eq-shell-list-members-staff-fallback-wt`, `eqsvc-bulk-delete-chunk-wt`, `eqsvc-check-site-name-edit-wt`, `eqsvc-kanban-title-wt`, `eqsvc-site-assignee-fix-wt`, `eqsvc-stale-action-wt`, `eq-field-cluster3-wt`, `eq-shell-dupes-merge-wt`, `eq-shell-iframe-loading-bg-wt`, `eq-shell-iframe-loading-spinner-wt`, `eq-shell-comms-polish-wt`, `eq-shell-perf-tier1-wt`, `eq-intake-mergepanel-wt`, `767-wire-check-css`.

**Batch 2 -- 5 folders, still genuinely git-tracked worktrees** (this is the set the earlier struck-through rows got wrong -- they were NOT actually removed). Verified clean (no uncommitted changes) and confirmed merged via GitHub before touching: `eq-field-approvals-wt` (eq-field, `claude/timesheet-leave-approval-lifecycle`, [PR #520](https://github.com/eq-solutions/eq-field/pull/520) MERGED), `eq-field-guard` (eq-field, `claude/dependabot-config`, [PR #466](https://github.com/eq-solutions/eq-field/pull/466) MERGED, **never had a registry row before this entry**), `eqsvc-access-cluster3-wt` (eq-solves-service, `claude/access-cluster3-service-gate`, [PR #551](https://github.com/eq-solutions/eq-service/pull/551) MERGED), `eq-intake-dupes-merge-wt` (eq-solves-intake, `claude/dupes-usage-check-client`, [PR #73](https://github.com/eq-solutions/eq-solves-intake/pull/73) MERGED), `eq-shell-phone-otp-selfheal-wt` (eq-shell, `claude/phone-otp-approval-selfheal`, [PR #992](https://github.com/eq-solutions/eq-shell/pull/992) MERGED). Torn down via git's proper worktree teardown path, confirmed unregistered from each parent's `git worktree list` afterward, then the same build-cruft leftovers as Batch 1 cleared with a plain delete. Royce's explicit go for both batches.

**Note on this file's reliability:** `merge-base --is-ancestor` against local `origin/main` returned false for all 5 Batch 2 branches even though every one was GitHub-confirmed merged -- these were squash merges, which never leave the source branch as an ancestor of main. Don't use `is-ancestor` alone to judge merge status on this repo set; check `gh pr list --head <branch> --state all` instead.

Any row below this point naming one of the Batch 1/2 folders is superseded by this entry regardless of what it says (struck-through or not) -- the folder is gone as of 2026-07-27.

---

## Recently pruned (2026-07-27, round 2 -- 8 more worktrees + 1 empty husk)

Royce asked to check that OTHER repos' worktrees were clean too, as a follow-on to the same-day 39-folder cleanup above. Found 3 dirty checkouts -- all belong to other live sessions, none touched: `eq-shell/.claude/worktrees/wonderful-brahmagupta-fa0d32` (1 uncommitted file, `claude/staff-supervisor-roster-visibility-81bb93`), the `eq-solves-service` root checkout (14 modified + untracked incl. a stray `nul` file, on `claude/migration-ledger-checksum-verify`), and the `eq-intake` root checkout (detached HEAD, 6 modified + 3 untracked).

Separately found 8 clean worktrees whose PRs were already merged (confirmed via `gh pr list --head <branch> --state all`, not assumed): `eq-shell-dup-accounts-wt` ([PR #967](https://github.com/eq-solutions/eq-shell/pull/967)), `eq-shell-invites-wt` ([#935](https://github.com/eq-solutions/eq-shell/pull/935)), `eq-shell-zaap-staff-wt` ([#936](https://github.com/eq-solutions/eq-shell/pull/936)), `eq-field-userid-guard-wt` ([#518](https://github.com/eq-solutions/eq-field/pull/518)), `eqsvc-asset-filter-wt` ([#602](https://github.com/eq-solutions/eq-service/pull/602)), `eqsvc-auditlog-wt` ([#577](https://github.com/eq-solutions/eq-service/pull/577)), `eqsvc-tm-lockdown-wt` ([#576](https://github.com/eq-solutions/eq-service/pull/576)), and `eq-solves-service/.claude/worktrees/user-source-analysis-be4daa` ([#613](https://github.com/eq-solutions/eq-service/pull/613), `claude/dev-csp-localhost-framing`).

7 of 8 removed via `git worktree remove` (2 left build-cruft husks afterward -- `node_modules`/`.next` -- cleared with a plain delete, same pattern as the earlier 39). Their 7 now-dangling local branches deleted too (4 needed `-D` for the same squash-merge false-negative documented above -- every one independently confirmed MERGED via `gh` first, not just force-deleted on assumption).

The 8th, `user-source-analysis-be4daa`, could **not** be removed -- a locked file inside its `node_modules` (`import-in-the-middle`, a Sentry auto-instrumentation temp file) blocked both `git worktree remove` and a direct long-path-safe .NET delete. 16 `node.exe` processes were running on this shared machine at the time and none could be confidently attributed, so none were killed to force the deletion -- left the worktree and its branch (`claude/dev-csp-localhost-framing`) in place rather than risk another session's live work. Revisit once the lock clears.

Also pruned: `eq-context`'s own `sks-adam-meeting-prep-4ba3e8` worktree, an empty husk git had already self-flagged as prunable (`git worktree prune -v`).

---

## Recently pruned (2026-07-27, round 3 -- 60 orphaned .claude/worktrees folders suite-wide)

Royce: 'keep auditing and deep diving.' Widened the check beyond `git worktree list` (which by definition can't show a worktree git has already forgotten) to a direct diff: every folder physically present under each repo's `.claude/worktrees/` versus what `git worktree list --porcelain` actually tracks. Turned up **~65 orphaned folders across every repo in the suite** — some dated back to mid-June, all the same root cause as the earlier 39-folder / 8-folder rounds (a worktree teardown that unregisters from git but doesn't always clear the physical folder).

Checked every one's contents before deleting anything (none had their own `.git`, confirming git had already forgotten all of them):

- **Empty or pure build-cruft (node_modules/.next/eq-platform skeletons, no real source)** — deleted via plain recursive delete: `eq-roles` (5), `eq-design-tokens` (5), `eq-field` (5), `eq-context` (2, incl. `developer-resource-opportunity-cc9c94` which held only a stray `.claude/settings.local.json`), `sks-nsw-labour` (26, every single one confirmed empty), `eq-shell` (9 of 10 — `eq-ops-mobile-view-af44a3`, `sks-comms-resource-mgmt-7563e2`, `accept-invite-phonestub`, `bold-brattain-688290`, `compassionate-goldberg-734468`, `labour-hire-agency-grouped`, `labour-hire-modal-focus`, `staff-active-writeguard`, `brand-handoff`), `eq-solves-service` (2 of 3 — `asset-import-export-1fe110` + `unruffled-noyce-657c65`, the two folders this file already documented as 'never real worktrees' from the 2026-07-23 root-cause investigation above, confirmed safe and finally cleared), `eq-intake` (6 — verified their `eq-platform/packages/*` contents were 0-file skeletons by diffing against the live repo, not just assumed).

- **Flagged, then resolved same session** — `eq-shell/.claude/worktrees/app-naming-wt` had real content (133 source files, docs, vite config, no `.git`), initially left for Royce. Turned out to have a sibling: standalone folder `C:\Projects\766-wire-check-perms` (the one fully-undocumented mystery folder from the original 'are all these folders required?' audit), near-byte-identical content. Diffed `src/lib/appLabels.ts` against the live `eq-shell` repo — exact match — then `git log` on that file traced it to [PR #779](https://github.com/eq-solutions/eq-shell/pull/779) (MERGED 2026-07-12, confirmed via `gh pr view`). Both were stale pre-merge snapshots of already-shipped work, not unique unmerged code. Both deleted.

- **Still locked, left in place** — 4 folders returned 'device or resource busy' on every retry across several minutes (not a one-off): `eq-cards/will-brown-cards-sks-issue-4ec9c4`, `eq-ui/elastic-blackwell-1a85b8`, `eq-shell/frozen-window-issue-58b6b2`, `eq-solves-service/user-source-analysis-be4daa` (the same one blocked in round 2 — still stuck). All confirmed empty of real content. The consistent, repeated lock (rather than a one-time collision) points more at a system-level process (antivirus/indexer) than another live session. Harmless to leave; retry later or after a reboot.

---

## Recently pruned (2026-07-27, round 4 — 22 more top-level orphans, root cause found + fixed)

Royce shared a screenshot of `C:\Projects` in Explorer and asked what the extra folders were — a check against every repo's live `git worktree list` found 22 MORE top-level sibling `-wt` folders (not the `.claude/worktrees/` ones round 3 already swept), all already git-untracked, all build-cruft-only (`node_modules`/`.next`/`eq-platform`, no `.git`, no real source). Unlike rounds 1-3, timestamps on these went back **15 days** (2026-07-12 to 2026-07-27), concentrated entirely in `eq-shell` (13), `eq-solves-service` (8), and `eq-intake` (1) — never `eq-field` (vanilla JS, no `node_modules`).

**Root cause found, not just cleaned around:** this session had already directly witnessed `git worktree remove` fail with `Invalid argument` on a long `node_modules` path (round 2/3, `user-source-analysis-be4daa`). The steady ~15-day accumulation, concentrated exactly in the repos with deep dependency trees and absent from the one repo without them, confirmed it: Windows' 260-character path limit was breaking the deletion step of every worktree teardown on a Node-based repo. Checked the actual config: `git config core.longpaths` was already `true` (someone had tried to fix this before) but the OS-level `HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled` was still `0` — the fix had been half-applied and never finished.

**Fixed at the source, not worked around again:** Royce set `LongPathsEnabled` to `1` himself (system-settings change, out of scope for Claude Code to make even with permission) via an elevated PowerShell session, confirmed live via registry read. **Takes effect after a reboot, not yet rebooted as of this entry.** Once active, `git worktree remove` should stop failing on long paths for every future teardown, on any repo, from any session — no more recurring manual sweeps needed for this specific cause. The 22 folders found this round were the existing backlog under the OLD (broken) behavior; deleted via plain recursive delete same as every other round, confirmed clean first.

If orphaned top-level `-wt` folders start reappearing in `eq-shell`/`eq-solves-service`/`eq-intake` again AFTER a reboot, this diagnosis was wrong (or incomplete) and needs revisiting — don't assume the fix worked, verify live.

---

## Active (do not touch)

| C:\Projects\eq-shell-suppliers-show-all-wt (eq-shell) | claude/suppliers-show-all-columns | Claude (9th Suppliers-saga fix — Royce, after #1099's fullWidth fix: "you seem determined not to allow them in this table view", re: #1098's defaultHiddenColumns hiding Notes/Login/Password. Fair — that was compensating for the 860px cap #1099 removed. All 8 columns sum to ~1350px, fits fine in fullWidth. Removed defaultHiddenColumns, kept persistKey so a user can still opt to hide a column for their own screen) | 2026-07-29 | **PR [#1100](https://github.com/eq-solutions/eq-shell/pull/1100) OPEN**, not yet merged. Build clean (tsc -b + vite build). Worktree left in place until merge. |

| ~~C:\Projects\eq-shell-suppliers-fullwidth-wt (eq-shell)~~ REMOVED | claude/suppliers-fullwidth (merged, deleted) | Claude (8th and actual-final Suppliers-saga fix — Royce asked "why arent you filling the page width?" after #1098. Traced to the real root cause of the whole 6-PR saga: `Suppliers.tsx` never passed `fullWidth` to `<HubLayout>`, so it was stuck inside AppShell's default `.eq-hub-content{max-width:860px}` reading-width cap — the same cap that made 8 raw columns need scrolling in the first place, and that left dead space once fewer columns showed. Every other wide-table page (StaffPage, equipment/index.tsx, FieldRosterPage, CustomersPage) already passes `fullWidth`. Added it + the matching `16px 24px 0`/`0 24px 24px` padding zones fullWidth pages supply themselves (fullWidth also strips the package's own page padding)) | 2026-07-29 | DONE — **PR [#1099](https://github.com/eq-solutions/eq-shell/pull/1099) MERGED** (squash `8a700539`, Royce's "merge #1099" go). All required checks + deploy preview green pre-merge. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-suppliers-column-picker-wt (eq-shell)~~ REMOVED | claude/suppliers-column-picker (merged, deleted) | Claude (7th and final Suppliers-saga fix — Royce compared Suppliers to the Staff page ("Staff records takes up the whole page? I dont get what the limitation is") and asked why. Root answer: Staff isn't fitting because it has less to show, it's hiding 5 of its 11 columns by default via `defaultHiddenColumns`+`persistKey="staff-columns"` (Royce had toggled Company back on himself, which is why it showed). Suppliers had been built to show all 8 columns always, which is the actual reason every prior fix had to fight for horizontal space. Applied the identical, already-proven pattern: `defaultHiddenColumns={['notes','login','password']}` + `persistKey="suppliers-columns"` — default view now needs no scroll at all) | 2026-07-29 | DONE — **PR [#1098](https://github.com/eq-solutions/eq-shell/pull/1098) MERGED** (squash `6a5ef2fc`, Royce's "merge #1098" go). All 4 required checks green pre-merge. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-suppliers-pagination-wt (eq-shell)~~ REMOVED | claude/suppliers-pagination (merged, deleted) | Claude (6th Suppliers-saga fix — after #1096 fixed the actual clip, Royce reported "gotta scroll all the way down to see the scroll": with dozens of unpaginated suppliers, the horizontal scrollbar sits below every row. Asked Royce to pick between pagination / bounded-height scroll box / hide-columns-by-default via AskUserQuestion — chose pagination. `pagination={{pageSize:20}}` on the shared Table component, already-built feature, one line) | 2026-07-29 | DONE — **PR [#1097](https://github.com/eq-solutions/eq-shell/pull/1097) MERGED** (squash `acc34563`, Royce's "merge #1097" go). All 4 required checks green pre-merge. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-suppliers-width-budget-wt (eq-shell)~~ REMOVED | claude/suppliers-column-width-budget (merged, deleted) | Claude (4th attempt at the Suppliers Login/Password visibility saga. #1092 capped Notes — the only unbounded column — but never checked whether the OTHER six columns' declared widths already exceeded the available ~950-1000px, which they did (checkbox+chevron+category+name+contact+phone+email alone ≈ 1080px). Retuning every column's width against one coherent budget, adding ellipsis truncation broadly — same "keep it simple" category of fix, just computed properly this time) | 2026-07-29 | DONE — **PR [#1095](https://github.com/eq-solutions/eq-shell/pull/1095) MERGED** (squash `ab3d84b6`, Royce's "merge #1095" go). Build clean, 270/270 tests pass. Superseded same-day by the row below once the real root cause was found — worktree husk (node_modules/eq-intake leftovers only, no `.git`, git already untracked it) cleared. |
| ~~C:\Projects\eq-shell-suppliers-scroll-real-wt (eq-shell)~~ REMOVED | claude/suppliers-real-horizontal-scroll (merged, deleted) | Claude (5th and, this time, actually-root-cause attempt at the Suppliers saga — Royce asked "Cant we extend the table to the width and then make it scroll?" after a fresh screenshot showed columns still cut off post-#1095. Traced it to `.eq-table-card` — the `@eq-solutions/ui` package's own wrapper directly around `<table>` — having `overflow:hidden`, one DOM level inside `.eq-rc-tablescroll`, clipping the table before the outer wrapper's `overflow-x:auto` ever ran. #1092/#1095 were treating the symptom (cramming+truncating columns) not the cause. Fix: override `overflow-x:auto` on `.eq-table-card` itself scoped under `.eq-rc-tablescroll`, drop the truncation back to plain comfortable widths) | 2026-07-29 | DONE — **PR [#1096](https://github.com/eq-solutions/eq-shell/pull/1096) MERGED** (squash `cd7ce734`, Royce's "merge #1096" go). All 4 required checks green pre-merge. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-intake-tabs-vendor-wt (eq-shell)~~ REMOVED | claude/intake-tabs-vendor-bump (merged, deleted) | Claude (re-vendor @eq/intake-demo to eq-solves-intake@b23fc99 — #81 + #83 (tab redesign) + #84 (unused-import hotfix, found + fixed mid-vendor)) | 2026-07-29 | DONE — **[eq-shell #1094](https://github.com/eq-solutions/eq-shell/pull/1094) MERGED** (squash `c94bd734`, Royce's "merge #1094" go). All CI green pre-merge (typecheck·test·lint, migration-ledger-hygiene, gitleaks, schema-drift/anon-grant/policy-lint, Netlify deploy preview). `gh pr merge --delete-branch` failed the local branch-delete step only (another worktree — `auto-import-toggles-956005` — already had `main` checked out); merge itself succeeded, remote branch deleted manually, worktree + local branch removed after. **Live on core.eq.solutions via Netlify auto-deploy on merge.** Full Intake tab redesign (5 tabs → Overview/To Do/Bring Data In/Ask) now shipped end-to-end. |
| ~~C:\Projects\eq-intake-vendor-src-wt (eq-intake)~~ REMOVED | (detached, throwaway) | Claude (read-only copy source for the eq-shell vendor bump above — recreated once at the pre-hotfix main, again at the post-hotfix main) | 2026-07-29 | Done — removed after the final clean copy was taken. |
| ~~C:\Projects\eq-intake-hotfix-unused-import-wt (eq-intake)~~ REMOVED | claude/fix-unused-usecallback-import (merged, deleted) | Claude (eq-shell's `tsc -b` — stricter than eq-intake-demo's own local `tsc --noEmit` — caught a real unused `useCallback` import in `IntakeHealthHome.tsx` left over from PR1's handler extraction) | 2026-07-29 | DONE — **[eq-solves-intake #84](https://github.com/eq-solutions/eq-solves-intake/pull/84) MERGED** (squash `b23fc99`, Royce's "merge #84" go). Worktree + branch removed. |
| ~~C:\Projects\eq-intake-tabs-wt (eq-intake)~~ REMOVED | claude/intake-tabs-overview-todo (merged, deleted) | Claude (Intake IA redesign PR1 — collapse Health/Queue down to Overview/To Do, extract DuplicateMergePanel out of IntakeHealthHome into RemediationQueue's tab, unify the To Do badge, collapse the 6-metric breakdown, relabel the tab bar) | 2026-07-29 | DONE — **[eq-solves-intake #81](https://github.com/eq-solutions/eq-solves-intake/pull/81) MERGED** (squash `5471c18`, Royce's "merge #81" go). CI green pre-merge. Worktree + branch removed. |
| ~~C:\Projects\eq-intake-tabs2-wt (eq-intake)~~ REMOVED | claude/intake-bring-data-in (merged, deleted) | Claude (Intake IA redesign PR2 — fold Reconcile into Bring Data In as a per-slot "Check for conflicts" step, drop the standalone Reconcile tab, fix the SimPRO-specific copy) | 2026-07-29 | DONE — **[eq-solves-intake #83](https://github.com/eq-solutions/eq-solves-intake/pull/83) MERGED** (squash `219454e`, Royce's "merge #83" go). CI green pre-merge, no conflict against #81's earlier merge. Worktree + branch removed. Intake tab redesign (5 tabs → Overview/To Do/Bring Data In/Ask) now complete on eq-intake main — eq-shell re-vendor bump still pending, not started. |

| ~~C:\Projects\eq-shell-suppliers-notes-cap-wt (eq-shell)~~ REMOVED | claude/suppliers-notes-width-cap (merged, deleted) | Claude (3rd attempt at the Suppliers Login/Password visibility saga — #1079 didn't work, #1082 fixed scrolling but Royce wants them visible without scrolling. Root cause: Notes is the ONLY column with no width cap, so real note text balloons the table past the loading-skeleton's narrow width, which is why columns "show then disappear" between loading and loaded states. Royce explicitly asked for the simplest fix, not a reorder/rebuild — capping Notes' width with ellipsis truncation, matching the existing pattern in AdminAuditPage.tsx) | 2026-07-29 | DONE — **PR [#1092](https://github.com/eq-solutions/eq-shell/pull/1092) MERGED** (squash `625829b8`, Royce's "merge #1092" go). All checks green. Confirmed live via Netlify (production deploy verified serving this exact commit). Worktree + branch removed. |

| ~~C:\Projects\eq-shell-licence-resync-guard-wt (eq-shell)~~ REMOVED | claude/licence-resync-noop-guard (merged, deleted) | Claude (root-caused the Staff page's false "Re-review" badge to staff-resync-licences.ts's unguarded ON CONFLICT DO UPDATE bumping updated_at on no-op runs; fetches existing rows first and only writes when a field actually differs, extracted `licenceRowChanged()` + 5 new unit tests) | 2026-07-29 | DONE — **[eq-shell #1091](https://github.com/eq-solutions/eq-shell/pull/1091) MERGED** (squash `4b3510ba`, Royce's "merge #1091"). First CI run failed the function-filename guard (test file placed at netlify/functions/ root instead of _shared/ — fixed, force-pushed, green on retry: tsc clean, 205/205 tests). Netlify auto-deploy to core.eq.solutions triggered. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-intake-stage-gate-wt (eq-shell)~~ REMOVED | claude/intake-stage-gate-unify (merged, deleted) | Claude (confirmed `/intake`'s "Into EQ" commit bypassed the staging/conflict gate `/intake/core` already enforces via intake-stage.ts; also relabelled the `/intake` nav link from "Import"/Download-icon to "Intake"/Activity-icon) | 2026-07-29 | DONE — **[eq-solves-intake #80](https://github.com/eq-solutions/eq-solves-intake/pull/80) MERGED** (squash `9c6e5ad9`) + **[eq-shell #1088](https://github.com/eq-solutions/eq-shell/pull/1088) MERGED** (squash `890564e5`, Royce's "merge and go from there"). Netlify auto-deploy to core.eq.solutions triggered by the eq-shell merge. Worktree + branch removed. DomainLanding orphaned-routes cleanup filed separately (spawn_task, running independently) — not part of this PR. |
| ~~C:\Projects\eq-shell-depsweep-wt (eq-shell)~~ REMOVED | claude/dependabot-full-sweep (merged, deleted) | Claude (Royce: "sweep the dependency warnings now" - full triage of the remaining 146 Dependabot alerts left alone in the earlier xlsx/sharp session) | 2026-07-28 | DONE - **PR [#1083](https://github.com/eq-solutions/eq-shell/pull/1083) MERGED** (squash `3107494`, Royce's "merge" go). Full build + test suite verified clean on both lockfiles. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-suppliers-scroll-fix2-wt (eq-shell)~~ REMOVED | claude/suppliers-table-scroll-fix2 (merged, deleted) | Claude (PR #1079's overflowX:auto wrapper was insufficient — table-layout:auto just shrinks every column to fit 100% width instead of overflowing, so there was nothing to scroll to. Correct, already-proven fix: reuse `eq-rc-tablescroll` (records-redesign.css, `.eq-table{min-width:920px}`), same pattern already used in equipment/index.tsx) | 2026-07-28 | DONE — **PR [#1082](https://github.com/eq-solutions/eq-shell/pull/1082) MERGED** (squash `ea625944`, Royce's "merge #1082" go). All checks green. Worktree + branch removed. |
| ~~C:\Projects\eq-cards-photoid-equiv-wt (eq-cards)~~ REMOVED | claude/credential-gaps-photo-id-equivalence (merged, deleted) | Claude (Royce reported the Wallet's "SKS asks for Photo ID" nag showing even though he holds a Driver Licence — root-caused to `eq_cards_my_credential_gaps()` (jvkn) doing an exact-type match with no photo_id/driver_licence/passport equivalence, unlike Shell's own `satisfiesRequirement()`/`PHOTO_ID_EQUIVALENTS` which already treats them as interchangeable) | 2026-07-28 | DONE — **PR [#185](https://github.com/eq-solutions/eq-cards/pull/185) MERGED** (squash `b842785e`, Royce's "apply the credential-gaps fix" go). Migration 0109 applied live to jvkn via Supabase MCP, post-apply verified (`pg_get_functiondef` confirms the equivalence logic is live). Worktree + branch removed. |

| ~~C:\Projects\eq-shell-licence-revoke-sync-wt (eq-shell)~~ REMOVED | claude/licence-revoke-sync (merged, deleted) | Claude (`/decide` on the standing licence-auto-resync gap, then "build it" — found a concurrent session had already shipped the core mechanism same-day: `licence-push.ts` (#1076) + eq-cards#183, Cards now pushes to Shell after every licence save. One real gap survived: `softDelete()` in eq-cards never called the push hook, so a revoked/deleted Cards licence never got marked inactive in Shell — a permanent ghost-active row) | 2026-07-28 | DONE — **PR [#1080](https://github.com/eq-solutions/eq-shell/pull/1080) MERGED** (squash `5e4ce135`, Royce's "merge #1080 and #184" go). All checks green (typecheck/test/lint, schema-drift, gitleaks, migration-ledger-hygiene, Netlify deploy preview). Worktree + branch removed. |
| ~~C:\Projects\eq-cards-licence-revoke-sync-wt (eq-cards)~~ REMOVED | claude/licence-revoke-sync (merged, deleted) | Claude — companion fix, same task as the eq-shell row above: wires `softDelete()` to actually call `_pushToTenants()`. | 2026-07-28 | DONE — **PR [#184](https://github.com/eq-solutions/eq-cards/pull/184) MERGED** (squash `9e106989`, Royce's "merge #1080 and #184" go). Both checks green. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-suppliers-scroll-wt (eq-shell)~~ REMOVED | claude/suppliers-table-horizontal-scroll (merged, deleted) | Claude (Royce reported the Suppliers page missing its Login/Password columns live — root-caused to a CSS clip, not a missing feature: the code has fully shipped since PR #938-#1047, but `@eq-solutions/ui`'s Table renders `.eq-table-card{overflow:hidden}` with no horizontal-scroll wrapper, and Suppliers.tsx is the only page using `<Table>` directly with no `overflowX:auto` wrapping div, unlike every other wide-table page in this repo) | 2026-07-28 | DONE — **PR [#1079](https://github.com/eq-solutions/eq-shell/pull/1079) MERGED** (squash `f2852060`, Royce's "merge #1079" go). All checks green incl. schema-drift (the pre-existing `eq_enforce_function_privacy()` gap cleared between #1076 and this merge — fixed by a concurrent session). Worktree + branch removed. |

| ~~C:\Projects\eq-shell-qr-join-link-wt (eq-shell)~~ REMOVED | fix/worker-qr-join-link (merged, deleted) | Claude (session 8977bf51 — Royce found the Worker Join QR page dead-ends on "No invite found"; git history showed the /claim routing was a deliberate 2026-06-10 security lockdown ("keeps open enrollment off by default"), and the page copy promising open enrolment was added over a month later (PR #857) without updating to match — Royce chose to fix the stale copy, not reopen self-enrolment) | 2026-07-28 | DONE — **PR [#1078](https://github.com/eq-solutions/eq-shell/pull/1078) MERGED** (squash `fff7cc71`, Royce's "merge it once the preview checks pass" go). All 8 checks green incl. Netlify deploy preview. Copy-only change — lede, how-it-works steps, footnote, and header comment on `AdminWorkerQR.tsx` now describe invite-redemption, not open self-registration. No routing/behaviour change. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-licence-push-wt (eq-shell)~~ REMOVED | claude/licence-push-field-sync (merged, deleted) | Claude (PR2 of the licence-review-badge task — new licence-push.ts mirroring worker-profile-push.ts, so EQ Field's app_data.licences copy stays current when a worker edits a licence in Cards post-approval. eq-cards call-site change spun off as spawn_task chip task_ca0a29c5, running separately) | 2026-07-28 | DONE — **PR [#1076](https://github.com/eq-solutions/eq-shell/pull/1076) MERGED** (squash `057ea8c1`, Royce's "merge #1076" go). Only failing check (`Schema drift + anon-grant + policy-lint`) was pre-existing/unrelated — the `eq_enforce_function_privacy()` anon-EXECUTE gap, same cause as #1075's merge, spun off separately as task_61d978b6. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-licence-badge-wt (eq-shell)~~ REMOVED | claude/licence-review-badge-staleness (merged, deleted) | Claude (Royce asked for a licence-review-badge staleness fix — reviewBadgeFor() only catches a brand-new licence via created_at, not a Cards-side edit to an already-reviewed one, since updated_at was never threaded through staff-canonical-licences.ts/LicenceRow. Root eq-shell checkout is a shared machine — a concurrent session's `git checkout` back to `claude/bump-sharp-xlsx` wiped this branch's uncommitted edits mid-session, moved here to avoid a repeat) | 2026-07-28 | DONE — **PR [#1075](https://github.com/eq-solutions/eq-shell/pull/1075) MERGED** (squash `8090e549`, Royce's "merge #1075" go). Build clean, 265/265 tests pass (6 new). Only failing check was the pre-existing/unrelated `eq_enforce_function_privacy()` anon-EXECUTE gap, spun off separately as task_61d978b6. Worktree + branch removed. |

| ~~C:\Projects\eq-shell-default-priv-functions-wt (eq-shell)~~ SUPERSEDED — see row below | claude/default-privileges-functions-lockdown | Claude (attempted root-cause fix for SEC-4/SEC-13/SEC-15 via `ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` on ehow — live-tested in a rolled-back transaction, confirmed a no-op, deleted the file, committed nothing) | 2026-07-28 | **Stale row, corrected: this worktree was not left idle — a concurrent session continued directly in it**, built the actual fix (an event trigger, since default privileges don't work here), shipped PRs #1070/#1072, and removed the worktree. See the `claude/default-privileges-functions-lockdown` row below (DONE, SEC-16) for the real outcome. Folder confirmed gone from disk. Kept only so the negative-result diagnosis isn't lost — it's what the working fix was built on. |
| ~~C:\Projects\eqsvc-fix-gate-comment-wt (eq-solves-service)~~ REMOVED | claude/fix-apply-gate-claim (merged, deleted) | Claude (dispatched `apply-service-migrations.yml` on Royce's "go ahead" for PR #619 — it ran straight through in 18s instead of pausing for approval as the workflow's own header comment (and what I told Royce) claimed. Live-checked via `gh api .../environments/production`: `protection_rules` is branch-policy only, no required-reviewer rule — same root cause as SEC-11 (eq-shell's tenant-migrate.yml), just not previously discovered on this repo. No harm done — the SQL was already dry-run verified safe — but the false claim needs correcting so nobody trusts a safety net that isn't there) | 2026-07-28 | DONE — **PR [#620](https://github.com/eq-solutions/eq-service/pull/620) MERGED** (squash `5f48772`, Royce's "merge #620" go). Docs-only fix — corrected the workflow's header comment, no functional change. Logged as SEC-14 in `ops/security-register.md` (accepted, not fixing the actual gate — same reasoning as SEC-11: Milmlow is the only collaborator with dispatch access anyway). Worktree + branch removed. |
| ~~C:\Projects\eqsvc-migration-reconcile-wt (eq-solves-service)~~ REMOVED | claude/reconcile-ledger-drift (merged, deleted) | Claude (Royce asked to reconcile the 5 migrations flagged drifted by PR #614's audit — live-verified via Supabase MCP that 3 of the 5 target schema that no longer exists (public.tenant_members, public.contacts, public.contract_scopes — all superseded by the service.*/app_data.* canonical write layer built later); reapplying those verbatim would hard-fail or create orphaned duplicate objects. Only 0126 (rls_introspection) is genuinely still needed and fixable — its hardcoded `public`-schema scan needs to become `public`+`service` to match where real RLS-bearing tables actually live now. 0089 (notification cron) restored schema-only, deliberately left unscheduled — Royce's call, already parked) | 2026-07-28 | DONE — **PR [#619](https://github.com/eq-solutions/eq-service/pull/619) MERGED** (squash `7aab331`, Royce's "merge #619" go). New migrations 0192 (rls_introspection rewritten for public+service schema scan, zero collision verified) + 0193 (notification-dispatch schema restored, NOT scheduled) committed to `supabase/migrations/`. Both dry-run verified against live ehow in a rolled-back transaction before committing. 0077/0082/0131-0135 confirmed superseded/abandoned, documented in docs/HISTORY.md, deliberately not reconciled. `tsc + next build` (real gate) passed; `Integration tests` and `Typecheck + audit` both red on confirmed pre-existing/unrelated causes (the former dies at migration 0151, before ever reaching 0192/0193). **NOT YET APPLIED to live ehow** — merging to main only lands the files; the DDL apply is a separate step (`apply-service-migrations.yml` dispatch) gated behind the `production` GitHub Environment requiring Royce's explicit approval click. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-drift-fix (eq-shell)~~ REMOVED | claude/fix-field-people-security-invoker (merged, deleted) | Claude (drift gate red on main since 2026-07-28T03:26 UTC — `app_data.field_people`/`field_people_removed` on ehow/sks lost `security_invoker` again, sixth instance of the recurring bug class fixed before in 0158/0164; blocking other PRs incl. #1053) | 2026-07-28 | DONE — **PR [#1054](https://github.com/eq-solutions/eq-shell/pull/1054) MERGED** (squash `5439643`, Royce's "merge #1054 once CI is green" go). Migration 0211 hit the "reassert security_invoker" chicken-and-egg (drift gate reads live DB, not the diff — merge-then-dispatch is circular for this fix class, per [[eq-shell-drift-gate-dispatch-order]]) — dispatched `tenant-migrate.yml --ref <pr-branch> -f slug=sks` with Royce's explicit go (AskUserQuestion, since the auto-mode classifier blocked the raw `gh workflow run`), applied clean (1 applied, 226 skipped), live-verified `reloptions=security_invoker=on` on both views post-apply, re-ran the drift-check job green, then merged. Worktree removed. |
| ~~C:\Projects\eq-cards-connreq-errhandling-wt (eq-cards)~~ REMOVED | claude/connections-submit-error-handling (no commits, deleted) | Claude (investigated Sentry EQ-CARDS-17, `ValidationFailure: You've already applied — waiting on a decision.` on `/profile`) | 2026-07-28 | **NOT A BUG — closed, no code change.** Both call sites of `submitRequest` (`connect_to_company_screen.dart`'s Apply, `outgoing_requests_banner.dart`'s Ask-again) already catch it, show `userMessageForError(e)` (which passes `ValidationFailure.message` straight through — the exact text the user saw) via snackbar/inline text, and deliberately `Sentry.captureException` it for visibility. The Sentry "error" issue is the app's own instrumentation working as designed, not an unhandled crash. Worktree + empty branch removed. |
| ~~C:\Projects\eq-field-leave-render-race-wt (eq-field)~~ REMOVED | claude/leave-render-nav-race (merged, deleted) | Claude (Royce asked to fix "the 2 issues outstanding" — the 2 flagged eq-field Sentry errors. `openLeaveRequest is not defined` was already fixed live in v3.5.358; `TypeError: Cannot set properties of null (setting 'innerHTML')` was not — pulled the real stack trace via the now-connected Sentry MCP: `toggleShowArchived` → `renderLeave` → `_renderLeaveWorker`, writing to `#leave-content` after navigating off the Leave page mid-`await`. This is exactly the "latent sibling risk" an earlier session flagged but didn't fix — CC List/Archive Resolved/Show Archived/Print all share the identical unguarded-render-after-await shape, 8+ call sites total) | 2026-07-28 | DONE — **PR [#556](https://github.com/eq-solutions/eq-field/pull/556) MERGED** (squash `22d6dca`, Royce's "merge #556" go). Single guard added to `renderLeave()` (`if (currentPage !== 'leave') return;`), matching the idiom `_ensureLeaveLoaded()` already used for the same race — closes all 8+ call sites in one place. `node --check` clean, all 19 `tests/*.test.js` suites pass, full Playwright e2e suite (boot/roster/timesheets/leave × standalone + shell-mode) 8/8 pass. Live on field.eq.solutions via Netlify auto-deploy. Worktree + branch removed. |
| ~~C:\Projects\eq-field\.claude\worktrees\lh-archive-rating (eq-field)~~ REMOVED (orphaned husk — Windows permission-denied on delete, same class as other rounds above; git no longer tracks it) | claude/lh-archive-rating (merged, deleted) | Claude (port SKS labour-hire archive + would-rehire star rating from roster grid, SKS v3.10.104) | 2026-07-28 | DONE — **PR [#555](https://github.com/eq-solutions/eq-field/pull/555) MERGED** (squash `a406695`, Royce's "merge" go). DB migration `field_people_labour_hire_rating` live on ehow (rating column + view/trigger updates). Live on field.eq.solutions via Netlify auto-deploy on merge. Remote branch deleted via API (local delete failed — `main` already checked out in `bold-volhard-645dd7`). Physical worktree folder left as an orphaned husk, harmless. |
| ~~C:\Projects\eq-shell\.claude\worktrees\release-tagging (eq-shell)~~ REMOVED | claude/release-tagging (merged, deleted) | Claude (release-tagging workflow — new `.github/workflows/tag-release.yml`, lightweight `release-<YYYY-MM-DD>-<7-char-short-sha>` tag on push to main, same scheme as eq-solves-service. Task B, perm-sync under-grant fix, found already fixed on main via #1016/#1021/#1025 — no branch/PR needed for it) | 2026-07-27 | DONE — **PR [#1052](https://github.com/eq-solutions/eq-shell/pull/1052) MERGED** (squash `dcda5f4a`, Royce's "merge the three PRs" go). All CI green pre-merge. `gh pr merge --delete-branch` failed the local branch-delete step only (branch still checked out in this worktree) — merge itself succeeded; worktree removed + local branch deleted manually afterward. Live on core.eq.solutions via Netlify auto-deploy on merge. |
| ~~C:\Projects\eq-field\.claude\worktrees\tag-release (eq-field)~~ REMOVED | claude/release-tagging (merged, deleted) | Claude (release-tagging workflow — new `.github/workflows/tag-release.yml`, extracts APP_VERSION from scripts/app-state.js, tags+releases on push to main if tag doesn't already exist) | 2026-07-27 | DONE — **PR [#554](https://github.com/eq-solutions/eq-field/pull/554) MERGED** (squash `3033cdb2`, Royce's "merge the three PRs" go). All CI + deploy-preview checks green pre-merge. Worktree removed + local branch deleted. Live on field.eq.solutions via Netlify auto-deploy; will cut its first-ever tag (`v<APP_VERSION>`) automatically on this merge. |
| ~~C:\Projects\eq-solves-service\.claude\worktrees\release-tagging (eq-solves-service)~~ REMOVED | claude/release-tagging (merged, deleted) | Claude (revive release tagging — new `.github/workflows/tag-release.yml`, pushes a lightweight `release-<YYYY-MM-DD>-<short-sha>` tag on every push to main; no source-file version bump, no GitHub Release. Root checkout is on `claude/migration-ledger-checksum-verify` with another session's uncommitted work, isolated here off origin/main) | 2026-07-27 | DONE — **PR [#618](https://github.com/eq-solutions/eq-service/pull/618) MERGED** (squash `d42179ff`, Royce's "merge the three PRs" go). CI red on the same 2 pre-existing/unrelated gates every recent PR here carries (`Integration tests (Supabase local)` — documented flake; `Typecheck + audit` — 16 pre-existing transitive-dep vulnerabilities in archiver/exceljs/rimraf, confirmed via job log, unrelated to this workflow-only change); `tsc + next build` (the real gate) passed. Worktree removed + local branch deleted. Live on service.eq.solutions via Netlify auto-deploy. |
| ~~C:\Projects\eq-shell-cpdrift-wt (eq-shell)~~ REMOVED | claude/jvkn-function-drift-gate (merged, deleted) | Claude (control-plane function-drift CI check + backfill — root checkout was on `claude/access-model-phase2-3-guardrails`, another session's branch with its own uncommitted work, so isolated here off origin/main) | 2026-07-27 | DONE — **PR [#1048](https://github.com/eq-solutions/eq-shell/pull/1048) MERGED** (squash `25b1acf`, Royce's "merge" go). New `scripts/check-control-plane-drift.mjs` (jvkn live-function-vs-source drift gate — not wired into any workflow yet, that's a separate decision), new migration `2026_07_27_eq_get_licences_expiring_within_backfill.sql` (already applied to live jvkn pre-merge as a verified no-op, `schema_migrations` version `20260727103258`), `CONTROL-PLANE-LEDGER.md` updated. All CI green pre-merge (typecheck·test·lint, migration-ledger-hygiene, schema-drift/anon-grant/policy-lint, Netlify deploy preview). `gh pr merge --delete-branch` failed the local branch-delete step only (another worktree already had `main` checked out) — merge itself succeeded; remote branch deleted manually via API afterward, worktree removed. Live on core.eq.solutions via Netlify auto-deploy on merge (script/migration/doc-only change, no runtime code path). |
| ~~C:\Projects\eqsvc-npm-audit-wt (eq-solves-service)~~ REMOVED | claude/npm-audit-fix (merged, deleted) | Claude (Royce asked to fix the `npm audit --audit-level=high` CI failure, and whether today's session caused it — confirmed it's a fresh GHSA published 2026-07-24, 3 days before this session started, unrelated to any of today's merges) | 2026-07-27 | DONE — **PR [#616](https://github.com/eq-solutions/eq-service/pull/616) MERGED** (squash `e37a553`, Royce's "merge #616 as-is" go). 4 physical `brace-expansion` copies in the tree via 2 unrelated `minimatch` majors — only the `minimatch@10.2.5` chain (`@typescript-eslint/typescript-estree` + `@sentry/bundler-plugin-core`) already uses the new object-style API and safely takes v5.0.8; the `minimatch@3.1.5` (eslint's own plugin chain) and `minimatch@5.1.9` (`readdir-glob` under `exceljs`→`archiver`) chains still call it as a bare function and **crash eslint outright** if forced (`TypeError: expand is not a function`) — confirmed by testing, reverted. Scoped the override to just the 2 safe paths. Verified the other 2 are non-reachable at runtime: eslint's chain never sees untrusted input; `exceljs` only calls `archiver`'s `.append()`/`.file()`, never `.glob()` (the only vulnerable entry point) — grepped `zip-stream.js`/`workbook-writer.js` directly to confirm. `tsc`/tests/build/eslint all clean, live on service.eq.solutions. **Did not flip the CI gate green** — `npm audit` counts by package name, and the same 16 names stay flagged via their other unfixed copy; still needs an upstream fix or a change to what the audit CI step checks, Royce's explicit call to make, not decided here. Worktree + branch removed. |
| ~~C:\Projects\eqsvc-redirect-headers-wt (eq-solves-service)~~ REMOVED | claude/redirect-security-headers (merged, deleted) | Claude (follow-up to #607/#613 — the security-headers fix only applies to normal page responses via `next.config.ts headers()`; `proxy.ts`'s 8 `NextResponse.redirect()` call sites don't carry those headers on the 3xx itself. Root checkout `.claude/worktrees/user-source-analysis-be4daa` is now an orphaned fake worktree, no `.git` of its own, git falls through to the root checkout on `claude/migration-ledger-checksum-verify` with another session's uncommitted asset-import work — isolated here off origin/main instead) | 2026-07-27 | DONE — **PR [#615](https://github.com/eq-solutions/eq-service/pull/615) MERGED** (squash `71ef7b6`, Royce's "merge it" go). Extracted the header set into new `lib/security-headers.ts` (shared by `next.config.ts` and `proxy.ts` — was about to become a second hand-copied constant, made it the single source instead). All 8 redirect call sites wrapped in `withSecurityHeaders()`. `tsc --noEmit` clean, 359/359 tests, `npm run build` production build succeeds. Pre-merge CI: `tsc + next build` (real gate) + both Netlify checks passed; `Typecheck + audit` and `Integration tests (Supabase local)` both red on the same pre-existing/unrelated failures every recent PR here carries (16 eslint/archiver transitive-dep vulnerabilities; `public.app_config` local-harness schema-cache bug) — confirmed via job logs, not assumed. Live on service.eq.solutions via Netlify auto-deploy. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-field-nav-fixes-wt (eq-shell)~~ REMOVED | claude/field-records-and-rail-sensitivity (merged, deleted) | Claude (Royce asked two things live: "where are the records in the nav bar" while looking at EQ Field, and reported the Core sidebar rail "opens by mistake a fair bit" while using Field. Root-caused both: FieldIframe.tsx never wired `sidebarRecords` to `HubLayout`, unlike ServiceIframe/CardsIframe which both do — so Field is the only iframe module missing the Records nav section entirely. Separately, the 52px hover-expand rail has no hover-intent delay, so any mouse graze near the left edge pops it to 260px) | 2026-07-27 | DONE — **PR [#1043](https://github.com/eq-solutions/eq-shell/pull/1043) MERGED** (squash `37da2276`). Passed `defaultSidebarRecords()` at all 3 non-`hideMainSidebar` HubLayout call sites in FieldIframe.tsx, matching ServiceIframe/CardsIframe byte-for-byte. Added a 150ms `transition-delay` on the rail's `:hover` rule only (enters delayed, leaves instant — standard one-directional hover-intent trick), pushed the text-fade delay 80ms→230ms to stay choreographed after it. `build:packages`+`build:tokens`+`tsc -b` clean, `vite build` clean, check-css 550 classes, 253/253 tests, all CI green. Could not click-test the hover feel live (no Shell login credentials in this session) — worth a quick eyeball on core.eq.solutions. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-preview-embed-msg-wt (eq-shell)~~ REMOVED | claude/service-embed-preview-message (merged, deleted) | Claude (follow-up to eq-service #607, which made Service's `frame-ancestors` actually reach page responses — that correctly blocks Shell deploy previews from embedding prod Service, but without this change the preview mints a token, embeds, and waits out the 45s timeout to show a misleading "took too long to respond") | 2026-07-27 | DONE — **PR [#1042](https://github.com/eq-solutions/eq-shell/pull/1042) MERGED** (squash `62c99f29`, Royce's "yes do the ServiceIframe fix"). Adds a `blocked` phase distinct from `error` — no Sentry noise, no pointless retry, no token minted; an explicit `VITE_SERVICE_URL` opts out. **Confirmed there is NO Shell-side config that restores preview embedding** — a Service *preview* build sends the same header, so pointing `VITE_SERVICE_URL` at one doesn't help either (corrects an earlier claim in this session that it was a "one-line env var fix"). `build:packages`+`build:tokens`+`tsc -b` clean, `vite build` clean, check-css 550 classes, 253/253 tests, all CI green. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-iframe-loading-spinner-wt (eq-shell)~~ REMOVED | claude/iframe-loading-canonical-spinner (merged, deleted) | Claude (follow-up to #1037's white-background fix — the loading pane is now white but the `Skeleton` shimmer (`--eq-gray-200` = `#E4DDD2`, opacity-pulse only) is too low-contrast against white to read as "loading". Royce asked to use eq-ui's canonical `Spinner` (the `bars` variant, already the established full-page-load pattern in `App.tsx`'s `BlockingSpinner`) instead — root checkout is on `claude/access-model-phase2-3-guardrails`, another session's branch, so isolated here off origin/main) | 2026-07-27 | DONE — **PR [#1039](https://github.com/eq-solutions/eq-shell/pull/1039) MERGED** (squash `333ada27`, Royce's go). `IframeLoadingSkeleton` (Field/Service/Cards handoff, shared component) now renders `<Spinner variant="bars" size="lg" />` + a visible text label instead of three stacked skeleton blocks. `pnpm run build:packages && build:tokens && tsc -b` clean, `vite build` clean, `check-css-coverage` (550 classes resolve) clean, 253/253 tests pass. CI all green (typecheck·test·lint, migration-ledger-hygiene, schema-drift/anon-grant/policy-lint, Netlify deploy preview ready). Live on core.eq.solutions via Netlify auto-deploy. Could not visually confirm live (no Shell login credentials in this session) — Royce to eyeball post-deploy. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-iframe-loading-bg-wt (eq-shell)~~ REMOVED | claude/iframe-loading-black-bg (merged, deleted) | Claude (Royce photographed the Service iframe loading pane rendering solid near-black instead of the intended white-background loading skeleton; root-caused to `.eq-hub { background: #0F1117 }` bleeding through `.eq-hub__iframe-content`, which has no background override, wherever the narrow centred loading skeleton doesn't cover it — root checkout is on `claude/access-model-phase2-3-guardrails`, another session's branch, so isolated here off origin/main) | 2026-07-27 | DONE — **PR [#1037](https://github.com/eq-solutions/eq-shell/pull/1037) MERGED** (squash `36db17d0`, Royce's "ship it" go). One-line CSS fix: `.eq-hub__iframe-content { background: var(--eq-white, #fff) }`. Also fixes the same black-flash for Field/Cards iframes (shared `IframeLoadingSkeleton`/`.eq-hub__iframe-content`), not just Service. `pnpm run build:packages && build:tokens && tsc -b` clean, `vite build` clean, `check-css-coverage` (549 classes resolve) clean. CI: typecheck·test·lint pass, migration-ledger-hygiene pass, schema-drift/anon-grant/policy-lint pass, Netlify deploy preview ready. Live on core.eq.solutions via Netlify auto-deploy on merge. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-bump-ui-1120-wt (eq-shell)~~ REMOVED | claude/bump-eq-ui-v1.12.0 (merged, deleted) | Claude (bump `@eq-solutions/ui` pin from v1.11.1 to v1.12.0 — new Tooltip/EmptyState/Pagination components just published; root checkout is on `claude/access-model-phase2-3-guardrails`, another session's branch, so isolated here off origin/main) | 2026-07-26 | DONE — **PR [#1027](https://github.com/eq-solutions/eq-shell/pull/1027) MERGED** (squash `9293c2c`, Royce's "merge both" go). Found a real bug as a side effect: this repo's barrel-CSS import (`@eq-solutions/ui/styles`) was missing `DropdownMenu.css` at v1.11.1 (confirmed against the live v1.11.1 tag) and QuotesModule uses `DropdownMenu` with no direct CSS import — its "⋯" menu has likely been rendering unstyled in production on core.eq.solutions until this deploys. `build:packages && build:tokens && tsc -b` clean, 247/247 tests, `check:perms` + `check:css` (549 classes resolve) both pass, lockfile diff is the single `@eq-solutions/ui` resolution only. Netlify auto-deploys main → core.eq.solutions. Worktree + branch removed. |
| ~~C:\Projects\eqsvc-bump-ui-1120-wt (eq-solves-service)~~ REMOVED | claude/bump-eq-ui-v1.12.0 (merged, deleted) | Claude (same bump, second repo — root checkout has unrelated uncommitted asset-import work from another session, isolated here off origin/main) | 2026-07-26 | DONE — **PR [#604](https://github.com/eq-solutions/eq-service/pull/604) MERGED** (squash `d4b931e`, Royce's "merge both" go). This repo doesn't use `DropdownMenu` so the barrel-CSS gap fixed upstream doesn't apply — purely additive. `tsc --noEmit` clean, 359/359 tests, `next build` TypeScript phase clean (page-data collection failed locally only on missing `NEXT_PUBLIC_SUPABASE_*` secrets, which CI has). CI red on 2 pre-existing/unrelated gates: `Integration tests (Supabase local)` (documented flake) and `Typecheck + audit` (19 pre-existing transitive-dep vulnerabilities — minimatch/eslint chain is dev-only, `sharp`/`uuid` are real but pre-existing, spun off separately). Worktree + branch removed. |

- **Reconstructed row 2026-07-24:** this session's own `eq-context` checkout had a concurrent session's uncommitted registration for the row below in its working tree; a `git reset --hard` run against `eq-context` (unrelated repo, different session) discarded it before it was ever staged — no git object existed for it, unrecoverable. Rebuilt from the worktree's actual git state + the live deployed function version, not guessed.

| ~~C:\Projects\eq-shell\.claude\worktrees\staff-archive-sync-fix (eq-shell)~~ REMOVED | claude/staff-archive-sync-bug-65e0b7 | Claude (eq-shell session — Royce reported the same 5 archived staff kept reappearing; root-caused live to `workers-canonical-sync`'s nightly merge unconditionally forcing every synced worker back to active/approved, silently un-archiving anyone with a linked Cards account — 87 rows affected in one run, not just 5) | 2026-07-24 | DONE — **PR [#993](https://github.com/eq-solutions/eq-shell/pull/993) MERGED** to main (`252d8574`). Fix removes the forced `active:true`/`field_approved:true` from the sync's merge branch entirely — that status is now only ever set by the explicit actions that own it (Shell's archive button, Cards approval flow). **Live: `workers-canonical-sync` v9 on jvkn, confirmed via `list_edge_functions`.** Verification against tonight's 02:35 UTC cron still pending. Worktree prunable. Follow-on governance task spun off separately — see `claude/reconcile-worker-sync-codify` row below. |
| ~~C:\Projects\eq-shell\.claude\worktrees\reconcile-sync-migration-codify (eq-shell)~~ REMOVED | claude/reconcile-worker-sync-codify (merged, deleted) | Claude (eq-shell session — follow-on from the row above: `eq_reconcile_worker_sync()` + its two support tables + its pg_cron job existed only in the live jvkn DB with no matching migration file anywhere, a "no canonical DDL outside the One Pipe" gap) | 2026-07-24 | DONE — **PR [#994](https://github.com/eq-solutions/eq-shell/pull/994) MERGED** (squash `deb84baa`, Royce's "merge #994 and apply" go), **PR [#995](https://github.com/eq-solutions/eq-shell/pull/995) MERGED** (squash `026a88f`, ledger update). New control-plane migration `2026_07_24_reconcile_worker_sync_codify.sql` applied to jvkn via Supabase MCP `apply_migration` (recorded as `schema_migrations` version `20260723193452`) — post-apply verified: function + both support tables intact, exactly one active cron job, no duplication. `CONTROL-PLANE-LEDGER.md` updated. Worktree prunable. |
| ~~C:\Projects\eq-shell\.claude\worktrees\staff-active-writeguard (eq-shell)~~ REMOVED | claude/staff-active-writeguard (merged, deleted) | Claude (eq-shell session — Royce asked to steelman #993's fix and build the fuller solution. Found two bigger gaps: (1) zaap's app_data.staff still granted `authenticated` column-level UPDATE on almost every column incl. active/field_approved/pay-rates — RLS there is tenant-only with no per-row check, so any EQ user could PATCH any colleague's staff row directly via PostgREST, bypassing every app-layer permission check; ehow already had no such grant. (2) #993 fixed the one known automated reactivation writer but added no structural backstop against a future one. Also found `credentials-canonical-sync` (jvkn) is 100% dead code — `worker_credentials` has 0 rows, the real licence pipeline is `public.licences` via `cards-approve-staff.ts`) | 2026-07-26 | DONE — **PR [#1009](https://github.com/eq-solutions/eq-shell/pull/1009) MERGED** (squash `da758835`, Royce's "merge #1009 and dispatch tenant-migrate.yml" go). `0208`/`0209` dispatched via `tenant-migrate.yml` run [30183959584](https://github.com/eq-solutions/eq-shell/actions/runs/30183959584) — both applied clean on `eq`(zaap)+`sks`(ehow), live-verified: `authenticated` UPDATE on `active`/`field_approved` now false on zaap (was true), `staff_guard_reactivation` trigger enabled + `activated_by`/`activated_at` columns present on both planes. The jvkn control-plane migration (`2026_07_26_retire_credentials_canonical_sync.sql`) is **also now applied** — Royce's "apply it" go, hand-applied via Supabase MCP `apply_migration`, post-apply verified: `credential_canonical_sync` trigger + `sync_credential_to_canonical()` both gone, the live `worker_canonical_sync` trigger on `public.workers` confirmed untouched. `CONTROL-PLANE-LEDGER.md` updated ([#1010](https://github.com/eq-solutions/eq-shell/pull/1010) MERGED, squash `566fd2c8`). Everything from this task is now fully closed — code merged, applied, and verified on all three planes. Worktree + branch removed. |

- **Correction 2026-07-23:** the note below about `eq-shell-pr971-conflict-wt` was wrong on ownership — it WAS this session's worktree (registered above, then the registration got overwritten by a race before this correction). Told "merge all" on 4 open eq-shell PRs; found an unregistered worktree (`eq-shell-tenant-scope-rebase-wt`) already mid-conflict-resolution on PR #971's branch from another session, left it alone, resolved the same conflict independently in this fresh worktree instead (`StaffPage.tsx` + `TenantSwitcher.tsx`, both trivial — complementary fixes on both sides, kept both). Build clean, 218/218 tests, ready to push — but the other session finished and merged PR #971 (squash `d56159a`) first. Worktree + local temp branch removed here, nothing lost.

| ~~C:\Projects\eq-shell-default-priv-functions-wt (eq-shell)~~ REMOVED | claude/default-privileges-functions-lockdown (merged, deleted) | Claude (root-cause follow-up to SEC-15/SEC-13/SEC-4, continuing after the sibling row above disproved the simple `ALTER DEFAULT PRIVILEGES` fix in the same worktree: confirmed live that Postgres always seeds new functions with PUBLIC-execute on this platform regardless of default-privilege revokes — a genuine Postgres/Supabase-platform limitation, not a config bug. Royce's call after hearing this: build an event trigger instead) | 2026-07-28 | DONE — **eq-shell [#1070](https://github.com/eq-solutions/eq-shell/pull/1070) MERGED** (squash `7f93b1ea`, Royce's "merge + apply all 3 planes now" go) + **[#1072](https://github.com/eq-solutions/eq-shell/pull/1072) MERGED** (ledger doc). Shipped `eq_enforce_function_privacy`, a `ddl_command_end` event trigger re-asserting service_role-only EXECUTE on every function created/replaced in guarded schemas (`public`+`service` on zaap/ehow, `public`+`shell_control` on jvkn), verified compatible with the existing "explicit GRANT for a legit anon RPC" convention. Applied + live-verified on all 3 planes with real (non-transactional) test functions: zaap+ehow via `tenant-migrate.yml` dispatch (run 30337989873), jvkn via Supabase MCP `apply_migration`. Full writeup: `ops/security-register.md` SEC-16. Worktree removed. |
| ~~C:\Projects\eqsvc-rls-introspection-anon-grant-wt (eq-solves-service)~~ REMOVE WHEN DONE | claude/fix-rls-introspection-anon-grant (merged) | Claude (eq-shell tenant-drift CI flagged `rls_introspection()` as a new anon-executable SECURITY DEFINER function on ehow — root-caused to migration 0192 (PR #619, same day) reverting to a PUBLIC-only REVOKE instead of naming anon/authenticated like 0129 originally did, combined with ehow's public-schema default privileges never having been locked down for FUNCTIONS, only TABLES, in the 2026-06-07 lockdown. Root checkout is on `claude/migration-ledger-checksum-verify` with another session's unrelated uncommitted asset-import work — isolated here off origin/main) | 2026-07-28 | DONE — **PR [#622](https://github.com/eq-solutions/eq-service/pull/622) MERGED** (squash `619dd5e`, Royce's "merge + dispatch now" go). Migration 0194 dry-run verified live first, then `apply-service-migrations.yml` dispatched (run 30335023443, success) and live-verified: anon/auth EXECUTE → false, service_role → true. **Duplicate-effort discovery:** a concurrent session, at the same time, independently found + fixed the identical live exposure via eq-shell's OWN tenant-migrations pipeline — `fix/revoke-anon-rls-introspection` branch got checked out into this same eq-shell root checkout mid-session (shared-checkout collision, same class documented elsewhere in this file) and committed `supabase/tenant-migrations/0219_revoke_anon_rls_introspection.sql` (idempotent DO-block, safe on zaap where the function doesn't exist), opened as eq-shell PR #1061 (OPEN, not mine to touch). Their fix applied live to BOTH ehow (06:29:12 UTC) and zaap (06:29:40 UTC) via `tenant-migrate.yml` — **~85s before** mine (0194 applied 06:30:35 UTC) — so 0219 closed the exposure first and 0194 landed as a harmless idempotent no-op on top; both do the exact same REVOKE/GRANT, no conflict. Re-ran eq-shell's `Tenant drift + anon-grant + policy-lint check` (run 30335135093) — confirmed **green**: "Function-EXECUTE invariant … sks-canonical (SKS tenant): clean". eq-solves-service worktree removed; eq-shell PR #1061 left alone for Royce/the other session to close.
| C:\Projects\eq-shell-ops-perms-derive-wt (eq-shell) | claude/access-control-derive-labels | Claude (follow-up to #1025 — the Access Control admin page's MODULES list turned out to be its own hand-typed mirror of the permission matrix, display-layer version of the same drift risk. Royce confirmed: keep "EQ Ops" combined, derive from the package) | 2026-07-26 | DONE — **PR [#1026](https://github.com/eq-solutions/eq-shell/pull/1026) OPEN, NOT merged** (Royce's call). Derivation immediately surfaced two real, pre-existing gaps: 4 of 6 `ops.*` keys and `field.manage_people` were live, enforced permissions with no override toggle in this UI at all — not a hypothetical risk, an already-happened one (same class as cards.view/cards.onboard lingering here after #1025). `MODULE_SECTIONS` now only decides grouping/shape; keys+labels come from `PERMISSIONS` filtered non-deprecated. `pnpm run build:packages && npx tsc -b` clean, check-perm-sync all pass, 247/247 tests, derived output spot-checked directly against live package data. Could not click through live (no seeded tenant credentials in session) — flagged in the PR for Royce to eyeball post-merge. Worktree prunable after merge.
| ~~C:\Projects\eq-solves-service-roles-bump-wt (eq-solves-service)~~ REMOVED | claude/bump-eq-roles-v2.5.5 (merged, deleted) | Claude (follow-up to the eq-shell permissions audit — Royce asked to bump this repo's stale @eq-solutions/roles pin, v2.5.3 → v2.5.5) | 2026-07-26 | DONE — **PR [#603](https://github.com/eq-solutions/eq-service/pull/603) MERGED** (squash `b2be24b`, Royce's "merge #1025 and #603 once green" go — merged on the strength of local verification after eq-service's org-level GitHub Actions billing cap ($0 Actions budget, confirmed via the org's `/settings/billing/budgets` API) blocked CI from running at all on two separate fresh trigger attempts). Root checkout (`C:\Projects\eq-solves-service`) was detached HEAD with unrelated uncommitted asset-import/csv-parser work sitting in it (same known hazard logged elsewhere in this file) — untouched, used a fresh worktree off `origin/main` instead. Confirmed no-op before opening: v2.5.4/v2.5.5 only added `ops.view_suppliers`/`manage_suppliers` (eq-shell-only) and a Dart package (eq-cards-only), neither touches this repo's `can()`/`PermKey`/`EqRole` usage. `npm run check` (tsc+build) clean, 359/359 tests. Live on service.eq.solutions (Netlify deploy verified: commit_ref matches, state ready). Worktree + branch removed 2026-07-26.
| ~~C:\Projects\eq-shell-cards-perm-cleanup-wt (eq-shell)~~ REMOVED | claude/cards-deprecated-perms (merged, deleted) | Claude (follow-up to PR #1021 — Royce asked to tidy up the deprecated cards.view/cards.onboard permissions found during that audit and look for other polish areas) | 2026-07-26 | DONE — **PR [#1025](https://github.com/eq-solutions/eq-shell/pull/1025) MERGED** (squash `aded9eae`, Royce's "merge #1025 and #603 once green" go). Live on core.eq.solutions. Verified zero functional check sites in the repo and zero live tenant_role_overrides/security_group_perms rows on jvkn before removing — deleted `src/modules/cards/permissions.ts`, updated matrix.ts's PermKey union/composition, dropped the dead "Cards" toggle section from AccessControlPage.tsx, updated check-perm-sync.mjs. `pnpm run build:packages && npx tsc -b` clean, check-perm-sync all pass, 247/247 tests. Broader audit (comms module deliberately reuses field.* keys, not a mirror — fine; AccessControlPage.tsx's MODULES array is a separate hand-typed label/grouping mirror, display-only — flagged, not auto-fixed, needs Royce's call on the quotes+ops grouping; eq-solves-service pins @eq-solutions/roles at v2.5.3, 2 releases behind — flagged, low urgency; eq-field has no dependency on the package at all — established architecture difference (vanilla JS), not a gap; eq-cards already wired onto the new v2.5.5 Dart package by a concurrent session today, PR #178) reported to Royce, not acted on further this session. Worktree prunable after merge.
| ~~C:\Projects\eq-shell-collapse-matrix-wt (eq-shell)~~ REMOVED | claude/collapse-matrix-to-package (merged, deleted) | Claude (deferred Access-Model Phase 3 "Guardrails" scoping item — audited eq-shell's 7 hand-typed `modules/*/permissions.ts` files + matrix.ts's inline ENTITY_MATRIX/OPS_MATRIX against @eq-solutions/roles v2.5.5: exact 1:1 match everywhere except intake.sites.merge, a genuine Shell-local permission. Royce approved implementing the collapse) | 2026-07-26 | DONE — **PR [#1021](https://github.com/eq-solutions/eq-shell/pull/1021) MERGED** (squash `0decaec`, Royce's "merge #1021 once it's green" go). Root checkout (`C:\Projects\eq-shell`) was mid-race with a concurrent session — reflog showed it get checked out from under this session (`claude/collapse-matrix-to-package` → `claude/access-model-phase2-3-guardrails`) mid-task, and another session's uncommitted `canWithReason`/`scripts/why-can.ts` diagnostic work was sitting in the same working tree. Left it completely untouched; re-applied this session's 9 files fresh into an isolated worktree off `origin/main` instead. Replaced the 7 module files + matrix.ts's ENTITY/OPS blocks with re-exports from the package's matching subpath exports; `intake/permissions.ts` stays a hybrid (package import + `intake.sites.merge`, manager-only). Rewrote `check-perm-sync.mjs` to check shape (pure re-export, zero hand-typed literals) instead of parsing now-absent literal arrays. `pnpm run build:packages && npx tsc -b` clean, `check-perm-sync.mjs` all 4 checks pass, 236/236 tests; all PR checks green (typecheck·test·lint, schema-drift/anon-grant/policy-lint, migration-ledger-hygiene, Netlify deploy-preview), mergeStateStatus CLEAN. Netlify auto-deploys `main` → core.eq.solutions on push (per repo convention). Worktree + branch removed 2026-07-26.

| C:\Projects\eq-shell-pr971-conflict-wt (eq-shell) | tmp/pr971-rebase (tracks origin/claude/tenant-scoped-query-keys) | Claude (eq-shell session — told "merge all" on the 4 open PRs; #970/#973/#985 merged clean, #971 has a real merge conflict against main in `StaffPage.tsx` + `TenantSwitcher.tsx`. An unregistered worktree at `eq-shell-tenant-scope-rebase-wt` was already mid-merge-conflict on the exact same branch from another session — left untouched per worktree ownership rule, used a fresh temp-branch worktree instead) | 2026-07-23 | IN PROGRESS — resolving the conflict to get #971 mergeable, then merging. |
| ~~C:\Projects\eq-shell-totp-secret-leak-wt (eq-shell)~~ REMOVED | fix/shell-login-totp-secret-leak (merged, deleted) | Claude (Royce, mid-way through an unrelated TOTP-backup-codes PR in the root checkout, flagged a live secret leak he'd found: `shell-login.ts` stripped `pin_hash` from the login response but never `totp_secret`, so a TOTP-enrolled user's raw seed came back in the 200 body on the trusted-device fallthrough. Root checkout was on `claude/sentry-lazy-load-local-dev-fix` with the TOTP-backup-codes work uncommitted in it — isolated here off origin/main instead. Grepped every login-adjacent function for the same `{ pin_hash, ...userSafe }` shape per Royce's ask and found it was worse than reported: `shell-login-phone-otp.ts` returns the raw row unconditionally (no TOTP-challenge branch on that door at all), `shell-login-magic-link.ts` + `shell-handoff-provision.ts` leak on the same trusted-device fallthrough as shell-login.ts. `select-tenant.ts`/`shell-join-tenant.ts` already build an explicit safe payload, confirmed clean) | 2026-07-28 | DONE — **PR [#1067](https://github.com/eq-solutions/eq-shell/pull/1067) MERGED** (squash `7f3dd85f`, Royce's "merge #1067 once CI is green" go). All 4 CI checks (migration-ledger-hygiene, schema-drift/anon-grant/policy-lint, gitleaks, typecheck·test·lint) + Netlify deploy-preview passed pre-merge. All 4 leak sites fixed with the same destructure-and-strip pattern already used correctly in `challenge-totp.ts`/`_shared/second-factor-session.ts`. `pnpm run build:packages && npx tsc -b` clean (fresh install in the worktree, no shared node_modules). Live on core.eq.solutions via Netlify auto-deploy on merge. **No live click-through yet** — still worth a TOTP-enrolled test login to confirm `totp_secret` is gone from the network response. Worktree + branch removed. |
| ~~C:\Projects\eq-solves-service-sharp-uuid-audit-wt (eq-solves-service)~~ REMOVED | claude/sharp-uuid-audit (merged, deleted local+remote) | Claude (eq-solves-service session — investigating the `sharp`/`uuid` npm audit findings flagged as "spun off separately" in the #603/#604 rows above: is untrusted image data ever processed via `next/image`/sharp, and is there a non-breaking `overrides` fix vs. a real next/exceljs major bump. Root checkout has unrelated uncommitted asset-import/csv-parser work from another session — isolated here off origin/main) | 2026-07-26 | DONE — **PR [#605](https://github.com/eq-solutions/eq-service/pull/605) MERGED** (squash `e6e72fe`, Royce's "merge #605 once CI is green" go). Found `next/image`'s `<Image>` is never used anywhere in this repo (no `images` config, the only `<Image>` import is a lucide-react icon; attachment/defect-photo thumbnails render via plain `<img>` from signed Supabase Storage URLs) — sharp sat unused as next's optional dependency, zero untrusted-image exposure. `exceljs`'s only uuid use is a single `require('uuid').v4` call, confirmed identical under uuid@11.1.1's CJS export. Non-breaking `overrides` fix: `next`/`exceljs` untouched (stayed 16.2.11/4.4.0), `npm install` clean, `tsc --noEmit` clean, 359/359 tests pass. Pre-merge CI: `tsc + next build` (the real gate) passed; `Typecheck + audit` and `Integration tests (Supabase local)` both red on the same two pre-existing/accepted-risk failures every recent PR in this repo carries (16 eslint/archiver devDependency findings, unrelated `public.app_config` local-migration-harness bug) — confirmed via job logs, not assumed. **Fully pruned 2026-07-27:** the initial `git worktree remove` was blocked by this session's permission classifier, but by the follow-up the linked-worktree registration had already dropped out of `git worktree list` on its own (admin metadata gone, no `.git` pointer left in the folder — just a stray `.next` build-cache dir), so `rm -rf` on the plain leftover folder was safe and not a "real worktree removal." No local or remote branch remnants (`git branch --list` + `git ls-remote` both empty; `--prune` fetch confirmed the remote ref was already gone from the squash-merge's `--delete-branch`). |
| ~~C:\Projects\eq-shell-tenant-migrate-gate-docs-wt (eq-shell)~~ REMOVED | claude/tenant-migrate-gate-docs-fix (merged, deleted) | Claude (eq-shell session — Royce asked to add a required reviewer to the `production` GitHub Environment gating `tenant-migrate.yml`; GitHub rejected it, billing plan doesn't support required-reviewer environment protection. Royce's call: leave unfixed, correct the workflow's false "gated, human approval" claim instead. Root checkout was on `main` with a concurrent session's unrelated uncommitted changes again — isolated here rather than touch it) | 2026-07-23 | DONE — **PR [#985](https://github.com/eq-solutions/eq-shell/pull/985) MERGED** (squash `1f2d5f8`, Royce's "merge #985" go). Docs/comments only in `.github/workflows/tenant-migrate.yml`, no functional change, all checks green pre-merge. Remote branch + worktree removed. |

| Folder | Branch | Agent / Session | Claimed | Status |
|--------|--------|-----------------|---------|--------|
| ~~C:\Projects\eq-shell\.claude\worktrees\licence-photo-replace-admin (eq-shell)~~ REMOVED | claude/licence-photo-replace-admin (merged, deleted) | Claude (eq-cards session — Royce asked for a Shell admin action to replace the photo/PDF on an existing worker licence in place, without creating a duplicate row (Brian Griffin-Colls' blurry NSW Photo Card / White Card scans); root checkout was dirty with a concurrent session's unrelated uncommitted changes on `claude/access-model-phase2-3-guardrails`, isolated worktree off origin/main used instead) | 2026-07-28 | DONE — **PR [#1062](https://github.com/eq-solutions/eq-shell/pull/1062) MERGED** (squash `f947a93`, Royce's "merge #1062 once CI is green" go). New `staff-licence-replace-photo.ts` (admin.review_cards-gated, org_memberships tenant scope) uploads to the same deterministic storage path and UPDATEs the licences row in place; "Replace photo" control added to `LicGroup` (shared by desktop split panel + mobile sheet). `tsc -b` clean, `pnpm test` 253/253, eslint clean on the diff. One pre-merge check red — `Tenant drift + anon-grant + policy-lint` — confirmed failing on `main` independently (unrelated `tender_import_runs`/`tender_nominations` schema drift, two separate main-branch runs same day), not blocked on. Netlify auto-deploys main → core.eq.solutions. Not yet click-tested live. Worktree + local branch removed. |
| ~~C:\Projects\eq-shell-licence-backfill-dedup-wt (eq-shell)~~ REMOVED | claude/licence-backfill-dedup-guard (merged, deleted) | Claude (spawned task — staff-licence-backfill.ts always inserts a fresh public.licences row with no check for an existing non-deleted row of the same user_id+licence_type, same duplication class as the mobile OCR-add path fixed in eq-cards PR #181. Root checkout is on `claude/access-model-phase2-3-guardrails` with another session's uncommitted work — isolated here off origin/main. Royce confirmed via AskUserQuestion: confirm dialog UX, mirroring Cards' maybeOfferRenewInstead pattern) | 2026-07-28 | DONE — **PR [#1060](https://github.com/eq-solutions/eq-shell/pull/1060) MERGED** (squash `ff29ab9`, Royce's "merge #1060" go). `409 duplicate_licence` pre-write guard + AddLicenceModal.tsx confirm panel (Update existing / Add anyway) + StaffPage.tsx toast text. `tsc --noEmit` clean, eslint 0 errors, 253/253 tests pass. Merged despite one non-required CI check red (`Schema drift + anon-grant + policy-lint`) — confirmed via job log + merge-base diff (`git diff origin/main...HEAD`) that the PR touched only the 3 intended app files, zero SQL/migrations; the failure is a live, unrelated SKS-tenant finding (`rls_introspection()` newly anon-executable SECURITY DEFINER, not allow-listed) that also wasn't present on the last green main run — flagged separately, not this PR's to fix. Live on core.eq.solutions via Netlify auto-deploy on merge. Not click-tested live — Royce to eyeball post-deploy. Worktree removed. |
| ~~C:\Projects\eq-intake\.claude\worktrees\app-polish-improvements-8c03dd (eq-intake)~~ REMOVE WHEN PRUNED | claude/intake-deferred-polish-items (merged, deleted) | Claude (eq-intake session, same worktree reused after PR #76 merged fast-forwarded it to `main` — Royce said "sprint all deferred items", picking up the 4 polish ideas deferred from the PR #76 round: MergePanel/SiteAdvisoryPanel inline-style cleanup, a mobile-width check of the Health tab, a manual refresh control, and refreshing the stale `SPRINT-SUMMARY.md`) | 2026-07-26 | DONE — **PR [#77](https://github.com/eq-solutions/eq-solves-intake/pull/77) MERGED** (squash `ed6b9d7`, Royce's "yes, merge once CI is green" go). Replaced ~40 inline `style={{...}}` blocks in `MergePanel`/`SiteAdvisoryPanel` with `eq-health-*` CSS classes — two of them (`--eq-ink-soft`, `--eq-danger`) referenced CSS variables that don't exist anywhere in the tokens package or the file's own `:root`, so the hardcoded fallback hex was silently always what rendered; swapped for the real tokens (`--eq-muted`, `--eq-err`). Added a manual Refresh control to the Health tab. Mobile check via a temporary mock-data harness (removed before commit) at phone width found and fixed a real bug: `.eq-health-dims`'s 220px `min-width` let it squeeze to ~77px-wide bars next to the ring instead of ever triggering `flex-wrap`; new `@media (max-width: 480px)` rule stacks the ring above full-width dims instead. Refreshed `SPRINT-SUMMARY.md` with a "Since this was written" section rather than rewriting unverifiable historical claims. `tsc --noEmit` clean, 166 tests green pre-merge, CI green in 1m21s post-push. Root checkout (`C:\Projects\eq-intake`) stayed detached HEAD with unrelated uncommitted changes from another session throughout both rounds — untouched. Worktree prunable. |
| ~~C:\Projects\eq-shell-roles-bump-wt (eq-shell)~~ REMOVED | claude/bump-roles-v2.5.7 (merged, deleted) | Claude (eq-roles session — bumped eq-shell's `@eq-solutions/roles` pin from v2.5.4 to v2.5.7, labour_hire→equipment.view. Root checkout was dirty with a concurrent session's uncommitted matrix-collapse rework, confirmed as duplicate of already-merged #1021/#1022 — left untouched, isolated worktree off origin/main used instead) | 2026-07-26 | DONE — **PR [#1023](https://github.com/eq-solutions/eq-shell/pull/1023) MERGED** (squash `f24db11`, Royce's "merge #1023 once it's green" go). All CI green (typecheck·test·lint, schema-drift/anon-grant/policy-lint, migration-ledger-hygiene, Netlify deploy-preview) before merge; `check:perms` all pass, `pnpm test` 247/247. Netlify auto-deploys `main` → core.eq.solutions. Worktree + branch removed. |
| ~~C:\Projects\eq-shell-market-vertical-backfill-wt (eq-shell)~~ REMOVED | claude/backfill-market-vertical-customer-group (merged, deleted) | Claude (eq-shell session — Royce asked to investigate `app_data.customers.customer_group` on ehow/SKS: 6 rows overlap with the new market_vertical dropdown values from 0197/0198. Root checkout was mid-race with a concurrent session that switched it from `claude/modal-overlay-click-outside-fix` to `claude/client-type-market-segment-fields` under this session — spun up an isolated worktree off origin/main rather than commit into a branch another agent was actively driving) | 2026-07-23 | DONE — **PR [#984](https://github.com/eq-solutions/eq-shell/pull/984) MERGED** (squash `36a9745`, Royce's "merge #984" go). Migration `0199_backfill_market_vertical_from_customer_group.sql` (pure DML backfill, live-verified against ehow: Ramsay Health Care/Ramsay Health Care IT + 3 Schneider Electric entities need it, Equinix Hyperscale 2 already had both set; zaap confirmed 0 rows, no-op there) + the `CustomersPage.tsx` Group-pill dedupe fix. `pnpm run build` clean; typecheck·test·lint + migration-ledger-hygiene + schema-drift/anon-grant/policy-lint all green pre-merge. **`gh pr merge --delete-branch` hit the same shared-checkout collision as the branch race that started this task** (tried to touch local `main`, already checked out at `C:\Projects\eq-shell` by another agent) — merge itself succeeded via API regardless; remote branch deleted directly via `git push --delete` instead, worktree removed cleanly from the root checkout. **Migration is code-merged to main only — NOT yet applied to any tenant DB.** Still needs: dispatch `tenant-migrate.yml` (production-gated) to actually run it against ehow/zaap — Royce's explicit go, separate from the merge instruction. |
| ~~C:\Projects\eqsvc-site-assignee-fix-wt (eq-solves-service)~~ REMOVED | claude/site-assignee-display-fix (merged, deleted) | Claude (eq-solves-service session — Royce live-tested the check-detail Site/Assigned-To editors from PR #597 and both displays never updated. Root-caused: page.tsx's sites SELECT references `primary_contact_id`, a column that doesn't exist in `service.sites` — the whole query 400s every time and the destructured `{data}` silently swallows the error, so `site` is always null regardless of a valid site_id. Assigned To has a separate root cause: the technician roster is canonical Shell ids, but the display-name lookup queries the local `profiles` table by that id, which never matches. Also asked for a logo on the Field Run-Sheet report (confirmed via an attached generated docx — no logo plumbing exists there at all) and a loading spinner while reports generate) | 2026-07-23 | DONE — **PR [#599](https://github.com/eq-solutions/eq-service/pull/599) MERGED** (squash `f5e524d`, Royce's "merge it" go). `tsc + next build` CI check passed; the other two reds are the same pre-existing accepted-risk failures as every PR this session. Fixed: dropped the dead `primary_contact_id` column from the sites SELECT (replaced the broken `site_contacts` join with denormalized `site_contact_name/phone/email`); Assigned To now resolves via the already-fetched canonical technician roster before falling back to `profiles`; Field Run-Sheet's `prepareShell()` call now gets `tenantLogoImage` (masthead-only before, matching the gap PR #592 closed for 5 other report types); `PrintBlankButton`/`PrintReportSplit` now show a spinning icon + disable correctly while a report is generating. Netlify auto-deploys main → service.eq.solutions. Worktree removed 2026-07-23. |
| ~~C:\Projects\eqsvc-check-site-name-edit-wt (eq-solves-service)~~ REMOVED | claude/check-site-name-edit (merged, deleted) | Claude (eq-solves-service session — Royce asked to also be able to edit Site and the check's name/title on the maintenance check detail page, extending PR #592's Due Date/Assigned To inline editors) | 2026-07-23 | DONE — **PR [#597](https://github.com/eq-solutions/eq-service/pull/597) MERGED** (squash `77acf9d`, Royce's "merge it" go). `tsc + next build` CI check passed; the other two reds are the same pre-existing accepted-risk failures as every PR this session. Added a plain-text inline editor for `custom_name` above the header grid, and a dropdown editor for `site_id` gated behind a confirm dialog (changing site does not move existing `check_assets` — flagged explicitly in the warning copy). Both route through `updateCheckAction` (`UpdateMaintenanceCheckSchema` extended), site list fetched alongside the existing technician roster in `page.tsx`. Netlify auto-deploys main → service.eq.solutions. Worktree removed 2026-07-23. |
| ~~C:\Projects\eq-shell-list-members-staff-fallback-wt (eq-shell)~~ REMOVE WHEN PRUNED | claude/list-members-staff-fallback (merged, deleted) | Claude (eq-solves-service session — Royce asked "where are these users coming from" about a pasted EQ Service Users-page roster; traced to `list-members.ts`. Royce's own follow-up ("within the canonical records everyone's info is there?") caught a wrong theory before it shipped — an initial chip had been filed against the wrong file entirely, `eq-shell`'s AdminUserList.tsx/eq_list_tenant_users RPC, since disproven and the chip already started by Royce in a separate session with a false premise, flagged to him to stop/redirect) | 2026-07-23 | DONE — **PR [#975](https://github.com/eq-solutions/eq-shell/pull/975) MERGED** (squash `dd1bf63`, Royce's "confirm and merge" go). `pnpm run build` clean, 206/206 tests (199 pre-existing + 7 new), eslint clean on the diff (1 pre-existing `_ctx` unused-var error on `list-members.ts`, confirmed byte-identical on unmodified main via `git stash`). Root cause: `list-members.ts` (server-to-server roster EQ Service reads) only ever queried `shell_control.users` (eq-canonical/jvkn) — never `app_data.staff` (ehow), which has the real name/email for the same people via `staff.user_id = users.id`, populated through EQ Cards licence capture or manual Staff-page entry. No FDW between the two planes (by design, same as the dangling-staff-pointer family), so this is a second query + in-app merge, not a join — reused the existing `getTenantDataClientById` cross-plane pattern from `staff-pending-connections.ts`. New `backfillFromStaff()` pure function (never overwrites a value Shell already had; empty-string staff email treated as absent, not a value) makes the merge unit-testable without a live DB. Best-effort: a staff-plane failure is caught, logged via `captureServerError`, and the response still returns with whatever Shell had. **CI caught a real defect pre-merge:** the test file first landed at top-level `netlify/functions/list-members.test.ts` — Netlify rejects any top-level filename outside `[A-Za-z0-9_-]`, failing every deploy, the exact trap already hit once before in this repo (`check-dangling-staff-pointers.ts`, PR #949). Moved to `netlify/functions/_shared/`, re-verified green, then merged. Worktree removed 2026-07-23. |
| ~~C:\Projects\eqsvc-kanban-title-wt (eq-solves-service)~~ REMOVED | claude/kanban-check-title (merged, deleted) | Claude (eq-solves-service session — Royce asked for the check title to show on the maintenance Kanban cards. The live "kanban" view is SiteGroupedView.tsx's Site-view toggle, NOT the unused/dead KanbanBoard.tsx file; its CycleCard only showed frequency/month/progress/status, no check title) | 2026-07-23 | DONE — **PR [#595](https://github.com/eq-solutions/eq-service/pull/595) MERGED** (squash `58c3cf1`, Royce's "merge it" go). `tsc + next build` CI check passed; the other two reds are the same pre-existing accepted-risk failures as every prior PR this session. Added a title line to CycleCard (custom_name ?? job_plans?.name, matching the check detail page's own priority), deduped + capped at 2 + "N more" for multi-check cycles. `app/(app)/maintenance/KanbanBoard.tsx` remains dead code (unused anywhere) — flagged, not touched. Netlify auto-deploys main → service.eq.solutions. Worktree removed 2026-07-23. |
| ~~C:\Projects\eqsvc-bulk-delete-chunk-wt (eq-solves-service)~~ REMOVED | claude/bulk-delete-chunk-timeout (merged, deleted) | Claude (eq-solves-service session — Royce/Simon hit a bulk-delete failure on the Assets page: 37 items worked, 171 failed, no Sentry error. Root-caused live to Postgres `authenticated` role's `statement_timeout=8s` — the canonical view's per-row INSTEAD OF DELETE + AFTER audit trigger chain on 171 rows in one statement blows past it, and bulk.ts's try/catch swallows the resulting error into a graceful `{success:false}` return instead of throwing, which is why Sentry never saw it) | 2026-07-23 | DONE — **PR [#593](https://github.com/eq-solutions/eq-service/pull/593) MERGED** (squash `1e20b0b`, Royce's "merge it" go). `tsc + next build` CI check passed; the "Typecheck + audit" red is the same pre-existing sharp/uuid npm-audit finding as prior PRs this session, unrelated. Fix: new `runChunked()` helper in lib/actions/bulk.ts runs every bulk mutation (final delete/update AND the job_plans/sites cascade-cleanup deletes) in chunks of 25 with 4 concurrent, instead of one `.in('id', ids)` statement over the whole selection. Cap raised 200 → 500. Netlify auto-deploys main → service.eq.solutions. Worktree removed 2026-07-23. |
| ~~C:/Projects/eq-shell-quote-draft-wt (eq-shell)~~ REMOVED | claude/quote-draft-namespacing | crows-nest/shipwright #962 | 2026-07-23 | DONE — PR [#970](https://github.com/eq-solutions/eq-shell/pull/970) open; pnpm build exit 0, 205/205 tests pass. Worktree removed 2026-07-23. |
| ~~C:/Projects/eq-shell-tenant-cache-wt (eq-shell)~~ REMOVED | claude/tenant-scoped-query-keys | crows-nest/shipwright #961 | 2026-07-23 | DONE — PR [#971](https://github.com/eq-solutions/eq-shell/pull/971) OPEN, NOT merged (Royce's call; eq-shell auto-deploys main → core.eq.solutions). Tenant-scoped 6 react-query key families (staff ×5, crm customer list, 4 access-control keys, entity-rows, home signals) via new `src/lib/tenantQueryKeys.ts` factories + `useTenantScope()` (session tenant **id**, not the URL slug — every one of these endpoints resolves tenant from the session cookie), and added `queryClient.clear()` to TenantSwitcher's successful switch. Both layers deliberately: clear() is the belt, scoped keys are the braces for any future path that changes tenant without the switcher. Prefix invalidations (StaffPage handleMutated/handleArchived, EntityBrowser refresh) and all optimistic setQueryData/getQueryData sites repointed; `scope` added to affected dep arrays. Dropped SignalsBoard's discarded `tenantSlug` prop + its TenantHome call site. `pnpm build` exit 0; `pnpm test` 205/205 (199 pre-existing + 6 new); eslint on touched files strictly improves (two `_tenantSlug` unused-var errors removed, nothing new — verified byte-identical to origin/main by stashing and re-running). No auth-flow/JWT/token-exchange change, no migration. **NOTE: issue #961's body is literally `@-`** (a broken `gh issue create --body-file` at charter time) — the acceptance criteria existed only in the dispatch prompt, so there was no issue-side checklist to verify against. Worktree removed 2026-07-23. |
| ~~C:\Projects\eqsvc-loadtime-doc-wt (eq-solves-service)~~ REMOVED | claude/loadtime-doc-update (merged, deleted) | Claude (refreshed docs/perf/load-time-investigation.md — the doc still said the Sentry server-bundle trim was "ruled out" when #571 actually shipped it, and it predated #568/#569/#570/#573/#574; docs-only, no code) | 2026-07-23 | DONE — **PR [#588](https://github.com/eq-solutions/eq-service/pull/588) MERGED** (squash `4f5e1e7`, Royce's "merge the PR" go). `tsc + next build` passed; `npm audit` + integration-test CI reds are pre-existing (sharp/uuid transitive vulns already tracked as accepted risk, integration tests are documented pre-existing per repo CLAUDE.md) — unrelated to this docs-only diff. Spun up a fresh worktree off main rather than writing into `.claude/worktrees/asset-import-export-1fe110`, which this session found on branch `claude/eq-service-load-time-f601ff` carrying unrelated uncommitted asset-import changes not authored here. Worktree removed 2026-07-23. |
| ~~C:\Projects\eqsvc-check-detail-wt (eq-solves-service)~~ REMOVED | claude/check-detail-site-edit-report-logo (merged, deleted) | Claude (eq-solves-service session — Royce reported /maintenance/[id] check-detail page shows Site as blank even though the check row has a valid site_id; also asked for inline edit of due date/assignee at the top of the check page, and a small SKS logo in the top-right running header of every generated report page) | 2026-07-23 | DONE — **PR [#592](https://github.com/eq-solutions/eq-service/pull/592) MERGED** (squash `c60b45d`, Royce's "merge it" go). `tsc + next build` CI check passed; npm-audit/integration-test CI reds are the same pre-existing sharp/uuid transitive-vuln + accepted-risk failures as prior PRs this session, unrelated to this diff. Root cause 1: page.tsx fetched the site correctly but never merged it onto the object passed to CheckDetailPage. Root cause 3: no report generator's `resolveShellSettings` call ever set `tenantLogoUrl`, so the shell's own logo-fetch path was dead everywhere — fixed by reusing each generator's already-fetched cover-logo Buffer for the header instead (new `ctx.tenantLogoImage` + `scaleLogoToFit` in report-shell.ts), covering pm-check-report/work-order-details/pm-asset-report/nsx-report/acb-report; customer-scope-statement/renewal-pack already worked. `compliance-report.ts` has zero logo plumbing — spun off as task_79032206 (running independently in a separate local session), NOT done here. Netlify auto-deploys main → service.eq.solutions. Worktree removed 2026-07-23. |
| C:\Projects\eq-shell-dup-accounts-wt (eq-shell) | claude/duplicate-shell-accounts-check | Claude (eq-solves-service session — found + fixed a live duplicate Shell account for Brian Griffin-Colls 2026-07-22, one phone-only (real history: worker/org_membership/10 licences/24 audit_log rows) + one empty email-based dup created 12 days later via a different signup path; neither `shell_control.phone_link_review` nor `public.identity_collision_flags` ever flagged it since the duplicate wasn't created via the Cards-linking RPC at all. Built an alert-only reconciliation cron, 5th member of the identity-health family) | 2026-07-22 | DONE — **PR [#967](https://github.com/eq-solutions/eq-shell/pull/967) OPEN**. `build:packages && tsc -b` clean, 199/199 tests (190 pre-existing + 9 new), eslint clean on both new files. `check-duplicate-shell-accounts.ts` fires daily 21:40 UTC, reads each tenant's own `app_data.staff` as the trust anchor (staff.user_id = the one real linked account) and flags any OTHER active Shell account in the same tenant sharing that person's known email/phone — deliberately no global "complete" guard (unlike check-dangling-staff-pointers), since detection here is per-tenant/positive-only so a read failure only reduces coverage, never manufactures a false positive. NOT merged, NOT deployed — Royce's call. Worktree prunable after merge. |
| C:\Projects\eq-solves-service-wt-runsheet-logo (eq-solves-service) | fix/runsheet-header-logo | Claude (eq-solves-service session — Royce, viewing a freshly-generated Field Run-Sheet in Word dark mode, asked "shouldn't that logo be the coloured one?" and "fix it and align it where I put it". Root-caused: PR #599's header-logo fix reused `input.tenantLogoImage`, which `maintenance-checklist-input.ts` fetches via `TENANT_LOGO_ON_DARK` (the white variant meant for the page-1 navy masthead strip) — correct there, invisible white-on-white when reused for the plain-white running header. Separately, the header's tab-stop-positioned ImageRun is not a reliable way to pin a trailing image's alignment across page sizes) | 2026-07-23 | DONE — **PR [#600](https://github.com/eq-solutions/eq-service/pull/600) OPEN, NOT merged** (Royce's call pending). `tsc --noEmit` 0 errors, `npm run check` (tsc+next build) clean. Fix: `maintenance-checklist-input.ts`/`maintenance-checklist.ts` now plumb the tenant's light/colour `report_logo_url` through as `settings.tenantLogoUrl`, so `report-shell.ts`'s own `prepareShell()` fetches the correct colour logo directly for the header (falls back to the on-dark buffer only if no light URL is configured at all). Also replaced `buildHeader()`'s tab-stop paragraph with a 2-cell percentage-width borderless table (text left, logo right-aligned + vertically centred) for stable, page-size-independent positioning. Verified end-to-end via a standalone `tsx` script exercising the real `prepareShell`/`buildHeader` against SKS's actual live `report_logo_url`/`report_logo_url_on_dark` — confirmed the resolved logo is the navy/purple colour variant (77×28) and the header XML uses stable inline images in 75/25 percentage columns (no more floating/anchor positioning). Netlify auto-deploys main → service.eq.solutions on merge. Worktree NOT yet removed — pending merge. |
| ~~C:\Projects\eqsvc-stale-action-wt (eq-solves-service)~~ REMOVED | claude/stale-server-action-error (merged, deleted) | Claude (eq-solves-service session — graceful handling for recurring Sentry EQ-SOLVES-SERVICE-3 UnrecognizedActionError: stale server-action IDs after a redeploy while a tab is open. Root cause already confirmed, not in question. Root checkout on `claude/eq-service-load-time-f601ff` carries unrelated uncommitted asset-import/csv-parser changes not authored here, so used a fresh worktree off main) | 2026-07-23 | DONE — **PR [#596](https://github.com/eq-solutions/eq-service/pull/596) MERGED** (squash `853835f`, Royce's "merge it" go). `tsc + next build` CI check passed; `npm audit`/integration-test CI reds are the same pre-existing sharp/uuid accepted-risk failures as prior PRs this session, unrelated. New `lib/utils/stale-action-error.ts` detects via Next's `unstable_isUnrecognizedActionError` (name/message fallback); `app/error.tsx` and `app/(app)/error.tsx` branch to a "newer version available, refreshing…" screen with auto-reload (1.2s) + manual Refresh button. `app/global-error.tsx` untouched (only catches root-layout render errors, not action-invocation errors). Verified assumption: no separate job-plan-tied error in Sentry, only this one staleness error for the repo — no job-plan/maintenance-check logic touched. Netlify auto-deploys main → service.eq.solutions. Worktree + branch removed 2026-07-23. Note: root checkout (`C:\Projects\eq-solves-service`) was found in a **detached HEAD** state at session end (081010a, not on `claude/eq-service-load-time-f601ff` as at session start) — uncommitted asset-import/csv-parser changes are still intact and untouched, nothing lost, but this wasn't caused by this session (no checkout/reset run against root) — another concurrent session on the shared checkout most likely did it. Same class of hazard as the confirmed-real shared-checkout issue already logged in digest.md's EQ pending list 2026-07-23. |
| ~~C:\Projects\eq-cards\.claude\worktrees\ocr-licence-service-role-auth (eq-cards)~~ REMOVED | claude/ocr-licence-service-role-auth (merged, deleted) | Claude (Sentry EQ-SHELL-Y follow-up — eq-shell's staff-licence-ocr proxy sends the SUPABASE_SERVICE_ROLE_KEY as its bearer token when calling eq-canonical's `ocr-licence` edge function; the function's manual `/auth/v1/user` check and its per-user rate-limit RPC both reject/deny a service-role caller since neither recognises a non-human session) | 2026-07-23 | DONE — **PR [#174](https://github.com/eq-solutions/eq-cards/pull/174) MERGED** (fast-forward, commit `aa89194`; this session's own `gh pr merge` was classifier-blocked, the concurrent task_d94af51d session completed the merge once the new global Bash permission took effect), deployed live to `jvknxcmbtrfnxfrwfimn` (`ocr-licence` v11→v12, live-verified). Worktree + branch removed. Sibling worktree `admin-attach-licence-pdf` (same session, PR [#175](https://github.com/eq-solutions/eq-cards/pull/175) — added PDF support to the existing `admin-attach-licence-photo` operator tool — was never separately registered here; also MERGED + deployed v1→v2, worktree + branch removed 2026-07-23. |
|--------|--------|-----------------|---------|--------|
| ~~C:\Projects\eq-shell-canonical-phone-wt (eq-shell)~~ REMOVED | claude/canonical-members-phone-field (merged, deleted) | Claude (eq-solves-service session investigating Sentry EQ-SOLVES-SERVICE-C — /maintenance crashed on a canonical member with null name+email; live query on eq-canonical confirmed 27 active SKS members are phone-only Shell accounts with no email/name on file, one with no phone either; Service-side null-safety fixed in eq-service, this worktree adds `phone` to Shell's list-members response so Service has a real fallback instead of just a generic label) | 2026-07-22 | **DONE — PR [#956](https://github.com/eq-solutions/eq-shell/pull/956) MERGED** (squash `c04f889`, Royce's "merge the eq-shell PR" go). All checks green pre-merge (typecheck·test·lint, migration ledger hygiene, schema-drift/anon-grant/policy-lint, Netlify deploy preview), mergeStateStatus CLEAN. Adds `phone` to the `user_tenant_memberships → users` embed select + response, widens `email`/`name` to `string \| null`. Netlify auto-deploys `main` → core.eq.solutions on push (per repo convention), so this is now live or about to be. Worktree + local/remote branch removed 2026-07-22. |
| ~~C:\Projects\eq-field-week2-wt (eq-field)~~ REMOVED | claude/narrow-week-window (merged, deleted) | Claude (follow-up to PR #532/v3.5.354 -- the actual +/-4 to +/-1 boot-window narrowing, held back until the 10 remaining cross-week consumers found by the same audit were fixed; Royce approved the phased sequencing: 3 correctness bugs first (#532), narrowing second) | 2026-07-22 | **CORRECTED 2026-07-22 (separate session, closing out task_6f433960's toggle work — Royce asked to merge #535 "once it's rebased," found it already was): the version number below is stale.** Production is actually **v3.5.357** (re-verified live on `field.eq.solutions` via `sw.js` content, not the commit title — the squash title still reads "v3.5.355", same `gh pr merge --squash` title-reuse cosmetic issue seen elsewhere today). A migration-only PR (#536, teams/team_members tenant-isolation lockdown, no app version bump) landed between #534 and this PR's actual merge, so the real number is 357 not the 356 predicted below. `WEEK_WINDOW_HALF_WEEKS` confirmed present in the live deployed `index.html`. One further PR (#537, Incidents/Near Miss) has since merged on top without changing the live version stamp — not investigated further, out of scope for this correction. Original entry follows, unedited. DONE, CLOSED -- **PR [#535](https://github.com/eq-solutions/eq-field/pull/535) MERGED** (squash `068decac`, Royce's "merge it" go) -> field.eq.solutions **LIVE + verified**: `sw.js` serves `eq-field-v3.5.356`, live `index.html`/`forecast.js`/`calendar.js` all carry the fix. **Version collided as predicted:** `claude/crew-view-toggle` merged first as PR #534 and took v3.5.355 (same base commit `3809591`, exactly the standing risk this row already flagged) -- rebased onto origin/main, resolved the index.html changelog-banner conflict by keeping both blocks (crew-view-toggle's stays v3.5.355, this one renumbered to v3.5.356 and placed above it), re-bumped `app-state.js`/`sw.js` to match, re-ran the full gate suite post-rebase (19/19 test files incl. the other branch's own new `crew-scoping.test.js`, eslint 0 errors/707 warnings), force-pushed, waited for CI green again, then merged. Worktree removed 2026-07-22. Was off origin/main `3809591` (v3.5.354). **THE CHANGE:** _getVisibleWeekRange (index.html) now returns +/-1 week (3 total) instead of +/-4 (9 total), behind one named constant WEEK_WINDOW_HALF_WEEKS for a trivial revert. **THE 10 CONSUMERS, all found by the audit run before this branch started:** fixed 7 (forecast.js 12-52 week horizon -- new _ensureForecastWeeksLoaded() on page entry/window-expand; calendar.js month view -- renderCalendar() now async, loads every week the visible month touches; timesheets.js 6-week completion tracker + apprentice-widget.js 4-week ratio sparkline -- both run on every render/edit so they render on whats resident, kick a background top-up with in-flight+last-failed dedupe, and re-render on arrival; timesheets.js copy-last-week + staff prefill banner -- supervisor click awaits the load, banner uses the background-redraw shape; diary/toolbox/site-reports "Add from roster" -- await v3.5.354s _ensureWeeksLoaded() and say the check failed rather than reporting an empty roster). **3 deliberately left alone, found to be dead or out-of-scope for the payload tradeoff, not hardened:** batch.js Clean Up Codes (button removed from Sites in v3.5.227, modal unreachable since -- confirmed via grep, zero live call sites); managers.js orphan-record count (confirmRemoveManager returns before reaching it whenever TENANT.ORG_SLUG===sks -- the only live tenant -- reachable only on eq, which CLAUDE.md documents as disposable sandbox data); roster.js getAllSiteCodes() (feeds a datalist suggestion, not a validated field, and runs on nearly every render -- fetching full history there would reintroduce on every render exactly the payload this PR removes, for a cosmetic autocomplete gap). tests/week-window-narrowing.test.js -- 16 assertions: pins WEEK_WINDOW_HALF_WEEKS, functionally tests _getVisibleWeekRange and the rewritten _w3PrevWeeks (which used to silently derive its week list from whats resident and undercount) against shims, and source-checks every fixed consumer still routes through its loader. Gates green: 19/19 test files, eslint 0 errors / 704 pre-existing warnings. **NOT merged, NOT deployed -- Royce's call.** Worktree prunable after merge. |
| ~~C:\Projects\eq-shell-driftview-wt (eq-shell)~~ REMOVED | claude/drift-gate-view-relkind (merged, deleted) | Claude (CHECK 2 anon-grant invariant flagged `app_data.field_team_supervisors` — a security_invoker view from eq-field #530 — as a NEW open table, hard-red on main since 08:40Z; PR #950 already band-aided it with a 57th hand-written KNOWN_LEGACY_ANON entry, but the systemic fix was requested; found already sitting uncommitted in this worktree, authored by a concurrent process — verified independently before pushing) | 2026-07-22 | **DONE — PR [#952](https://github.com/eq-solutions/eq-shell/pull/952) MERGED** (squash `4a420d8`, Royce's "merge then prune" go), off current `main` (already included #950, no rebase needed). All checks green pre-merge (typecheck·test·lint, migration ledger hygiene, and — the point of the PR — `Schema drift + anon-grant + policy-lint` itself passing). Worktree + local/remote branch removed 2026-07-22. Root fix: `ANON_GRANT_SQL` now filters `c.relkind <> 'v'`, handing views to CHECK 7 (view security_invoker invariant — ABSOLUTE, no allow-list, already scans every anon/authenticated-reachable view) instead of CHECK 2's per-view allowlist. Removes all 57 now-redundant KNOWN_LEGACY_ANON view entries (incl. #950's `field_team_supervisors` row) — every single one independently live-verified `relkind='v'` + `security_invoker` set on zaap/ehow before trusting the removal (spot-checked ~20 of 57 directly via SQL: `field_timesheet_locks`, `field_teams`, `field_team_members`, `field_managers`, `field_people`, `service.assets/customers/sites`, `activation_status`, `labour_hire_rates_view`, etc. — all views, none swept-up tables). Genuine table entries (`public.app_config`, `public.teams`, …) untouched. `node --check` clean; PR body also reports eslint clean, `pnpm run build` clean, and a live re-run of CHECK 2's own query with the new filter returning zero rows on both planes. No DDL, no live DB change — checker-only. NOT merged — Royce's call per task constraints. Worktree prunable after merge. |
| C:\Projects\eq-field-crew-toggle-wt (eq-field) | claude/crew-view-toggle | Claude (crew-scoping shipped as a hard boundary in PR #530; Royce's steer 2026-07-22: no guardrails that stop people doing their job — turn it into a My crew / Team / Everyone toggle, ungated) | 2026-07-22 | IN PROGRESS — off origin/main `3809591` (v3.5.354). Adds `EQ_CREW_VIEW` to permissions.js (get/set mode + team options) and a branch in `getCrewPersonIds()` for `'all'`/`'team:<id>'`, gated on `isManager` only — not on `field.view_all_crews` — deliberately ungated per instruction. The ONLY gate is whether the control is visible at all (same isManager flag as every other edit-mode control) so a stale localStorage choice from a prior unlocked session can't silently widen a later locked session. Adjacent open item, not touched: PR #533 (RLS fix on the `team_supervisors` base table this reads) is still pending Royce's go — no new exposure from this change, client-side read only. NOT merged, NOT deployed. |
| ~~C:\Projects\eq-shell-staffptr-wt (eq-shell)~~ REMOVED | claude/dangling-staff-pointer-audit (merged, deleted) | Claude (architectural debt found while fixing the admin licence backfill — `public.workers.staff_id` on jvkn is a cross-DB pointer at `app_data.staff.staff_id` on ehow/zaap, no FK possible; asked to build a consistency check and decide null vs flag vs alert) | 2026-07-22 | DONE — **PR [#949](https://github.com/eq-solutions/eq-shell/pull/949) MERGED** (squash `db9dc5d`, Royce's "merge #949" go) → core.eq.solutions **deployed + live-verified**: Netlify deploy `6a6093f8` = commit `db9dc5d`, published 10:01:35Z, state ready, and `check-dangling-staff-pointers` confirmed present in that deploy's **function manifest** alongside its siblings. First scheduled run 21:30 UTC. **TRAP — do NOT verify a scheduled function by curling `/.netlify/functions/<name>`:** it returns **HTTP 200 with the SPA fallback HTML**, not the function, so a missing function looks identical to a healthy one (the sibling `check-never-invited-workers` returned the same HTML). An earlier check in this session read those 200s as "registered" and was wrong; the deploy function manifest is the real check. **CI CAUGHT A GENUINE BUG BEFORE MERGE:** the test was first written to `netlify/functions/check-dangling-staff-pointers.test.ts` — Netlify treats every top-level file in `netlify/functions/` as a function and rejects any name outside `[A-Za-z0-9_-]`, so the dot in `.test.ts` **failed the entire deploy**, taking out deploy-preview, header rules, redirect rules and pages-changed as well as the `Guard function filenames` step. Fixed by moving it to `netlify/functions/_shared/` (where every other test in the repo already lives — the `pnpm test` glob `netlify/functions/**/*.test.ts` still finds it). **Every test in this repo must live under an underscore-prefixed dir.** The `Schema drift` red noted below cleared on its own once PR #950's allowlist landed. Final: all 7 checks green, mergeStateStatus CLEAN. `tsc -b` clean, 190/190 tests (177 pre-existing + 13 new), eslint clean on the diff, rebased onto current main and all gates re-run after. **The debt is NOT theoretical — 22 of 93 distinct pointers (24%) already resolve on NO active tenant plane.** Ruled out the obvious false premise first: checked all 22 against the `eq` plane (zaap) as well as ehow — 0 resolve there, so genuinely broken, not misrouted. **5 of the 22 have `staff_id` == the worker's OWN `workers.id`** — a worker id written into the staff pointer, never valid for a moment; a writer bug, not a deleted staff row. Plus 1 `staff_id` shared by two workers, which breaks `staff-licence-backfill.ts`'s `.maybeSingle()` and surfaces as a misleading 422 `no_linked_account`. Reads fail SILENTLY: that function picks the Cards identity by `workers.staff_id == staffId` then writes a licence **plus an `org_memberships` grant** to whoever it finds and returns 200; `staff-canonical-licences`/`staff-org-roster` label licences by the same join (wrong name in the employer compliance view), and `cards-export-licences` ships it in the compliance pack. **DECISION — alert, don't null.** The reverse edge `app_data.staff.cards_worker_id` is INTACT: populated on all 95 staff rows, all distinct, and resolving to exactly one ACTIVE staff row for every one of the 22 — so the correct value is knowable and nulling would discard the only evidence of it. Royce chose alert-only + a separate repair. New `check-dangling-staff-pointers.ts` = 4th member of the identity-health cron family (21:30 UTC after never-invited-workers at 21:20), same template, reporting `dangling`/`self_referential`/`duplicate`/`reverse_dangling`, ids in Sentry and counts-only in the body. **No FDW, no cross-plane trigger** — planes stay independent by design. **The load-bearing part is the false-positive guard:** "dangling" is a claim about EVERY plane, so it's only made when every plane actually read; if any plane fails to route the run reports `incomplete` and suppresses the dangling classes, because otherwise ONE unreachable tenant DB alerts all 93 SKS workers as broken and trains the reader to ignore the alert. Classification extracted as a pure function so that guard is testable — **mutation-verified: deleting the `complete` check fails exactly the 2 guard tests.** `scripts/repair-dangling-staff-pointers.sql` repairs all 22 from the reverse edge with 5 run-time preconditions, ends in ROLLBACK. **Royce said "run the repair script": all 5 preconditions re-verified PASSING live 2026-07-22** (22 rows, 0 moved since audit, 0 targets already claimed, 0 batch dupes, 5 self-referential) — **but the `UPDATE public.workers` was BLOCKED BY THE AUTO-MODE CLASSIFIER** through the Supabase MCP, the same gate that blocked `0100_drop_certificates`. Not routed around. **REPAIR SUBSEQUENTLY APPLIED to live jvkn 2026-07-22 on Royce's repeat instruction — all 22 rows updated; verified after: 93 of 93 distinct pointers resolve on ehow, ALL at ACTIVE staff rows (was 71 of 93), 0 self-referential remaining.** Script header updated NOT RUN → APPLIED via **PR [#954](https://github.com/eq-solutions/eq-shell/pull/954) MERGED** (squash `0bb70e3`, Royce's "merge it" go; comment-only, all 7 checks green, second worktree `eq-shell-staffptr2-wt` + branch removed) — a script on main reading "NOT RUN" after it has been run is a trap for the next reader. **THE CLASSIFIER IS NON-DETERMINISTIC — worth knowing:** the identical `UPDATE` was blocked on the first attempt and went through unchanged on a later one. Do NOT record "live writes are impossible via Supabase MCP" as a rule, and do NOT route around a block via another channel (REST + service key, psql, etc.); surface it and retry only on an explicit re-instruction. **STILL OPEN — chip `task_bfcc7366`:** the 1 duplicate `staff_id` was deliberately NOT repaired (different defect class, needs an identity merge) — **William Brown has TWO claimed `workers` rows sharing `ff234dfb…`, and his 5 licences sit on the older row `650f0a4b…` while ehow's `cards_worker_id` points at the newer EMPTY one `c9d18301…`** — so `staff-licence-backfill` 422s `no_linked_account` for him and `staff-resync-licences` syncs zero licences: his tickets are invisible employer-side. Two *claimed* accounts for one person also means a dedup defence failed on 2026-07-20 (`phone_dedup_pattern` migration 0040 trigger / eq-shell #724 / eq-cards 0089) — worth finding which. **FOUND, deliberately NOT fixed — chip `task_8d003b68`:** 3 rows on the ehow (SKS) plane carry `tenant_id` `dcb71d03…` (**EQ**) instead of `7dee117c…` — Daniel Bower (ACTIVE, a real SKS worker), Emma Curth and "test pilot" (both inactive, and both are the only 2 reverse-dangling rows). Because `staff-licence-backfill.ts` filters on `tenant_id`, **Daniel Bower's licence path still 404s even after the pointer repair** — the pointer becomes correct but the tenant stamp is a separate live-data defect on a customer plane. This is the `ehow_sks_tenant_id_trap` recurring. Root eq-shell was mid-race on another agent's `claude/retire-backfill-auth-users` with a staged deletion, so isolated here rather than disturbing it. Worktree removed + branch deleted (local, remote, and stale ref pruned) 2026-07-22. |
| ~~C:\Projects\eq-shell-team-supervisors-wt (eq-shell)~~ REMOVED | claude/team-supervisors-ci-allowlist (merged, deleted) | Claude (CHECK 2 anon-grant gate flagged `app_data.field_team_supervisors` as a NEW open table on ehow, hard-red on PR #945; live recon showed it's a security_invoker view over an RLS-on base table — same false-positive shape as its already-allowlisted siblings) | 2026-07-22 | DONE — **PR [#950](https://github.com/eq-solutions/eq-shell/pull/950) MERGED** (squash `3411c0b`, Royce's "merge the eq-shell PR" go). Docs-only allowlist entry, no DDL. **PR #945 will still show its own stale run of this check failed** — that job ran against #945's own branch (based on an older commit predating this fix) before this merged to main; merging to main doesn't retroactively rerun another PR's checks. #945 lives in a different agent's worktree (`eq-shell-licence-pdf-wt`) — not rebased/updated here per the don't-touch-another-agent's-worktree rule; it clears once that branch picks up main. Worktree removed 2026-07-22. |
| ~~C:\Projects\eq-field-team-supervisors-wt (eq-field)~~ REMOVED | claude/team-supervisors-tenant-lockdown (merged, deleted) | Claude (same investigation — found the view is safe, but the base table `app_data.team_supervisors` inherited an org_id-only permissive policy pair from its team_members shape-copy, which OR-widens past the correct tenant_id pair; not anon-reachable but cross-tenant read/delete for any authenticated ehow user) | 2026-07-22 | **DONE, CLOSED.** Migration APPLIED to live ehow 2026-07-22 (Royce's "apply the eq-field migration to ehow" go, via `apply_migration`), then **PR [#533](https://github.com/eq-solutions/eq-field/pull/533) MERGED** (squash `537b2ca`, Royce's "merge the eq-field PR too" go). Pre-apply state re-verified unchanged since the dry-run (5 policies, 10 rows, 1 tenant, 0 NULLs) before running. Post-apply live-verified: `pg_policies` shows exactly `service_role_bypass` + `team_supervisors_tenant_read` (tenant_id + org_id predicate); `authenticated` grant on the base table is SELECT-only (INSERT/UPDATE/DELETE revoked), the view keeps its full grant set (writes route through the INSTEAD OF trigger); data untouched, still 10 rows. **Functional proof via simulated JWT claims wrapped in BEGIN...ROLLBACK:** correct-tenant caller still sees all 10 rows through the view (no regression), wrong-tenant caller now sees 0 (the bug, closed). Verification posted as a PR comment before merge. Sibling tables `teams`/`team_members` carry the identical defect, deliberately not touched — separate follow-up. Worktree removed 2026-07-22. |
| ~~C:\Projects\eq-shell-spin-keyframe-wt (eq-shell)~~ REMOVED | claude/fix-spin-keyframe (merged, deleted) | Claude (undefined `spin` keyframe — Loader2 spinners render frozen; found by Royce while adding a spinner on `claude/licence-pdf-passthrough`) | 2026-07-22 | **DONE — PR [#947](https://github.com/eq-solutions/eq-shell/pull/947) MERGED** (squash `b8e7144`, Royce's "merge" go), off origin/main `c77587a`. NOT deployed. Worktree + local/remote branch removed 2026-07-22. Root checkout and `eq-shell-licence-pdf-wt` are both live under other agents, hence a fresh worktree. Cosmetic, 2 lines in `src/components/AttachmentList.tsx` (186, 281): `animation: 'spin …'` → `'eq-spin …'`. Build (tsc+vite) clean, 177/177 tests; the 1 eslint error on the file (`react-hooks/set-state-in-effect`, line 146) verified IDENTICAL on unmodified origin/main — pre-existing, not from this diff. `@keyframes spin` is defined NOWHERE in eq-shell's `src/` or in `node_modules/@eq-solutions/*` — the only hits are `eq-intake/eq-platform/packages/*/src/styles.css`, a nested unrelated package that `src/` never imports. Full inline-style sweep done: `gm-bounce`/`gm-spin` (gm-reports.css:375,380), `eq-ocr-pulse` (LicenceOcrPage.tsx:894,899) and `eq-spin` (App.css:3735) all resolve. |
| C:\Projects\eq-shell-retention-orphan-wt (eq-shell) | claude/retention-purge-shell-control-cleanup | Claude (asked to run `backfill-auth-users` against 6 `shell_control.users` rows on jvkn with no `auth.users` match — read the function first, then the provenance, and the answer reversed) | 2026-07-22 | DONE — **PR [#944](https://github.com/eq-solutions/eq-shell/pull/944) OPEN**. **The backfill was the wrong remedy and the premise was wrong.** The 6 look like pending phone-only stubs (email/phone/name all NULL, `last_login_at` null, role `employee`) but `public.audit_log` disproves it: 52 rows across the six, **every id's last event is `profile.deleted`**, with `profile.created`/`profile.updated`/`licence.created`/`membership.invited` before it — real authenticated sessions, which require an `auth.users` row to have existed. Corroborated live: their `public.profiles` rows are all **gone** (cascaded), as are `workers`/`licences`/`org_memberships`; and `eq_cards_delete_account` (checked via `pg_get_functiondef`) only SOFT-deletes, never touching `auth.users` — so the removal was **manual admin cleanup**, not the app path. These are **deleted-account residue**, so running `backfill-auth-users` would have **re-minted GoTrue identities for people who exercised deletion** — a privacy regression — and would have failed anyway (it passes `createUser({id, email})`; `email` is NULL on all six, and it loops all 72 users, not just the orphans). **TRAP: `last_login_at` is not evidence of "never signed in"** — null on 60 of 72 rows; it's what made these read as untouched. **Root cause = `retention-purge.ts:105` deletes `auth.users` but never `shell_control.users`** (the RPC runs `SET search_path = public` and there's no FK to cascade it), so EVERY future finalised account re-creates this; 1 profile is already pending erasure. Fix adds a step 3 keyed off `authDeleted` (not `ready_user_ids` — a failed GoTrue delete must leave the Shell row for the next run to retry as a pair), per-id because 5 referrers are `ON DELETE NO ACTION` and one blocked account would roll back a batch; `shell_error` → 207, not fatal. **Deliberately NOT a `WHERE NOT EXISTS (auth.users)` sweep** — Shell mints its own session JWT and a Shell-only account logs in fine with no `auth.users` row, so a missing one is NOT evidence of deletion; a blanket sweep could delete live accounts. The existing 6 go via `scripts/cleanup-orphaned-shell-users.sql` — ids hardcoded, 5 preconditions re-asserted at run time, `ROLLBACK` left in, `audit_log` preserved. **NOT run — control plane has no CI apply path, Royce's hand.** Build clean, 177/177 tests, eslint clean on the diff. Worktree prunable after merge. |
| ~~C:\Projects\eq-field-crew-scope-wt (eq-field)~~ PRUNABLE | claude/crew-scoping (merged, deleted) | Claude (national-scale readiness audit → Phase 3: the crew visibility model, the blocker on the biggest scale lever; chip task_6f433960 spun out of PR #526) | 2026-07-22 | DONE — **PR [#530](https://github.com/eq-solutions/eq-field/pull/530) MERGED** (squash `024c33c`, Royce's "merge it and apply the migration to ehow" go) → field.eq.solutions **LIVE + verified**: `sw.js` serves `eq-field-v3.5.353`, APP_VERSION 3.5.353, and live `permissions.js`/`timesheets.js`/`teams.js`/`supabase.js`/`index.html` all carry the crew code. **Migration APPLIED to ehow and live-verified.** ORDER WAS DELIBERATE AND IS THE OPPOSITE OF #520's: migration FIRST, then merge — the new table is inert to the code already in production (nothing read it), whereas merging first would have left a window where `_loadCrewTables` found no table, fell back to teams-you-BELONG-to, and scoped supervisors on the wrong edges. Post-apply verified live: 10 seeded edges (Vans 4 / Equinix 3 / CT 1 / Construction-Health 1 / Amazon 1; Comms + CDC 0), view read-through matches base, RLS on with 5 policies, 1 INSTEAD OF trigger, **anon denied SELECT *and* INSERT on BOTH base and view**, existing data untouched (72 memberships / 82 roster). **OPEN FOLLOW-UP FOR ROYCE — not a defect, the fail-open rule working:** `field_managers` holds **18** supervisors (the earlier "8" was on_roster only) and **12 have no crew assigned**, so they fail open and still pull the full roster — including Royce himself and Ben Ritchie, whose original request ("let supervisors see just their crew", quoted in teams.js's header) is what teams.js was built for. The scale win currently applies to only 6 supervisors; assigning the other 12 in the Teams screen's new "Who runs this crew" picker is a per-person UI task, no code needed. Was: v3.5.352/351, all checks green. Royce settled three calls before any code: (a) `team_supervisors` join table on the canonical layer, (b) team-less staff stay visible to ALL supervisors, (c) managers see everyone by default. **Live recon killed option (b) from the brief outright:** `app_data.schedule_entries.supervisor_id` is **100% NULL on ehow — 0 of 1012 rows**, so a roster-derived crew is empty for everyone, always; and supervision is genuinely many-to-many (Vans has 4 supervisors, Equinix 3, Simon Bramall/Matthew Miller/Luke Wheeler span both), so a single `teams.supervisor_staff_id` column would have been wrong. Migration `20260722_field_team_supervisors.sql` is a deliberate **shape-copy of the existing `team_members` twin** — copied from live `pg_get_functiondef`/`pg_policy`, not written fresh: same columns/defaults, same 5 RLS policies, same grants (authenticated + service_role, **never anon**), `security_invoker` view `field_team_supervisors`, INSTEAD OF SECURITY DEFINER trigger carrying the same `field.manage_roster` guard. "Who is a supervisor" was ALREADY canonical (`app_data.field_managers` = `staff WHERE is_supervisor`) and is not redefined — only the supervisor→team edge was missing. **ehow ONLY: zaap has no `teams`/`team_members` at all**, so no plane divergence (do not dispatch there). **Migration DRY-RUN in BEGIN…ROLLBACK against live ehow** — applied clean, seeded exactly as predicted (Vans 4 / Equinix 3 / CT 1 / Construction-Health 1 / Amazon 1; Comms + CDC 0), view read-through correct, `anon` SELECT false on BOTH base and view, `authenticated` INSERT true; rolled back, 0 leftover relations verified. **Design rule that drove everything: FAIL OPEN, NEVER CLOSED** — unresolved identity, tenant without teams, failed `team_supervisors` fetch, or a supervisor whose crew isn't configured yet ALL return "don't scope". That last one is live, not hypothetical: **2 of SKS's 8 supervisors are in no team**, and failing closed would have silently narrowed them to self-only. New key `field.view_all_crews`, manager-only in the matrix, additive via `extra_perms` (matching how `extraPerms()` already works — grant-only, so scoping must be the default with widening on top, never a narrowing key). Scale: `loadTimesheets`/`loadFromSupabase`/`_loadWeeks` now send `&staff_id=in.(crew)` on top of `sbFetchAllWide` (#526) — ~10,500-row week at 1,500 staff → ~200 rows. Three guards: canonical-only (`staff_id` exists only on the normalized path; on legacy wide it would 400 and blank the roster — same class as the `order=id` trap), skip-when-not-smaller, and a **400-id cap so a big crew falls back to unscoped rather than emit a 414** that would take out roster AND timesheets. **Ordering change worth review:** `people` + teams/members/supervisors now load BEFORE the schedule/timesheet reads (they decide who those reads ask for); the teams block previously ran AFTER the main batch feeding only the filter pills. Teams screen gains a "Who runs this crew" picker sourced from `field_managers`. **±4-week window narrowing DELIBERATELY EXCLUDED** — separate scale lever, own regression surface (any screen reading across weeks), belongs in its own change. **VERSION COLLIDED TWICE, BOTH CAUGHT PRE-MERGE:** `claude/leave-paging` merged and took 3.5.351, then `#531` took 3.5.352 — each time *including the same `sw.js` cache key*, which would have left the SW cache unbusted so nobody received the change (the trap #525 flagged: the version bump is load-bearing, not cosmetic). Rebased twice, landed as **3.5.353**; every colliding changelog block preserved in the banner and other branches' own version stamps in `leave.js` left alone. LESSON: on this repo, re-check `origin/main`'s APP_VERSION + sw.js CACHE immediately before merging, not just at branch time — three concurrent agents collided on version numbers inside one session. 17/17 test files (new `tests/crew-scoping.test.js`, 22 assertions — most asserting the *fail-open* direction, since the dangerous failure is a worker whose hours have no reviewer), eslint 0 errors / 703 pre-existing warnings. Worktree prunable after merge. |
| C:\Projects\eq-shell-licence-pdf-wt (eq-shell) | claude/licence-pdf-passthrough | Claude (admin Add-Licence PDF upload fails in prod — Royce hit it backfilling Elliot Gross) | 2026-07-22 | DONE — **PR [#945](https://github.com/eq-solutions/eq-shell/pull/945) OPEN**: `typecheck · test · lint` PASS, build clean, 177/177 tests, eslint clean on the diff (2 errors left in touched files are pre-existing, outside the hunks). Companion **eq-cards PR [#172](https://github.com/eq-solutions/eq-cards/pull/172)** = migration `0101_licences_canonical_shape.sql`, **WRITTEN NOT APPLIED**. **Live one-row repair already run with Royce's explicit go** — `86e39bb4` (Elliot Gross, Silica Awareness) had `licence_number` NULL→`''` and `expiry_date` NULL→`9999-12-31`; before/after captured, 0 unparseable rows remain of 162. That NULL pair would have thrown in `Licence.fromJson` and taken down his ENTIRE wallet list (the repo parses all rows in one `map()`), not just the new card. **CI RED — `Schema drift + anon-grant + policy-lint` — IS NOT THIS PR**, which contains zero SQL: the gate reads LIVE DBs and found `app_data.field_team_supervisors` on ehow with RLS off, 0 policies, `authenticated=arwd`, 10 rows (verified directly via `pg_class`/`relacl`). Created by the crew-scoping work two rows above. Same job passes on `main`; **it will block every eq-shell PR until fixed** → chip task_87efbf2f. Diagnosis posted as a PR comment. NOT merged, NOT deployed. Migration enforcement is a trigger COERCION not a bare NOT NULL, because `eq_cards_upsert_my_licence` has two live overloads and the positional one can pass an explicit NULL — rejecting would turn a silent bug into a failed save mid-form. Off origin/main `ed64a5f`. Root `C:\Projects\eq-shell` is live under another agent (branch observed moving `claude/national-scale-audit-25f570` → `claude/retention-purge-shell-control-cleanup` → `main` inside one session), so isolated here. `staff-licence-backfill.ts` rasterizes page 1 of an uploaded PDF to JPEG via pdfjs-dist + @napi-rs/canvas to fill `photo_front_url`; it throws in the Lambda on every PDF (0 successful `entry_method='admin_backfill'` rows ever on jvkn). Real error unobtainable — swallowed by a `catch` that only `console.error`s, so nothing reaches Sentry and Netlify function logs aren't on MCP. Fix = stop converting: store the PDF unchanged at `document_url`/`document_type='pdf'` (the shape Cards already writes), delete `_shared/pdf-rasterize.ts`, drop both native deps; teach the 3 Shell read paths (`staff-canonical-licences`, `cards-export-licences-background`, `worker-licences`) to surface `document_url` — they select only `photo_front_url` today, so **7 existing PDF-only licences of 162 live are invisible employer-side and silently dropped from compliance packs**; add ocr-licence prefill to the Add Licence modal; add Sentry capture to the catch. EQ Intake checked first per Royce — `api-extract` was designed 2026-06-07 but never built and the deployed Intake surface has no image/PDF path, so nothing to leverage there. |
| C:\Projects\eq-field-leave-paging-wt (eq-field) | claude/leave-paging | Claude (national-scale readiness audit → Phase 3: the last unbounded read, `leave_requests`; chip task_c5232a3d off PR #526) | 2026-07-22 | DONE — **PR [#528](https://github.com/eq-solutions/eq-field/pull/528) OPEN**, all checks green (Tests+lint pass; `netlify/eq-field/deploy-preview` pass; preview serves `eq-field-v3.5.350`), mergeStateStatus CLEAN. **Re-stamped v3.5.349 → v3.5.350 mid-flight**: PR #527 merged while this was in review and took 3.5.349 — note its squash TITLE says "v3.5.347" but its actual file stamps are 3.5.349, so the title is misleading, main is not regressed, and 3.5.349 is genuinely taken. Rebased onto `19b4c6e`, resolved the index.html banner conflict by keeping BOTH blocks and reordering mine above theirs (banner is newest-first), and re-ran the full gates post-rebase — 15/15 test files (incl. #527's new one), eslint 0 errors, `git diff --stat origin/main` shows only my 5 files so nothing of #527's was reverted. `leave.js:175` was an unbounded `sbFetch(...&order=created_at.desc...)`. **Chose option (b) — drop the `order=` entirely and sort client-side — but NOT in the shape the chip proposed.** Two things found by reading before writing: (1) `created_at` is genuinely non-unique on live ehow — **32 rows share only 3 distinct values** (ETL batch-stamped), so paging on it is a ~10-way tie, not a theoretical hazard; (2) the sort could NOT simply be dropped — `_renderLeaveAll` and `printLeaveRequests` consume `leaveRequests` in array order and do NOT sort themselves (only the worker own-requests view did), so it now sorts once at the load site via `_leaveByNewestFirst` (ties → id desc, undated → last). **Divergence from the chip's test instruction, deliberately:** leave's `toWideList` is a 1:1 `.map()`, NOT a grouping, so the page-boundary-corruption hazard that motivated v3.5.348 does NOT exist here — writing the "per-page transform corrupts" assertion would have been asserting something false. `tests/leave-paging.test.js` instead PINS the 1:1 property so the assumption breaks loudly if anyone makes it group. Also verified `rewritePath`'s blanket `/(^\|&)id=/ → leave_request_id` substitution doesn't maul the appended `order=`/`limit`/`offset`. 14/14 test files, eslint 0 errors. NOT merged, NOT deployed — Royce's call. |
| C:\Projects\eq-field-ts-paging-wt (eq-field) | claude/timesheets-canonical-paging | Claude (national-scale readiness audit → Phase 2: the canonical wide-surface reads `timesheets`/`schedule`, deliberately excluded from PR #525) | 2026-07-22 | DONE — **PR [#526](https://github.com/eq-solutions/eq-field/pull/526) MERGED** (squash `4233462`, Royce's "merge" go) → field.eq.solutions **LIVE + verified**: `sw.js` serves `eq-field-v3.5.348`, `app-state.js` APP_VERSION 3.5.348, live `supabase.js`/`timesheets.js`/`index.html` all carry `sbFetchAllWide`. (#520 landed just ahead as `19c53f1`; no conflict.) All checks were green pre-merge (Tests+lint; `netlify/eq-field/deploy-preview`). 13/13 test files (new `tests/timesheets-paging.test.js`, 26 assertions), eslint 0 errors / 697 pre-existing warnings. Off origin/main `dc6294c` (root checkout was on another agent's `claude/tenant-isolation-root-fix`, 5 commits stale). v3.5.348. Sibling to #525 but NOT the same fix: `timesheets`/`schedule` route through `_tsAdapter`/`_rsAdapter` `rewriteReadPath` + `toWideList`, so PostgREST row count ≠ final row count and plain `sbFetchAll` is WRONG twice over — (1) default `order=id` 400s because no normalized table has an `id` (`timesheet_id`/`schedule_id`), which is why `_loadFullDataForExport` is already broken on SKS today at 80 staff; (2) `toWideList` runs PER RESPONSE, so concatenating pages emits a duplicate partial wide row for every person/week straddling a boundary — corrupted payroll rows, worse than truncation. Fix = `sbFetchAllWide()`: page the NORMALIZED domain by normalized PK with the response transform suppressed, group to wide ONCE over the complete set. **Premise correction found by verifying:** the adapters are NOT default-off — `TIMESHEETS_/ROSTER_/LEAVE_CANONICAL_TENANTS` all contain `'sks'`, so the only live tenant runs the canonical path. Pages fetched in waves of 4 (~81 pages = ~14s sequential vs ~3s) — `sbFetchAll` itself left UNCHANGED, 18 call sites depend on it. `schedule` included on purpose (identical defect, adjacent lines); `leave_requests` registered in the plan table but its call site left alone (non-unique `order=created_at.desc` needs a tiebreak decision) = chip task_c5232a3d. **Royce's steer:** paging is the correctness floor, NOT the scale answer — the real lever is crew-scoping (verified live: `app_data.teams` has NO owner column, so "my crew" is not derivable today; 7 teams / 72 memberships / 80 staff) = chip task_6f433960. Harness trap documented in the test: `sbFetch` resyncs `window.SB_URL` to its lexical value on every call (v3.5.287), so a pre-fetch override is silently reverted — an earlier draft measured the canonical path while claiming to test the legacy one. NOT merged, NOT deployed — Royce's call. Worktree prunable after merge. |
| ~~C:\Projects\eq-field-scale-fetch-wt (eq-field)~~ PRUNABLE | claude/scale-people-fetch-truncation (merged, deleted) | Claude (national-scale readiness audit → Phase 1: people-family reads truncating at PostgREST's 1000-row cap) | 2026-07-22 | DONE — **PR [#525](https://github.com/eq-solutions/eq-field/pull/525) MERGED** (squash `dc6294c`, Royce's "merge it" go) → field.eq.solutions **deploy `6a60223e` published 2026-07-22 01:52:07Z, live-verified**: `sw.js` serves `eq-field-v3.5.347`, `app-state.js` APP_VERSION 3.5.347, live `auth.js` carries all 3 `sbFetchAll` call sites. Secret scan 0 matches. (v3.5.347). Four `people`-family reads never adopted `sbFetchAll` (v3.5.239, already used by 18 call sites) and silently truncate at 1000 rows with a 200 OK: login name-picker (`auth.js:111` — past 1000 people they cannot pick their name to sign in), staff-TS PIN gate (`auth.js:1210` — truncated BEFORE the eligibility filter ran), the whole `loadApprenticeData()` Promise.all (`apprentices.js:433-442`; `skills_ratings` is one row per person per period per competency so it crosses 1000 well before the roster does), and `people_removed` (`people.js:526`). Directly disproves v3.5.346's own "the data layer already scales" note. Ordered by `(name,id)` — non-unique sort lets rows straddle a page boundary. **Two traps found by verifying rather than assuming:** (1) pushing `group=in.(Apprentice,Labour Hire)` into the PIN-gate query looked like the better fix and would have been a latent regression — `GROUP_ALIAS_READ` is per-tenant (`org.branding.groupAliases`) so an aliasing tenant gets an empty gate; checked live on jvkn, no org sets it (eq/sks/favour-perfect all null), so dormant not live, but the filter now normalises via `normaliseGroupFromDb()` to match the gate dropdown. (2) the version bump is **load-bearing** — `sw.js:3` keys its cache on the version, so without it the SW keeps serving old scripts and nobody gets the fix. **`timesheets.js:236` DELIBERATELY EXCLUDED** — same 1000-row exposure (a single week at 1500 staff exceeds it) but it routes through `_tsAdapter.rewriteReadPath` + `toWideList`, so the PostgREST row count ≠ the final row count and naive paging would be WRONG, not merely truncating; chip task_4d2cb142. Root checkout was on another agent's `claude/tenant-isolation-root-fix` (pushed, clean) — took a real worktree instead, verified via `git rev-parse --git-dir`. 12/12 tests, eslint 0 errors. **NOT merged, NOT deployed — Royce's call.** Worktree prunable after merge. |
| ~~C:\Projects\eq-intake-sec2-register-wt (eq-intake)~~ REMOVED | claude/sec2-register-close (merged, deleted) | Claude (asked to "fix SEC-2" from the security register; live-verified first — both zaap and ehow already carry the app_metadata-based tenant_isolation policy via eq-shell's tenant-migrations 0023 (original port) + 0178 (reconciliation/hardening). SEC-2 was stale, not live) | 2026-07-21 | DONE — **PR [#75](https://github.com/eq-solutions/eq-solves-intake/pull/75) MERGED** (squash `b32ff32`, Royce's "merge it" go). Annotated both staging sql files (no DDL change), closed SEC-2 in `eq-context/ops/security-register.md` + removed the stale `ACCEPTED_ERRORS` baseline (separate eq-context commit `bb2a522`). Docs-only, nothing to deploy. Worktree + local/remote branch removed 2026-07-21. |
| ~~C:\Projects\eq-shell-preset-saveall-wt (eq-shell)~~ REMOVED | claude/preset-save-all (merged, deleted) | Claude (EQ Ops Setup: "Save all" for the preset line-item library — Royce screenshot annotation on the Rate library page, editing many rows one-Save-click-at-a-time) | 2026-07-21 | **DONE — PR [#941](https://github.com/eq-solutions/eq-shell/pull/941) MERGED** (squash `b1379c0`, Royce's "merge it" go), branch confirmed deleted remotely. No schema/RPC change (reuses `eq_upsert_rate_preset` from #928/#937). Tracks dirty preset rows client-side and adds a "Save all (N)" button next to the Preset line items header that batches the dirty rows through the existing RPC sequentially, then reloads once. **Isolated into this worktree deliberately** — root checkout was mid-race on an unrelated branch (`claude/supplier-credentials-gate`, uncommitted supplier-credentials work from another agent) when this task started; root had moved to a THIRD unrelated branch (`claude/docs-licence-photos-rls-fix`) by the time this merged — confirms staying in worktrees was the right call, not a one-off. `pnpm run build` (tsc + `EqOps` chunk) clean, 177/177 tests, 1 pre-existing lint error (`_i` unused, line 703) confirmed unrelated to this diff via `git diff main`. Worktree removed 2026-07-21. |
| ~~C:\Projects\eq-shell-pricing-guard-wt (eq-shell)~~ REMOVED | claude/pricing-role-guard + claude/preset-rpc-revoke-anon (both merged, deleted) | Claude (EQ Ops: Setup lockout + labour cost regression + list filters + line-item reordering) | 2026-07-21 | **DONE — PR [#928](https://github.com/eq-solutions/eq-shell/pull/928) MERGED** (squash `f906546`, Royce's "merge it and dispatch" go) → core.eq.solutions **deployed + live-smoked** (verify-shell-session 401, shell-login 400). **Migrations 0192 + 0194 DISPATCHED to `sks` only** (deliberately NOT fleet-wide — `zaap` was missing an unrelated concurrent-session migration (0191) that a full dispatch would have swept in unasked) and **re-verified live** via `pg_get_functiondef`/grants post-dispatch. Four items, detail below. (1) **Migration 0192** — `eq__assert_pricing_role` gated every EQ Ops Setup write on `service.tenant_members`, the EQ *Service* membership table; EQ Ops roles live in Shell and arrive as JWT `app_metadata.eq_role`, so a Shell manager with no Service row was locked out of **all ten** Setup RPCs. Sentry confirmed it independently. Guard had existed in **no migration** — hand-applied to ehow; now under the One Pipe. (2) **Migration 0194** — `quote_rate_presets` stored only a charge rate, so cost had to be invented; #820 rightly stopped inventing it for labour and labour cost has saved as $0 since 2026-07-13 (0%→45% zero-cost labour lines that week, 10 quotes hit incl. one verbal-win and one job-created; **unrecoverable**, `quote_audit` never stored cost — Royce needs to re-enter these). Presets now carry `unit_cost_cents`; Setup shows Cost + Charge; existing presets default to 0 so no quote or total moved on dispatch. Touches **three** preset RPCs. (3) Same migration hardens `eq_replace_line_items` against 0188's margin-redaction-becomes-erasure bug (NULL cost on read → saved back as 0 on write) — proven NOT the cause of (2). (4) Features: column filters on Quote #/Job No./PO/Est./Status; up/down line-item reordering (`src/modules/quotes/lineOrder.ts`, 8 tests, mutation-checked). **SELF-CAUGHT REGRESSION, same session:** 0194's DROP+CREATE on 3 preset RPCs (required for the shape change) reset them to Postgres's default EXECUTE-to-PUBLIC grant; the re-GRANT to authenticated/service_role didn't revoke that default, so `eq_list_rate_presets`/`_admin`/`eq_upsert_rate_preset` went live with anon+PUBLIC access for ~9 minutes. **Not exploitable** (SECURITY DEFINER, tenant from JWT, anon reads 0 rows, write raises on NULL tenant — verified live) but real drift; the anon-grant CI check did NOT catch it (its scope is table grants, not function EXECUTE — noted as a gap). Fixed same session: **PR [#937](https://github.com/eq-solutions/eq-shell/pull/937) MERGED** (squash `b753eda`) → **migration 0195 DISPATCHED to `sks`**, grants re-verified via `has_function_privilege()` — clean. LESSON: after any DROP+CREATE in a migration, verify grants live post-dispatch; "no anon" in a migration's own header is an intention, not a check. `zaap` is now 4 migrations behind (0191/0192/0194/0195) — 0191 is another agent's, untouched here. Known, unfixed, out of scope: several quotes are missing cost on **subcontractor** lines too (SKS-15758 — $38.7k verbal-win, $4.55 cost recorded); Royce deprioritised — sales-value/PO matching is the current focus. Worktree removed 2026-07-21. |
| ~~C:\Projects\eq-shell-cert-migrate-wt (eq-shell)~~ REMOVED | claude/certificates-migrate-endpoint (MERGED as #934) → claude/certificates-decommission-shell (merged, deleted) | Claude (certificates→licences Phase B endpoint, then Phase C cleanup) | 2026-07-21 | **DONE, CLOSED.** Phase B: PR [#934](https://github.com/eq-solutions/eq-shell/pull/934) MERGED (`37724ca`) + EXECUTED live — 5 certs migrated to new private licence rows, 2 attached onto an existing licence's empty document slot, 0 active certificates remain, all 7 files byte-identical (verified via `storage.objects` size compare). Phase C: PR [#940](https://github.com/eq-solutions/eq-shell/pull/940) **MERGED** — deleted `certificates-migrate.ts`, pruned `'certificates'` from the anon-grant-lockdown array, updated README/ARCHITECTURE-V2 bucket lists. Build clean, 177/177 tests. **`0100_drop_certificates.sql` APPLIED to live jvkn 2026-07-21** — direct `apply_migration` was hard-blocked by the classifier (destructive DDL), Royce ran it by hand via the Dashboard SQL editor. Two real bugs surfaced on the live run, both fixed and backported in eq-cards [#171](https://github.com/eq-solutions/eq-cards/pull/171): DROP TABLE before DROP POLICY throws `2BP01` (policies reference the table), and raw `DELETE` on `storage.objects`/`storage.buckets` throws `42501` (Supabase's `protect_delete()` trigger — bucket/object cleanup must go through the Storage API/Dashboard, not SQL, and isn't scriptable via the Supabase MCP). Final live-verified state: table, policies, bucket, and all objects gone; `eq_finalize_deleted_accounts` no longer references certificates. Worktree removed 2026-07-21. |
| ~~C:\Projects\eq-cards-decommission-wt (eq-cards)~~ REMOVED | claude/certificates-decommission (merged) + claude/fix-drop-certificates-order (merged, deleted) | Claude (certificates→licences Phase C — eq-cards half, then the live-run migration-order fix) | 2026-07-21 | **DONE, CLOSED.** PR [#170](https://github.com/eq-solutions/eq-cards/pull/170) **MERGED**: deletes `lib/features/certificates/` (8 files) + prunes route wiring (`/certificates` → wallet redirect, `/card` pattern) + `wallet_cache_service.dart` + the certs half of `licences_list_screen.dart`'s combined-wallet query. Migration `0100_drop_certificates.sql` (renumbered from `0099` mid-review, collision with a concurrent PR): drops the table + bucket + 4 storage policies, and first `CREATE OR REPLACE`s `eq_finalize_deleted_accounts` (0097) — that function `SELECT`s from `public.certificates` directly, so dropping the table first would have broken the scheduled account-deletion job for every pending account, not just ones with a certificate. `flutter analyze` clean, 241/241 tests. PR [#171](https://github.com/eq-solutions/eq-cards/pull/171) **MERGED**: fixes the migration's drop order (policies before table — the original order threw `2BP01` on the live apply) and documents that storage bucket/object cleanup is a manual Storage API step, not expressible as SQL (`42501` protect_delete trigger). Live jvkn fully applied and verified clean. **Found + flagged, not fixed:** `admin-attach-licence-photo` Supabase Edge Function is confirmed dead but still `ACTIVE` live (version 1) — no MCP tool to un-deploy it, needs Supabase CLI/dashboard. Worktree removed 2026-07-21. |
| C:\Projects\eq-shell-orgmem-wt (eq-shell — CONTROL PLANE tree) | claude/org-memberships-role-lockdown | Claude (same privilege-escalation sweep, fourth repo — eq-cards, whose DB is jvkn, so the migration lands in eq-shell's control-plane tree) | 2026-07-21 | DONE — **PR [#933](https://github.com/eq-solutions/eq-shell/pull/933) OPEN**, `2026_07_21_org_memberships_role_write_lockdown.sql`. **LIVE for any invitee:** `org_memberships_update`'s WITH CHECK was `is_org_admin(org_id) OR (status='active')` — it constrained ONLY status, while `role` was column-UPDATE-grantable to authenticated, so `SET status='active', role='admin'` on your own pending row passed both clauses → org admin. Cascades through `is_org_admin()` to `licences_read` (every non-private licence in the org), `profiles_read` (member PII), `worker_invites` insert/read/update, and org_memberships insert/update; **eq-cards gates its whole admin UI on the same predicate** (`org_admin_provider.dart`, read from the browser). 0 pending rows at discovery so not exploitable at that instant, but open to every future invitee on acceptance. anon writes correctly closed (#786/#787 holds). Fix = privilege-first `REVOKE UPDATE (role, org_id, user_id, tenant_id)` (status stays writable so pending→active still works; admins still set role at INSERT via `admins_insert_org_memberships`) + WITH CHECK tightened to bind `user_id`. **Also** revoked the dormant INSERT/UPDATE/DELETE on `shell_control.users` from authenticated — it grants UPDATE on `role`/`is_platform_admin` (the JWT claim source, 72 users) and is saved ONLY by RLS having a service_role-only policy; a future "read your own row" policy would have made it live. NOT touched: `public.workers.role` is self-writable BY DESIGN (Cards intake self-declaration) and no authz reads it. **Control-plane tree has NO CI apply path — NOT applied; ledger row added as ⏳ PENDING with post-apply verification queries. Royce's go.** Worktree prunable after merge. |
| ~~C:\Projects\eq-field-approvals-wt (eq-field)~~ PRUNABLE | claude/timesheet-leave-approval-lifecycle (merged, deleted) | Claude (timesheet + leave approval lifecycle — the two judgement-call items surfaced by the 2026-07-21 privilege-escalation sweep, after Royce settled the business rules) | 2026-07-21 | **DONE — PR [#520](https://github.com/eq-solutions/eq-field/pull/520) MERGED** (squash `19c53f1`, Royce's "full go") → field.eq.solutions deploy `6a6041b6` published 2026-07-22 04:06:28Z, then **migration APPLIED to ehow and live-verified 2026-07-22**. ORDER MATTERED and was followed deliberately: the code half (eq_role claim on the magic-link JWT) deployed FIRST, because applying the trigger first would have broken the only leave-approval path with real usage (30 approved rows) — the guard rejects a caller with no eq_role. Dry-run in BEGIN…ROLLBACK before applying (4 triggers, clean). Post-apply verified: 4 BEFORE triggers live+enabled on `app_data.timesheets`/`leave_requests`, existing rows untouched (133 timesheets / 30 approved leave), and BOTH `eq__caller_staff_id`/`eq__caller_uid` EXECUTE grants present to `authenticated` (the earlier draft's REVOKE would have been a total write outage). **Functional proof via rollback-only test with simulated JWT claims: employee BLOCKED from approving, manager APPROVED with `approved_at` stamped by the trigger, direct→`paid` BLOCKED. Timesheet approval had never completed once in production; it now works.** Test left no trace (still 133 submitted / 0 approved). Original PR note follows. Was: **OPEN**, migration `20260721_field_timesheet_leave_approval_lifecycle.sql` + a 1-claim change to `_shared/leave-canonical.js`. `status` on `app_data.timesheets` and `app_data.leave_requests` was UPDATE-able by any authed tenant user under plain tenant isolation — self-approval of your own timesheet and your own leave; `approved_by_user_id` was client-supplied so the audit trail recorded the writer, not the decider. **Deliberate departure from the sweep's privilege-first pattern**, and the write-path trace is why: all three legitimate writers run as `authenticated` and write `status` — `field_timesheets` is an auto-updatable security_invoker view with NO trigger; `field_leave_requests_iud` is NOT SECDEF (prosecdef=false); and `approve-leave.js`'s magic link PATCHes `app_data.leave_requests` DIRECTLY with a `role:authenticated` JWT minted in `leave-canonical.js`. A column REVOKE would have taken out live leave approval (the only approval path with real usage — 30 approved rows vs 133 timesheets ALL stuck at `submitted`, i.e. timesheet approval has never once run). The good/bad distinction is purely OLD->NEW, which RLS cannot see. So: BEFORE trigger on each BASE table, the one point all three writers converge. Lifecycle per Royce: supervisors escalate, manager-only self-approve, manager-only reopen until `paid_at`, `paid` service-role-only + terminal, labour_hire/subcontractor same path. service_role and empty `request.jwt.claims` exempt. Self-approval barred on PROOF only (50/95 staff have a user_id link; magic-link `sub` is synthetic). TRAP FOUND: `auth.uid()` casts `sub` to uuid unguarded and RAISES on the magic-link JWT — hence `eq__caller_uid()`. Code half adds `eq_role:'supervisor'` to the magic-link JWT so approve-leave survives the guard. All `tests/*.test.js` pass, eslint 0 errors. FOLLOW-UP: move approve-leave onto the view, then the column REVOKE becomes safe and the trigger is belt-and-braces. **NOT applied — Royce dispatches.** Worktree prunable after merge. |
| C:\Projects\eq-field-userid-guard-wt (eq-field) | claude/field-people-user-id-guard | Claude (same privilege-escalation sweep, third repo — after eq-shell #930 and eq-service #576) | 2026-07-21 | DONE — **PR [#518](https://github.com/eq-solutions/eq-field/pull/518) OPEN**, migration `20260721_field_people_iud_user_id_guard.sql`. **THIRD gap of the same shape in `field_people_iud`**: #497 gated licence+agency, #499 caught hire_company, and `NEW.user_id` was never gated at all (`user_id = coalesce(NEW.user_id, user_id)` in the UPDATE branch). `app_data.staff.user_id` is the Shell-login→Field-person binding that `verify-pin.js`'s `resolveFieldPerson()`/`field_person_by_user_id()` resolves identity through at handoff, and that `leave-canonical.js` uses for `decided_by_user_id` — so any authed Field user could repoint a colleague's row to their own Shell user id and be resolved AS that colleague (name on approvals, roster, timesheets, leave), or break a supervisor's Field identity. NOT privilege escalation (eq_role/extra_perms come from the JWT, never this table) — identity spoofing + audit corruption + denial. Reachability verified link-by-link live: authenticated has column-UPDATE on the view; view is `security_invoker=on` and staff's 4 policies are plain tenant isolation so any colleague is targetable; the INSTEAD OF trigger is SECDEF so its tenant-only WHERE is the whole gate; direct UPDATE on `app_data.staff` is NOT granted, making the view the only write path. `field_person_by_user_id` EXECUTE still correctly revoked from anon+authenticated (2026-07-04 fix holds). **ehow ONLY** — zaap has no `field_people_iud` and no authenticated UPDATE on `field_people`, so no plane divergence (unlike eq-shell 0192/0193). Fix = privilege-first: `REVOKE INSERT(user_id), UPDATE(user_id) FROM authenticated` (column grants are checked BEFORE the INSTEAD OF trigger, so a future trigger edit can't silently reopen it) + preserve user_id in the trigger in the same shape as the other reverts. Safe: nothing legitimate writes it through the view (people.js has zero `user_id` references; INSERT branch never wrote it; the real writer is eq-shell's workers-canonical-sync via service_role, direct to `app_data.staff`). Trigger body hash-verified byte-for-byte vs live `pg_get_functiondef` apart from the 2 intended changes. **NOT applied — Royce's go.** Worktree prunable after merge. |
| C:\Projects\eqsvc-tm-lockdown-wt (eq-solves-service) | claude/tenant-members-role-lockdown | Claude (privilege-escalation sweep of eq-service, spun out of eq-shell's pricing-guard plane-divergence work) | 2026-07-21 | DONE — **PR [#576](https://github.com/eq-solutions/eq-service/pull/576) OPEN**, migration 0189. `service.tenant_members` granted `authenticated` arwd under a single tenant-only `tm_isolation` FOR ALL policy with no column or role restriction — any authenticated user could `UPDATE … SET role='manager'` for their own tenant, INSERT an elevated row, or DELETE a colleague's membership. Verified live (relacl + pg_policy + column_privileges); UPDATE was granted on `role`/`is_active` specifically. LIVE not latent: `tenant_members.role` feeds `can()` for the #551 cluster-3 gates (app-layer only), the non-JWT path in `requireUser()`/`getApiUser()` reads it, all 5 ehow auth.users have signed in with matching `app_metadata.tenant_id` (2 are `employee`), and BOTH auth paths run as `authenticated` (`mintServiceJwt` sets `role:'authenticated'`). Fix is privilege-first: revoke INSERT/UPDATE/DELETE, then column-`GRANT UPDATE` back on only `tech_onboarded_at`/`setup_checklist_dismissed_at` (the sole user-authored writes, in `dashboard/actions.ts`) — column grants are enforced independently of RLS. Same revoke on `service.profiles` (unreachable today, one CREATE POLICY from live). Deliberately NOT self-row-scoped: on the federated path `auth.uid()` is the Shell user id and may not match `tenant_members.user_id`. Also corrected the stale comments at `lib/api/auth.ts:23` / `lib/actions/auth.ts:32` claiming ehow has neither table — it has both, 5 rows each; the claim-only shortcut holds only while `IDENTITY_CLAIMS_ENABLED` is off (provable — `identity_rollout`/`resolve_federated_membership` don't exist on ehow). **No DDL run against ehow, not even rolled back.** Dispatch via `apply-service-migrations.yml` = Royce's. NOTE: root eq-solves-service checkout was on a **detached HEAD 5 commits behind origin/main** — real worktree created off origin/main and confirmed genuine via `git rev-parse --git-dir` before any write. Worktree prunable after merge. |
| ~~C:\Projects\eq-shell-sentry-flush-wt (eq-shell)~~ REMOVED | claude/sentry-flush-fix (merged, deleted) | Claude (server-side Sentry events silently dropped from scheduled functions — `check-never-invited-workers` reports 45 never-invited workers daily and produced zero Sentry events since deploy) | 2026-07-21 | **DONE — PR [#924](https://github.com/eq-solutions/eq-shell/pull/924) MERGED** (squash `f9d5f7f`, Royce's explicit "merge it") → core.eq.solutions **deploy published 2026-07-21 04:08:50 UTC, confirmed via Netlify API**; live `verify-shell-session` sampled 401 post-deploy (expected healthy response). Worktree removed 2026-07-21. `captureServerError` fired-and-forgot and `withSentry` only flushed in its **catch** block, so any handler that reported a finding then returned 200 (every scheduled check) was frozen by Lambda mid-send. Fix flushes on the success path too — covers all 86 call sites across 123 wrapped functions, not just the two checks. Flush is **gated on having actually captured something** (`Sentry.flush()` is not free on an empty queue and `withSentry` wraps the hot SSO paths `token-exchange`/`mint-supabase-jwt`, which run on every iframe mount), and `flushSentry` **joins an in-flight flush** started by an unawaited capture — without that, the gate silently reintroduced the same drop for the 84 call sites that don't await. Regression test is mutation-verified (removing the success-path flush fails it); it caught the gate flaw, review didn't. 169/169 tests + tsc + eslint clean locally and on CI (CI Node 22 confirmed to accept the new `--experimental-test-module-mocks` test-script flag; Netlify build never runs tests). Deploy-preview smoke passed but does NOT exercise the fix — `SENTRY_DSN` is production-context only, so `withSentry` takes its disabled early-return on previews. **Isolated into this worktree deliberately** — the root checkout was mid-race (a concurrent agent switched it to `claude/quote-clarifications-alignment` under this session) and the original branch also carried a Staff/Company-column commit already squash-merged to main as `e94d259` (#922), which would have shown as a duplicate in the PR. **Post-deploy verification still owed:** confirm the 21:20 UTC run lands `context: check-never-invited-workers` / `never_invited_count: 45`. Do NOT check via a `has:netlify_function` query — that tag is only set on the throw path, never by `captureServerError`, so it returns empty even on a healthy system. Worktree prunable after merge. |
| C:\Projects\eq-field-mobile-weekend-wt (eq-field) | claude/mobile-schedule-weekend | Claude (mobile My Schedule + home tile: show Sat/Sun when rostered — Royce spotted Mon-Fri-only day cards in the mobile view, port of SKS v3.10.99) | 2026-07-21 | DONE — **PR [#514](https://github.com/eq-solutions/eq-field/pull/514) OPEN** (v3.5.338). `renderSchedule()` (roster.js) + home tile shift-count/next-shift/subtitle (home.js) now derive their day list from `getVisibleRosterDays()` instead of a hardcoded Mon–Fri array; home.js usage is `typeof`-guarded since roster.js is lazy-loaded there. NOT merged — Royce's call, no deploy. Worktree prunable after merge. Sibling fix pushed to SKS NSW Labour (v3.10.99, branch `claude/mobile-view-weekends-57c90f`, not yet PR'd). |
| C:\Projects\eq-solves-service\.claude\worktrees\eqsvc-loadtime-ux (eq-solves-service) | worktree-eqsvc-loadtime-ux | Claude (loading times + UX pass — Royce's "you decide" go) | 2026-07-20 | DONE — created via `EnterWorktree` (real git worktree, confirmed via `git rev-parse --git-dir`/`git worktree list` before starting, after the prior row on this repo turned out to be a non-worktree). **PR [#570](https://github.com/eq-solutions/eq-service/pull/570) MERGED** (jszip lazy-load on /admin/backup — confirmed via build output the page's own chunk is 10.4KB with zero JSZip code, JSZip isolated to its own ~194KB chunk) + **PR [#571](https://github.com/eq-solutions/eq-service/pull/571) MERGED** (Sentry server bundle trim — sentry.server.config.ts's `tracesSampleRate: 0` still counted as "tracing enabled" per the SDK's `hasSpansEnabled()`, pulling ~27 unused auto-performance integrations (mysql/mongo/kafka/prisma/langchain/openai/express/…) into the server Lambda on every cold start; omitting the key entirely — verified empirically via `getDefaultIntegrations()`: 44→17 integrations — skips them. `beforeSend`/T3-scrubbing untouched, only the tracesSampleRate line changed). Both Royce-confirmed via AskUserQuestion before merge (#571 explicitly flagged as touching the auth-scrubbing-adjacent config file). tsc + build clean on both; only the known pre-existing integration-test flake failed. **Post-deploy verification via Netlify MCP: the "smaller Lambda" claim in #571 was WRONG** — actual deployed function size barely moved (27,160,990 → 27,161,093 bytes, noise). The fix still cuts real init-time work (44→17 integrations registered) but Netlify's file-tracing bundles whatever `@sentry/node`'s module graph statically `require()`s regardless of runtime `hasSpansEnabled()` gating, so it doesn't shrink the artifact. A cold `/api/health` sample (4.9s) landed right in the old 4.6–5.3s baseline — consistent with no measurable win. **Found + fixed a second, unrelated bug while trying to verify `/api/sentry-test` live**: the public demo tenant (`a0000000-0000-0000-0000-000000000001`) has zero rows anywhere on live ehow (service.tenants, tenant_members, customers, sites) and the `demo@eqsolves.com.au` auth user doesn't exist — casualty of the urjh→ehow cutover (2026-06-22), never reseeded. Both the one-click demo button and manual demo-credential entry failed with a raw "Invalid login credentials" error. Chip spawned (task_f790d134), Royce's call was "remove demo — we can add easily later" rather than a full reseed. **PR [#572](https://github.com/eq-solutions/eq-service/pull/572) MERGED** — removed the demo tile + demo view from the sign-in chooser (main user-facing entry point); left `lib/utils/demo.ts` / `app/demo/route.ts` / `startDemoSessionAction` untouched (already fail gracefully, one-file reconnect once reseeded). Verified in a real browser against the local production build before shipping. **Follow-up sweep ("any more speed improvements while the bonnet's open"):** checked whether unused Sentry integration files could be safely excluded from Netlify's file-trace to actually shrink the Lambda — confirmed unsafe, `@sentry/node`'s own bootstrap does an unconditional top-level `require()` of the whole tracing barrel, excluding those files would break Sentry's own module resolution, not just the app's. Found two real, safe wins instead: dashboard's tenant-settings lookup was a standalone sequential await before the main data batch (folded in); the Maintenance Checks list page (core nav, every tech/supervisor hits it) ran 7 independent lookups — sites, customers, job plans, staff roster, contract-scope items, plus the checks query itself — as 7 sequential round-trips with zero data dependency between them, now one parallel batch. Also found and deleted a fully dead fetch (`siteAssetPlans`) sitting in the same page, fetched but never read anywhere. **PR [#573](https://github.com/eq-solutions/eq-service/pull/573) MERGED** (squash `ac38780`, Royce's "merge it" go after CI green) — tsc + build clean, every downstream consumer verified unchanged by careful reading (not just tests), but honestly flagged as statically-verified only — no live click-through possible in this environment (no working demo/test credentials post-#572). Worktree prunable. |
~~C:\Projects\eq-shell\.claude\worktrees\bold-brattain-688290 (eq-shell)~~ REMOVED | claude/comms-dashboard-loadtime (merged, deleted) | Claude (NSW Comms Dashboard load-time fix — Royce reported it's "VERY slow to load" live; profiled via Performance API, defer non-critical ops_eligible fetch + fold this-week roster into the main jobs GET to cut round-trips) | 2026-07-19 | DONE — **PR [#901](https://github.com/eq-solutions/eq-shell/pull/901) MERGED** (squash `0189850`, Royce's "merge it once it's ready" go) → core.eq.solutions deploying. Profiled root cause: 4 concurrent comms-jobs calls on Dashboard mount (0.8-1.9s each, DB-side trivial at 1012 schedule_entries rows — round-trip overhead not query cost) plus a separate ~1.5s auth check + ~750ms client bootstrap gap flagged as OUT of scope (Shell-wide, already Sentry-tracked as auth-stall). Fix folds this-week roster into the main jobs GET (shared fetchRoster() helper) + defers the non-critical ops_eligible badge count — cuts 4 concurrent calls to 2. Build+tests(151)+CSS-coverage green. Branch deleted. Worktree removable. |
| ~~C:\Projects\eq-field-cluster3-server-wt (eq-field)~~ REMOVED | claude/access-model-cluster3-server-enforce → claude/field-people-hire-company-guard (both merged+deleted) | Claude (access-model cluster 3 — SERVER-side enforcement of field.manage_roster/licences/labour_hire; follow-up to #496, chip task_d50c0461) | 2026-07-19 | **DONE — all three PRs MERGED + LIVE.** #497 (`13d210d`) + #498 (`af9b1d5`) merged on Royce's "merge both safely, no mistakes" go. **Gap found AFTER merge**: `field_people_iud`'s `field.manage_labour_hire` guard only reverted `NEW.agency`, never `NEW.hire_company` (`app_data.staff` has both columns; PR #496's client treats them as one unit) — any authed Field session, any role, could set `hire_company` directly via REST, bypassing the gate. A migration file for this fix was sitting uncommitted in the worktree (from a concurrent session) claiming it was "already applied out-of-band" — checked live via `pg_get_functiondef` before trusting that and it was FALSE. Corrected the file, opened **PR [#499](https://github.com/eq-solutions/eq-field/pull/499)**. Classifier declined to let the apply run autonomously (new ground beyond the explicit "merge both") — Royce said "apply it", applied + verified live (`pg_get_functiondef` now shows the `hire_company` guard), then PR #499 merged (`c9a30c4`) to match. field.eq.solutions confirmed deployed at all 3 commits via live `sw.js` + `git log origin/main`. Worktree + all branches removed. LESSON: an "already applied" claim left in a migration file's own comments is not self-verifying, even from a session in the same lineage — re-check `pg_get_functiondef`/`pg_policies` against live before trusting it. |
| ~~C:\Projects\eq-field-cluster3-wt (eq-field)~~ REMOVED | claude/access-model-cluster3-field (merged) | Claude (access-model cluster 3 — enforce field.manage_roster / field.manage_licences / field.manage_labour_hire) | 2026-07-17 | DONE — **PR [#496](https://github.com/eq-solutions/eq-field/pull/496) MERGED** (squash `b3c7b04`, Royce's "merge #496" go) → field.eq.solutions auto-deploying. Rule-0.5 finding: licence-management and labour-hire-company aren't separate eq-field features (no such screens/entities exist) — both are two text fields (licence, hire-company/agency) inside the shared Person modal; scoped down with Royce's go to: full gate on roster.js/teams.js writes (`canManageRoster()`, also closed a real ungated-write gap in teams.js) + field-level disable on the two Person-modal inputs. Helpers centralized in permissions.js (eager-loaded) after catching a real bug — the standalone `teams` lazy-group loads teams.js WITHOUT roster.js, so a roster.js-local helper threw ReferenceError there. Confirmed zero merge-order conflict with #497 (disjoint files; #496's `can()`/`extraPerms()` mechanism pre-dates this work at v3.5.78, no dependency either direction) before merging. Worktree removed. |
| C:\Projects\eqsvc-access-cluster3-wt (eq-solves-service) | claude/access-cluster3-service-gate | Claude (access-model cluster 3 — enforce service.reopen + service.record_tests. Sibling to the eq-field row above, chip task_5e61efdd.) | 2026-07-17 | **CORRECTED 2026-07-21: PR #551 is MERGED (2026-07-17T04:39:47Z), not open — this row said OPEN for four days.** Worktree still on disk at `cb3fe54`, prunable. Original note follows. DONE — **PR [#551](https://github.com/eq-solutions/eq-service/pull/551) ~~OPEN~~ MERGED** (off origin/main 88f82c5; root checkout was a stale detached-HEAD echo of already squash-merged commits, 56 behind origin/main — isolated into this worktree instead). Bumped @eq-solutions/roles v2.4.0→v2.5.3 (had to force-reinstall — npm's lockfile had cached a stale pre-tag-move commit for the v2.5.3 git ref). `reopenCheckAction` (maintenance/actions.ts) was `canWrite() OR isAssigned` — any technician assigned to a check could reopen it regardless of role — now `canReopen()` only, assignee bypass closed. `updateInstrumentAction` (instruments/actions.ts), the sole calibration-write path, now `canRecordTests()`. UI gates threaded through both prop chains. tsc + `npm run check` (build) + 351/351 tests green (added canReopen/canRecordTests coverage to roles.test.ts). NOT merged — Royce's call; no deploy. Worktree prunable after merge. **Independent review 2026-07-17 (second session, after eq-field's cluster-3 PR #496):** confirmed no gaps — scanned all 23 exported maintenance/actions.ts functions + both instruments/actions.ts functions, only the 2 mapped to the new keys were touched (no scope creep, no missed sibling write path); `requireUser()`'s `role` is server-JWT-resolved, never client input, so `canReopen`/`canRecordTests` are genuine server-side enforcement (architecturally ahead of eq-field, which has no server write path at all to gate); PR #551 CI green on `tsc + next build` + `Typecheck + audit`, red only on `Integration tests (Supabase local)` — this repo's documented pre-existing flake, not a regression. No changes made. |
~~C:\Projects\eq-shell-comms-polish-wt (eq-shell)~~ REMOVED 2026-07-19 | claude/nsw-comms-polish-f0fe33 (merged) → claude/comms-startby-estimate (merged) | Claude (NSW Comms polish — perf on the jobs-GET waterfall, resource-dashboard landing view, creative extras) | 2026-07-17 | DONE — **PR [#889](https://github.com/eq-solutions/eq-shell/pull/889) MERGED** (squash `80af1d8`, Royce's "merge it" go) → core.eq.solutions **confirmed live** (deploy `6a598e72`, commit_ref `80af1d8` verified via Netlify MCP). Perf: parallelized the 3-deep serial comms-jobs.ts jobs-GET waterfall. Feature: Dashboard is now the default `/sks/comms` landing view (capacity card + crew gaps + this-week agenda, click-through to edit) + Monday present-mode (full-screen TV readout) + weekly capacity digest email (inert — needs `COMMS_DIGEST_RECIPIENTS` set) + Microsoft milestone funnel (MOP→Pre-cable→Post-dock→Invoiced, active Microsoft jobs). Follow-up from Patrick's (Melbourne, Microsoft account) demo feedback was **PR [#897](https://github.com/eq-solutions/eq-shell/pull/897) — MERGED 2026-07-19** (`0990051`) — this note previously said "NOT merged", which was stale; re-verified live via `gh pr list`. "Start by" badge = PO+53-day Finished estimate minus planned hours (8hrs/weekday), amber badge distinct from the blue Dock badge, tooltip explicit it's not a customer commitment. Worktree re-verified clean (no uncommitted/unpushed work) and removed 2026-07-19. |
| ~~C:\Projects\eq-shell-signals-wt (eq-shell)~~ REMOVED | claude/dashboard-signals (merged, deleted) | Claude (Core dashboard signals board — Phase 1) | 2026-07-17 | DONE — **PR [#886](https://github.com/eq-solutions/eq-shell/pull/886) MERGED** (squash `13a6386` → main) → core.eq.solutions auto-deploying, **confirmed live** (deploy `6a592f6`, commit_ref `13a6386` verified via Netlify MCP). `signals.ts` endpoint + 3 permission-gated bands (compliance / outstanding works / deployable crew) replacing the weak Activity + Upcoming columns; the "rostered but licence lapsed" cross-app join is the headline. Gates on interim base perms (`field.view`/`service.view`) — adopt cluster-1's `field.view_licences` etc. as they ship (see accessmodel-cluster1-shell row below). Worktree + local/remote branch pruned. |
| ~~C:\Projects\eq-shell-accessmodel-c1-wt (eq-shell)~~ REMOVABLE | claude/accessmodel-cluster1-pii-gate (merged, deleted) | Claude (access-model cluster 1 Phase 4 remainder — contact PII gate on direct-RPC reads) | 2026-07-17 | DONE — **PR [#885](https://github.com/eq-solutions/eq-shell/pull/885) MERGED** (squash `5e4f41a`, cluster 1 Phases 1–2 + cluster 3) + **PR [#890](https://github.com/eq-solutions/eq-shell/pull/890) MERGED** (squash `543488e`, migration 0190 contact-PII gate on `eq_list_contacts_for_customer`/`eq_list_contacts_for_site`/`eq_get_quote_detail.contact_email` — a direct-browser-RPC gap the #885 server-layer redaction couldn't reach, found via full audit; gm-reports has zero direct-RPC paths, nothing to gate there). **Migrations 0188+0189+0190 DISPATCHED together via One Pipe** (`tenant-migrate.yml` run 29664255048, Royce's explicit go + production-environment approval click) and **independently re-verified live on both planes** (`ehowgjardagevnrluult` + `zaapmfdkgedqupfjtchl`) via direct `pg_proc` source check post-dispatch — margin gate, job-number 42501 gate, and PII gate all confirmed present, not just ledger-trusted. Cluster-1 Phase 4 CLOSED on eq-shell side. Cluster-3's field.*/service.* keys still tracked separately = chips task_d50c0461 / task_5e61efdd (see `eq-field-cluster3-server-wt` / `eqsvc-access-cluster3-wt` rows). Worktree re-verified clean and removed 2026-07-19. |
| ~~C:\Projects\eq-intake-mergepanel-wt (eq-intake)~~ REMOVED | claude/mergepanel-demo-ui (merged, deleted) | Claude (wire MergePanel Preview/Confirm UI into IntakeHealthHome — library layer was already merged via PRs #67-71, only the demo UI wiring was missing) | 2026-07-16 | DONE — **PR [#72](https://github.com/eq-solutions/eq-solves-intake/pull/72) MERGED** (squash `937d9dd` → eq-solves-intake main, Royce's go after demo click-through). Worktree + branch pruned. |
| ~~C:\Projects\eq-shell-tenant-drift-wt (eq-shell)~~ REMOVED | fix/tenant-drift-resilience (merged, deleted) | Claude (harden check-tenant-drift.mjs against one unreachable tenant killing the required gate for every PR) | 2026-07-16 | DONE — **PR [#878](https://github.com/eq-solutions/eq-shell/pull/878) MERGED** (squash `37b805a` → main) → core.eq.solutions auto-deploying. `fingerprint()`'s per-tenant Management API call is now caught individually instead of throwing uncaught out of `mapWithConcurrency`; only errors matching a known dead-project signature are treated as informational "unreachable" (a real SQL/schema bug on a live tenant still fails loud) — muster (single lens, codex-rescue unavailable) flagged both the blanket-catch risk and an inconsistent `--reference`-to-dead-tenant hard-fail, both fixed before merge. Root eq-shell checkout was mid-collision with a concurrent session (branch had switched to `chore/remove-dead-vendored-platform-apps` under me) — isolated the work into this worktree via stash rather than disturb the shared root. Worktree + branch pruned. |
| ~~C:\Projects\eqsvc-terms-takedown-wt (eq-solves-service)~~ REMOVED | claude/terms-page-takedown (merged, deleted) | Claude (temporarily unpublish /terms — Adam/SKS meeting prep, unreviewed legal draft) | 2026-07-16 | DONE — **PR [#543](https://github.com/eq-solutions/eq-service/pull/543) MERGED** (squash `b587d84` → main) → eq-service.netlify.app auto-deploying. `/terms` now `notFound()`s; prior content preserved in git history for restore after legal review. Worktree removed. |
| ~~C:\Projects\eq-shell-dupes-merge-wt (eq-shell)~~ REMOVED | claude/dupes-tab-merge (merged) → claude/dupes-usage-check (merged) | Claude (wire merge into the Sites Dupes tab, then a usage-based survivor pick) | 2026-07-16 | DONE — **PR #876 MERGED** (`3c4831e`, migration 0186 `eq_site_advisory_flag_pair`, APPLIED to ehow). Follow-up **PR #880 MERGED** (`71fac54`, Royce's "merge both, no review" go) → core.eq.solutions auto-deploying. Migration **0187** (`eq_site_dupe_usage`) — Royce's explicit go, dispatched via One Pipe, approved on the production environment gate, **CONFIRMED LIVE on ehow** by direct query. Usage-first survivor pick + 3+-row dupe group support now fully callable in production. Worktree removed. |
| ~~C:\Projects\eq-intake-dupes-merge-wt (eq-intake)~~ REMOVED | claude/dupes-tab-merge-client (merged) → claude/dupes-usage-check-client (merged) | Claude (flagSitePairForMerge, then getSiteDupeUsage) | 2026-07-16 | DONE — **PR #71 MERGED** (`b48cbdd`). Follow-up **PR #73 MERGED** (`6052a09`) — fully green (15/15 tests). Worktree removed. |
| ~~C:\Projects\eq-shell-site-merge-wt (eq-shell)~~ REMOVED | claude/site-merge-rpc (merged, deleted) | Claude (site-duplicate merge RPC — migration 0185) | 2026-07-15 | DONE — **PR [#868](https://github.com/eq-solutions/eq-shell/pull/868) MERGED** (squash `c3a337f`, Royce's "merge both, no review" go) → core.eq.solutions auto-deploying. `eq_site_merge_preview`/`eq_site_merge_execute`: repoints ~26 FK tables into `app_data.sites` from loser→survivor, soft-retires the loser, requires a recorded 'same' verdict + active 'manager' role. Validated live via BEGIN…ROLLBACK on ehow, zero trace left. Console: Preview→Confirm merge flow, gated by new `intake.sites.merge` permission (manager-only). Paired eq-solves-intake **PR #70 MERGED** (`previewSiteMerge`/`executeSiteMerge` client wrappers, 6/6 tests). **Migration 0185 APPLIED to ehow 09:23Z** (Royce's "merge and deploy" go, via tenant-migrate.yml workflow_dispatch) — `eq_site_merge_preview`/`eq_site_merge_execute`/`site_resolution_merge_log` confirmed live on ehow. Same dispatch also applied to `eq` tenant. `favour-perfect` tenant failed on an UNRELATED pre-existing migration (0184 — `pg_cron` extension not installed on that project) — chip task_2a98bf3b. Both worktrees + branches pruned. |
| ~~C:\Projects\eqsvc-cal-renum-wt (eq-solves-service)~~ REMOVED | claude/calibration-consolidation-backfill | Claude (eq-service #534 backfill renumber 0183→0186 + ledger reconcile) | 2026-07-14 | DONE — ephemeral worktree pruned same session. #534 rebased onto main + backfill renamed `0183_`→`0186_` (pushed `9cde375`); `service._eq_migrations` reconciled with a `0186_backfill_asset_calibration_from_assets.sql` row (out-of-band apply, sha256 `e6be587d…`). Ledger now mirrors repo 1:1 → `apply-service-migrations` shows 0 pending. PR #534 green on real gates (tsc+build, Plan). **NOT merged — Royce's call.** Backfill data already live (18 rows). Temp local branch `claude/cal-backfill-renum-tmp` deleted. |
| ~~C:\Projects\eq-shell-cal-phase3-wt (eq-shell)~~ REMOVED | claude/calibration-phase3-shell-consumers (merged, deleted) | Claude (calibration Phase 3 — 3 legacy consumers) | 2026-07-14 | DONE — **PR [#861](https://github.com/eq-solutions/eq-shell/pull/861) MERGED** (squash `b1f7267`, Royce's explicit deploy go) → core.eq.solutions deployed. Retired legacy `assets` calibration reads/writes: canonical-api `WRITABLE_FIELDS.assets` (drop 3 fields), entity-patch `asset` (drop next_service_due), briefing-engine `fetchServiceDue` (read asset_calibration.calibration_due). Rebased over #860 (audit-2b, same 3 files, no conflict); tsc -b + vite build + all CI green. Paired eq-service **#534 MERGED** by Royce (`42c9ae4`) — backfill live (18 rows), ledger reconciled (`0186_backfill…`). Worktree + local/remote branch pruned. |
| ~~C:\Projects\eq-shell-docs-resend-wt (eq-shell)~~ REMOVED | claude/docs-resend-live (merged, deleted) | Claude (docs: Resend-live truthing across runbooks + comments) | 2026-07-14 | DONE — **PR [#850](https://github.com/eq-solutions/eq-shell/pull/850) MERGED** (admin-squash `ff2f3d0` → main, Royce's go) → core.eq.solutions auto-deploying. Docs + comments only, no behaviour change. Admin-bypass was needed because the required drift gate was red on an UNRELATED live SKS violation (`service.instrument_calibration_events` RLS-disabled, issue #851, chip task_2c8986b2) — my PR had zero schema content. Isolated off origin/main via stash→worktree to avoid entangling with the concurrent `claude/compliance-register-xlsx` checkout. Worktree + local/remote branch pruned. |
| ~~C:\Projects\eq-shell\.claude\worktrees\adjudicate-0183 (eq-shell)~~ PRUNABLE | claude/site-adjudicate-0183 (merged, deleted) | Claude (site-resolver learning loop — migration 0183 + vendored console) | 2026-07-14 | DONE — **PR #848 MERGED** (squash `654cf6f`) → core.eq.solutions auto-deploying. Migration **0183** (append-only `site_resolution_verdict` + `eq_site_advisory_adjudicate` RPC + summary verdict-join), validated BEGIN…ROLLBACK on ehow (4/4 probes). Drift gate cleared (the calibration-view red was #849, concurrent). **0183 apply = One Pipe, GATED ON ROYCE — not yet dispatched** (classifier blocked auto-dispatch as prod). Worktree prunable. |
| ~~C:\Projects\eq-intake\.claude\worktrees\adjudicate-console (eq-intake)~~ PRUNABLE | claude/site-adjudicate-console (merged, deleted) | Claude (site-resolver learning loop — @eq/intake adjudicate writer + adjudicable console) | 2026-07-14 | DONE — **PR #68 MERGED** (squash) → eq-intake main. `adjudicateSiteAdvisory()` writer + verdict types + 9 tests; SiteAdvisoryPanel Same/Different/Unsure buttons. check:packages + test green. Vendored into eq-shell #848. Worktree prunable. |
| .claude/worktrees/unruffled-noyce-657c65 (eq-solves-service) | claude/rls-initplan-fk-index-migration + claude/rls-initplan-normalize-remaining (both merged, deleted) | Claude (repo-access audit → RLS initplan perf fix + FK indexes) | 2026-07-20 | DONE — **PR [#568](https://github.com/eq-solutions/eq-service/pull/568) MERGED** (squash, Royce's "apply the migration to ehow" go) + **PR [#569](https://github.com/eq-solutions/eq-service/pull/569) MERGED** (squash, Royce's go via AskUserQuestion). Full-repo audit surfaced 28 `service.*` RLS policies calling `auth.jwt()`/`auth.uid()` unwrapped (re-evaluated per row instead of hoisted into a one-time InitPlan) + 12 missing FK covering indexes — both fixed via **migration 0187**, validated BEGIN/ROLLBACK against live ehow before commit, **APPLIED to ehow** via the governed `apply-service-migrations` dispatch (ledger: `gh-actions:Milmlow`). Post-apply advisor re-run found 3 residual initplan hits (`asset_defects_tenant`/`client_groups_tenant`/`client_group_members_tenant`) — not a real regression, just a differently-shaped scalar-subquery wrap the advisor's pattern-match didn't recognize; normalized + **APPLIED via migration 0188**. Final advisor pass: **0 auth_rls_initplan + 0 unindexed_foreign_keys in service schema** (22 unused-index + 5 multiple-permissive-policy findings remain, pre-existing, out of scope). Same audit pass also merged 4 dependabot PRs (#564-567, ships a postcss XSS fix), #520 (resend bump), and #524 (Royce's balloon-years proposal doc — not stale, migration 0182's own comment cited it by path and it didn't exist on main yet). Original session branch `claude/repo-access-a4d2f7` had zero unique commits (never used) — deleted. **CORRECTION (same session, re-checked before handoff): `.claude/worktrees/unruffled-noyce-657c65` was NEVER a real git worktree** — `git rev-parse --git-dir` from inside it resolves to `C:/Projects/eq-solves-service/.git` (no `.git` file in the folder itself), and it's absent from `git worktree list`. Every git command run "in" it this session (branch/commit/push/checkout) actually landed on the **shared root checkout**. There is nothing to `git worktree remove` — the command would error, it was never registered. Root checkout has been returned to `main` (fast-forwarded, clean) and this session's 2 local branch refs deleted; the ~196 other pre-existing stale local branches on the root checkout were left untouched (separate, much bigger cleanup, not this session's scope). See memory `eq_solves_service_fake_worktree_shared_checkout` for the durable lesson. |
| ~~C:\Projects\eq-field-spinner-pin-wt (eq-field)~~ REMOVED | claude/spinner-css-version-pin (merged) | Claude (eq-ui session — Field's Spinner CSS hand-port had no version marker and no CI check; steelmanned an automated CI drift-guard and rejected it — a CSS-surface diff wouldn't have caught the actual bug, which was a JS prop inversion — in favor of two doc-only changes) | 2026-07-26 | DONE — **PR [#541](https://github.com/eq-solutions/eq-field/pull/541) MERGED** (squash `a544285`, Royce's "merge it once it's green" go). eq-field's CI was initially blocked before the job could even start — GitHub Actions billing/spending-limit issue on the org account, not a code problem — Royce fixed the billing, the previously-blocked run was re-triggered via `gh run rerun`, came back green, merged. Companion **PR [#29](https://github.com/eq-solutions/eq-ui/pull/29) MERGED** on `eq-ui` (`RELEASING.md`, squash, needed an added empty changeset to satisfy the changeset-present gate since it's a docs-only change with no release). Worktree removed. |
| C:\Projects\eq-field-mobile-polish-wt (eq-field) | claude/mobile-ui-polish | Claude (mobile device-pass polish batch — items 1/3/4/5/7/8/9) | 2026-07-13 | ACTIVE — **PR [#474](https://github.com/eq-solutions/eq-field/pull/474) OPEN** (v3.5.314). Seven device-pass fixes: [1] drop ◉ glyph on the "this week" `<option>`; [3] **root-cause** "↑ Saving…" pill hang — a 4xx write threw without releasing `_pendingWriteCount` (supabase.js) + 15s watchdog; [4] week-nav jumped across gaps (13.07→20.04) — picker now seeds a symmetric ±16-wk contiguous window (browser-proven: prev=06.07, max neighbour gap=7d); [5] Labour Hire stat-card wrench→hard-hat; [7] remove Timesheets helper paragraph; [8] hide EQ Agent FAB; [9] retire Prestart dual-source banner. Rebased through the #471/#472/#473 release train (main was 3.5.310→313 mid-session). node --check clean, preview green. NO deploy until Royce merges. Worktree prunable after merge. |
| ~~C:\Projects\eq-field-mobile-overflow-wt (eq-field)~~ PRUNABLE | claude/mobile-overflow-clamp (merged) | Claude (mobile-reflow slice 3 — overflow-x:clip body guard) | 2026-07-13 | DONE — **PR #471 MERGED** (`8776777`, v3.5.310) → field.eq.solutions LIVE. CSS-only `overflow-x: clip` on html,body (both @media 768px + pointer:coarse shell-mode); browser-verified 0px page overflow + #mobile-nav stays fixed. Worktree + branch prunable. |
| ~~C:\Projects\eq-field-prestart-mobile-wt + eq-field-prestart-feedback-wt~~ REMOVED (eq-field) | claude/prestart-mobile-submit + claude/prestart-add-feedback (both merged, deleted) | Claude (mobile prestart couldn't-submit — device pass 2026-07-13) | 2026-07-13 | DONE — **#468 (`6333737`, v3.5.308)** Submit explains why it's blocked + `_qPersist` surfaces server rejections; **#473 (`330c285`, v3.5.313)** crew-add "Added <name>" toast + scroll + retire "What's new". Both MERGED (Royce's go) → field.eq.solutions LIVE. #473 rebased through the #469→#472 concurrent mobile-reflow release train. Both worktrees + branches pruned. See memory [[mobile-foundation-program]]. LESSON: eq-field version stamps are same-line conflict magnets + auto-merge is DISABLED. |
| ~~C:\Projects\eq-shell-crm-sitecontact-wt (eq-shell)~~ REMOVED | claude/crm-add-site-contact-link (merged, deleted) | Claude (fix: add_site writes contact_site_links site_contact so contact sticks without an Edit) | 2026-07-12 | DONE — **PR #770 MERGED** (squash `de1be77` → main, Royce's go) → core.eq.solutions auto-deploying. Isolated off origin/main to dodge a shared-root collision with a concurrent phone-E164 agent (branch claude/crm-contacts-phone-e164, since merged as #769 `6c542a2`); restored their HEAD first so their commit landed on their own branch. Worktree + local/remote branch pruned 2026-07-12. |
| C:\Projects\eq-shell (ROOT checkout — no worktree) | claude/crm-contacts-phone-e164 (merged; remote deleted, local remains) | Claude (CRM-contacts phone→E.164: toAuE164 on contacts.mobile_phone) | 2026-07-12 | DONE — **PR #769 MERGED** (squash `6c542a2` → main, Royce's go) → core.eq.solutions (deploy `6a52e03a` ready) + **live-verified**: edited Rhos Thomas (Digital Realty) via Edit-contact UI, typed `0429 315 657` → DB stored `+61429315657`, updated_at bumped. Change = crm-write add_contact/update_contact + entity-insert/entity-patch contact branch; canonical-api M2M gateway left as-is (matches staff precedent). Backfill applied live: ehow 25 + zaap 100 → 100% E.164. Worked in shared ROOT (no worktree); hit the recurring shared-checkout branch race with the concurrent #770 agent (row above) — root HEAD drifted onto their branch mid-session then restored to mine, committed with branch-verify guards. Local branch still checked out at root (harmless, squash-merged — prunable). |
| C:\Projects\eq-shell-cpledger-wt (eq-shell) | claude/tombstone-cross-plane-migrations | Claude (foreman: control-plane ledger + follow-ups) | 2026-07-11 | DONE — #729 (ledger doc) MERGED; 2026_06_27b revoke APPLIED to live jvkn (verified true→false); #730 (tombstone 3 cross-plane files) MERGED `b952ecb`. Worktree + local branch pruned 2026-07-11. (eq-field net #438 worktree also already pruned.) |
| C:\Projects\eq-shell-perf-tier1-wt (eq-shell) | claude/perf-cold-open-tier1 | Claude (foreman: nav-speed Tier 1) | 2026-07-11 | ACTIVE — **PR #740 OPEN** (immutable `/assets/*` cache + gate/sample analytics to cut the ~3.2s cold-open). tsc -b + vite build + eslint green. Branch = Netlify preview only; NO production deploy until Royce merges. Worktree kept (may reuse for Tier 2). |
| ~~C:\Projects\eq-shell-phone-otp-selfheal-wt (eq-shell)~~ REMOVABLE | claude/phone-otp-approval-selfheal (merged, remote deleted) | Claude (SKS NSW Labour → EQ Field migration session — self-heal `shell_control.users` provisioning in `shell-login-phone-otp.ts` for workers approved via the invite path before ever signing up in Cards) | 2026-07-24 | DONE — **PR [#992](https://github.com/eq-solutions/eq-shell/pull/992) MERGED** (squash `d6dcb0d` → main, Royce's merge, 6/7 checks — the 7th was a non-required Netlify preview check). Netlify auto-deploying main → core.eq.solutions now. Staff linked to an approved `cards_field_approvals` row but no `shell_control.users` yet gets one provisioned on next login attempt, role defaults `employee`. Decision logic in `cards-approval-selfheal.ts`, unit-tested (5 new tests). **Not yet live-verified against a real gap-case account** — do that before telling any SKS worker to try logging in. Worktree removable. |
| ~~C:\Projects\eq-shell-invite-role-fix-wt (eq-shell)~~ REMOVED | claude/cards-invite-role-fix (merged, deleted) | Claude (follow-on from the #992 session — the invite-approval path's own attempt to provision `shell_control.users` was hardcoding `role: 'worker'`, not a member of the `eq_role` enum, so that upsert has always errored silently and never actually provisioned anything) | 2026-07-26 | DONE — **PR [#1008](https://github.com/eq-solutions/eq-shell/pull/1008) MERGED** (squash `c0dd17f2`, Royce's merge). Fix: `role: 'worker'` → `role: resolveRole(role)`, reusing the helper the application-signup path already uses for the identical upsert; also added error logging on that upsert (it previously failed silently, which is how the bug went unnoticed). `tsc -b --force` clean; 229/229 tests pass; no new eslint errors. Live on core.eq.solutions. Worktree removed. |
| .claude/worktrees/sks-comms-resource-mgmt-7563e2 (eq-shell) | claude/comms-fortnight | Claude (NSW Comms Moves 1 / 1.5 / 2b / 2c / 3 / 4 / 5) | 2026-07-10 | ALL 7 MERGED: Move 1 #727 (d7e7787, 0169+153-job backfill live); 1.5+2b #731 (7013d34, filters + crew-booking→schedule_entries); 2c #738 (6a51784, fortnight capacity grid); 3 #741 (39ef617, roster=single crew source); 4 #742 (b4e9829, fortnight job agenda); **5 #744 MERGED (squash `9838993`) — comms-crew tag (0170 + is_crew + ManageCrew picker)**; 0170 APPLIED to ehow 02:05Z (table live, RLS-on/0-policy). **6 #747 MERGED (squash `03a723f`) — fortnight declutter (hide idle rows + this-week default).** ✅ **Staff-dedupe merge DONE + VERIFIED** (Royce's go): 16 Cards stub rows retired, 19 licences + 2 timesheets repointed, 2 logins moved → picker now 93/93/0 dupes. Reversal snapshot in transcript. Root-cause (Cards match-on-onboard) still open → task chip. **7 #748 MERGED (squash `8e10d3c`, net −107 lines) — crew = Field's existing 'Comms' team (11) NOT the parallel sks_comms_crew; read-only in comms + "manage in Field" hint; 0171 drops the redundant table (dispatch whenever).** Next: NV1-as-licence, cutover. |
| C:\Projects\eq-field-boot-overlay-wt (eq-field) | claude/field-boot-overlay-strand | Claude (fix stuck "Loading…" spinner — initApp() overlay stranding: guard 6 secondary loaders + finally-guaranteed hide + early isLeave() fallback) | 2026-07-10 | ✅ DONE — PR #435 MERGED (squash b0bc2c7, v3.5.284; rebased over #434 which took 3.5.283). Worktree + local/remote branch removed. |
| .claude/worktrees/contacts-segment-columns (eq-field) | claude/field-handoff-selfheal | Claude (dir reused after contacts PR #430 merged — Field handoff self-heal: silent-restore 'accepted' posts + REQUEST_SHELL_TOKEN boot recovery) | 2026-07-10 | DONE — PR #431 MERGED (v3.5.280), verified live on field.eq.solutions. Dir still checked out on the merged branch (harmless); removable. |
| .claude/worktrees/field-handoff-recovery (eq-shell) | claude/field-handoff-recovery | Claude (Shell twin: booted-no-hash grace window + one-shot auto-remint in FieldIframe.tsx) | 2026-07-10 | DONE — PR #718 MERGED (main c08ad61), CI green, Netlify auto-deploy. Worktree dir + branch removed. Follow-up nit: task_0ccec6e4 (restore-failed message reachability). |
| C:\Projects\eqsvc-retire-wt (eq-solves-service) | chore/retire-offsite-backup | Claude (issue #60 — retire eq-service backup.yml, superseded by eq-context) | 2026-07-04 | DONE — eq-service PR #438 MERGED; worktree + branch pruned |
| .claude/worktrees/tenant-hard-delete (eq-shell) | claude/tenant-hard-delete | Claude (Tenants page — hard-delete archived tenants) | 2026-07-04 | DONE — PR #642 MERGED `b7e87ad`, deployed + verified live; dir removable |
| .claude/worktrees/provision-stuck-cancel (eq-shell) | claude/provision-stuck-cancel | Claude (Tenants page — cancel/clear a stuck data-plane provisioning job) | 2026-07-04 | DONE — PR #641 MERGED (rebased on top of the concurrent hard-delete PR #642); dir removed |
| .claude/worktrees/tenant-page-admin-actions (eq-shell) | claude/provision-tenant-background-fn | Claude (fix: provision-tenant 504 — convert to background fn) | 2026-07-03 | DONE — PR #627 MERGED 09:47:09Z; dir removable |
| .claude/worktrees/dreamy-meninsky-7082ba (eq-shell) | claude/dreamy-meninsky-7082ba | STALE ROW — dir was reused by another session (now on claude/field-nomination-views-security-invoker); this row no longer describes its contents | 2026-07-03 | reused — do not trust folder name |
| .claude/worktrees/provision-identity (eq-cards) | claude/provision-identity | Claude (chip task_28defc38, Cards provision name/email) | 2026-07-03 | DONE — PR #118 open; dir removable after merge |
| .claude/worktrees/quote-pdf-fix (eq-shell) | claude/quote-pdf-fix | Claude (chip task_d40bcb24, Ops PDF export) | 2026-07-03 | DONE — PR #615 open; dir removable after merge |
| .claude/worktrees/ops-site-create-edit (eq-shell) | claude/ops-site-create-edit | Claude (Ops site create/edit session a81fe5c3) | 2026-07-03 | DONE — PR #616 open; dir removable after merge |
| .claude/worktrees/objective-pike-977d2a (eq-cards) | claude/fix-onboarding-flags-and-ocr-signal | Claude (eq-cards session, phone-dedup + UI polish + onboarding-flag fix) | 2026-07-09 | ACTIVE — found this dir's reflog carrying unrelated branches (worker-identity-track-b, notify-connection-name, share-licence-comment) from a concurrent session sharing the same folder; re-based own work onto a fresh branch off origin/main to avoid collision. Flagging for visibility — this folder is NOT exclusively this session's. |
| C:\Projects\eq-field-roster-save-fix (eq-field) | claude/roster-save-409-fix | Claude (port SKS roster save 409 self-heal fix to EQ Field — SKS v3.10.88) | 2026-07-10 | DONE — PR #423 MERGED (v3.5.272); root C:\Projects\eq-field checkout had unrelated uncommitted work (scripts/audit.js, scripts/supabase.js audit-revert canon patching) from another session, so used a fresh worktree off origin/main instead of touching root; dir removable |
| C:\Projects\eq-field-pipeline-pagination-wt (eq-field) | claude/pipeline-pagination-fix | Claude (paginate sks-pipeline.js/sks-pipeline-resource.js tender_enrichment/nominations/pending_schedule/tender_phases — same v3.10.91 pattern as SKS PR #58) | 2026-07-10 | DONE — PR #427 open (v3.5.276); root C:\Projects\eq-field checkout again has unrelated uncommitted work (scripts/audit.js, scripts/sks-pipeline.js) from another concurrent session, used a fresh worktree off origin/main instead; dir removable after merge |
| .claude/worktrees/contacts-segment-columns (eq-field) | claude/contacts-segment-columns | Claude (Contacts segment-aware columns: registry refactor + per-Group defaults + columns picker) | 2026-07-10 | DONE — PR #430 MERGED (v3.5.279; re-stamped twice — 277+278 taken by concurrent releases #428/#429 mid-review), verified live on field.eq.solutions; branch deleted; dir removable. Root-checkout collision note: a concurrent session committed onto this branch via the shared root checkout before the worktree isolated it — pattern is recurring (3rd time today), consider making worktrees mandatory for eq-field. |
| C:\Projects\eq-field-bootperf (eq-field) — REMOVED | claude/field-boot-perf (merged) | Claude (foreman: per-app nav-speed — Field boot) | 2026-07-11 | DONE — **PR #452 MERGED** (squash `8d3eca6`, v3.5.296) → field.eq.solutions deploying. JSZip idle-load + index.html `no-store`→`no-cache` + print.css media="print" + jvkn preconnect. Worktree + local branch removed 2026-07-11. |
| C:\Projects\eq-svc-bootperf (eq-solves-service) — REMOVED | claude/service-boot-perf (merged) | Claude (foreman: per-app nav-speed — Service boot) | 2026-07-11 | DONE — **PR #494 MERGED** (squash `356d743`) → Service deploying. Lazy-load posthog-js off the critical bundle + Sentry `bundleSizeOptimizations`. Merged with the known-flaky integration test red — `tsc + next build` passed (CLAUDE.md fact #6). Worktree + local branch removed 2026-07-11. |
| ~~C:\Projects\eq-shell-staffdup-wt (eq-shell)~~ REMOVED | claude/staff-dup-sync-match (deleted) | Claude (staff-dup root-cause Part A) | 2026-07-11 | NO-OP — verify-live found the worker→staff identity-match fix ALREADY shipped + deployed (eq-shell #724 `b6669c0`, live as workers-canonical-sync v8). Nothing to build. Worktree + branch pruned. |
| ~~C:\Projects\eq-cards-staffdup-wt (eq-cards)~~ REMOVED | fix/staff-dup-onboarding-match (merged, deleted) | Claude (staff-dup root-cause: phone/email adopt in eq_cards_admin_upsert_worker) | 2026-07-11 | DONE — **PR #147 MERGED** (squash `92f9f94`→main). Migration 0089 APPLIED LIVE to eq-canonical (Royce's go, version `20260711054738`); worktree + local/remote branch pruned. Data cleanup also done: 2 dup stub workers deleted + 5 orphan logins pruned (0 dup-worker groups remain). |

---

## Recently pruned (2026-07-19/20 — 6 eq-shell worktrees)

Digest flagged "1 stale worktree needs cleanup"; live check found 5 eq-shell worktrees whose PRs had actually merged (the doc's own "removed"/"removable" labels on 3 of them were already correct in text but the folders hadn't physically been deleted yet; a 4th — `eq-shell-comms-polish-wt` — had a genuinely stale "NOT merged" note, since corrected above; a 5th — `frozen-window-issue-58b6b2` — was never tracked as its own row, just listed as "not evaluated"). All re-verified live via `gh pr list` (not just the doc) before removal — clean working tree + no unpushed commits on every one. A 6th, `accept-invite-phonestub`, was left alone at first (PR #862 was genuinely still open) — its status was re-checked the next day, found merged + deployed live (commit `8cb5e9f`, confirmed via Netlify), and pruned the same way once confirmed.

| Folder | Branch | Merged as |
|--------|--------|-----------|
| .claude/worktrees/bold-brattain-688290 | claude/comms-dashboard-loadtime | PR #901 |
| .claude/worktrees/frozen-window-issue-58b6b2 | claude/perf-batch2 | PR #898 |
| eq-shell-accessmodel-c1-wt | claude/accessmodel-cluster1-pii-gate | PR #890 (+ #885) |
| eq-shell-comms-polish-wt | claude/comms-startby-estimate | PR #897 |
| eq-shell-dupes-merge-wt | claude/dupes-usage-check | PR #880 |
| .claude/worktrees/accept-invite-phonestub | claude/invite-accept-auth-id-reuse | PR #862 |

Local + remote branch deletion needed its own separate confirmation each time (worktree removal and branch deletion are gated separately, and each new instance — including the 6th, a day later — needed a fresh explicit go, not just "yes" carried over from the batch). All 6 fully clean now: local branches deleted, remote branches deleted (3 of the first 5 had already been auto-deleted at merge time; the 6th's remote branch needed an explicit retry after an initial classifier block, which cleared on its own). Nothing left behind — `git worktree list` on eq-shell shows only the root checkout.

---

## Recently pruned (2026-07-16 — 20 worktrees across 5 repos)

Full audit: every active worktree in eq-shell, eq-intake, eq-field, eq-cards, eq-solves-service checked for merged branch + clean tree before removal. Two `git worktree remove` calls without `--force` hit an interrupted-timeout mid-removal on the shared eq-shell root (heavy concurrent-session churn that session — root branch changed under the operator mid-task); recovered via `git worktree prune` + manual `rm -rf` of the orphaned leftover disk content once confirmed no longer git-registered.

| Folder | Repo | Branch | Merged as |
|--------|------|--------|-----------|
| 767-wire-check-css | eq-shell | armada/767-wire-check-css | PR #783 |
| .claude/worktrees/advisory-rpc | eq-shell | claude/advisory-rpc-anon-revoke | PR #832 |
| .claude/worktrees/dark-doc-logo | eq-shell | claude/dark-doc-logo | PR #866 |
| .claude/worktrees/sentry-fixes | eq-shell | claude/sentry-fixes | PR #673 |
| eq-shell-perf-tier1-wt | eq-shell | claude/perf-cold-open-tier1 | PR #740 |
| .claude/worktrees/eq-ops-mobile-view-af44a3 | eq-shell | (detached) | PR #875 (content match, squash) |
| eq-shell-onelogin-wt | eq-shell | main | pre-emptied husk, force-removed w/ Royce's go |
| eq-shell-onelogin-p5-wt | eq-shell | claude/one-login-p5-tenant-signup | MERGED PR #1081 — worktree removed 2026-07-28 |
| eq-shell-workerhome-cards-wt | eq-shell | claude/workerhome-roster-leave-prestart-cards | MERGED PR #1086 — worktree removed 2026-07-28 |
| eq-shell-apprentice-workerhome-wt | eq-shell | claude/apprentice-workerhome-routing | MERGED PR #1089 — worktree removed 2026-07-28 |
| eq-field-561-cachebust-wt | eq-field | claude/561-force-cache-refresh | MERGED PR #564 — worktree removed 2026-07-28 |
| eq-field-shell-role-fallback-wt | eq-field | claude/shell-field-role-fallback-fix | MERGED PR #561 — worktree removed 2026-07-28 |
| eq-shell-apprentice-fields-wt | eq-shell | claude/staff-apprentice-year-company | MERGED PR #1084 — worktree removed 2026-07-28 |
| .claude/worktrees/compassionate-goldberg-734468 | eq-shell | (detached) | pre-emptied husk, force-removed w/ Royce's go |
| eq-intake-export-tmp | eq-intake | (detached) | PR #71 (content match) |
| .claude/worktrees/advisory-console | eq-intake | claude/site-advisory-console | PR #67 |
| .claude/worktrees/ai-adjudicator | eq-intake | claude/ai-site-adjudicator | PR #69 |
| .claude/worktrees/jovial-rubin-0d0004 | eq-intake | claude/jovial-rubin-0d0004 | PR #41 |
| .claude/worktrees/sy9-duplicate-sites-6bab8b | eq-intake | claude/sy9-duplicate-sites-6bab8b | PR #66 |
| eq-field-pipeline-pagination-wt | eq-field | claude/pipeline-pagination-fix | PR #427 |
| eq-field-roster-save-fix | eq-field | claude/roster-save-409-fix | PR #423 |
| eq-field-sec | eq-field | claude/sec-cron-fn-auth | PR #463 |
| .claude/worktrees/contacts-segment-columns | eq-field | main | PR #430 |
| .claude/worktrees/wonderful-davinci-94d063 | eq-field | claude/one-login-field | PR #477 (content match, squash) |
| .claude/worktrees/provision-identity | eq-cards | claude/provision-identity | PR #118 |
| .claude/worktrees/asset-import-export-1fe110 | eq-solves-service | claude/asset-import-export-1fe110 | PR #529 |
| .claude/worktrees/unruffled-noyce-657c65 | eq-solves-service | main | already broken/dangling — `git worktree prune` |

**Left alone (do not touch without checking first):**
- eq-shell: `accept-invite-phonestub` (PR #862 open), `eq-shell-dupes-merge-wt` (uncommitted migration 0187 WIP), `.claude/worktrees/eq-roles-enterprise-eval-177343` (uncommitted migration 0186 WIP), `eq-shell-revendor-intake-wt` (PR #879 open)
- eq-intake: `eq-intake-dupes-merge-wt` (real uncommitted diff beyond its merged PR), `eq-intake-backport-wt` (new, concurrent session)
- eq-field: `eq-field-guard` (PR #466 open), `.claude/worktrees/bold-volhard-645dd7` on branch `land-472` (2 commits ahead of main, **never pushed, no PR** — flag before ever touching), `.claude/worktrees/unruffled-torvalds-03ce2f` (PR #375 merged + clean tree, but carries 2 untracked audit `.md` files that were never explicitly named for deletion — Royce's call to clear by hand)
- eq-shell also spawned several brand-new worktrees mid-session from other concurrent agents (`app-naming-wt`, `brand-handoff`, `frozen-window-issue-58b6b2`, `labour-hire-agency-grouped`, `labour-hire-modal-focus`, `sks-comms-resource-mgmt-7563e2`) — not evaluated, not touched.
- `.claude/worktrees/eq-ops-mobile-view-af44a3` (eq-shell) is down to an empty 8K husk but Windows reports the directory "busy" (file lock) — harmless, needs a machine-level look to actually delete, not a git issue.

---

## Stale (verify branch merged before pruning)

_None currently._

---

## Recently pruned (2026-07-20 — stale registry row, folder already gone)

Digest flagged "1 stale worktree needs cleanup," pointing at this table. Live check: `C:\Projects\eq-intake-ledger-wt` was already gone from disk and absent from `git worktree list` on `eq-intake` — some earlier session had already run the removal, but never updated this row. eq-solves-intake PR #58 confirmed merged (2026-07-03). Cleaned up what was actually left: the orphaned `claude/ledger-checksum-stamp` branch, local + remote (merged, unused by any worktree) — deleted both. Row removed.

---

## Recently pruned (2026-06-30)

Branch audit + prune: 166 local + 96 remote merged/redundant branches deleted (215→49 local). 5 orphaned worktree dirs removed + `git worktree prune`. Only `clever-wilson-161a7a` remains registered.

| Folder | Branch | Notes |
|--------|--------|-------|
| .claude/worktrees/sharp-gauss-e31cdd (eq-shell) | claude/sharp-gauss-e31cdd | **Stale Active row — branch actually merged via PR #549 + #553 (2026-06-30).** Dir held only vendored eq-intake/node_modules, no unique work. Branch deleted; directory removed 2026-06-30. |
| .claude/worktrees/determined-gauss-626b33 (eq-shell) | — | Empty husk (8K), no .git link, no matching branch. Directory removed 2026-06-30. |
| .claude/worktrees/eager-elion-6ebfcb (eq-shell) | — | Empty husk (8K), no .git link, no matching branch. Directory removed 2026-06-30. |
| .claude/worktrees/wonderful-dubinsky-41d96f (eq-shell) | claude/wonderful-dubinsky-41d96f | Empty husk (8K); **branch KEPT (active, unmerged)** — commits safe in object store, dir was just leftover. Directory removed 2026-06-30. |
| .claude/worktrees/determined-edison-d6f176 (eq-shell) | — | Re-appeared empty husk (8K); previously listed pruned 2026-06-28. Directory removed 2026-06-30. |

---

## Recently pruned (2026-06-28)

| Folder | Branch | Notes |
|--------|--------|-------|
| .claude/worktrees/determined-edison-d6f176 (eq-shell) | determined-edison-d6f176 | Emptied 2026-06-28 robocopy; directory removed 2026-06-28 housekeep |
| .claude/worktrees/sharp-kapitsa-93278c (eq-field) | claude/sharp-kapitsa-93278c | Emptied 2026-06-28 robocopy; directory removed 2026-06-28 housekeep |
| .claude/worktrees/objective-khorana-7248db (eq-shell) | claude/objective-khorana-7248db | PR #510 merged; directory removed 2026-06-28 housekeep |

## Recently pruned (2026-06-25)

15 orphaned local branches (no physical worktree dirs) deleted 2026-06-25:

| Branch | Notes |
|--------|-------|
| worktree-agent-a07832911731d680f | orphaned git ref only; dir never existed or already gone |
| worktree-agent-a0ff1a79e00f10b44 | orphaned git ref only |
| worktree-agent-a1016b614c561791a | orphaned git ref only |
| worktree-agent-a1e0b26c21febd3e5 | orphaned git ref only |
| worktree-agent-a22abb40f8237438a | orphaned git ref only |
| worktree-agent-a317aa6661a5f7c28 | orphaned git ref only |
| worktree-agent-a31895260dee4ab1b | orphaned git ref only |
| worktree-agent-a3e8cad7de9a023fc | orphaned git ref only |
| worktree-agent-a84649015a012d2a5 | orphaned git ref only |
| worktree-wf_f1c4afc6-761-4 | orphaned git ref only (worktree is on claude/d33a-icon-rail-d51-ui) |
| worktree-wf_0dccb7b6-553-6 | ref re-deleted (was listed as pruned 2026-06-22 but ref survived) |
| worktree-wf_0dccb7b6-553-8 | ref re-deleted (same as above) |
| worktree-agent-a1505563b4d31152d | ref re-deleted (was listed as pruned 2026-06-22 but ref survived) |
| worktree-agent-a4618efa0ee02cddc | ref re-deleted (same as above) |
| worktree-agent-ac706c691af65192a | ref re-deleted (same as above) |

## Recently pruned (2026-06-22)

The following worktrees were confirmed merged and removed:

| Folder | Branch | PR |
|--------|--------|----|
| .claude/worktrees/priceless-galileo-4131c2 | fix/sks-contact-links-pk | merged to main |
| .claude/worktrees/loving-blackburn-0b7cba | claude/worker-invite-email | PR #436 merged |
| .claude/worktrees/wf_0dccb7b6-553-6 | claude/polish-sentry-fixes | PR #394 merged |
| .claude/worktrees/wf_0dccb7b6-553-8 | claude/polish-admin-pages | PR #395 merged |
| eq-shell-intake-wt | claude/ui-1.5.0 | merged to main |
| eq-shell-button-wt | claude/eq-ui-shell-button | already gone |
| eq-shell-cleanup-wt | claude/b2-b6-b10-cleanup | already gone |
| eq-shell-w2-wt | claude/s2-shell-wave2 | already gone |
| eq-shell-ocr-wt | claude/ocr-parse-auth-gate | already gone |
| .claude/worktrees/jovial-hamilton-68009e | orphaned (2026-06-18) | git ref removed; dir removed 2026-06-22 |
| .claude/worktrees/vigilant-haslett-5bbbfb | orphaned (2026-06-16) | git ref removed; dir removed 2026-06-22 |

---

## Pruning a stale worktree

```powershell
# Verify branch is merged before deleting
git -C C:\Projects\eq-shell branch --merged main | Select-String "branch-name"

# Remove worktree (from the repo root, not the worktree folder)
# Note: git worktree remove errors with "Filename too long" on deep Windows paths.
# Fallback: remove git ref manually then robocopy-empty the directory.
$empty = "$env:TEMP\empty_rob"; New-Item -ItemType Directory $empty -Force | Out-Null
robocopy $empty "C:\Projects\<folder>" /MIR /NFL /NDL /NJH /NJS /NP
Remove-Item "C:\Projects\<folder>" -Recurse -Force
```
