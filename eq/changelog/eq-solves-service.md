---
title: eq-solves-service changelog
owner: Royce Milmlow
last_updated: 2026-08-16
scope: EQ Service append-only history. UNRECONCILED PAIR — see the warning at the head of eq/changelog/eq-service.md before citing anything here as complete.
read_priority: reference
status: live
---

# eq-solves-service changelog

## 2026-08-16 (PR #734 MERGED + deployed live — admin.ts stopped failing silently on a bad CANONICAL_SERVICE_ROLE_KEY)
- `admin.ts` fell back past an auth failure on the ehow/sks-canonical service-role key instead of erroring loud — found during the 2026-08-16 secrets audit's silent-failure sweep, same root-cause family as the eq-shell/eq-field/eq-cards fixes shipped the same day.
- eq-service [PR #734](https://github.com/eq-solutions/eq-service/pull/734), merged, live (Netlify auto-deploy on merge to `main`). A related, already-merged fix on the same file (PR #732) was found in progress during this pass and confirmed complete rather than duplicated.

## 2026-08-16
- **PR [#735](https://github.com/eq-solutions/eq-service/pull/735), merged and applied to ehow same day — P0: every Service write was enforced by app code only, nothing in the database beyond tenant match.** Verified live against ehow first: PostgREST exposes `app_data` directly alongside `service` (`PGRST106` lists both), so gating only the `service.*` view triggers would have been bypassable via `Content-Profile: app_data` — any signed-in tenant member, any role, could otherwise write maintenance checks/defects/test results/assets straight past all ~66 app-layer `canWrite()`/`canDoTestWork()` gates. Two migrations: `0208` adds role predicates to `app_data` RLS for the 19 objects whose view triggers are `SECURITY INVOKER` (closes both the view door and the direct door in one change); `0209` guards the 6 `SECURITY DEFINER` entities (sites/customers/assets/customer_contacts/site_contacts/instruments) inside the trigger body itself, injected into whatever's live at apply time rather than a pasted copy since those functions are under active concurrent maintenance (0199, 0202). Role tiers pulled from the app's real gate call sites, not invented — `defects`/`assets` needed the broader tier of two disagreeing call sites (ACB/NSX fail-marking, inline Add Asset) or a working flow breaks; `maintenance_checks` + children keep the app's `canWrite() || isAssigned` shape via a definer helper. Full `begin`/`rollback` dry run against live ehow for both files (nothing persisted): 51/51 and 16/16 gated respectively, all 35 triggers and all 6 `SECURITY DEFINER` flags intact afterward. Dry run caught a real bug pre-ship: 6 of 16 trigger functions anchor on lowercase `perform`, not `PERFORM` — fixed to case-insensitive match. CI green on the gate that matters (`tsc + next build`); `Integration tests (Supabase local)` fails at migration 0152 (pre-existing, 56 files upstream of this change, unrelated `app_data`-not-bootstrapped gap — confirmed via the actual failure log, not assumed). **Merged (`3d40d65`, squash) and dispatched the same session** — `workflow_dispatch` run succeeded in ~16s, both files committed to `service._eq_migrations` with matching checksums (verified via CI log and a live query, not assumed from a green job). Live spot-check post-apply: `maintenance_checks` now carries 4 split policies (was 1); all sampled trigger functions — including 2 of the 6 lowercase-`perform` cases — are still `SECURITY DEFINER` and now carry the injected guard. Security + performance advisors re-run post-apply per AGENTS.md: zero new ERROR-level findings; the two related WARNs are an intentional authenticated-callable `SECURITY DEFINER` helper (documented above, same shape as existing accepted helpers) and an `auth_rls_initplan` count bump from splitting 1 policy into 4 per table — confirmed by direct comparison that `contract_scopes`, the precedent this migration generalised, has the identical unwrapped-tenant-predicate shape today, so this is pre-existing schema-wide convention, not a regression. Still open: click-through with a real low-privilege account, which needs a live signed-in session Royce or a real user has to do.

> **⚠ This file has a live twin: `eq/changelog/eq-service.md`.** Neither is
> complete alone — this file carried no frontmatter and no self-awareness of
> the twin at all until 2026-08-15. That silence was the actual gap: PR #727
> got recorded as still-open in `eq-service.md` a day after it had merged,
> while THIS file recorded the merge correctly on the day — a session reading
> only the other one had no way to know a second, contradicting record even
> existed. Read both files. Consolidating them is a call about which product
> history survives; that stays Royce's, see the fuller warning in
> `eq-service.md`. This note only adds a pointer — no entry below is changed.

## 2026-08-16 (nav-visibility tier-inversion + missing service.view entry gate)
- **PR [#736](https://github.com/eq-solutions/eq-service/pull/736) open, not yet merged.** A P2 finding (nav built by exclusion — any role the code doesn't name gets full nav) arrived with file:line citations that turned out partly stale: 2 cited paths were wrong (`components/ui/Sidebar.tsx`, `app/(app)/settings/UserSettingsForm.tsx`, not the paths given), and the claimed missing-subcontractor-from-role-maps gap was already fixed on `origin/main` — caught by fetching and diffing live rather than trusting the citations. The branch this arrived on (`fix/integration-ci-app-data-bootstrap`) was 43 commits behind `origin/main` and held another session's uncommitted work (same state as the #735 entry above) — branched fresh off current `main` via a worktree instead, left untouched.
- **Real and fixed:** `Sidebar.tsx`'s `isTechnician = role === 'employee'`, and a hand-copied duplicate of the same predicate in `layout.tsx`'s embedded Shell nav bar (#729), both hid Records/Insight by excluding only `employee` by name — apprentice/labour_hire/subcontractor all fell through the single exclusion and saw both sections (the widest nav of any non-manager role), while employee (the actual on-site tech) was wrongly hidden from pages it can reach. Both now gate on `canViewRecords` (canonical `entity.view`), the same permission already enforced on the 7 pages Records links to (#716).
- **Real and fixed:** `service.view` (canonical grant: manager/supervisor/employee/apprentice, not labour_hire/subcontractor) was never checked anywhere — `app/(app)/layout.tsx`'s entry gate only checked for an active `tenant_members` row, not what it grants. Wired `canViewService()` as a real entry gate with a denial screen matching the existing `NoTenantGate`. Live-queried ehow directly (`service.tenant_members` grouped by role/is_active): 5 active rows, 2 manager/1 supervisor/2 employee, zero apprentice/labour_hire/subcontractor — confirmed zero effect on any current user.
- Also corrected a wrong claim sitting in this repo's own permission-enforcement drift-guard baseline (`tests/lib/utils/permission-enforcement-baseline.json`), which asserted `service.view`'s canonical grant was "every non-empty role tier" — it isn't. Dropped `service.view` from the baseline's dead-key list (20→13, matching the file's established pattern from #712/#713/#716/#717).
- `tsc --noEmit` 0 errors, full `vitest` suite 442/442 (77/77 for the roles+drift-guard files specifically), `npm audit --audit-level=high` 0 vulnerabilities. `npm run build` fails in-worktree on a Turbopack workspace-root error — confirmed pre-existing and unrelated by reproducing a (differently-shaped) build failure on a completely untouched sibling worktree.
- Two out-of-scope gaps found and spun off rather than fixed here: `/job-plans` has no view-level role gate at all (its 4 RECORDS_PATHS siblings got one in #716, job-plans didn't — `task_68813d6a`); `service.view_commercials` (a real canonical key, distinct from the tenant-level `commercial_features_enabled` flag) is completely unwired (`task_61bda775`). Both started running in separate sessions.
- **Merged (`8d556f9`) and confirmed live.** Squash-merged on Royce's explicit "merge the PR." CI's only red check (`Integration tests (Supabase local)`) independently re-verified as the same pre-existing, unrelated gap — fails applying an old migration (`schema "app_data" does not exist`) before ever reaching anything in this PR's diff, which touches zero migration files. Deploy verified via the actual Netlify deploy record, not assumed from the merge: the Netlify MCP's cached "current deploy" pointer was briefly stale (still showing #735's build) — cross-checked against the full deploy list instead, found the real one (`context: production`, `state: ready`, `commit_ref` matching the merge SHA exactly, created 2 seconds after merge).

## 2026-08-16 (Settings page: Profile/Password stopped working for anyone signed in through Shell)
- **PR [#733](https://github.com/eq-solutions/eq-service/pull/733) merged (`2f40f58`), deployed live.** Royce flagged UI on `core.eq.solutions/sks/service/settings` that "shouldn't be there" (a broken "Member Since: Invalid Date"). Root cause, confirmed live against `ehow` and `eq-canonical`: `service.profiles` is keyed by Service's own legacy `auth.users` id, but a Shell-embedded session's `user.id` is the canonical Shell id carried in the `eq_service_jwt` cookie — a different UUID on a different project entirely. The `profiles` lookup in `app/(app)/settings/page.tsx` always missed for embedded sessions, so Member Since/Last Login rendered blank, "Update Profile" silently no-op'd (0 rows matched, still reported success), and "Change Password" would have targeted an auth session that doesn't exist on `ehow`.
- Shell-embedded sessions now collapse Account/Profile/Password to a "Manage in Shell ↗" link, mirroring the existing Brand-deferral pattern already live in `TenantSettingsForm.tsx`. Standalone/demo logins are unaffected. `tsc --noEmit` clean (real check, after `npm ci` in a fresh worktree with no prior `node_modules`). Netlify production deploy confirmed live via commit-ancestry check (`commit_ref` on the newest ready production deploy matches the merge commit exactly), not just the deploy log/title. **Not click-tested live in either branch** — no valid Shell JWT available locally (would need a production secret not present in local dev config), and no working standalone test account either (the demo tenant's data was lost in the urjh→ehow cutover on 2026-06-22 and was never reseeded — `SignInForm.tsx` deliberately hides the demo tile for this reason, unrelated pre-existing gap, found while trying to test this fix).

## 2026-08-14 (NSX Test Report: dropped dead ACB-only fields that printed blank on every report)
- **PR [#731](https://github.com/eq-solutions/eq-service/pull/731) merged (`d015dd7`), deployed live.** Simon Bramall reported an ACB check's report came out empty in some sections. ACB's report wiring traced clean end-to-end — every rendered field has a real save path back to the workflow UI. The actual bug was on the NSX side: `lib/reports/nsx-report.ts`'s breaker-details table still carried three fields copy-pasted from the ACB template that the NSX workflow never collects (Performance Level, Protection Unit Fitted, Earth-Leakage Tripping Delay — confirmed live, `nsx_tests` has no earth-leakage column at all), plus an unconditional "Main Contact Resistance" table even though Royce deliberately removed that reading from NSX data collection back on 2026-05-27 (`6c7eb74`, "not typically performed on NSX/MCCB") — the report generator, written afterward (#644), was never updated to match. All four printed blank on every NSX report regardless of data completeness.
- Removed the three dead attribute rows (re-paired the remaining valid ones so there's no leftover gap in the table) and the whole Contact Resistance section. `tsc --noEmit` and `eslint` both clean. Netlify production deploy confirmed live, `commit_ref` matches the merge commit exactly, secret scan clean. CI's one failing check ("Integration tests (Supabase local)") is the same documented pre-existing `schema "app_data" does not exist` bootstrap gap as the #728 entry below — unrelated to this diff. Not click-tested against a real generated report — verified via typecheck + full code trace to the workflow UI only.

## 2026-08-14 (admin pages: closed the remaining unprotected /admin/* routes)
- **PR [#728](https://github.com/eq-solutions/eq-service/pull/728) merged (`fae2ee5`), deployed live.** The Admin sidebar entry was hidden for non-managers, but the `/admin/*` URLs had no read-side check — only the sidebar link and each screen's mutating actions gated. Any signed-in technician who navigated directly to `/admin` could view the hub, Users (full canonical roster incl. email/phone), Workspace Settings, Media Library, Report Settings, and Backup. Gated the last 6 of 9 admin routes (`page.tsx`/hub, `users`, `settings`, `media`, `reports`, `backup`) with `getApiUser()` + `isAdmin(role)` + `redirect('/dashboard')`, the exact pattern already live on `admin/archive` and `admin/quality`.
- **Deliberately left `admin/imports` and `admin/activity` untouched** — both already carry a narrower `canViewAuditLog` gate (manager + supervisor, PR #709, Royce-confirmed). A single blanket `isAdmin` layout gate over all of `/admin/*` would have silently re-tightened those two back to manager-only, undoing that decision — gated the 6 remaining pages individually instead of via a shared layout.
- `admin/users/page.tsx` already called `requireUser()` for tenant context; that helper checks auth, not role (a stale comment claiming otherwise was corrected) — only needed the `isAdmin` call added.
- Verified: `tsc --noEmit` clean, `eslint` clean, full `npm run build` clean, `permission-enforcement-drift.test.ts` ratchet 4/4 pass (no baseline change — `isAdmin` was already enforced elsewhere in the corpus). CI's one failing check, "Integration tests (Supabase local)", is the documented pre-existing `schema "app_data" does not exist` bootstrap gap — unrelated to this diff. Netlify production deploy confirmed live, `commit_ref` matches the merge commit exactly, secret scan clean. Not click-tested by a real non-manager account — no such login available in this environment.

## 2026-08-14 (session-expiry Server Action crash — EQ-SOLVES-SERVICE-D)
- **PR [#725](https://github.com/eq-solutions/eq-service/pull/725) merged, deployed live.** Session-expiry Server Action errors (a mid-flight POST redirected to `/auth/signin` when the session expires) now surface a clean "sign in again" message instead of an unhandled crash — found and fixed by a background agent dispatched from the suite-wide Sentry sweep, scoped to the maintenance module first. A concurrent session extended the fix to the remaining ~58 files/121 call sites suite-wide, merged as [#727](https://github.com/eq-solutions/eq-service/pull/727) (merged directly by Royce, `f7b378b`) — see this same day's `eq/pending.md` entry for the full write-up.

## 2026-08-13 (migrations dispatched live; mobile check-detail header overflow fixed)
- **Migrations 0206 (RCD 40ms threshold) and 0207 (backfill `maintenance_checks.deleted_at`) dispatched and confirmed applied live on ehow.** 0207 independently re-verified by direct query.
- **PR [#724](https://github.com/eq-solutions/eq-service/pull/724) merged, deployed live.** Royce reported 3 real phone screenshots of a completed maintenance check — action buttons unreachable, dead space at the bottom, reports wouldn't download. Root cause: `CheckDetailPage.tsx`'s header row (title + action buttons) never stacked below `sm:`, so the button block and title block fought for width — title squeezed to one word per line, button row (including the report-download buttons) overflowed off the right edge instead of wrapping. One-line class fix stacks the header on mobile. All three reported symptoms traced to this single bug. Netlify production deploy confirmed live and matches the merge commit exactly. Full click-through not possible in this environment (Shell-iframe auth unreachable here) — verified via `tsc --noEmit` plus a static Tailwind-class repro at 375px sent directly to Royce; a real phone check is still outstanding.

## 2026-08-11 (RCD: mobile fixes, threshold correction, in-app entry with photo/bulk-generate)
- **PR [#699](https://github.com/eq-solutions/eq-service/pull/699) merged (`212b4c9`).** The RCD circuit editor's desktop table forced horizontal scroll and sub-44px touch targets at phone/iPad width (measured: 809px of table content in a ~349px mobile viewport) — flagged by Royce ("there's a high chance our guys may use mobile / iPad for this feature"). Desktop table left unchanged, wrapped `hidden md:block`; new `md:hidden` card layout renders one card per circuit — header with circuit # + pass/fail badge + critical badge, 44px-tall timing-pair inputs, 24px button-test checkbox, 44px action/notes input, 20px critical-load override checkbox. Royce chose "full mobile card layout" over a narrower scroll-hint fix via AskUserQuestion. Verified via a static Tailwind-mirror geometry measurement (`getBoundingClientRect()`/`scrollWidth` against a faithful markup copy served over a real local HTTP server), not a live click-through — auth wasn't available in this environment, disclosed in the PR body.
- **PR [#701](https://github.com/eq-solutions/eq-service/pull/701) merged.** RCD's 5×-rated-current pass/fail threshold was genuinely wrong: migration 0203's 100ms limit cited "AS/NZS 3000 Table J1", which doesn't check out against any real source — the actual RCD product standard (IEC/AS 61008) and NZ's Electricity (Safety) Regulations 2010 both put it at 40ms. Checked live impact before fixing (zero rows affected — nothing had been tested since 0203 went live). Fixed via migration 0206. **Migration not yet applied to the live database** — needs Royce to manually dispatch `apply-service-migrations.yml`.
- **PR [#702](https://github.com/eq-solutions/eq-service/pull/702) merged.** Root-caused Royce's "the navigation on ACBtest doesn't allow you to open the check" report (3 real phone screenshots) — not a routing bug. The checks-list table (ACB/NSX's own hand-rolled version, and the shared `@eq-solutions/ui` Table component used app-wide) has no mobile column-width handling, so a long check name wraps one word per line on a phone and blows the row out to 8+ lines, reading as broken even though the tap handler was fine. Fixed the ACB/NSX list specifically: desktop truncates with an ellipsis, new mobile card layout below. The shared Table component's own gap is a separate, bigger `eq-ui` fix, not done here.
- **PRs [#703](https://github.com/eq-solutions/eq-service/pull/703)/[#704](https://github.com/eq-solutions/eq-service/pull/704)/[#705](https://github.com/eq-solutions/eq-service/pull/705) merged — RCD in-app entry workflow, built end to end.** RCD previously had no way to start a check on-site (xlsx import was the only path). New `/testing/rcd/create`: pick a site + board, then populate circuits three ways — one at a time, upload a photo of the switchboard schedule (new [eq-solves-intake PR #115](https://github.com/eq-solutions/eq-solves-intake/pull/115) endpoint, reusing the same vision-extraction pattern that already powers the Maximo PDF importer), or pick the board's circuit count from a dropdown to generate all the rows at once (#705, "make it easier" than adding per row — Royce's ask). One confirm screen regardless of source, RCD checkbox per row pre-ticked from the photo's own asterisk/keyword detection, always editable. #704 fixed 40px/32px touch targets down to the 44px standard once Royce asked directly whether mobile was "actually finished" and it wasn't quite. **`RCD_SCHEDULE_PARSE_ENDPOINT_URL` still needs setting in Netlify** for the photo-upload path to work; manual entry and bulk-generate work regardless.

## 2026-08-10
- **RCD (safety switch) circuit pass/fail computed and auto-defected for the first time.** RCD was the only test module with zero automated compliance signal — `rcd_test_circuits` stored trip-time ms values but nothing ever compared them to a limit, so a failed circuit never reached the defects register (unlike ACB/NSX/generic tests, which all auto-create a defect from a failed reading, migration 0148). New migration adds a `BEFORE INSERT OR UPDATE` compute trigger on `app_data.rcd_test_circuits` (30mA-rated circuits only, per AS/NZS 3000 Table J1 — 300ms max trip time at rated current, 100ms at 5×; other ratings get `is_pass = NULL` rather than a guessed result) and an `AFTER INSERT OR UPDATE` auto-defect trigger on `app_data.defects`, mirroring `fn_acb_reading_to_defect` exactly — including reusing that pattern's fix for a real trigger-firing gotcha (`AFTER UPDATE OF is_pass` never fires when `is_pass` is trigger-derived rather than named in the app's own UPDATE statement). Discovered mid-build that `service.rcd_test_circuits`/`service.defects` are canonical views over `app_data.*` (migration 0148), not the plain tables the original 2026-04-27 schema defined — caught before writing any DDL by checking live triggers, not assumed from the older migration file; also had to `CREATE OR REPLACE VIEW` both views since Postgres freezes a view's `SELECT *` column list at creation time. Report (docx) and the live RCD editor both now shade pass/fail with the same green/red convention ACB/NSX already use. Verified end-to-end against live ehow in a rolled-back transaction (every case: untested, pass, fail, nuisance-trip, unverified-rating; create-on-fail and auto-resolve-on-refix both confirmed) and by inspecting the generated docx's actual XML for the expected shading. [PR #694](https://github.com/eq-solutions/eq-service/pull/694) merged (`4fd704d`); migration 0203 dispatched via `apply-service-migrations.yml` and live-verified on ehow. **Not yet click-tested live in a browser**; threshold values are web-verified only, not checked against the primary AS/NZS 3000 text — both flagged for Royce.
- **Fixed a stale, self-contradicting comment in `apply-service-migrations.yml`, found while dispatching the RCD migration above.** One inline comment claimed the `production` environment pauses for a required-reviewer approval; the file's own header comment said the opposite. Re-verified live (`gh api repos/.../environments/production`): no required-reviewer rule exists, dispatch runs DDL against ehow immediately. [PR #695](https://github.com/eq-solutions/eq-service/pull/695) merged (`ed984ee`).
- **Closed a live cross-tenant info-disclosure gap in `service.next_variation_number`.** Migration `0140_harden_next_variation_number.sql` (PR #321, merged 2026-06-20) had never actually applied to ehow — its ledger row was falsely marked "applied" by the one-time 2026-07-03 grandfather backfill that bootstrapped `service._eq_migrations` (`checksum: null, applied_by: 'backfill-2026-07-03'`), so the governed pipeline's own runner silently skipped it on every dispatch since. Live impact: the RPC still had its old 2-arg signature (`p_tenant_id uuid, p_year integer`), EXECUTE-granted to `authenticated` — any signed-in user could pass another tenant's UUID and enumerate that tenant's variation-number sequence. Fixed by deleting the phantom ledger row and dispatching `apply-service-migrations.yml` (run [31381351162](https://github.com/eq-solutions/eq-service/actions/runs/31381351162)). Live-verified: the RPC now takes a single `p_year integer` arg, tenant derived from the JWT.
- **Audited all 172 migrations phantom-marked by the same 2026-07-03 backfill** before treating `0140` as an isolated case. Found: 4 others (`0102`/`0104`, `0126`/`0129`, `0089`) were already independently fixed by this repo's own PR #614 `--verify` tooling between 2026-07-08 and 2026-07-28; one more (`0136_lock_internal_identity_helpers.sql`) is dead code with nothing live to expose (its own target functions were never created either). No other open gaps.
- **`--verify` extended to catch the exact blind spot that hid `0140`.** [PR #696](https://github.com/eq-solutions/eq-service/pull/696) squash-merged (`0db9a83`) — for a migration that DROPs a function's old signature and CREATEs a new one, `--verify` now queries live ehow's actual signature and flags it if it still matches the pre-migration (dropped) form, same severity as a checksum mismatch. First implementation used `pg_get_function_identity_arguments`, assumed types-only per Postgres docs; verified live before trusting it and found it actually still includes parameter names, which would have silently made the check inert — switched to `proargtypes`/`format_type` instead. End-to-end validated in the PR's own CI run against real ehow credentials: `171 unverified · 0 MISMATCH · 0 SIGNATURE DRIFT`, correctly clean with zero false positives across the other 171 migration files. `tsc + next build` and `Typecheck + audit` both pass.

## 2026-08-06
- **PR #691 MERGED (`940323c`) — `canonical_outbox` retry job fixed a schema mismatch, not a credential problem.** `drainCanonicalOutbox()`/`enqueueCanonicalOutbox()` used `createAdminClient()` (pinned to the `service` schema) to query `public.canonical_outbox`, so every call 404'd — confirmed live the table itself was fine (`select count(*)` succeeds directly). Not a blanket client swap: the same function also writes `canonical_id` back to `service.customers`/`service.sites` (a separate pair of tables from `app_data.customers`/`app_data.sites`), which needed the `service`-scoped client kept intact. `enqueueCanonicalOutbox()` moved fully to `createPublicAdminClient()`; `drainCanonicalOutbox()` now holds two schema-pinned clients. `tsc --noEmit` clean, eslint clean, 16/16 tests pass (mock updated to route both client functions through the same spy). Verified live post-merge, not just merged: Netlify deploy confirmed matching the merge commit, then ehow's own API logs confirmed the cron's first post-deploy firing (22:55:19 UTC) returned 200 on the exact query that 404'd before. Real-world impact: none confirmed — the outbox was empty (0 rows) and no Sentry captures existed on the enqueue-failure path, so this closed the gap before a future transient sync failure would have needed it.

## 2026-08-03
- **PR #689 MERGED (`362f6dd`) — "Canonical types drift" CI check fixed, red since 2026-08-01T02:28Z.** Confirmed live against ehow that two columns were genuinely missing from the hand-maintained `lib/supabase/database.types.ts`: `service.media_library.entity_ids` (text[], migration 0200) and `service.sites.supervisor_id` (uuid, migration 0201/PR #679-681). Added both to the committed types; removed the `as any` cast + eslint-disable in `app/(app)/admin/media/actions.ts` that had been working around the first gap since 0200 shipped. `npx tsc -b --force` clean; `scripts/check-canonical-types-drift.mjs` run locally against a fresh live-schema snapshot confirms zero build-break drift (only the separate, pre-existing `tenant_settings.archive_grace_period_days` warning remains, out of scope). The PR's one red check, `Integration tests (Supabase local)`, is an unrelated pre-existing Supabase-startup schema-cache bug (`public.app_config` missing an `id` column, migration 0151) — confirmed unrelated before merging.

## 2026-08-02
- **PR #683 MERGED — `buildPmAssetReportInput` (`lib/reports/pm-asset-report-input.ts`) never read the site-supervisor feature (migration `0201`/PR #681) at all.** `supervisorName` was sourced from `maintenance_checks.created_by` (whoever internally created the check row — a tech/admin user, resolved via `userMap`), and `contactEmail`/`contactPhone` were hardcoded literal `'—'` unconditionally — completely disconnected from `sites.supervisor_id` → `site_contacts`. Found while checking a generated PM Check Report for site SY1 (Supervisor/Email/Phone all blank) and tracing the field instead of assuming missing data. Fixed: the site query now also selects `supervisor_id`; a new parallel fetch joins `service.site_contacts` on that id and reports its `name`/`email`/`phone` directly, replacing the `created_by` lookup and the hardcoded dashes. `reviewerName` (the separate internal sign-off field, "Technician / prepared by" section) was left untouched — it's a different concept (the EQ-internal reviewer) and was already correctly wired. `tsc --noEmit` clean; `pm-asset-report.smoke.test.ts` + `pm-asset-report-with-tests.smoke.test.ts` 4/4 pass (unaffected — they feed hardcoded input directly, not through `buildPmAssetReportInput`). Verified live against `ehow`: SY3 (`supervisor_id` set) now resolves Pradeep Singh's real name/email/phone; SY1 (`supervisor_id` null) correctly still resolves all three to `'—'` — confirming the earlier screenshot was accurate site data, not a symptom of this bug for that specific site.
- **PR #682 MERGED — `service.sites_update_trig()`/`service.assets_update_trig()` referenced `OLD.deleted_at`, but neither view exposes that column; every UPDATE through `service.sites`/`service.assets` has been silently failing since migration `0193` (2026-07-27).** plpgsql doesn't type-check field access on the implicit `OLD`/`NEW` trigger records at `CREATE FUNCTION` time (generic `record` type), so this shipped clean and only surfaced at runtime — `ERROR: record "old" has no field "deleted_at"`. First-ever exercise of the broken path: Royce's live test of the new site-supervisor picker (migration `0201`), reported as "Could not update site access — please try again" on site SY3. `service.audit_logs` confirmed zero successful `site`/`asset` update entries anywhere since `0193` — both triggers had been dead the whole six days, just never used. Root-caused via Postgres logs + `information_schema.columns` (confirming `deleted_at` absent from both views) + `pg_get_functiondef`. Migration `0199` (2026-08-01, unrelated PR #663) had already fixed the identical bug for `customers_update_trig()` — this migration (`0202`) applies the same fix (read `deleted_at` live from `app_data.*` instead of `OLD`) to the two triggers `0199` didn't cover. Dry-run verified in a rolled-back transaction against live `ehow` both before writing the fix and after deploying it — the second run exercised the actual live (fixed) trigger against the exact site (SY3) and supervisor (Pradeep Singh) from Royce's failed screenshot, confirming success. Applied via governed `apply-service-migrations.yml` dispatch (run `30726591629`). Not swept: whether the same "trigger references a column the view doesn't expose" bug class exists on any of the other ~22 canonical objects — this fix only covers the three objects (`customers`/`sites`/`assets`) touched by `0193`.
- **PR #679/#680/#681 MERGED — site supervisor assignment (migration `0201_site_supervisor.sql`) + Sites list Status→Show-archived fix.** `docs/SPRINT-2026-06-golive-hardening.md` marked SUPERSEDED (its "Import from Canonical" Phase 2 was never built, now architecturally dead under the read-only canonical view model). Sites list's always-static `is_active` "Status" column replaced with a URL-param "Show archived" checkbox + inline "Archived" tag. New `app_data.sites.supervisor_id` (plain uuid, no FK — same shape as `maintenance_checks.assigned_to`, verified live it carries no `auth.users` FK either, since Plan B JWT auth never creates a Supabase session), threaded through `service.sites`'s canonical view + `sites_update_trig()`, editable via a picker on `SiteAccessCard.tsx`. **First migration dispatch failed** (`42P16` — new column inserted before the `canonical_id`/`canonical_field_id`/`canonical_synced_at` trio shifted their view position; `CREATE OR REPLACE VIEW` requires exact name+position match for pre-existing columns); rolled back cleanly (verified live, no partial state), fixed by moving `supervisor_id` to genuinely be the last column, re-dispatched successfully (PR #680). **Picker's first version sourced from the wrong roster** — `getTechnicianRoster()` (SKS/tenant staff) instead of the customer's own site contacts; Royce caught it live. Rewired to `site_contacts` (already fetched for the same page's Contacts card); removed the now-unused `Technician`/`getTechnicianRoster` export from `lib/canonical-members.ts` (PR #681, no schema change needed). `tsc --noEmit` clean throughout. Royce still to click through live: assign a supervisor, save/reload, confirm it persists; toggle Show-archived and confirm it filters correctly.
- **PR #677 MERGED (`6589c18`) — PM Asset Report cover: brand band now always renders, site photo (if any) sits underneath it instead of replacing it.** `buildCoverPage()` in `lib/reports/pm-asset-report.ts` previously branched: `input.sitePhoto` present → render the photo + a caption strip with a duplicate customer-logo plate on the right; absent → render the flat brand-colour band with the customer logo centred. Royce, from a screenshot comparing the two states: keep the band, put the photo below it. Collapsed to: band always renders (unconditional, using `customerLogoOnDark`), then the photo (if present) renders below with a simplified site name/address caption only — the duplicate customer-plate on the caption strip was dropped since the band above it already carries that identity. Removed the now-dead `customerLogo` (light variant) local and its `customerPlateChildren` block. `tsc --noEmit` clean; `pm-asset-report.smoke.test.ts` + `pm-asset-report-with-tests.smoke.test.ts` 4/4 pass. Royce still to spot-check a generated report live for a site with a photo on file.
- **PR #678 MERGED (`216ee71`) — Media Library upload cap raised 2 MB → 5 MB.** Prompted by Royce hitting the limit uploading a real site photo screenshot (~2-3 MB typical for a building exterior shot). `MEDIA_MAX_SIZE` in `admin/media/actions.ts` + the matching error string + the upload form's helper text in `MediaLibraryClient.tsx` all updated together; no client-side size gate elsewhere in that component to also fix. `tsc --noEmit` clean.

## 2026-08-01 (cont.)
- **PR #676 MERGED (`9b033fb`) — closed the 8 `no-img-element` findings left over from PR #675, `next.config.ts` `images.remotePatterns` added.** Traced all 8 `<img>` sites (Sidebar brand logo x2, MediaPicker preview/dropdown/thumbnail x3, admin Media Library grid + edit-modal x3) to a single confirmed host — ehow Supabase storage, `logos` bucket, via `getPublicUrl()` in `admin/media/actions.ts` — added as the one `remotePatterns` entry. Every swap uses `unoptimized`: `MEDIA_ALLOWED_TYPES` permits SVG uploads (confirmed live — one grid tile even has an explicit `content_type?.startsWith('image/svg')` branch), and next/image's built-in optimizer refuses SVG without `dangerouslyAllowSVG` + a relaxed image CSP, not worth opening on this app's deliberately tight, carefully-documented CSP (`next.config.ts`) just for thumbnails. `tsc --noEmit` clean, `next build` succeeds, `npx eslint .` confirms all 8 gone with zero new findings (67 warnings / 19 errors, down from 75/19, the 19 unrelated pre-existing errors unchanged). Merged on green real gate (`tsc + next build`); `Integration tests (Supabase local)` failed, the standing accepted pre-existing failure. Royce still to spot-check the sidebar logo + Media Library page render live for a tenant with a logo set.
- **The other PR #675-deferred item (17 `no-explicit-any` on Supabase insert/update calls) turned out to already be known, documented team debt — not a fresh mystery.** Re-investigated properly this time instead of re-describing the earlier framing. Root cause: `lib/supabase/database.types.ts` is generated `--schema public` only (confirmed via a fresh `generate_typescript_types` pull — no `service` key present at all), but every server client in this app is constructed with `db: { schema: 'service' as any }` (`lib/supabase/server.ts:34,69`, already carrying its own `as any` + eslint-disable on the schema option itself) — so `.from('assets')` etc. type-check against a schema key (`service`) the generated `Database` type doesn't have, hence `never`. Confirmed live on ehow: `app_data.assets` is the real base table, `service.assets` is a view over it with its own INSTEAD-OF write trigger (`service.assets_update_trig`, referenced in `docs/HISTORY.md`) — not a simple auto-updatable view, so even a correct multi-schema type regen may still not emit Insert/Update types for it without extra handling. **This is `docs/30-day-plan.md` item B6, already flagged by the team with its own effort estimate** ("re-run `supabase gen types typescript` — they're stale"), and `docs/HISTORY.md` already documents the deliberate choice to leave it (register types are hand-written/Zod-validated instead). Not attempted this session — needs actual Supabase CLI + multi-schema flag (not available through the MCP tool used here, which only pulls `public`), plus per-site re-verification of ~17 call sites given the view's custom trigger. Royce confirmed: do item 2 only, leave this as the already-tracked B6 item.

## 2026-08-01
- **PR #672 MERGED (`8332cc9`) — fixed `npm run lint` crashing outright on a clean install.** Root cause: `package.json`'s `overrides.brace-expansion: "^5.0.8"` (a same-day CVE fix, mirrored from eq-shell #1158) forces brace-expansion v5's new named-export shape onto every consumer tree-wide, but the hoisted `minimatch@3.1.5` (a dependency of `@eslint/config-array`) and a nested `readdir-glob` copy at `5.1.9` both still call it as the old callable-function API — `TypeError: expand is not a function`. Confirmed by inspecting the installed module's actual exports and grepping every `dependencies['brace-expansion']` entry in the lockfile. Fixed by adding `"minimatch": "^10.2.6"` to `overrides` — the same major already resolved elsewhere in the tree (`glob`, `typescript-estree`) and already written for the new API — collapsing 3 fragmented minimatch majors into 1. Verified: crash reproduces pre-fix and is gone post-fix on a fresh `npm ci`; `npm run lint` now runs to completion (176,533 pre-existing findings surfaced); `npm test` 391/391 pass, no regression.
- **PR #673 MERGED (`dc9bcfb`) — the 176,533 findings PR #672 surfaced were 99.8% noise.** `eslint.config.mjs`'s `globalIgnores()` was a hand-written, non-recursive 4-pattern list that replaced `eslint-config-next`'s own defaults entirely — so eslint was walking every leftover build/vendor directory `.gitignore` already excludes from git but eslint never knew about: another session's own worktree build output nested under `.claude/worktrees/*/.next/` (154,244 findings), `node_modules/` (7,275), this repo's own custom-path build output `tmp/eq-next-build/` (7,957), plus two more stale build dirs (~2,900). Real first-party findings: 342. Fixed by switching to `@eslint/compat`'s `includeIgnoreFile()`, reading `.gitignore` directly — recursive by construction, can't drift out of sync with git's own ignore list again.
- **PR #674 MERGED (`f21fe6a`) — cleaned up the safe, mechanical majority of the real 342 findings: 342 → 125.** `catch (e) { ... }` → `catch { ... }` where the error was never read (~120 occurrences), dead imports/locals removed, one fully-orphaned helper function deleted, raw `'`/`"` in JSX text escaped to `&apos;`/`&quot;`, 2 stale `eslint-disable` comments removed. `tsc --noEmit` clean, 393/393 tests pass — independently re-verified after the automated cleanup pass, not just trusted.
- **Correction to this same day's earlier write-up**: the `react-hooks/*` findings above were described as "all in `AcbWorkflow.tsx`/`app/(app)/testing/acb/page.tsx` + the NSX equivalents" — wrong, an artifact of a grep that only caught single-line matches and missed ESLint's multi-line error-block format. The real spread (confirmed via ESLint's own `--format json`, not text-parsing): `react-hooks/set-state-in-effect` alone was in 25 files across the whole app (dialogs, calendar, sidebar, notification bell, dashboard — the standard load-on-mount pattern everywhere), plus 4 `purity` and 3 `preserve-manual-memoization` findings elsewhere and 1 genuinely different bug (`set-state-in-render` in `MergeContactsModal.tsx`). Worth remembering: parsing ESLint's human-readable "stylish" output with grep is unreliable for multi-line error blocks — use `--format json` for anything that needs to be counted or grouped correctly.
- **PR #675 MERGED (`db048e4`) — resolved the remaining 125: 125 → 94, every one now understood.** Investigated each category properly instead of leaving them as an unexplained pile. Found and fixed 5 real bugs, not just lint noise: `MergeContactsModal.tsx` was using `useMemo` purely to trigger a `setState` side effect (React doesn't guarantee that runs exactly once — replaced with React's own documented "adjust state during render" pattern); `CreateCheckForm.tsx` recomputed two default form dates on every render instead of once at mount; `AcbWorkflow.tsx` had a variable used inside a `useMemo` but computed outside it, missing from the deps array; `AttachmentList.tsx`'s effect read state via closure without depending on it (deliberately, to avoid a re-trigger loop) — switched to a ref so the intent is now explicit instead of accidentally correct; 7 stale `eslint-disable` comments removed. Also gave 9 `any`-typed spots real types (`window.clarity` ambient declaration, `Record<string, unknown>` for analytics property bags, `vi.mocked()` for a test mock, rest params replacing `arguments` in a vendor snippet) and fixed 2 `prefer-const` + 1 `jsx-a11y/alt-text`. **Config decision, confirmed with Royce first, not silently applied**: `eslint-plugin-react-hooks` 7.x's "recommended" preset (pulled in via `eslint-config-next`) escalated its React Compiler-readiness rules to error severity — this app hasn't adopted React Compiler (no `babel-plugin-react-compiler`, no `reactCompiler` config), so those rules were flagging the app's normal load-on-mount effect pattern and Server Component `Date.now()` calls (which run once per request, not subject to the client re-render purity concern the rule assumes) as hard errors. Downgraded `set-state-in-effect`/`purity`/`preserve-manual-memoization` to `warn` — visible, not silenced. **Left 19 findings alone, confirmed as 2 separate real architecture gaps rather than guessed at**: 17 `no-explicit-any` on Supabase insert/update calls — verified by actually removing one and reading the real `tsc` output, which came back `never`/`never[]` even though the client is correctly constructed with `createServerClient<Database>()`, meaning `database.types.ts` doesn't resolve a concrete type for these tables; and 8 `no-img-element` (`<img>` → `next/image`) — `next.config.ts` has zero `images.remotePatterns` configured, so swapping without that config first is a guaranteed production crash, not a cosmetic risk. Both need their own session. `tsc --noEmit` clean, 393/393 tests pass.

## 2026-07-30
- **PR #648 MERGED — Assigned To dropdown now sorts flat A-Z.** Both the maintenance-check inline editor and the New Check form were grouping technicians by role tier first (manager → supervisor → employee → apprentice → labour_hire) and only alphabetising within each tier — produced a "two stitched-together alphabetical lists" look, exactly matching Royce's screenshot. Dropped the role grouping in both duplicated `sortByRoleThenName` copies. `tsc --noEmit` clean.
- **Audited every report generator's actual Report Settings wiring (not the docs) and found real gaps.** Full toggle matrix: Customer Report / ACB Test Report / NSX Test Report / Customer Scope Statement / Customer Renewal Pack respect the full set (cover/executive-summary/sign-off/complexity); Compliance Report respects cover only; PM Check Report respects cover + executive-summary (no sign-off section exists); Field Run-Sheet respects sign-off only (cover is unconditional, no summary section exists); **Work Order Details respects none of the four.** Confirmed against a live downloaded Field Run-Sheet docx that Report Settings' cover/contents/executive-summary switches genuinely do nothing to that report type — not user error. `docs/FEATURES.md`'s Report Settings section was also stale (still described a "Customer logo"/"Site photos" cover toggle removed from the code and admin form on 2026-04-26). Two background tasks spawned to close this — outcomes below.
- **PR #649 MERGED — Work Order Details now respects sign-off; PM Check Report's complexity setting now actually does something.** Wired `report_show_sign_off` into Work Order Details' Technician Capture block, matching Field Run-Sheet. Separately found + fixed PM Check Report silently ignoring `report_complexity` — Summary tier now drops the itemised checklist for a pass/fail count, matching the admin UI's own description; Standard/Detailed unchanged (no fabricated differentiation the data doesn't support). Resolved the "should these reports grow full cover/contents/summary sections" question by correcting the settings-page copy and `docs/FEATURES.md` instead — both now state plainly that Work Order Details and Field Run-Sheet are fixed-layout reports where only sign-off is ever optional. `tsc --noEmit` clean, 9/9 report tests pass.
- **The sibling Field Run-Sheet background task made no changes.** It confirmed the identical gap, asked Royce directly whether Field Run-Sheet should grow full sections, got no reply, and closed cleanly rather than guess — superseded by PR #649's documentation resolution above. Reopening it (building real sections into Field Run-Sheet) remains available on request.

## 2026-07-29
- **PR #647 MERGED — fixed Secondary Injection labels on the ACB/NSX Test Report, plus a bigger sibling bug in the same lookup function.** Both `acb_test_readings`/`nsx_test_readings` store every electrical reading wrapped in `Electrical: ` by the save action (e.g. `Electrical: SI: Long Time - Trip Time (s)`) and every visual checklist row under `Visual Check: ` — but `findReading()`'s prefix-stripping regex only handled a bare `Visual:`, so it never matched `Visual Check: ...` at all. That meant #644's checklist relabel showed the right item names next to permanently blank results. Fixed the regex, and rewired the Protection/Trip Test Results tables (`buildProtectionResultsTable`/`buildTripTestTable`) to look up the real `SI: <Function> - <Field>` readings instead of a `Protection <name>`/`Trip <name>` label nothing ever wrote. Also replaced ACB's three invented quick-items (Operation Counter - Before/Castle Key Fitted/safety shutters) with the real Greasing/Op Counter/Racking fields, and dropped two free-text lines with no backing data. Verified the checklist fix 12/12 against live NSX data on ehow (was 0/12); no live check has reached the Electrical Testing/SI step yet, so the SI-specific fix is verified via a synthetic end-to-end docx generation using the exact real stored label format. `tsc --noEmit` clean.
- **PR #644, #645, #646 MERGED — ACB/NSX Test Report shipped with real data; Report Settings toggles extended to 3 more reports.** New per-check ACB/NSX Test Report (`lib/reports/acb-report.ts`/`nsx-report.ts` + matching input-builders/routes), following the existing three-file generator pattern. Live QA against the one real dataset on ehow found the dormant report template's checklist wording and CB-attribute lookups didn't match the real technician workflow (`AcbWorkflow.tsx`/`NsxWorkflow.tsx`) at all — relabelled ~35 checklist items and rewired ~20 breaker-attribute fields per generator to their real DB columns before shipping (#644). Report Settings' sign-off / cover page / executive-summary toggles, previously only respected by the Customer Report, now also apply to the Field Run-Sheet and PM Check Report (#645) and, after extracting the cover into `buildComplianceCover()`, the Compliance Report (#646). All default-safe (`?? true`) for tenants who've never touched Report Settings. Deferred: Secondary Injection test-result readings still don't match the report's lookup keys (same bug class, left out of scope); ACB path unverified against a real completed ACB check (none exist live yet).
- **PR #638 — Field Run-Sheet asset headers now show the maintenance plan's Job Code.** Royce shared a generated Run-Sheet (SY3, standard) and asked for it. Per-asset job plan lookup in `lib/reports/maintenance-checklist-input.ts` was already fetching the plan but only selected `id, name` — added `code` (the "Job Code" field, e.g. "SJPNL1") to that select and a new `ChecklistAsset.jobPlanCode`, rendered into the existing `ID | Location | WO` info line in `buildAssetSection` (`lib/reports/maintenance-checklist.ts`). Scoped to the standard PPM checklist only, per Royce's choice — ACB/NSX breaker cards, RCD test run-sheets, the PM asset report, and work-order-details all have the same gap, none touched this session. `tsc --noEmit` clean; CI pending at time of writing.

## 2026-07-26
- **PR #603 MERGED — `@eq-solutions/roles` dependency bumped v2.5.3 → v2.5.5.** Was 2 releases stale; confirmed a pure no-op before opening (v2.5.4/v2.5.5 only added an eq-shell-only ops permission and an eq-cards-only Dart package, neither touched by this repo's `can()`/`PermKey`/`EqRole` usage in `lib/utils/roles.ts`/`lib/types/index.ts`). `npm run check` (tsc+build) clean, 359/359 tests. Merged on local verification after a GitHub Actions org-billing gap (separate from the payment method — an Actions-specific spending cap left at $0) blocked CI from running at all; confirmed live on service.eq.solutions once the billing issue was actually resolved.
- **PR #604 MERGED — `@eq-solutions/ui` bumped v1.10.1 → v1.12.0** to pick up the new Tooltip/EmptyState/Pagination components. Purely additive here — this repo has no `DropdownMenu` usage, so the barrel-CSS bug fixed upstream in the same release (see eq-shell's changelog) never affected this app. `tsc --noEmit` clean, 359/359 tests, `next build` TypeScript phase clean.
- **PR #605 MERGED — closed the `sharp`/libvips + `uuid` npm-audit findings via `package.json` `overrides`, not a breaking `next`/`exceljs` downgrade.** Checked reachability first: `next/image`'s `<Image>` is never imported anywhere in this repo (the only `<Image>` import is a `lucide-react` icon); attachment/defect-photo thumbnails render via a plain `<img>` from signed Supabase Storage URLs, so no untrusted image ever touches the vulnerable `sharp` path. `exceljs`'s only `uuid` usage is a single `require('uuid').v4` call, confirmed identical under the patched version. Pinned `sharp@^0.35.3` + `uuid@^11.1.1` directly; `next`/`exceljs` themselves untouched. Merged via `gh pr merge --squash --delete-branch` (`e6e72fe`) on Royce's "merge #605 once CI is green," after confirming the real gate (`tsc + next build`) passed and the two reds were the same pre-existing accepted-risk failures every recent PR in this repo carries.

## 2026-07-24
- **PR #601, #602 MERGED — Excel-style checklist filters on the maintenance-check Assets table.** Location, Work Order #, and Maintenance Plan get a header filter icon (popover: search, Select all/Reset, checkbox list of unique values), same interaction as the new multiselect filter shipped in the shared `@eq-solutions/ui` Table component. Built locally rather than migrating this table onto the shared component — it's a bespoke Tailwind build with row selection, bulk actions, CSV export, and inline editing that would all need rebuilding through `Table`'s props. PR #602 added cascading (picking a Location now narrows what Work Order # and Maintenance Plan offer, matching Excel AutoFilter — Royce asked after confirming the base filter worked). Both deployed live to service.eq.solutions, **Royce confirmed working**.

## 2026-07-23
- **PR #581 — phone-only Shell accounts treated as first-class, not a crash.** A technician with no name/email (mobile phone-only sign-up) crashed the Maintenance page sort (`null.localeCompare`). Fixed with a shared `displayNameFrom(name, email, phone)` helper across Maintenance, Admin Users, Defects, and Audit log — falls back to phone number instead of blank/crashing. `CanonicalMember`/`Asset` types widened to match the real nullable shape.
- **Admin Users duplicate-account cleanup.** Brian Griffin-Colls had two active Shell accounts for the same tenant (phone-only + email-based). Verified zero real references to the stale one across both data planes, then merged. Swept all tenants for the same pattern — no other instances found.
- **PR #582, #584 — Asset # display fixed everywhere it was wrong.** Maintenance check detail, Assets list, and Asset detail pages were all showing the always-empty `maximo_id` field instead of the real, populated `external_id` (Asset # from the customer's import sheet). Fixed in all three places; `external_id` added to the shared `Asset` type. Added the missing Maintenance Plan column and an Export-to-Excel button on the check detail page.
- **PR #585 — scope-funding visibility on the check detail page and in reports.** New `lib/scope-context/asset-funding.ts` batch-resolves each asset's funding status (contracted / ad-hoc / not-funded / excluded) against `contract_scopes`, matching by asset pin first then by job-plan code. Amber "Not funded" badge on the check detail list + a visible count banner; a `hideOutOfScope` toggle (visible-by-default); threaded through to both the Field Run-Sheet/Print-Blank report and the Customer report, each stamping an amber "(NOT FUNDED)" marker inline rather than silently dropping the asset unless the toggle says otherwise.
- **Not yet fixed:** 47 asset names across 3 sites carry stray "Â" mojibake from a legacy import. The one-line `UPDATE` fix has silently no-op'd twice via the DB tool (non-deterministic write block, not a data or permissions issue) — needs Royce to run it directly in the Supabase SQL editor.
- **PR #591 MERGED — ACB/NSX Create Check screens were saving frequency as plain English ("Annual", "5 Yearly") instead of the short code ("annual", "5yr") every other part of the app expects, with no validation at all on that save.** One live check had already gone in wrong (a DigiCo NSX check) — confirmed against the live database before fixing, then corrected as part of the same migration (0192). Both ACB and NSX screens now save the short code while still showing the plain-English label; the missing validation is now in place. Deployed to service.eq.solutions.
- **PR #594 MERGED — Compliance Report now shows the tenant logo.** It was the one report generator with no logo plumbing at all after PR #592 added the running-header logo slot to the other 5 report types. Added `tenantLogoImage` to `ComplianceReportInput`, wired into `prepareShell()`; `/api/compliance-report` now fetches `tenant_settings.report_logo_url ?? tenants.logo_url`, same pattern as the other generators. Deployed to service.eq.solutions.
- **Confirmed, not a gap: the Maintenance/PM Check report's per-page header logo (PR #592) is genuinely live.** Checked by generating a real test document and inspecting its raw contents — the logo is embedded on every page, not just the cover — and by confirming production is running the exact commit that includes it. No fix needed.
- **PR #596 MERGED — a tab left open across a redeploy no longer crashes on its next action.** Root cause (Sentry `EQ-SOLVES-SERVICE-3`, 10 lifetime occurrences): Next.js server-action IDs are build-specific, so a stale tab invoking one after the server redeployed threw `UnrecognizedActionError` as a raw, unrecoverable crash. New `lib/utils/stale-action-error.ts` detects it via Next 16's own `unstable_isUnrecognizedActionError`; both `app/error.tsx` and `app/(app)/error.tsx` now show "a newer version of this page is available — refreshing…" and auto-reload (plus a manual button). Confirmed no separate job-plan-tied bug was hiding behind the earlier "crashes creating a check with a job plan" report — only this one error showed in Sentry for the whole repo. Deployed to service.eq.solutions.
- **PR #597, #599 MERGED — check detail page's Site editor was completely broken (silent 400).** `service.sites` no longer has a `primary_contact_id` column (contacts moved to a canonical join table in migration 0137) — the SELECT referencing it 400'd on every request, and the destructured `{data}` pattern silently swallowed the error, so Site always rendered "—" regardless of a valid `site_id`. Replaced with the sites view's own denormalized `site_contact_name/phone/email` fields. Also fixed Assigned To always showing "Unassigned": the technician dropdown offers canonical Shell ids, but the display-name lookup only ever checked the local `profiles` table — now resolves through the already-fetched canonical roster first. Both live-verified this session via direct DB query (site "CA1" + assignee "Simon Bramall" resolve correctly) and Royce's own screenshot of the working page. Also added editable Site/check-name fields and print-button loading spinners in the same PRs.
- **PR #600 MERGED — Field Run-Sheet header logo was the wrong, invisible variant + drifting position.** PR #599's header-logo fix reused the same Buffer as the page-1 navy masthead strip — deliberately the on-dark (white) logo, invisible against the header's white background (Royce caught this from Word's dark mode: "shouldn't that logo be the coloured one?"). Header now fetches the tenant's actual light/colour logo URL directly. Also replaced the header's tab-stop-positioned image with a 2-cell percentage-width table (text left, logo right-aligned) — tab stops don't reliably pin a trailing image's position across page sizes, which is why it was landing off-position. Verified via a standalone script against SKS's real live logo before merging. Deployed to service.eq.solutions.

## 2026-08-04
- **PR #690 MERGED (`40dea4c`) — two service-role queries had no tenant defense-in-depth.** Found by the eq-context tenant-rule audit (`non-negotiables.md` #11). `send-pre-visit-brief.ts` (the day-before-job briefing email, run by a cron with service-role access — RLS bypassed) filtered none of its 9 queries by tenant; checked ehow's live schema first rather than guessing — every table had `tenant_id` except `service.profiles`, correctly left alone. `archive/helpers.ts`'s `countDependencies()` gained a required `tenantId` parameter + filter on its one service-role branch, threaded through both call sites. Neither gap was ever exploited — both were safe only because the calling code happened to validate the tenant first, not because the functions defended themselves. `tsc --noEmit` (full non-incremental) + `eslint` clean; no test coverage exists for either function. Auto-deploys `main` via Netlify CD — live on the next build, no separate step needed.

## 2026-08-08 (empty "assign to" picker fixed; live security gap found in next_variation_number)

- **PR [#692](https://github.com/eq-solutions/eq-service/pull/692) merged (`770fbc97`).** `listTestingMembersAction` embedded `tenant_members -> profiles` via a PostgREST relationship that doesn't exist (`tenant_members` only has an FK to `auth.users`) — the unresolvable embed errored, and the code only destructured `data` (dropped `error`), so it silently returned `[]` for every tenant. Emptied the ACB/NSX Create Check "assign to" dropdown suite-wide. Rebuilt on the same canonical-first/local-fallback pattern already shipped on the Maintenance page's own picker. Live-verified the fixed query returns real members (5 for SKS) instead of the old silent `[]`. Not yet click-tested in the browser — dev server hung on an unrelated issue during the fix session.
- **Found, not yet fixed: `next_variation_number`'s hardening migration (0140) was never actually applied to ehow**, despite merging to main 2026-07-03 (PR #321). Its ledger row (`service._eq_migrations`) was falsely marked "applied" by the one-time grandfather backfill (`checksum: null, applied_by: 'backfill-2026-07-03'`) — the governed pipeline's runner treats it as done and silently skips it on every dispatch. Live right now: the RPC still has its pre-fix 2-arg signature, EXECUTE-granted to `authenticated` — any tenant can pass another tenant's UUID and enumerate their variation-number sequence. Fix is written and ready (the migration file itself); direct DDL apply was blocked twice by the Claude Code permission classifier. Needs Royce's own hands via the Supabase SQL editor, or a ledger correction so the normal pipeline picks it up.
- 131 stale branches deleted from this repo this session (of 470 suite-wide) — see `eq/pending.md` for the full breakdown, including 9 branches kept as real unshipped work (5 open Dependabot PRs + 4 others).
