# EQ Cards — Changelog

## 2026-07-08
- **Duplicate-worker phone gap fixed (commit `34e1b5f`, branch `claude/jovial-tu-de6995`, PR not yet opened) + 2 migrations applied live to eq-canonical.** Root cause of two unlinked "Sam Powell" worker records: scan-first onboarding's first profile write never carried the authenticated phone forward, so `profiles.mobile`/`workers.phone` could stay null forever for phone-OTP sign-ups even though `auth.users.phone` was populated — breaking phone-based dedup. Client fix: `profile_fill_from_licence_screen.dart` now passes the session phone into the draft. **Migration 0080:** `eq_cards_upsert_my_profile` gets a `COALESCE(..., auth.users.phone)` fallback (same pattern as 0077) + one-off backfill of existing null rows. **Migration 0081:** new `licence_last_changed_at` column on `org_access_requests` + trigger `mark_pending_requests_licence_changed()` on `public.licences`, so a worker updating a licence (e.g. adding a photo) while their connection request is still pending can be badged in eq-shell's review UI — previously nothing signalled this at all (`shell_control.cards_field_approvals` only ever gets a row at approval/rejection time, confirmed 0 pending rows live). eq-shell UI wiring is a follow-up (`task_309c92e5`), running independently.
- **PR #131 MERGED + DEPLOYED** (`5653093`): offline licence/certificate photos. New `OfflineCachedImage` (drop-in for `Image.network` at wallet thumbnail, licence card, licence detail) — online shows the live signed-URL image and best-effort caches the bytes; offline renders cached bytes via `Image.memory`, else the existing placeholder; keyed by the signed URL's stable path. New `photo_cache`: IndexedDB byte-store (web) on `package:web` + `dart:js_interop` (the SDK removed `dart:indexed_db`), base64 values, localStorage size ledger, **25 MB cap + oldest-first eviction**, requests persistent storage. Per-device + bounded to the worker's own photos (avg 183 KB; whole fleet 33 MB), best-effort (CORS/quota failure silently degrades to online-only). `flutter analyze` clean; widget tests green. Completes the offline wallet (ID card + tickets + photos all work with no signal).

## 2026-07-07
- **`ocr-licence` CORS drift reconciled — v10 LIVE** (PR #130 `75e0416`). The deployed function had drifted from source: `main` used the shared `_shared/cors.ts` (fail-closed, Netlify deploy-preview origins allowed) while deployed **v9** shipped an inline copy that fell back to `cards.eq.solutions` and blocked deploy previews. Redeployed **v10** on the shared module via Supabase MCP (both `index.ts` + `_shared/cors.ts` in the files array — sibling import resolves). Live preflight verified: deploy-preview + `cards.eq.solutions` echoed; unknown origin gets no allow-origin header (fail-closed). Repo `main` now == deployed byte-for-byte. **Deferred:** the Supabase CLI deploy path is blocked for this repo (config template-path bug, CLI 2.95.4) — task `task_61ff8686`.
- **PR #128 MERGED + DEPLOYED** (`71889a3`): the scan-first onboarding + OCR name auto-fill work below went live. `ocr-licence` edge fn redeployed to **v9** via Supabase MCP (holder_name from any card; `verify_jwt:false` preserved); Cards Build & Deploy green. Live-verified the from-scratch flow (dummy phone → name captured → pending application to SKS). **Sentry EQ-CARDS-10 resolved** (name-gate no longer captured as a crash; confirmed quiet).
- **PR #129 MERGED + DEPLOYED** (`a7808cf`): offline ID card + add-to-home-screen nudge. Profile now cached to localStorage alongside licences/certs (`ProfileNotifier` mirrors `LicencesListNotifier`'s cache-aware fetch), so the worker's **name** renders on the ID card offline — role/org/worker-id already come from the JWT. Photos stay online-only (signed URLs expire 1h; excluded from cache). New `AddToHomeScreenNudge` (once-only, dismissible, web + not-installed only; iOS/Android install steps via conditional-import `pwa_env` probe). `flutter analyze` clean; nudge widget test green. **Context:** sessions are already effectively permanent (132 live, oldest 48d, none time-boxed) — no auth change needed. **Fast-follow deferred:** cache licence photo bytes for offline image display.
- **Audit (no code change):** a manager can approve a worker with zero licences — the only gate anywhere is "must have a name" (P0023); no per-org required-credentials concept exists (parked). Minimum-requirements model left for Royce's call.

## 2026-07-06
- **PR #128**: scan-first onboarding + OCR name auto-fill + pending-application UX + Sentry noise fix. `ocr-licence` edge fn now extracts `holder_name` from **any** card (not just driver licences; DOB/address stay DL-only); a non-DL scan silently fills the empty profile name (name-only, never overwrites) so the ID card self-populates and the picker's name prompt self-skips. `FirstScanScreen` reordered **before** the company picker and gains a "No card on you? Enter what you know" manual path (blank `ProfileFillFromLicenceScreen`, now optional `licencePrefill`). Both onboarding gates guarded on `ModalRoute.isCurrent` — root-cause fix for the screen-stacking bug (child routes push over the list in the `StatefulShellRoute.indexedStack` branch, list keeps rebuilding underneath). Added "Can't find your company?" hatch to the picker and `OutgoingRequestsBanner` (worker's own pending application) to the wallet. **Sentry EQ-CARDS-10 fixed**: picker no longer captures the expected `ValidationFailure` (P0023 name gate / P0022 duplicate) as an exception — shows the real message. `flutter analyze` clean; company-picker (7) + first-scan (2) widget tests green. **Deploy needs: `ocr-licence` redeploy + Cards Build & Deploy.**
- **PR #122 (MERGED `d9fbce3`, deployed live via `deploy.yml` run `28751868975`)**: mobile-view audit fixes. Tap targets grown to 44×44 on the licence privacy-lock toggle (`licences_list_screen.dart`) and profile disconnect/open-portal buttons (`profile_screen.dart`, replacing `VisualDensity.compact`). Licence detail photos (`licence_detail_screen.dart`) constrained to the ID-1 card aspect ratio (1.586) instead of rendering at native upload size. Long display names now clip with an ellipsis instead of wrapping unbounded — ID card, profile header, and the join-tenant heading (2-line cap). `flutter analyze` clean, 207/207 tests passing. Live-verified the join-tenant ellipsis fix on the public `/join` route.
- **Full-codebase security audit**: clean across secrets, CSP, auth flows, RPC calls, PII/token logging, deep-links, storage paths. One doc-accuracy finding (not a vuln): STATUS.md's "service worker always unregistered" claim is stale — `web/index.html` only purges legacy SWs once, then a new SW stays registered for offline wallet support.

## 2026-07-05
- **PR #121 (MERGED, deployed live)**: cleared eq-cards' 3 unresolved Sentry issues. `provisionTenantExchange`'s 500-path Sentry capture (`EQ-CARDS-Z`) logged only the HTTP status, dropping the server's actual error string — now includes it (`lib/features/auth/data/auth_repository.dart`). Resolved `EQ-CARDS-X` ("Script error.", opaque cross-origin noise, no stack) and `EQ-CARDS-Y` ("Unable to load asset: NOTICES", Flutter web engine's own license-asset preload — app never shows a license page) by filtering both in `main.dart`'s Sentry `beforeSend`, since neither is app code nor actionable from what Sentry captures. `flutter analyze` (0 issues) and `flutter test` (207/207) both clean afterward. Merged fast-forward to `fb03a83`, manually deployed via `deploy.yml` (`6a4a1af4`), smoke-tested live on `cards.eq.solutions` — loads clean, 37/37 network requests 200, zero console errors.
- **PostHog dashboard "EQ Cards — Adoption" (id `794417`) built**: North Star (weekly active wallets), acquisition, activation funnel, retention, stickiness. Caught and fixed a misattribution bug in the first draft — `error_thrown`/`unlock_failed` turned out to be eq-shell/eq-field/eq-solves-service events (shared PostHog project), not Cards; removed the false "Health" tile. Linked from eq-shell's `/_platform/tenants` page (see eq-shell changelog).

## 2026-07-04
- **PR #119 (MERGED, deployed live)**: licence-photo download revoked its blob URL immediately after triggering the download in `image_download_web.dart`, racing the browser's own read of it on slower connections. Delayed the `revokeObjectUrl` call by 1s.
- **PR #120 (MERGED, deployed live)**: root cause of self-serve tenant provisioning's 0-ever-redemptions history, bug #1 of 3 (see eq-shell #638 for #2/#3) — `ProvisionContextNotifier`/`JoinContextNotifier` were plain `@riverpod` (autoDispose) but only ever read via `ref.read()`, with no `watch`/`listen` anywhere keeping them alive. Riverpod disposed the provisioning context between the phone-verify screen and the name/email screen, silently dropping the invite token. Both switched to `@Riverpod(keepAlive: true)`.

## 2026-07-03
- **PR #118 (MERGED, deployed live to cards.eq.solutions via manual `workflow_dispatch`)**: self-serve tenant provision screen gained name (required) + email (optional) fields, sent through to `shell-provision-tenant` (eq-shell #617). `provisionTenantExchange`'s return type changed from a positional tuple to a named record so the new `emailInUse` flag can't be silently dropped at the call site. Non-blocking snackbar when the entered email already belongs to another account (workspace still created, phone-only sign-in). Note: eq-cards does NOT auto-deploy on merge — `deploy.yml` is `workflow_dispatch`/release-tag only by design; this required an explicit dispatch after the merge to actually ship.

## 2026-07-02
- chore(migrations): PR #117 — "Migration hygiene" CI job fails any PR with a duplicate `NNNN` migration number (the recurring concurrent-branch collision, e.g. dual-`0071`); `supabase/MIGRATIONS.md` runbook documents the real apply path (manual/MCP to jvkn, drift-check first — `supabase db push` is dead: CLI linked to a DELETED project + repo NNNN ≠ ledger timestamps); renamed `0071_recycled_phone_review_guard → 0076` to clear the existing collision (SQL already applied under its ledger name, mapping in the runbook).
- feat(identity): Track B worker-identity resolver unification — migrations 0070–0073 applied to jvkn control plane + PR #113 merged. 0070 `link_pending_invites` claims a worker stub by email only on an exactly-one match (was unbounded → double-claim); 0071 `handle_phone_dedup` 90-day recycled-phone guard + `shell_control.identity_recycle_review` queue + admin RPCs; 0072 routes `eq_cards_claim_invite` + `eq_cards_submit_access_request` through the single resolver; 0073 adds `eq_cards_find_or_create_worker_for_invite` (no-user invite resolver). Replay-tested against 74 live workers (0 new dups); applied via MCP (CLI linked to a deleted project). Consumed by eq-shell #597/#598.
- fix(licences): crop screen no longer reallocates a blob URL on every drag-gesture rebuild — `Image.memory()` inline in `build()` swapped for a single `MemoryImage` held in State (same pattern as `_PhotoSlot` in `licence_edit_screen.dart`). Fixes recurring "Could not load Blob from its URL" (Sentry EQ-CARDS-W). Commit `caf91d1`, deployed via manual `deploy.yml` dispatch (run 28579115186)
- fix(ci): web image-compress moved behind a conditional import (`photo_compress_web.dart` + `_io` stub via `if (dart.library.html)`); `photo_upload.dart` no longer pulls `dart:js_interop`/`package:web` into the VM test graph. Fixes "Analyze and test" red since #110. Verified Flutter 3.41.9: analyze clean, 207 tests pass. PR #114 merged, CI green
- fix(notify): connection-request email CTA now "Review the request" → `core.eq.solutions/<slug>/staff` (was "Review in EQ Shell" → homepage); migration `0069` adds `org_slug` to `eq_notify_connection_request_targets`; edge fn v4. Live + verified (test send `sent:1`). PR #112
- fix(profile): `eq_cards_upsert_my_worker` gained 3 no-default params in `0067`, breaking its 12-arg callers → Profile tab "function does not exist" 500. Migration `0071` defaults the trailing params. Live + verified (impersonated upsert, rolled back). Also unblocks credential-save + invite-link. PR #112
- fix(ocr): web compression no longer blocks the main thread — `canvas.toDataURL()` swapped for `canvas.toBlob()` in a new web-only `_compressForWeb`, fixing the frozen OCR spinner on iOS Safari/PWA; native iOS/Android untouched (commit `d9d87a3`, PR #110, run 28540590608)
- fix(ocr): "Fill manually" escape hatch when OCR can't read a photo (e.g. back of card) — was a dead end with only a snackbar
- fix(onboarding): first-scan "welcome" screen now picks the photo synchronously in the tap handler instead of after a Navigator round-trip — browsers were silently refusing to open the camera on the first attempt (commit `617b8de`, PR #111, run 28541424467)
- fix(ocr): OCR loading dialog's "taking longer than usual" message no longer fires at 5s when the same dialog says scans normally take 5–10s — threshold raised to 9s, copy softened
- chore(data): deleted demo/trial account `0466118646` — standalone empty signup, no org/licence data
- fix(notify): connection-request email worker name now sourced from `workers` (was `profiles.full_name`, which self-signup workers never populate) — was rendering as "A worker". Falls back to formatted AU mobile, then "A new worker". Migration `0074`, applied live + verified. PR #115
- fix(licences): `share-licence` edge fn `holder_name` had the same wrong-table bug — sourced from `workers` first, `profiles` fallback. Deployed v8, verified live via curl. PR #115
- chore(data): `0075` backfilled `profiles.full_name` from `workers` for 6 NULL rows (0 conflicts). Applied live
- fix(connections): `P0023` name-gate error (`eq_cards_submit_access_request`) now shows a clean "Add your name…" message instead of `ServerFailure(500): ...`. PR #115, deployed run `28585818154`

## 2026-07-01
- fix(auth): `handle_phone_dedup` SECURITY DEFINER — all new signups were 500ing (migration 0066, applied live)
- fix(auth): "No internet connection" → "Unable to connect…" + Sentry capture for NetworkFailure
- fix(photos): bottom sheet camera/gallery choice on web — Android users no longer dropped straight to library
- fix(workers): `eq_cards_upsert_my_worker` adopts orphan shells before inserting — prevents duplicate worker rows (migration 0067, applied live)
- chore(workers): migration 0068 — swept 4 at-risk orphans: John Angangan orphan deleted, Cicero/Zemi/Marcus proactively linked
- deploy: run 28489589899 — all 2026-07-01 fixes live
- feat(onboarding): first-scan screen shown once on empty wallet (commit 37f8eb3, run 28509766188)
- feat(onboarding): rich empty state — updated headline + employer context copy for connected workers
- feat(onboarding): explicit camera+gallery buttons in FirstScanScreen; worker card hidden on empty wallet; CTAs at top (commit 493d895, run 28511411215)
- feat(onboarding): first-licence success sheet + connection confirmation snackbar + PostHog signup_completed fix (commit 1a141a6, run 28512783582)
- fix(ui): CircularProgressIndicator.adaptive → plain in EqButton + NotProvisionedScreen; worker ID card moved to wallet bottom (commit 9f2b408, run 28513226954)
