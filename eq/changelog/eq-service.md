---
title: EQ Service — Changelog
owner: Royce Milmlow
last_updated: 2026-07-20
scope: EQ Service append-only history. NOTE — duplicates eq/changelog/service.md, which stalls mid-deploy at 2026-06-09; this file is the one actually kept current. Consolidate, flagged as a follow-up.
read_priority: reference
status: live
---

# EQ Service — Changelog

## 2026-07-21
- **[#576](https://github.com/eq-solutions/eq-service/pull/576) (0189) + [#577](https://github.com/eq-solutions/eq-service/pull/577) (0190) — merged, dispatched via `apply-service-migrations.yml`, verified live on ehow.** 0189: `service.tenant_members` granted `authenticated` full CRUD under a tenant-only `FOR ALL` policy, so any tenant user could set their own `role` to `manager` — table-level writes revoked, `UPDATE (tech_onboarded_at, setup_checklist_dismissed_at)` granted back, `tm_isolation [*]` replaced by `tm_select` + `tm_onboarding_update`; `service.profiles` write grants closed too. 0190: `service.audit_logs` had full CRUD for `authenticated` gated only by tenant, so entries could be written under a colleague's name, edited, or deleted — now SELECT-only with a fixed INSERT column list, and `user_id` takes `DEFAULT auth.uid()` so authorship is derived by Postgres rather than accepted from the client. `lib/actions/audit.ts` stops sending `user_id` (its unit test now asserts the key is absent); the deploy was allowed to land before the migration so the column grant never disappeared under old code.
- **Corrected stale comments in `lib/api/auth.ts:23` and `lib/actions/auth.ts:32`** claiming ehow has no `tenant_members`/`profiles` tables — it has both, populated, which is what made the escalation live rather than latent.
- **`npm audit --audit-level=high` was failing on `main`**, not on any branch — two advisories (`brace-expansion` GHSA-3jxr-9vmj-r5cp, `js-yaml` GHSA-52cp-r559-cp3m). Merged #576/#577 over it deliberately (`tsc + next build` green) rather than normalise a permanently-red gate; fixed separately in [#579](https://github.com/eq-solutions/eq-service/pull/579).

## 2026-07-21
- **PR #574 MERGED + LIVE (`f6cf01d`) — `proxy.ts` middleware: combined duplicate `mfa_grace_started_at`/`is_active` profile queries into one.** Also carried forward two speed fixes that had landed on `main` (PR #573) but hadn't reached this working branch yet — merged rather than risk redoing/conflicting.
- **PR #575 MERGED + LIVE (`4abed24`) — Fixed scheduled functions' own trigger URLs getting redirected before their handler ran.** `proxy.ts`'s middleware matcher covers every path except a short static-asset list, so Netlify's own `/.netlify/functions/<name>` cron-trigger requests were 307-redirecting to `/auth/signin` — a distinct layer from the already-public `/api/cron/*`/`/api/health` targets those handlers fetch internally. Confirmed live via Observability: every `keep-warm`/`canonical-outbox-scheduler` tick in the log window hit this. `keep-warm` still incidentally warmed the shared Lambda (redirect target is the same function), masking the bug in load-time terms, but its real `/api/health` probe never fired; `canonical-outbox-scheduler`'s drain logic never ran at all (checked `public.canonical_outbox` directly — currently empty, no backlog/data-loss evidence). `pre-visit-brief-scheduler`/`supervisor-digest-scheduler` share the identical architecture, exempted pre-emptively. Added all four to `PUBLIC_PATHS` in `lib/auth/mfa-routing.ts`. Verified live in production post-merge: `/.netlify/functions/keep-warm` now returns 200 (was 307).
- **PR #578 MERGED + LIVE (`9d8df5e`) — notify-substrate diagnostics ported from eq-cards.** Same fix as eq-shell #942: `curl -sf` swallowed the real HTTP status/body on a failed dispatch to eq-context, leaving only an opaque exit 22; now surfaces status + body. Doesn't fix the dispatch itself — `EQ_CONTEXT_PAT` still needs the same working classic PAT eq-cards holds, Royce-only. Merged despite a red `Typecheck + audit` check — confirmed it's a pre-existing `npm audit` transitive-dependency finding (js-yaml/postcss/uuid) unrelated to this workflow-YAML-only change; `tsc + next build`, the real gate, passed.
- **PR #579 MERGED (`a6f8d38`) — `Typecheck + audit` is green on `main` again; `npm audit --audit-level=high` had been failing branch-independently.** Closes the loop on the caveat in #578 above ("merged despite a red `Typecheck + audit`… pre-existing `npm audit` transitive finding"). `main` itself was red, so **every** open PR in the repo inherited a red required gate — the real cost being that a permanently-red light trains everyone to merge past it, and a genuine finding then slips through unnoticed. Fix was a plain `npm audit fix` (**no `--force`**) for the two HIGH advisories: `brace-expansion` [GHSA-3jxr-9vmj-r5cp](https://github.com/advisories/GHSA-3jxr-9vmj-r5cp) (DoS via exponential-time expansion; 5.0.6→5.0.7, 1.1.13→1.1.16, 2.0.3→2.1.2) and `js-yaml` [GHSA-52cp-r559-cp3m](https://github.com/advisories/GHSA-52cp-r559-cp3m) (quadratic CPU via YAML merge-key chains; 4.2.0→4.3.0). **Lockfile-only** — `package.json` untouched, 15 insertions / 11 deletions, all transitive patch bumps, one commit one file. Verified from a clean `npm ci` off the new lockfile: audit exit 0 (was 1), `tsc --noEmit` 0 errors, `npm run check` clean with all routes built; both real gates confirmed green on the PR before merging and `Typecheck + audit: success` re-confirmed on `main` after. **The 4 MODERATE advisories are deliberately left** (`postcss` via `next`, `uuid` via `exceljs`) — they need `--force` and a *major downgrade* (`next@9.3.3`, `exceljs@3.4.0`), i.e. a rewrite, and the gate is `--audit-level=high` so they don't block. Two traps worth knowing: a fresh worktree has no `.env.local` (gitignored, so `git worktree add` never carries it) and `npm run check` then dies on `NEXT_PUBLIC_SUPABASE_*` undefined at *page-data collection* only, after compile+TS have already passed; and `gh pr merge --delete-branch` errors with `fatal: 'main' is already used by worktree …` from its own local cleanup step **even though the merge succeeded** — verify with `gh pr view --json state,mergeCommit` before retrying.

## 2026-07-16
- **PR #544 MERGED + LIVE (`358858d`) — Admin hub: stale card copy fixed, unused "Today" view removed.** Royce asked for every `/admin` card to be traced to its actual code, not its label. Found two mismatches: **Users** (`app/(app)/admin/users/page.tsx`) went read-only when Shell took over invite/roles/deactivate, but the card still promised those actions; **Connected Apps** (`app/(app)/admin/integrations/page.tsx`) never had EQ Field sync — it's a static canonical-linkage completion bar gated on `CANONICAL_API_KEY_SERVICE`, with an empty `actions.ts` (the old backfill action was retired when Shell became the write owner). Both card descriptions in `app/(app)/admin/page.tsx` corrected to match. Also removed the **Today** card + `/admin/today` route entirely (duplicated Activity's live-status purpose; grep confirmed no other code referenced it) on Royce's "no need for that." `npm run check` clean. Shipped as a fresh branch off current `main` (the commit had first landed on the stale, already-merged `fix/type-bypass-column-audit` branch, which diverges from `main` well beyond this change — cherry-picked onto a clean base instead of merging that branch directly).
- **PR #545 MERGED + LIVE (`ad1a03c`) — Removed the "Connected Apps" card entirely; its sync metric measured dead architecture.** Follow-up to #544: dug into why "Connected Apps" showed 0/5 customers vs. 9/9 sites "linked to canonical." `canonical_id`/`canonical_synced_at` and the `service.customer_local`/`service.site_local` sidecar tables feeding them are leftovers from an old push-based sync design (Service used to write its own customer/site records up to canonical) — retired when Shell became sole owner, per `lib/canonical-sync.ts`'s own docstring. Live-verified: both sidecar tables are 100% empty across every tenant, `canonical_outbox` has zero `customers` rows ever. Worse than idle: `service.sites.canonical_id` used `COALESCE(sl.canonical_id, s.site_id)`, silently falling back to the site's own local id whenever there's no real link (always) — so the "Sites: 9/9 (100%)" figure could never read as anything but fully synced regardless of reality; customers had no such fallback, which is the only reason 0/5 ever surfaced as a signal. Presented the finding + three options to Royce (replace with a status note / remove entirely / leave it); Royce chose full removal. Deleted `app/(app)/admin/integrations/{page.tsx,actions.ts,IntegrationsClient.tsx}` and the card entry in `admin/page.tsx`. `npm run check` clean, same fresh-branch-off-main pattern as #544.
- **3 stale Dependabot bumps merged** — #456 (tailwindcss), #458 (tailwind-merge), #459 (@vitejs/plugin-react); real gates (typecheck, `tsc + next build`) green, only the known pre-existing integration-test flake red.

## 2026-07-15
- **PR #540 MERGED + LIVE (`2ed22ef`) — NSX/ACB Testing lists + cert-chain fixed for the Shell iframe.** Root cause: the NSX/ACB Testing home pages (list + Create-Check site/member/asset pickers) and `InstrumentDetail.tsx`'s calibration cert-chain panel read Supabase via the **browser client** (`createClient()` from `lib/supabase/client`). Under Plan B JWT auth inside the Shell iframe there is no Supabase browser session — `/api/shell-auth` sets only the httpOnly `eq_service_jwt` cookie, and `/shell`'s JWT fast-path navigates without ever calling `verifyOtp()` — so those reads hit PostgREST as `anon`, and the tenant RLS policy (`tenant_id = auth.jwt()->app_metadata->>tenant_id`) matched nothing: empty list, no error, no console signal. It only appeared to work while a stale `sb-*` session cookie from an earlier direct login lingered (see `proxy.ts:66`'s existing comment). Server-rendered pages (Maintenance, RCD) were never affected — they already read via the JWT-bearer server client. Fixed by moving the reads server-side: new `app/(app)/testing/read-actions.ts` (`listNsxChecksAction`, `listAcbChecksAction`, `listTestingSitesAction`, `listTestingMembersAction`, `listNsxAssetsAction`, `listAcbAssetsAction`) + `listInstrumentCalibrationEventsAction` in `instruments/actions.ts`, all `requireUser()`-gated. `nsx/page.tsx` / `acb/page.tsx` / `InstrumentDetail.tsx` swapped onto them; dropped the now-unused browser client + local NSX-plan matcher. Contained blast radius — `grep '@/lib/supabase/client'` under `app/` found only these three files (plus the shell OTP-fallback page, never hit in the SKS flow). `tsc + next build` clean.
- **PR #541 MERGED + LIVE (`8fa1ac4`) — Field Run-Sheet: breaker identity no longer silently dropped + assets print in name order.** `maintenance-checklist-input.ts` fetched `cb_make/cb_model/cb_serial/brand/breaker_type/...` from the `acb_tests`/`nsx_tests` row but never assigned them anywhere — `ChecklistAsset` had no fields for them, so `buildBreakerCard`'s "1 · Asset Collection" grid in `maintenance-checklist.ts` always rendered hardcoded-blank cells regardless of what a tech had recorded via the 3-step workflow's Step 1. The existing shared helper `resolveBreakerIdentity()` (`lib/reports/breaker-identity.ts`, already used correctly by the Customer Report) was never called from this path. `ChecklistAsset` gains optional `cbMake/cbModel/cbSerial/cbPoles/cbRating/tripUnit/performanceLevel/fixedWithdrawable`; the acb/nsx query expanded to the full identity column set (branched literal `.select()` strings — a template-interpolated select breaks Supabase's static type parsing); grid renders the resolved values. Also sorted `checklistAssets` by asset name (numeric-aware, `localeCompare(..., {numeric:true})`) across all check kinds — test rows previously printed in creation order (the order a tech clicked assets on Create Check — e.g. NSX 03..13 then NSX 01, 02 last), not a supervisor-legible order. Regression test added (`maintenance-checklist.smoke.test.ts`): populates identity on a synthetic NSX asset, unzips the generated docx, asserts the values appear. `npm run check` clean; verified live DB state of the reported check (all 13 NSX tests `step1_status='pending'`, identity columns null) confirms the export was correctly blank for that specific check — the bug is that the mechanism would have stayed broken forever even after Step 1 was completed.
- **PR #542 MERGED + LIVE (`7ff3dd6`) — Field Run-Sheet reflects recorded Visual & Functional ticks, comments, Electrical readings, and overall result.** Follow-up to #541: PR #541's own description flagged the Visual/Functional checkboxes and Electrical readings staying blank as *believed intentional* (`PrintBlankButton.tsx`: "sometimes we print empty and the guys complete on site") and asked to be corrected if wrong — Royce attached a fresh export showing `NSX 01`'s step 2 marked Complete in-app with all items OK, yet every checkbox still printed empty, and confirmed the run-sheet should show recorded results, not stay a blank clipboard form. `nsx_test_readings`/`acb_test_readings` already store one row per checklist item (`"Visual Check: <label>"` / `"Electrical: <label>"`, with `is_pass`/`value`/`unit`) using label text that already matches the run-sheet's own checklist verbatim (both built from the same item list). `maintenance-checklist-input.ts` now fetches these per test, strips the storage-namespace prefix, and passes them through as new `ChecklistAsset.readings` + `overallResult` fields. `buildBreakerCard` renders a bold ☑ for whichever of OK/Fail/N/A was recorded (was always ☐ `makeCheckboxCell`, now takes a `checked` param) + prints any freeform comment; IR Closed/Open (both kinds) and ACB Contact Resistance print the actual recorded value instead of a blank cell; "Overall Result: Pass / Fail / Defect" bolds+underlines whichever word matches the recorded `overall_result`. Temperature stays genuinely blank — neither ACB nor NSX workflow has ever recorded it. Regression test extended: a recorded pass-with-comment, a recorded fail, an electrical reading, and the overall result all assert into the generated docx (5/5 pass). `npm run check` clean.

## 2026-07-14
- **Retired the Report-Settings logo pickers — one source of truth = Shell (eq-service #539 `28f68ea`, MERGED + deploying).** Removed both "Report Logo — Light/Dark Surface" `MediaPicker`s from `ReportSettingsForm` + dropped `report_logo_url` / `report_logo_url_on_dark` from `updateReportSettingsAction`'s Zod schema AND the DB `update` — a Report-Settings save had been writing (and thus clobbering) the Shell-synced logo. The card is now a note pointing to EQ Shell → Settings → Document logo (light + "Dark background"). Reports still READ the columns; only the setter moved. Completes the dark-doc-logo move to canonical (#866/#538). tsc+build clean.
- **Dark-background document logo consumed — eq-service #538 (`5b7bce4`, MERGED + deploying).** `shell-auth` write-behind persists the new `brand_doc_logo_dark_url` claim → `tenant_settings.report_logo_url_on_dark` (the column reports read for the dark navy masthead). So a tenant's dark document logo, set in Shell, flows to reports on the next login. `service-jwt` ServiceJwtClaims extended. Pairs with eq-shell #866 (canonical `gateLogoDark` + admin upload + JWT). Real gates green (`tsc+build`, `Typecheck+audit`); only red = the known integration flake. Retires the interim hard-set dark logo.
- **Reports render each tenant's real brand + auto-update — eq-service #533 (`b3ffc9a`) + #536 (`86d73ac`), both MERGED + LIVE.** #533: `applyBrandColour` applied at the `getCachedTenantSettings` choke point so ALL server-side consumers (reports, notification emails, dashboards, cron) inherit tenant brand — was only the interactive `getTenantSettings()` UI path, so reports came out EQ-sky with no logo. `applyBrandColour` extended to honour an explicit deep/ice/ink document palette (keeps SKS purple `#8070C0` vs a navy-mono derivation). Run-sheet redesign: navy register header + white text, purple section markers + supervisor→tech divider, grey zebra banding, coherent navy masthead, white on-dark logo; shared shell purple page numbers + navy hairline footer, purple cover accent, tenant ink; plus the federated `getTenantSettings` read of the new JWT brand claims (app chrome). Interim: SKS palette + doc/white logos mirrored into `service.tenant_settings` (ehow) because the Shell handoff only carried primary + app-logo. #536: extended the `app/api/shell-auth` write-behind sync to persist the widened claims (deep/ice/ink + report_logo_url) into `tenant_settings` on each login — **this is the report auto-update path** (reports read `tenant_settings`, NOT the JWT), guarded so pre-widening tokens never clobber with null; `report_logo_url_on_dark` deliberately not synced (Shell has no dark-logo slot). Pairs with eq-shell #856. Real-gate CI green (`tsc+build`, `Typecheck+audit`); only red = the known integration flake. Verified: both production deploys `ready`, core+service healthy, run-sheet structurally carries navy×18 / purple×10 / grey-zebra + the embedded white logo.
- **Calibration backfill (`0186_backfill_asset_calibration_from_assets`) — APPLIED to ehow via MCP + live-verified; PR #534 MERGED (`42c9ae4`) after renumber + ledger reconcile.** Seeds `app_data.asset_calibration` from the legacy `assets` columns (`last_service_date`→cal date / `next_service_due`→cal due / `cert_url`) for the 18 plant_equipment items → eq-service Test Equipment shows the real dates again (was blank/0-of-18); the history trigger seeded 18 events. First step of the cross-repo calibration single-source consolidation (eq-shell #855 + eq-field #490, both merged+deployed). ✅ **Both follow-ups this created are now RESOLVED (2026-07-14, same day):** (a) the `0183` filename collision with #535's renamed `0183_instruments_canonical_read_through` was cleared by merging main into #534's branch (rename-resolves the instruments trio) then `git mv 0183_backfill→0186_backfill` + retruthing the in-file/HISTORY numbering (pushed `9cde375`); (b) the out-of-band MCP apply (never in the governed `service._eq_migrations` ledger) was reconciled with a `0186_backfill_asset_calibration_from_assets.sql` ledger row (real sha256 `e6be587d…`, `applied_by='reconcile-2026-07-14:mcp-applied-backfill-534'`) — verified against the live ledger both directions: `repo(190 files) − ledger = ∅` AND `ledger − repo = ∅`, so `apply-service-migrations` reads **0 pending** on #534 and on main. #534 squash-merged (`42c9ae4`, admin-override on the known integration-test flake; `tsc+build` + Plan green) → main now carries 0180–0186, no dup prefixes. 2nd instance of the MCP-bypass drift the memory documents. **Data note:** `assets.last_service_date/next_service_due/cert_url` are SHARED asset-service columns (feed `eq_ppm_*` + dashboard + intake), NOT calibration-only — never drop them. **Phase 4 (same day):** the 18 stale plant_equipment values on those three columns were then nulled via a scoped data UPDATE on ehow (plant_equipment only; the 2830 maintainable assets untouched) so canonical `app_data.asset_calibration` is the sole calibration store — data-only, reversible, closing the single-source consolidation; the shared columns were kept.
- **PR #535 (MERGED squash → main) — migration prefix collisions deduped + governed ledger reconciled.** Resolves the `0180/0181/0182` duplicate-prefix note below: renamed the LATER file of each colliding pair (by application timestamp) so every prefix is unique — the instrument/calibration cluster moved to `0183_instruments_canonical_read_through` / `0184_calibration_canonical` / `0185_instrument_calibration_history_view` (pure `git mv`, no SQL change; dependency order preserved for a clean `db reset` — 0184 creates the events table 0185's view reads). Kept the external_id-fix pair (`0180_assets_external_id_unique_per_site` / `0181_drop_stale_assets_tenant_external_id_uidx`) + independent `0182_asset_major_due_year` at their numbers. Separately reconciled `service._eq_migrations` (the governed apply ledger): 0180–0182 had been MCP-applied, bypassing `migrate-service.mjs`, leaving the ledger 6 rows behind (max `0179_…`) — so a `apply-service-migrations` dispatch would have tried to re-run live DDL. Backfilled the 6 at final names + real sha256 (`applied_by='reconcile-2026-07-14:mcp-applied-525-531-532'`; the CHECK forces non-null checksum); verified the 189-file merged-main list vs ledger = **0 pending**. Real-gate CI green; only red = the known pre-existing 0151 `public.app_config` integration flake (dies ~30 files before the renamed ones). Deploy = functional no-op (only SQL filenames changed). Done in an isolated worktree off origin/main (root checkout was on a concurrent agent's branch). **Durable rule (memory):** eq-service's real migration gate is filename-keyed `service._eq_migrations`, NOT timestamp-keyed `schema_migrations` — MCP `apply_migration` bypasses the governed runner and drifts it; apply via the governed dispatch or reconcile by backfill.
- **PR #526 (MERGED `a588776`, deployed → service.eq.solutions) — balloon years + per-asset frequency chips.** Two Assets-register features shipped together and live-verified on real SKS data (SY3/E1.12, 154 panels). **(1) Balloon years** (P1 of `docs/proposals/balloon-years.md`, PR #524): migration **0182** adds `major_due_year` jsonb to `service.asset_local` (writable table), surfaced on the `service.assets` security-invoker view + routed by both INSTEAD OF trigger functions — mirrors 0172's passthrough pattern (additive, nullable). `lib/scope-context/balloon.ts` is the pure rollup core: `year_totals = annual floor + placed majors`, where a major **replaces** the asset's annual that year (reproduces the import math exactly — unit-checked vs live E1.24/E1.17/E1.12). A **Major years** panel (`BalloonPanel.tsx`, supervisor+) opens when the register is filtered to a single site + plan whose scope prices a low-frequency major: per-asset year picker + bulk-assign + live Now→After funding preview + Recompute (writes through the canonical `contract_scopes` view). **Pre-fill from contract** (`seedNominationsFromScope` + `prefillBalloonFromScopeAction`) projects the scope's existing `due_years`/`year_totals` split onto per-asset nominations so recompute is idempotent on existing scopes; a guard (`existingHasMajors`) blocks recompute while a distributed scope has zero placements — else it would zero the funding (the seed→recompute-reproduces-existing property is the fix). Funding consumers (`lib/scope-context/*`) read `year_totals` only → zero consumer change. Migration 0182 APPLIED to ehow, advisors clean (0 new ERROR). **(2) Frequency chips** (`lib/utils/asset-frequency.ts` + `FrequencyBadges`): each asset shows its plan's aggregated task cadences (M/Q/6M/A/2Y…/DS) on the grouped-view plan headers, a table Frequency column, and the asset detail page. All `job_plan_items` are plan-wide → plan aggregate = per-asset footprint.
- **Types-drift fixes bundled in #526 (`chore(types)` commits) — cleared the `Canonical types drift` gate (red on main).** Added `major_due_year` to `asset_local`/`assets` in `lib/supabase/database.types.ts` (mine, from 0182) AND `service.instruments.canonical_id`/`cert_url` (pre-existing drift from migration 0180/#525, merged 2026-07-13 without regenerating types) — both were the "build-break class" (live column missing from a writable Tables entry). `tsc + next build`, drift, invariants, advisors all green; only red = the known pre-existing integration-test flake.
- **Calibration is canonical (PR #531 MERGED `cee3ec1`, migration 0181 applied to ehow).** cal date/due/cert/status moved from the Service sidecar (`app_data.instruments`) into `app_data.asset_calibration` (current state, UNIQUE asset_id, CHECK status) + `app_data.asset_calibration_events` (append-only cert history via `tg_asset_calibration_history`). Cross-app `SELECT` to `authenticated`; `service.tg_instruments_iud` upgraded INVOKER → **SECURITY DEFINER + `assert_jwt_tenant`** write-through (matches the sites/customers/assets convention). 18 SKS items verified live (an edit writes the canonical row + appends a history event); advisors 0 ERROR. Bundled: dashboard overdue banner now filters `status='Active'` (was inflating the count with retired gear); RLS cross-tenant-sweep test no longer seeds the now-view `instruments`.
- **Calibration cert-chain surfaced in the UI (PR #532 MERGED `e25f411`, migration 0182 applied to ehow).** Test Equipment detail panel gains a "Calibration history" section reading `service.instrument_calibration_events` — a **READ-ONLY** `security_invoker` view over `app_data.asset_calibration_events`. No INSTEAD OF trigger; INSERT/UPDATE/DELETE `REVOKE`d from `authenticated` — new `service` views inherit `arwd` from an `ALTER DEFAULT PRIVILEGES` rule, so pure read views must revoke explicitly (views with triggers are unaffected). `InstrumentDetail.tsx` lazy-loads the chain on open; `lib/types` gained `InstrumentCalibrationEvent`, queried via the `SupabaseClient<any>` escape hatch (view absent from the stale generated types). One source of truth held — a window, not a copy. tsc+build green, view verified read-only live, 0 ERROR advisors. **Filename `0182_` collides with #526's `0182_asset_major_due_year.sql`; ehow tracks migrations by timestamp version so application is correct + complete — see sessions/2026-07-14.md.**

## 2026-07-13
- **Test Equipment = canonical plant & equipment read-through (PR #525 MERGED `bb7c3c2`, migration 0180 applied to ehow).** "Instrument Register" renamed → "Test Equipment" (route/table stay `instruments`). `service.instruments` rebuilt as a read-through view over `app_data.assets WHERE asset_type='plant_equipment'` (16 SKS meters: Fluke/Megger/Metrel/NORBAR) LEFT JOIN the reused `app_data.instruments` calibration sidecar (new `canonical_id`); identity (name/make/model/serial + cert URL) read-only from canonical. Add/Import/bulk/deactivate removed; edit = calibration fields only + "View certificate". ACB/NSX `instrument_id` FK repointed `app_data.instruments(id)` → `app_data.assets(asset_id)` (0 rows referenced; that picker now lists the 16 meters). Reverses the 0158 "Sync from Shell" copy (killed by 0164) — one version, no copy/sync. (Sidecar superseded next day by 0181's canonical calibration.)
- **PR #523 (MERGED `93481a2`, auto-deployed) — auth-handoff canary: `slug_unresolved` demoted from error to warning.** The digest's new production error "auth handoff: slug_unresolved" (EQ-SOLVES-SERVICE-A, slug `eq`, 3 events, 0 users) was the fail-closed login guard working as designed — a user whose tenant isn't provisioned in Service (EQ tenant; Service carries only SKS + demo) clicked into Service and got a clean 403. The defect was in the canary (`lib/observability/auth-handoff-canary.ts`): it still classified `slug_unresolved` as error-level, justified by an empty-app-200 failure mode that PR #376's fail-closed guard (`app/api/shell-auth/route.ts`) already eliminated. Demoted to warning alongside expired/malformed/cookie_*; `secret_mismatch`/`no_email` (every login breaks) stay error-level. Signal preserved — a real tenant-wide lockout (a live slug like `sks` suddenly not resolving) surfaces as a warning-rate SPIKE. Pure Set-literal + comment change; `tsc --noEmit` clean, build-inert. Isolated worktree off origin/main (root checkout was on another session's branch). Merge auto-deploys.
- **PR #517 (MERGED `1dd7d00`, auto-deployed) — bump `@eq-solutions/ui` v1.9.0 → v1.10.1.** Primary reason: the shared Modal no longer steals focus on parent re-renders when `onClose` has an unstable identity (eq-ui #23 → v1.10.1; keeps the caret in the field while typing). The two-range jump also picks up v1.10.0 — Table `persistKey` + `defaultHiddenColumns`, Spinner `inverted`, inactive sidebar nav contrast — all additive, no API removals. Pin-only (package.json + package-lock.json). Real-gate CI green (`tsc + next build`, `Typecheck + audit`); the `Integration tests (Supabase local)` fail is the known pre-existing standing failure, untouched by a dep bump. Done in an isolated worktree off origin/main; the npm lockfile needed an explicit `npm install "@eq-solutions/ui@github:…#v1.10.1" --package-lock-only` because `--package-lock-only` alone left the resolved commit stale.
- **SY9 (Equinix) import audited against SY1/SY7 — 100% correct** — every asset column matches the app's own importer output (equipment type, defaults, IDs, plan links); 499 assets linked to their maintenance plans; contract funding-correct. Fixed the one gap: the active SY9 site had a blank short `code`, set to `SY9` so future schedule imports resolve it.

## 2026-07-11
- **PR #494 (MERGED `356d743`, deployed) — boot perf: posthog-js off the critical bundle + Sentry SDK trimmed.** Nav-speed pass. Service's own boot is healthy (Next.js, immutable-cached, route-split, batched dashboard) — the only measured weight was observability. `posthog-js` (~62 KB compressed) was a static top-level import in the root `providers.tsx`, so it shipped in the initial/critical JS of every root-layout route (the sign-in page + the Shell-embedded `/shell` entry included); now a dynamic `import()` inside the existing post-hydration effect (a lazy async chunk). Traced the import graph to confirm `providers.tsx` was the only thing pulling posthog onto the pre-login path (`lib/analytics.ts`, the other importer, loads only on authed `(app)`/`(portal)` pages) — same singleton, same events, analytics behaviour unchanged. Dropped the unused `<PostHogProvider>` wrapper (zero `usePostHog` consumers). Plus Sentry `bundleSizeOptimizations` (excludeTracing/Replay*) — Service runs errors-only (traces/replay sampled to 0), so those bundles were dead weight on the largest script of every cold load. `next build` green; `/shell` returns 200 post-deploy. Not touched: cache headers (already `immutable`), auth (the standalone-login triple `getUser()` is on the standalone path only), Clarity/PostHog session-recording gating (a product-analytics decision).

## 2026-07-10
- **Dashboard now respects the App Activation "Service" toggle (PR #484, migration 0176, applied live to ehow).** Royce noticed switched-off customers (Jemena) still showing on the EQ Service dashboard. Root cause: the Sites tile (`get_dashboard_counts`) and the Site Locations map (`get_sites_for_map`) read `app_data.sites` directly with only `active=true`, bypassing the `service.sites` view (which filters `service_enabled` since migration 0163) that every other Service page/picker uses. So the overview counted the whole SKS estate — 242 sites / 27 map pins — while only 7 sites are actually switched on for Service (6 geocoded). Added `service_enabled=true` to both functions; verified live the tile now shows 7 and the map 6. **Scope deliberately sites + map only** (Royce's call) — customers/assets/job_plans counts unchanged, since "what makes a customer/asset in-Service" is a separate definition not decided here. Reversible; wrapped in the app_data-existence guard so CI without app_data skips. Note: the App Activation admin page in Shell is already fully canonical — its Field/Service toggles write straight to `app_data.sites.field_enabled`/`service_enabled` (7 service-on, 40 field-on live), and both apps read those columns; nothing to wire up there, this was purely the dashboard's two summary reads skipping the filter.
- **Dashboard Customers + Assets tiles scoped too (PR #485, migration 0177, live).** Royce: "scope customers and assets too." The activation switch lives on the SITE, so in-Service customers/assets derive from it. Assets → scoped to service-enabled sites (mirrors the `service.assets` view); customers → the customer-level `service_enabled` flag is unused (0 customers carry it, verified across all tenants), so "in Service" = owns a service-enabled site. Verified live: customers 41 → 3 (the Equinix entities), assets 345 → 345 (all active non-plant assets already sit on service sites; scope future-proofs it).
- **Fixed the empty SKS Customers list page (PR #486, migration 0178, live).** Discovered while scoping the above: `service.customers` filtered that same unused customer-level flag, so the Customers page + every customer picker/report showed **0** for SKS (the only tenant with customers). Rewrote the view to be site-driven (customer owns an active service-enabled site), consistent with `service.sites`/`service.assets` and the dashboard. Now shows 3 (Equinix Australia National, Equinix Australia Pty Ltd, Metronode NSW), matching the tile. Column list/order/aliases + `security_invoker` + all 3 INSTEAD OF triggers reproduced exactly (0156 duplicate-alias caution); advisors 0 ERROR. Safe: widens 0 → the real serviced customers, no tenant relied on the old flag.

## 2026-07-08
- **Shell-embed session bug fully fixed across 4 PRs (#469, #474, #475, #478 — all merged, live).** The exact "workspace isn't set up" + wrong-chrome screenshot Royce reported was 3 separate bugs, not one: (1) `ShellSessionRecovery` self-heals a lapsed session instead of showing a false setup-not-complete message (was never a setup gap — SKS's `setup_completed_at` was already set); (2) the Shell-handoff cookies (`eq_service_jwt`/`eq_shell_bridge`) are now CHIPS-partitioned (`SameSite=None; Secure; Partitioned`) — the plain `SameSite=Lax` cookie wasn't persisting on at least one real device even served same-site with Shell, confirmed fixed live; (3) `isShellIframe` now also trusts the browser's `Sec-Fetch-Dest: iframe` header, fixing a stray duplicate sidebar that showed when a session authenticated via a non-JWT path, confirmed fixed live. Added `dashboard-duration-canary.ts` (Sentry warning/error thresholds, mirrors the existing report-duration canary) after a separate "dashboard is slow" report had zero performance telemetry to diagnose against — purely additive instrumentation, no query changes. Along the way, found and (with Royce) fixed a live, repo-specific GitHub Actions outage (check-suite auto-trigger silently disabled for this repo only) that was blocking one of the PRs from getting a CI signal at all.
- **Type-bypass column audit — 7 broken queries fixed (PR #477, merged, deploying).** Part of the same-day 3-repo schema-mismatch audit: code that bypassed the generated TypeScript `Database` type let wrong column names slip past `tsc`. Fixed the Customers list API (`phone` didn't exist on the raw table), the landing dashboard silently swallowing a tenant-lookup error (root cause: migration `0175` for `tenant_members.tech_onboarded_at`/`setup_checklist_dismissed_at` had never actually been applied — now live on ehow, also unbroke 3 dashboard dismiss buttons), the admin canonical-export tool's missing abn/acn columns, and the service-contract export (was claiming ~29 fields, only `contract_template` actually exists — now honest about partial coverage). Follow-up sweep on the same PR found 3 more instances of the identical bypass pattern, each verified broken via a live `curl` against the PostgREST API (not inferred from code): the Customer Portal "Your Reports" page has shown "No reports delivered yet" to every customer regardless of real history since it shipped (`report_deliveries` has no FK to `maintenance_checks`/`customers`); a customer upcoming-visit notification type has never sent; the ACB/NSX "assign to" picker has always loaded empty (`tenant_members` has no FK to `profiles`). Merged after confirming the one failing CI check (`Integration tests (Supabase local)`) is pre-existing/unrelated (also fails identically on already-merged PRs #465/#469).
- **RCD job-plan self-provisioning — sticky for every tenant (PR #476, merged live).** `seedRcdScheduledChecks()` used to silently no-op for any customer with no RCD job plan of their own and no tenant-wide fallback (the gap that left Equinix with 0 RCD checks). It now auto-creates the tenant's copy of the existing `STARTER-RCD-BIANNUAL` starter plan the first time it's needed — the same catalogue entry the "Use starter templates" button already offers, reused rather than duplicated. No admin action, no per-tenant hardcode, ever again. Live SKS data reconciled onto the canonical plan (retired the earlier ad-hoc stopgap, re-pointed all 8 Equinix RCD checks). 0 new security-advisor findings.
- **Generic RCD job plan created + Equinix RCD checks seeded (live data, no code change).** Created a tenant-wide `RCD Testing` job plan with no customer tied to it, so any customer with contracted RCD scope but no plan of their own now gets one via the existing customer→global fallback (previously only Jemena had an RCD plan, so nobody else's RCD checks could ever seed). Verified it resolves correctly for all 5 Equinix customer records without affecting Jemena. Retroactively seeded 8 RCD checks (annual + semi-annual) across Equinix's 4 contracted sites — the RCD-seed feature only fires on import, so pre-existing contracts needed a manual catch-up. Written through the canonical `service.*` trigger path with a matching audit-log entry; 0 new security-advisor findings.
- **Job-plan coverage report on commercial-sheet import (PR #473, merged live).** Every import now reports, unconditionally (not gated on the "create assets" checkbox), which contracted job-plan codes couldn't be turned into work: unmapped (no matching plan), ambiguous (matches >1 plan), or mapped to an empty plan (zero tasks — a check off it opens blank). Surfaced in the commit result, the audit log, and a green/amber card in the importer. New pure `deriveCoverage()` in `lib/actions/asset-reconciliation-core.ts`, 4 unit tests. No schema change. Live audit of the whole import→asset-list pipeline (canonical adherence, asset/RCD coverage) also produced a shareable infographic — no other code changes; remaining items are data/ops Royce is handling directly (site enablement, missing contract imports).
- **RCD scheduled checks seeded from commercial-sheet import (PR #465, merged live).** Importing a contract's "RCD Testing" scope lines now creates header-only `kind='rcd'` scheduled maintenance checks, so the contracted RCD visits surface in the maintenance queue instead of living only as dollar line items. Cadence is read from the sheet's interval text ("Annual"/"Semi-Annual") — NOT a hardcoded month (the `testing/rcd` `month===5 ? 'annual'` inference is deliberately not reused). Due dates are editable May/Nov defaults (next occurrence → always future, so the overdue cron never false-flags a fresh seed); checks are unassigned (date + assignee set at time of work); data-gated (no RCD job plan → no-op) + idempotent (wipe/reimport-safe). New shared `lib/actions/maintenance-check-seed.ts` + 10 unit tests. No schema change.
- **Canonical wiring re-verified correct (PR #466, docs).** Live structural verification on ehow across the import → check/report and ACB/NSX/RCD → report chains: 25/25 canonical `service.*` objects pass full INSERT/UPDATE/DELETE trigger set + `security_invoker` view + correct `app_data` routing + RLS/policy + a write-path tenant fence (5 SECURITY DEFINER via `assert_jwt_tenant()`, 20 SECURITY INVOKER via RLS `WITH CHECK`); 0 ERROR advisors. Corrected the stale CLAUDE.md "contacts FRAGMENTED" note → contacts are canonical (object count 20→25). Published a Shell/Service/Canonical wiring infographic (claude.ai artifact) with a verification panel.

## 2026-07-06 (evening)
- **SKS "workspace isn't set up yet" — migration `0174` dispatched + applied live to ehow, verified.** PRs #453 (backfill `service.tenants.setup_completed_at` for SKS) and #454 (permanently disable the first-run wizard) had already been merged by a concurrent session, but merging doesn't apply DDL in this repo (dispatch-gated, same pattern as eq-shell's One Pipe) — the column was still live-`NULL` post-merge. Confirmed `0174` was the only pending migration (this workflow has no per-tenant scope input, unlike eq-shell's — it applies everything pending) before dispatching; Royce approved the `production` gate; verified `setup_completed_at` now stamped. Not yet visually reconfirmed on the actual dashboard page.

## 2026-07-06 (later)
- **PR #452 (MERGED, live) — "Also create assets for every job-plan code in this sheet."** Opt-in checkbox on the commercial-sheet import wizard itself: shows a live preview count before commit, then generates stub assets for every gap in the same commit action — no separate visit to the asset-reconciliation screen needed for sites with nothing to link against. Server re-derives every gap from what was just committed and skips (never guesses on) ambiguous/unresolved job-plan matches. Shared the job_plans.name gap-matching + stub-insert logic with the standalone reconciliation screen via new `lib/actions/asset-reconciliation-core.ts`. The repo's own server-action security audit caught the new shared helper missing a `requireUser()` guard — allowlisted as an internal helper (same shape as two existing entries), 0 ERROR findings after.
- **Migration `0172`/`0173` dispatch verified end-to-end** — ledger has both rows, `is_stub` column live, 0 new security-advisor ERROR findings.

## 2026-07-06
- **Asset reconciliation screen shipped + live (PRs #444, #445, #446; migrations 0172/0173 applied to ehow).** New `/commercials/asset-reconciliation` closes the gap between commercial-sheet contract quantities and the real asset register (3,605 contracted units / 4 sites / zero linked assets at session start). Per-job-plan gap rows → generate stub assets / link existing / skip, staged + single-commit, `isAdmin`-gated. `app_data.assets.is_stub` + rebuilt `service.assets` view/triggers; amber "Unverified" badge until cleared via `markAssetVerifiedAction`.
- **Post-merge review (PR #445) found and fixed real issues**: two cross-tenant write gaps (`site_id`/`job_plan_id` never checked against caller's tenant — no live exploit yet, SKS is the only tenant, but closed pre-emptively), a silently-swallowed partial-link shortfall, a client-trusted stale-quantity gap (now capped server-side), a commit-loop dead-end on failure (now resumable), and `is_stub` missing from `/assets`'s default grouped view (migration 0173, `get_assets_for_grouping` RPC). Also fixed a hardcoded `"SY3"` label caught live-testing a real CA1 import.
- **Migration 0172 failed its first live apply (`42P16`)** — `is_stub` inserted mid-column-list shifted `is_active`'s position; `CREATE OR REPLACE VIEW` requires exact name+position match on every pre-existing column, new ones only append at the end. Rolled back atomically (verified no partial damage), fixed in place (PR #446), re-applied clean.
- **CA1/E1.27 pilot run, live** — one real stub asset created end-to-end (`cbf535d9-a03f-4952-9396-7ae6c6e765ad`) to verify the full view+trigger+audit-log chain before Royce runs the real reconciliation set.
- **Contract-scope import tie-out fix (`cd7eb08`, live).** `previewAssetCountsAction` was matching `contract_scopes.jp_code` against `job_plans.code` instead of `job_plans.name` — `code` is an internal short type key ("LVACB"), the commercial-sheet code ("E1.25") lives in `name`. The wizard's "Linked assets" column had shown 0 for every commit, every site, since it was written. Verified live against CA1 (SKS tenant) pre/post-fix: 0/19 jp_codes resolved before, 19/19 after.
- **Duplicate `job_plans` row (E1.25) — resolved.** The stray fixture row flagged here earlier today was soft-deleted in a concurrent SKS data-hygiene session (part of a wider 4-row fixture batch cleanup). No follow-up needed.

## 2026-07-05
- **"EQ Service — Adoption" PostHog dashboard built** (id `794503`, combined-host `service.eq.solutions` + `eq-solves-service.netlify.app`). Root-caused the `error_thrown` spike it surfaced (382/week, ~100% of active users) — React 19 hydration mismatch from browser extensions injecting DOM attrs pre-hydration, not app-breaking (Sentry `eq-solves-service` shows zero issues ever — confirmed monitoring blind spot, not a broken pipe).
- **PR #441 (MERGED, deployed live, `067bf38`)**: added `/Minified React error #418/` to `NOISE_PATTERNS` in `app/providers.tsx` to quiet the confirmed non-blocking hydration noise from the `error_thrown` PostHog stream. Merged past one pre-existing unrelated integration-test failure (`app_config` relation missing — a schema-completeness gap, nothing to do with this change).
- **PR #440 (MERGED)** — `@eq-solutions/roles` bumped v2.3.0 → v2.4.0, `subcontractor` role wired into `UserSettingsForm.tsx`'s label map + `roles.test.ts`. Found already open (an earlier session's fix) while investigating a reported tsc break that turned out to be a false alarm from drifted local `node_modules` — merged the existing PR instead of duplicating.
- **PR #442 (MERGED)** — generic UI sweep: removed "DELTA ELCOM" / Equinix / Jemena / Maximo customer branding from every import screen (commercial-sheet, work-order, RCD, ACB) and onboarding/setup/media placeholders. `extractSiteHint` parser regex widened to `[<prefix>_]<SITE> Elec…` — verified empirically that legacy `DELTA ELCOM_*` filenames still auto-resolve their site alongside generic names.
- **PR #443 (MERGED)** — "Maximo ID" / "Jemena ID" / "JM #" labels renamed to generic "ID" across asset form/list/detail, check forms, import previews, search hint, and (caught during a full code review of the diff) the customer-facing DOCX report generators (`pm-asset-report.ts`, `maintenance-checklist.ts`) that the UI-only sweep had missed. Label-only — `maximo_id`/`jemena_asset_id` DB columns and DB enum values untouched, no migration.

## 2026-07-04
- **Offsite backup retired (PR #438 merged)** — deleted `backup.yml` + tombstoned its runbook (pointer to eq-context). Platform DR (ehow + eq-canonical + eq-canonical-internal) now owned by eq-context, not baked into a consuming app. Merged on the green `tsc + next build` gate (integration tests are the known pre-existing failure).

## 2026-07-03
- **Governed migration-apply pipeline LIVE (PR #412).** `service._eq_migrations` ledger (172-row grandfather seed) + `migrate-service.mjs` (atomic per-file apply, one transaction per migration) + `apply-service-migrations.yml` (dispatch gated behind a new `production` GitHub Environment, Milmlow required reviewer) + `check-service-invariants.mjs`/`service-invariants.yml` (anon zero-grant, view-invoker, table-isolation checks + app_data grant diff-scan on PRs). Closes the gap that let 0167 reach production with zero apply-time audit trail.
- **Security regression found + fixed (0169).** The 2026-07-01 hand-apply that excluded plant_equipment from `service.assets` used `CREATE OR REPLACE VIEW` without repeating `security_invoker` — Postgres resets the option list, so the view had been reading `app_data` with definer rights (RLS bypass on reads) since then. Fixed as the first migration through the new pipe; verified live, invariants gate now fully green.
- **PRs #413, #414, #415 merged → deployed.** #413: migration 0170 (dashboard tile's asset count never got the plant_equipment exclusion the `/assets` page did) + plain-English rename of "Y1 tie-out" copy. #414: batch-resolve on `/defects` (multi-select + resolve, not delete — defects have no delete concept). #415: scope brief for commercial-sheet asset creation (sheet only has per-JP-code quantities, not per-unit identity — any build here means stub rows).
- **Security + performance advisor audit.** Clean for eq-service's own surface post-0169. Found 2 out-of-scope ERROR findings (SECURITY DEFINER views reading as EQ Field domain) — chipped out, not eq-service's to fix.
- **Live verification cascade — 3 more bugs found + fixed, all merged and deployed.** Clicking through the newly-shipped defects batch-resolve feature as a real user found it 100% broken (`Invalid UUID` on every real defect — `BatchResolveDefectsSchema` used Zod's `.uuid()`, too strict for hand-seeded fixture ids; PR #417). Checking the dashboard found migration 0170 was never dispatched (the pipe's own merge-reminder had silently failed — PR #418 hardens it two ways) and that dispatching it live then failed too (missing trailing semicolon in a `CREATE FUNCTION` body broke the runner's statement-wrap — PR #416). All three merged, re-verified live: dashboard shows 0 assets, batch-resolve confirmed end-to-end (DB-verified `status='resolved'`).
- **Follow-up audit: `.uuid()` strictness bug found a 2nd time.** Audited all 67 Zod `.uuid()` validators against live data. `customers`/`sites`/`assets` clean (0 non-standard ids) — left untouched. `job_plans` confirmed broken: 4 of 56 rows (the foundational LVACB/LVNSX/HVSWGR/THERM plans) are non-RFC-compliant. Fixed `asset.ts`/`maintenance-check.ts`'s `job_plan_id` fields, extracted a shared `looseUuid` helper (PR #419, merged + deployed).

## 2026-07-02
- **PRs #410 + #411 MERGED → deployed** (2026-07-02 20:13/20:16 UTC). Contacts cutover + canonical-audit branch on main; Netlify auto-deploy. Also fixed the repo-wide-red canonical-types-drift gate (instrument_id types from 0164/0165 never synced; stale instruments canonical cols removed).
- **Contacts canonical cutover LIVE (migration 0167, PR #410).** service.customer_contacts/site_contacts became per-link security_invoker views over app_data.contacts + link tables with INSTEAD OF triggers — one source of truth with Shell/Quotes. Same names/shapes = zero app-page changes; 3 FK-hint embeds rewritten. Legacy tables renamed *_legacy_20260702 for the soak; drop + drift-guard ERROR flip = Steps 4-5. RLS read now 224/23 rows (was 109/0 on the fork). PR #411 = the day's canonical-audit branch.
- **Contacts Step 1 → canonical.** 22 real Service-local contacts (+links) merged into canonical `app_data.contacts` (208→230); 4 links added for already-canonical people; junk/dups/typo excluded; all tagged for rollback. `/contacts` still reads local tables until Step 3 (repoint). The 2026-06-25 roadmap's "contacts Service done" was false — local `customer_contacts` (base table, 109) was shadowing canonical.
- **Source-of-truth drift guard** (`audits/run.sql` + `CHECKS.md`) — flags any service base table shadowing a canonical entity; fires 2 (contact forks), WARN→ERROR once consolidated. The guard the roadmap asked for but never built.
- **Migration 0166** — `service.assets` view excludes `asset_type='plant_equipment'` (13 test instruments were surfacing as maintainable assets). app_data untouched.
- **Shell nav fix** — Calendar + Defects added to the Shell-embedded nav bar (`app/(app)/layout.tsx`); both were built + enabled but unreachable from Shell.
- **Spinner** "EQ Solves Service"→"EQ Service"; root `<html>` `suppressHydrationWarning` (React #418 forced full client re-render on Shell-embedded dashboard).
- **Debris dropped** — `app_data.pm_calendar`/`contract_variations`/`contract_scopes_history` (force-push orphans); 154 history rows backed up first.
- **Docs** — CLAUDE.md canonical scope corrected 3→~20 entities; D2 resolved (job_plans canonical).
- **ARMADA lighthouse — 2 recon passes, 9 issues shipped.** Budget bumped to 6 issues/600s runtime (PR #397). PRs #394–396 (dead shell-sso path, Zod validation on report settings, idempotency tests) + #404–409 (Sentry captureException wired across report-generation routes, canonical outbox/sync/members, generate-and-store PDF pipeline, cron dispatch-notifications, and server action helpers; unit tests for `lib/actions/audit.ts`) all merged.
- **3 dependency/docs PRs merged** — #363 (LOCAL_DEV.md `urjh`→`ehow`), #368 (`@netlify/functions` 5.2.0→5.3.0), #370 (React 19.2.4→19.2.7 — fixes a `FormData` regression in Server Actions from 19.2.6).

## 2026-07-01
- **Migration 0164** — dropped `canonical_id`/`canonical_synced_at` from `app_data.instruments` (incorrect 0158 link to `app_data.assets` plant_equipment). Rebuilt `service.instruments` view + INSTEAD OF trigger. `pullCanonicalInstrumentsAction` and "Sync from Shell" button removed. Instruments register is now self-contained.
- **Migration 0165** — `instrument_id uuid FK` added to `app_data.acb_tests` + `app_data.nsx_tests` (ON DELETE SET NULL). Rebuilt both service views and INSTEAD OF triggers.
- **ACB/NSX instrument picker** — Step 1 of both workflows now shows a calibrated instrument dropdown from the instruments register. `instrument_id` persisted on save.

## 2026-06-30
- **Migration 0163** — `service.sites` view now filters `active = true` (was `service_enabled` only). Archived sites retire from Service at the view layer. PR #381 merged to main.
- **Shell→Service handoff hardened into a shared, compile-enforced contract** — PR #374 (+ eq-shell #543): Service consumes `ShellHandoffClaims` and runtime-validates it, so `tsc` fails on either side if the handoff shape drifts. PR #373 added structured auth-handoff Sentry canaries (`captureAuthHandoff`: secret_mismatch/no_email/slug_unresolved = error, expired/malformed/cookie_unusable = warning; `validateSupabaseJwt` returns a discriminated reason, signature checked before expiry). PR #371 added RPC return-shape drift to the `canonical-types-drift` gate and deduped the inbound JWT type onto `ServiceJwtClaims`.
- **PR #376 — fail closed on an unresolved tenant slug** — a handoff for a tenant not provisioned in Service now returns 403 `service-account-not-found` instead of minting a JWT with Shell's UUID → blank app. ehow has one active row (slug `sks`), so SKS was unaffected.

## 2026-06-29
- **PR #365 — maintenance-check detail page parallelized** — ~10 sequential awaits collapsed into 3 `Promise.all` waves (~60% load-time reduction on the EU Supabase instance).

## 2026-06-28
- **PR #364 — `ShellTokenRefresh` component** — keeps `eq_service_jwt` alive for Shell-iframe sessions (REQUEST_SHELL_TOKEN → SHELL_TOKEN_RESPONSE → /api/shell-auth, fires 5 min before the 4h expiry). Admin link added to the Shell-iframe inline nav for manager-role users.

## 2026-06-10
- **Shell SSO — ROOT CAUSE found + fixed (eq-shell PR #306).** After 4 Service-side bugs fixed the day before, Service still showed the login page — root cause was in Shell, not Service. `COOKIE_AUTH = true` in Shell's `ServiceIframe.tsx` was set whenever `VITE_SERVICE_URL` ended in `.eq.solutions`; in COOKIE MODE Shell skips token minting entirely and loads the iframe at the root URL, relying on `eq_shell_session` being auto-sent — but Shell restores from Supabase cookies on refresh without re-minting `eq_shell_session`, so the cookie was absent at iframe-load time and `proxy.ts` never saw it → never called shell-sso. Fix: `const COOKIE_AUTH = false` — TOKEN MODE always (eq-shell PR #306, commit `16d3f19`, deploy `6a285d53`). TOKEN MODE (proven path): `token-exchange?aud=service` → Supabase JWT → `/shell#sh=<jwt>` → `/api/shell-auth` validates → `eq_shell_bridge=1` + redirect to `/` → `ShellReadySignal` fires `EQ_SERVICE_READY` → Shell reveals iframe. Diagnostic console.logs removed from `proxy.ts` + `shell-sso` (eq-service PR #274).

## 2026-06-09
- **Shell SSO — 4 bugs found and fixed (PRs #267–#270).** Service was showing its own login page instead of auto-authenticating when loaded as a Shell iframe. **Bug 1 — Edge runtime crypto failure (`proxy.ts`):** all HMAC verification, `generateLink`, and `verifyOtp` logic was in `proxy.ts`, which runs in the Netlify edge (Deno) runtime — `node:crypto` silently fails on Deno, so SSO always fell through to the login page; fixed by moving all auth logic to a new Node.js API route `/app/api/shell-sso/route.ts` (`proxy.ts` now only detects the cookie and redirects). **Bug 2 — wrong redirect hostname:** `/api/shell-sso` built its success redirect by cloning `request.nextUrl`; on Netlify, `nextUrl.host` is an internal handler hostname — fixed via `new URL(safePath, process.env.NEXT_PUBLIC_SITE_URL)`. **Bug 3 — HMAC key mismatch (`EQ_SESSION_SALT` vs `EQ_SECRET_SALT`):** Shell signs `eq_shell_session` with `EQ_SESSION_SALT`; Service was reading `EQ_SECRET_SALT` only — fixed via `const salt = process.env.EQ_SESSION_SALT ?? process.env.EQ_SECRET_SALT`. **Bug 4 — infinite redirect loop on SSO failure (PR #270):** `proxy.ts` intercepted ALL non-bridged requests including `/auth/signin` — when shell-sso fails it redirects to `/auth/signin`, which proxy intercepts again → shell-sso → ... → `ERR_TOO_MANY_REDIRECTS`; fixed via `isSsoExempt = alreadyBridged || pathname === '/api/shell-sso' || isPublicPath(pathname)`. Commits: `9d26b85` (Bug 1), `5e5046e` (Bug 2), `4155abf` (Bug 3), `f3b1c5e` (Bug 4). Deploy `6a27f277`.
- **Site-credentials encryption (migration 0123) — PRs #268/#269 merged** — encryption schema corrected (`public`→`app_data`), RPCs fixed, `SITE_CREDENTIALS_KEY` set; rekey confirmed 0 rows.

## 2026-06-08
- **Sprint 7 — EQ Service cut over urjh → ehow (PR #257 merged)** — 28 CMMS tables + data + 9 storage files migrated to ehow; Netlify env (Supabase URL/keys, SITE_URL, Sentry) swapped; code domain refs updated. The old `urjhmkhbgaxrofurpbgc` project was decommissioned and deleted 2026-06-22 (PR #327).

## 2026-07-08
- Fixed a live-database-column-mismatch bug class across the customers/sites/assets API routes, the compliance report, the admin canonical export tool, and the dashboard's onboarding-dismiss actions. Root cause was code that bypassed the generated TypeScript types (an untyped Supabase client pointed at the raw app_data schema instead of the canonical service views) so the wrong column names were never caught at compile time. Site and asset creation via the API had likely never worked; the compliance report's maintenance/testing/ACB/NSX sections were silently always empty.
- The pre-visit-brief fix in this batch was rewritten defensively (matches its sibling report-builders' pattern) but the claim it was fixing a live "migration 0147 broke this" bug does not hold up under live testing — corrected after the fact, see the follow-up entry below.
- Migration 0175 applied live: adds tech_onboarded_at and setup_checklist_dismissed_at to service.tenant_members, restoring the dashboard welcome-card and setup-checklist dismiss buttons (previously silently failing on every click since the columns never actually existed).
- **Follow-up sweep, same day:** live-tested every PostgREST embedded-join pattern in the repo against the actual API (not just docs/comments) and found 3 more genuine, unrelated bugs — none caused by migration 0147, all caused by code assuming a foreign-key relationship that was never actually declared in the database. The Customer Portal's "Your Reports" page has been silently showing "no reports delivered" to every customer regardless of their real history (report_deliveries has no FK to maintenance_checks/customers). The dispatch-notifications cron's customer-upcoming-visit-notice has silently never sent since it shipped (maintenance_checks.assigned_to has no FK to profiles) — a code comment claiming this was tested and correct was not accurate. The ACB/NSX "assign to" picker on Create Check has always loaded empty (tenant_members has no FK to profiles). All 3 fixed by batch-fetching instead of embedding.
- All of the above is on PR #477 (**merged live**, https://github.com/eq-solutions/eq-service/pull/477 — Netlify deploy fired as a direct result). Full test suite (311 tests), tsc, build, and lint all pass; CI green apart from one pre-existing, already-documented, unrelated integration-test failure.

## 2026-07-16 (2)
- **PR #543 MERGED + LIVE (`b587d84`) — `/terms` temporarily unpublished pending legal review.** Page carried a self-admitted internal comment ("plain-English draft — review with Webb Financial / SaaS-literate lawyer before relying on in a commercial dispute") and read in a harder B2B/IP-protective tone than the rest of the suite. Now `notFound()`s; prior content preserved in git history for restore after actual legal review. Verified live via Netlify MCP (deploy `ready`, commit ref matches) and a browser check (`/terms` → redirects to `/auth/signin`, confirming the old content is gone). Prompted by Royce's Monday meeting with an SKS stakeholder — full context in `eq-context/eq/pending.md` "Terms/legal review" entry (2026-07-16).

## 2026-07-19
- **PR #551 MERGED + LIVE — access-model cluster 3: `service.reopen` + `service.record_tests` (`@eq-solutions/roles` v2.5.3).** `reopenCheckAction` was `canWrite() OR isAssigned` — any technician assigned to a check could reopen it regardless of role; now `canReopen()` only, closing that bypass. `updateInstrumentAction` (the sole calibration-write path) now `canRecordTests()`. Genuine server-side enforcement — `requireUser()`'s `role` is resolved from a server-verified JWT, never client input.

## 2026-07-20
- **Migrations 0187 + 0188 APPLIED LIVE to ehow** — fixed 28 `service.*` RLS policies re-evaluating `auth.jwt()`/`auth.uid()` per row instead of once (wrapped in a scalar subquery so Postgres hoists it into a one-time InitPlan), plus added 12 missing FK covering indexes. Both validated via BEGIN/ROLLBACK against live ehow before commit, applied through the governed `apply-service-migrations` pipeline. Confirmed clean via a post-apply Supabase advisor re-run: 0 remaining findings of either kind in the service schema.
- **PR #564-567 MERGED** — 4 dependabot version bumps (next, react-dom, tailwindcss, @tailwindcss/postcss), the last of which ships a real postcss XSS fix. **PR #520 MERGED** — resend bump. **PR #524 MERGED** — Royce's balloon-years proposal doc (`docs/proposals/balloon-years.md`); not stale, migration 0182's own comment cited it by path before it existed on `main`.
- **PR #570 MERGED — jszip lazy-loaded off `/admin/backup`'s first load.** Only the Preview card's file handler needs it; converted the top-level import to a dynamic `import()` inside `handleFile`, same pattern as the acb-excel lazy-load (#509). Confirmed via build output: the page's own chunk is 10.4KB with zero JSZip code.
- **PR #571 MERGED — trimmed Sentry's server-side default integration set from 44 to 17.** `sentry.server.config.ts` had `tracesSampleRate: 0`, but the SDK's `hasSpansEnabled()` treats `0` as tracing-configured (not nullish), so the full ~27-integration auto-performance-instrumentation set (mysql/mongo/kafka/prisma/langchain/openai/express/etc., mostly for frameworks this app doesn't use) was still being initialized on every cold start. Omitted `tracesSampleRate` entirely instead — verified via `getDefaultIntegrations()` directly against the installed SDK. **Post-deploy correction:** checked the actual deployed Lambda size via Netlify — it did not shrink (27,160,990 → 27,161,093 bytes, noise). Netlify's file-tracing bundles by static require-graph, not runtime gating, so this cuts real init-time work but not the packaged bundle size as originally claimed.
- **PR #572 MERGED — removed the broken "Try the demo" tile from the sign-in page.** Found while trying to live-verify the Sentry fix: the demo tenant (`a0000000-0000-0000-0000-000000000001`) has zero rows anywhere on live ehow (tenants, tenant_members, customers, sites), and the `demo@eqsolves.com.au` auth user doesn't exist — casualty of the urjh→ehow cutover (2026-06-22), never reseeded. Both the one-click demo button and manual credential entry failed with a raw "Invalid login credentials" error. Royce's call: remove the tile now, rebuild the demo data later rather than doing a full reseed in this pass. `lib/utils/demo.ts` / `app/demo/route.ts` / `startDemoSessionAction` left intact for a one-file reconnect once reseeded.
- **PR #573 MERGED — parallelized sequential fetches on the dashboard and Maintenance Checks list.** Dashboard: `getCachedTenantSettings()` ran as a standalone sequential await before the main data batch; folded in (the value isn't read until well after the batch resolves). Maintenance list: 6 independent lookups (sites, customers, job plans, canonical roster, current-FY scope items) plus the checks query itself ran as 7 sequential round-trips despite zero data dependency between them — merged into one `Promise.all`. Also deleted `siteAssetPlans`, an 8th sequential fetch that was dead code (fetched, never read anywhere). Confirmed via Netlify post-deploy: commit `ac38780` live, secret scan clean, `/dashboard` and `/maintenance` both return clean 307s (no 500s) for an unauthenticated smoke check. Separately confirmed the earlier Sentry-integration-file-exclusion idea (to actually shrink the Lambda) is unsafe -- `@sentry/node`'s own bootstrap does an unconditional top-level `require()` of the whole tracing barrel.
