# EQ Intake — Changelog

## 2026-08-01 (cont. — PR #102 MERGED)
- **Ported eq-shell's same-day production hotfix back upstream so it can't regress.** eq-shell shipped `ed0e59de` fixing `/intake` crashing for every user (duplicate React instance from a version-pin mismatch: root bumped to `react@^19.2.7` for the react-router v8 migration, but `@eq/confirm-ui`/`@eq/intake-demo` stayed exact-pinned to `19.2.6`) — but only against its own vendored copy, never here. Bumped both packages' `react`/`react-dom` devDependency to `^19.2.7` to match; lockfile now resolves both to `19.2.8`, same as eq-shell.
- Found while verifying eq-shell's new `scripts/revendor-intake.mjs` against a clean checkout of this repo — see eq-shell changelog, PR #1169. Without this, the next routine re-vendor would have silently reverted eq-shell's fix and reintroduced the outage.
- `pnpm -r build`, `pnpm -r test` (238 pass, 2 pre-existing skips), `pnpm run check:packages` all clean. [PR #102](https://github.com/eq-solutions/eq-solves-intake/pull/102), merged (`0de1fd7`).

## 2026-08-01 (cont. — PR #98 MERGED)
- **Duplicate-sites console had two dead ends — fixed.** Checked the actual RPC gating rather than assuming: `eq_site_merge_preview` (eq-shell 0185) carries no role check server-side, only `eq_site_merge_execute` does — so hiding the whole `MergePanel` behind `canMergeSites` was stricter than the backend requires. `eq_site_advisory_adjudicate` (0183) has always accepted an optional `p_note` param the UI never sent.
- **`DuplicateMergePanel.tsx`**: `MergePanel` no longer early-returns for non-managers — everyone sees "Preview merge" and the row/table counts; only the "Confirm merge" button itself is gated on `canMerge`, replaced with a "Ask a manager to confirm this merge" hint otherwise. "Unsure" now opens an inline optional note (`notingId`/`noteDraft` local state) before calling `onAdjudicate(id, "unsure", note)`; the note threads through `handleAdjudicate` → `adjudicateSiteAdvisory` → the RPC, and renders back via `it.verdict_note` next to the recorded verdict.
- `tsc --noEmit` clean, `vitest run` 48/48 (no prior test coverage on this component — nothing to regress). [PR #98](https://github.com/eq-solutions/eq-solves-intake/pull/98), merged. Re-vendored into eq-shell same session ([PR #1156](https://github.com/eq-solutions/eq-shell/pull/1156)) via a throwaway detached worktree at `origin/main` as the copy source — real diff 2 files (confirmed `--ignore-space-at-eol`), ~285 files of CRLF-normalization noise from the copy step.
- **Found live-testing the shipped fix, not caused by it:** `eq_site_merge_execute` was missing its `authenticated` EXECUTE grant on both ehow and zaap — see eq-shell changelog, PR #1168.

## 2026-08-01 (cont. — PR #101 MERGED)
- **Fixed a stale manifest pin that was keeping eq-shell's last Dependabot alert open.** PR #100's `ajv` fix went in as a root `pnpm.overrides` entry only — it correctly forced the installed version to 8.20.0 everywhere, but never touched `eq-schemas/package.json`'s own declared `"ajv": "8.17.1"`. Bumped the manifest to `8.20.0` to match; `pnpm install` produced zero lockfile diff (confirming nothing was actually changing at install time). Typecheck + test clean. [PR #101](https://github.com/eq-solutions/eq-solves-intake/pull/101), merged (`c3762eb`). Re-vendored into eq-shell same session ([PR #1167](https://github.com/eq-solutions/eq-shell/pull/1167)) — confirmed via the Dependabot API that the alert flipped to `fixed` immediately after merge.

## 2026-08-01
- **Closed the original 3 Dependabot alerts eq-shell inherits through the vendored copy.** vitest 2.1.5 → 3.2.6 across all 7 `eq-platform/packages/*` (2 CVEs). vite forced to 6.4.3 workspace-wide via `pnpm.overrides` — the "already patched" 5.4.21 turned out to still be vulnerable to 2 advisories disclosed in 2026 (GHSA-fx2h-pf6j-xcff, GHSA-4w7w-66w2-5vf9) that were never backported to the 5.x branch, confirmed via direct GHSA vulnerable-range lookup rather than trusting the fix-version list. xlsx 0.18.5 removed entirely — production code (`eq-intake/src/readers/xlsx.ts`) had already migrated to ExcelJS in July specifically to drop this dependency; xlsx only survived as a devDependency building in-memory `.xlsx` test fixtures in 3 test files, rewritten against ExcelJS instead (no patched xlsx version exists on npm — SheetJS only ships fixes via their own CDN now). 616/616 tests pass, build+typecheck clean on all 7 packages. eq-solves-intake [PR #99](https://github.com/eq-solutions/eq-solves-intake/pull/99), merged, tagged `v2026.08.01-deps`.
- **Closed the rest of the `pnpm audit` findings in `eq-platform/`, the 20 left over after PR #99's vitest/vite/xlsx fix.** eq-solves-intake [PR #100](https://github.com/eq-solutions/eq-solves-intake/pull/100), merged (`cbeda65`). `ajv`/`fast-uri` (real runtime deps of `@eq/schemas`' validation layer) and `uuid` (real runtime dep via `exceljs`, major 8→11 bump — same override already validated safe in `eq-solves-service` PR #605) got a proper fix; `tar` (critical, major 6→7 bump, but reached only via `canvas`'s install-time-only native-binary extraction — tested in isolation first, no runtime exposure) and the rest (`js-yaml`/`postcss`/`@babel/core`/`brace-expansion`/`undici`/`esbuild`, all build/dev-tool-only paths) closed via `pnpm.overrides`. `pnpm audit` → 0 vulnerabilities, `pnpm -r build && pnpm check:packages && pnpm -r test` green (645/645 tests).
- **First CI run failed and got fixed same session** — the initial overrides used unbounded `">=X.Y.Z"` ranges; pnpm resolved several of them past what was actually tested on CI's Node 20 (`@babel/core` jumped to a v8 that needs Node ≥22, breaking `pnpm install --frozen-lockfile`; `fast-uri`/`js-yaml`/`undici`/`uuid` also silently jumped further than described). Invisible locally on Node 24. Fixed by bounding every override to an explicit upper bound on the verified major line, re-checked resolved versions directly against `pnpm-lock.yaml`, re-ran CI green.
- Re-vendored into eq-shell separately via [PR #1159](https://github.com/eq-solutions/eq-shell/pull/1159) (a different, concurrent session — picked up the earlier #99 fix, not #100; #100 doesn't need a re-vendor since it only touches `eq-platform`'s root workspace config, not vendored package source). Confirmed live on core.eq.solutions.

## 2026-07-31
- **Contacts/Staff Dupes tab gains Archive; Tidy tab's "Record" column shows real context.** eq-solves-intake [PR #96](https://github.com/eq-solutions/eq-solves-intake/pull/96), merged (`59849f5`). Royce, testing live: the Dupes tab had zero actions (Sites already had Flag/Preview/Confirm merge), and the Tidy tab's bare row labels ("Accounts", "Rafael") gave no way to tell two same-labelled rows apart. Archive reuses `archiveDuplicateRecord`/`isArchivableDuplicate` (#95). Record context is a local join against the already-fetched full row — no `GapItem`/tidy-pass schema change.
- **New Trades settings screen + persisted "Not a duplicate" dismiss.** eq-solves-intake [PR #97](https://github.com/eq-solutions/eq-solves-intake/pull/97), merged (`911241a`). New `getFieldSuggestedValues()` reads `staff.schema.json`'s `x-eq-suggested-values` directly (`RemediationQueue`'s trade dropdown previously hardcoded a copy). Dismiss is group-level (shared value), not pairwise like Sites' advisory model — backed by new eq-shell tables (`app_data.tenant_trades`, `app_data.eq_duplicate_dismissals`). **Found and fixed while wiring dismiss:** `EntityDrillDown`'s `duplicateRows` used each row's own raw field value for `_dupeKey` instead of the shared normalized group value — two differently-cased members of the same group (e.g. differently-cased emails) could silently disagree on their own group's identity. Also affects Sites' merge-candidate grouping, fixed at the source.
- **Site-merge manager gate fixed in eq-shell (not this repo — flagged here for the full story).** Royce hit `eq_site_merge_execute: caller is not an active manager` on a real merge despite genuinely holding manager access — the RPC's check was reading identity the wrong way. eq-shell [PR #1137](https://github.com/eq-solutions/eq-shell/pull/1137).
- All four items came from Royce's live screenshot review of the Review Queue / Tidy / Dupes screens on core.eq.solutions — investigated against the real code and the live database before building. Re-vendored into eq-shell via PRs #1140/#1142, all merged and live.

## 2026-07-30
- **Tidy tab: Customers/Sites gap fixes now render a dropdown for closed-enum fields and actually save.** eq-solves-intake [PR #93](https://github.com/eq-solutions/eq-solves-intake/pull/93), squash-merged to `main` (`fc46a41`), Royce's "merge it once CI's green" go. Royce challenged the screen directly off a screenshot: the Edit control on a data-gap row always used a plain text input, even for fields like `customers.type` that only accept a closed set of values (lead/prospect/active/churned) — the schema already carried the enum, nothing read it. Separately, neither Edit nor the AI-assisted Suggest action ever actually saved anything; both only mutated local component state (`setRows` + a `hasLocalEdits` flag), the "Changes are local only" banner was the literal truth, not a workaround message. `GapItem` gained `allowed_values` (from the validator's `field_enum_invalid.allowed`, with a schema-enum fallback via new export `getFieldEnumValues()` so a blank required-enum gap gets a dropdown too); the Fix column renders a `<select>` when present. `commitEdit`/`acceptSuggestion` now call the existing `commitTidyFixes` RPC path for real (with a saving/error state), falling back to the old local-only behaviour only when no live Supabase client exists. `@eq/intake` + `@eq/intake-demo` both `tsc --noEmit` clean; `@eq/intake` test suite 123/123 green (2 new tests on `allowed_values`).
- **Live DB fix: `eq_tidy_commit_fixes`'s per-`table:field` whitelist was missing `customers:type` and `sites:site_type`** — both closed-enum fields the tidy scan already flags as gaps, silently un-savable via the one real commit path regardless of any frontend fix. Found by checking the live function on ehow (`pg_get_functiondef`) rather than trusting the repo's `sql/049` (already superseded live by `sql/062`'s shape). New migration `sql/063_tidy_commit_fixes_enum_fields.sql`, applied live to ehow via Supabase MCP on Royce's explicit go, verified live post-apply.
- **Possible duplicate sites: "Confirm merge" failures were captured but never rendered.** `MergePanel`'s post-preview render branch never checked `mergeErr` — only the pre-preview branch did — so a real server-side rejection (wrong role, stale verdict, already merged, missing site) looked identical to the button just not working. eq-solves-intake [PR #94](https://github.com/eq-solutions/eq-solves-intake/pull/94), merged (`c320629`).
- **Remediation Queue: one-click Archive for "Other duplicate flags"** (staff/contacts). New RPC `eq_archive_duplicate_record` (`sql/064`) replicates eq-shell's own archive field set (staff: `active`+`on_roster`; contacts: `active` only) via a tenant-JWT-scoped function, since eq-shell's mechanism is session-cookie-authenticated code eq-intake can't call directly. Full open-event/commit/close-event/resolve audit lineage, same as Approve. eq-solves-intake [PR #95](https://github.com/eq-solutions/eq-solves-intake/pull/95), merged (`9d9403d`). Migration applied live to ehow, verified.
- **PR #84 (MERGED squash `b23fc99`) — dropped a now-unused `useCallback` import in `IntakeHealthHome.tsx`, left over from PR #81's handler extraction.** eq-intake-demo's own `tsc --noEmit` doesn't flag unused imports; eq-shell's stricter `tsc -b` project build does — caught while re-vendoring for eq-shell #1094. `tsc --noEmit` clean, 48/48 tests pass.
- **PR #83 (MERGED squash `219454e`) — Import + Reconcile folded into one "Bring Data In" screen (tab redesign PR2).** The two screens read as near-identical "drop a file here" tabs with no signal for which to use. `IntakeDropZone` is now the single entry point; each classified file slot gets an optional "Check for conflicts" action that opens `ReconcileModule` scoped to just that one slot (new `initialSlot`/`onClose` props — skips Reconcile's own parse/classify since the slot already did it, straight to fetch-canonical + reconcile), instead of a separate tab. Tracked by slot object reference, not array index, in `IntakeModule` — removing an earlier slot shifts indices, which could otherwise silently reconcile the wrong file. Standalone Reconcile tab/mode removed; the drop-zone-driven `ReconcileModule` path used by `App.tsx`'s standalone playground is untouched (new props optional, backward compatible). SimPRO-specific copy on this screen replaced with generic wording. Deliberately NOT attempted: routing resolved reconcile rows into the shared multi-entity `CommitView` bundle — flagged as a separate future iteration. `tsc --noEmit` clean, 48/48 tests pass.
- **PR #81 (MERGED squash `5471c18`) — Health + Queue collapsed into Overview + To Do (tab redesign PR1).** Royce found the live 5-tab layout overwhelming — Health crammed a score, six sub-metrics, a fix-it list, and a site-merge decision tool onto one screen. Extracted `SiteAdvisoryPanel`/`MergePanel` (the real `readSiteAdvisory`/`previewSiteMerge`/`executeSiteMerge` merge flow) out of `IntakeHealthHome.tsx` into a new, self-contained `DuplicateMergePanel.tsx` (own load/refresh effect), mounted inside `RemediationQueue`'s tab instead — it's a decision, not a diagnostic. `DuplicateStrip`'s separate on-demand `detectAllDuplicates` scan stayed on Overview (genuinely diagnostic). `RemediationQueue`'s pre-existing, unrelated `category:"duplicate"` signal (from `eq_queue_list`) got distinct copy so it doesn't read as contradicting the new merge widget in the same tab. Health/Queue tab badges merged into one To Do count; six-metric breakdown collapsed behind a `<details>` disclosure. Tabs relabelled: Health→Overview, Queue→To Do, Import→Bring Data In (Reconcile untouched, folded in PR #83). `tsc --noEmit` clean, 48/48 tests pass.
- **PR #80 (MERGED squash `9c6e5ad9`) — closed the /intake commit-path gap that bypassed staging review.** Confirmed live that the Health-console's "Into EQ" commit wrote straight to canonical tables via `eq_intake_commit_batch`, while the other importer's commit routed through `intake-stage.ts`'s staging/conflict-detection gate first — two divergent commit paths for the same kind of write. Added a pluggable `stageCommit` hook to `commit-canonical.ts` (`CommitOptions`/`EntityCommitResult` gained `stageCommit`/`stagedCount`); when supplied, chunks route through the host's staging function instead of the direct RPC, and FK-map-building guards now also fire on `stagedCount > 0` so a fully-staged parent still produces an explicit rejection for dependent rows rather than a silent null-FK pass. Companion wiring in eq-shell #1088. `tsc --noEmit` clean.

- **PR #82 (MERGED squash `b0c7fb6`) — Reconcile phone/ABN normalization now actually fires for customer and contact rows.** `PHONE_FIELDS`/`normaliseRow` in `ReconcileModule.tsx` keyed off plural entity names (`customers`, `contacts`), but `classifySheet` (via `ROLE_REGISTRY` → `x-eq-entity` in `simpro-schemas.ts`) returns singular names (`customer`, `contact`, `staff`, `site`) — the lookup never matched for two of the three entities, only `staff` happened to work since singular/plural coincide there. Fixed the keys to singular. `tsc --noEmit` clean on the file. Branch was 14 commits behind `main`; merged up before opening the PR so the diff showed only the real 4-line change.
- **PR #79 (MERGED `139f515`) — git history for `parse-maximo-pdf-wo` caught up to what's already live.** The edge function was iterated live via direct Supabase deploys (v5-v9) across several sessions fixing real production bugs against real Equinix PDF exports: switched from vision-only to text-first extraction (`unpdf`) with vision as a fallback only; fixed two separate `WORKER_RESOURCE_LIMIT` crashes (concurrent per-file text extraction, then a client that still bundled every file into one request); added the `"5A"` → 5-yearly frequency-suffix mapping; fixed job-plan matching to use the short mnemonic code instead of the E-number. This PR is the git record of all of it — the deployed function was already correct.
- **PR #85/#86 (MERGED `c02dfd0`/`a784cab`) — two real bugs found while triaging a Licences Tidy-pass crash.** #85: the licence cross-field rule used `NOT IN`, an operator the validation parser never implemented — silently matched nothing, live-fixed to a chained `!=`/`AND` form. #86: `coerceDate` treated every blank date field as an error, unlike every one of its sibling coercers (blank stays blank, only a malformed non-blank value errors) — was flagging every no-expiry licence as a data problem.
- **PR #87/#89/#90 (MERGED `47f51c8`/`8c92c1f`/`59571c6`) — field-importance rulebook: one shared source of truth for "does a blank field count as a gap," replacing 4 independently-disagreeing field lists** (`health-score.ts`'s `INSPECTED_FIELDS`, `EntityDrillDown.tsx`'s `GAP_FIELDS`, `compliance-metrics.ts`'s hand-picked checks, `IntakeHealthHome.tsx`'s action-card copy). New `field-importance.ts` tiers every nullable field per entity (Staff/Licences/Assets/Sites) `critical`/`important`/`optional`; only critical+important ever count as a gap anywhere in the UI. Royce rated the first cut (#87, tiers only, nothing wired to any consumer) 35/100 — "im still not convinced about this... what do you think we are doing?" — the honest answer was that it changed nothing visible yet and the one live effect (adding previously-uninspected fields) increased flagged rows, the opposite of his original complaint. #89 made the rulebook actually enforced across all 3 consumers + added Sites. **Live-data verification against real ehow rows caught a genuine tiering mistake before it shipped wrong**: `sites.customer_id` was tiered critical on the assumption a blank meant an orphaned record, but real data showed 50% of sites (129/258) legitimately have no customer (internal/unassigned/prospective) — #90 downgraded it to optional after Royce's call, re-verified live that Sites' flagged-row count returned to the pre-#89 baseline (101/258).
- **PR #91 (MERGED `53a50f9`) — tenant-editable field-importance settings screen (client half).** A gear icon off Overview (deliberately not a 5th tab) lets a tenant re-tier one of their own fields without touching the shared EQ-wide defaults. `getFieldTier`/`getFlaggableFields` gained an optional `FieldImportanceOverride[]` param (additive — every existing call site unchanged); `health-score.ts`'s `INSPECTED_FIELDS` moved from a module constant to a per-call build so overrides show up immediately. New `useFieldImportanceOverrides` hook (one fetch/write path) + `FieldImportanceSettings` component (entity tabs, tier segmented control, "EQ default" indicator + reset). Companion server-side migration: eq-shell #1104 (table + 3 RPCs) + #1106 (re-vendor to surface it). Customers/Contacts deliberately excluded — a separate, pre-existing bug (EntityDrillDown checks a synthesized `phone` field that health-score.ts's raw-column check doesn't match) needs fixing first. Click-tested live in the standalone dev playground (no backend): gear icon, all 4 entity tabs, Sites' `customer_id` correctly defaulting to Optional.
- **PR #92 (MERGED `d1a0ebd3`, vendored into eq-shell #1113) — Customers/Contacts migrated into the rulebook, closing the last 3 items deferred from #91.** Fixed the actual phone-field mismatch first: `EntityDrillDown.tsx` derived a coalesced phone value (`mobile_phone || primary/work_phone`) pre-gap-check while `health-score.ts` checked only the single raw column — confirmed live on ehow this was silently costing 30 of 210 contacts a wrong "missing phone" flag (mobile on file, no landline). Fixed at the rulebook level: `FieldImportanceEntry` gains `sourceFields?: string[]`, and new `isFieldBlank()` is the one place the coalescing logic lives now. Customers (email/phone/abn) and Contacts (email/phone) added to `FIELD_IMPORTANCE`, both tiered `important` — deliberately preserving existing flagged-behavior rather than inventing a new judgment call the way `sites.customer_id` needed one. `health-score.ts`/`EntityDrillDown.tsx` both read these two entities from the shared rulebook now; `FieldImportanceSettings.tsx`'s Customers/Contacts tabs are enabled. Also generalized `IntakeHealthHome.tsx`'s `deriveActions()` — the 3 hardcoded staff-only checks replaced with a generic loop over every entity's `HealthScore.gaps` (new `gapCounts` field), reading tier + why straight from `FIELD_IMPORTANCE`; the two licence-specific actions (no records / expiring) stay hand-written since they're not blank-field checks. Verified: 121+48 tests pass, both packages typecheck clean, click-tested live in the standalone playground, and the settings screen's full save round-trip (set → read → reset) proven live against ehow via direct RPC calls with a simulated JWT tenant claim — no browser sign-in needed, since Claude declines to enter the shared dev PIN itself. Merged 2026-07-30; live end-to-end on core.eq.solutions once eq-shell #1113 (re-vendor) deployed.

## 2026-07-26
- **PR #77 (MERGED squash `ed6b9d7`) — shipped the 4 items deferred from PR #76's polish round: style cleanup, manual refresh, mobile fix, stale docs.** `MergePanel`/`SiteAdvisoryPanel` in `IntakeHealthHome.tsx`: replaced ~40 inline `style={{...}}` blocks with `eq-health-*` CSS classes — caught two phantom CSS variables in the process (`--eq-ink-soft`, `--eq-danger`) that don't exist in `@eq-solutions/tokens` or this file's own `:root`, so the hardcoded fallback hex was always what actually rendered; swapped for the real tokens (`--eq-muted`, `--eq-err`). Added a manual Refresh control to the Health tab (a `refreshTick` state re-runs the load effect on demand) so acting elsewhere doesn't require leaving and re-entering the tab. Mobile check via a temporary mock-data harness in `App.tsx` (reverted before commit) at phone width found and fixed a real bug: `.eq-health-dims`'s 220px `min-width` let it squeeze to ~77px-wide bars instead of ever triggering `flex-wrap`; new `@media (max-width: 480px)` stacks the ring above full-width dims. `SPRINT-SUMMARY.md` got a "Since this was written" section instead of a full rewrite, pointing at this changelog as the live source of truth. `tsc --noEmit` clean, 166 tests green pre-merge, CI green in 1m21s.
- **PR #76 (MERGED squash `7429424`) — demo app polish: tab pending-count badges, progressive Health-tab loading, un-capped dupes list, Ask filter carry-through.** Royce asked for a review of the demo app plus polish ideas; reviewed the live code across all 5 tabs, offered 8 grounded ideas, Royce picked 4. `IntakeModule.tsx` now fetches lightweight Health/Queue pending counts and shows them as tab badges. `IntakeHealthHome.tsx`'s loading gate no longer blocks the whole section — score ring/dimension bars and the entity-card grid show a small placeholder until `scores` lands, everything else was already independently gated via `Promise.allSettled`. `SiteAdvisoryPanel`'s hard 8-item cap gets a "Show all N" toggle (caught a Rules-of-Hooks bug mid-edit — `useState` had landed after an early return — before it shipped). Exported `applyFilters` from `ask-canonical.ts` (+ `index.ts`) so `EntityDrillDown` reuses Ask's own filter semantics via new `initialFilters`/`initialFilterLabel` props + a clearable chip; `AskCanonical`'s "Open {entity} →" now passes the question's filters through instead of dropping them. `tsc --noEmit` clean on both packages, 166 tests green. No auto-deploy configured for this repo.

## 2026-07-21
- **PR #75 (MERGED squash `b32ff32`) — closed out security-register finding SEC-2, which turned out to already be fixed.** Asked to fix "RLS trusts forgeable `user_metadata` for tenant isolation" on `app_data.eq_intake_rate_limits`; live-verified `pg_policies` on both tenant planes (zaap/eq-canonical-internal, ehow/sks-canonical) before writing anything — both that table and `api_intake_calls` already key `tenant_isolation` on `app_metadata`, via eq-shell's canonical `tenant-migrations/0023_intake_infra.sql` (the original SKS→canonical port) + `0178_intake_rate_limit_harden.sql` (later reconciliation). This repo's own `sql/029_rate_limiting.sql` + `sql/032_api_audit_log.sql` are pre-port staging copies (never self-serve applyable to live planes) that still showed the superseded `user_metadata` version — annotated both with a pointer to the real fix, left the historical DDL untouched. No schema/DDL change, docs only. Companion fix in eq-context: closed SEC-2 in `ops/security-register.md` and removed its stale `security_audit.py` `ACCEPTED_ERRORS` entry (past its 2026-06-12 review-by date).

## 2026-07-16
- **PR #73 (MERGED squash `6052a09`) — `getSiteDupeUsage` + usage-based survivor pick for the Sites Dupes tab.** PR #71's survivor auto-pick used a shallow active+customer heuristic that got SY9 backwards — the "expected wrong" active row actually held every real record (499 assets, 10 contract scopes, 2 quotes). New `getSiteDupeUsage()` wraps eq-shell migration 0187's `eq_site_dupe_usage` RPC (fast triage counts across assets/quote/contract_scopes/jobs/maintenance_checks). Console: usage becomes the primary survivor signal, legacy heuristic now a fallback only for 2-row groups tied at zero; unlocks 3+-row dupe groups previously locked out entirely — one row with real usage in a group of any size is enough to auto-pick a survivor, one idempotent flag call per loser. New "Records" column shows the evidence. 15 tests (10 existing + 5 new). `pnpm -r build` + `check:packages` green. Vendored into eq-shell PR #880.
- **PR #74 (MERGED squash `9906530`) — backported two eq-shell-only fixes into source.** The 2026-07-16 re-vendor (PR #879) had manually restored two fixes that only existed in eq-shell's vendored copy (calibration-cert tsup/export split from shell PR #506, a production 502 fix; `@fontsource/plus-jakarta-sans` as a direct not phantom dependency) — this backports both to source so the next re-vendor doesn't need to rediscover and re-restore them. `pnpm -r build` + `pnpm -r test` green, 219 tests.
- **PR #72 (MERGED squash `937d9dd`) — wired the merge Preview/Confirm buttons into the demo console.** The write-time resolver, verdict adjudication, and AI-suggested verdicts (PRs #67-71) had all shipped, but nothing in `IntakeHealthHome.tsx` actually called the already-exported `previewSiteMerge`/`executeSiteMerge` (#70) — `canMergeSites` was threaded to `EntityDrillDown` only. Added `MergePanel` + threaded `canMergeSites` and merge preview/execute state through `SiteAdvisoryPanel`; passed the prop from `IntakeModule`. Ported from eq-shell's vendored copy (shell PR #868), which had this UI but it was never backported to source — root cause was a stale local branch (18 commits behind main) making the already-merged library layer look unbuilt. `pnpm -r build` + 161 tests (both packages) green. Re-vendored into eq-shell PR #879.
- **PR #71 (MERGED squash `b48cbdd`) — `flagSitePairForMerge` + "Flag for merge" on the Sites Dupes tab.** The generic "Sites → Dupes" tab (name-match, read-only) finds real duplicates the write-time resolver (0179) never saw — 43 candidates on ehow with no write path today. New client wrapper `flagSitePairForMerge()` wraps eq-shell migration 0186's `eq_site_advisory_flag_pair` RPC. Console: for exact 2-row Sites dupe groups, a "Flag for merge" button (survivor auto-picked: active + has-customer wins); 3+ row groups (the SY9 shape) explicitly left to manual review. Routes back to the Health tab on success, where the existing Same/Different/Unsure → Preview → Confirm flow (0183/0185) takes over unchanged. `pnpm -r build` + `check:packages` green, 4 new unit tests. Vendored into eq-shell PR #876.

## 2026-07-15
- **PR #70 (MERGED, squash) — `previewSiteMerge` / `executeSiteMerge` client wrappers.** The last piece of the site-resolver learning loop's client layer: thin RPC wrappers over eq-shell migration 0185's `eq_site_merge_preview`/`eq_site_merge_execute`, following the same pattern as `readSiteAdvisory`/`adjudicateSiteAdvisory` (0179/0183). Exported from `@eq/intake`'s public API. 6 new unit tests (RPC param mapping, missing-field defaults, RPC-error passthrough); full monorepo `pnpm -r build` + `pnpm check:packages` green. Console wiring (Preview/Confirm buttons, manager-only gate) landed in the eq-shell PR that vendors this in (#868).
- **AI site-duplicate adjudicator confirmed genuinely live end-to-end (PR #69, `eq-ai-assist` v4).** `ANTHROPIC_API_KEY` had been reported "still not set" on ehow (sks-canonical) as recently as yesterday by a live curl test — Royce set the Edge Function secret this session (no redeploy needed, `Deno.env.get` reads it at request time). Re-tested live via curl against the deployed `adjudicate_duplicate` action: 3/3 correct, high confidence — SVHN vs St Vincents Health Network → same (recognised the acronym); Level 2 Office vs Loading Dock at one street address → different (the "same address, different tenancy" trap); SY6 vs SY7 in one suburb → different (distinct data centres, not a shared code). This is the first confirmed-real call any of the three `eq-ai-assist` actions (`suggest_gaps`, `ask_canonical`, `adjudicate_duplicate`) has made since PR #41 shipped 2026-06-24 — all three were silently dead on missing-key errors the entire time. Vendored the console's "Ask Claude" button into eq-shell separately (see eq-shell changelog, PR #867).

## 2026-07-13
- **PR #66 (MERGED `0442f14`) — duplicate detector was blind to inactive rows (the SY9 silent-failure).** The health-home "Scan for possible duplicates" filtered out `active=false` rows before clustering, hiding the SY9 shape (four `app_data.sites` rows for one site, only the retired one holding the correct `customer_id` → the customer silently dropped from the site-driven `service.customers` view). Now includes inactive rows, adds a site-`code` match signal, suggests a survivor with `survivor_confidence:'low'` when the choice is contested (SY9: two rows carried a customer link, the active one wrong), and emits a `needs_reconcile` count the badge leads with. First-ever tests for the detector incl. an SY9 regression fixture. The actual merge/retire action + dependent-record counts (~30-table sweep) deferred to Core (eq-shell#781). Vendored into eq-shell (PR #819) and deployed live to core.eq.solutions (Netlify `6a54c3c3`), so the Intake dashboard there now surfaces SY9-type clusters.
- **Write-time site resolver shipped (migration 0179, eq-shell PR #825, applied fleet-wide via the One Pipe).** A fail-safe `BEFORE INSERT` trigger on `app_data.sites` runs `eq_resolve_site()` on every new-site write and logs match/ambiguous to `app_data.site_resolution_advisory` — universal (catches the `eq_upsert_site` RPC, the `service.sites` view trigger, and direct service_role writes, since it sits on the base table below all of them), EXCEPTION-wrapped so it never blocks a write, includes inactive rows as candidates (the SY9 fix), and won't call a code-exact hit "high" confidence when names disagree (the SY6/SY7 shared-code trap). `pg_trgm`/code indexes keep the match indexed. Advisory-only — records what it would decide, merges/blocks nothing; enforcing phase deferred pending real advisory evidence. The existing SY3–SY7 + SY1/2 duplicate stubs were reconciled on ehow in the same session.
- **`ANTHROPIC_API_KEY` added to CI** (Royce, via repo secrets) — the integration test now makes a real Anthropic call in CI instead of skipping. Verified by re-running the existing workflow and reading the log: `Mobile -> phone (conf=0.90)`, `Type -> employment_type (conf=0.70)`. Both CI gaps from PR #64 are now closed with real, not theoretical, coverage.

## 2026-07-12
- **Repo gets its first CI** (PR #64, merged `e62009c`) — was completely uncovered (zero GitHub Actions workflows, confirmed via API). New workflow: install → build → typecheck + schema-lint → test, across all 7 `eq-platform/` packages. Caught and fixed a real ordering bug on the way in (typecheck must run after build, since `eq-validation` resolves `@eq/ai` against its built types) and a `pnpm`/`setup-node` cache-path friction point (dropped the cache rather than debug it). `pnpm ci:drift` and `pnpm test:integration` deliberately left out of the gate — see PR #64 for why.
- **Both gaps closed** (PR #65, merged `f6f41e3`) — `ci:drift`'s "fails on a clean checkout" finding turned out to be contaminated-environment noise, not real; re-verified clean on a genuine fresh clone and wired up as a real gate. `test:integration` already self-skips without `ANTHROPIC_API_KEY`, so it now runs unconditionally (free today, real coverage once the secret's added). Both confirmed on real GitHub Actions logs.

## 2026-07-03
- **Guardian go-live executed on ehow** — 058/059/060 applied, Edge Function deployed (first ever; now v3), Vault-keyed nightly cron live at 03:00 AEST. First alerts ever persisted: critical LVR expired 268d (Huon Henne) + LVR 29d and electrical_licence 25d warnings. PR #59 (cron hour), PR #60 (auth by privilege probe — injected env key ≠ dashboard key on ehow), PR #61 (health-score columns: sites/contacts no longer false 0%).
- Review-queue tab SHIPPED: eq-intake PR #55 merged; eq-shell port PR #606 merged (Royce, 05:08Z) and deployed to core.eq.solutions (05:13Z). sql/062 (queue RPCs + whitelist extension) applied to ehow and verified — eq_queue_list returns 137 under tenant JWT.
- PR #58 (open, not merged): stamps `checksum='eq-intake-lineage'` on the 058–062 ledger self-inserts + adds `sql/README.md` convention doc. The live ehow rows (058/059/060/062) are already backfilled to this checksum (verified — zero NULL-checksum rows ≥2026-07-03 on either plane); merging #58 just lands the convention in the repo so future self-inserts don't regress it. (#58 subsequently merged, 2026-07-03.)
- Steward run 001 executed on ehow: DAMA-UK composite score rebuild (PR #54 — dimension re-weighting incl. a low-sample confidence flag on entity cards); migration 057 applied (queue table, 137 rows populated); 19 adversarially-adjudicated fixes committed live (13 staff trades, 5 contact formats, 1 link), all intake-stamped.
- Licence strip fail-open fixed (PR #56): "All N licences current" was rendering over expired licences because severity counters only incremented after a successful `eq_quality_upsert_alert` call, and that RPC was never executable by `authenticated` on ehow (053 shipped no GRANT — zero alerts ever written). Counters now come from the licence data itself; new `alerts_failed` field; sql/058 adds caller-tenant guard + grant (merged, NOT yet applied to ehow).
- quality-guardian fixed end-to-end (PR #57): service-role tenant-context RPCs (sql/059), nightly cron registration via Vault (sql/060), Edge Function rewritten — explicit-tenant reads, fixed tenant listing + run bookkeeping, exact service-role-key auth check. Merged, NOT yet applied/deployed to ehow — production go pending.
- **Health/Tidy dashboard field-name + row-identity fixes, and Tidy-tab inline Edit/Suggest (PR #62, vendored via eq-shell PR #623).** `EntityDrillDown.tsx`'s `GAP_FIELDS`/`DISPLAY_COLUMNS`/`DUPE_KEYS` referenced columns that don't exist on the real `app_data` tables (`site_name` vs `name`, `address` vs `address_line_1`, `asset_name` vs `name`; contacts/customers `full_name`/`phone` derived) — which is why every one of 241 sites showed a blank Site Name though none was actually missing (verified live on ehow). `tidy-pass.ts` also matched the wrong validation kind (`required_field_missing` vs the emitted `field_required`), mis-badging every required-field gap as "Invalid format" and printing the raw internal code; added `describeValidationError()`/`describeFlag()`/`fieldLabel()` for plain-English messages. Row-identity bug fixed alongside: dedup, inline-edit targeting, Suggest-accept and the Table React key all matched on `row.id`, but no `app_data` table has an `id` column (they're `<entity>_id`) — a per-entity PK map (`rowKey()`) replaced it, having been silently dropping every duplicate group after the first and risking mis-targeted edits. The Tidy tab also gained inline Edit + AI-assisted Suggest (previously fixable only via a full Download→Reconcile CSV round-trip) plus a scoped "Download N gaps as CSV". New `test/tidy-pass.test.ts` exercises the real validator.

## 2026-07-02
- Fixed health-score dashboard bug: entities with zero rows (e.g. Assets before first record) were scoring 100% complete, inflating the composite score. Added `started` flag; empty entities excluded from dimension averages and shown as "No records yet" instead of a false 100%. PR #53.

## 2026-07-14
- adjudicateSiteAdvisory() writer + verdict types + adjudicable SiteAdvisoryPanel (Same/Different/Unsure); 9 tests (#68). Backing DB layer = migration 0183 (eq-shell PR #848, applied fleet-wide on eq + sks + favour-perfect, live-verified): append-only `app_data.site_resolution_verdict` (RLS-on, tenant-isolated) + `eq_site_advisory_adjudicate()` write RPC (authenticated-only, rejects cross-tenant rows) + verdict-joined advisory summary with pending/decided counts. Captures the human's Same/Different/Unsure call as an append-only label (fuel for a future match model); merges nothing.

## 2026-06-30
- ARMADA fleet-automation trialled on the repo (PR #45 pre-bake + 4 clean manual cycles) — vendored the fleet skills/scripts + `.armada/config.json` + fleet labels (skills local-only, gitignored). Mirrors the eq-service pre-bake.
- **PR #48 (#47) — fixed eq-confirm-ui tests resolving `@eq/intake` against a stale/gitignored `dist`.** Added a vitest `resolve.alias` to `@eq/*` source (green from a fresh checkout, no build) + an `augment()` dependency-surface guard, and fixed 2 masked type errors in `FlaggedRowsTable.tsx` (the `ai_enrichment`/`duplicate` flag kinds). 58 tests pass.
- **PR #50 (#49) — added a `check:packages` script as the fleet gate** (the old `pnpm -C eq-platform check` ran the whole workspace and went red on stale `apps/*`).
- **PR #51 (#46) — removed the abandoned PHASE-0 monorepo apps** (`apps/eq-service` + `apps/eq-shell`, 675 files) + dropped `apps/*` from `pnpm-workspace.yaml` and pruned the lockfile. Real apps ship from their own standalone repos; full-workspace `check` is green again.
- **PR #52 — doc-rot follow-through** (superseded banner on the PHASE-0 migration doc; fixed a stale `apps/eq-shell/.env.local` ref in `EQ-TENANCY-MODEL.md`).
